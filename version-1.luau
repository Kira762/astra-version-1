-- [[++++++++ WAX BUNDLED DATA BELOW ++++++++]]

-- [[Generated by scripts/generate_bundle.js from the modular source tree.]]
-- [[Do not edit by hand; source files remain the source of truth.]]

-- [[Will be used later for getting flattened globals]]
local ImportGlobals

-- [[Holds direct closure data (defining this before the DOM tree for line debugging etc)]]
local ClosureBindings = {
    [2] = function()local wax,script,require=ImportGlobals(2)local ImportGlobals return (function(...)
local Core = require(script.Parent.core)
local State = require(script.Parent.core.state)
local Image = require(script.Parent.images.image)
local Locale = require(script.Parent.utilities.locale)
local Constants = require(script.Parent.utilities.constants)
local Icons = require(script.Parent.icons)
local Motion = require(script.Parent.utilities.motion)
local Settings = require(script.Parent.settings)

export type Theme = types.Theme
export type Translator = types.Translator
export type Translations = types.Translations
export type WindowConfiguration = types.WindowConfiguration

export type WindowProps = types.WindowProps
export type TabProps = types.TabProps
export type SectionProps = types.SectionProps
export type GroupProps = types.GroupProps
export type CollapsibleGroupProps = types.CollapsibleGroupProps
export type CollapsibleElementProps = types.CollapsibleElementProps
export type ButtonProps = types.ButtonProps
export type ToggleProps = types.ToggleProps
export type SliderProps = types.SliderProps
export type DropdownProps = types.DropdownProps
export type InputProps = types.InputProps
export type StatProps = types.StatProps
export type TextProps = types.TextProps
export type DividerProps = types.DividerProps
export type NotifyProps = types.NotifyProps
export type PopupBox = types.PopupBox
export type PopupOption = types.PopupOption
export type PopupProps = types.PopupProps

export type Moveable = types.Moveable
export type Lockable = types.Lockable
export type Window = types.Window
export type Tab = types.Tab
export type Group = types.Group
export type CollapsibleGroup = types.CollapsibleGroup
export type Button = types.Button
export type Toggle = types.Toggle
export type Slider = types.Slider
export type Dropdown = types.Dropdown
export type Input = types.Input
export type Stat = types.Stat
export type Section = types.Section
export type TabSection = types.TabSection
export type Text = types.Text
export type Divider = types.Divider
export type Popup = types.Popup
export type Astra = types.Astra

type WindowModule = { new: (types.WindowProps) -> types.Window }

local Astra = {} :: Astra
local activeWindow: types.Window? = nil
local ACTIVE_WINDOW_KEY = "__ASTRA_ACTIVE_WINDOW_V1"

-- [[How long a font download waits for the entrance before it triggers its own
-- full theme pass. It is a cooldown between *parts* of startup, not extra
-- work: a font swap walks every instance the window owns, so running it while
-- the window is still arriving would put that pass on the frames the entrance
-- needs. Bounded, because a host that never shows the window must still end
-- up with its font. There is deliberately no reveal gate beside it: the
-- window shows on the next frame and the remaining constructors stream in
-- behind it in small budget-limited batches.]]
local FONT_SETTLE_BUDGET = 3

local function getGlobalStore(): { [string]: any }
	local globalEnv = _G :: any
	local ok, genvResult = pcall(function()
		return (getgenv :: any)()
	end)
	return if ok and type(genvResult) == "table" then genvResult else globalEnv
end

local globalStore = getGlobalStore()

Astra.Icons = Icons

-- [[The motion service is public: hosts can animate with the library's own
-- specs (`Astra.Motion.tween(frame, props, "snappy")`) and steer the whole
-- interface's timing (`setProfile`, `setTimeScale`, `setEnabled`).]]
Astra.Motion = Motion

Astra.Core = Core
Astra.Settings = Settings

function Astra:CreateWindow(props: types.WindowProps): types.Window
	local existingWindow = activeWindow or globalStore[ACTIVE_WINDOW_KEY]
	activeWindow = existingWindow
	if existingWindow then
		local existingAny = existingWindow :: any
		if existingAny.unloaded then
			activeWindow = nil
			if globalStore[ACTIVE_WINDOW_KEY] == existingWindow then
				globalStore[ACTIVE_WINDOW_KEY] = nil
			end
		else
			local antiDuplicateEnabled = Settings.readPersisted("antiWindowDuplicate", true)
			if antiDuplicateEnabled and (not existingAny.settings or existingAny.settings.antiWindowDuplicate ~= false) then
				existingAny:Unload()
				activeWindow = nil
				if globalStore[ACTIVE_WINDOW_KEY] == existingWindow then
					globalStore[ACTIVE_WINDOW_KEY] = nil
				end
			end
		end
	end

	local ok, result = pcall(function()
		return (require(script.Parent.components.window) :: WindowModule).new(props)
	end)
	if not ok then
		error(result, 0)
	end

	local window = result :: types.Window
	activeWindow = window
	globalStore[ACTIVE_WINDOW_KEY] = window
	-- [[One secure-mode branch: the optional-icon preload and the brand-font
	-- swap are independent, so they run as sibling threads rather than in two
	-- separately-guarded blocks.]]
	if State.secureMode then
		task.defer(function()
			Image.preload(function(failedCount, failedRoles)
				if failedCount <= 0 or window.unloaded then
					return
				end
				local failedLabel = if #failedRoles > 0
					then table.concat(failedRoles, ", ")
					else "optional window icon"
				window:Notify({
					title = Locale.resolve("Optional icon warning"),
					content = Locale.resolve("Failed to load: ") .. failedLabel,
				})
			end)
		end)
		task.spawn(function()
			local mediumFont = State.fontManager:loadFont(Constants.fontAsset, Enum.FontWeight.Medium)
			local semiBoldFont = State.fontManager:loadFont(Constants.fontAsset, Enum.FontWeight.SemiBold)
			if
				not window.unloaded
				and mediumFont
				and semiBoldFont
				and mediumFont ~= State.fallbackFont
			and semiBoldFont ~= State.fallbackFont
		then
			-- [[A font swap is a theme pass over every instance the window
			-- owns, so running it while the window is still arriving puts that
			-- pass on the frames the entrance needs. It waits for the window to
			-- land instead — bounded, because a host that never shows the
			-- window must still end up with its font.]]
			local waited = 0
			while
				waited < FONT_SETTLE_BUDGET
				and not window.unloaded
				and (not window.hasShownOnce or window.animating)
			do
				waited += task.wait()
			end
			if not window.unloaded then
				window:ChangeTheme({ Font = mediumFont, TitleFont = semiBoldFont })
			end
		end
	end)
	end

	if not window.unloaded then
		-- Reveal on the next frame instead of on a fixed one-second deadline.
		-- The old gate waited out the interval even for an empty window, which
		-- is the 1s of the reported startup lag that every script paid. The
		-- checkpoints below keep pacing the remaining constructors in small
		-- budget-limited batches until the build goes quiet, so the opening
		-- tween keeps receiving frames while late controls stream in behind
		-- the already-visible shell.
		task.defer(function()
			-- Still yield once so the caller can add its first tab before the
			-- shell appears.
			task.wait()

			if window.unloaded then
				return
			end
			if window._autoShowCancelled then
				window._startupComplete = true
				return
			end
			if window.hidden then
				window:Show()
			end

			-- Keep pacing post-reveal constructors until two consecutive frames
			-- observe no construction activity.
			local stableFrames = 0
			while not window.unloaded and not window._autoShowCancelled and stableFrames < 2 do
				local revision = window._constructionRevision
				task.wait()
				if (window._constructionBusy or 0) == 0 and revision == window._constructionRevision then
					stableFrames += 1
				else
					stableFrames = 0
				end
			end

			if window.unloaded or window._autoShowCancelled then
				return
			end
			window:Show()

			-- [[Auto-load persisted control state once the host's constructors
			-- [[have gone quiet: that quiet point is the de-facto "startup
			-- [[finished" signal (the constructor itself returns before the
			-- [[host has added any tab). The load applies over the controls in
			-- [[small checkpoints, and controls registered even later are
			-- [[still covered by Window:_restoreLate. Runs once per window; a
			-- [[missing file or autoLoad turned off simply skips it.]]
			if window.configuration and window.configuration.autoLoad and not window._autoLoadDone then
				window._autoLoadDone = true
				window:Load()
			end
		end)
	end

	return window
end

return Astra

end)() end,
    [3] = function()local wax,script,require=ImportGlobals(3)local ImportGlobals return (function(...)
export type Theme = string | { [string]: any }

export type Translator = (source: string, localeId: string) -> string?

export type Translations = { [string]: { [string]: string } }
export type IconPack = "lucide" | "material" | "tabler" | "phosphor" | "heroicons" | "feather" | "remix"

export type ChangelogChange = {
    symbol: string?,
    text: string?,
    category: string?,
}

export type ChangelogEntry = {
    version: string?,
    date: string?,
    game: string?,
    gameId: string?,
    title: string?,
    changes: { ChangelogChange }?,
}

export type ChangelogProps = {
    name: string?,
    emptyText: string?,
    entries: { ChangelogEntry }?,
}

export type WindowConfiguration = {
    autoSave: boolean?,
    autoLoad: boolean?,
    fileName: string?,
    customFolder: string?,
}

export type WindowProps = {
    name: string?,
    subtitle: string?,
    theme: Theme?,
    icon: (string | number)?,
    showName: string?,
    showIcon: (string | number)?,
    showIconOnly: boolean?,     profile: string?,     configuration: WindowConfiguration?,
    fallbackFont: (Font | Enum.Font)?,     locale: string?,     translations: Translations?,     translator: Translator?, }

export type TabProps = {
    name: string?,
    icon: (string | number)?,
}

export type SectionProps = {
    name: string?,
    icon: (string | number)?,
}

export type TextProps = {
    name: string?,     text: string?,     icon: (string | number)?,
}

export type DividerProps = {
    text: string?,     spacing: number?,     line: boolean?, }

export type GroupProps = {
    direction: string?, }

-- Definitions use the corresponding element's normal properties plus `type`.
-- `elements` is allowed only for ordinary Groups; CollapsibleGroups cannot nest.
export type CollapsibleElementProps = {
    type: "Button" | "Toggle" | "Switch" | "Slider" | "Dropdown" | "Input"
        | "Stat" | "Section" | "Text" | "Changelog" | "Divider" | "Group",
    elements: { CollapsibleElementProps }?,
    [string]: any,
}

export type CollapsibleGroupProps = {
    name: string?,
    icon: (string | number)?,
    elements: { CollapsibleElementProps }?,
}

export type ButtonProps = {
    name: string?,
    icon: (string | number)?,
    -- Built-in right-edge tap glyph: false hides it, a name/asset id replaces it.
    tapIcon: (string | number | boolean)?,
    callback: (() -> ())?,
}

export type ToggleProps = {
    name: string?,
    icon: (string | number)?,
    flag: string?,
    value: boolean?,
    forgetState: boolean?,
    callback: ((value: boolean) -> ())?,
}

export type SliderProps = {
    name: string?,
    icon: (string | number)?,
    flag: string?,
    range: { number }?,     increment: number?,
    value: number?,
    suffix: string?,
    minimal: boolean?,     forgetState: boolean?,
        callback: ((value: number, dragging: boolean) -> ())?,
}

export type DropdownProps = {
    name: string?,
    icon: (string | number)?,
    flag: string?,
    options: { string }?,
    value: (string | { string })?,
    multiSelect: boolean?,
    placeholder: string?,
    forgetState: boolean?,
    callback: ((value: any) -> ())?,
}

export type InputProps = {
    name: string?,
    icon: (string | number)?,
    flag: string?,
    value: string?,
    placeholder: string?,
    numeric: boolean?,
    clearOnFocus: boolean?,
    forgetState: boolean?,
    callback: ((value: string) -> ())?,
}

export type StatProps = {
    name: string?,
    icon: (string | number)?,
    prefix: string?,
    suffix: string?,
    value: (number | string)?,
    display: string?,     compact: boolean?,
    -- Show the value as a single letter badge (no icon) instead of the numeric
    -- readouts; text values use this automatically unless set to false, in
    -- which case the whole value reads out as a text readout (no odometer, no
    -- change readout) that Set / SetText / ResetBaseline all write to.
    letter: boolean?,
    changeMode: string?,     changeBaseline: string?,     numberEasing: boolean?,
}

export type NotifyProps = {
    title: string?,
    content: string?,
    icon: (string | number)?,
    duration: number?,
}

export type PopupBox = {
    title: string?,
    description: string?,
    icon: (string | number)?,
}

export type PopupOption = {
    text: string?,
    style: string?,     callback: (() -> ())?,
}

export type PopupProps = {
    title: string?,
    subtitle: string?,
    icon: (string | number)?,
    content: string?,     boxes: { PopupBox }?,     options: { PopupOption }?,     dismissable: boolean?, }

export type Moveable = {
    MoveTo: (self: any, index: number) -> (),
    MoveToTop: (self: any) -> (),
    MoveToBottom: (self: any) -> (),
    MoveUp: (self: any) -> (),
    MoveDown: (self: any) -> (),
}

export type Lockable = {
    Lock: (self: any, reason: string?) -> (),
    Unlock: (self: any) -> (),
    IsLocked: (self: any) -> boolean,
}

export type Button = Moveable & Lockable & {}

export type Toggle = Moveable & Lockable & {
    value: boolean,
    Set: (self: Toggle, value: boolean, skipCallback: boolean?) -> (),
}

export type Slider = Moveable & Lockable & {
    value: number,
    Set: (self: Slider, value: number, skipCallback: boolean?) -> (),
}

export type Dropdown = Moveable & Lockable & {
    value: { string },     Set: (self: Dropdown, value: string | { string }, skipCallback: boolean?) -> (),
    Refresh: (self: Dropdown, options: { string }) -> (),
    Add: (self: Dropdown, option: string) -> (),
    Remove: (self: Dropdown, option: string) -> (),
}

export type Input = Moveable & Lockable & {
    value: string,
    Set: (self: Input, value: string, skipCallback: boolean?) -> (),
}

export type Stat = Moveable & {
    value: number,
    Set: (self: Stat, value: number) -> (),
    ResetBaseline: (self: Stat, value: number?) -> (),
}

export type Section = Moveable & {}

export type TabSection = {
    Remove: (self: TabSection) -> (),
}

export type Text = Moveable & {
    name: string,     text: string,     Set: (self: Text, text: string) -> (),
    SetTitle: (self: Text, title: string) -> (),
}

export type Changelog = Moveable & {
    Set: (self: Changelog, entries: { ChangelogEntry }) -> (),
    Refresh: (self: Changelog, entries: { ChangelogEntry }) -> (),
    Add: (self: Changelog, entry: ChangelogEntry, prepend: boolean?) -> (),
    Clear: (self: Changelog) -> (),
}

export type Divider = Moveable & {
    text: string,     Set: (self: Divider, text: string?) -> (),
}

export type Popup = {
    Close: (self: Popup) -> (),
}

export type Group = Moveable & {
    CreateButton: (self: Group, props: ButtonProps) -> Button,
    CreateToggle: (self: Group, props: ToggleProps) -> Toggle,
    CreateStat: (self: Group, props: StatProps) -> Stat,
    CreateSlider: (self: Group, props: SliderProps) -> Slider,
        CreateDropdown: (self: Group, props: DropdownProps) -> Dropdown?,
    CreateSection: (self: Group, props: SectionProps) -> Section?,
    CreateText: (self: Group, props: TextProps) -> Text?,
    CreateDivider: (self: Group, props: DividerProps?) -> Divider?,
    CreateGroup: (self: Group, props: GroupProps?) -> Group,
}

export type CollapsibleGroup = Moveable & Lockable & {
    elements: { any },
}

export type Tab = {
    CreateCollapsibleGroup: (self: Tab, props: CollapsibleGroupProps) -> CollapsibleGroup,
    Select: (self: Tab, noAnimation: boolean?) -> (),
    Deselect: (self: Tab, noAnimation: boolean?) -> (),
    Remove: (self: Tab) -> (),
    CreateButton: (self: Tab, props: ButtonProps) -> Button,
    CreateToggle: (self: Tab, props: ToggleProps) -> Toggle,
    CreateSlider: (self: Tab, props: SliderProps) -> Slider,
    CreateDropdown: (self: Tab, props: DropdownProps) -> Dropdown,
    CreateInput: (self: Tab, props: InputProps) -> Input,
    CreateStat: (self: Tab, props: StatProps) -> Stat,
    CreateSection: (self: Tab, props: SectionProps) -> Section,
    CreateText: (self: Tab, props: TextProps) -> Text,
    CreateChangelog: (self: Tab, props: ChangelogProps) -> Changelog,
    CreateDivider: (self: Tab, props: DividerProps?) -> Divider,
    CreateGroup: (self: Tab, props: GroupProps?) -> Group,
}

export type Window = {
    unloaded: boolean,
    ResolveIcon: (self: Window, value: (string | number)?, pack: IconPack?) -> (string | number)?,
    Flags: { [string]: any },
    CreateTab: (self: Window, props: TabProps) -> Tab,
        CreateSection: (self: Window, props: SectionProps) -> TabSection,
    Notify: (self: Window, props: NotifyProps) -> (),
    Popup: (self: Window, props: PopupProps) -> Popup?,     Show: (self: Window) -> (),
    Hide: (self: Window) -> (),
    ToggleHide: (self: Window) -> (),
    ToggleMinimise: (self: Window) -> (),
    Navigate: (self: Window, tab: string | Tab) -> (),
    ChangeTheme: (self: Window, theme: Theme) -> (),
    SetLocale: (self: Window, localeId: string) -> (),
    SetTranslator: (self: Window, translator: Translator?) -> (),
    RegisterTranslations: (self: Window, translations: Translations) -> (),
    Save: (self: Window, name: string?) -> boolean,
    Load: (self: Window, name: string?) -> boolean,
    ListConfigs: (self: Window) -> { string },
    DeleteConfig: (self: Window, name: string) -> boolean,
    GetPath: (self: Window) -> (string, string),
    Get: (self: Window, flag: string) -> any,
    Set: (self: Window, flag: string, value: any) -> boolean,
    Unload: (self: Window) -> (),
}

export type IconCatalog = {
    Lucide: IconPack,
    Material: IconPack,
    Tabler: IconPack,
    Phosphor: IconPack,
    Heroicons: IconPack,
    Feather: IconPack,
    Remix: IconPack,
    -- [[`name` may be a bare icon name (searched in every pack, in priority]]
    -- [[order) or a pack-qualified `"pack:name"`; `pack` addresses one pack.]]
    get: (name: string, pack: IconPack?) -> string?,
    getByPack: (pack: IconPack, name: string) -> string?,
    list: (pack: IconPack?) -> { string },
    packs: () -> { string },
    count: (pack: IconPack?) -> number,
    isPack: (pack: string?) -> boolean,
    priority: () -> { string },
    loaded: () -> { string },
    refreshCustom: () -> number,
    resolve: (value: (string | number)?, pack: IconPack?) -> (string | number)?,
}

export type MotionSpec = string | TweenInfo

-- [[The service is a plain table of functions (dot calls, like `Icons`),
-- [[so the fields carry no `self` - `Astra.Motion.tween(frame, props)`.
export type MotionService = {
    enabled: boolean,
    timeScale: number,
    profile: string,
    profiles: { [string]: { timeScale: number?, enabled: boolean? } },
    specNames: { string },
    spec: (spec: MotionSpec?) -> TweenInfo,
    step: (base: number) -> number,
    setTimeScale: (scale: number) -> number,
    getTimeScale: () -> number,
    setEnabled: (enabled: boolean) -> boolean,
    isEnabled: () -> boolean,
    setProfile: (name: string) -> boolean,
    getProfile: () -> string,
    cancel: (instance: Instance?) -> boolean,
    tween: (instance: Instance?, props: { [string]: any }, spec: MotionSpec?, onCompleted: (() -> ())?) -> Tween?,
    set: (instance: Instance?, props: { [string]: any }) -> (),
}

export type Astra = {
    CreateWindow: (self: Astra, props: WindowProps) -> Window,
    Icons: IconCatalog,
    Motion: MotionService,
}

return {}

end)() end,
    [8] = function()local wax,script,require=ImportGlobals(8)local ImportGlobals return (function(...)
local state = require(script.state)
local registry = require(script.registry)
local loader = require(script.loader)

local core = {
    state = state,
    registry = registry,
    loader = loader,
}

return core

end)() end,
    [5] = function()local wax,script,require=ImportGlobals(5)local ImportGlobals return (function(...)
local loader = {}

local loading: { [string]: boolean } = {}

local knownModules = {
	state = true,
	registry = true,
}

function loader.load(name: string)
	assert(knownModules[name], "Astra core loader: unknown module '" .. tostring(name) .. "'")
	if loading[name] then
		return nil
	end
	loading[name] = true
	local ok, result = pcall(function()
		return require((script :: any).Parent[name])
	end)
	loading[name] = nil
	if not ok then
		error(result, 0)
	end
	return result
end

function loader.service(name: string, factory: () -> any)
	local reg = loader.load("registry")
	reg.registerFactory(name, factory)
	return reg
end

return loader

end)() end,
    [6] = function()local wax,script,require=ImportGlobals(6)local ImportGlobals return (function(...)
local registry = {}

local modules: { [string]: any } = {}
local factories: { [string]: () -> any } = {}

function registry.register(name: string, module: any)
	modules[name] = module
end

function registry.registerFactory(name: string, factory: () -> any)
	factories[name] = factory
end

function registry.get(name: string): any?
	local cached = modules[name]
	if cached ~= nil then
		return cached
	end
	local factory = factories[name]
	if factory then
		local instance = factory()
		modules[name] = instance
		return instance
	end
	return nil
end

function registry.names(): { string }
	local list = {}
	for name in modules do
		table.insert(list, name)
	end
	for name in factories do
		if modules[name] == nil then
			table.insert(list, name)
		end
	end
	table.sort(list)
	return list
end

return registry

end)() end,
    [7] = function()local wax,script,require=ImportGlobals(7)local ImportGlobals return (function(...)
local services = require(script.Parent.Parent.utilities.services)
local constants = require(script.Parent.Parent.utilities.constants)
local log = require(script.Parent.Parent.utilities.log)
local fileSystemManager = require(script.Parent.Parent.utilities.filesystemManager)
local assetResolver = require(script.Parent.Parent.utilities.assetResolver)
local fontManager = require(script.Parent.Parent.utilities.fontManager)

type FileSystemManager = fileSystemManager.FileSystemManager
type AssetResolver = assetResolver.AssetResolver
type FontManager = fontManager.FontManager

export type VariablesState = {
	secureMode: boolean,
	coreGui: CoreGui,
	workspace: Workspace,
	runService: RunService,
	userInputService: UserInputService,
	guiService: GuiService,
	localPlayer: Player?,
	tweenService: TweenService,
	httpService: HttpService,
	textService: TextService,
	replicatedStorage: ReplicatedStorage,
	localizationService: LocalizationService,
	guiContainer: Instance,

	fallbackFont: Font,
	fileSystemManager: FileSystemManager,
	assetResolver: AssetResolver,
	fontManager: FontManager,
	setFallbackFont: (font: Enum.Font | Font) -> (),
	brandFont: (weight: Enum.FontWeight?) -> Font,
}

local state = {} :: VariablesState

state.secureMode = (function()
	if typeof(getgenv) ~= "function" then
		return false
	end
	local ok, result = pcall(function()
		return getgenv().ASTRA_SECURE
	end)
	return ok and result == true
end)()

state.coreGui = services.getService("CoreGui") :: CoreGui
state.workspace = services.getService("Workspace") :: Workspace
state.runService = services.getService("RunService") :: RunService
state.userInputService = services.getService("UserInputService") :: UserInputService
state.guiService = services.getService("GuiService") :: GuiService
state.localPlayer = (services.getService("Players") :: Players).LocalPlayer
state.tweenService = services.getService("TweenService") :: TweenService
state.httpService = services.getService("HttpService") :: HttpService
state.textService = services.getService("TextService") :: TextService
state.replicatedStorage = services.getService("ReplicatedStorage") :: ReplicatedStorage
state.localizationService = services.getService("LocalizationService") :: LocalizationService

state.guiContainer = (function(): Instance
	if state.runService:IsStudio() then
		local player = state.localPlayer
		if player then
			return player.PlayerGui
		end
		return state.coreGui
	end
	if typeof(gethui) == "function" then
		local ok, hui = pcall(gethui)
		if ok and hui then
			return hui
		end
	end
	return state.coreGui
end)()

log.setSecureModeSource(function()
	return state.secureMode
end)

state.fallbackFont = Font.fromEnum(Enum.Font.BuilderSans)

state.fileSystemManager = fileSystemManager.new()
state.assetResolver =
	assetResolver.new(true, assetResolver.Enum.AssetDownloadUrl.RoProxyDownloadUrl) :: AssetResolver
state.fontManager = fontManager.new(
	state.fileSystemManager:getAssetsFolder("fonts"),
	false,
	true,
	assetResolver.Enum.AssetDownloadUrl.RoProxyDownloadUrl,
	{
		saveToDisk = true,
		skipCache = false,
		fallbackFont = state.fallbackFont,
	}
) :: FontManager

function state.setFallbackFont(font: Enum.Font | Font)
	if typeof(font) == "EnumItem" then
		font = Font.fromEnum(font)
	end
	if typeof(font) == "Font" then
		state.fallbackFont = font
		state.fontManager.defaultOptions.fallbackFont = font
	end
end

function state.brandFont(weight: Enum.FontWeight?): Font
	if state.secureMode then
		return Font.new(state.fallbackFont.Family, weight)
	end
	return Font.new(constants.fontAsset, weight)
end

return state

end)() end,
    [9] = function()local wax,script,require=ImportGlobals(9)local ImportGlobals return (function(...)
local Action = {}
Action.__index = Action
Action.__type = "Action"

local utilities = script.Parent.Parent.utilities

local state = require(utilities.Parent.core.state)
local log = require(utilities.log)
local haptic = require(utilities.HapticEngine)
local motion = require(utilities.motion)

function Action.new(window, props)
    props = if typeof(props) == "table" then props else {}

    local self = setmetatable({
        window = assert(window, "Missing argument #1 (Window expected)"),
        name = props.name or props.Name or "Action",
        icon = assert(window:ResolveIcon(props.icon or props.Icon), "Missing argument (Icon expected)"),
        callback = assert(props.callback or props.Callback, "Missing argument (Function expected)"),
        linkedTab = props.linkedTab or props.LinkedTab,
    }, Action)

    self.action = self.window:Create("Frame", {
        Name = self.name,
        BorderSizePixel = 0,
        LayoutOrder = -(props.order or 0),
        Size = UDim2.fromOffset(24, 24),
        BackgroundTransparency = 1,
        Parent = self.window.actionContainer,
    })

    self.iconLabel = self.window:Create("ImageLabel", {
        Image = self.icon,
        Size = UDim2.fromOffset(20, 20),
        BorderSizePixel = 0,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.fromScale(0.5, 0.5),
        BackgroundTransparency = 1,
        ImageTransparency = 1,
        Parent = self.action,
    }, { ImageColor3 = "ActionColor" })

    self.interact = self.window:Create("TextButton", {
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        Position = UDim2.fromScale(0.5, 0.5),
        AnchorPoint = Vector2.new(0.5, 0.5),
        TextTransparency = 1,
        Parent = self.action,
    })

    -- [[Optional unread dot: a fixed red badge pinned to the]]
    -- [[action's top-right corner. Created after the]]
    -- [[interact button so it renders above it; frames sink no input.]]
    if props.badge or props.Badge then
        self.badge = self.window:Create("Frame", {
            Name = "Badge",
            AnchorPoint = Vector2.new(1, 0),
            Position = UDim2.new(1, 1, 0, -1),
            Size = UDim2.fromOffset(8, 8),
            BorderSizePixel = 0,
            BackgroundColor3 = Color3.fromRGB(239, 68, 68),
            BackgroundTransparency = 1,
            Visible = false,
            Parent = self.action,
        })
        self.window:Create("UICorner", {
            CornerRadius = UDim.new(1, 0),
            Parent = self.badge,
        })
    end

    local function idleTransparency()
        if self.window.unloaded then
            return
        end
        if not self.window:_settled() then
            return
        end
        if self.linkedTab and self.window.selectedTab == self.linkedTab then
            return
        end
        if self.isLit and self:isLit() then
            return
        end
        motion.tween(self.iconLabel, { ImageTransparency = 0.6 }, "snappy")
    end

    self.window:Connect(self.interact.MouseButton1Click, function()
        haptic.click()
        task.spawn(function()
            local ok, err = pcall(self.callback)
            if not ok then
                log.warn(
                    `Astra encountered an error, with the callback for a {self.__type} component named '{self.name}':`
                )
                log.print(err)
            end
            idleTransparency()
        end)
    end)

    self.window:Connect(self.interact.MouseEnter, function()
        if not self.window:_interactive() then
            return
        end
        motion.tween(self.iconLabel, { ImageTransparency = 0.2 }, "snappy")
    end)

    self.window:Connect(self.interact.MouseLeave, idleTransparency)

    return self
end

function Action:SetBadge(shown, animate)
    if not self.badge or self.window.unloaded then
        return
    end
    self.badge.Visible = shown == true
    local target = if shown then 0 else 1
    if animate == false or not self.window:_interactive() then
        self.badge.BackgroundTransparency = target
        return
    end
    motion.tween(self.badge, { BackgroundTransparency = target }, "snappy")
end


return Action

end)() end,
    [10] = function()local wax,script,require=ImportGlobals(10)local ImportGlobals return (function(...)
local utilities = script.Parent.Parent.utilities
local state = require(utilities.Parent.core.state)
local motion = require(utilities.motion)
local fs = require(utilities.filesystem)
local constants = require(utilities.constants)
local locale = require(utilities.locale)
local haptic = require(utilities.HapticEngine)

local chrome = {}

local dragThreshold = 5

function chrome.buildCollapsedFace(window)
    local iconOnly = window.showIconOnly

    window.collapsedIcon = window:Create("ImageLabel", {
        Name = "CollapsedIcon",
        AnchorPoint = if iconOnly then Vector2.new(0.5, 0.5) else Vector2.new(0, 0.5),
        Position = if iconOnly then UDim2.fromScale(0.5, 0.5) else UDim2.new(0, 16, 0.5, 0),
        Size = UDim2.fromOffset(24, 24),
        BackgroundTransparency = 1,
        Image = window.showIcon,
        ZIndex = constants.zIndex.restoreContent,
        ImageTransparency = 1,
        Parent = window.main,
    }, { ImageColor3 = "TitlingColor" })

    -- [[Gen2: PillCornerRadius drives tab pills and the collapsed capsule icon.]]
    window:Create("UICorner", {
        Parent = window.collapsedIcon,
    }, { CornerRadius = "PillCornerRadius" })

    local collapsedTextFrame = window:Create("Frame", {
        Name = "CollapsedText",
        Visible = not iconOnly,
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0, 50, 0.5, 0),
        Size = UDim2.new(1, -60, 0, 32),
        BackgroundTransparency = 1,
        ZIndex = constants.zIndex.restoreContent,
        Parent = window.main,
    })

    window:Create("UIListLayout", {
        Padding = UDim.new(0, 1),
        VerticalAlignment = Enum.VerticalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = collapsedTextFrame,
    })

    window.collapsedTitle = window:Create("TextLabel", {
        Name = "Title",
        Text = window.showName,
        Size = UDim2.new(1, 0, 0, 16),
        BackgroundTransparency = 1,
        FontFace = state.brandFont(Enum.FontWeight.Medium),
        RichText = true,
        TextSize = 16,
        TextWrapped = true,
        TextXAlignment = Enum.TextXAlignment.Left,
        LayoutOrder = 1,
        ZIndex = constants.zIndex.restoreContent,
        TextTransparency = 1,
        Parent = collapsedTextFrame,
    }, { TextColor3 = "TitlingColor" })

    window.collapsedSubtitle = window:Create("TextLabel", {
        Name = "Subtitle",
        Text = locale.t("Tap to show"),
        Size = UDim2.new(1, 0, 0, 14),
        BackgroundTransparency = 1,
        FontFace = state.brandFont(Enum.FontWeight.Medium),
        TextSize = 14,
        TextXAlignment = Enum.TextXAlignment.Left,
        LayoutOrder = 2,
        ZIndex = constants.zIndex.restoreContent,
        TextTransparency = 1,
        Parent = collapsedTextFrame,
    }, { TextColor3 = "TitlingColor" })

    window.collapsedInteract = window:Create("TextButton", {
        Name = "CollapsedInteract",
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        Text = "",
        TextTransparency = 1,
        Visible = false,
        ZIndex = constants.zIndex.restoreInteract,
        Parent = window.main,
    })

    chrome.bindCollapsedDrag(window)
end

function chrome.bindCollapsedDrag(window)
    local inputService = state.userInputService
    local isDragging, hasMoved = false, false
    local dragOffset, startMousePos = Vector2.zero, Vector2.zero
    local renderConnection

    local function getGuiInset()
        if window.screenGui and window.screenGui.IgnoreGuiInset then
            return state.guiService:GetGuiInset()
        end
        return Vector2.zero
    end

    local function cleanupDrag()
        isDragging = false
        if renderConnection then
            window:Disconnect(renderConnection)
            renderConnection = nil
        end
    end

    local function updateDrag()
        if not isDragging then
            cleanupDrag()
            return
        end
        if window.unloaded or not window.hidden or window.animating then
            cleanupDrag()
            return
        end

        local mousePos = inputService:GetMouseLocation()
        if not hasMoved and (mousePos - startMousePos).Magnitude < dragThreshold then
            return
        end
        if not (window.settings and window.settings.dragMinimisedBar == true) then
            hasMoved = true
            cleanupDrag()
            return
        end
        hasMoved = true

        local newPos = mousePos + dragOffset + getGuiInset()
        window.main.Position = UDim2.fromOffset(newPos.X, newPos.Y)
    end

    window:Connect(window.collapsedInteract.InputBegan, function(input, gameProcessed)
        if gameProcessed or not window.hidden or window.animating then
            return
        end
        local inputType = input.UserInputType.Name
        if inputType ~= "MouseButton1" and inputType ~= "Touch" then
            return
        end

        isDragging, hasMoved = true, false
        if renderConnection then
            window:Disconnect(renderConnection)
            renderConnection = nil
        end
        renderConnection = window:Connect(state.runService.RenderStepped, updateDrag)
        startMousePos = inputService:GetMouseLocation()
        dragOffset = window.main.AbsolutePosition + window.main.AbsoluteSize * window.main.AnchorPoint - startMousePos
    end)

    window:Connect(inputService.InputEnded, function(input)
        local inputType = input.UserInputType.Name
        if inputType ~= "MouseButton1" and inputType ~= "Touch" then
            return
        end
        if not isDragging then
            return
        end
        cleanupDrag()

        if hasMoved then
            window._collapsedPosition = window.main.Position
            return
        end

        haptic.click()
        window:ToggleHide()
    end)

    window:Connect(inputService.WindowFocusReleased, function()
        cleanupDrag()
    end)
end

function chrome.isNewUser()
    local player = state.localPlayer
    if not player then
        return false
    end

    if typeof(fs.isfile) ~= "function" or typeof(fs.writefile) ~= "function" then
        return true
    end

    local path = state.fileSystemManager:getPath("profile.txt")
    local userId = tostring(player.UserId)
    local isNew = true

    pcall(function()
        if fs.isfile(path) then
            isNew = fs.readfile(path) ~= userId
        end
    end)

    pcall(function()
        fs.writefile(path, userId)
    end)

    return isNew
end

-- [[The capsule's face (icon, title, subtitle) fades in with `spec` — a
-- motion-service spec name (the fold plays "settle"), so the collapse answers
-- the speed setting like everything else — or applies instantly when no spec
-- is given (the silent state-sync path).]]
function chrome.setCollapsedShown(window, shown, spec)
    local targets = {
        [window.collapsedIcon] = { ImageTransparency = if shown then 0 else 1 },
    }

    if not window.showIconOnly then
        targets[window.collapsedTitle] = { TextTransparency = if shown then 0 else 1 }
        targets[window.collapsedSubtitle] = { TextTransparency = if shown then 0.5 else 1 }
    end

    for instance, properties in targets do
        if spec then
            motion.tween(instance, properties, spec)
        else
            for prop, value in properties do
                instance[prop] = value
            end
        end
    end
end

return chrome

end)() end,
    [11] = function()local wax,script,require=ImportGlobals(11)local ImportGlobals return (function(...)
local Drag = {}
Drag.__index = Drag
Drag.__type = "Drag"

local utilities = script.Parent.Parent.utilities

local state = require(utilities.Parent.core.state)
local constants = require(utilities.constants)
local motion = require(utilities.motion)

-- [[The handle's look, written once. The window's settle paths and this
-- [[module's own hover/drag states all land on these four specs, so the pill
-- [[can never be left at a size or a transparency nothing owns:
--
-- [[  * `hover`  — the pointer is on the handle;
-- [[  * `grab`   — the handle is what is being dragged (the window follows);
-- [[  * `idle`   — the faint hint it rests at once the window has been moved:
-- [[               48px wide, 3px tall, centred under the window. This is the
-- [[               pill in the reference screenshot — it shows up on a move
-- [[               and stays as a "you can grab me" affordance until the
-- [[               window leaves the screen;
-- [[  * `parked` — off the screen: the pill shrinks to nothing instead of
-- [[               being left mid-hover for the next entrance to inherit.
local handleHover = { Size = UDim2.fromOffset(64, 3), BackgroundTransparency = 0.5 }
local handleGrab = { Size = UDim2.fromOffset(56, 3), BackgroundTransparency = 0 }
local handleIdle = { Size = UDim2.fromOffset(48, 3), BackgroundTransparency = 0.7 }
local handleParked = { Size = UDim2.fromOffset(0, 3), BackgroundTransparency = 1 }

-- [[How long the pill takes to shrink away before the hitbox that carries it
-- [[may be switched off without cutting the fade short: the `fast` spec the
-- [[fade itself rides (0.16s) plus a frame.
local handleFadeTime = 0.18

-- [[The handoff between the window and this module, named so the two ends
-- [[cannot drift: `Window:_syncDragBar` and every position this file writes
-- [[place the handle's centre this far below the window's bottom edge.
local handleGap = 22

-- [[Where the pill comes to rest: the hover look when the pointer is already
-- [[on the handle, the idle hint otherwise. Both the end of the handle's own
-- [[drag and the end of a topbar move settle through here.
local function restingLook(self)
    return if self.hovering then handleHover else handleIdle
end

function Drag.new(window, props)
    props = if typeof(props) == "table" then props else {}

    local self = setmetatable({
        window = assert(window, "Missing argument #1 (Window expected)"),
    }, Drag)

    -- Small centered detached handle below window, not overlapping
    -- Gap = 22px from window bottom edge to handle center
    --
    -- [[The hitbox is deliberately wider than the pill it carries (80px vs the
    -- [[48px idle pill): a 3px-tall white line is a hard thing to point at, so
    -- [[the region that answers the pointer is generous and the pill grows
    -- [[into it.
    --
    -- [[It starts off. A hidden parent takes MouseEnter/MouseLeave with it, so
    -- [[the window owns the moment the handle becomes reachable: `Drag:enable`
    -- [[from each of its settle paths (_firstShow, _quickRestore, the minimise
    -- [[settle), `Drag:disable` / `Drag:fadeOut` from Hide, Close and the fold.
    -- [[Without that the reveal below could never fire in the expanded window —
    -- [[the handle was left off by ten writes and turned on by one.
    self.drag = self.window:Create("Frame", {
        BackgroundTransparency = 1,
        Size = UDim2.fromOffset(80, 16),
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, self.window.size.Y.Offset / 2 + handleGap),
        ZIndex = constants.zIndex.drag,
        Visible = false,
        Parent = self.window.screenGui,
    })

    self.dragCosmetic = self.window:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.fromScale(0.5, 0.5),
        ZIndex = constants.zIndex.drag,
        BackgroundTransparency = handleParked.BackgroundTransparency,
        Size = handleParked.Size,
        Parent = self.drag,
    })

    self.window:Create("UICorner", {
        CornerRadius = UDim.new(0, 20),
        Parent = self.dragCosmetic,
    })

    self.window:CreateGlow(self.dragCosmetic, Color3.fromRGB(255, 255, 255), 8, 0.6)

    self.dragInteract = self.window:Create("TextButton", {
        Active = true,
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        TextTransparency = 1,
        ZIndex = constants.zIndex.drag,
        Parent = self.drag,
    })

    -- [[Handle state. `dragging` is the handle itself being dragged (the
    -- [[window follows it), `moving` the window being moved by its topbar
    -- [[(the pill brightens for that, and the hover reveal must not fight it)
    -- [[and `hovering` the pointer resting on the handle — what `restingLook`
    -- [[reads when a move ends.
    self.dragging = false
    self.moving = false
    self.hovering = false

    -- [[Bumped by every transition that takes the handle over. A `fadeOut`
    -- [[waiting to switch the hitbox off stands down when a later entrance
    -- [[(or a move) has claimed the handle in the meantime.
    self._handleToken = 0

    local dragOffset = nil

    local guiInset = Vector2.zero
    local screenGui = self.window.screenGui

    if screenGui and screenGui.IgnoreGuiInset then
        guiInset = state.guiService:GetGuiInset()
    end

    local function getMousePosition()
        local mouseLocation = state.userInputService and state.userInputService:GetMouseLocation()
            or Vector2.new(0, 0)
        local offset = dragOffset or Vector2.new(0, 0)
        local insetVal = guiInset or Vector2.new(0, 0)

        return mouseLocation + offset + insetVal
    end

    local function constrainPosition()
        local position = getMousePosition()
        local x, y = position.X, position.Y

        if self.window.settings and self.window.settings.keepOnScreen then
            local mainSize = self.window.main.AbsoluteSize
            local screenSize = self.window.screenGui.AbsoluteSize
            local margin = 8
            local halfWidth, halfHeight = mainSize.X / 2, mainSize.Y / 2
            x = math.clamp(x, halfWidth + margin, math.max(halfWidth + margin, screenSize.X - halfWidth - margin))
            y = math.clamp(y, halfHeight + margin, math.max(halfHeight + margin, screenSize.Y - halfHeight - margin))
        end

        local mainPos = UDim2.fromOffset(x, y)
        local dragPos = UDim2.fromOffset(x, y + (self.window.main.Size.Y.Offset / 2 + handleGap))
        return mainPos, dragPos
    end

    -- [[Hover reveal. `hovering` is recorded even while a move is running —
    -- [[it is where the pill has to settle when that move ends — but the tween
    -- [[stands down then: a window being dragged under the pointer would
    -- [[otherwise flicker the pill between the hover and idle looks as its own
    -- [[movement sweeps the handle past the cursor.
    self.window:Connect(self.drag.MouseEnter, function()
        self.hovering = true
        if self.dragging or self.moving or self.window.hidden then
            return
        end
        motion.tween(self.dragCosmetic, handleHover, "fast")
    end)

    self.window:Connect(self.drag.MouseLeave, function()
        self.hovering = false
        if self.dragging or self.moving or self.window.hidden then
            return
        end
        motion.tween(self.dragCosmetic, handleIdle, "fast")
    end)

    local function endDrag()
        if not self.dragging then
            return
        end
        self.dragging = false

        if self._dragConnection then
            self.window:Disconnect(self._dragConnection)
            self._dragConnection = nil
        end

        if not self.window:_canDragCapsule() then
            return
        end

        motion.tween(self.dragCosmetic, restingLook(self), "settle")

        -- [[The capsule settling back after a drag is the spring's moment:
        -- a short overshoot that reads as weight, not lag.]]
        local mainPos, dragPos = constrainPosition()
        motion.tween(self.window.main, { Position = mainPos }, "spring")
        motion.tween(self.drag, { Position = dragPos }, "spring")
    end

    local lerpSmoothing = 1e-7
    local smoothVelocity = 1e-60
    local renderStepHandler

    self.window:Connect(self.dragInteract.InputBegan, function(input, gameProcessed)
        if gameProcessed then
            return
        end

        if not self.window:_canDragCapsule() then
            return
        end

        local inputType = input.UserInputType.Name
        if inputType == "MouseButton1" or inputType == "Touch" then
            if not self.window:_interactive() then
                return
            end

            self.dragging = true

            if self._dragConnection then
                self.window:Disconnect(self._dragConnection)
                self._dragConnection = nil
            end

            if screenGui and screenGui.IgnoreGuiInset then
                guiInset = state.guiService:GetGuiInset()
            end

            dragOffset = self.window.main.AbsolutePosition
                + self.window.main.AbsoluteSize * self.window.main.AnchorPoint
                - state.userInputService:GetMouseLocation()
            if not self.window.hidden then
                motion.tween(self.dragCosmetic, handleGrab, "fast")
            end

            self._dragConnection = self.window:Connect(state.runService.RenderStepped, renderStepHandler)
        end
    end)

    self.window:Connect(state.userInputService.InputEnded, function(input)
        local inputType = input.UserInputType.Name
        if inputType == "MouseButton1" or inputType == "Touch" then
            endDrag()
        end
    end)

    self.window:Connect(state.userInputService.WindowFocusReleased, endDrag)

    renderStepHandler = function(dt)
        if not self.dragging then
            if self._dragConnection then
                self.window:Disconnect(self._dragConnection)
                self._dragConnection = nil
            end
            return
        end

        if self.window.unloaded or not self.window:_canDragCapsule() then
            endDrag()
            return
        end

        local mainPos, dragPos = constrainPosition()

        self.window.main.Position = self.window.main.Position:Lerp(mainPos, 1 - lerpSmoothing ^ dt)
        self.drag.Position = self.drag.Position:Lerp(dragPos, 1 - smoothVelocity ^ dt)
    end

    return self
end

-- [[The handle's lifecycle, driven by the window — the only thing that knows
-- [[whether the window is on screen, settled, or on its way out.
--
-- [[`enable` is called from every settle path (Window:_firstShow,
-- [[_quickRestore, the minimise settle): the hitbox turns on so the pointer
-- [[can find the handle, and the pill keeps whatever look its own states left
-- [[it at — a handle that has just helped move the window stays faintly
-- [[visible (that is the reveal the user is looking for), while one that has
-- [[never been touched waits unseen instead of announcing itself under every
-- [[window that opens.
function Drag:enable()
    self._handleToken += 1
    self.drag.Visible = true
end

-- [[Off the screen at once — a close, or the entrance a Show starts with: the
-- [[hitbox goes and the pill is parked at nothing, so the next entrance starts
-- [[from an unseen handle rather than at whatever look the last hover left
-- [[behind.
function Drag:disable()
    self._handleToken += 1
    self.drag.Visible = false
    motion.tween(self.dragCosmetic, handleParked, "fast")
end

-- [[Off the screen with the fade read first, for the exits that fold or fade
-- [[the window away: the pill shrinks away now and the hitbox follows once the
-- [[fade is over, so the handle never cuts out from under the pointer. The
-- [[token makes a pending hide stand down when a later entrance (or a move)
-- [[has claimed the handle in the meantime.
function Drag:fadeOut(spec: string?)
    self._handleToken += 1
    local token = self._handleToken
    local window = self.window

    motion.tween(self.dragCosmetic, handleParked, spec or "fast")
    task.delay(handleFadeTime, function()
        if token ~= self._handleToken or window.unloaded then
            return
        end
        self.drag.Visible = false
    end)
end

-- [[The window is being moved by its topbar (or by a drag started while it is
-- [[minimised): the pill brightens for the length of the move and settles back
-- [[to its resting look when the move ends. This is the one moment the handle
-- [[has to show itself without being pointed at — the drag began on the
-- [[topbar, so no MouseEnter is coming. It also re-arms the hitbox, so a
-- [[handle that some earlier exit switched off cannot swallow the move.
function Drag:setMoving(active: boolean)
    active = active == true
    if self.moving == active then
        return
    end
    self.moving = active

    if active then
        self._handleToken += 1
        self.drag.Visible = true
        motion.tween(self.dragCosmetic, handleHover, "fast")
        return
    end

    if self.window.hidden then
        -- [[The window left the screen mid-move (a hide, a close): the pill
        -- [[belongs to that exit now and stays parked.
        return
    end
    motion.tween(self.dragCosmetic, restingLook(self), "settle")
end

return Drag

end)() end,
    [12] = function()local wax,script,require=ImportGlobals(12)local ImportGlobals return (function(...)
local Notification = {}
Notification.__index = Notification
Notification.__type = "Notification"

local utilities = script.Parent.Parent.utilities

local state = require(utilities.Parent.core.state)
local functions = require(script.Parent.Parent.functions)
local constants = require(utilities.constants)
local haptic = require(utilities.HapticEngine)
local motion = require(utilities.motion)

-- [[The card's entrance rides the motion service's shared vocabulary: the
-- shell slides in on `emphasized`, surfaces fade on `normal`, copy on
-- `snappy`, and the whole card accelerates out on `exit` — quicker than the
-- entrance that made it, so the stack never feels crowded by its own past.]]
local showTween, fadeInTween, textFadeTween = "emphasized", "normal", "snappy"
local dismissTween = "exit"

local bodyHiddenPosition = UDim2.new(0.5, 360, 0.5, 0)
local bodyShownPosition = UDim2.new(0.5, 0, 0.5, 0)

local maxVisible = 6

local padding = 8

local function autoDuration(content)
    return math.clamp(#content * 0.06 + 3, 3, 9)
end

function Notification.new(window, props, release)
    props = if typeof(props) == "table" then props else {}

    local self = setmetatable({
        window = assert(window, "Missing argument #1 (Window expected)"),
        title = props.title or props.Title or "Notification",
        content = props.content or props.Content or "",
        icon = window:ResolveIcon(props.icon or props.Icon),
        _hovered = false,
        _dismissed = false,
        -- [[Handed over by the entrance queue when this card was queued: the
        -- next overlay does not start until this one releases the slot, which
        -- is what turns the burst a host fires at load time into a cascade. Nil
        -- for a card built directly, which then owns its own timing.]]
        _release = release,
    }, Notification)

    self.duration = props.duration or props.Duration or autoDuration(self.content)

    local hasIcon = self.icon ~= nil and self.icon ~= 0 and self.icon ~= ""

    self.main = self.window:Create("Frame", {
        Name = "Notification",
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 0, 0),
        BorderSizePixel = 0,
        ZIndex = constants.zIndex.notification,
        Parent = self.window.notifications,
    })

    self.window:Create("UIPadding", {
        PaddingTop = UDim.new(0, padding),
        Parent = self.main,
    })

    self.body = self.window:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        Size = UDim2.new(1, 0, 1, 0),
        Position = bodyHiddenPosition,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Active = true,
        BorderSizePixel = 0,
        ZIndex = constants.zIndex.notification,
        BackgroundTransparency = 1,
        Parent = self.main,
    })

    self.window:Create("UIGradient", {
        Rotation = 270,
        Offset = Vector2.new(0, -0.1),
        Parent = self.body,
    }, { Color = { "WindowColor", functions.toColorSequence } })

    self.window:Create("UICorner", {
        Parent = self.body,
    }, { CornerRadius = "CornerRoundness" })

    self.stroke = self.window:Create("UIStroke", {
        Transparency = 1,
        Parent = self.body,
    }, { Color = "SurfaceStroke" })

    self.shadow = self.window:CreateGlow(self.body, "ShadowColor", 20, 1)

    self.window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, 20),
        PaddingRight = UDim.new(0, 20),
        Parent = self.body,
    })

    self.window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, 14),
        Parent = self.body,
    })

    if hasIcon then
        self.iconLabel = self.window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(24, 24),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            LayoutOrder = 1,
            ZIndex = constants.zIndex.notification,
            ImageTransparency = 1,
            Parent = self.body,
        }, { ImageColor3 = "ContentColor" })
    end

    self.container = self.window:Create("Frame", {
        Size = UDim2.fromOffset(hasIcon and 222 or 260, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        LayoutOrder = 2,
        ZIndex = constants.zIndex.notification,
        Parent = self.body,
    })

    self.window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Vertical,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, 4),
        Parent = self.container,
    })

    self.titleLabel = self.window:Create("TextLabel", {
        Text = self.title,
        Size = UDim2.new(1, 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundTransparency = 1,
        TextSize = 16,
        TextWrapped = true,
        TextXAlignment = Enum.TextXAlignment.Left,
        LayoutOrder = 1,
        ZIndex = constants.zIndex.notification,
        TextTransparency = 1,
        Parent = self.container,
    }, { TextColor3 = "ContentColor", FontFace = "TitleFont" })

    if self.content ~= "" then
        self.descriptionLabel = self.window:Create("TextLabel", {
            Text = self.content,
            Size = UDim2.new(1, 0, 0, 0),
            AutomaticSize = Enum.AutomaticSize.Y,
            BackgroundTransparency = 1,
            TextSize = 15,
            TextWrapped = true,
            TextXAlignment = Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Top,
            LayoutOrder = 2,
            ZIndex = constants.zIndex.notification,
            TextTransparency = 1,
            Parent = self.container,
        }, { TextColor3 = "ContentColor", FontFace = "Font" })
    end

    self.window._notificationCount = (self.window._notificationCount or 0) + 1
    self.main.LayoutOrder = self.window._notificationCount

    local liveNotifications = self.window._liveNotifications
    if not liveNotifications then
        liveNotifications = {}
        self.window._liveNotifications = liveNotifications
    end
    table.insert(liveNotifications, self)
    while #liveNotifications > maxVisible do
        local oldest = table.remove(liveNotifications, 1)
        if oldest and oldest ~= self then
            task.spawn(oldest._dismiss, oldest)
        end
    end

    self._connections = {
        self.window:Connect(self.body.MouseEnter, function()
            self._hovered = true
        end),
        self.window:Connect(self.body.MouseLeave, function()
            self._hovered = false
        end),
        self.window:Connect(self.body.InputBegan, function(input)
            if
                input.UserInputType == Enum.UserInputType.MouseButton1
                or input.UserInputType == Enum.UserInputType.Touch
            then
                self:_dismiss()
            end
        end),
    }

    task.spawn(function()
        self:_show()
    end)

    return self
end

function Notification:_measure()
    local contentWidth = self.iconLabel and 222 or 260

    local textHeight = functions.textHeight(self.window.theme.TitleFont, 16, self.title, contentWidth)
    if self.descriptionLabel then
        textHeight = textHeight + 4 + functions.textHeight(self.window.theme.Font, 15, self.content, contentWidth)
    end

    return math.max(textHeight, self.iconLabel and 24 or 0) + 28
end

-- [[Hand the entrance slot back to the queue. Fires when the last piece of the
-- entrance has been committed, and also on any early exit: a card that is
-- dismissed before it opens (the visibility cap, a click, an unload) must not
-- leave the next one waiting for a tween that will never finish. Idempotent,
-- because the show path and the dismiss path can both reach it.]]
function Notification:_entranceDone(early)
    local release = self._release
    if not release then
        return
    end
    self._release = nil
    -- [[`early` comes from a dismissal/retirement mid-entrance: the queue uses
    -- [[it to skip its full entrance cadence (nothing is on screen to keep
    -- [[from overlapping). The natural end of the staggered reveal passes
    -- [[nothing, so the next card waits behind this one's transition.]]
    release(early == true)
end

function Notification:_show()
    local totalHeight = self:_measure() + padding
    if self._dismissed or not self.main.Parent then
        self:_entranceDone(true)
        return
    end

    haptic.notify()

    motion.tween(self.main, { Size = UDim2.new(1, 0, 0, totalHeight) }, showTween)
    motion.tween(self.body, { Position = bodyShownPosition, BackgroundTransparency = 0 }, showTween)
    motion.tween(self.titleLabel, { TextTransparency = 0 }, textFadeTween)
    motion.tween(self.stroke, { Transparency = 0.95 }, fadeInTween)
    motion.tween(self.shadow, { Transparency = 0.6 }, textFadeTween)

    -- [[The stagger beats answer the motion profile: with animation switched
    -- [[off they cost no time and the queue drains a card per frame.]]
    local firstBeat = motion.step(0.05)
    if firstBeat > 0 then
        task.wait(firstBeat)
    end
    if self._dismissed or not self.main.Parent then
        self:_entranceDone(true)
        return
    end
    if self.iconLabel then
        motion.tween(self.iconLabel, { ImageTransparency = 0 }, textFadeTween)
    end

    local secondBeat = motion.step(0.05)
    if secondBeat > 0 then
        task.wait(secondBeat)
    end
    if self._dismissed or not self.main.Parent then
        self:_entranceDone(true)
        return
    end
    if self.descriptionLabel then
        motion.tween(self.descriptionLabel, { TextTransparency = 0.35 }, textFadeTween)
    end

    -- [[The card is fully on screen from here: everything that is still
    -- running is its hold time, not its entrance, so the next one may start.
    -- The queue itself holds the lane for the entrance cadence; a dismissal
    -- (below) releases early, which the queue reads as "skip the cadence".]]
    self:_entranceDone()

    local elapsed = 0
    while elapsed < self.duration and not self._dismissed and self.main.Parent do
        local dt = task.wait()
        if not self._hovered then
            elapsed += dt
        end
    end

    self:_dismiss()
end

function Notification:_dismiss()
    if self._dismissed then
        return
    end
    self._dismissed = true

    -- [[A card retired before its entrance finished still owes the queue its
    -- slot; `_entranceDone` is a no-op once the show path has handed it back.
    -- Early release tells the queue to skip the full entrance cadence.]]
    self:_entranceDone(true)

    local liveNotifications = self.window._liveNotifications
    local index = liveNotifications and table.find(liveNotifications, self)
    if liveNotifications and index then
        table.remove(liveNotifications, index)
    end

    if not self.main.Parent then
        return
    end

    motion.tween(self.body, { BackgroundTransparency = 1, Size = UDim2.new(1, -90, 1, 0) }, dismissTween)
    motion.tween(self.stroke, { Transparency = 1 }, dismissTween)
    motion.tween(self.shadow, { Transparency = 1 }, dismissTween)
    motion.tween(self.titleLabel, { TextTransparency = 1 }, dismissTween)
    if self.descriptionLabel then
        motion.tween(self.descriptionLabel, { TextTransparency = 1 }, dismissTween)
    end
    if self.iconLabel then
        motion.tween(self.iconLabel, { ImageTransparency = 1 }, dismissTween)
    end

    local collapseTween = state.tweenService:Create(self.main, motion.spec(dismissTween), { Size = UDim2.new(1, 0, 0, 0) })
    local connection
    connection = collapseTween.Completed:Connect(function(playbackState)
        if connection then
            connection:Disconnect()
            connection = nil
        end
        if playbackState ~= Enum.PlaybackState.Completed or not self._dismissed or not self.main.Parent then
            return
        end

        for _, conn in self._connections do
            self.window:Disconnect(conn)
        end
        self.window:DestroySubtree(self.main)
    end)
    collapseTween:Play()
end

return Notification

end)() end,
    [13] = function()local wax,script,require=ImportGlobals(13)local ImportGlobals return (function(...)
-- [[Astra overlay entrance queue.
--
-- Notifications are the cheapest thing in the interface to look at
-- and the most expensive thing to build on a cold start: a host that fires
-- twenty of them while it boots — one per module it loaded — builds twenty
-- cards on the same frame the window's own entrance lands, and every card is
-- a dozen instances plus its full set of entrance tweens. The frame spikes,
-- the entrance stutters, and the whole interface pops in at once.
--
-- The queue turns that burst into a cascade:
--   * an overlay is *built* on its own turn rather than on the frame it was
--     asked for, so the construction cost is spread over frames instead of
--     stacked onto one;
--   * only one entrance is in flight at a time, and the next turn starts
--     after a cooldown, so the cards arrive one at a time;
--   * the gate holds every turn while the window is busy with its own
--     entrance, so the window arrives first and the overlays follow it;
--   * a backlog is bounded: past `maxQueued` pending requests the oldest one
--     that has not been built yet gives up its slot, the same way
--     `maxVisible` retires cards that are already on screen.
--
-- The queue owns no timing of its own beyond the cooldown: an overlay keeps
-- its entrance and simply calls `release()` when the last piece of it is
-- committed, which is what hands the slot to the next card. The cooldown
-- answers to the motion service, so the "Instant" speed profile (or animation
-- switched off) drains the queue one overlay per frame instead of one per
-- cooldown — ordered, but never slow. The gate is polled a frame at a time
-- rather than slept through, because the thing it waits for is another
-- coroutine's completion, not a duration.
-- ]]
local utilities = script.Parent.Parent.utilities

local motion = require(utilities.motion)
local log = require(utilities.log)

local OverlayQueue = {}

-- [[Cooldown between two overlays landing, and the shorter one used while a
-- backlog is still waiting so a long queue drains instead of crawling. Both
-- are beats for the *next* card to start on: the entrance itself belongs to
-- the overlay.
-- [[]]
-- `entranceCadence` protects the first card to finish its staggered reveal:
-- the card hands its slot back ~0.1s in (its later pieces are just tweens),
-- so without this the next card's 0.6s entrance tween started on top of the
-- first one - the overlap the single lane exists to prevent. It applies once
-- per pump, to the first natural hand-off; a deep backlog then drains with
-- the short `backlogGap`, and a card dismissed mid-entrance releases early and
-- skips it (there is no transition left on screen to protect).
local entranceGap = 0.18
local entranceCadence = 0.45
local backlogGap = 0.06
local backlogSize = 3

-- [[Safety budgets, in seconds of waiting rather than in frames: an entrance
-- that never reports back (a card dismissed mid-flight by the visibility cap,
-- say) must not strand the queue behind it, and a gate nobody opens must not
-- strand it either.]]
local entranceBudget = 1.2
local gateBudget = 2.5

-- [[Pending requests kept during a burst. Anything past this is dropped
-- before it is ever built, which is the whole point.]]
local maxQueued = 6

-- [[The queue table lives on the window, but the window is allowed to hold a
-- placeholder for it: `Window.new` pre-seeds `_overlayQueue` so `Window:Unload`
-- always has something to clear, and a host that assigns its own table lands
-- here too. A placeholder is not a queue — it has no `pending` list to pump —
-- so the shape is completed rather than trusted. Every entry point into the
-- queue comes through here, which is what lets the rest of the module read
-- `queue.pending` unconditionally.]]
local function queueFor(window): { [string]: any }
    local queue = window._overlayQueue
    if type(queue) ~= "table" or type(queue.pending) ~= "table" then
        local previous = if type(queue) == "table" then queue else nil
        queue = {
            pending = {},
            running = false,
            paused = false,
            closed = false,
        }
        -- [[A placeholder is the queue's *initial* state: a window paused
        -- before its first request has to stay paused through the upgrade, or
        -- the entrance gate would open on the very thing that made it wait.]]
        if previous then
            queue.running = previous.running == true
            queue.paused = previous.paused == true
            queue.closed = previous.closed == true
        end
        window._overlayQueue = queue
    end
    return queue
end

-- [[Suspend the queue. Called when the window starts a transition that should
-- read as the main event — its own entrance — and paired with
-- `OverlayQueue.resume` when that transition is over.]]
function OverlayQueue.pause(window)
    local queue = queueFor(window)
    queue.paused = true
end

-- [[Open the gate. A window that never queued anything has a placeholder or no
-- queue at all, so there is nothing to wake: read the shape through
-- `queueFor` and the `paused` test below does the rest of the work.]]
function OverlayQueue.resume(window)
    local queue = queueFor(window)
    if not queue.paused then
        return
    end
    queue.paused = false
    -- [[A pump that gave up on a gate nobody opened (gateBudget) leaves the
    -- backlog parked until the next request, so wake it here too.]]
    if not queue.running and #queue.pending > 0 and not window.unloaded then
        OverlayQueue.pump(window, queue)
    end
end

-- [[Give up the slot for a cooldown. A wait expressed in seconds rather than
-- `task.wait(seconds)`, because a frame that is already over must not be
-- charged against the beat: the queue should never run *ahead* of the clock it
-- is pacing itself against.]]
local function waitSeconds(seconds)
    local waited = 0
    while waited < seconds do
        waited += task.wait()
    end
end

-- [[Drain the pending list one overlay at a time. One pump per window at any
-- moment: a request that arrives while the queue is running is picked up by
-- the loop that is already going.]]
function OverlayQueue.pump(window, queue)
    queue = queue or queueFor(window)
    if queue.running then
        return
    end
    queue.running = true

    task.spawn(function()
        -- [[The window can be unloaded between turns. Unload leaves the queue
        -- [[object in one piece, but older paths (and hosts that assign their
        -- [[own table) can leave it without a `pending` list: retire rather
        -- [[than index nil.]]
        -- [[The first natural hand-off in a pump waits the full entrance
        -- [[cadence so the lead card's 0.6s transition cannot overlap the
        -- [[next one; later turns use the steady (or backlog) gap.]]
        local firstTurn = true
        while type(queue.pending) == "table" and #queue.pending > 0 do
            if window.unloaded or queue.closed then
                if queue.pending then
                    table.clear(queue.pending)
                end
                break
            end

            local entry = table.remove(queue.pending, 1)

            -- [[Wait out the window's own entrance before taking the turn. The
            -- budget keeps a gate nobody opened from parking the queue: worst
            -- case the overlay arrives on its own instead of with the window.]]
            if queue.paused then
                local gateWaited = 0
                while queue.paused and gateWaited < gateBudget and not window.unloaded do
                    gateWaited += task.wait()
                end
            end

            if window.unloaded or queue.closed then
                if queue.pending then
                    table.clear(queue.pending)
                end
                break
            end

            -- [[The overlay reports back when its entrance is committed. A
            -- build that errors out has already reported nothing, so the
            -- budget below moves the queue along regardless. `early` is set
            -- when the card was dismissed/retired mid-entrance, in which case
            -- there is nothing on screen for the next card to avoid
            -- overlapping and the cadence wait is skipped.]]
            local released = false
            local earlyRelease = false
            local function release(early)
                released = true
                earlyRelease = early == true
            end

            local ok, err = pcall(entry.build, release)
            if not ok then
                log.warn("Astra: an overlay failed to open - " .. tostring(err))
                released = true
            end

            local entranceWaited = 0
            while not released and entranceWaited < entranceBudget and not window.unloaded do
                entranceWaited += task.wait()
            end

            if window.unloaded or queue.closed then
                if queue.pending then
                    table.clear(queue.pending)
                end
                break
            end

            -- [[Cooldown before the next card. The first natural hand-off of
            -- [[a pump waits out the entrance cadence so transitions do not
            -- [[overlap; an early dismiss skips it, and a deep backlog drains
            -- [[with the short gap instead of crawling.]]
            local gap
            if earlyRelease then
                gap = entranceGap
            elseif firstTurn then
                gap = entranceCadence
            else
                gap = if #queue.pending > backlogSize then backlogGap else entranceGap
            end
            firstTurn = false
            waitSeconds(motion.step(gap))
        end

        queue.running = false
    end)
end

-- [[Ask for an overlay. `build(release)` constructs it on this overlay's turn —
-- never on the frame the request was made on — and the overlay calls
-- `release()` when its entrance is committed. Returns true when the request was
-- accepted, which is false only for a window that is already gone.]]
function OverlayQueue.request(window, build): boolean
    if window.unloaded then
        return false
    end

    local queue = queueFor(window)
    if queue.closed then
        return false
    end

    -- [[A burst stays bounded: the oldest request that was never built is
    -- dropped for the newest one, which is the one the host still cares about.]]
    while #queue.pending >= maxQueued do
        table.remove(queue.pending, 1)
    end

    table.insert(queue.pending, { build = build })

    -- [[A window whose first entrance was cancelled (Hide before it ever
    -- [[showed) re-parks anything that arrives afterward: Hide opened the
    -- [[gate only to flush what was already waiting, so a cancelled entrance
    -- [[cannot strand a card - new work must still wait for a window that is
    -- [[not on screen. A window that showed once queues normally while
    -- [[hidden.]]
    if window.hidden and not window.hasShownOnce then
        queue.paused = true
    end

    OverlayQueue.pump(window, queue)
    return true
end

-- [[Stops the pump and drops everything that never got built. Called from
-- `Window:Unload`, where the whole overlay layer is going away anyway. A
-- window that never queued an overlay only has the placeholder, which is
-- already empty — `queueFor` gives the flag somewhere to live either way.]]
function OverlayQueue.close(window)
    -- [[A window that never had a queue at all has nothing to stop; a
    -- placeholder only has to be given somewhere to record the flag.]]
    local queue = if window._overlayQueue then queueFor(window) else nil
    if not queue then
        return
    end
    queue.closed = true
    -- [[`Window:Unload` clears the queue table whole afterwards, so *every*
    -- field has to be left at its placeholder value: a caller that reads the
    -- queue after the unload — the entrance coroutines still winding down do —
    -- must find an empty list, never a missing one.]]
    table.clear(queue.pending)
    queue.running = false
    queue.paused = false
end

return OverlayQueue

end)() end,
    [14] = function()local wax,script,require=ImportGlobals(14)local ImportGlobals return (function(...)
local Popup = {}
Popup.__index = Popup
Popup.__type = "Popup"

local utilities = script.Parent.Parent.utilities
local motion = require(utilities.motion)
local state = require(utilities.Parent.core.state)
local functions = require(script.Parent.Parent.functions)
local constants = require(utilities.constants)
local locale = require(utilities.locale)
local log = require(utilities.log)
local haptic = require(utilities.HapticEngine)

-- [[The dialog arrives on the shared vocabulary: the card glides the last
-- few pixels to centre on `smooth` (a large surface decelerating), surfaces
-- fade on `normal`, copy on `snappy`, the backdrop dims quickest of all so
-- focus lands on the card. Closing plays the same paths in reverse on
-- `snappy` — a dialog leaves quicker than it came.]]
local cardShowTween, fadeInTween, textFadeTween, backdropTween = "smooth", "normal", "snappy", "fast"

local cardWidth = 400
local cardPadLeft = 22
local cardPadTop = 24
local cardPadBottom = 22
local sectionGap = 16
local contentInnerWidth = cardWidth - cardPadLeft * 2

local titleFontSize = 18
local subtitleFontSize = 14
local bodyFontSize = 15
local iconSize = 16
local boxIconSize = 22
local maxContentHeight = 300
local contentPadding = 4
local contentInnerWidthPadded = contentInnerWidth - contentPadding * 2

local boxPadLeft = 16
local boxPadTop = 13
local boxIconPad = 20
local boxDescPad = 10
local boxTitleFontSize = 15
local boxDescFontSize = 14
local boxIconSpacing = 8
local boxContentPad = 10
local footerHeight = 40
local footerSpacing = 8
local buttonCorner = UDim.new(1, 0)
local buttonTextSize = 16

local backdropTransparency = 0.5
local popupStack = {}

local lastEscapeInput = nil

local function cleanupStack()
    for i = #popupStack, 1, -1 do
        local popup = popupStack[i]
        if popup._closed or not popup.screenGui.Parent then
            table.remove(popupStack, i)
        end
    end
end

local function isTopmost(popup)
    cleanupStack()
    return popupStack[#popupStack] == popup
end

function Popup.new(window, props)
    props = if typeof(props) == "table" then props else {}

    local self = setmetatable({
        window = assert(window, "Missing argument #1 (Window expected)"),
        title = props.title or props.Title or "Popup",
        subtitle = props.subtitle or props.Subtitle,
        content = props.content or props.Content,
        icon = window:ResolveIcon(props.icon or props.Icon),
        boxes = props.boxes or props.Boxes,
        options = props.options or props.Options,
        dismissable = if props.dismissable ~= nil
            then props.dismissable
            elseif props.Dismissable ~= nil then props.Dismissable
            else true,
        _reveal = {},
        _connections = {},
        _closed = false,
    }, Popup)

    if not self.options or #self.options == 0 then
        self.options = { { text = "Okay" } }
    end

    self:_build()
    cleanupStack()
    table.insert(popupStack, self)
    task.spawn(function()
        self:_show()
    end)

    return self
end

function Popup:_fade(instance, prop, to)
    table.insert(self._reveal, { instance = instance, prop = prop, to = to })
    return instance
end

function Popup:_build()
    local window = self.window
    local hasIcon = self.icon ~= nil and self.icon ~= 0 and self.icon ~= ""

    self.screenGui = window:Create("ScreenGui", {
        Name = state.httpService:GenerateGUID(false),
        IgnoreGuiInset = true,
        ResetOnSpawn = false,
        Enabled = true,
        DisplayOrder = constants.displayOrder.popup,
        ZIndexBehavior = Enum.ZIndexBehavior.Global,
        Parent = state.guiContainer,
    })

    self.backdrop = window:Create("Frame", {
        Name = "Backdrop",
        BackgroundColor3 = Color3.fromRGB(0, 0, 0),
        BorderSizePixel = 0,
        Size = UDim2.fromScale(1, 1),
        Active = true,
        BackgroundTransparency = 1,
        Parent = self.screenGui,
    })
    self:_fade(self.backdrop, "BackgroundTransparency", backdropTransparency)

    self.card = window:Create("Frame", {
        Name = "Card",
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 14),
        Size = UDim2.fromOffset(cardWidth, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        Active = true,
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        Parent = self.screenGui,
    })
    self:_fade(self.card, "BackgroundTransparency", 0)

    window:Create("UIGradient", {
        Rotation = 270,
        Offset = Vector2.new(0, -0.1),
        Parent = self.card,
    }, { Color = { "WindowColor", functions.toColorSequence } })

    window:Create("UICorner", {
        Parent = self.card,
    }, { CornerRadius = "CornerRoundness" })

    self.cardStroke = window:Create("UIStroke", {
        Transparency = 1,
        Parent = self.card,
    }, { Color = "SurfaceStroke" })
    self:_fade(self.cardStroke, "Transparency", 0.95)

    self.cardShadow = window:CreateGlow(self.card, "ShadowColor", 26, 1)
    self:_fade(self.cardShadow, "Transparency", 0.55)

    window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, cardPadLeft),
        PaddingRight = UDim.new(0, cardPadLeft),
        PaddingTop = UDim.new(0, cardPadTop),
        PaddingBottom = UDim.new(0, cardPadBottom),
        Parent = self.card,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Vertical,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, sectionGap),
        Parent = self.card,
    })

    local headerHeight = self:_measureHeader(hasIcon)
    self:_buildHeader(hasIcon, headerHeight)
    local contentHeight = self:_buildContent()
    self:_buildFooter()

    local sectionCount = 2 + (contentHeight > 0 and 1 or 0)
    local totalHeight = cardPadTop
        + headerHeight
        + contentHeight
        + footerHeight
        + cardPadBottom
        + sectionGap * (sectionCount - 1)
    self.card.Size = UDim2.fromOffset(cardWidth, totalHeight)
    self.card.AutomaticSize = Enum.AutomaticSize.None

    if self.dismissable then
        table.insert(
            self._connections,
            window:Connect(self.backdrop.InputBegan, function(input)
                if
                    input.UserInputType == Enum.UserInputType.MouseButton1
                    or input.UserInputType == Enum.UserInputType.Touch
                then
                    self:Close()
                end
            end)
        )
        table.insert(
            self._connections,
            window:Connect(state.userInputService.InputBegan, function(input, gameProcessed)
                if not gameProcessed and input.KeyCode == Enum.KeyCode.Escape and isTopmost(self) then
                    if input == lastEscapeInput then
                        return
                    end
                    lastEscapeInput = input
                    self:Close()
                end
            end)
        )
    end
end

function Popup:_buildHeader(hasIcon, headerHeight)
    local window = self.window

    local headerFrame = window:Create("Frame", {
        Name = "Header",
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 0, headerHeight),
        LayoutOrder = 1,
        Parent = self.card,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, boxIconPad),
        Parent = headerFrame,
    })

    if hasIcon then
        self:_fade(
            window:Create("ImageLabel", {
                Image = self.icon,
                Size = UDim2.fromOffset(iconSize, iconSize),
                BackgroundTransparency = 1,
                BorderSizePixel = 0,
                LayoutOrder = 1,
                ImageTransparency = 1,
                Parent = headerFrame,
            }, { ImageColor3 = "TitlingColor" }),
            "ImageTransparency",
            0
        )
    end

    local textFrame = window:Create("Frame", {
        Name = "Text",
        BackgroundTransparency = 1,
        Size = UDim2.new(1, hasIcon and -(iconSize + boxIconPad) or 0, 0, self._columnH),
        LayoutOrder = 2,
        Parent = headerFrame,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Vertical,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, 3),
        Parent = textFrame,
    })

    self:_fade(
        window:Create("TextLabel", {
            Text = locale.t(self.title),
            Size = UDim2.new(1, 0, 0, self._titleH),
            BackgroundTransparency = 1,
            TextSize = titleFontSize,
            TextXAlignment = Enum.TextXAlignment.Left,
            TextWrapped = true,
            LayoutOrder = 1,
            TextTransparency = 1,
            Parent = textFrame,
        }, { TextColor3 = "TitlingColor", FontFace = "Font" }),
        "TextTransparency",
        0
    )

    if self.subtitle and self.subtitle ~= "" then
        self:_fade(
            window:Create("TextLabel", {
                Text = locale.t(self.subtitle),
                Size = UDim2.new(1, 0, 0, self._subH),
                BackgroundTransparency = 1,
                TextSize = subtitleFontSize,
                TextXAlignment = Enum.TextXAlignment.Left,
                TextWrapped = true,
                LayoutOrder = 2,
                TextTransparency = 1,
                Parent = textFrame,
            }, { TextColor3 = "TitlingColor", FontFace = "Font" }),
            "TextTransparency",
            0.55
        )
    end
end

function Popup:_buildContent()
    if (not self.content or self.content == "") and (not self.boxes or #self.boxes == 0) then
        return 0
    end

    local window = self.window
    local measuredContentHeight = if self.boxes and #self.boxes > 0 then self:_measureBoxes() else self:_measureText()
    local scrollHeight = math.min(measuredContentHeight, maxContentHeight) + contentPadding * 2

    local scrollFrame = window:Create("ScrollingFrame", {
        Name = "Content",
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Size = UDim2.new(1, 0, 0, scrollHeight),
        CanvasSize = UDim2.new(0, 0, 0, 0),
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        ScrollBarThickness = 4,
        ScrollBarImageColor3 = Color3.fromRGB(255, 255, 255),
        ScrollingDirection = Enum.ScrollingDirection.Y,
        LayoutOrder = 2,
        ScrollBarImageTransparency = 1,
        Parent = self.card,
    })
    self:_fade(scrollFrame, "ScrollBarImageTransparency", 0.8)

    window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, contentPadding),
        PaddingRight = UDim.new(0, contentPadding),
        PaddingTop = UDim.new(0, contentPadding),
        PaddingBottom = UDim.new(0, contentPadding),
        Parent = scrollFrame,
    })

    if self.boxes and #self.boxes > 0 then
        window:Create("UIListLayout", {
            FillDirection = Enum.FillDirection.Vertical,
            HorizontalAlignment = Enum.HorizontalAlignment.Center,
            SortOrder = Enum.SortOrder.LayoutOrder,
            Padding = UDim.new(0, boxIconSpacing),
            Parent = scrollFrame,
        })
        for i, box in self.boxes do
            self:_buildBox(scrollFrame, box, i)
        end
    else
        self:_fade(
            window:Create("TextLabel", {
                Text = locale.t(self.content),
                Size = UDim2.new(1, 0, 0, measuredContentHeight),
                BackgroundTransparency = 1,
                TextSize = bodyFontSize,
                TextXAlignment = Enum.TextXAlignment.Left,
                TextYAlignment = Enum.TextYAlignment.Top,
                TextWrapped = true,
                TextTransparency = 1,
                Parent = scrollFrame,
            }, { TextColor3 = "ContentColor", FontFace = "Font" }),
            "TextTransparency",
            0.5
        )
    end

    return scrollHeight
end

function Popup:_buildBox(scrollFrame, box, index)
    local window = self.window
    box = if typeof(box) == "table" then box else { title = tostring(box) }
    local hasBoxIcon = box.icon ~= nil and box.icon ~= 0 and box.icon ~= ""
    local boxHeight, titleHeight, descHeight, contentHeight = self:_measureBox(box)

    local boxFrame = window:Create("Frame", {
        Name = "Box",
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        Size = UDim2.new(1, -boxContentPad, 0, boxHeight),
        LayoutOrder = index,
        BackgroundTransparency = 1,
        Parent = scrollFrame,
    })
    self:_fade(boxFrame, "BackgroundTransparency", 0)

    local boxStroke = window:StyleElementPanel(boxFrame)
    self:_fade(boxStroke, "Transparency", window.theme.ElementStrokeTransparency)

    window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, boxPadLeft),
        PaddingRight = UDim.new(0, boxPadLeft),
        PaddingTop = UDim.new(0, boxPadTop),
        PaddingBottom = UDim.new(0, boxPadTop),
        Parent = boxFrame,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, boxDescPad),
        Parent = boxFrame,
    })

    if hasBoxIcon then
        self:_fade(
            window:Create("ImageLabel", {
                Image = box.icon,
                Size = UDim2.fromOffset(boxIconSize, boxIconSize),
                BackgroundTransparency = 1,
                BorderSizePixel = 0,
                LayoutOrder = 1,
                ImageTransparency = 1,
                Parent = boxFrame,
            }, { ImageColor3 = "ContentColor" }),
            "ImageTransparency",
            0
        )
    end

    local textBox = window:Create("Frame", {
        BackgroundTransparency = 1,
        Size = UDim2.new(1, hasBoxIcon and -(boxIconSize + boxDescPad) or 0, 0, contentHeight),
        LayoutOrder = 2,
        Parent = boxFrame,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Vertical,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, 3),
        Parent = textBox,
    })

    self:_fade(
        window:Create("TextLabel", {
            Text = locale.t(box.title or box.Title or ""),
            Size = UDim2.new(1, 0, 0, titleHeight),
            BackgroundTransparency = 1,
            TextSize = boxTitleFontSize,
            TextXAlignment = Enum.TextXAlignment.Left,
            TextWrapped = true,
            LayoutOrder = 1,
            TextTransparency = 1,
            Parent = textBox,
        }, { TextColor3 = "ContentColor", FontFace = "TitleFont" }),
        "TextTransparency",
        0
    )

    local description = box.description or box.Description
    if description and description ~= "" then
        self:_fade(
            window:Create("TextLabel", {
                Text = locale.t(description),
                Size = UDim2.new(1, 0, 0, descHeight),
                BackgroundTransparency = 1,
                TextSize = boxDescFontSize,
                TextXAlignment = Enum.TextXAlignment.Left,
                TextYAlignment = Enum.TextYAlignment.Top,
                TextWrapped = true,
                LayoutOrder = 2,
                TextTransparency = 1,
                Parent = textBox,
            }, { TextColor3 = "ContentColor", FontFace = "Font" }),
            "TextTransparency",
            0.65
        )
    end
end

function Popup:_buildFooter()
    local window = self.window

    local footerFrame = window:Create("Frame", {
        Name = "Footer",
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 0, footerHeight),
        LayoutOrder = 3,
        Parent = self.card,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        HorizontalFlex = Enum.UIFlexAlignment.Fill,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, footerSpacing),
        Parent = footerFrame,
    })

    for i, option in self.options do
        self:_buildButton(footerFrame, option, i)
    end
end

function Popup:_buildButton(footerFrame, option, index)
    local window = self.window
    option = if typeof(option) == "table" then option else { text = tostring(option) }
    local style = option.style or option.Style or "neutral"
    local text = option.text or option.Text or option.name or option.Name or "Okay"
    local callback = option.callback or option.Callback

    local bgColor, hoverColor, strokeColor, strokeTransparency
    if style == "primary" then
        bgColor, hoverColor, strokeColor, strokeTransparency =
            window.theme.AccentColor, window.theme.AccentStroke, window.theme.AccentStroke, 0.1
    elseif style == "danger" then
        bgColor, hoverColor, strokeColor, strokeTransparency =
            window.theme.ErrorColor, window.theme.ErrorStrokeColor, window.theme.ErrorStrokeColor, 0
    else
        bgColor, hoverColor, strokeColor, strokeTransparency =
            window.theme.NeutralButton, window.theme.NeutralButtonHover, window.theme.NeutralButtonStroke, 0.85
    end

    local buttonFrame = window:Create("Frame", {
        Name = "Button",
        BackgroundColor3 = bgColor,
        BorderSizePixel = 0,
        Size = UDim2.new(0, 0, 0, footerHeight),
        LayoutOrder = index,
        BackgroundTransparency = 1,
        Parent = footerFrame,
    })
    self:_fade(buttonFrame, "BackgroundTransparency", 0)

    window:Create("UIFlexItem", { FlexMode = Enum.UIFlexMode.Fill, Parent = buttonFrame })
    window:Create("UICorner", { CornerRadius = buttonCorner, Parent = buttonFrame })

    local buttonStroke = window:Create("UIStroke", {
        Color = strokeColor,
        Transparency = 1,
        Parent = buttonFrame,
    })
    self:_fade(buttonStroke, "Transparency", strokeTransparency)

    self:_fade(
        window:Create("TextLabel", {
            Text = locale.t(text),
            Size = UDim2.fromScale(1, 1),
            BackgroundTransparency = 1,
            TextSize = buttonTextSize,
            TextXAlignment = Enum.TextXAlignment.Center,
            TextTruncate = Enum.TextTruncate.AtEnd,
            TextColor3 = functions.contrastText(bgColor),
            TextTransparency = 1,
            Parent = buttonFrame,
        }, { FontFace = "Font" }),
        "TextTransparency",
        0
    )

    local buttonInteract = window:Create("TextButton", {
        Text = "",
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        ZIndex = 2,
        Parent = buttonFrame,
    })

    local buttonHoverTween = "snappy"
    table.insert(
        self._connections,
        window:Connect(buttonInteract.MouseEnter, function()
            if self._closed then
                return
            end
            motion.tween(buttonFrame, { BackgroundColor3 = hoverColor }, buttonHoverTween)
        end)
    )
    table.insert(
        self._connections,
        window:Connect(buttonInteract.MouseLeave, function()
            motion.tween(buttonFrame, { BackgroundColor3 = bgColor }, buttonHoverTween)
        end)
    )

    table.insert(
        self._connections,
        window:Connect(buttonInteract.MouseButton1Click, function()
            if self._closed then
                return
            end
            haptic.click()
            motion.tween(buttonStroke, { Transparency = 1 }, buttonHoverTween)
            if callback then
                task.spawn(function()
                    local ok, err = pcall(callback)
                    if not ok then
                        log.warn("Astra: popup button '" .. text .. "' callback errored:")
                        log.print(err)
                    end
                end)
            end
            self:Close()
        end)
    )
end

function Popup:_measureHeader(hasIcon)
    local availableWidth = contentInnerWidth - (if hasIcon then iconSize + boxIconPad else 0)
    self._titleH = functions.textHeight(self.window.theme.Font, titleFontSize, locale.resolve(self.title), availableWidth)
    self._subH = if self.subtitle and self.subtitle ~= ""
        then functions.textHeight(self.window.theme.Font, subtitleFontSize, locale.resolve(self.subtitle), availableWidth)
        else 0
    self._columnH = self._titleH + (if self._subH > 0 then 3 + self._subH else 0)
    return math.max(self._columnH, if hasIcon then iconSize else 0)
end

function Popup:_measureText()
    return functions.textHeight(self.window.theme.Font, bodyFontSize, locale.resolve(self.content), contentInnerWidthPadded)
end

function Popup:_measureBox(box)
    box = if typeof(box) == "table" then box else { title = tostring(box) }
    local hasBoxIcon = box.icon ~= nil and box.icon ~= 0 and box.icon ~= ""
    local availableWidth = contentInnerWidthPadded - boxContentPad - boxPadLeft * 2 - (if hasBoxIcon then boxIconSize + boxDescPad else 0)

    local titleHeight = functions.textHeight(
        self.window.theme.TitleFont,
        boxTitleFontSize,
        locale.resolve(box.title or box.Title or ""),
        availableWidth
    )
    local descHeight = 0
    local description = box.description or box.Description
    if description and description ~= "" then
        descHeight = functions.textHeight(self.window.theme.Font, boxDescFontSize, locale.resolve(description), availableWidth)
    end

    local contentHeight = titleHeight + (if descHeight > 0 then 3 + descHeight else 0)
    local boxHeight = math.max(contentHeight, if hasBoxIcon then boxIconSize else 0) + boxPadTop * 2
    return boxHeight, titleHeight, descHeight, contentHeight
end

function Popup:_measureBoxes()
    local totalHeight = 0
    for i, box in self.boxes do
        totalHeight += (self:_measureBox(box))
        if i < #self.boxes then
            totalHeight += boxIconSpacing
        end
    end
    return totalHeight
end

function Popup:_show()
    if self._closed or not self.screenGui.Parent then
        return
    end

    haptic.notify()

    motion.tween(self.card, { Position = UDim2.new(0.5, 0, 0.5, 0) }, cardShowTween)

    for _, reveal in self._reveal do
        local tweenInfo = if reveal.instance == self.backdrop then backdropTween else fadeInTween
        motion.tween(reveal.instance, { [reveal.prop] = reveal.to }, tweenInfo)
    end
end

function Popup:Close()
    if self._closed then
        return
    end
    self._closed = true

    local index = table.find(popupStack, self)
    if index then
        table.remove(popupStack, index)
    end

    for _, conn in self._connections do
        self.window:Disconnect(conn)
    end
    self._connections = {}

    if not self.screenGui.Parent then
        return
    end

    if self._closeConnection then
        self._closeConnection:Disconnect()
        self._closeConnection = nil
    end

    local closeTween = state.tweenService:Create(
        self.card,
        motion.spec(textFadeTween),
        { Position = UDim2.new(0.5, 0, 0.5, 10) }
    )
    self._closeConnection = closeTween.Completed:Connect(function(playbackState)
        local oldConnection = self._closeConnection
        self._closeConnection = nil
        if oldConnection then
            oldConnection:Disconnect()
        end
        if playbackState == Enum.PlaybackState.Completed and self._closed and self.screenGui.Parent then
            self.window:DestroySubtree(self.screenGui)
        end
    end)
    closeTween:Play()

    for _, reveal in self._reveal do
        motion.tween(reveal.instance, { [reveal.prop] = 1 }, textFadeTween)
    end
end

return Popup

end)() end,
    [15] = function()local wax,script,require=ImportGlobals(15)local ImportGlobals return (function(...)
local utilities = script.Parent.Parent.utilities
local state = require(utilities.Parent.core.state)
local motion = require(utilities.motion)
local services = require(utilities.services)
local image = require(script.Parent.Parent.images.image)
local functions = require(script.Parent.Parent.functions)
local locale = require(utilities.locale)
local sidebar = require(script.Parent.sidebar)
local network = require(utilities.network)

-- [[The profile panel lives OUTSIDE the window frame, beside it: a companion]]
-- [[card whose height always matches the current window height. The window and]]
-- [[the panel are centred on screen as a single unit, so the window sits half a]]
-- [[panel (width + gap) away from the screen centre, on the opposite side of]]
-- [[the panel.]]
-- [[
--   Visual language: the card is the window, in miniature. Its surface is the
--   window frame's own (WindowColor gradient, CornerRoundness, hairline
--   SurfaceStroke, ShadowColor glow); every section plate is a window element
--   body (Window:StyleElementBody — ElementGradient over ElementTransparency,
--   ElementCornerRadius, ElementStroke at ElementStrokeTransparency); every
--   heading is a window Section title (16px ContentColor icon at 0.65 beside
--   a 15px ContentColor label at 0.6); every row reuses the element row
--   colours (ContentColor icons and values, muted ContentColor labels) and
--   the brand Font. Nothing here introduces a colour, radius, gradient or
--   shadow the rest of Astra does not already use.
--
--   Layout, top to bottom:
--     header   avatar (with its presence dot) on the left; display name,
--              @username and the tier pill (PREMIUM / FREEMIUM, or the host's
--              own word) beside it. The header is pinned — it never scrolls.
--     divider  one hairline rule, inset by the card padding.
--     details  a vertically scrolling column of sections, each a window-style
--              section heading above an element-style plate of compact
--              label/value rows:
--                Account       user ID (+ COPY), account created, elapsed time,
--                              key (+ COPY), whitelist
--                Current Game  thumbnail, game name, place ID (+ COPY)
--                Server        players, job ID (+ COPY)
--                User Session  time in server
--
--   Every plate, heading, row and pill on the card is styled with the very
--   same theme tokens the window and its elements use — nothing here
--   introduces a colour, radius, gradient or transparency of its own.
--
--   Every value is bound to a real source (see the DATA section below) and
--   falls back to a neutral "—" when the source is unavailable. Nothing on the
--   card is fabricated, and values the app treats as identifying stay masked
--   until "Reveal profile details" is on. The game name comes from Roblox's
--   official two-step API (place id -> universe id -> games list), falling
--   back to the DataModel's own name while the request is in flight or when
--   the executor exposes no request function.
-- ]]

local profilePanel = {}

profilePanel.width = 260
profilePanel.gap = 12

-- [[Card metrics. Kept in lockstep with the layout code below; the scroll
-- [[region is the only part of the card that grows with content.]]
local avatarSize = 48
local avatarTop = 12
local paddingX = 12
local headerHeight = 74
local dividerHeight = 1
local scrollPaddingTop = 12
local scrollPaddingBottom = 16
local sectionGap = 12
-- [[Heading metrics mirror the window's Section element: a 16px icon beside a]]
-- [[16px-tall title row, both in ContentColor at the section's own]]
-- [[transparencies (title 0.6, icon 0.65).]]
local headingHeight = 16
local headingGap = 8
local headingIconSize = 16
local headingIconGap = 6
local cardPaddingX = 10
local cardPaddingY = 10
local rowHeight = 18
local rowGap = 6
local rowIconSize = 14
local labelWidth = 92
local actionSize = 16
local actionInset = actionSize + 6
local gameThumbnailSize = 40
local tooltipHeight = 32
local scrollBarThickness = 6
local scrollBarTransparency = 0.8
local refreshInterval = 1
-- [[How long a copy action keeps its green check before the copy icon]]
-- [[returns.]]
local copyFlashDuration = 1.2
-- [[Copy button wording: the idle label is built from the row's own name]]
-- [[("Copy " .. "Job ID"), the confirmation stands alone ("Copied"). Both go]]
-- [[through locale.resolve, so a host can translate them by their source]]
-- [[strings like every other line on the card.]]
local copyLabelPrefix = "Copy "
local copiedLabel = "Copied"
-- [[Tier pill box: measured from its own label so any tier word fits.]]
local tierHeight = 16
local tierPaddingLeft = 6
local tierPaddingRight = 7
local tierIconSize = 10
local tierGap = 4

-- [[Neutral placeholder for a value the application cannot supply. Never a
-- [[made-up number: "—" reads as "no data" in every locale.]]
local fallbackValue = "—"

-- [[Placeholder shown in place of an identifier while the reveal toggle is
-- [[off: a fixed-width block, so neither the value nor its length leaks. Names
-- [[keep the sidebar mask ("Tes****") instead.]]
local maskedId = "••••••"
local maskedKey = "••••••••"

local nameTransparency = 0
-- [[The window's own secondary-text transparency (the topbar subtitle fades]]
-- [[to 0.7), reused for the card's @username line.]]
local subtitleTransparency = 0.7
local labelTransparency = 0.45
-- [[Section title / icon transparencies, taken from the Section element's]]
-- [[reveal values so headings read exactly like the window's own.]]
local headingTransparency = 0.6
local iconTransparency = 0.65
local avatarBgTransparency = 0.95
local idleStrokeTransparency = 0.95
local glowTransparency = 0.6

local settleTween, hoverTween = "snappy", "fast"

local fallbackUri = "rbxassetid://0"

profilePanel.settleSpec = settleTween

local buildGeneration = 0

local avatarGenerations = setmetatable({}, { __mode = "k" })
local avatarReady = setmetatable({}, { __mode = "k" })
local gameThumbnailGenerations = setmetatable({}, { __mode = "k" })

local edgeMargin = 8

-- [[Effective card height: derived from the current window height so the]]
-- [[card never exceeds the window. At the default 420px window height the]]
-- [[card is exactly 420px; at other responsive sizes it matches the window.]]
function profilePanel.effectiveHeight(window): number
    if not window or not window.size then
        return 420
    end
    local windowH = window.size.Y.Offset
    if type(windowH) ~= "number" or windowH <= 0 then
        return 420
    end
    return windowH
end

local function currentCardHeight(window): number
    return profilePanel.effectiveHeight(window)
end

local function hasRoom(window): boolean
    local camera = state.workspace.CurrentCamera
    local viewport = camera and camera.ViewportSize
    if not viewport then
        return true
    end
    if viewport.X < window.size.X.Offset + profilePanel.gap + profilePanel.width + edgeMargin * 2 then
        return false
    end
    local cardH = currentCardHeight(window)
    return viewport.Y >= cardH + edgeMargin * 2
end

-- [[The panel is "enabled" when its content should exist at all: the user]]
-- [[toggled it on, the player is known, and the screen has room. Window]]
-- [[visibility (hidden/minimised) layers on top in setShown.]]
function profilePanel.isEnabled(window): boolean
    return window.settings.showProfile
        and state.localPlayer ~= nil
        and hasRoom(window)
end

-- [[How far the window rests off the screen centre so that the]]
-- [[window + gap + panel pair is centred (0 while the panel is off).]]
function profilePanel.shiftFor(window): number
    if not profilePanel.isEnabled(window) then
        return 0
    end
    return (profilePanel.width + profilePanel.gap) / 2
end

-- [[Is the card on screen right now? `isEnabled` answers "should its]]
-- [[content exist" (setting + player + room); this adds the window's own]]
-- [[visibility, which is what the on-screen clamp cares about — a]]
-- [[minimised capsule must not be shoved around by a card that is not]]
-- [[there.]]
function profilePanel.isShown(window): boolean
    local panel = window.profilePanel
    return panel ~= nil and panel.Visible == true
end

-- [[Half-extents of the window + card pair, measured from the window's]]
-- [[centre: how far the pair reaches left, right and up/down. The card]]
-- [[height matches the window height, so vertical half-extent is simply]]
-- [[the window half. Returns the plain window halves while the card is not shown.]]
-- [[`width` / `height` override the window size for callers mid-animation (the drag]]
-- [[path measures the frame, which is smaller while minimised).]]
function profilePanel.pairHalfSize(window, width: number?, height: number?)
    local size = window.size
    local halfW = (width or size.X.Offset) / 2
    local halfH = (height or size.Y.Offset) / 2
    if not profilePanel.isShown(window) then
        return halfW, halfW, halfH
    end
    local extra = profilePanel.width + profilePanel.gap
    local left = (window.settings.profileSide or "right") == "left"
    local cardH = if height then height else currentCardHeight(window)
    local pairHalfH = math.max(halfH, cardH / 2)
    if left then
        return halfW + extra, halfW, pairHalfH
    end
    return halfW, halfW + extra, pairHalfH
end

-- [[The "Reveal profile details" toggle (persisted under the legacy]]
-- [[`showFullUsername` key). Off = every identifying value on the card is]]
-- [[masked; on = display name, username, user ID, place ID, job ID and]]
-- [[license key show in full.]]
function profilePanel.revealEnabled(window): boolean
    return window.settings.showFullUsername == true
end

-- [[The reveal toggle's prerequisite: it unmasks values that live ON the]]
-- [[card, so it is only meaningful while "Show profile" is on. Leaving it]]
-- [[active with the card off would silently unmask identity data the moment]]
-- [[the card came back, so the dependency is enforced rather than implied.]]
function profilePanel.revealAllowed(window): boolean
    return window.settings.showProfile == true
end

-- [[Driver for the "Reveal profile details" toggle. Refuses the on state]]
-- [[while "Show profile" is off: the setting stays off, the card stays]]
-- [[masked, and a notification says what is required first. Returns whether]]
-- [[the requested value was accepted, so the caller can roll its own switch]]
-- [[back to match.]]
function profilePanel.setReveal(window, enabled): boolean
    local want = enabled == true
    if want and not profilePanel.revealAllowed(window) then
        window.settings.showFullUsername = false
        profilePanel.applyIdentity(window)
        profilePanel.applyLive(window)
        window:Notify({
            title = locale.resolve("Show profile is required"),
            content = locale.resolve(
                "Turn on \"Show profile\" first. Profile details can only be revealed while the profile card is visible."
            ),
        })
        return false
    end
    window.settings.showFullUsername = want
    profilePanel.applyIdentity(window)
    profilePanel.applyLive(window)
    return true
end

-- [[Dependency normalisation for settings that arrive from somewhere other]]
-- [[than the toggle: a config saved before the rule existed (or edited by]]
-- [[hand) can carry reveal = on with the card off. Returns true when]]
-- [[something had to be corrected, so callers can resync a switch.]]
function profilePanel.syncReveal(window): boolean
    if window.settings.showFullUsername ~= true or profilePanel.revealAllowed(window) then
        return false
    end
    window.settings.showFullUsername = false
    profilePanel.applyIdentity(window)
    return true
end

-- =========================================================================
-- Formatting helpers
-- =========================================================================

-- [[Write a label's text only when it actually changes: the live refresh runs
-- [[every second, and touching a Text property repaints the tree.]]
local function setText(label, text)
    if label and label.Text ~= text then
        label.Text = text
    end
end

-- [[Compact duration notation: "8s", "47m 12s", "2h 34m", "3d 4h". Used for]]
-- [[the current session; a missing/negative source is the neutral]]
-- [[placeholder, never a zero that would read as real.]]
local function formatDuration(seconds: number?): string
    if type(seconds) ~= "number" or seconds ~= seconds or seconds < 0 then
        return fallbackValue
    end
    local total = math.floor(seconds)
    local days = math.floor(total / 86400)
    local hours = math.floor(total % 86400 / 3600)
    local minutes = math.floor(total % 3600 / 60)
    local secs = total % 60
    if days > 0 then
        return `{days}d {hours}h`
    end
    if hours > 0 then
        return `{hours}h {minutes}m`
    end
    if minutes > 0 then
        return `{minutes}m {secs}s`
    end
    return `{secs}s`
end

local monthNames = { "Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec" }

-- [[Civil date from a Unix timestamp without relying on os.date, which some
-- [[executor sandboxes strip (Howard Hinnant's days-from-civil algorithm).]]
local function civilDate(daysSinceEpoch: number): (number, number, number)
    local z = daysSinceEpoch + 719468
    local era = math.floor(z / 146097)
    local doe = z - era * 146097
    local yoe = math.floor((doe - math.floor(doe / 1460) + math.floor(doe / 36524) - math.floor(doe / 146096)) / 365)
    local y = yoe + era * 400
    local doy = doe - (365 * yoe + math.floor(yoe / 4) - math.floor(yoe / 100))
    local mp = math.floor((5 * doy + 2) / 153)
    local d = doy - math.floor((153 * mp + 2) / 5) + 1
    local m = mp + (if mp < 10 then 3 else -9)
    y += (if m <= 2 then 1 else 0)
    return y, m, d
end

local function formatDate(unixSeconds: number?): string
    if type(unixSeconds) ~= "number" or unixSeconds <= 0 then
        return fallbackValue
    end
    local ok, y, m, d = pcall(function()
        return civilDate(math.floor(unixSeconds / 86400))
    end)
    if not ok or type(y) ~= "number" then
        return fallbackValue
    end
    local month = monthNames[m]
    if not month then
        return fallbackValue
    end
    return `{d} {month} {y}`
end

-- Elapsed time since creation, in the largest useful unit.
local function formatAccountAge(seconds: number?): string
    if type(seconds) ~= "number" or seconds ~= seconds or seconds < 0 then
        return fallbackValue
    end
    for _, unit in { { 31557600, "year" }, { 2629800, "month" }, { 86400, "day" }, { 3600, "hour" } } do
        local count = math.floor(seconds / unit[1])
        if count >= 1 then
            return `{count} {unit[2]}{if count == 1 then "" else "s"} ago`
        end
    end
    return "Less than an hour ago"
end

local function nowSeconds(): number?
    local ok, value = pcall(function()
        return os.time()
    end)
    if ok and type(value) == "number" then
        return value
    end
    return nil
end

-- =========================================================================
-- Card icons
-- =========================================================================
-- [[Every icon the card draws, written as the lucide name (the library's
-- [[default pack) followed by the names the other packs ship for the same
-- [[idea. A window created with tabler / phosphor / heroicons / feather /
-- [[material therefore draws the card's icons as well, instead of leaving an
-- [[empty square wherever the backend pack does not carry the lucide name —
-- [[which is exactly what the tier pill used to do: its badge is a lucide-only
-- [[name, so FREEMIUM showed a word with no icon beside it and the pill's
-- [[crown vanished the same way on the packs without one.
-- [[The first name the active pack really has wins, and because every list
-- [[starts with the lucide name the default pack keeps the exact icon it had
-- [[before this table existed.]]
local iconAliases = {
    ["id-card"] = {
        "id-card",
        "id-badge",
        "identification-card",
        "identification-badge",
        "identification",
        "credit-card",
        "account_box",
        "user-square",
    },
    ["calendar-days"] = { "calendar-days", "calendar", "date_range", "calendar_month", "calendar-today" },
    hourglass = { "hourglass", "hourglass-empty", "hourglass_empty", "timer", "clock" },
    ["key-round"] = { "key-round", "key", "vpn_key", "keyhole", "lock" },
    ["shield-check"] = { "shield-check", "verified_user", "security", "shield" },
    ["user-round"] = { "user-round", "user", "person", "account_circle", "account-box" },
    ["gamepad-2"] = {
        "gamepad-2",
        "gamepad",
        "device-gamepad-2",
        "device-gamepad",
        "game-controller",
        "sports_esports",
        "cube",
        "globe",
    },
    users = { "users", "users-alt", "user-group", "group", "people", "person" },
    server = { "server", "dns", "database", "storage", "hard-drive", "cloud" },
    activity = { "activity", "pulse", "bolt", "timeline", "show_chart", "history" },
    clock = { "clock", "access_time", "schedule", "timer" },
    -- [[Tier pill: the crown for a paid membership, a badge for a free one.]]
    crown = { "crown", "star", "award", "medal" },
    ["badge-check"] = {
        "badge-check",
        "check-badge",
        "seal-check",
        "verified_user",
        "badge",
        "award",
        "star",
    },
    -- [[Copy actions: the copy glyph and the check it flashes on success.]]
    copy = { "copy", "content_copy", "clipboard", "clipboard-copy", "file_copy", "document-duplicate" },
    check = { "check", "done", "circle-check", "check-circle" },
}

-- [[Resolve one of those names against the window's active pack. The resolver
-- [[hands an unknown name straight back, so the first candidate that comes
-- [[back changed is the one the pack actually ships; the answer is cached per
-- [[window because a window's pack never changes.]]
local function cardIcon(window, name: string): string?
    local cache = window.profileIconCache
    if not cache then
        cache = {}
        window.profileIconCache = cache
    end
    local cached = cache[name]
    if cached ~= nil then
        return cached
    end
    local resolved = nil
    for _, candidate in iconAliases[name] or { name } do
        local value = window:ResolveIcon(candidate)
        if type(value) == "string" and value ~= "" and value ~= candidate then
            resolved = value
            break
        end
    end
    resolved = resolved or window:ResolveIcon(name)
    cache[name] = resolved
    return resolved
end

-- =========================================================================
-- Data sources
-- =========================================================================
-- [[Every value on the card comes from one of these, with a neutral fallback]]
-- [[when the source is unavailable.
--
--   display name / username / user ID  Players.LocalPlayer
--   tier pill                          the host's `tier` from
--                                      Window:SetProfile, else
--                                      LocalPlayer.MembershipType
--                                      (PREMIUM / FREEMIUM)
--   account created / elapsed time     users.roblox.com creation timestamp
--                                      (AccountAge gives an approximate elapsed fallback)
--   game name                          Roblox's official two-step API:
--                                      place id -> universe id -> the games
--                                      list's "name" (DataModel.Name until
--                                      the request answers)
--   place ID                           DataModel.PlaceId
--   game thumbnail                     thumbnails.roblox.com (universe id)
--   players                            Players:GetPlayers() / Players.MaxPlayers
--   job ID                             DataModel.JobId
--   session time                       os.clock() since the client loaded
--   key / whitelist                    supplied by the host through
--                                      Window:SetProfile — no built-in store
-- ]]

local function sessionStartedAt(): number?
    local ok, value = pcall(function()
        return os.clock()
    end)
    if ok and type(value) == "number" then
        return value
    end
    return nil
end

local clientSessionStart = sessionStartedAt()

local function sessionSeconds(): number?
    if clientSessionStart == nil then
        return nil
    end
    local now = sessionStartedAt()
    if now == nil then
        return nil
    end
    return math.max(0, now - clientSessionStart)
end

local function getJobId(): string?
    local ok, value = pcall(function()
        return (game :: any).JobId
    end)
    if ok and type(value) == "string" and value ~= "" then
        return value
    end
    return nil
end

local function isStudio(): boolean
    local ok, value = pcall(function()
        return state.runService:IsStudio()
    end)
    return ok and value == true
end

local function playerCounts(): (number?, number?)
    local ok, players = pcall(function()
        return services.getService("Players")
    end)
    if not ok or players == nil then
        return nil, nil
    end
    local listOk, list = pcall(function()
        return players:GetPlayers()
    end)
    local maxOk, maxPlayers = pcall(function()
        return players.MaxPlayers
    end)
    local current = if listOk and type(list) == "table" then #list else nil
    local capacity = if maxOk and type(maxPlayers) == "number" and maxPlayers > 0 then maxPlayers else nil
    return current, capacity
end

-- [[MembershipType is the real Roblox source for the premium pill: legacy
-- [[Builders Club tiers count, Free/None do not. When the platform cannot be
-- [[read the pill stays hidden rather than claiming a status.]]
local function isPremium(player): boolean
    if not player then
        return false
    end
    local ok, membership = pcall(function()
        return player.MembershipType
    end)
    if not ok or membership == nil or typeof(membership) ~= "EnumItem" then
        return false
    end
    return membership ~= Enum.MembershipType.None and membership ~= Enum.MembershipType.Free
end

-- =========================================================================
-- Remote data: universe id, game name, game thumbnail
-- =========================================================================

local resolvedGameIcons: { [number]: string? } = {}
local pendingGameIconRequests: { [number]: { (string?) -> () } } = {}
local resolvedUniverseIds: { [number]: number? } = {}
local pendingUniverseRequests: { [number]: { (number?) -> () } } = {}
local resolvedGameNames: { [number]: string? } = {}
local pendingGameNameRequests: { [number]: { (string?) -> () } } = {}

local function toValidNumber(value: any): number?
    if type(value) == "number" and value > 0 then
        return value
    end
    if type(value) == "string" then
        local n = tonumber(value)
        if n and n > 0 then
            return n
        end
    end
    return nil
end

-- [[One shared JSON GET helper over the executor's request function — the
-- [[same `network.getRequestFn()` the rest of Astra uses. Missing request
-- [[function or a failed call hands the callback nil so every caller can fall
-- [[back to the neutral placeholder instead of inventing data.]]
local function requestJson(url: string, callback: (any?) -> ())
    local requestFn = network.getRequestFn()
    if not requestFn then
        callback(nil)
        return
    end
    task.spawn(function()
        local decoded = nil
        local ok, result = pcall(requestFn, {
            Url = url,
            Method = "GET",
        })
        if ok and type(result) == "table" then
            local body = (result :: any).Body
            local success = false
            if type((result :: any).StatusCode) == "number" then
                local code = (result :: any).StatusCode :: number
                success = code >= 200 and code < 300
            elseif type((result :: any).Success) == "boolean" then
                success = (result :: any).Success == true
            else
                success = type(body) == "string" and #body > 0
            end
            if success and type(body) == "string" and #body > 0 then
                local decodeOk, parsed = pcall(function()
                    return state.httpService:JSONDecode(body)
                end)
                if decodeOk then
                    decoded = parsed
                end
            end
        end
        callback(decoded)
    end)
end

-- Fetch once per player/window; never infer an exact date from rounded AccountAge.
function profilePanel.applyAccountCreated(window)
    if window.unloaded or not window.profileJoinDateValue then
        return
    end
    local player = state.localPlayer
    local userId = player and player.UserId
    if window.profileAccountUserId ~= userId then
        window.profileAccountUserId = userId
        window.profileAccountCreatedAt = nil
        window.profileAccountRequested = false
    end
    local created = window.profileAccountCreatedAt
    local now = nowSeconds()
    local age = player and player.AccountAge
    setText(window.profileJoinDateValue, formatDate(created))
    local elapsed = if created and now then formatAccountAge(now - created)
        elseif type(age) == "number" and age >= 0 then
            (if age == 0 then "Less than a day ago" else "~ " .. formatAccountAge(age * 86400))
        else fallbackValue
    setText(window.profileAccountAgeValue, elapsed)

    if type(userId) ~= "number" or userId <= 0 or window.profileAccountRequested then
        return
    end
    window.profileAccountRequested = true
    requestJson(`https://users.roblox.com/v1/users/{userId}`, function(parsed)
        if window.unloaded or window.profileAccountUserId ~= userId then
            return
        end
        local ok, timestamp = pcall(function()
            if type(parsed) ~= "table" or type(parsed.created) ~= "string" then
                return nil
            end
            return DateTime.fromIsoDate(parsed.created).UnixTimestamp
        end)
        local currentTime = nowSeconds()
        if ok and type(timestamp) == "number" and timestamp > 0 and currentTime and timestamp <= currentTime then
            window.profileAccountCreatedAt = timestamp
            profilePanel.applyAccountCreated(window)
        end
    end)
end

local function getUniverseId(): number?
    local ok, result = pcall(function()
        return (game :: any).GameId
    end)
    if ok then
        return toValidNumber(result)
    end
    return nil
end

local function getPlaceId(): number?
    local ok, result = pcall(function()
        return (game :: any).PlaceId
    end)
    if ok then
        return toValidNumber(result)
    end
    return nil
end

local function buildFallbackUrl(placeId: number?): string
    if not placeId then
        return ""
    end
    -- [[Legacy rbxthumb GameIcon fallback: uses PlaceId in this runtime.]]
    return `rbxthumb://type=GameIcon&id={placeId}&w=150&h=150`
end

-- [[Roblox's official thumbnails endpoint expects the *universe* id
-- [[(game.GameId), not the place id. The displayed "Place ID" row still uses
-- [[game.PlaceId; the legacy rbxthumb fallback uses it as well, which is the
-- [[documented convention for that URI.]]
local function fetchOfficialGameIcon(universeId: number, callback: (string?) -> ())
    local cached = resolvedGameIcons[universeId]
    if cached ~= nil then
        callback(if type(cached) == "string" and cached ~= "" then cached else nil)
        return
    end

    local pending = pendingGameIconRequests[universeId]
    if pending then
        table.insert(pending, callback)
        return
    end

    pendingGameIconRequests[universeId] = { callback }

    task.spawn(function()
        local finalImageUrl: string? = nil
        local url = `https://thumbnails.roblox.com/v1/games/icons?universeIds={universeId}&size=512x512&format=Png&isCircular=false`
        requestJson(url, function(parsed)
            if type(parsed) == "table" then
                local data = (parsed :: any).data
                if type(data) == "table" then
                    -- [[Match by targetId; the response order is not guaranteed.]]
                    for _, entry in ipairs(data) do
                        if type(entry) == "table" then
                            local tid = (entry :: any).targetId or (entry :: any).targetID or (entry :: any).id
                            local stateName = (entry :: any).state
                            local matches = tonumber(tid) == universeId
                            if matches and (stateName == "Completed" or stateName == nil) then
                                local img = (entry :: any).imageUrl or (entry :: any).contentUrl or (entry :: any).url
                                if type(img) == "string" and #img > 0 then
                                    finalImageUrl = img
                                    break
                                end
                            end
                        end
                    end
                    -- [[Fallback: first completed entry, still real response data.]]
                    if not finalImageUrl then
                        for _, entry in ipairs(data) do
                            if type(entry) == "table" then
                                local stateName = (entry :: any).state
                                if stateName == "Completed" or stateName == nil then
                                    local img = (entry :: any).imageUrl or (entry :: any).contentUrl
                                    if type(img) == "string" and #img > 0 then
                                        finalImageUrl = img
                                        break
                                    end
                                end
                            end
                        end
                    end
                end
            end

            if finalImageUrl and #finalImageUrl > 0 then
                resolvedGameIcons[universeId] = finalImageUrl
            end

            local callbacks = pendingGameIconRequests[universeId]
            pendingGameIconRequests[universeId] = nil
            for _, cb in ipairs(callbacks or {}) do
                pcall(cb, finalImageUrl)
            end
        end)
    end)
end

-- [[Step 1 of the official game-name flow: the universes endpoint converts a]]
-- [[place id into the universe that owns it. Cached per place and queued]]
-- [[while a request is in flight, exactly like the icon fetch above.]]
local function fetchUniverseId(placeId: number, callback: (number?) -> ())
    local cached = resolvedUniverseIds[placeId]
    if cached ~= nil then
        callback(cached)
        return
    end

    local pending = pendingUniverseRequests[placeId]
    if pending then
        table.insert(pending, callback)
        return
    end

    pendingUniverseRequests[placeId] = { callback }

    requestJson(`https://apis.roblox.com/universes/v1/places/{placeId}/universe`, function(parsed)
        local universeId: number? = nil
        if type(parsed) == "table" then
            universeId = toValidNumber(
                (parsed :: any).universeId
                    or (parsed :: any).universeID
                    or (parsed :: any).universe_id
                    or (parsed :: any).id
            )
        end
        if universeId then
            resolvedUniverseIds[placeId] = universeId
        end
        local callbacks = pendingUniverseRequests[placeId]
        pendingUniverseRequests[placeId] = nil
        for _, cb in ipairs(callbacks or {}) do
            pcall(cb, universeId)
        end
    end)
end

-- [[The universe id the thumbnail and the name flow need: DataModel.GameId]]
-- [[when the runtime supplies one, otherwise the official place -> universe]]
-- [[lookup (step 1).]]
local function resolveUniverseId(callback: (number?) -> ())
    local direct = getUniverseId()
    if direct then
        callback(direct)
        return
    end
    local placeId = getPlaceId()
    if placeId then
        fetchUniverseId(placeId, callback)
    else
        callback(nil)
    end
end

-- [[Step 2 of the official game-name flow: the games endpoint answers the]]
-- [[game's detail list for a universe, and the matching entry's "name" field]]
-- [[IS the game name (DataModel.Name only carries the current place's title,]]
-- [[which is why the card asks the platform instead). The DataModel name]]
-- [[stays on the label until this answers, so the row is never empty.]]
local function fetchGameName(placeId: number, callback: (string?) -> ())
    local cached = resolvedGameNames[placeId]
    if cached ~= nil then
        callback(if type(cached) == "string" and cached ~= "" then cached else nil)
        return
    end

    local pending = pendingGameNameRequests[placeId]
    if pending then
        table.insert(pending, callback)
        return
    end

    pendingGameNameRequests[placeId] = { callback }

    local function finish(name: string?)
        if type(name) == "string" and name ~= "" then
            resolvedGameNames[placeId] = name
        end
        local callbacks = pendingGameNameRequests[placeId]
        pendingGameNameRequests[placeId] = nil
        for _, cb in ipairs(callbacks or {}) do
            pcall(cb, name)
        end
    end

    fetchUniverseId(placeId, function(universeId: number?)
        if not universeId then
            finish(nil)
            return
        end
        requestJson(`https://games.roblox.com/v1/games?universeIds={universeId}`, function(parsed)
            local name: string? = nil
            if type(parsed) == "table" then
                local data = (parsed :: any).data
                if type(data) == "table" then
                    -- [[Match by the universe id when the entry carries one;]]
                    -- [[the response order is not guaranteed.]]
                    for _, entry in ipairs(data) do
                        if type(entry) == "table" then
                            local entryId = tonumber((entry :: any).id or (entry :: any).universeId)
                            local entryName = (entry :: any).name
                            if
                                type(entryName) == "string"
                                and #entryName > 0
                                and (entryId == nil or entryId == universeId)
                            then
                                name = entryName
                                break
                            end
                        end
                    end
                end
            end
            finish(name)
        end)
    end)
end

-- =========================================================================
-- Card writers
-- =========================================================================

local function licenseDaysLeft(data, now: number?): number?
    local whitelist = data.whitelist
    if not whitelist then
        return nil
    end
    if type(whitelist.daysLeft) == "number" then
        return whitelist.daysLeft
    end
    if type(whitelist.expiresAt) == "number" and now then
        return math.ceil((whitelist.expiresAt - now) / 86400)
    end
    return nil
end

-- [[License + whitelist rows. Astra ships no key store of its own, so these
-- [[come from what the host handed to Window:SetProfile; with nothing supplied
-- [[the rows read "—" rather than showing a made-up key.]]
function profilePanel.applyLicense(window)
    local data = window.profileData or {}
    local reveal = profilePanel.revealEnabled(window)

    local key = data.key
    setText(window.profileKeyValue, if type(key) == "string" and key ~= ""
        then (if reveal then key else maskedKey)
        else fallbackValue)

    local days = licenseDaysLeft(data, nowSeconds())
    local status = if data.whitelist and type(data.whitelist.status) == "string" then data.whitelist.status else nil
    -- [[Compact, reference-shaped value: a countdown when one is known, the
    -- [[word "Expired" once it lapses, the host's own word when there is no
    -- [[expiry to count, and the neutral placeholder when there is neither.
    -- [[An unusual status ("Trial") is still spelled out, since dropping it
    -- [[would hide real information.]]
    local whitelistText
    if days == nil then
        whitelistText = status or fallbackValue
    elseif days < 0 then
        whitelistText = "Expired"
    else
        local dayText = if days == 1 then "1 day left" else `{days} days left`
        whitelistText = if status and string.lower(status) ~= "active" then `{status} · {dayText}` else dayText
    end
    setText(window.profileWhitelistValue, whitelistText)
end

-- [[The tier word the header pill shows. A host-supplied `tier` (through]]
-- [[Window:SetProfile) wins — uppercased, so "premium" and "PREMIUM" read the]]
-- [[same; without one the platform's own membership decides between PREMIUM]]
-- [[and FREEMIUM. The pill therefore always states a real tier instead of]]
-- [[vanishing for free accounts.]]
function profilePanel.tierText(window): string
    local data = window.profileData or {}
    local custom = data.tier
    if type(custom) == "string" and custom ~= "" then
        return string.upper(custom)
    end
    return if isPremium(state.localPlayer) then "PREMIUM" else "FREEMIUM"
end

-- [[Write the tier pill: word, icon, colour and width. PREMIUM keeps the]]
-- [[accent crown; every other tier reads in the muted placeholder colour]]
-- [[with a plain badge icon, so a free account is labelled, not decorated.]]
function profilePanel.applyTier(window)
    local badge = window.profilePremium
    local label = window.profileTierLabel
    local icon = window.profileTierIcon
    if not (badge and label and icon) then
        return
    end
    local text = profilePanel.tierText(window)
    setText(label, text)
    local premium = text == "PREMIUM"
    local color = if premium then window.theme.AccentColor else window.theme.PlaceholderColor
    if label.TextColor3 ~= color then
        label.TextColor3 = color
    end
    if icon.ImageColor3 ~= color then
        icon.ImageColor3 = color
    end
    -- [[Both tier states get an icon the active pack really ships: the pill
    -- [[states a tier, so it must never render as a word with a blank square
    -- [[beside it.]]
    local wantedIcon = cardIcon(window, if premium then "crown" else "badge-check")
    if window.profileTierIconSource ~= wantedIcon or window.profileTierIconTarget ~= icon then
        window.profileTierIconTarget = icon
        window.profileTierIconSource = wantedIcon
        image.assign(icon, "Image", wantedIcon)
    end
    -- [[A fixed box measured from its own label, so the pill only ever grows]]
    -- [[as wide as its word needs.]]
    local textWidth = functions.textWidth(window.theme.Font, label.TextSize, text)
    local width = tierPaddingLeft + tierIconSize + tierGap + textWidth + tierPaddingRight
    badge.Size = UDim2.fromOffset(width, tierHeight)
    label.Size = UDim2.fromOffset(textWidth, tierHeight)
    profilePanel.layoutPremium(window)
end

-- [[The executor's clipboard entry point. `setclipboard` is the common name;]]
-- [[the other spellings (and a Clipboard table) cover the remaining]]
-- [[executors. A missing or failing function means "copied nowhere", so the]]
-- [[icon gives no false success feedback.]]
local function getClipboardFn(): ((string) -> ())?
    local env = getfenv()
    for _, candidate in ipairs({ "setclipboard", "set_clipboard", "toclipboard", "to_clipboard" }) do
        local fn = env[candidate]
        if type(fn) == "function" then
            return fn
        end
    end
    local clipboard = env.Clipboard
    if type(clipboard) == "table" and type(clipboard.set) == "function" then
        return function(text: string)
            clipboard.set(clipboard, text)
        end
    end
    return nil
end

local function clipboardSet(text: string): boolean
    local fn = getClipboardFn()
    if not fn then
        return false
    end
    local ok = pcall(fn, text)
    return ok
end

-- [[Drop a scheduled timeout that is still waiting. Executors without
-- [[`task.cancel` (and the offline test harness) simply skip this: the stamp
-- [[check below already makes a superseded timer a no-op, so the cancel is
-- [[belt-and-braces rather than the thing correctness rests on.]]
local function cancelTimer(thread)
    if type(thread) ~= "thread" or type(task.cancel) ~= "function" then
        return
    end
    if coroutine.status(thread) == "suspended" then
        task.cancel(thread)
    end
end

-- [[The words a copy button owns: what it will copy while idle ("Copy Job
-- [[ID"), or the confirmation after a successful press ("Copied"). The button
-- [[is glyph-only, so the line is never drawn: it is the button's accessible
-- [[label alone — the press confirms itself with the green check, and no
-- [[hover surface rides on the button: a tooltip popping over the copy glyph
-- [[on every hover and press read as stray UI over the row, so the rows' own
-- [[truncation hover-help stays the only tooltip on the card. It is written
-- [[through the window's own locale binding, so a host translating
-- [["Copy Place ID" / "Copied" gets both, and Window:SetLocale re-states
-- [[whichever wording is on screen.]]
function profilePanel.setCopyStatus(window, name: string, copied: boolean)
    local entry = (window.profileCopyButtons or {})[name]
    if not entry then
        return
    end
    -- [[The binding stores the *source* wording ("Copy Job ID", "Copied"), so
    -- [[a locale switch re-resolves the current state instead of freezing the
    -- [[old language into it; _bindLocale writes the resolved text for us.]]
    local source = if copied then copiedLabel else copyLabelPrefix .. (entry.label or name)
    if entry.button and entry.button.Parent and entry.button.Text ~= locale.resolve(source) then
        window:_bindLocale(entry.button, "Text", source)
    end
end

-- [[Copy feedback: the row's copy icon turns into a green check for a moment]]
-- [[and then the copy icon returns, with the button's status word following]]
-- [[("Copied" while the check is up, the action's own name after it). Only one
-- [[restore is ever outstanding per button: a repeat click cancels the pending
-- [[timer and restarts the window, and a rebuilt card (stale entry) is simply
-- [[ignored by the timer.]]
function profilePanel.flashCopied(window, name: string)
    local entry = (window.profileCopyButtons or {})[name]
    if not entry or not entry.icon or not entry.icon.Parent then
        return
    end
    image.assign(entry.icon, "Image", cardIcon(window, "check"))
    entry.icon.ImageColor3 = window.theme.Success
    cancelTimer(entry.flashThread)
    entry.flashStamp = (entry.flashStamp or 0) + 1
    local stamp = entry.flashStamp
    entry.flashThread = task.delay(copyFlashDuration, function()
        if entry.flashStamp ~= stamp or not entry.icon or not entry.icon.Parent then
            return
        end
        entry.flashThread = nil
        image.assign(entry.icon, "Image", cardIcon(window, "copy"))
        entry.icon.ImageColor3 = window.theme.ActionColor
        profilePanel.setCopyStatus(window, name, false)
    end)
    profilePanel.setCopyStatus(window, name, true)
end

-- [[Tier pill placement. The reference keeps the pill on the handle row, so
-- [[it rides just after the subtitle text; it only drops under the headline
-- [[when there is no subtitle, or when the pair would not fit before the
-- [[card's right edge. Re-run whenever the headline, subtitle or tier text
-- [[changes (the pill's width is measured from its own label).]]
function profilePanel.layoutPremium(window)
    local badge = window.profilePremium
    local subtitle = window.profileSubtitle
    if not badge or not subtitle then
        return
    end
    local width = badge.Size.X.Offset
    if width <= 0 then
        return
    end
    local nameX = paddingX + avatarSize + 10
    local limit = profilePanel.width - paddingX - 8
    local belowY = 49
    local function inlineAfter(instance, y)
        local text = instance.Text
        local measured = if text == "" then 0 else functions.textWidth(window.theme.Font, instance.TextSize, text)
        local x = nameX + measured + 8
        if measured <= instance.Size.X.Offset and x + width <= limit then
            badge.Position = UDim2.fromOffset(x, y)
            return true
        end
        return false
    end
    if subtitle.Visible and subtitle.Text ~= "" then
        if inlineAfter(subtitle, subtitle.Position.Y.Offset - 0.5) then
            return
        end
        badge.Position = UDim2.fromOffset(nameX, belowY)
        return
    end
    if not inlineAfter(window.profileName, 13) then
        badge.Position = UDim2.fromOffset(nameX, belowY)
    end
end

-- [[Write every identity field on the card from the current player and the]]
-- [[reveal toggle: display name, @username, premium pill, user ID (with its]]
-- [[COPY action), place ID, job ID and the license rows. nil-safe on both]]
-- [[the instances and the player, so a missing player leaves placeholders]]
-- [[rather than erroring.]]
function profilePanel.applyIdentity(window)
    local player = state.localPlayer
    local reveal = profilePanel.revealEnabled(window)
    local data = window.profileData or {}

    local username = ""
    local displayName = ""
    if player then
        username = tostring(player.Name or "")
        displayName = tostring(player.DisplayName or "")
        if displayName == "" then
            displayName = username
        end
    end

    local name = window.profileName
    if name then
        setText(
            name,
            if displayName == "" then ""
                elseif reveal then displayName
                else sidebar.maskUsername(displayName)
        )
    end

    local subtitle = window.profileSubtitle
    if subtitle then
        -- [[An explicit Window:SetProfile subtitle wins over the generated
        -- [[@username: it is the developer's own copy, not identity data, so
        -- [[the reveal toggle leaves it untouched.]]
        local custom = if type(data.subtitle) == "string" and data.subtitle ~= "" then data.subtitle else nil
        local text = if custom then locale.resolve(custom) else nil
        if text == nil and username ~= "" then
            text = "@" .. (if reveal then username else sidebar.maskUsername(username))
        end
        setText(subtitle, text or "")
        subtitle.Visible = subtitle.Text ~= ""
    end
    profilePanel.applyTier(window)

    local userId = window.profileUserIdValue
    if userId then
        local id = if player and player.UserId then tostring(player.UserId) else ""
        setText(userId, if id == "" then fallbackValue elseif reveal then id else maskedId)
    end

    -- [[Copy actions ride only on values the reveal toggle unmasks: with the]]
    -- [[detail masked there is nothing meaningful to copy, so the buttons]]
    -- [[hide with it.]]
    for name, entry in pairs(window.profileCopyButtons or {}) do
        local shown = false
        if reveal then
            if name == "UserId" then
                shown = player ~= nil
            elseif name == "PlaceId" then
                shown = getPlaceId() ~= nil
            elseif name == "ServerId" then
                shown = getJobId() ~= nil and not isStudio()
            elseif name == "Key" then
                local key = (window.profileData or {}).key
                shown = type(key) == "string" and key ~= ""
            end
        end
        if entry.button then
            entry.button.Visible = shown
        end
    end

    local placeId = window.profilePlaceId
    if placeId then
        local id = getPlaceId()
        setText(placeId, if not id then fallbackValue elseif reveal then tostring(id) else maskedId)
    end

    local serverId = window.profileServerIdValue
    if serverId then
        local jobId = getJobId()
        if not jobId then
            -- [[Studio / local test servers have no JobId: say so instead of
            -- [[showing a stale or invented server.]]
            setText(serverId, if isStudio() then "Studio" else fallbackValue)
        else
            setText(serverId, if reveal then jobId else maskedId)
        end
    end

    profilePanel.applyAccountCreated(window)

    profilePanel.applyLicense(window)
end

-- [[Live values: player count and the current session. Called by the refresh
-- [[loop, by reveal changes and whenever the card comes back.]]
function profilePanel.applyLive(window)
    if window.unloaded or not window.profileDetails then
        return
    end

    local current, capacity = playerCounts()
    setText(
        window.profilePlayersValue,
        if current ~= nil and capacity ~= nil then `{current} / {capacity}` else fallbackValue
    )

    setText(window.profileSessionValue, formatDuration(sessionSeconds()))
    profilePanel.applyAccountCreated(window)

    -- [[A whitelist entry that expires at a timestamp counts down here too.]]
    profilePanel.applyLicense(window)
end

-- =========================================================================
-- Card construction
-- =========================================================================

local function sectionHeight(rowCount: number): number
    if rowCount <= 0 then
        return cardPaddingY * 2
    end
    return cardPaddingY * 2 + rowCount * rowHeight + (rowCount - 1) * rowGap
end

local function addRow(window, card, spec)
    local fade = function(instance, prop, shown)
        table.insert(window.profileFadeTargets, { instance = instance, prop = prop, shown = shown })
    end

    local y = spec.y
    local x = spec.x or cardPaddingX
    local textX = x
    local rowLabelWidth = spec.labelWidth or labelWidth
    -- [[Row icons follow the window's element icons: ContentColor, revealed]]
    -- [[to the section icon's transparency so a compact row reads like a]]
    -- [[small element line.]]
    if spec.icon then
        local icon = window:Create("ImageLabel", {
            Name = spec.name .. "Icon",
            Image = cardIcon(window, spec.icon),
            Position = UDim2.fromOffset(x, y + (rowHeight - rowIconSize) / 2),
            Size = UDim2.fromOffset(rowIconSize, rowIconSize),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            ImageTransparency = 1,
            Parent = card,
        }, { ImageColor3 = spec.iconColor or "ContentColor" })
        fade(icon, "ImageTransparency", iconTransparency)
        textX = x + rowIconSize + 5
    end

    local label = window:Create("TextLabel", {
        Name = spec.name .. "Label",
        Text = spec.label,
        Position = UDim2.fromOffset(textX, y),
        Size = UDim2.fromOffset(rowLabelWidth, rowHeight),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextTruncate = Enum.TextTruncate.AtEnd,
        TextTransparency = 1,
        Parent = card,
    }, { TextColor3 = spec.labelColor or "ContentColor", FontFace = "Font" })
    fade(label, "TextTransparency", labelTransparency)

    local inset = if spec.action then actionInset else 0
    local value = window:Create("TextLabel", {
        Name = spec.instanceName or (spec.name .. "Value"),
        Text = fallbackValue,
        Position = UDim2.fromOffset(textX + rowLabelWidth, y),
        Size = UDim2.new(1, -(textX + rowLabelWidth + cardPaddingX + inset), 0, rowHeight),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = spec.valueSize or 12,
        TextXAlignment = Enum.TextXAlignment.Right,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextTruncate = Enum.TextTruncate.AtEnd,
        TextTransparency = 1,
        Parent = card,
    }, { TextColor3 = spec.valueColor or "ContentColor", FontFace = "Font" })
    fade(value, "TextTransparency", nameTransparency)

    if spec.action == "copy" then
        local button = window:Create("TextButton", {
            Name = "Copy" .. spec.name,
            -- [[The button is glyph-only: its Text is never drawn (the
            -- [[transparency above stays 1) and carries the button's own
            -- [[accessible label instead — "Copy Job ID" while idle, "Copied"
            -- [[after a successful press; setCopyStatus keeps it in step.
            Text = "",
            Position = UDim2.new(1, -(cardPaddingX + actionSize), 0, y + (rowHeight - actionSize) / 2),
            Size = UDim2.fromOffset(actionSize, actionSize),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            TextTransparency = 1,
            Visible = false,
            Parent = card,
        })
        local iconLabel = window:Create("ImageLabel", {
            Name = "Icon",
            Image = cardIcon(window, "copy"),
            Size = UDim2.fromOffset(13, 13),
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.fromScale(0.5, 0.5),
            BackgroundTransparency = 1,
            ImageTransparency = 1,
            Parent = button,
        }, { ImageColor3 = "ActionColor" })
        fade(iconLabel, "ImageTransparency", 0.15)
        window.profileCopyButtons = window.profileCopyButtons or {}
        window.profileCopyButtons[spec.name] = {
            button = button,
            icon = iconLabel,
            -- [[What this press copies, for the label.]]
            label = spec.label,
        }
        profilePanel.setCopyStatus(window, spec.name, false)
        if spec.name == "UserId" then
            -- [[Legacy handle, kept so hosts and tests that grabbed the]]
            -- [[user ID's copy button still find it.]]
            window.profileCopyUserId = button
        end
        -- [[No hover surface on the button: the glyph and the green check are
        -- [[the whole story on press, and the row's own truncation hover-help
        -- [[below stays the card's only tooltip.]]
        window:Connect(button.Activated, function()
            -- [[The action only exists while the value is revealed: with the]]
            -- [[detail hidden there is nothing meaningful to copy, and the]]
            -- [[button is hidden too (applyIdentity). A successful copy runs]]
            -- [[through the executor's clipboard and flashes the green check.]]
            if not profilePanel.revealEnabled(window) then
                return
            end
            local text = if spec.copy then spec.copy() else nil
            if type(text) ~= "string" or text == "" then
                return
            end
            if clipboardSet(text) then
                profilePanel.flashCopied(window, spec.name)
            end
        end)
    end

    -- [[Hover-help for values that do not fit: the row's full text is shown in
    -- [[a small themed tooltip above (or below) the row.]]
    local tooltipY = (spec.cardY or 0) + y
    window:Connect(value.MouseEnter, function()
        local text = value.Text
        if text == "" or text == fallbackValue then
            return
        end
        local available = value.AbsoluteSize.X
        local measured = functions.textWidth(window.theme.Font, value.TextSize, text)
        if available <= 0 or measured <= available then
            return
        end
        profilePanel.showTooltip(window, text, tooltipY)
    end)
    window:Connect(value.MouseLeave, function()
        profilePanel.hideTooltip(window)
    end)

    return value
end

-- [[Position and reveal the hover tooltip. It lives on the card surface (not
-- [[inside the scroll region) so it is never clipped by the scrolling area;
-- [[`detailsY` is the row's offset from the top of the card's content.]]
function profilePanel.showTooltip(window, text, contentY: number)
    local tip = window.profileTooltip
    if not tip or not window.profileTooltipLabel then
        return
    end
    local details = window.profileDetails
    local scrollY = 0
    if details then
        local ok, canvasPosition = pcall(function()
            return details.CanvasPosition
        end)
        if ok and canvasPosition and typeof(canvasPosition) ~= "number" then
            scrollY = canvasPosition.Y or 0
        end
    end
    local cardH = currentCardHeight(window)
    local y = headerHeight + dividerHeight + (contentY - scrollY) - tooltipHeight - 6
    y = math.clamp(y, 4, math.max(4, cardH - tooltipHeight - 4))
    setText(window.profileTooltipLabel, text)
    tip.Position = UDim2.fromOffset(paddingX + cardPaddingX, y)
    tip.Visible = true
    motion.tween(tip, { BackgroundTransparency = 0 }, hoverTween)
    motion.tween(window.profileTooltipLabel, { TextTransparency = 0 }, hoverTween)
end

function profilePanel.hideTooltip(window)
    local tip = window.profileTooltip
    if not tip or not tip.Visible then
        return
    end
    motion.tween(tip, { BackgroundTransparency = 1 }, hoverTween)
    if window.profileTooltipLabel then
        motion.tween(window.profileTooltipLabel, { TextTransparency = 1 }, hoverTween)
    end
    task.delay(hoverTween.Time, function()
        if tip.Parent and tip.BackgroundTransparency >= 1 then
            tip.Visible = false
        end
    end)
end

-- [[The card used to be built for every window - dozens of themed instances
-- [[on the spawn path - even though "Show profile" ships switched off. It is
-- [[now built on demand: the first time the setting is on (from disk during
-- [[construction, or when the user flips the toggle later). Every other
-- [[function in this module reads the card's instances through nil guards, so
-- [[an unbuilt card is simply inert. Returns the panel (or nil when the card
-- [[is disabled/gone).
function profilePanel.ensureBuilt(window)
    if window.profilePanel then
        return window.profilePanel
    end
    if window.unloaded or window.settings.showProfile ~= true then
        return nil
    end
    return profilePanel.build(window)
end

function profilePanel.build(window)
    if window.profilePanel then
        window:DestroySubtree(window.profilePanel)
        window.profilePanel = nil
    end

    local player = state.localPlayer
    local cardHeight = currentCardHeight(window)
    profilePanel.height = cardHeight
    window.profileFadeTargets = {}

    window.profilePanel = window:Create("Frame", {
        Name = "ProfilePanel",
        AnchorPoint = Vector2.new(0, 0),
        Position = UDim2.fromOffset(0, 0),
        Size = UDim2.fromOffset(profilePanel.width, cardHeight),
        BackgroundTransparency = 1,
        ZIndex = 1,
        Visible = false,

        Parent = window.screenGui,
    })

    -- [[The card surface: the window's own background, not a lookalike. It is
    -- [[built by Window:StyleWindowSurface — the very call that builds the
    -- [[window frame's background — so the card takes the same base colour, the
    -- [[same WindowColor theme token, the same gradient rotation/offset and the
    -- [[same opacity the window shows, and a theme change moves both together
    -- [[(both gradients are registered theme bindings). The card is centred on
    -- [[the window's own height, so the shared gradient also lands on the same
    -- [[shade at the same screen row: the two read as one surface, never as a
    -- [[panel that is nearly the window's colour.]]
    window.profilePanelSurface = window:Create("Frame", {
        Name = "Surface",
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        BackgroundTransparency = window:WindowSurfaceTransparency(),
        Parent = window.profilePanel,
    })

    -- [[Kept on the window so the window's live gradient animation can carry
    -- [[the card with it (see Window:_syncLiveAnimation). A card rebuilt in the
    -- [[middle of that animation starts from the window's current gradient
    -- [[state rather than from the resting one, so a rebuild cannot leave the
    -- [[pair mismatched until the next cycle.]]
    window.profilePanelGradient = window:StyleWindowSurface(window.profilePanelSurface)
    local liveGradient = window.windowGradient
    if liveGradient then
        window.profilePanelGradient.Rotation = liveGradient.Rotation
        window.profilePanelGradient.Offset = liveGradient.Offset
    end

    window.profilePanelCorner = window:Create("UICorner", {
        Parent = window.profilePanelSurface,
    }, { CornerRadius = "CornerRoundness" })

    window.profilePanelStroke = window:Create("UIStroke", {
        Transparency = 1,
        Parent = window.profilePanelSurface,
    }, { Color = "SurfaceStroke" })

    window.profilePanelGlow = window:CreateGlow(window.profilePanelSurface, "ShadowColor", 20, 1)

    -- [[Everything the card fades in/out as a unit registers here once: the
    -- [[panel show/hide pass then just walks the list.]]
    local function fade(instance, prop, shown)
        table.insert(window.profileFadeTargets, { instance = instance, prop = prop, shown = shown })
    end

    -- ===== header: avatar + presence, name / username / premium, gear =====

    window.profileAvatar = window:Create("ImageLabel", {
        Name = "Avatar",
        Position = UDim2.fromOffset(paddingX, avatarTop),
        Size = UDim2.fromOffset(avatarSize, avatarSize),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        ImageTransparency = 1,
        Parent = window.profilePanelSurface,
    }, { BackgroundColor3 = "ContentColor" })

    window:Create("UICorner", {
        CornerRadius = UDim.new(1, 0),
        Parent = window.profileAvatar,
    })
    window:Create("UIStroke", {
        Thickness = 1,
        Transparency = 0.4,
        Parent = window.profileAvatar,
    }, { Color = "SurfaceStroke" })

    local onlineDot = window:Create("Frame", {
        Name = "Online",
        AnchorPoint = Vector2.new(1, 1),
        Position = UDim2.fromOffset(paddingX + avatarSize, avatarTop + avatarSize),
        Size = UDim2.fromOffset(12, 12),
        BorderSizePixel = 0,
        Parent = window.profilePanelSurface,
    }, { BackgroundColor3 = "Success" })
    window:Create("UICorner", { CornerRadius = UDim.new(1, 0), Parent = onlineDot })
    window:Create("UIStroke", {
        Thickness = 2,
        Transparency = 0,
        Parent = onlineDot,
    }, { Color = "Background" })

    local nameX = paddingX + avatarSize + 10
    -- [[No gear occupies the header's right edge any more, so the handle]]
    -- [[text may run to the card's own padding.]]
    local nameWidth = profilePanel.width - nameX - paddingX

    -- [[The headline/subtitle are filled by applyIdentity at the end of the
    -- [[build, so the card never flashes an unmasked display name first.]]
    window.profileName = window:Create("TextLabel", {
        Name = "Name",
        Text = "",
        Position = UDim2.fromOffset(nameX, 12),
        Size = UDim2.fromOffset(nameWidth, 18),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextWrapped = false,
        TextTruncate = Enum.TextTruncate.AtEnd,
        TextTransparency = 1,
        Parent = window.profilePanelSurface,
    }, { TextColor3 = "TitlingColor", FontFace = "Font" })

    fade(window.profileName, "TextTransparency", nameTransparency)

    window.profileSubtitle = window:Create("TextLabel", {
        Name = "Subtitle",
        Text = "",
        Position = UDim2.fromOffset(nameX, 31),
        Size = UDim2.fromOffset(nameWidth, 15),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextWrapped = false,
        TextTruncate = Enum.TextTruncate.AtEnd,
        Visible = false,
        TextTransparency = 1,
        Parent = window.profilePanelSurface,
    }, { TextColor3 = "TitlingColor", FontFace = "Font" })

    fade(window.profileSubtitle, "TextTransparency", subtitleTransparency)

    -- [[Tier pill: always present, because a tier always exists — the host's]]
    -- [[own word from Window:SetProfile, else PREMIUM / FREEMIUM from the]]
    -- [[platform membership (applyTier writes word, icon, colour and width).]]
    -- [[The pill width is measured from its own label, so it only ever grows]]
    -- [[as wide as its word needs.]]
    local premium = window:Create("Frame", {
        Name = "TierBadge",
        Position = UDim2.fromOffset(nameX, 49),
        Size = UDim2.fromOffset(tierPaddingLeft + tierIconSize + tierGap + tierPaddingRight, tierHeight),
        BorderSizePixel = 0,
        Visible = true,
        Parent = window.profilePanelSurface,
    }, { BackgroundColor3 = "ElementSurface", BackgroundTransparency = "ElementTransparency" })
    window.profilePremium = premium
    window:Create("UICorner", {
        Parent = premium,
    }, { CornerRadius = "PillCornerRadius" })
    window:Create("UIStroke", {
        Transparency = 0.35,
        Parent = premium,
    }, { Color = "ElementStroke" })
    local premiumIcon = window:Create("ImageLabel", {
        Name = "Crown",
        -- applyTier selects the correct icon; do not start a competing crown load.
        Image = "",
        Position = UDim2.fromOffset(tierPaddingLeft, (tierHeight - tierIconSize) / 2),
        Size = UDim2.fromOffset(tierIconSize, tierIconSize),
        BackgroundTransparency = 1,
        ImageTransparency = 1,
        Parent = premium,
    }, { ImageColor3 = "AccentColor" })
    local premiumLabel = window:Create("TextLabel", {
        Name = "Label",
        Text = "",
        Position = UDim2.fromOffset(tierPaddingLeft + tierIconSize + tierGap, 0),
        Size = UDim2.fromOffset(0, tierHeight),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = 9,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextTransparency = 1,
        Parent = premium,
    }, { TextColor3 = "AccentColor", FontFace = "Font" })
    window.profileTierIcon = premiumIcon
    window.profileTierLabel = premiumLabel
    fade(premiumIcon, "ImageTransparency", 0)
    fade(premium, "BackgroundTransparency", 0)
    fade(premiumLabel, "TextTransparency", 0)

    -- [[Header hover-help for a truncated display name.]]
    window:Connect(window.profileName.MouseEnter, function()
        local text = window.profileName.Text
        if text == "" then
            return
        end
        local available = window.profileName.AbsoluteSize.X
        if available <= 0 or functions.textWidth(window.theme.Font, window.profileName.TextSize, text) <= available then
            return
        end
        profilePanel.showTooltip(window, text, 12)
    end)
    window:Connect(window.profileName.MouseLeave, function()
        profilePanel.hideTooltip(window)
    end)

    -- [[Divider under the pinned header.]]
    window.profileDivider = window:Create("Frame", {
        Name = "Divider",
        Position = UDim2.fromOffset(paddingX, headerHeight),
        Size = UDim2.new(1, -paddingX * 2, 0, dividerHeight),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        Parent = window.profilePanelSurface,
    }, { BackgroundColor3 = "ElementStroke" })
    table.insert(window.profileFadeTargets, {
        instance = window.profileDivider,
        prop = "BackgroundTransparency",
        shown = 0.5,
    })

    local function label(parent, name, text, position, size, textSize, color, align, transparency)
        local instance = window:Create("TextLabel", {
            Name = name,
            Text = text,
            Position = position,
            Size = size,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            TextSize = textSize,
            TextXAlignment = align or Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Center,
            TextTruncate = Enum.TextTruncate.AtEnd,
            TextTransparency = 1,
            Parent = parent,
        }, { TextColor3 = color, FontFace = "Font" })
        fade(instance, "TextTransparency", transparency or nameTransparency)
        return instance
    end

    -- ===== scrollable details =====

    window.profileDetails = window:Create("ScrollingFrame", {
        Name = "Details",
        Position = UDim2.fromOffset(0, headerHeight + dividerHeight),
        Size = UDim2.new(1, 0, 1, -(headerHeight + dividerHeight)),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 0,
        ScrollBarImageColor3 = Color3.fromRGB(255, 255, 255),
        ScrollBarImageTransparency = scrollBarTransparency,
        ScrollingDirection = Enum.ScrollingDirection.Y,
        ClipsDescendants = true,
        Parent = window.profilePanelSurface,
    })

    local content = window:Create("Frame", {
        Name = "Content",
        Position = UDim2.fromOffset(0, scrollPaddingTop),
        Size = UDim2.new(1, 0, 0, 0),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Parent = window.profileDetails,
    })
    window.profileDetailsContent = content

    -- [[Section heading: the window's Section element, verbatim — a 16px]]
    -- [[ContentColor icon beside a 15px ContentColor title at the section's]]
    -- [[own transparencies, so the card titles its groups exactly the way]]
    -- [[the window titles its tabs' sections.]]
    local function heading(y: number, title: string, iconName: string): number
        local icon = window:Create("ImageLabel", {
            Name = title .. "Icon",
            Image = cardIcon(window, iconName),
            Position = UDim2.fromOffset(paddingX, y + (headingHeight - headingIconSize) / 2),
            Size = UDim2.fromOffset(headingIconSize, headingIconSize),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            ImageTransparency = 1,
            Parent = content,
        }, { ImageColor3 = "ContentColor" })
        fade(icon, "ImageTransparency", iconTransparency)

        label(
            content,
            title,
            title,
            UDim2.fromOffset(paddingX + headingIconSize + headingIconGap, y),
            UDim2.new(1, -(paddingX * 2 + headingIconSize + headingIconGap), 0, headingHeight),
            15,
            "ContentColor",
            Enum.TextXAlignment.Left,
            headingTransparency
        )
        return y + headingHeight + headingGap
    end

    -- [[A section's plate: Window:StyleElementBody, verbatim — the element]]
    -- [[gradient over a white body at ElementTransparency, the element]]
    -- [[corner, and the element stroke at the theme's own transparency, so]]
    -- [[every container on the card IS one of the window's element bodies.]]
    local function card(y: number, name: string, height: number)
        local frame = window:Create("Frame", {
            Name = name,
            Position = UDim2.fromOffset(paddingX, y),
            Size = UDim2.new(1, -paddingX * 2, 0, height),
            BackgroundColor3 = Color3.fromRGB(255, 255, 255),
            BorderSizePixel = 0,
            Parent = content,
        }, { BackgroundTransparency = "ElementTransparency" })
        local stroke = window:StyleElementBody(frame)
        fade(frame, "BackgroundTransparency", 0)
        fade(stroke, "Transparency", window.theme.ElementStrokeTransparency or 0)
        return frame
    end

    local y = 0

    -- [[Pinned header done; give the frame back before the detail cards if
    -- [[the budget was spent. Unconditional pacing: the card may be building
    -- [[lazily on a toggle click, when startup checkpoints have gone quiet.]]
    window:_paceBudget()

    -- ===== ACCOUNT =====

    local accountRows = {
        {
            name = "UserId",
            label = "User ID",
            icon = "id-card",
            action = "copy",
            copy = function()
                local current = state.localPlayer
                return if current and current.UserId then tostring(current.UserId) else nil
            end,
        },
        { name = "JoinDate", label = "Account created", icon = "calendar-days", labelWidth = 88, valueSize = 11 },
        { name = "AccountAge", label = "Elapsed", icon = "hourglass" },
        {
            name = "Key",
            label = "Key",
            icon = "key-round",
            labelWidth = 62,
            valueSize = 12,
            action = "copy",
            copy = function()
                local key = (window.profileData or {}).key
                return if type(key) == "string" and key ~= "" then key else nil
            end,
        },
        { name = "Whitelist", label = "Whitelist", icon = "shield-check", labelWidth = 62, valueSize = 12 },
    }

    y = heading(y, "Account", "user-round")
    local accountCardY = y
    local accountCard = card(y, "AccountCard", sectionHeight(#accountRows))
    for index, row in ipairs(accountRows) do
        local value = addRow(window, accountCard, {
            name = row.name,
            label = row.label,
            icon = row.icon,
            action = row.action,
            copy = row.copy,
            instanceName = row.instanceName,
            labelWidth = row.labelWidth,
            valueSize = row.valueSize,
            y = cardPaddingY + (index - 1) * (rowHeight + rowGap),
            cardY = accountCardY,
        })
        if row.name == "UserId" then
            window.profileUserIdValue = value
        elseif row.name == "JoinDate" then
            window.profileJoinDateValue = value
        elseif row.name == "AccountAge" then
            window.profileAccountAgeValue = value
        elseif row.name == "Key" then
            window.profileKeyValue = value
        elseif row.name == "Whitelist" then
            window.profileWhitelistValue = value
        end
    end
    y = accountCardY + sectionHeight(#accountRows) + sectionGap

    -- ===== CURRENT GAME =====

    y = heading(y, "Current Game", "gamepad-2")
    local gameCardY = y
    local gameCardHeight = cardPaddingY * 2 + gameThumbnailSize
    local gameCard = card(y, "CurrentGame", gameCardHeight)

    local gameThumbnail = window:Create("ImageLabel", {
        Name = "GameThumbnail",
        Image = "",
        Position = UDim2.fromOffset(cardPaddingX, cardPaddingY),
        Size = UDim2.fromOffset(gameThumbnailSize, gameThumbnailSize),
        BorderSizePixel = 0,
        BackgroundTransparency = 0,
        Parent = gameCard,
    }, { BackgroundColor3 = "ElementSurface" })
    window:Create("UICorner", { Parent = gameThumbnail }, { CornerRadius = "ElementCornerRadius" })
    window:Create("UIStroke", {
        Parent = gameThumbnail,
    }, { Color = "ElementStroke", Transparency = "ElementStrokeTransparency" })

    local gameNameX = cardPaddingX + gameThumbnailSize + 10
    -- [[The DataModel's own name fills the row immediately; the official]]
    -- [[two-step lookup (place -> universe -> games list) replaces it with]]
    -- [[the real game name as soon as the platform answers.]]
    local gameName = label(
        gameCard,
        "GameName",
        tostring(game.Name or fallbackValue),
        UDim2.fromOffset(gameNameX, cardPaddingY),
        UDim2.new(1, -(gameNameX + cardPaddingX), 0, 17),
        13,
        "TitlingColor"
    )
    window.profileGameName = gameName
    window.profilePlaceId = addRow(window, gameCard, {
        name = "PlaceId",
        label = "Place ID",
        instanceName = "GameBy",
        y = cardPaddingY + 22,
        x = gameNameX,
        labelWidth = 52,
        cardY = gameCardY,
        action = "copy",
        copy = function()
            local id = getPlaceId()
            return if id then tostring(id) else nil
        end,
    })

    window:Connect(gameName.MouseEnter, function()
        local text = gameName.Text
        if text == "" or text == fallbackValue then
            return
        end
        local available = gameName.AbsoluteSize.X
        if available <= 0 or functions.textWidth(window.theme.Font, gameName.TextSize, text) <= available then
            return
        end
        profilePanel.showTooltip(window, text, gameCardY + cardPaddingY)
    end)
    window:Connect(gameName.MouseLeave, function()
        profilePanel.hideTooltip(window)
    end)

    y = gameCardY + gameCardHeight + sectionGap

    -- ===== SERVER =====

    local serverRows = {
        { name = "Players", label = "Players", icon = "users" },
        {
            -- [[The value is DataModel.JobId, so the row says so: "Job ID".
            -- [[(The row's internal name stays ServerId — hosts, saved state
            -- [[and the tests reach it as window.profileServerIdValue.)]]
            name = "ServerId",
            label = "Job ID",
            icon = "server",
            labelWidth = 64,
            action = "copy",
            copy = function()
                local jobId = getJobId()
                return if jobId and not isStudio() then jobId else nil
            end,
        },
    }

    y = heading(y, "Server", "server")
    local serverCardY = y
    local serverCard = card(y, "ServerCard", sectionHeight(#serverRows))
    for index, row in ipairs(serverRows) do
        local value = addRow(window, serverCard, {
            name = row.name,
            label = row.label,
            icon = row.icon,
            action = row.action,
            copy = row.copy,
            y = cardPaddingY + (index - 1) * (rowHeight + rowGap),
            cardY = serverCardY,
        })
        if row.name == "Players" then
            window.profilePlayersValue = value
        elseif row.name == "ServerId" then
            window.profileServerIdValue = value
        end
    end
    y = serverCardY + sectionHeight(#serverRows) + sectionGap

    -- ===== USER SESSION =====

    y = heading(y, "User Session", "clock")
    local sessionCardY = y
    local sessionCard = card(y, "SessionCard", sectionHeight(1))
    window.profileSessionValue = addRow(window, sessionCard, {
        name = "Session",
        label = "Time in server",
        icon = "activity",
        y = cardPaddingY,
        cardY = sessionCardY,
    })
    y = sessionCardY + sectionHeight(1) + sectionGap

    content.Size = UDim2.new(1, 0, 0, y)
    window.profileDetails.CanvasSize = UDim2.fromOffset(0, scrollPaddingTop + y + scrollPaddingBottom)

    -- [[Detail cards done; the tooltip and its wiring start on a fresh frame
    -- [[when the previous section spent the budget.]]
    window:_paceBudget()

    -- [[Hover tooltip surface. Parented to the card (not the scroll region) so
    -- [[it is never clipped by the scrolling area.]]
    local tooltip = window:Create("Frame", {
        Name = "Tooltip",
        Position = UDim2.fromOffset(paddingX + cardPaddingX, 40),
        Size = UDim2.new(1, -(paddingX * 2 + cardPaddingX * 2), 0, tooltipHeight),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Visible = false,
        -- [[The window's ScreenGui stacks by Global z-index, so this surface]]
        -- [[has to outrank the card rows it floats over...]]
        ZIndex = 10,
        Parent = window.profilePanelSurface,
    }, { BackgroundColor3 = "PopupSurface" })
    window.profileTooltip = tooltip
    window:Create("UICorner", { CornerRadius = UDim.new(0, 6), Parent = tooltip })
    window:Create("UIStroke", {
        Transparency = 0.25,
        Parent = tooltip,
    }, { Color = "ElementStroke" })
    window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, 6),
        PaddingRight = UDim.new(0, 6),
        PaddingTop = UDim.new(0, 4),
        PaddingBottom = UDim.new(0, 4),
        Parent = tooltip,
    })
    window.profileTooltipLabel = window:Create("TextLabel", {
        Name = "Label",
        Text = "",
        Size = UDim2.fromScale(1, 1),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = 11,
        TextWrapped = true,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        TextTruncate = Enum.TextTruncate.AtEnd,
        TextTransparency = 1,
        -- [[... and under Global stacking a parent draws over any child with
        -- [[a lower z-index, so the label has to outrank its own surface or
        -- [[the tooltip shows as an empty box with the text buried under it.
        ZIndex = 11,
        Parent = tooltip,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    -- [[The panel is a sibling of the window frame: mirroring the frame's]]
    -- [[position keeps it following drags, restores and resizes without a]]
    -- [[per-frame loop.]]
    window:Connect(window.main:GetPropertyChangedSignal("Position"), function()
        profilePanel.layout(window)
    end)
    window:Connect(window.main:GetPropertyChangedSignal("Size"), function()
        profilePanel.layout(window)
    end)

    -- [[Live values (player count, uptime, session) refresh on a slow tick
    -- [[while the card is on screen; the connection is created once per window
    -- [[so rebuilds cannot stack loops.]]
    if not window.profileRefreshConnection then
        local elapsed = 0
        window.profileRefreshConnection = window:Connect(state.runService.Heartbeat, function(dt: number)
            if window.unloaded then
                return
            end
            elapsed += dt
            if elapsed < refreshInterval then
                return
            end
            elapsed = 0
            if profilePanel.isShown(window) then
                profilePanel.applyLive(window)
            end
        end)
    end

    -- [[Avatar: same generation guard as the rest of the card so a rebuilt or
    -- [[unloaded panel never receives a stale image.]]
    buildGeneration += 1
    local generation = buildGeneration
    window.profileBuildGeneration = generation
    gameThumbnailGenerations[gameThumbnail] = generation

    local function safeAssign(url: string)
        if window.unloaded or gameThumbnailGenerations[gameThumbnail] ~= generation then
            return
        end
        if not gameThumbnail.Parent then
            return
        end
        image.assign(gameThumbnail, "Image", url)
    end

    -- [[Game thumbnail: the universe id comes from DataModel.GameId when the]]
    -- [[runtime supplies one, otherwise from the official place -> universe]]
    -- [[lookup; the legacy rbxthumb URI is the last-resort fallback.]]
    local fallbackUrl = buildFallbackUrl(getPlaceId())
    resolveUniverseId(function(universeId: number?)
        if not universeId then
            if fallbackUrl ~= "" then
                safeAssign(fallbackUrl)
            end
            return
        end
        local cachedIcon = resolvedGameIcons[universeId]
        if type(cachedIcon) == "string" and cachedIcon ~= "" then
            safeAssign(cachedIcon)
        else
            fetchOfficialGameIcon(universeId, function(officialUrl: string?)
                if officialUrl and officialUrl ~= "" then
                    safeAssign(officialUrl)
                elseif fallbackUrl ~= "" then
                    safeAssign(fallbackUrl)
                end
            end)
        end
    end)

    -- [[The official game name (place -> universe -> games list). The label]]
    -- [[keeps the DataModel's own name until the platform answers, and a]]
    -- [[cached answer lands immediately on the next build.]]
    local placeId = getPlaceId()
    if placeId then
        local cachedName = resolvedGameNames[placeId]
        if type(cachedName) == "string" and cachedName ~= "" then
            setText(gameName, cachedName)
        else
            fetchGameName(placeId, function(officialName: string?)
                if window.unloaded or gameThumbnailGenerations[gameThumbnail] ~= generation then
                    return
                end
                if type(officialName) == "string" and officialName ~= "" then
                    setText(gameName, officialName)
                end
            end)
        end
    end

    if player then
        local avatarGeneration = generation
        avatarGenerations[window.profileAvatar] = avatarGeneration

        local function applyAvatar(avatarLabel, uri)
            if avatarGenerations[avatarLabel] ~= avatarGeneration then
                return
            end
            if uri ~= nil and uri ~= "" then
                avatarReady[avatarLabel] = true
                image.assign(avatarLabel, "Image", uri)
                -- [[The avatar may finish loading after the panel already
                -- [[faded in: reveal it now instead of waiting for the
                -- [[next show/hide cycle.]]
                if window.profilePanel and window.profilePanel.Visible then
                    motion.tween(avatarLabel, { ImageTransparency = 0 }, settleTween)
                end
            else
                avatarReady[avatarLabel] = nil
                image.assign(avatarLabel, "Image", fallbackUri)
            end
        end

        local cachedUri = image.avatar(player.UserId, function(uri)
            if window.unloaded then
                return
            end
            local avatar = window.profileAvatar
            if not avatar then
                return
            end
            applyAvatar(avatar, uri)
        end)

        if cachedUri ~= "" then
            applyAvatar(window.profileAvatar, cachedUri)
        end

    end

    profilePanel.applyIdentity(window)
    profilePanel.applyLive(window)
    profilePanel.layout(window)
    return window.profilePanel
end

-- [[Place the card beside the window frame, vertically centred on the]]
-- [[window's centre. The frame's position may carry scale (the centred]]
-- [[resting position) or be pure offset after a drag: the scale passes]]
-- [[through untouched, the offset gets the half-window + gap edge. The]]
-- [[card's height is derived from window.size.Y.Offset so it never exceeds]]
-- [[the window and aligns top/bottom edges with the window at default size.]]
function profilePanel.layout(window)
    local panel = window.profilePanel
    if not panel then
        return
    end

    local main = window.main
    local pos = main.Position
    local width = main.Size.X.Offset
    local left = (window.settings.profileSide or "right") == "left"
    local edge = width / 2 + profilePanel.gap
    local cardH = currentCardHeight(window)
    profilePanel.height = cardH

    panel.AnchorPoint = Vector2.new(if left then 1 else 0, 0)
    panel.Position = UDim2.new(
        pos.X.Scale,
        pos.X.Offset + (if left then -edge else edge),
        pos.Y.Scale,
        pos.Y.Offset - cardH / 2
    )
    panel.Size = UDim2.fromOffset(profilePanel.width, cardH)

    -- [[The details region takes whatever the pinned header leaves. The
    -- [[scrollbar only appears when the sections are taller than that region,
    -- [[and the content narrows by its width so nothing slides underneath.]]
    local details = window.profileDetails
    local content = window.profileDetailsContent
    if not (details and content) then
        return
    end
    local regionHeight = math.max(0, cardH - headerHeight - dividerHeight)
    details.Size = UDim2.new(1, 0, 0, regionHeight)
    local scrollable = content.Size.Y.Offset > math.max(0, regionHeight - scrollPaddingTop - scrollPaddingBottom)
    details.ScrollBarThickness = if scrollable then scrollBarThickness else 0
    content.Size = UDim2.new(1, if scrollable then -scrollBarThickness else 0, 0, content.Size.Y.Offset)
end

function profilePanel.setShown(window, shown, tweenInfo)
    local panel = window.profilePanel
    if not panel then
        return
    end

    -- [[Window visibility wins over the requested state: never fade the]]
    -- [[panel in while the window is hidden or minimised.]]
    shown = shown == true
        and profilePanel.isEnabled(window)
        and not window.hidden
        and not window.minimised

    local avatar = window.profileAvatar
    local stroke = window.profilePanelStroke
    local glow = window.profilePanelGlow

    panel.Visible = shown

    if not (avatar and stroke) then
        return
    end

    if shown then
        -- [[Fresh values the moment the card (re)appears.]]
        profilePanel.applyLive(window)
    end

    local hasImage = avatarReady[avatar] == true
        or (type(avatar.Image) == "string" and avatar.Image ~= "" and avatar.Image ~= fallbackUri)

    local targets: { [Instance]: { [string]: any } } = {
        [avatar] = {
            ImageTransparency = if shown and hasImage then 0 else 1,
            BackgroundTransparency = if shown then avatarBgTransparency else 1,
        },
        [stroke] = { Transparency = if shown then idleStrokeTransparency else 1 },
    }

    -- [[The card's own plate fades with the window's: while the window is]]
    -- [[still fading in its background is part-transparent, so the card beside]]
    -- [[it must be too — otherwise the card reads as a solid panel floating]]
    -- [[next to a half-faded window.]]
    local surface = window.profilePanelSurface
    if surface then
        targets[surface] = {
            BackgroundTransparency = if shown then window:WindowSurfaceTransparency() else 1,
        }
    end

    if glow then
        targets[glow] = { Transparency = if shown then glowTransparency else 1 }
    end

    for _, entry in ipairs(window.profileFadeTargets or {}) do
        local props = targets[entry.instance]
        if not props then
            props = {}
            targets[entry.instance] = props
        end
        props[entry.prop] = if shown then entry.shown else 1
    end

    for instance, props in targets do
        local needsUpdate = false
        for prop, value in props do
            if instance[prop] ~= value then
                needsUpdate = true
                break
            end
        end
        if not needsUpdate then
            continue
        end
        if tweenInfo then
            motion.tween(instance, props, tweenInfo)
        else
            for prop, value in props do
                instance[prop] = value
            end
        end
    end
end

function profilePanel.setEnabled(window, enabled)
    window.settings.showProfile = enabled == true

    -- [[The card is built on demand the first time the setting is switched
    -- [[on, so the default-off window never paid for its dozens of instances.
    -- [[Build before the writers below run: applyIdentity fills the fresh
    -- [[card and setShown fades it in as part of the same toggle.]]
    if window.settings.showProfile then
        profilePanel.ensureBuilt(window)
        -- [[Player not replicated yet? The startup waiter only armed while
        -- [[construction knew the setting was on; a card enabled later gets
        -- [[its own waiter so it still reveals on arrival.]]
        if not state.localPlayer and window._armProfilePlayerWaiter then
            window:_armProfilePlayerWaiter()
        end
    end

    -- [[The reveal toggle depends on the card, so switching the card off]]
    -- [[switches the reveal off with it: it can never sit active while]]
    -- [[there is nothing on screen to reveal. The caller (the settings UI)]]
    -- [[rolls its own switch back from the return value.]]
    local revealCleared = false
    if not window.settings.showProfile and window.settings.showFullUsername then
        window.settings.showFullUsername = false
        revealCleared = true
    end
    profilePanel.applyIdentity(window)

    profilePanel.setShown(window, window.settings.showProfile, settleTween)
    window:_recenterForProfile(window.settings.showProfile)

    if revealCleared then
        window:Notify({
            title = locale.resolve("Reveal profile details"),
            content = locale.resolve(
                "Turned off with the profile card: it unmasks the values on that card, so it needs \"Show profile\" to be on."
            ),
        })
    end

    -- [[The toggle is only half the story: the card also needs a player and]]
    -- [[room on screen (260px beside the window, window-height tall). A missing]]
    -- [[player resolves itself through the late-player waiter in Window.new,]]
    -- [[but a small viewport would otherwise leave the user looking at an]]
    -- [[active toggle and no card — so say why.]]
    if window.settings.showProfile and state.localPlayer and not hasRoom(window) then
        window:Notify({
            title = locale.resolve("Profile card"),
            content = locale.resolve(
                "This screen has no room for the profile card, so it stays hidden. It needs 260px beside the window and enough height for the window — enlarge the window or rotate the device and it appears."
            ),
        })
    end

    return revealCleared
end

function profilePanel.setSide(window, side)
    window.settings.profileSide = if side == "left" then "left" else "right"
    profilePanel.layout(window)
    window:_recenterForProfile(true)
end

-- [[Window:SetProfile accepts either the legacy subtitle string / nil or a
-- [[table carrying everything the card's host-owned rows show:
-- [[  { subtitle = "…", key = "…", tier = "PREMIUM",
-- [[    whitelist = { status = "Active", daysLeft = 14 } }
-- [[`tier` drives the header pill (any word, uppercased on the card); with
-- [[none supplied the pill falls back to PREMIUM / FREEMIUM from the
-- [[platform membership.
-- [[`whitelist.expiresAt` (unix seconds) is accepted instead of `daysLeft` and
-- [[counted down live. Anything not supplied reads "—".]]
function profilePanel.setProfile(window, props)
    if type(props) ~= "table" then
        -- [[Legacy call shape: a subtitle string (or nil to clear it). The
        -- [[host's key / whitelist rows are left exactly as they were.]]
        return profilePanel.setSubtitle(window, props)
    end

    local data = {}
    if type(props) == "table" then
        local subtitle = props.subtitle or props.Subtitle
        if type(subtitle) == "string" and subtitle ~= "" then
            data.subtitle = subtitle
        end
        local key = props.key or props.Key
        if type(key) == "string" and key ~= "" then
            data.key = key
        end
        local tier = props.tier or props.Tier
        if type(tier) == "string" and tier ~= "" then
            data.tier = tier
        end
        local whitelist = props.whitelist or props.Whitelist
        if type(whitelist) == "table" then
            local status = whitelist.status or whitelist.Status
            local daysLeft = whitelist.daysLeft or whitelist.DaysLeft
            local expiresAt = whitelist.expiresAt or whitelist.ExpiresAt
            data.whitelist = {
                status = if type(status) == "string" and status ~= "" then status else nil,
                daysLeft = if type(daysLeft) == "number" then daysLeft else nil,
                expiresAt = if type(expiresAt) == "number" then expiresAt else nil,
            }
        end
    end
    window.profileData = data
    window.profileText = data.subtitle
    profilePanel.applyIdentity(window)
    profilePanel.applyLive(window)
    return data
end

-- [[Legacy setter (string / nil): swaps the subtitle while leaving any key,
-- [[tier or whitelist the host supplied in place.]]
function profilePanel.setSubtitle(window, text)
    if type(text) == "table" then
        return profilePanel.setProfile(window, text)
    end
    local data = window.profileData or {}
    -- [[Copy before mutating: the caller may still hold the table it passed.]]
    data = { subtitle = data.subtitle, key = data.key, tier = data.tier, whitelist = data.whitelist }
    data.subtitle = if type(text) == "string" and text ~= "" then text else nil
    window.profileData = data
    window.profileText = data.subtitle
    profilePanel.applyIdentity(window)
end

-- [[Kept as an alias: the reveal toggle no longer drives the name alone, so]]
-- [[refreshing the name refreshes the whole identity block.]]
function profilePanel.refreshName(window)
    profilePanel.applyIdentity(window)
end

return profilePanel

end)() end,
    [16] = function()local wax,script,require=ImportGlobals(16)local ImportGlobals return (function(...)
local utilities = script.Parent.Parent.utilities
local state = require(utilities.Parent.core.state)
local motion = require(utilities.motion)
local constants = require(utilities.constants)
local locale = require(utilities.locale)
local actionComponent = require(script.Parent.action)

local search = {}

-- [[The search overlay shares the library's hover spec for its fades; the
-- two task.delay beats below read the spec's duration so the pill never
-- hides while its fade is still running.]]
local HOVER_SPEC = "snappy"
local HOVER_SPEC_TIME = 0.25

local defaultTransparency = 0.6
local hoverTransparency = 0.2

local function getElementText(element)
    if element.__type == "Group" or element.__type == "CollapsibleGroup" then
        local names = { element.name or "" }
        local function collectNames(group)
            for _, child in group.elements do
                if child.__type == "Group" or child.__type == "CollapsibleGroup" then
                    collectNames(child)
                elseif child.name then
                    table.insert(names, child.name)
                end
            end
        end
        collectNames(element)
        return table.concat(names, "\n")
    end
    return element.name or ""
end

local function getAllElements(window)
    local elements = {}
    for _, tab in window.tabs do
        if tab.neglectSelector then
            continue
        end
        for _, element in tab.elements do
            if element.__type ~= "Section" then
                table.insert(elements, element)
            end
        end
    end
    return elements
end

local function setPillVisibility(window, visible, spec)
    motion.tween(window.searchPill, { BackgroundTransparency = if visible then 0.9 else 1 }, spec)
    motion.tween(window.searchStroke, { Transparency = if visible then 0.85 else 1 }, spec)
    motion.tween(window.searchShadow, { Transparency = if visible then 0.92 else 1 }, spec)
    motion.tween(window.searchIcon, { ImageTransparency = if visible then 0.65 else 1 }, spec)
    motion.tween(window.searchInput, { TextTransparency = if visible then 0.2 else 1 }, spec)
end

local function setTabVisibility(window, visible, tweenInfo)
    if window.layout.mode == "sidebar" then
        return
    end
    for _, tab in window.tabs do
        if not tab.neglectSelector and tab.topbarItem then
            tab:_applyVisual(
                if visible then (if window.selectedTab == tab then "selected" else "unselected") else "hidden",
                tweenInfo
            )
        end
    end
end

local function filterElements(window, query)
    query = string.lower(query or "")
    local hasVisible = false
    for _, unit in window._searchUnits do
        local isVisible = query == "" or string.find(unit.text, query, 1, true) ~= nil
        unit.unit.main.Visible = isVisible
        if unit.homeExpanded ~= nil then
            unit.unit:_setExpanded(if query ~= "" and isVisible then true else unit.homeExpanded, false)
        end
        hasVisible = hasVisible or isVisible
    end
    window.searchEmpty.Visible = not hasVisible and query ~= ""
end

local function collectElements(window)
    window._searchUnits = {}
    local order = 0

    -- [[Elements of tabs the player has not opened yet are still hidden (see
    -- [[`Window:_markTabElementsPending`). The search lays every element out on
    -- [[its own page, so bring them all in first - exactly the state the old
    -- [[reveal left them in.]]
    for _, tab in pairs(window.tabs) do
        if tab._elementsPending then
            window:_showTabElements(tab)
        end
    end
    for _, element in getAllElements(window) do
        order += 1
        local mainFrame = element.main
        table.insert(window._searchUnits, {
            unit = element,
            homeExpanded = if element.__type == "CollapsibleGroup" then element._expanded else nil,
            text = string.lower(getElementText(element)),
            homeParent = mainFrame.Parent,
            homeOrder = mainFrame.LayoutOrder,
        })

        mainFrame.LayoutOrder = order * 10
        mainFrame.Parent = window.searchPage
        mainFrame.Visible = true
    end
end

local function restoreElements(window)
    for _, unit in window._searchUnits do
        local element = unit.unit
        if unit.homeExpanded ~= nil then
            element:_setExpanded(unit.homeExpanded, false)
        end
        if element.main and element.main.Parent then
            element.main.LayoutOrder = unit.homeOrder
            element.main.Parent = unit.homeParent
            element.main.Visible = true
        end
    end
    window._searchUnits = {}
end

function search.open(window)
    if window._searching or window.minimised or not window:_interactive() then
        return
    end
    search.buildContent(window)
    window:_applyRailWidth()
    window._searching = true

    collectElements(window)
    filterElements(window, "")
    window:_jumpTo(window.searchPage)

    setTabVisibility(window, false, HOVER_SPEC)
    task.delay(HOVER_SPEC_TIME, function()
        if not window.unloaded and window._searching and window.layout.mode ~= "sidebar" then
            window.tabList.Visible = false
        end
    end)

    window.searchPill.Visible = true
    setPillVisibility(window, true, HOVER_SPEC)
    window.searchInput:CaptureFocus()

    motion.tween(window.searchAction.iconLabel, { ImageTransparency = hoverTransparency }, HOVER_SPEC)
end

function search.close(window, opts)
    if not window._searching then
        return
    end
    opts = opts or {}
    window._searching = false

    window.searchInput.Text = ""
    window.searchInput:ReleaseFocus()
    window.searchEmpty.Visible = false

    restoreElements(window)
    local jumpTarget = if opts.jumpTo == nil then window.selectedTab and window.selectedTab.tabPage else opts.jumpTo
    if jumpTarget then
        window:_jumpTo(jumpTarget)
    end

    setPillVisibility(window, false, HOVER_SPEC)
    task.delay(HOVER_SPEC_TIME, function()
        if not window.unloaded and not window._searching then
            window.searchPill.Visible = false
        end
    end)

    if opts.showTabs then
        if window.layout.mode == "sidebar" and window._applySettingsLayout then
            window:_applySettingsLayout(window._settingsMode)
        else
            window.tabList.Visible = true
        end
        setTabVisibility(window, true, HOVER_SPEC)
    end

    motion.tween(window.searchAction.iconLabel, { ImageTransparency = defaultTransparency }, HOVER_SPEC)
end

function search.toggle(window)
    if window._searching then
        search.close(window, { showTabs = true })
    else
        search.open(window)
    end
end

function search.railWidth(window, railWidth)
    if window.searchPill then
        window.searchPill.Size = UDim2.new(1, -(railWidth + 30), 0, 35)
    end
end

function search.buildContent(window)
    if window.searchPage then
        return
    end

    window.searchPage = window:Create("ScrollingFrame", {
        Name = "Search",
        Size = UDim2.new(1, -20, 1, 0),
        Position = UDim2.new(0, 0, 0, 0),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 0,
        ScrollingDirection = Enum.ScrollingDirection.Y,
        LayoutOrder = 2000,
        Parent = window.elements,
    })

    window:Create("UIListLayout", {
        Padding = UDim.new(0, 7),
        FillDirection = Enum.FillDirection.Vertical,
        VerticalAlignment = Enum.VerticalAlignment.Top,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = window.searchPage,
    })

    window:Create("UIPadding", {
        PaddingTop = UDim.new(0, if window.layout.mode == "sidebar" then 53 else 10),
        PaddingBottom = UDim.new(0, 33),
        Parent = window.searchPage,
    })

    window.searchEmpty = window:Create("TextLabel", {
        Name = "NoResults",
        Text = locale.t("No results"),
        FontFace = state.brandFont(Enum.FontWeight.Medium),
        TextSize = 15,
        TextTransparency = 0.6,
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.55, 0),
        Size = UDim2.fromOffset(200, 20),
        Visible = false,
        ZIndex = 3,
        Parent = window.main,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    local isSidebar = window.layout.mode == "sidebar"
    local pillTop = if isSidebar then window.layout.chromeHeight + 8 else window.layout.tabStripTop + 1

    window.searchPill = window:Create("Frame", {
        Name = "SearchBar",
        AnchorPoint = Vector2.new(if isSidebar then 1 else 0.5, 0),
        Position = UDim2.new(if isSidebar then 1 else 0.5, if isSidebar then -15 else 0, 0, pillTop),
        Size = UDim2.new(1, if isSidebar then -(window.layout.railWidth + 30) else -35, 0, 35),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        ZIndex = 10,
        BackgroundTransparency = 1,
        Visible = false,
        Parent = window.main,
    })

    window:Create("UICorner", {
        CornerRadius = UDim.new(1, 0),
        Parent = window.searchPill,
    })

    window.searchStroke = window:Create("UIStroke", {
        Color = Color3.fromRGB(255, 255, 255),
        Thickness = 1,
        Transparency = 1,
        Parent = window.searchPill,
    })

    window.searchShadow = window:Create("UIShadow", {
        BlurRadius = UDim.new(0, 20),
        Color = Color3.fromRGB(255, 255, 255),
        ZIndex = -1,
        Transparency = 1,
        Parent = window.searchPill,
    })

    window.searchIcon = window:Create("ImageLabel", {
        Image = "rbxassetid://" .. tostring(constants.icons.search),
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0, 15, 0.5, 1),
        Size = UDim2.fromOffset(16, 16),
        BackgroundTransparency = 1,
        ZIndex = 10,
        ImageTransparency = 1,
        Parent = window.searchPill,
    }, { ImageColor3 = "ContentColor" })

    window.searchInput = window:Create("TextBox", {
        Text = "",
        PlaceholderText = locale.t("Search all pages"),
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0, 40, 0.5, 0),
        Size = UDim2.new(1, -110, 0, 18),
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        ClearTextOnFocus = false,
        ClipsDescendants = true,
        ZIndex = 10,
        TextTransparency = 1,
        Parent = window.searchPill,
    }, { TextColor3 = "ContentColor", FontFace = "Font", PlaceholderColor3 = "PlaceholderColor" })

    window:Connect(window.searchInput:GetPropertyChangedSignal("Text"), function()
        if window._searching then
            filterElements(window, window.searchInput.Text)
        end
    end)

end

function search.build(window)
    window._searching = false
    window.searchAction = actionComponent.new(window, {
        name = "Search",
        icon = constants.icons.search,
        order = 4,
        callback = function()
            search.toggle(window)
        end,
    })
    window.searchAction.isLit = function()
        return window._searching
    end
end

return search

end)() end,
    [17] = function()local wax,script,require=ImportGlobals(17)local ImportGlobals return (function(...)
local utilities = script.Parent.Parent.utilities
local constants = require(utilities.constants)
local locale = require(utilities.locale)
local haptic = require(utilities.HapticEngine)
local profilePanel = require(script.Parent.profilePanel)
local layouts = require(utilities.layouts)

local settingsComponent = {}

local function layoutKey(layout)
    if type(layout) == "table" and layout.mode == "sidebar" and layout.forceCollapsed then
        return "collapsedSidebar"
    end
    return if type(layout) == "table" and layout.mode then layout.mode else "top"
end

-- [[The Settings toggle binding is an ordinary Input field: it shows the bound
-- key's name and reads one back, so the row uses the same typed-value input as
-- every other field in the library. Labels read "None" for a cleared binding
-- and MB2/MB3 for mouse buttons, so window.settings.toggleKeybind stays an
-- EnumItem — which is what Window:_bindKeybind compares against and what the
-- settings file stores.]]
local mouseLabels = {
    [Enum.UserInputType.MouseButton2] = "MB2",
    [Enum.UserInputType.MouseButton3] = "MB3",
}

-- [[The short mouse forms the field accepts. MouseButton1 is absent on purpose:
-- a menu toggle bound to left click would fire on every click in the game.]]
local mouseNames = {
    mb2 = Enum.UserInputType.MouseButton2,
    mousebutton2 = Enum.UserInputType.MouseButton2,
    rmb = Enum.UserInputType.MouseButton2,
    mb3 = Enum.UserInputType.MouseButton3,
    mousebutton3 = Enum.UserInputType.MouseButton3,
    mmb = Enum.UserInputType.MouseButton3,
}

local leftMouseNames = { mb1 = true, lmb = true, mousebutton1 = true, leftmouse = true }

-- [[Canonical KeyCode names, keyed by what people actually type. A mis-cased
-- Enum lookup is an error in the engine, so the field resolves the usual keys
-- itself ("k", "space", "left shift") and leaves the rest to the lookup below,
-- which keeps uncommon but real names ("KeypadPlus") working.]]
local keyNames = {
    esc = "Escape",
    enter = "Return",
    ctrl = "LeftControl",
    control = "LeftControl",
    shift = "LeftShift",
    alt = "LeftAlt",
    del = "Delete",
    spacebar = "Space",
    back = "Backspace",
}
for _, name in {
    "Space", "Escape", "Return", "Tab", "Backspace", "Delete", "Insert", "Home", "End",
    "PageUp", "PageDown", "Up", "Down", "Left", "Right",
    "LeftShift", "RightShift", "LeftControl", "RightControl", "LeftAlt", "RightAlt",
    "CapsLock", "Minus", "Equals", "LeftBracket", "RightBracket", "Semicolon", "Quote",
    "Comma", "Period", "Slash", "BackSlash",
} do
    keyNames[name:lower()] = name
end
for letter = string.byte("A"), string.byte("Z") do
    keyNames[string.char(letter):lower()] = string.char(letter)
end
for index = 1, 12 do
    keyNames[("f%d"):format(index)] = ("F%d"):format(index)
end
for digit, name in { "Zero", "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine" } do
    keyNames[tostring(digit - 1)] = name
end

local function keyLabel(item)
    if typeof(item) ~= "EnumItem" or item == Enum.KeyCode.Unknown then
        return "None"
    end
    return mouseLabels[item] or item.Name
end

-- [[Returns the key the text names, or Enum.KeyCode.Unknown for an empty or
-- "none" field (an unbound menu key). Text that names no key returns nil, with
-- a reason when the refusal needs its own wording.]]
local function parseKey(text)
    if type(text) ~= "string" then
        return nil
    end
    local trimmed = (text:gsub("^%s+", ""):gsub("%s+$", ""))
    local typed = trimmed:lower()
    if typed == "" or typed == "none" then
        return Enum.KeyCode.Unknown
    end
    -- [[Separators are dropped so "left shift" and "MB-2" read like their enum
    -- names; whatever is left still has to be a plain word.]]
    local name = (typed:gsub("[%s_%-]", ""))
    if name == "" or not name:match("^%w+$") then
        return nil
    end
    if leftMouseNames[name] then
        return nil, "leftMouse"
    end
    local mouse = mouseNames[name]
    if mouse then
        return mouse
    end
    local canonical = keyNames[name]
    if canonical then
        local ok, key = pcall(function()
            return Enum.KeyCode[canonical]
        end)
        if ok and typeof(key) == "EnumItem" then
            return key
        end
    end
    for _, candidate in { trimmed, trimmed:sub(1, 1):upper() .. trimmed:sub(2), trimmed:upper() } do
        local keyOk, key = pcall(function()
            return Enum.KeyCode[candidate]
        end)
        if keyOk and typeof(key) == "EnumItem" then
            return key
        end
        local typeOk, inputType = pcall(function()
            return Enum.UserInputType[candidate]
        end)
        if typeOk and mouseLabels[inputType] then
            return inputType
        end
    end
    return nil
end

function settingsComponent.buildUI(window)
    if window._settingsUIBuilt then
        return
    end
    window._settingsUIBuilt = true
    local appearance = window:CreateTab({ name = "Appearance", icon = "palette", customOrder = 1002, forgetState = true, isSettingsTab = true })
    local persistence = window:CreateTab({ name = "Persistence", icon = "save", customOrder = 1003, forgetState = true, isSettingsTab = true })
    local about = window:CreateTab({ name = "About", icon = "info", customOrder = 1004, forgetState = true, isSettingsTab = true })
    for _, tab in { appearance, persistence, about } do
        tab._isSettingsTab = true
        table.insert(window._settingsTabs, tab)
    end

    -- [[The settings shells are created on first settings access.]]
    -- [[Each tab's elements are created the first time that tab is opened]]
    -- [[(see _buildSettingsContent), so opening the window stays fast. The]]
    -- [[builders run at most once, in the original creation order.]]
    window._settingsContentBuilt = window._settingsContentBuilt or {}

    local function buildAbout()
        about:CreateCollapsibleGroup({
            name = "Rayfield Gen2",
            icon = "info",
            elements = {
                {
                    type = "Text",
                    name = "About Rayfield Gen2",
                    text = "Rayfield Gen2 is a ground-up rebuild of the Rayfield Interface Suite, built entirely in code for a faster, more stable, and more consistent experience.",
                },
                {
                    type = "Text",
                    name = "Highlights",
                    text = "State can persist between sessions, themes can change at runtime, labels can be translated, and the interface is designed for both desktop and mobile.",
                },
            },
        })
        about:CreateCollapsibleGroup({
            name = "Astra",
            icon = "sparkles",
            elements = {
                {
                    type = "Text",
                    name = "Our Story",
                    text = "Astra began from Rayfield Gen2 and was reshaped into a distinct interface library with its own visual language, behavior, and features.",
                },
                {
                    type = "Text",
                    name = "What Astra Provides",
                    text = "A responsive, customizable interface with persistent settings, runtime themes, localization support, animated window transitions, and a growing set of reusable elements.",
                },
                {
                    type = "Text",
                    name = "Customized by Haijo",
                    text = "Originally from Rayfield, Astra is customized, refined, and maintained by Haijo.",
                },
            },
        })
        about:CreateCollapsibleGroup({
            name = "Links",
            icon = "link",
            elements = {
                {
                    type = "Text",
                    name = "Documentation",
                    text = "https://docs.sirius.menu/rayfield-gen2",
                },
            },
        })
    end
    about._settingsContentBuilder = buildAbout

    local function buildGeneral()
        local keybindField

        -- [[A typed key becomes the menu toggle. The field is refused, with the
        -- previous binding restored, when the text is not a key.]]
        local function refuseKeybind(message, previous)
            window:Notify({ title = locale.resolve("Keybind unavailable"), content = message })
            keybindField:Set(previous, true)
            -- [[The field already flashed for the commit that reached here; the
            -- refusal flash replaces it so the row reads as rejected.]]
            window:_flashResult(keybindField, false)
        end

        local function commitKeybind(text)
            local previous = keyLabel(window.settings.toggleKeybind)
            local key, reason = parseKey(text)
            if not key then
                local message = if reason == "leftMouse"
                    then string.format(locale.resolve("Left click can't toggle the menu. Kept %s."), previous)
                    else string.format(locale.resolve("%s is not a key name. Kept %s."), tostring(text), previous)
                refuseKeybind(message, previous)
                return
            end
            if key == window.settings.toggleKeybind then
                -- [[Unchanged binding: normalise the field ("k" reads "K", an
                -- empty field reads "None") without writing settings again.]]
                keybindField:Set(previous, true)
                return
            end
            window.settings.toggleKeybind = key
            keybindField:Set(keyLabel(key), true)
            window:SaveSettings()
        end

        local generalControls = window.rfSettings:CreateCollapsibleGroup({
            name = "General Controls",
            icon = constants.icons.settings,
            elements = {
                {
                    type = "Input",
                    name = "Toggle Keybind",
                    icon = "keyboard",
                    value = keyLabel(window.settings.toggleKeybind),
                    placeholder = "e.g. K, Space, MB2",
                    callback = commitKeybind,
                },
                {
                    type = "Toggle",
                    name = "Unlock cursor while open",
                    value = window.settings.mouseOverride,
                    callback = function(value)
                        window.settings.mouseOverride = value
                        window:SaveSettings()
                    end,
                },
            },
        })
        keybindField = generalControls.elements[1]

        window.rfSettings:CreateCollapsibleGroup({
            name = "Window Behavior",
            icon = "layout",
            elements = {
                {
                    type = "Toggle",
                    name = "Prevent duplicate windows",
                    value = window.settings.antiWindowDuplicate,
                    callback = function(value)
                        window.settings.antiWindowDuplicate = value
                        window:SaveSettings()
                    end,
                },
                {
                    type = "Toggle",
                    name = "Keep window on screen",
                    value = window.settings.keepOnScreen,
                    callback = function(value)
                        window.settings.keepOnScreen = value
                        window:SaveSettings()
                    end,
                },
                {
                    type = "Toggle",
                    name = "Draggable capsule",
                    value = window.settings.dragMinimisedBar,
                    callback = function(value)
                        window.settings.dragMinimisedBar = value == true
                        window:SaveSettings()
                    end,
                },
                {
                    type = "Button",
                    name = "Reset Window Position",
                    callback = function()
                        window:_recenterForProfile(true)
                    end,
                },
                {
                    type = "Button",
                    name = "Reset Capsule Position",
                    callback = function()
                        window:_resetCapsulePosition()
                    end,
                },
            },
        })

        local motionLabels = { relaxed = "Relaxed", normal = "Normal", snappy = "Snappy", instant = "Instant" }
        local motionOptions = { "Relaxed", "Normal", "Snappy", "Instant" }
        local motionProfiles = { Relaxed = "relaxed", Normal = "normal", Snappy = "snappy", Instant = "instant" }
        local currentMotion = motionLabels[window.settings.motionSpeed] or motionLabels.normal

        window.rfSettings:CreateCollapsibleGroup({
            name = "Performance & Motion",
            icon = "gauge",
            elements = {
                {
                    type = "Toggle",
                    name = "Haptics",
                    value = window.settings.haptics,
                    callback = function(value)
                        window.settings.haptics = value
                        haptic.setEnabled(value)
                        window:SaveSettings()
                    end,
                },
                {
                    type = "Dropdown",
                    name = "Animation speed",
                    options = motionOptions,
                    value = currentMotion,
                    callback = function(value)
                        local profile = motionProfiles[value]
                        if not profile or profile == window.settings.motionSpeed then
                            return
                        end
                        window.settings.motionSpeed = profile
                        window:_applyMotionSetting()
                        window:SaveSettings()
                    end,
                },
            },
        })
    end
    window.rfSettings._settingsContentBuilder = buildGeneral

    local function buildAppearance()
        local themeOrder = { "default", "amethyst", "cobalt", "crimson", "ember", "emerald", "frost", "gold", "onyx", "rose" }
        local themes = {
            default = "Default",
            amethyst = "Amethyst",
            cobalt = "Cobalt",
            crimson = "Crimson",
            ember = "Ember",
            emerald = "Emerald",
            frost = "Frost",
            gold = "Gold",
            onyx = "Onyx",
            rose = "Rose",
        }
        local themeOptions = {}
        for _, key in ipairs(themeOrder) do
            table.insert(themeOptions, themes[key])
        end

        local function themeLabel(key)
            return themes[key] or themes.default
        end
        local function themeKey(label)
            for key, candidate in themes do
                if candidate == label then
                    return key
                end
            end
            return nil
        end

        -- [[The theme row follows a staged apply: the dropdown only *selects*
        -- a candidate (currentTheme stays untouched until the Apply button is
        -- confirmed), while the stat above shows the theme in effect — the
        -- full theme name, no icon, via the stat's text readout. Reset to
        -- Default is always on the row; only the Apply half is staged, and it
        -- appears beside Reset while a candidate differs from the active
        -- theme, then goes back to being absent once that candidate is
        -- applied (or dropped by picking the active theme again).]]
        local currentTheme = if themes[window.settings.theme] then window.settings.theme else "default"
        local pendingTheme

        -- Forward-declared so the element callbacks below can capture them;
        -- their bodies are assigned after the group is built.
        local themeStat, themeDropdown, themeButtonGroup, themeApplyButton
        local setPendingThemeButtons, syncThemeControls, confirmThemeReset

        local themeGroup = appearance:CreateCollapsibleGroup({
            name = "Theme",
            icon = "palette",
            elements = {
                {
                    type = "Stat",
                    name = "Current theme",
                    -- Reads the whole theme name out; letter = false opts this
                    -- text value out of the stat's single-letter badge.
                    value = themeLabel(currentTheme),
                    letter = false,
                },
                {
                    type = "Dropdown",
                    name = "Theme",
                    options = themeOptions,
                    value = themeLabel(currentTheme),
                    callback = function(value)
                        local candidate = themeKey(value)
                        if not candidate or candidate == currentTheme then
                            pendingTheme = nil
                            setPendingThemeButtons(false)
                            return
                        end
                        pendingTheme = candidate
                        setPendingThemeButtons(true)
                    end,
                },
                {
                    type = "Group",
                    elements = {
                        {
                            type = "Button",
                            name = "Reset to Default",
                            icon = "rotate-ccw",
                            callback = function()
                                window:Popup({
                                    title = "Reset theme?",
                                    content = "Set this window back to the " .. themeLabel("default") .. " theme?",
                                    options = {
                                        { text = "Cancel" },
                                        {
                                            text = "Reset",
                                            style = "primary",
                                            callback = confirmThemeReset,
                                        },
                                    },
                                })
                            end,
                        },
                        {
                            type = "Button",
                            name = "Apply Theme",
                            icon = "check",
                            callback = function()
                                if not pendingTheme then
                                    return
                                end
                                window:Popup({
                                    title = "Apply theme?",
                                    content = "Apply the " .. themeLabel(pendingTheme) .. " theme to this window?",
                                    options = {
                                        { text = "Cancel" },
                                        {
                                            text = "Apply",
                                            style = "primary",
                                            callback = function()
                                                currentTheme = pendingTheme
                                                pendingTheme = nil
                                                window.settings.theme = currentTheme
                                                window:ChangeTheme(currentTheme)
                                                window:SaveSettings()
                                                syncThemeControls()
                                                window:Notify({ title = "Theme applied", subtitle = themeLabel(currentTheme) })
                                            end,
                                        },
                                    },
                                })
                            end,
                        },
                    },
                },
            },
        })

        themeStat = themeGroup.elements[1]
        themeDropdown = themeGroup.elements[2]
        themeButtonGroup = themeGroup.elements[3]
        themeApplyButton = themeButtonGroup and themeButtonGroup.elements[2]
        function setPendingThemeButtons(show)
            if not themeApplyButton or not themeApplyButton.main then
                return
            end
            -- [[Only the Apply half of the row is staged: Reset to Default is
            -- permanent, so the row is never hidden and Reset never appears
            -- and disappears. Apply is absent until a candidate differs from
            -- the active theme, and returns to absent the moment that
            -- candidate is applied or dropped again.]]
            themeApplyButton.main.Visible = show == true
        end
        function syncThemeControls()
            if themeDropdown then
                themeDropdown:Set(themeLabel(currentTheme), true)
            end
            if themeStat then
                themeStat:SetText(themeLabel(currentTheme))
            end
            setPendingThemeButtons(pendingTheme ~= nil and pendingTheme ~= currentTheme)
        end
        function confirmThemeReset()
            pendingTheme = nil
            currentTheme = "default"
            window.settings.theme = "default"
            window:ChangeTheme("default")
            window:SaveSettings()
            syncThemeControls()
            window:Notify({ title = "Theme reset", subtitle = themeLabel("default") })
        end

        setPendingThemeButtons(false)

        local layoutMap = { top = "Default Topbar", sidebar = "Sidebar (Responsive)", collapsedSidebar = "Collapsed Sidebar" }
        local layoutDropdown
        local currentLayout = window.settings.layoutMode or window.layout.mode

        local layoutGroup = appearance:CreateCollapsibleGroup({
            name = "Layout",
            icon = "panel-left",
            elements = {
                {
                    type = "Dropdown",
                    name = "Bar Layout",
                    options = { layoutMap.top, layoutMap.sidebar, layoutMap.collapsedSidebar },
                    value = layoutMap[currentLayout],
                    callback = function(choice)
                        local requested
                        for key, label in layoutMap do
                            if choice == label then
                                requested = key
                                break
                            end
                        end
                        if not requested then
                            return
                        end
                        local active = layoutKey(window.layout)
                        if requested == active then
                            window:Notify({ title = "Bar layout", content = "The " .. choice .. " layout is already active." })
                            if layoutDropdown then
                                layoutDropdown:Set(layoutMap[active], true)
                            end
                            return
                        end
                        if layoutDropdown then
                            layoutDropdown:Set(layoutMap[active], true)
                        end
                        window:Popup({
                            title = "Apply bar layout?",
                            content = "Are you sure you want to change the layout to " .. choice .. "?",
                            options = {
                                { text = "Cancel" },
                                {
                                    text = "Apply",
                                    style = "primary",
                                    callback = function()
                                        if window:_setLayoutMode(requested) then
                                            currentLayout = requested
                                            window.settings.layoutMode = requested
                                            window:SaveSettings()
                                            if layoutDropdown then
                                                layoutDropdown:Set(layoutMap[requested], true)
                                            end
                                            window:Notify({ title = "Bar layout changed", content = "Switched to the " .. layoutMap[requested] .. " layout." })
                                        else
                                            currentLayout = layoutKey(window.layout)
                                            if layoutDropdown then
                                                layoutDropdown:Set(layoutMap[currentLayout], true)
                                            end
                                            window:Notify({ title = "Bar layout", content = "The selected bar layout could not be applied right now." })
                                        end
                                    end,
                                },
                            },
                        })
                    end,
                },
            },
        })
        layoutDropdown = layoutGroup.elements[1]

        local revealToggle
        local profileGroup = appearance:CreateCollapsibleGroup({
            name = "Profile",
            icon = "user",
            elements = {
                {
                    type = "Toggle",
                    name = "Show profile",
                    value = window.settings.showProfile,
                    callback = function(value)
                        local revealCleared = profilePanel.setEnabled(window, value)
                        if revealCleared and revealToggle then
                            revealToggle:Set(false, true)
                        end
                        window:SaveSettings()
                    end,
                },
                {
                    type = "Dropdown",
                    name = "Profile side",
                    options = { "Right", "Left" },
                    value = if (window.settings.profileSide or "right") == "left" then "Left" else "Right",
                    callback = function(choice)
                        local side = if choice == "Left" then "left" else "right"
                        if (window.settings.profileSide or "right") == side then
                            return
                        end
                        profilePanel.setSide(window, side)
                        window:SaveSettings()
                    end,
                },
                {
                    type = "Toggle",
                    name = "Reveal profile details",
                    value = window.settings.showFullUsername,
                    callback = function(value)
                        if not profilePanel.setReveal(window, value) and revealToggle then
                            revealToggle:Set(false, true)
                        end
                        window:SaveSettings()
                    end,
                },
            },
        })
        revealToggle = profileGroup.elements[3]
    end
    appearance._settingsContentBuilder = buildAppearance

    local function buildPersistence()
        persistence:CreateCollapsibleGroup({
            name = "Auto Save & Load",
            icon = "save",
            elements = {
                {
                    type = "Toggle",
                    name = "Auto Save Config",
                    value = window.settings.autoSaveConfig,
                    callback = function(value)
                        window.settings.autoSaveConfig = value
                        window.configuration.autoSave = value
                        window:SaveSettings()
                    end,
                },
                {
                    type = "Toggle",
                    name = "Auto Load Config",
                    value = window.settings.autoLoadConfig,
                    callback = function(value)
                        window.settings.autoLoadConfig = value
                        window.configuration.autoLoad = value
                        window:SaveSettings()
                    end,
                },
            },
        })

        local function namedConfigs()
            return window:ListConfigs()
        end
        local selected = namedConfigs()[1]
        local configDropdown, configName
        local function refresh()
            local configs = namedConfigs()
            if configDropdown then
                configDropdown:Refresh(configs)
                if selected and not table.find(configs, selected) then
                    selected = configs[1]
                elseif not selected and #configs > 0 then
                    selected = configs[1]
                end
                if selected then
                    configDropdown:Set(selected, true)
                else
                    configDropdown:Set(nil, true)
                end
            end
        end

        local configGroup = persistence:CreateCollapsibleGroup({
            name = "Configurations",
            icon = constants.icons.config,
            elements = {
                {
                    type = "Dropdown",
                    name = "Saved Configurations",
                    icon = constants.icons.config,
                    options = namedConfigs(),
                    value = selected,
                    placeholder = "No saved configurations",
                    callback = function(value)
                        selected = value
                    end,
                },
                {
                    type = "Input",
                    name = "Configuration Name",
                    placeholder = "e.g. PvP Loadout",
                    clearOnFocus = false,
                },
                {
                    type = "Group",
                    elements = {
                        {
                            type = "Button",
                            name = "Save",
                            icon = constants.icons.config,
                            callback = function()
                                local name = if configName then configName.value else ""
                                if name == "" then
                                    name = selected
                                end
                                if not name or name == "" then
                                    name = window.configuration.fileName or "Configuration"
                                end
                                if window:Save(name) then
                                    if configName then
                                        configName:Set("")
                                    end
                                    selected = name
                                    refresh()
                                    window:Notify({
                                        title = locale.resolve("Saved configuration"),
                                        content = name,
                                        icon = constants.icons.config,
                                    })
                                else
                                    window:Notify({
                                        title = locale.resolve("Couldn't save configuration"),
                                        content = name,
                                    })
                                end
                            end,
                        },
                        {
                            type = "Button",
                            name = "Load",
                            callback = function()
                                if not selected or selected == "" then
                                    window:Notify({ title = locale.resolve("Pick a configuration to load") })
                                    return
                                end
                                if window:_applyNamedConfig(selected) then
                                    window:Notify({
                                        title = locale.resolve("Loaded configuration"),
                                        content = selected,
                                    })
                                else
                                    window:Notify({
                                        title = locale.resolve("Couldn't load configuration"),
                                        content = selected,
                                    })
                                end
                            end,
                        },
                        {
                            type = "Button",
                            name = "Delete",
                            callback = function()
                                local name = selected
                                if not name or name == "" then
                                    window:Notify({ title = locale.resolve("Pick a configuration to delete") })
                                    return
                                end
                                if window:DeleteConfig(name) then
                                    selected = nil
                                    refresh()
                                    window:Notify({
                                        title = locale.resolve("Deleted configuration"),
                                        content = name,
                                    })
                                else
                                    window:Notify({
                                        title = locale.resolve("Couldn't delete configuration"),
                                        content = name,
                                    })
                                end
                            end,
                        },
                    },
                },
            },
        })
        configDropdown = configGroup.elements[1]
        configName = configGroup.elements[2]
    end
    persistence._settingsContentBuilder = buildPersistence
end

function settingsComponent.buildContent(window, tab)
    settingsComponent.buildUI(window)
    -- [[Lazy settings content: a tab's elements are created the first]]
    -- [[time it is opened, keeping window construction fast. The pcall]]
    -- [[keeps one bad element from taking the whole window down.]]
    if window._settingsContentBuilt[tab] or not tab._settingsContentBuilder then
        return
    end
    window._settingsContentBuilt[tab] = true
    local ok, err = pcall(tab._settingsContentBuilder)
    if not ok then
        window:Notify({ title = "Settings", content = "Some settings content failed to load: " .. tostring(err) })
    end
end

function settingsComponent.applySettingsLayout(window, isSettings)
    -- [[isSettings is retained for callers; the flags derive from the live]]
    -- [[rail so settings modes share one layout switch.]]
    local rail = window:_activeRail()
    window._settingsLayoutActive = rail == "settings"
    if window.tabList then
        window.tabList.Visible = not window.hidden and not window.minimised
    end
    if window.sidebar then
        window.sidebar.Visible = not window.hidden and not window.minimised
    end
    for _, tab in window.tabs do
        if tab.topbarItem then
            tab.topbarItem.Visible = not window.hidden and not window.minimised
                and (window:_railGroup(tab) == window:_activeRail())
        end
    end
    if window.selectedTab and window.selectedTab.tabPage then
        window.selectedTab.tabPage.Size = if window.layout.mode == "sidebar" then UDim2.new(1, -35, 1, 0) else UDim2.new(1, -20, 1, 0)
        window.selectedTab.tabPage.Position = if window.layout.mode == "sidebar" then UDim2.new(0, 35, 0, 0) else UDim2.new(0, 0, 0, 0)
    end
    -- [[The visible rail group changed: re-derive the responsive rail's]]
    -- [[width from the tabs now shown (no-op for the other layouts).]]
    window:_applyContentRailWidth()
end

function settingsComponent.toggleSettingsMode(window)
    if window._settingsMode then
        local previousTab = window._previousTab
        window._previousTab = nil
        if previousTab and previousTab.window == window and previousTab.tabPage and not previousTab._isSettingsTab then
            previousTab:Select()
            return
        end
        for _, tab in window.tabs do
            if not tab._isSettingsTab and tab.window == window and tab.tabPage then
                tab:Select()
                return
            end
        end
        return
    end

    window._previousTab = window.selectedTab
    window.rfSettings:Select()
end

function settingsComponent.setSettingsMode(window, active)
    active = active == true
    if active and window._constructed then
        settingsComponent.buildUI(window)
    end
    window._settingsMode = active
    if active and window._previousTab == nil and window.selectedTab and not window.selectedTab._isSettingsTab then
        window._previousTab = window.selectedTab
    end
    for _, tab in window.tabs do
        if tab.topbarItem then
            tab.topbarItem.Visible = not window.hidden and not window.minimised
                and (window:_railGroup(tab) == window:_activeRail())
        end
    end
    settingsComponent.applySettingsLayout(window, active)
end

return settingsComponent

end)() end,
    [18] = function()local wax,script,require=ImportGlobals(18)local ImportGlobals return (function(...)
local tabSelector = require(script.Parent.tabSelector)
local search = require(script.Parent.search)

local sidebar = {}

function sidebar.maskUsername(name)
    name = tostring(name or "")
    if name == "" then
        return "****"
    end
    return string.sub(name, 1, math.min(3, #name)) .. "****"
end

function sidebar.buildTabRail(window, layout, collapsed)
    window.tabList = window:Create("ScrollingFrame", {
        Name = if collapsed then "CollapsedTabs" else "Tabs",
        Active = true,
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 0,
        ScrollBarImageTransparency = 1,
        ScrollingDirection = Enum.ScrollingDirection.Y,
        ClipsDescendants = true,
        Parent = window.sidebar,
    })
    window:Create("UIPadding", {
        PaddingTop = UDim.new(0, layout.railPadding),
        PaddingBottom = UDim.new(0, layout.railPadding),
        Parent = window.tabList,
    })
    window.tabListLayout = window:Create("UIListLayout", {
        Padding = UDim.new(0, layout.rowSpacing),
        FillDirection = Enum.FillDirection.Vertical,
        VerticalAlignment = Enum.VerticalAlignment.Top,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = window.tabList,
    })
end

function sidebar.applyRailRows(window, width, layout)
    search.railWidth(window, width)
    -- [[The rail is collapsed only when it is at the icon-only width. The]]
    -- [[responsive rail otherwise hugs the widest tab name, which is often]]
    -- [[narrower than the fixed 219px rail the layout used to have.]]
    local isCollapsed = width <= (layout.railCollapsedWidth :: number)
    for _, tab in window.tabs do
        if not tab.neglectSelector then
            tabSelector.setRowCollapsed(tab, isCollapsed, layout)
        end
    end
    tabSelector.relayoutSidebarRows(window, layout, width)
end

return sidebar

end)() end,
    [19] = function()local wax,script,require=ImportGlobals(19)local ImportGlobals return (function(...)
local utilities = script.Parent.Parent.utilities
local state = require(utilities.Parent.core.state)
local motion = require(utilities.motion)
local functions = require(script.Parent.Parent.functions)
local locale = require(utilities.locale)

local tabSelector = {}

local function getInitial(str: string?): string
    if type(str) ~= "string" or str == "" then
        return "?"
    end
    local nextOffset = utf8.offset(str, 2)
    local initial = if nextOffset then string.sub(str, 1, nextOffset - 1) else str
    return string.upper(initial)
end

tabSelector.states = {
    top = {
        selected = { background = 0, stroke = 0, content = 0 },
        hover = { background = 0.4, stroke = 0.3, content = 0.3 },
        unselected = { background = 0.8, stroke = 0.65, content = 0.5 },
        hidden = { background = 1, stroke = 1, content = 1 },
    },
    sidebar = {
        selected = { background = 0.4, stroke = 0.5, content = 0, shadow = 0.8 },
        hover = { background = 0.7, stroke = 0.8, content = 0.3, shadow = 1 },
        unselected = { background = 1, stroke = 1, content = 0.5, shadow = 1 },
        hidden = { background = 1, stroke = 1, content = 1, shadow = 1 },
    },
}

local function buildGradients(tab, item, stroke)
    tab.topbarItemGradient = tab.window:Create("UIGradient", {
        Rotation = 90,
        Parent = item,
    }, { Color = { "TabBackground", functions.toColorSequence } })

    tab.topbarItemStrokeGradient = tab.window:Create("UIGradient", {
        Rotation = 90,
        Parent = stroke,
    }, { Color = { "TabStroke", functions.toColorSequence } })
end

local function buildContent(tab, iconSize, useInitial)
    if useInitial and not tab.icon then
        tab.topbarItemInitial = tab.window:Create("TextLabel", {
            Text = getInitial(tab.name),
            Size = UDim2.fromOffset(iconSize, iconSize),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            TextSize = iconSize - 4,
            TextXAlignment = Enum.TextXAlignment.Center,
            TextYAlignment = Enum.TextYAlignment.Center,
            Visible = false,
            TextTransparency = 1,
            Parent = tab.topbarItemContainer,
        }, { TextColor3 = "TabColor", FontFace = "Font" })
    end

    if tab.icon then
        tab.topbarItemIcon = tab.window:Create("ImageLabel", {
            Image = tab.icon,
            Size = UDim2.fromOffset(iconSize, iconSize),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            ImageTransparency = 1,
            Parent = tab.topbarItemContainer,
        }, { ImageColor3 = "TabColor" })
    end

    if tab.name then
        tab.topbarItemTitle = tab.window:Create("TextLabel", {
            Text = locale.t(tab.name),
            Size = UDim2.fromOffset(0, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            TextSize = 16,
            AutomaticSize = Enum.AutomaticSize.XY,
            TextXAlignment = Enum.TextXAlignment.Left,
            TextWrapped = true,
            LayoutOrder = 1,
            TextTransparency = 1,
            Parent = tab.topbarItemContainer,
        }, { TextColor3 = "TabColor", FontFace = "Font" })
    end
end

local function buildTopStyle(tab)
    tab.topbarItem = tab.window:Create("Frame", {
        Name = tab.name,
        Size = UDim2.fromOffset(0, 34),
        AutomaticSize = Enum.AutomaticSize.X,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        Visible = false,
        LayoutOrder = tab.customOrder or 0,
        Parent = tab.window.tabList,
    })

    tab.topbarItemInteract = tab.window:Create("TextButton", {
        Active = false,
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        Text = "",
        TextTransparency = 1,
        Parent = tab.topbarItem,
    })

    tab.window:Create("UICorner", {
        CornerRadius = UDim.new(0, 14),
        Parent = tab.topbarItem,
    })

    tab.topbarItemStroke = tab.window:Create("UIStroke", {
        ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
        Color = Color3.fromRGB(255, 255, 255),
        Transparency = 1,
        Parent = tab.topbarItem,
    })

    buildGradients(tab, tab.topbarItem, tab.topbarItemStroke)

    tab.topbarItemContainer = tab.window:Create("Frame", {
        Size = UDim2.fromOffset(0, 34),
        AutomaticSize = Enum.AutomaticSize.X,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Parent = tab.topbarItem,
    })

    tab.window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, 13),
        PaddingRight = UDim.new(0, 14),
        Parent = tab.topbarItemContainer,
    })

    tab.topbarItemLayout = tab.window:Create("UIListLayout", {
        Padding = UDim.new(0, 6),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = tab.topbarItemContainer,
    })

    buildContent(tab, 16, false)
end

local function buildSidebarStyle(tab, layout)
    tab.topbarItem = tab.window:Create("Frame", {
        Name = tab.name,
        Size = UDim2.new(1, -layout.rowInset * 2, 0, layout.rowHeight),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        Visible = false,
        LayoutOrder = tab.customOrder or 0,
        Parent = tab.window.tabList,
    })

    tab.topbarItemInteract = tab.window:Create("TextButton", {
        Active = false,
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        Text = "",
        TextTransparency = 1,
        Parent = tab.topbarItem,
    })

    tab.window:Create("UICorner", {
        CornerRadius = UDim.new(0, layout.rowCornerRadius),
        Parent = tab.topbarItem,
    })

    tab.topbarItemStroke = tab.window:Create("UIStroke", {
        ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
        Color = Color3.fromRGB(255, 255, 255),
        Transparency = 1,
        Parent = tab.topbarItem,
    })

    buildGradients(tab, tab.topbarItem, tab.topbarItemStroke)

    tab.topbarItemShadow = tab.window:Create("UIShadow", {
        BlurRadius = UDim.new(0, 20),
        Color = Color3.fromRGB(255, 255, 255),
        Offset = UDim2.new(0, 0, 0, -15),
        Spread = UDim2.new(0, 10, 0, -30),
        ZIndex = -1,
        Transparency = 1,
        Parent = tab.topbarItem,
    })

    tab.topbarItemContainer = tab.window:Create("Frame", {
        Size = UDim2.new(1, -layout.rowPadding, 0, 24),
        Position = UDim2.new(0, layout.rowPadding, 0.5, 0),
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Parent = tab.topbarItem,
    })

    tab.topbarItemLayout = tab.window:Create("UIListLayout", {
        Padding = UDim.new(0, layout.rowContentSpacing),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = tab.topbarItemContainer,
    })

    buildContent(tab, layout.rowIconSize, true)
end

-- [[Whether the rail is currently at its icon-only width. The rail's own Size
-- [[is the source of truth: it is written by ApplyWidth (the layout's Build
-- [[sizes it too), and row rebuilds happen after that. A window whose rail has
-- [[not been built yet falls back to the layout's requested width.]]
function tabSelector.railCollapsed(window, layout)
    if layout.mode ~= "sidebar" then
        return false
    end
    if layout.forceCollapsed then
        return true
    end
    local sidebar = window.sidebar
    local width = if sidebar then sidebar.Size.X.Offset else (layout.railWidth :: number)
    return width <= (layout.railCollapsedWidth :: number)
end

function tabSelector.build(tab, layoutMode)
    if layoutMode.mode == "sidebar" or layoutMode.forceCollapsed then
        buildSidebarStyle(tab, layoutMode)
        -- [[A row has to be born in whatever state the rail is in right now.
        -- [[Rows are rebuilt after the rail is sized (a layout switch, or a tab
        -- [[created while the rail is icon-only), and a row built as an
        -- [[expanded one keeps its title: in a 64px rail that leaked "El…" of
        -- [[the tab name past the icon instead of the icon-only row.
        if tabSelector.railCollapsed(tab.window, layoutMode) then
            tabSelector.setRowCollapsed(tab, true, layoutMode)
        end
    else
        buildTopStyle(tab)
    end
end

-- [[Responsive sidebar sizing: the tab names drive the rail's width. The]]
-- [[longest visible row sets the rail's natural width, so every row (which]]
-- [[stays full-rail, as before) shares the width of the longest tab, and]]
-- [[the elements area takes back whatever the rail frees up. The window's]]
-- [[available space is the cap, so a long name wraps in place instead of]]
-- [[swallowing the content area.]]

local function inCurrentRail(window, tab)
    -- [[Parent guards out rows destroyed mid-layout-switch: their parent]]
    -- [[rail is gone before the rebuilt rows exist, and a destroyed]]
    -- [[instance must not have properties written to it.]]
    return not tab.neglectSelector
        and tab.topbarItem ~= nil
        and tab.topbarItem.Parent ~= nil
        and window:_railGroup(tab) == window:_activeRail()
end

local function rowNaturalWidth(tab, window, layout)
    local width = layout.rowPadding * 2
    if tab.icon then
        width += layout.rowIconSize
        if tab.name then
            width += layout.rowContentSpacing
        end
    end
    if tab.name then
        width += functions.textWidth(window.theme.Font, 16, locale.resolve(tab.name))
    end
    return width
end

-- [[The rail width at which the widest row in the current rail group fits]]
-- [[exactly (row insets included). 0 when the group is empty.]]
function tabSelector.railContentWidth(window, layout)
    local maxRow = 0
    for _, tab in window.tabs do
        if inCurrentRail(window, tab) then
            maxRow = math.max(maxRow, rowNaturalWidth(tab, window, layout))
        end
    end
    if maxRow <= 0 then
        return 0
    end
    return maxRow + layout.rowInset * 2
end

-- [[Rows stay full-rail; this only holds a title inside its row when the]]
-- [[rail's available width capped below the name's natural width.]]
function tabSelector.relayoutSidebarRows(window, layout, railWidth)
    if layout.mode ~= "sidebar" or layout.forceCollapsed then
        return
    end
    if not window.sidebar then
        return
    end
    railWidth = railWidth or layout.railWidth
    if railWidth <= (layout.railCollapsedWidth :: number) then
        -- [[Collapsed rail: icon-only rows, nothing to hold.]]
        return
    end

    local rowWidth = railWidth - layout.rowInset * 2
    if rowWidth <= 0 then
        return
    end

    for _, tab in window.tabs do
        if inCurrentRail(window, tab) then
            local title = tab.topbarItemTitle
            if title and tab.name then
                local slot = rowWidth - layout.rowPadding * 2
                    - (if tab.icon then layout.rowIconSize + layout.rowContentSpacing else 0)
                if
                    slot >= 24
                    and functions.textWidth(window.theme.Font, 16, locale.resolve(tab.name)) > slot
                then
                    -- [[Capped: hold the label inside the row; its existing]]
                    -- [[TextWrapped then keeps the name readable.]]
                    title.Size = UDim2.fromOffset(slot, 16)
                    title.AutomaticSize = Enum.AutomaticSize.Y
                else
                    title.Size = UDim2.fromOffset(0, 16)
                    title.AutomaticSize = Enum.AutomaticSize.XY
                end
            end
        end
    end
end

function tabSelector.setRowCollapsed(tab, collapsed, layout)
    -- [[Parent check: during a layout-mode switch the previous rail's rows]]
    -- [[are destroyed before the rebuilt ones exist, and properties must]]
    -- [[not be written to a destroyed instance.]]
    if layout.mode ~= "sidebar" or not tab.topbarItem or not tab.topbarItem.Parent then
        return
    end

    if tab.topbarItemTitle then
        if collapsed then
            -- [[Icon-only rows must never leak a title while the rail is being
            -- [[collapsed. Hide the label before the layout contracts; the
            -- [[icon remains visible throughout the transition.]]
            tab.topbarItemTitle.Visible = false
            tab.topbarItemTitle.TextTransparency = 1
        else
            tab.topbarItemTitle.Visible = true
            motion.tween(tab.topbarItemTitle, { TextTransparency = 0 }, "fast")
        end
    end
    if tab.topbarItemInitial then
        tab.topbarItemInitial.Visible = collapsed
    end
    -- [[An icon-only row is a square tile the height of the row. The rail is
    -- [[64px wide, so a full-rail row (inset 15px each side) was 34x38: the
    -- [[icon sat 7px from its sides but 9px from its top and bottom, which
    -- [[reads as an off-centre glyph. The square tile keeps the list's
    -- [[centring and leaves the icon the same clearance on all four edges.
    tab.topbarItem.Size = if collapsed
        then UDim2.fromOffset(layout.rowHeight :: number, layout.rowHeight :: number)
        else UDim2.new(1, -(layout.rowInset :: number) * 2, 0, layout.rowHeight :: number)
    local rowPadding = if collapsed then 0 else layout.rowPadding
    tab.topbarItemContainer.Size = UDim2.new(1, -rowPadding, 0, 24)
    tab.topbarItemContainer.Position = UDim2.new(0, rowPadding, 0.5, 0)
    tab.topbarItemLayout.HorizontalAlignment = if collapsed
        then Enum.HorizontalAlignment.Center
        else Enum.HorizontalAlignment.Left
end

function tabSelector.applyVisual(tab, visualState, tweenInfo)
    local targets = {
        [tab.topbarItem] = { BackgroundTransparency = visualState.background },
        [tab.topbarItemStroke] = { Transparency = visualState.stroke },
    }

    if tab.topbarItemIcon then
        targets[tab.topbarItemIcon] = { ImageTransparency = visualState.content }
    end
    if tab.topbarItemTitle then
        targets[tab.topbarItemTitle] = { TextTransparency = visualState.content }
    end
    if tab.topbarItemInitial then
        targets[tab.topbarItemInitial] = { TextTransparency = visualState.content }
    end
    if tab.topbarItemShadow and visualState.shadow then
        targets[tab.topbarItemShadow] = { Transparency = visualState.shadow }
    end

    for instance, properties in targets do
        if tweenInfo then
            -- [[`tweenInfo` is a motion spec (name or TweenInfo): routing the
            -- pill states through the service gives them the shared curve,
            -- the time scale and one owner per property.]]
            motion.tween(instance, properties, tweenInfo)
        else
            for prop, value in properties do
                instance[prop] = value
            end
        end
    end
end

return tabSelector

end)() end,
    [20] = function()local wax,script,require=ImportGlobals(20)local ImportGlobals return (function(...)
local utilities = script.Parent.Parent.utilities
local image = require(script.Parent.Parent.images.image)
local icons = require(script.Parent.Parent.icons)
local functions = require(script.Parent.Parent.functions)
local persistence = require(utilities.persistence)
local constants = require(utilities.constants)
local motion = require(utilities.motion)
local locale = require(utilities.locale)
local log = require(utilities.log)
local haptic = require(utilities.HapticEngine)
local windowSizing = require(utilities.windowSizing)
local layouts = require(utilities.layouts)
local chrome = require(script.Parent.chrome)
local search = require(script.Parent.search)
local tabSelector = require(script.Parent.tabSelector)
local profilePanel = require(script.Parent.profilePanel)
local settingsComponent = require(script.Parent.settings)
local overlayQueue = require(script.Parent.overlayQueue)

local state = require(utilities.Parent.core.state)

local themes = require(script.Parent.Parent.themes)

local Window = {}
Window.__index = Window

-- [[Element reveals, theme cross-fades and the notification stack's lane changes all
-- ride the motion service's shared specs (see utilities/motion.luau) instead
-- of per-call TweenInfo values: one vocabulary, one time scale, one owner per
-- animated property.]]

-- [[The beat between the window's shell landing and its page coming in. Long
-- enough that the two read as one entrance in the right order — frame, then
-- content — and short enough that nothing looks like it is waiting for the
-- next thing to happen. Scaled by the motion service, and skipped entirely
-- when animation is switched off.]]
local contentRevealBeat = 0.12

local collapsedWideSize = UDim2.fromOffset(185, 50)
local collapsedIconOnlySize = UDim2.fromOffset(50, 50)
local collapsedPosition = UDim2.new(0.5, 0, 0, 20)


local compactRowHeight = 41

-- [[The drag handle's centre sits this far below the window's bottom edge
-- [[(the same `handleGap` components/drag.luau owns). Every place that puts
-- [[the handle under the window reads it, so the two files cannot drift apart
-- [[and leave the pill floating a few pixels off the window it belongs to.
local dragHandleGap = 22

-- [[The window's own surface, defined once. Everything that has to read as
-- [["the window background" — the frame itself and the profile card beside it —
-- [[is styled through Window:StyleWindowSurface, so the two can never drift
-- [[into near-miss shades of each other: same base colour, same WindowColor
-- [[gradient (theme token + the same rotation/offset, so a theme change moves
-- [[both together) and the same opacity once shown.]]
local windowSurfaceColor = Color3.fromRGB(255, 255, 255)
local windowSurfaceTransparency = 0
local windowSurfaceGradientRotation = 270
local windowSurfaceGradientOffset = Vector2.new(0, -0.1)

local lockScrimTransparency = 0.55
local lockedTextTransparency = 0.55

local viewportCheckInterval = 2

-- Startup is deliberately paced in small, completed-object batches, before the
-- first reveal and after it alike. This prevents a large host script from
-- monopolising a frame while still letting the opening tween render as the
-- remaining controls stream in behind the already-visible shell.
local constructionFrameBudget = 0.003
local constructionInstanceBudget = 48

-- Notifications share one entrance lane. Their 0.6s transitions do
-- not overlap, but already-visible messages are still allowed to remain in
-- their normal stacks while the next one enters.
-- The first open reads as an entrance instead of one large visibility flip:
-- the shell animates in, then `_stageContentReveal` walks the visible page one
-- control per beat (see `_firstShow`). There is exactly one cascade — an older
-- second timer used to walk the same page concurrently, restarting the first
-- cascade's tweens mid-flight through the motion service's cancel-on-overlap.

local function computeSize(mode): UDim2
    local size = state.workspace.CurrentCamera
    return windowSizing.fit(size and size.ViewportSize, mode)
end

local cornerKeys = { "TopLeftRadius", "TopRightRadius", "BottomLeftRadius", "BottomRightRadius" }

local hasPerCornerRadius = (function()
    return (
        pcall(function()
            local instance = Instance.new("UICorner")
            instance.TopLeftRadius = UDim.new(0, 1)
            instance:Destroy()
        end)
    )
end)()

local function layoutKey(layout)
    if layout.mode == "sidebar" and layout.forceCollapsed then
        return "collapsedSidebar"
    end
    return layout.mode
end

local coerceValue = themes.coerceValue
local firstColor = themes.firstColor
local deriveStrokes = themes.deriveStrokes
local resolveTheme = themes.resolve

function Window:ResolveIcon(value, pack)
    return icons.resolve(value, pack)
end

function Window.new(props)
    props = if typeof(props) == "table" then props else {}

                if props.translations or props.Translations then
        locale.register(props.translations or props.Translations)
    end
    if props.translator or props.Translator then
        locale.translator = props.translator or props.Translator
    end
    locale.setActive(props.locale or props.Locale or locale.detect())

            local fallbackFont = props.fallbackFont or props.FallbackFont
    if fallbackFont then
        state.setFallbackFont(fallbackFont)
    end

        local layout = layouts.top -- [[layout is controlled via Settings (Astra Settings → Bar Layout); CreateWindow no longer takes sidebarLayout]]
    local providedIcon = props.icon or props.Icon

    local self = setmetatable({
        name = props.name or props.Name or "Astra Window",
        subheading = props.subtitle or props.Subtitle,
        layout = layout,
        size = computeSize(layout.mode),
        instances = {},
        connections = {},

        -- Cleared by the entrypoint after the caller's initial construction
        -- has been quiet for two frames. Checkpoints continue after the window
        -- appears so the remaining controls cannot freeze its entrance.
        _startupComplete = false,
        -- [[Placeholder for the entrance queue: the queue's own shape (pending
        -- list, gate flags) is completed by `queueFor` in
        -- `components/overlayQueue.luau` on first use, so what lives here is
        -- only "the window has somewhere to put its overlays", which is what
        -- lets `Unload` clear it unconditionally.]]
        _overlayQueue = {},
        _overlayQueueRunning = false,

        icon = icons.resolve(providedIcon or "Sirius"),
        showName = props.showName or props.ShowName or "Astra",
        showIcon = icons.resolve(providedIcon or "Astra"),
                showIconOnly = props.showIconOnly or props.ShowIconOnly or false,
                profileText = nil,

        themeProperties = {},
        localeProperties = {},
        tabs = {},
        tabSections = {},
        selectedTab = nil,
        _settingsLayoutActive = false,
        _settingsTabs = {},
        _settingsMode = false,
        theme = resolveTheme(props.theme or props.Theme),

        controls = {},

        configuration = (function()
            -- Legacy overrides remain accepted, but normal usage needs no config table.
            local config = props.configuration or props.Configuration or {}
            local save = config.autoSave
            if save == nil then save = config.AutoSave end
            local load = config.autoLoad
            if load == nil then load = config.AutoLoad end
            return {
                autoSave = if save == nil then true else save == true,
                autoLoad = if load == nil then true else load == true,
                fileName = config.fileName or config.FileName or "Astra-Hub",
                customFolder = config.customFolder or config.CustomFolder or "AstraConfigs",
            }
        end)(),
    }, Window)

            self.Flags = setmetatable({}, {
        __index = function(_, key)
            local control = self.controls[key]
            return control and control.value
        end,
        __newindex = function(_, key, value)
            local control = self.controls[key]
            if not control then
                log.warn("Astra: no flag '" .. tostring(key) .. "' to set")
                return
            end
            control:Set(value)
        end,
        __iter = function()
            local key
            return function()
                local value
                key, value = next(self.controls, key)
                if key then
                    return key, value.value
                end
                return nil
            end
        end,
    })

            self.settings = {
        toggleKeybind = Enum.KeyCode.K,
        theme = if type(props.theme or props.Theme) == "string" then string.lower(props.theme or props.Theme) else "default",
        mouseOverride = false,         keepOnScreen = true,         haptics = false,
        dragMinimisedBar = false,
        showProfile = false,
        showFullUsername = false,
        profileSide = "right",
        antiWindowDuplicate = true,
        layoutMode = layout.mode,
        activeSubTab = 1,
        motionSpeed = "normal",
        autoSaveConfig = self.configuration.autoSave,
        autoLoadConfig = self.configuration.autoLoad,
    }
        self._layoutTransitioning = false

        self.screenGui = self:Create("ScreenGui", {
        Name = state.httpService:GenerateGUID(false),
        IgnoreGuiInset = true,
        ResetOnSpawn = false,
        Enabled = true,
        DisplayOrder = constants.displayOrder.window,
        ZIndexBehavior = Enum.ZIndexBehavior.Global,

        Parent = state.guiContainer,
    })
        self.main = self:Create("Frame", {
        Name = self.name,
        ZIndex = 1,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
                Size = self.size,
        BackgroundTransparency = 1,
        -- [[Roblox gives every GuiObject a 1px border by default. The border
        -- draws the frame's rectangular outline — straight lines that ignore
        -- the UICorner arcs — so on the rounded window it painted a hard
        -- square edge across all four corners once the surface faded in.
        -- The window's silhouette is the UICorner's job alone.]]
        BorderSizePixel = 0,
        Visible = false,
        Parent = self.screenGui,
    })

    -- Scale is used only for the first entrance. Animating the Frame's Size
    -- forces every descendant layout to recompute on every tween step; UIScale
    -- gives the same soft pop without turning a large window into a layout
    -- hotspot while it opens.
    self.windowScale = self:Create("UIScale", {
        Scale = 1,
        Parent = self.main,
    })

    self.drag = require(script.Parent.drag).new(self)

    self.windowCorner = self:Create("UICorner", {

        Parent = self.main,
    }, { CornerRadius = "CornerRoundness" })

    self.windowStroke = self:Create("UIStroke", {
                Transparency = 1,
        Parent = self.main,
    }, { Color = "SurfaceStroke" })

    self.windowGradient = self:StyleWindowSurface(self.main)

                self.bottomFade = self:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        AnchorPoint = Vector2.new(1, 1),
        Position = UDim2.fromScale(1, 1),
        Size = self.layout.fadeSize,
        ZIndex = constants.zIndex.bottomFade,

                BackgroundTransparency = 1,
        Parent = self.main,
    })

    self.bottomFadeCorner = self:_roundCorners(self.bottomFade, self.layout.fadeCorners)

    self.bottomFadeGradient = self:Create("UIGradient", {
        Rotation = 270,
        Offset = Vector2.new(0, 0.2),
        Transparency = self.layout.fadeTransparency,

        Parent = self.bottomFade,
    }, {
                        Color = {
            "WindowColor",
            function(color)
                return ColorSequence.new(functions.toColorSequence(color).Keypoints[1].Value)
            end,
        },
    })

        self.topbar = self:Create("Frame", {

        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 0, self.layout.topbarHeight),
                        Active = true,

        Parent = self.main,
    })

    self.topContainer = self:Create("Frame", {
        Size = UDim2.new(0, 300, 0, 24),
        Position = UDim2.new(0, 25, 0.5, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,

        Parent = self.topbar,
    })

    self.topContainerLayout = self:Create("UIListLayout", {
        Padding = UDim.new(0, 8),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.topContainer,
    })

    self.titleContainer = self:Create("Frame", {
        Size = UDim2.fromOffset(50, 24),
        Position = UDim2.new(0, 25, 0.5, 0),
        AutomaticSize = Enum.AutomaticSize.XY,
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,
        LayoutOrder = 1,

        Parent = self.topContainer,
    })

    self.titleContainerLayout = self:Create("UIListLayout", {
        Padding = UDim.new(0, 3),
        FillDirection = Enum.FillDirection.Vertical,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.titleContainer,
    })

    if self.name then
        self.title = self:Create("TextLabel", {
                        Text = locale.t(self.name),
            FontFace = state.brandFont(Enum.FontWeight.Medium),

            Size = UDim2.fromOffset(50, 20),
            AutomaticSize = Enum.AutomaticSize.X,

            BackgroundTransparency = 1,
            TextSize = 20,
            TextXAlignment = Enum.TextXAlignment.Left,
            TextWrapped = true,

                        TextTransparency = 1,
            Parent = self.titleContainer,
        }, { FontFace = "Font", TextColor3 = "TitlingColor" })
    end

    if self.icon then
        self.topbarIcon = self:Create("ImageLabel", {
            Image = self.icon,

            BackgroundTransparency = 1,
            Size = UDim2.fromOffset(32, 32),

                        ImageTransparency = 1,
            Parent = self.topContainer,
        }, { ImageColor3 = "TitlingColor" })
    end

    if self.subheading then
        self.subtitle = self:Create("TextLabel", {
                        Text = locale.t(self.subheading),
            Size = UDim2.fromOffset(50, 12),
            AutomaticSize = Enum.AutomaticSize.X,
            BackgroundTransparency = 1,
            TextSize = 12,
            TextXAlignment = Enum.TextXAlignment.Left,
            TextWrapped = true,

                        TextTransparency = 1,
            Parent = self.titleContainer,
        }, { TextColor3 = "TitlingColor", FontFace = "Font" })
    end

        self.windowShadow = self:CreateGlow(self.main, "ShadowColor", 20, 1)

                self.elements = self:Create("Frame", {
        Size = UDim2.new(1, 0, 1, -self.layout.chromeHeight),
        Position = UDim2.fromScale(1, 1),
        AnchorPoint = Vector2.new(1, 1),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        ClipsDescendants = true,

        Parent = self.main,
    })

    if self.layout.mode == "sidebar" then
        self.elementsCorner = self:_roundCorners(self.elements, self.layout.cardCorners)

        self.elementsStroke = self:Create("UIStroke", {
            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,

                        Transparency = 1,
            Parent = self.elements,
        }, { Color = "SurfaceStroke" })

                        self:Create("UIGradient", {
            Rotation = self.layout.cardStrokeRotation,
            Transparency = self.layout.cardStrokeTransparency,

            Parent = self.elementsStroke,
        })
    end

    self.elementsLayout = self:Create("UIPageLayout", {
        Padding = UDim.new(0, 0),
        FillDirection = self.layout.pageDirection,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,

                        ScrollWheelInputEnabled = false,
        GamepadInputEnabled = false,
        TouchInputEnabled = false,

        EasingStyle = Enum.EasingStyle.Exponential,
        TweenTime = 0.4,

        Parent = self.elements,
    })

            local initialLayout = layouts.implementation(layoutKey(self.layout))
    initialLayout.Build(self, self.layout)
    initialLayout.ApplyWidth(self, self:_railWidth())

        self.actionContainer = self:Create("Frame", {

        AnchorPoint = Vector2.new(1, 0.5),
        AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, 24),
        Position = UDim2.new(1, -20, 0.5, 0),
        BackgroundTransparency = 1,

        Parent = self.topbar,
    })

    self.actionsListLayout = self:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Right,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.actionContainer,
    })

    self:_constructionCheckpoint(true)

    self.rfSettings = self:CreateTab({
        name = "General",
        icon = "sliders-horizontal",
        customOrder = 1001,
        forgetState = true,
        isSettingsTab = true,
    })
    self.rfSettings._isSettingsTab = true
    self._settingsTabs = { self.rfSettings }
    -- [[Settings tab shell landed; hand the frame back before the action
    -- [[buttons and search chrome are added.]]
    self:_constructionCheckpoint(true)

    require(script.Parent.action).new(self, {
        name = "Close",
        icon = constants.icons.close,
        order = 1,

        callback = function()
            self:Popup({
                title = "Close window?",
                content = "Are you sure you want to close this window?",
                options = {
                    { text = "Cancel" },
                    {
                        text = "Close",
                        style = "primary",
                        callback = function()
                            self:Close()
                        end,
                    },
                },
            })
        end,
    })

    self.minimiseAction = require(script.Parent.action).new(self, {
        name = "Minimise",
        icon = constants.icons.minimise,
        order = 2,

        callback = function()
            self:ToggleHide()
        end,
    })

    self.settingsAction = require(script.Parent.action).new(self, {
        name = "Settings",
        icon = constants.icons.settings,
        order = 3,
        linkedTab = self.rfSettings,

        callback = function()
            self:_toggleSettingsMode()
        end,
    })

    search.build(self)
        self:_applyRailWidth()
    -- [[Toolbar complete; another frame before persisted settings are read
    -- [[and applied (a disk read, the theme pass and possibly a layout swap).]]
    self:_constructionCheckpoint(true)

    self.unloaded = false
    self.minimised = false
    self.hidden = true
    self.animating = false
    self._revealing = false
    self.hasShownOnce = false
    self._collapsedShown = false

        self:LoadSettings()
    self:ChangeTheme(self.settings.theme)
    if self.settings.layoutMode == "collapsedSidebar"
        or (self.settings.layoutMode and self.settings.layoutMode ~= self.layout.mode)
    then
        self:_setLayoutMode(self.settings.layoutMode)
    end

    haptic.setContainer(self.screenGui)     haptic.setEnabled(self.settings.haptics)
    chrome.buildCollapsedFace(self)

    self:_constructionCheckpoint(true)
    -- [[The profile card is opt-in: it builds only when "Show profile" is on
    -- [[(the default is off), so the common window skips its ~90 themed
    -- [[instances on the spawn path. The first toggle to enable it builds the
    -- [[card on demand through profilePanel.ensureBuilt.]]
    profilePanel.ensureBuilt(self)
    profilePanel.setSubtitle(self, self.profileText)
    if profilePanel.isEnabled(self) then
        self.main.Position = self:_profileCenterPosition()
    end
    -- [[Card on but the player object is not replicated yet: this waiter fills
    -- [[and reveals it the moment the player arrives. It is also re-armed by
    -- [[setEnabled when the card is switched on later (lazy build), so the
    -- [[toggle path keeps the same late-player behaviour as startup.]]
    self:_armProfilePlayerWaiter()
    self:_bindKeybind()
    self:_bindMouseOverride()
    self:_bindTopbarDrag()
    self:_watchViewport()
    self:_updateWindowTitle()

    self:_applyMotionSetting()
    self:_syncLiveAnimation()

    -- [[Construction is complete: settings tab content may now be built]]
    -- [[lazily on first open (it is skipped while the window is still]]
    -- [[being constructed, e.g. by the first-tab auto-select).]]
    self._constructed = true

    -- [[Overlays wait for the window's own entrance before any of them starts]]
    -- [[building: the gate opens when that entrance lands (in `_firstShow`,]]
    -- [[after its settle and the reveal cascade that follows it). A host that]]
    -- [[fires a notification between CreateWindow and the first show still gets it —]]
    -- [[just behind the window instead of on top of it.]]
    overlayQueue.pause(self)

    return self
end

-- Yield at a completed construction boundary when the current frame has
-- spent its budget. Batches are limited by both CPU time and instance count,
-- and the check is unconditional: components that build lazily after startup
-- (the profile card on first enable, late settings panels) use this directly
-- so a deferred build can never monopolise a frame either. Yields land on the
-- next Heartbeat (not another deferred task in the same frame).
function Window:_paceBudget(force)
    local now = os.clock()
    local last = self._constructionClock or now
    local count = #self.instances
    if
        not force
        and now - last < constructionFrameBudget
        and count - (self._constructionInstances or 0) < constructionInstanceBudget
    then
        self._constructionClock = last
        return
    end
    self._constructionRevision = (self._constructionRevision or 0) + 1
    self._constructionBusy = (self._constructionBusy or 0) + 1
    task.wait()
    self._constructionBusy -= 1
    assert(not self.unloaded, "Window unloaded during construction")
    self._constructionClock = os.clock()
    self._constructionInstances = #self.instances
end

-- Startup-only checkpoint: element constructors stream in between the shell's
-- first reveal and the build going quiet, so yielding is gated to that window.
-- `force` marks an explicit shell phase boundary inside Window.new (where
-- `_constructed` is still false). Every boundary yields whole, completed
-- objects, never half a control, so each constructor resumes with a fully
-- usable object.
function Window:_constructionCheckpoint(force)
    if self._startupComplete or (not force and not self._constructed) then
        return
    end
    self:_paceBudget(force)
end

-- Configuration restoration can execute host callbacks. Apply a small group,
-- then return a frame to rendering so a large saved configuration cannot make
-- the freshly-opened window hitch. This is called only by persistenceConfig.
function Window:_loadCheckpoint(appliedCount)
    if appliedCount % 6 == 0 then
        task.wait()
        assert(not self.unloaded, "Window unloaded while loading configuration")
    end
end

-- [[The "Animation speed" setting owns the motion service's time scale. It is
-- applied from the window (not only from the settings control) so a window
-- built from persisted settings, a configuration load and a live change all
-- reach the same state; an unknown persisted value falls back to `normal`
-- instead of leaving the library on whatever the last window used.]]
function Window:_applyMotionSetting()
    if not motion.setProfile(self.settings.motionSpeed) then
        self.settings.motionSpeed = "normal"
        motion.setProfile("normal")
    end
end

function Window:_syncLiveAnimation()
    if not self.theme.LiveAnimation then
        self._liveAnimating = false         return
    end
    if self._liveAnimating then
        return     end

    self._liveAnimating = true
            self._liveGeneration = (self._liveGeneration or 0) + 1
    local generation = self._liveGeneration
    task.spawn(function()
        local forward = true
        -- [[Shared spec from the motion service: the ambient drift is long
        -- enough that re-creating its TweenInfo per cycle was pure waste.]]
        local liveTweenInfo = motion.spec("drift")

        while self._liveGeneration == generation and self._liveAnimating and not self.unloaded do
            -- [[The card beside the window is the same surface, so it rides the
            -- [[same animation: a static card next to a live window would show
            -- [[two different shades of the same gradient. It is read fresh
            -- [[each cycle because the card is rebuilt from scratch whenever
            -- [[the layout or the theme changes.]]
            local liveTargets = { self.windowGradient }
            local panelGradient = self.profilePanelGradient
            if panelGradient then
                table.insert(liveTargets, panelGradient)
            end

            local tweens = {}
            for _, gradient in liveTargets do
                local tween = state.tweenService:Create(gradient, liveTweenInfo, {
                    Offset = Vector2.new(if forward then 0.4 else -0.2, 0),
                    Rotation = (if forward then 220 else 280),
                })
                table.insert(tweens, tween)
                tween:Play()
            end

            self._liveTweens = tweens
            tweens[1].Completed:Wait()
            forward = not forward
        end
        if self._liveGeneration == generation then
            self._liveAnimating = false
            self._liveTweens = nil
        end
    end)
end

function Window:ChangeTheme(themeInput)
            local themeTable = if typeof(themeInput) == "table" then themeInput else resolveTheme(themeInput)
    for key, value in themeTable do
        self.theme[key] = coerceValue(key, value)
    end
        if typeof(themeInput) == "table" then
        deriveStrokes(self.theme, themeTable)
    end

    -- [[Before the first show the window is invisible, so routing every
    -- Color3/number through a 0.5s tween only buys dozens of tween allocations
    -- on the startup path — nothing can be seen animating. Assign the end
    -- state directly; the values are identical to what the tweens would have
    -- settled on, and everything from the first entrance on still tweens.
    -- When the theme *can* be seen changing, the per-instance properties are
    -- batched into one motion-service tween each (instead of one TweenService
    -- tween per property), so a theme switch both animates as one gesture and
    -- hands ownership of every animated property to the service.]]
    local instantApply = self.hidden and not self.hasShownOnce
    local pendingThemeTweens: { [Instance]: { [string]: any } } = {}
    for instance, properties in self.themeProperties do
        for prop, binding in properties do
            local resolvedValue = if typeof(binding) == "table" then binding[2](self.theme[binding[1]]) else self.theme[binding]

            if instance[prop] == resolvedValue then
                continue
            end
            if not instantApply and (typeof(resolvedValue) == "Color3" or typeof(resolvedValue) == "number") then
                local batch = pendingThemeTweens[instance]
                if not batch then
                    batch = {}
                    pendingThemeTweens[instance] = batch
                end
                batch[prop] = resolvedValue
            else
                instance[prop] = resolvedValue
            end
        end
    end
    if not instantApply then
        for instance, batch in pendingThemeTweens do
            motion.tween(instance, batch, "smooth")
        end
    end

                    if self.hidden then
                        self._themeRefreshPending = true
    else
        self:_refreshElementThemes()
    end

    self:_syncLiveAnimation()
    -- [[A theme can change the row title font, hence the rail width.]]
    self:_applyContentRailWidth()

    -- [[Capsule / minimised bar: keep the live corner on the theme's pill
    -- [[radius so ChangeTheme restyles the bar without needing a re-collapse.]]
    if self.windowCorner and (self._collapsedShown or self.minimised) and not self.animating then
        local pill = self.theme.PillCornerRadius or UDim.new(1, 0)
        if self.windowCorner.CornerRadius ~= pill then
            motion.tween(self.windowCorner, { CornerRadius = pill }, "smooth")
        end
    end
end

function Window:_refreshElementThemes()
    for _, tab in self.tabs do
        for _, element in tab.elements do
            if element._refreshTheme then
                element:_refreshTheme()
            end
        end
    end
end

function Window:CreateTab(props)
    assert(not self.unloaded, "Cannot create a tab on an unloaded window.")
    local tab = require(script.Parent.Parent.elements.tab).new(self, props)

    table.insert(self.tabs, tab)

    if not tab.neglectSelector then
        local isFirst = true
        for _, existingTab in self.tabs do
            if existingTab ~= tab and not existingTab.neglectSelector and self:_railGroup(existingTab) == self:_railGroup(tab) then
                isFirst = false
                break
            end
        end
        if isFirst then
            tab:Select(true)
        end

                        if not self.hidden and not self.minimised then
            tab.topbarItem.Visible = true
            tab:_applyVisual(
                if self.selectedTab == tab then "selected" else "unselected",
                "normal"
            )
        end
        -- [[A runtime-created tab may belong to the other rail.]]
        -- [[Re-apply the rail filter so it lands in the correct rail.]]
        self:_applySettingsLayout(self._settingsMode)
    end

    self:_constructionCheckpoint(false)
    return tab
end

function Window:CreateSection(props)
    assert(not self.unloaded, "Cannot create a section on an unloaded window.")
    local section = require(script.Parent.Parent.elements.tabSection).new(self, props)
    table.insert(self.tabSections, section)

            if not section.inert and not self.hidden and not self.minimised then
        section:_setVisible(true)
        section:_setShown(true, false)
    end

    return section
end

function Window:_setTabSectionsShown(shown, tweenInfo)
    for _, section in self.tabSections do
        section:_setShown(shown, tweenInfo)
    end
end

function Window:_setTabSectionsVisible(visible)
    for _, section in self.tabSections do
        section:_setVisible(visible)
    end
end

function Window:_registerControl(element)
    if not element.flag or element.flag == "" or element.forgetState then
        return
    end

    local flag = element.flag
    if self.controls[flag] then
        local n = 2
        while self.controls[flag .. n] do
            n += 1
        end
        flag = flag .. n
    end

    element.flag = flag
    self.controls[flag] = element
    return flag
end

function Window:_restoreLate(element)
    if not self._loadedConfig or not element.flag or element.forgetState then
        return
    end

    local wasLoading = self._loading
    self._loading = true     persistence.applyTo(element, self._loadedConfig[element.flag])
    self._loading = wasLoading
end

function Window:_persist(element)
    if not element.flag or element.forgetState or not self.configuration.autoSave or self._loading or self._saveScheduled then
        return
    end
    -- Default-on autosave must not write a whole file for every slider tick.
    self._saveScheduled = true
    task.delay(0.25, function()
        self._saveScheduled = false
        if not self.unloaded and self.configuration.autoSave and not self._loading then
            self:Save()
        end
    end)
end

function Window:_unregisterControl(element)
    if element.flag and self.controls[element.flag] == element then
        self.controls[element.flag] = nil
    end
end

function Window:_copyOverlayProps(props)
    local copy = {}
    if typeof(props) == "table" then
        for key, value in props do
            copy[key] = value
        end
    end
    return copy
end

-- Public notification API. Keep the queueing implementation private so all
-- callers, including settings and secure-mode diagnostics, use the same
-- bounded entrance lane.
function Window:Notify(props)
    if self.unloaded then
        return
    end
    return self:_showNotification(props)
end

function Window:_showNotification(props)
    if not self.notifications then
        self.notifications = self:Create("Frame", {
            Name = "Notifications",
            Size = UDim2.new(0, 300, 0, 800),
            Position = UDim2.new(1, -20, 1, -20),
            AnchorPoint = Vector2.new(1, 1),
            BackgroundTransparency = 1,

            Parent = self.screenGui,
        })

        self:Create("UIListLayout", {
            FillDirection = Enum.FillDirection.Vertical,
            VerticalAlignment = Enum.VerticalAlignment.Bottom,
            HorizontalAlignment = Enum.HorizontalAlignment.Center,
            SortOrder = Enum.SortOrder.LayoutOrder,
            Padding = UDim.new(0, 0),

            Parent = self.notifications,
        })
    end

    -- [[The card is built on the queue's turn rather than right here: a host
    -- that fires a notification for every module it loaded used to pay for
    -- twenty frames' worth of instances and tweens on one. The layer itself is
    -- created up front, so the first notification's own turn only builds the
    -- card. See `components/overlayQueue.luau`. The props are snapshotted for
    -- the same reason: a host that reuses one table between queued calls must
    -- not have its last write end up on every card.]]
    local snapshot = if typeof(props) == "table" then table.clone(props) else props
    overlayQueue.request(self, function(release)
        require(script.Parent.Parent.components.notification).new(self, snapshot, release)
    end)
end

function Window:Popup(props)
    if self.unloaded then
        return
    end
    return require(script.Parent.Parent.components.popup).new(self, props)
end

function Window:Close()
    if self.animating or self.unloaded then
        return
    end
    self.animating = true
    self.hidden = true
    self.collapsedInteract.Visible = false
    self._closeTransitionId = (self._closeTransitionId or 0) + 1
    local transitionId = self._closeTransitionId
    local originalSize = self.main.Size
    local originalPosition = self.main.Position
    -- [[A close is a departure: the shell contracts on the `glide` spec (a
    -- symmetric ease reads as the window folding itself away) while the
    -- surface accelerates out on `exit`, so the window never lingers at the
    -- tail of its own goodbye.]]
    local contractTween = "glide"
    local fadeOutTween = "exit"
    local targetSize = UDim2.new(
        originalSize.X.Scale * 0.88,
        math.floor(originalSize.X.Offset * 0.88),
        originalSize.Y.Scale * 0.88,
        math.floor(originalSize.Y.Offset * 0.88)
    )

    self.main.ClipsDescendants = true
    self.main.AnchorPoint = Vector2.new(0.5, 0.5)
    self.main.Position = originalPosition
    -- [[The handle leaves with the window it belongs to, and leaves parked:
    -- [[a close is not a state the next entrance should inherit a look from.
    self.drag:disable()
    self:_fadeSurfaces(false, fadeOutTween)
    if self.title then
        motion.tween(self.title, { TextTransparency = 1 }, fadeOutTween)
    end
    if self.subtitle then
        motion.tween(self.subtitle, { TextTransparency = 1 }, fadeOutTween)
    end
    if self.topbarIcon then
        motion.tween(self.topbarIcon, { ImageTransparency = 1 }, fadeOutTween)
    end
    self:_fadeSelectedElementsOut()
    -- [[The contraction goes through the motion service so the close answers
    -- the speed setting like the rest of the window. A nil return means
    -- nothing was animated (motion switched off, or the frame is already
    -- where the transition was heading): the teardown still has to run, or
    -- the window would stay alive and invisible.]]
    local closed = false
    local function finishClose()
        if closed or self.unloaded or self._closeTransitionId ~= transitionId then
            return
        end
        closed = true
        self.drag:disable()
        self:Unload()
    end

    -- [[The surface accelerates out first (a separate tween from the
    -- contraction: one owner per property, and the fade never delays the
    -- settle path the teardown waits on).]]
    motion.tween(self.main, { BackgroundTransparency = 1 }, fadeOutTween)
    if not motion.tween(self.main, {
        Size = targetSize,
        Position = originalPosition,
    }, contractTween, finishClose) then
        -- [[Nothing animated, so the teardown just ran and the window's
        -- instances are gone — the corner below must not be touched.]]
        finishClose()
        if self.unloaded then
            return
        end
    end
    -- [[Collapse to a capsule using the theme's pill radius so a custom
    -- [[PillCornerRadius still reads on the closing shrink. A synchronous
    -- completion (the test harness plays tweens on the spot) may have torn
    -- the window down inside the call above; never touch the corner then.]]
    if not self.unloaded then
        motion.tween(self.windowCorner, { CornerRadius = self.theme.PillCornerRadius or UDim.new(1, 0) }, contractTween)
    end
end

function Window:Hide()
    -- Cancel pending startup reveal even while the window is still hidden.
    self._autoShowCancelled = true
    -- [[A window whose entrance is never going to happen (the cancelled
    -- [[auto-show above) must not keep the overlays it was already asked for
    -- [[parked behind an entrance that will not arrive.]]
    if not self.hasShownOnce then
        overlayQueue.resume(self)
    end
    if self.animating or self.hidden then
        return
    end

            if self._searching then
        search.close(self, { showTabs = false, jumpTo = self.selectedTab and self.selectedTab.tabPage })
    end

    self.animating = true
    self._revealing = true
    self.hidden = true
    self.collapsedInteract.Visible = false
                if self.minimised then
        self.minimised = false
        image.assign(self.minimiseAction.iconLabel, "Image", self:ResolveIcon(constants.icons.minimise))     end

        self._restorePosition = self.main.Position

    local collapsedPos, collapsedSize = self:_collapsedRect()

    -- [[The fold: content leaves quickly (snappy fades), the shell glides
    -- into the capsule with the symmetric ease, and the capsule's face
    -- appears with a small settle overshoot so the fold ends on a landing,
    -- not a stop.]]
    local hideTween = "snappy"
    local surfaceTween = "glide"
    local cornerTween = "glide"
    local collapseShowTween = "settle"

            self._hideTransitionId = (self._hideTransitionId or 0) + 1
    local transitionId = self._hideTransitionId

    -- [[The handle folds away with the surface: the pill shrinks out first
    -- [[and the hitbox follows once the fade has read (the handle owns that
    -- [[timing, and stands down if a Show claims it back in the meantime).
    self.drag:fadeOut("fast")

            self:_fadeSurfaces(false, hideTween)

    if self.title then
        motion.tween(self.title, { TextTransparency = 1 }, hideTween)
    end
    if self.subtitle then
        motion.tween(self.subtitle, { TextTransparency = 1 }, hideTween)
    end
    if self.topbarIcon then
        motion.tween(self.topbarIcon, { ImageTransparency = 1 }, hideTween)
    end

    for _, actionFrame in ipairs(self.actionContainer:GetChildren()) do
        local iconLabel = self:_actionIconLabel(actionFrame)
        if iconLabel then
            motion.tween(iconLabel, { ImageTransparency = 1 }, hideTween)
        end
        if actionFrame:IsA("Frame") then
            self:_syncActionChrome(actionFrame, false, hideTween)
        end
    end

    for _, tab in pairs(self.tabs) do
        if not tab.neglectSelector and tab.topbarItem then
            tab:_applyVisual("hidden", hideTween)
        end
    end
    self:_setTabSectionsShown(false, hideTween)

    self:_fadeSelectedElementsOut()

    -- [[Same contract as the close: settle from the tween's Completed, or
    -- immediately when there was nothing to animate.]]
    local collapsed = false
    local function settleCollapse()
                        if collapsed or self.unloaded or not self.hidden or self._hideTransitionId ~= transitionId then
            return
        end
        collapsed = true

                        self._collapsedShown = true
        self.collapsedInteract.Visible = true
        self.animating = false
        self._revealing = false
    end

    if not motion.tween(self.main, { Size = collapsedSize, Position = collapsedPos }, surfaceTween, settleCollapse) then
        settleCollapse()
    end
    -- [[Capsule bar: theme PillCornerRadius (Gen2 default is a full pill).]]
    motion.tween(self.windowCorner, { CornerRadius = self.theme.PillCornerRadius or UDim.new(1, 0) }, cornerTween)

    self.topbar.Visible = true
    self:_setContentVisible(false)
    chrome.setCollapsedShown(self, true, collapseShowTween)
end

function Window:ToggleHide()
    if self.animating then
        return
    end
    if self.hidden then
        self:Show()
    else
        self:Hide()
    end
end

function Window:ToggleMinimise()
    if self.animating or self.hidden then
        return
    end

        if self._searching then
        search.close(self, { showTabs = true })
    end

    self.animating = true
    -- [[Either direction moves one surface between two shapes, and the handle
    -- [[is not one of them: it fades out with the shell and is handed back by
    -- [[the settle below (the fold's own `minimised` write turns it on again
    -- [[under the capsule, exactly as before).
    self.drag:fadeOut("fast")
    self.collapsedInteract.Visible = false

    -- [[Both directions of the minimise move one surface between two shapes,
    -- so both ride the same pair of specs: `smooth` carries the geometric
    -- unfold (a big decelerating move reads as arriving), `settle` lands the
    -- capsule face when folding away.]]
    local expandTween = "smooth"
    local surfaceTween = "smooth"

    self._hideTransitionId = (self._hideTransitionId or 0) + 1

    if self.minimised then
        self.minimised = false
        image.assign(self.minimiseAction.iconLabel, "Image", constants.icons.minimise)
        motion.tween(self.main, { Size = self.size }, expandTween)
        motion.tween(self.windowCorner, { CornerRadius = self.theme.CornerRoundness }, expandTween)
        self:_fadeSurfaces(true, surfaceTween)	chrome.setCollapsedShown(self, false, surfaceTween)

        -- [[The card comes back with the expanded window and the pair has]]
        -- [[to fit: the capsule's own clamp only knew the capsule, so one]]
        -- [[dragged into a corner used to expand with the window — or its]]
        -- [[card — hanging off the edge. Re-clamp now the panel is shown]]
        -- [[again (a no-op while the pair already fits, and while the card]]
        -- [[is off).]]
        self:_clampToScreen()

        motion.tween(self.drag.drag, {
            Position = UDim2.new(
                self.main.Position.X.Scale,
                self.main.Position.X.Offset,
                self.main.Position.Y.Scale,
                self.main.Position.Y.Offset + self.size.Y.Offset / 2 + dragHandleGap
            ),
        }, expandTween)

                        task.delay(0.2, function()
            if self.unloaded or self.minimised or self.hidden then
                return             end
            self:_setContentVisible(true)

            for _, tab in pairs(self.tabs) do
                if not tab.neglectSelector and tab.topbarItem then
                    tab.topbarItem.Visible = true
                    tab:_applyVisual(if self.selectedTab == tab then "selected" else "unselected", surfaceTween)
                end
            end
            -- [[Restore the settings/normal rail split; the loop above shows all tabs.]]
            self:_applySettingsLayout(self._settingsMode)
            self:_setTabSectionsVisible(true)
            self:_setTabSectionsShown(true, surfaceTween)

            self:_revealElements(0.035, 0.4)
        end)

        task.delay(0.5, function()
            if self.unloaded or self.minimised or self.hidden then
                return
            end
            -- [[The window is back to its full size, so the handle is
            -- [[reachable again. It is handed back unseen (the pill was
            -- [[parked when the fold started) and waits for the pointer,
            -- [[exactly like the handle of a freshly opened window.
            self.drag:enable()
            self.animating = false
        end)
    else
        self.minimised = true
        image.assign(self.minimiseAction.iconLabel, "Image", self:ResolveIcon(constants.icons.maximise))
                        self:_fadeSelectedElementsOut()
        for _, tab in pairs(self.tabs) do
            if not tab.neglectSelector and tab.topbarItem then
                tab:_applyVisual("hidden", surfaceTween)
            end
        end
        self:_setTabSectionsShown(false, surfaceTween)

        task.delay(0.3, function()
            if self.unloaded or not self.minimised then
                return             end
            self:_setContentVisible(false)
            for _, tab in pairs(self.tabs) do
                if not tab.neglectSelector and tab.topbarItem then
                    tab.topbarItem.Visible = false
                end
            end
            self:_setTabSectionsVisible(false)
        end)

        self:_fadeSurfaces(false, surfaceTween)	chrome.setCollapsedShown(self, true, surfaceTween)
        motion.tween(self.main, { Size = UDim2.fromOffset(self.size.X.Offset, self.layout.topbarHeight) }, expandTween)
        -- [[Minimised bar reads as a theme-aware capsule (PillCornerRadius).]]
        motion.tween(self.windowCorner, { CornerRadius = self.theme.PillCornerRadius or UDim.new(1, 0) }, expandTween)

        motion.tween(self.drag.drag, {
            Position = UDim2.new(
                self.main.Position.X.Scale,
                self.main.Position.X.Offset,
                self.main.Position.Y.Scale,
                self.main.Position.Y.Offset + self.layout.topbarHeight / 2 + dragHandleGap
            ),
        }, expandTween)

        task.delay(0.5, function()
            if self.unloaded then
                return
            end
            if self.minimised and not self.hidden then
                self:_syncDragBar()
                self.drag:enable()
            end
            self.animating = false
        end)
    end
end

function Window:_syncDragBar()
    local dragFrame = self.drag and self.drag.drag
    if not dragFrame then
        return
    end
    local pos = self.main.Position
    local dragOffset = if self.minimised then self.layout.topbarHeight / 2 + dragHandleGap else self.size.Y.Offset / 2 + dragHandleGap
    dragFrame.Position = UDim2.new(pos.X.Scale, pos.X.Offset, pos.Y.Scale, pos.Y.Offset + dragOffset)
end

function Window:_clampedPosition(position: UDim2): UDim2
    if not self.settings or not self.settings.keepOnScreen then
        return position
    end
            if position.X.Scale ~= 0 or position.Y.Scale ~= 0 then
        return position
    end

    local screenSize = self.screenGui.AbsoluteSize
    -- [[The window and its profile card travel as one unit, so the clamp]]
    -- [[keeps the *pair* in view: the card adds its width + gap to one]]
    -- [[side and, being taller than a short window, raises the vertical]]
    -- [[half-extent too. Clamping the window alone let the card be dragged]]
    -- [[off the edge while "keep window on screen" was still satisfied.]]
    local halfLeft, halfRight, halfTop = profilePanel.pairHalfSize(self)
    local margin = 8
        local x = math.clamp(position.X.Offset, halfLeft + margin, math.max(halfLeft + margin, screenSize.X - halfRight - margin))
    local y = math.clamp(position.Y.Offset, halfTop + margin, math.max(halfTop + margin, screenSize.Y - halfTop - margin))
    if x == position.X.Offset and y == position.Y.Offset then
        return position
    end
    return UDim2.fromOffset(x, y)
end

function Window:_clampToScreen()
    self.main.Position = self:_clampedPosition(self.main.Position)
end

-- [[The window's canonical resting position. With the profile panel]]
-- [[enabled, the window + gap + panel are treated as one unit and that]]
-- [[unit is centred: the window itself sits half a panel away from the]]
-- [[screen centre, on the opposite side of the panel, so neither side of]]
-- [[the pair looks cramped.]]
function Window:_profileCenterPosition(): UDim2
    local shift = profilePanel.shiftFor(self)
    if shift <= 0 then
        return UDim2.new(0.5, 0, 0.5, 0)
    end
    local left = (self.settings.profileSide or "right") == "left"
    return UDim2.new(0.5, if left then shift else -shift, 0.5, 0)
end

function Window:_recenterForProfile(animate)
    local target = self:_profileCenterPosition()
    if self.hidden or self.minimised then
        -- [[Nothing is on screen to move; park the remembered position so]]
        -- [[the next restore lands centred for the current panel state.]]
        self._restorePosition = target
        return
    end
    if self.main.Position == target then
        return
    end
    if animate and not self.animating then
        -- [[A recenter slides the pair sideways between resting places: the
        -- symmetric `glide` ease reads as deliberate, not reactive.]]
        motion.tween(self.main, { Position = target }, "glide")
        motion.tween(self.drag.drag, {
            Position = UDim2.new(target.X.Scale, target.X.Offset, target.Y.Scale, target.Y.Offset + self.size.Y.Offset / 2 + dragHandleGap),
        }, "glide")
    else
        self.main.Position = target
        self:_syncDragBar()
    end
end

-- [[One-shot waiter for a LocalPlayer that has not replicated when the card
-- [[was enabled. On arrival it builds the (lazily skipped) card if needed,
-- [[fills its identity and reveals the window + card pair. Bounded by a short
-- [[deadline so a host with no player at all does not keep polling forever.
-- [[Safe to call repeatedly; the first arm wins.
function Window:_armProfilePlayerWaiter()
    if self._profileWaiterArmed or self.unloaded then
        return
    end
    if self.settings.showProfile ~= true or state.localPlayer then
        return
    end
    self._profileWaiterArmed = true

    task.defer(function()
        local deadline = os.clock() + 2
        local connection
        connection = state.runService.RenderStepped:Connect(function()
            if self.unloaded then
                connection:Disconnect()
                return
            end
            if state.localPlayer or os.clock() > deadline then
                connection:Disconnect()
                if state.localPlayer and profilePanel.ensureBuilt(self) then
                    profilePanel.applyIdentity(self)
                    profilePanel.setShown(self, true, profilePanel.settleSpec)
                    -- [[Centre the pair if the user has not dragged the
                    -- [[window off its resting spot.]]
                    if self.main.Position.X.Scale == 0.5 and self.main.Position.Y.Scale == 0.5 then
                        self:_recenterForProfile(true)
                    end
                end
            end
        end)
        -- [[Adopt the connection directly: Window:Connect takes a *signal*,
        -- [[not a connection object.]]
        table.insert(self.connections, connection)
    end)
end

function Window:_applyWindowSize()
    if self.unloaded then
        return
    end
    -- [[Desktop/PC: newSize tracks the viewport (responsive, min/max protected).]]
    -- [[Mobile: windowSizing.fit returns the fixed mobile profile on every call, so]]
    -- [[viewport changes (rotation, keyboard, inset) never resize the window here.]]
    local newSize = computeSize(self.layout.mode)
    local sizeChanged = newSize ~= self.size
    self.size = newSize
    -- [[Nothing moved: no rail write, no layout invalidation. This is the
    -- [[window's steady state - the viewport poll below calls this every
    -- [[couple of seconds for the whole session, and the rail write used to
    -- [[happen before the check.]]
    if not sizeChanged and not self._pendingResize then
        return
    end
    -- [[The rail follows the window's size, its layout and its tabs, all of
    -- [[which are in step by the time this runs: it only has to be written
    -- [[when one of them moved, which is what this function is for.]]
    self:_applyRailWidth()
    if self.hidden or self.minimised or self.animating or self._revealing then
        self._pendingResize = true
        return
    end
    self._pendingResize = false
    self.main.Size = newSize
    self:_clampToScreen()
    self:_syncDragBar()
    -- [[A viewport change (rotation, inset, keyboard) can turn the profile]]
    -- [[panel on or off: there may suddenly be room for it, or none.]]
    -- [[Re-evaluate its visibility alongside the resize and keep the card]]
    -- [[height matched to the window height.]]
    profilePanel.layout(self)
    profilePanel.setShown(self, true, nil)
    if self.main.Position.X.Scale == 0.5 and self.main.Position.Y.Scale == 0.5 then
        -- [[Still anchored at the screen centre (the user has not dragged]]
        -- [[it): re-apply the resting centre for the new panel state.]]
        self:_recenterForProfile(false)
    end
end

-- [[Effective rail width. Non-sidebar and collapsed rails keep the existing]]
-- [[fixed behavior; the expanded responsive rail instead hugs the widest]]
-- [[visible tab row (railContentWidth), capped by the window's available]]
-- [[space so the elements area always keeps at least half the window.]]
function Window:_railWidth()
    local base = layouts.railWidthFor(self.layout, self.size.X.Offset)
    if self.layout.mode ~= "sidebar" or self.layout.forceCollapsed then
        return base
    end
    if base < (self.layout.railWidth :: number) then
        return base
    end
    local content = tabSelector.railContentWidth(self, self.layout)
    if content <= 0 then
        return base
    end
    local cap = math.floor(self.size.X.Offset * 0.5)
    return math.min(content, cap)
end

function Window:_applyContentRailWidth()
    if self.layout.mode ~= "sidebar" or self.layout.forceCollapsed then
        return
    end
    local implementation = layouts.implementation(layoutKey(self.layout))
    implementation.ApplyWidth(self, self:_railWidth())
end

function Window:_applyRailWidth()
    local implementation = layouts.implementation(layoutKey(self.layout))
    implementation.ApplyWidth(self, self:_railWidth())

    if self._settingsLayoutActive or self.selectedTab == self.rfSettings then
        self:_applySettingsLayout(true)
    end
end

function Window:_watchViewport()
    local cameraConnection: RBXScriptConnection? = nil
    local pending = false

    local function scheduleUpdate()
                if pending then
            return
        end
        pending = true
        task.defer(function()
            pending = false
            self:_applyWindowSize()
        end)
    end

    local function rebindCamera()
        if cameraConnection then
            self:Disconnect(cameraConnection)
            cameraConnection = nil
        end
        local camera = state.workspace.CurrentCamera
        if camera then
            cameraConnection = self:Connect(camera:GetPropertyChangedSignal("ViewportSize"), scheduleUpdate)
        end
        scheduleUpdate()
    end

    self:Connect(state.workspace:GetPropertyChangedSignal("CurrentCamera"), rebindCamera)
    rebindCamera()

            local elapsed = 0
    self:Connect(state.runService.Heartbeat, function(dt: number)
        elapsed += dt
        if elapsed < viewportCheckInterval then
            return
        end
        elapsed = 0
        self:_applyWindowSize()
    end)
end

function Window:_bindKeybind()
    self:Connect(state.userInputService.InputBegan, function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == self.settings.toggleKeybind or input.UserInputType == self.settings.toggleKeybind then
            self:ToggleHide()
        end
    end)
end

function Window:_bindMouseOverride()
    local inputService = state.userInputService

    local function shouldOverride()
        return self.settings.mouseOverride and not self.hidden and not self.minimised
    end

    local function restoreMouse()
        if not shouldOverride() then
            return
        end
                        if inputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
            return
        end
        if inputService.MouseBehavior ~= Enum.MouseBehavior.Default then
            inputService.MouseBehavior = Enum.MouseBehavior.Default
        end
        if not inputService.MouseIconEnabled then
            inputService.MouseIconEnabled = true
        end
    end

            self:Connect(inputService:GetPropertyChangedSignal("MouseBehavior"), restoreMouse)
    self:Connect(inputService:GetPropertyChangedSignal("MouseIconEnabled"), restoreMouse)

            self:Connect(inputService.InputEnded, function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton2 then
            restoreMouse()
        end
    end)

        self._freeMouse = restoreMouse
end

function Window:_bindTopbarDrag()
    local inputService = state.userInputService
    local isDragging = false
    local hasMoved = false
    local dragOffset = Vector2.zero
    local renderConnection
    local startMousePos = Vector2.zero

            local guiInset = Vector2.zero
    if self.screenGui and self.screenGui.IgnoreGuiInset then
        guiInset = state.guiService:GetGuiInset()
    end

    local function getConstrainedPosition()
        local rawPos = inputService:GetMouseLocation() + dragOffset + guiInset
        local x, y = rawPos.X, rawPos.Y

        if self.settings and self.settings.keepOnScreen then
            local mainSize = self.main.AbsoluteSize
            local screenSize = self.screenGui.AbsoluteSize
            local margin = 8
            -- [[Clamp the window + profile card pair rather than the window]]
            -- [[alone, so a drag cannot leave the card hanging off the]]
            -- [[screen. The frame's own size is passed in: mid-minimise it]]
            -- [[is shorter than window.size.]]
            local halfLeft, halfRight, halfTop = profilePanel.pairHalfSize(self, mainSize.X, mainSize.Y)
            x = math.clamp(x, halfLeft + margin, math.max(halfLeft + margin, screenSize.X - halfRight - margin))
            y = math.clamp(y, halfTop + margin, math.max(halfTop + margin, screenSize.Y - halfTop - margin))
        end

        return UDim2.fromOffset(x, y)
    end

    local function endDrag()
        isDragging = false
        if hasMoved then
            -- [[The move is over: the handle drops back to its resting look
            -- [[and stays where it is — that faint pill under the window is
            -- [[the affordance the next drag starts from.
            hasMoved = false
            if self.drag then
                self.drag:setMoving(false)
            end
        end
        if renderConnection then
            self:Disconnect(renderConnection)
            renderConnection = nil
        end
    end

    local function updateDrag()
        if not isDragging then
            endDrag()
            return
        end
        if self.unloaded or not self:_canDragCapsule() then
            endDrag()
            return
        end

        -- [[Do not reposition the window for a simple click/tap. Wait until]]
        -- [[the pointer has actually moved before treating the gesture as drag.]]
        local pointer = inputService:GetMouseLocation()
        if (pointer - startMousePos).Magnitude < 4 then
            return
        end

        if not hasMoved then
            -- [[The pointer has left the threshold, so this is a move and not
            -- [[a click: bring the handle out for the length of it. Nothing is
            -- [[pointing at the handle (the gesture started on the topbar), so
            -- [[this is the one moment it has to show itself — the pill
            -- [[brightens here and settles back when the move ends.
            hasMoved = true
            if self.drag then
                self.drag:setMoving(true)
            end
        end

        local constrainedPos = getConstrainedPosition()
        if self.main.Position == constrainedPos then
            return
        end
        self.main.Position = constrainedPos
        if self.drag and self.drag.drag then
            local mainPos = self.main.Position
            self.drag.drag.Position = UDim2.new(
                mainPos.X.Scale,
                mainPos.X.Offset,
                mainPos.Y.Scale,
                mainPos.Y.Offset + (self.main.Size.Y.Offset / 2 + dragHandleGap)
            )
        end
    end

                local function isOverInteractive(x, y)
        for _, region in { self.tabList, self.actionContainer } do
            local absPos, absSize = region.AbsolutePosition, region.AbsoluteSize
            if x >= absPos.X and x <= absPos.X + absSize.X and y >= absPos.Y and y <= absPos.Y + absSize.Y then
                return true
            end
        end
        return false
    end

    self:Connect(self.topbar.InputBegan, function(input, gameProcessed)
        if gameProcessed then
            return
        end

        if not self:_canDragCapsule() then
            return
        end

        local inputType = input.UserInputType.Name
        if inputType ~= "MouseButton1" and inputType ~= "Touch" then
            return
        end

        if isOverInteractive(input.Position.X, input.Position.Y) then
            return
        end

                if not self:_interactive() then
            return
        end

        isDragging = true
        if renderConnection then
            self:Disconnect(renderConnection)
            renderConnection = nil
        end
        renderConnection = self:Connect(state.runService.RenderStepped, updateDrag)

        if self.screenGui and self.screenGui.IgnoreGuiInset then
            guiInset = state.guiService:GetGuiInset()
        end

        startMousePos = inputService:GetMouseLocation()
        dragOffset = self.main.AbsolutePosition + self.main.AbsoluteSize * self.main.AnchorPoint - inputService:GetMouseLocation()
    end)

    self:Connect(inputService.InputEnded, function(input)
        local inputType = input.UserInputType.Name
        if inputType == "MouseButton1" or inputType == "Touch" then
            endDrag()
        end
    end)

            self:Connect(inputService.WindowFocusReleased, function()
        endDrag()
    end)
end

function Window:_buildSettingsUI()
    settingsComponent.buildUI(self)
end

function Window:_buildSettingsContent(tab)
    settingsComponent.buildContent(self, tab)
end

-- [[Profile card content owned by the host. Accepts either the legacy
-- [[subtitle string (nil clears it and falls back to the @username line) or a
-- [[table carrying the card's host-supplied rows:
-- [[  window:SetProfile({ subtitle = "…", key = "…", tier = "PREMIUM",
-- [[    whitelist = { status = "Active", daysLeft = 14 } })
-- [[Anything not supplied reads "—" on the card; Astra holds no key or
-- [[whitelist store of its own.]]
function Window:SetProfile(profile)
    local data = profilePanel.setProfile(self, profile)
    self.profileText = data and data.subtitle or nil
    return self.profileText
end

function Window:SaveSettings()
    return persistence.saveSettings(self)
end

function Window:LoadSettings()
    local loaded, migrated = persistence.loadSettings(self)
    self.configuration.autoSave = self.settings.autoSaveConfig
    self.configuration.autoLoad = self.settings.autoLoadConfig
    -- [[The animation-speed setting lives in the same file, and the motion
    -- service is a library-wide singleton, so it is re-applied here rather
    -- than only when the settings control is touched — otherwise a window
    -- that loaded settings would animate at whatever the last one used.]]
    self:_applyMotionSetting()
    -- [["Reveal profile details" depends on "Show profile", and a settings]]
    -- [[file written before that rule (or edited by hand) can carry reveal]]
    -- [[= on with the card off. Normalise the pair on the way in, so the]]
    -- [[card never unmasks identity data while it is hidden and the toggle]]
    -- [[is built showing the truth.]]
    profilePanel.syncReveal(self)
    if migrated then
        -- [[Persist the normalized legacy profile state once. This keeps the
        -- [[default off without overriding a deliberate choice on later boots.]]
        self:SaveSettings()
    end
    return loaded
end

-- [[`token` is the theme key the rounded corners read; it defaults to the
-- [[window's own roundness, but callers inside an element pass that element's
-- [[radius token so an inner surface can share the container's arcs.]]
function Window:_roundCorners(frame, corners, token)
    token = token or "CornerRoundness"
    if not corners or not hasPerCornerRadius then
        return self:Create("UICorner", {
            Parent = frame,
        }, { CornerRadius = token })
    end

    local props = { Parent = frame }
    local themeMap = {}
    for _, key in cornerKeys do
        props[key] = UDim.new(0, 0)
    end
    for _, corner in corners do
        props[corner] = nil         themeMap[corner] = token
    end

    return self:Create("UICorner", props, themeMap)
end

-- [[Companion to `_roundCorners` for a corner set an element flips with its own
-- [[state instead of once at construction: `corners` (default: all four, as in
-- [[`_roundCorners`) ride the `token` radius and every other corner of the frame
-- [[goes back to square, so a caller can move which of an inner surface's edges
-- [[match the container's arcs — a collapsible header is the card's bottom edge
-- [[only while it is closed. No theme binding is recorded here (`themeProperties`
-- [[still owns whatever `_roundCorners` bound), so a later `ChangeTheme` re-reads
-- [[`token` through this same call from the element instead of fighting it. There
-- [[is nothing to move on an engine without per-corner radii, where
-- [[`_roundCorners` already gave the frame one radius on all four corners, so the
-- [[call is a no-op there.]]
function Window:_setRoundedCorners(corner, corners, token)
    if not corner or not hasPerCornerRadius then
        return
    end

    local radius = if token then self.theme[token] else nil
    for _, key in cornerKeys do
        local wanted = radius ~= nil and (not corners or table.find(corners, key) ~= nil)
        local target = if wanted then radius else UDim.new(0, 0)
        if corner[key] ~= target then
            corner[key] = target
        end
    end
end

function Window:_setElementLocked(element, locked, message)
    locked = locked == true
                local wasLocked = element.locked == true
    if wasLocked == locked and not (locked and message) then
        return
    end
    element.locked = locked

    if not element.lockScrim then
        self:_buildLockScrim(element)
    end

    -- [[The lock scrim arrives on the shared hover spec; the unlock delay
    -- tracks its duration so the scrim never hides after it has faded.]]
    local lockSpec = "snappy"

    if locked then
        element.lockScrim.Visible = true
    end
    motion.tween(element.lockScrim, {
        BackgroundTransparency = if locked then lockScrimTransparency else 1,
    }, lockSpec)
    if not locked then
        task.delay(0.25, function()
            if not element.locked and element.lockScrim and element.lockScrim.Parent then
                element.lockScrim.Visible = false             end
        end)
    end

        local label = element.descriptionLabel
    if not label then
        return
    end

    if locked then
                        element._descriptionBefore = element._descriptionBefore or label.Text
        label.Text = locale.resolve(message or "This element is locked.")
    elseif element._descriptionBefore then
        label.Text = element._descriptionBefore
        element._descriptionBefore = nil
    end

    motion.tween(label, {
        TextTransparency = if locked then lockedTextTransparency else 0.45,
    }, lockSpec)
end

function Window:_buildLockScrim(element)
    element.lockScrim = self:Create("TextButton", {
        Name = "ElementLock",
        Active = true,
        AutoButtonColor = false,
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        Text = "",
        TextTransparency = 1,
        ZIndex = constants.zIndex.elementLock,
        Visible = false,

                        BackgroundTransparency = 1,
        Parent = element.main,
    }, { BackgroundColor3 = { "WindowColor", firstColor } })

    self:Create("UICorner", {
        Parent = element.lockScrim,
    }, { CornerRadius = "ElementCornerRadius" })
end

-- [[Topbar tab-strip chrome: the strip is inset from the window edge and a]]
-- [[gradient fade marks whichever edge is clipping tabs. Runs off canvas/]]
-- [[size change signals (see Topbar.Build) and skips writes when the target]]
-- [[state is unchanged, so steady-state cost is one comparison per signal.]]
function Window:_refreshTabStripChrome()
    local strip = self.tabList
    local fadeLeft = self.tabFadeLeft
    local fadeRight = self.tabFadeRight
    if self.layout.mode ~= "top" or not strip or not strip.Parent then
        return
    end

    -- [[The canvas carries an end gutter on each side (UIPadding), and the]]
    -- [[frame itself is inset from the window edge, so clipping is read from]]
    -- [[the last pill's canvas-space right edge against the frame's full]]
    -- [[width — AbsoluteWindowSize alone would count the trailing gutter as]]
    -- [[clipped content and light the fade a gutter too early.]]
    local edgePadding = self.layout.tabStripEdgePadding or 14
    local frameWidth = strip.AbsoluteSize.X
    local scrollX = strip.CanvasPosition.X
    local contentRight = strip.AbsoluteCanvasSize.X - edgePadding

    local rightClipped = contentRight > scrollX + frameWidth + 0.5
    if fadeRight and fadeRight.Parent and fadeRight.Visible ~= rightClipped then
        fadeRight.Visible = rightClipped
    end

    local scrolledFromLeft = scrollX > 0.5
    if fadeLeft and fadeLeft.Parent and fadeLeft.Visible ~= scrolledFromLeft then
        fadeLeft.Visible = scrolledFromLeft
    end
end

-- [[Keep the selected pill inside the strip's viewport after a selection.]]
-- [[Children of the ScrollingFrame sit in canvas space (the end-gutter]]
-- [[padding shifts the first pill's origin), so item.Position is the tab's]]
-- [[offset along the strip without any absolute-position math; the landing]]
-- [[clearance parks the pill just clear of whichever edge fade is showing.]]
function Window:_scrollSelectedTabIntoView(tweened)
    local strip = self.tabList
    if self.layout.mode ~= "top" or not strip or not strip.Parent then
        return
    end

    local tab = self.selectedTab
    local item = tab and (not tab.neglectSelector) and tab.topbarItem
    if not item or not item.Parent then
        return
    end

    local edgePadding = self.layout.tabStripEdgePadding or 14
    local fadeLanding = self.layout.tabStripFadeWidth or 30
    local frameWidth = strip.AbsoluteSize.X
    local maxScroll = math.max(0, strip.AbsoluteCanvasSize.X - frameWidth)
    local left = item.Position.X.Offset
    local right = left + item.AbsoluteSize.X
    local pos = strip.CanvasPosition.X

    -- [[Move the strip only when the pill is clipped or pressed up
    -- [[against an edge fade; a comfortably visible tab stays put, so
    -- [[selecting it does not re-scroll the strip for no reason. The left]]
    -- [[landing clears the fade; the right landing leaves the end gutter]]
    -- [[visible beside the pill.]]
    local target = nil
    if left < pos + fadeLanding then
        target = left - fadeLanding
    elseif right > pos + frameWidth - edgePadding then
        target = right + edgePadding - frameWidth
    end
    if target == nil then
        return
    end
    target = math.clamp(target, 0, maxScroll)
    if math.abs(pos - target) < 1 then
        return
    end

    if tweened then
        motion.tween(strip, { CanvasPosition = Vector2.new(target, 0) }, "snappy")
    else
        strip.CanvasPosition = Vector2.new(target, 0)
    end
end

function Window:_setLayoutMode(modeName)
    if self.unloaded then
        return false
    end
    if self.animating or self.minimised or (self.hidden and self.hasShownOnce) then
        return false
    end
    local newLayout = layouts.get(modeName)
    local oldKey = layoutKey(self.layout)
    if not newLayout or modeName == oldKey then
        return false
    end
    if self._layoutTransitioning then
        return false
    end
    self._layoutTransitioning = true
    self._layoutTransitionId = (self._layoutTransitionId or 0) + 1
    local transitionId = self._layoutTransitionId
    local tweenInfo = "fast"
    self:_fadeSurfaces(false, tweenInfo)
    self:_fadeSelectedElementsOut()
    for _, tab in self.tabs do
        if not tab.neglectSelector and tab.topbarItem then
            tab:_applyVisual("hidden", tweenInfo)
        end
    end
    task.wait(0.12)
    if self.unloaded then
        self._layoutTransitioning = false
        return false
    end
    if self._searching then
        search.close(self, { showTabs = true, jumpTo = self.selectedTab and self.selectedTab.tabPage })
    end
    local oldKey = layoutKey(self.layout)
    self.layout = newLayout
    self.settings.layoutMode = modeName
    if self.sidebar then
        self:DestroySubtree(self.sidebar)
        self.sidebar = nil
    elseif self.tabList then
        self:DestroySubtree(self.tabList)
    end
    -- [[The edge fades live beside the topbar strip, so they are topbar]]
    -- [[chrome: tear them down on the way out of "top" mode.]]
    if self.tabFadeLeft then
        self:DestroySubtree(self.tabFadeLeft)
        self.tabFadeLeft = nil
    end
    if self.tabFadeRight then
        self:DestroySubtree(self.tabFadeRight)
        self.tabFadeRight = nil
    end
    local implementation = layouts.implementation(modeName)
    implementation.Build(self, newLayout)
    if newLayout.mode == "sidebar" then
        if not self.elementsCorner then
            self.elementsCorner = self:_roundCorners(self.elements, newLayout.cardCorners)
            self.elementsStroke = self:Create("UIStroke", {
                ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
                Transparency = 1,
                Parent = self.elements,
            }, { Color = "SurfaceStroke" })
            self:Create("UIGradient", {
                Rotation = newLayout.cardStrokeRotation,
                Transparency = newLayout.cardStrokeTransparency,
                Parent = self.elementsStroke,
            })
        end
    else
        if self.elementsCorner then
            self.elementsCorner.CornerRadius = UDim.new(0, 0)
        end
        if self.elementsStroke then
            self.elementsStroke.Transparency = 1
        end
        self.elements.Size = UDim2.new(1, 0, 1, -newLayout.chromeHeight)
        self.bottomFade.Size = UDim2.new(1, 0, newLayout.fadeSize.Y.Scale, newLayout.fadeSize.Y.Offset)
    end
    implementation.ApplyWidth(self, self:_railWidth())
    self.elementsLayout.FillDirection = newLayout.pageDirection

    for _, tab in self.tabs do
        if not tab.neglectSelector then
            tab:_rebuildSelector()
            tab.topbarItem.Visible = self.elements.Visible and not self.hidden and not self.minimised
            tab:_applyVisual(if self.selectedTab == tab then "selected" else "unselected")
        end
    end
    -- [[The rows above are brand-new instances, and the rail's width was
    -- [[applied before they existed. Re-applying it now lands the row state on
    -- [[the rows that actually live in the rail — icon-only rows in a
    -- [[collapsed rail (a rail that came back showing tab names), titles held
    -- [[inside a capped one (a rail that came back overflowing).
    self:_applyRailWidth()
    self:_applySettingsLayout(self._settingsMode)
    if self.selectedTab then
        self:_jumpTo(self.selectedTab.tabPage)
        self.selectedTab:_applyVisual("selected")
    end
    self:_fadeSurfaces(true, tweenInfo)
    self:_setTabSectionsShown(true, tweenInfo)
    task.spawn(function()
        if not self.unloaded and self._layoutTransitionId == transitionId then
            self:_revealElements(0.02, 0.5, transitionId)
        end
    end)
    self._layoutTransitioning = false
    return oldKey ~= modeName
end
function Window:_applySettingsLayout(isSettings)
    settingsComponent.applySettingsLayout(self, isSettings)
end

-- [[Rail groups: the rail shows exactly one group at a time - main tabs]]
-- [[or settings tabs. Every rail filter reads these helpers so the two]]
-- [[of them cannot drift apart.]]
function Window:_railGroup(tab)
    if tab._isSettingsTab then
        return "settings"
    end
    return "main"
end

function Window:_activeRail()
    if self._settingsMode then
        return "settings"
    end
    return "main"
end

-- [[The icon label of a topbar action frame, or nil. `frame.ImageLabel` is a]]
-- [[direct child read on Roblox, not a safe one: the action container also]]
-- [[holds chrome with no icon - the search pill and other dividers - and]]
-- [[indexing a missing child raises "ImageLabel is not a valid member of]]
-- [[Frame". Every action-loop read goes through here instead of reading]]
-- [[frame.ImageLabel directly.]]
function Window:_actionIconLabel(actionFrame)
    return actionFrame:FindFirstChild("ImageLabel")
end

-- [[Badge + divider follow their action frame: hidden with them, restored]]
-- [[with them. The badge restores to opaque only while it is meant to show.]]
function Window:_syncActionChrome(actionFrame, shown, tween)
    local badge = actionFrame:FindFirstChild("Badge")
    if badge then
        local badgeTarget = if shown and badge.Visible then 0 else 1
        if tween then
            motion.tween(badge, { BackgroundTransparency = badgeTarget }, tween)
        else
            badge.BackgroundTransparency = badgeTarget
        end
    end
    if actionFrame.Name == "Divider" then
        local dividerTarget = if shown then 0.8 else 1
        if tween then
            motion.tween(actionFrame, { BackgroundTransparency = dividerTarget }, tween)
        else
            actionFrame.BackgroundTransparency = dividerTarget
        end
    end
end

-- [[Shared by the topbar gear action and the profile panel's gear.]]
function Window:_toggleSettingsMode()
    settingsComponent.toggleSettingsMode(self)
end

function Window:_setSettingsMode(active)
    settingsComponent.setSettingsMode(self, active)
    self:_updateWindowTitle()
end

function Window:_updateWindowTitle()
    if not self.title then return end
    local text = self.name
    self.title.Text = locale.resolve(text)
end

function Window:_setContentVisible(visible)
    self.elements.Visible = visible
    if not visible then
        self.tabList.Visible = false
        if self.sidebar then
            self.sidebar.Visible = false
        end
        return
    end

    self:_applySettingsLayout(self._settingsMode)
end

function Window:_fadeSurfaces(shown, tweenInfo)
    local targets = {
        [self.windowShadow] = { Transparency = if shown then 0.6 else 1 },
        [self.windowStroke] = { Transparency = if shown then 0.95 else 1 },
        [self.bottomFade] = { BackgroundTransparency = if shown then 0 else 1 },
    }

    if self.elementsStroke then
        targets[self.elements] = {
            BackgroundTransparency = if shown then self.layout.cardTransparency else 1,
        }
        targets[self.elementsStroke] = { Transparency = if shown then 0 else 1 }
    end

    for instance, props in targets do
        if tweenInfo then
            motion.tween(instance, props, tweenInfo)
        else
            for prop, value in props do
                instance[prop] = value
            end
        end
    end

    profilePanel.setShown(self, shown, tweenInfo)
end

function Window:_fadeSelectedElementsOut()
    if self.selectedTab then
        -- [[These elements are the ones about to be transparent: the tab is
        -- [[marked so that opening it again shows them (the reveal below
        -- [[handles the tab that is on screen).]]
        self:_markTabElementsPending(self.selectedTab)
        for _, element in ipairs(self.selectedTab.elements) do
            element:_setShown(false, true)
        end
    end
end

-- [[Show every element of one tab (instantly) and clear its pending mark.]]
function Window:_showTabElements(tab)
    tab._elementsPending = false
    for _, element in ipairs(tab.elements) do
        element:_setShown(true, false)
    end
end

-- [[Mark a tab whose elements are hidden and must be shown when it is next
-- [[seen. Only two things hide an element: the fade above (the tab that was on
-- [[screen) and being registered while the window is hidden; everything else
-- [[is shown on the spot. A tab that is not the current page of
-- [[`self.elements` cannot be looked at (UIPageLayout keeps the other pages
-- [[out of the render), so the mark is honoured when the player opens the tab
-- [[(`tab:Select`) or when the search pulls its elements onto its own page -
-- [[the two moments they can be seen. This replaces walking every tab's
-- [[elements on every open just to keep the tabs nobody had opened in step.]]
function Window:_markTabElementsPending(tab)
    tab._elementsPending = true
end

function Window:_revealElements(stepDelay, totalDuration, layoutTransitionId)
    local function isStillValid()
        return not self.unloaded
            and not self.hidden
            and not self.minimised
            and (layoutTransitionId == nil or self._layoutTransitionId == layoutTransitionId)
    end

    if not isStillValid() then
        return
    end

    local tab = self.selectedTab
    if not tab then
        return
    end

    -- [[The cascade is the moment this page's elements are being looked at, so]]
    -- [[it owns the pending mark `_showTabElements` would otherwise clear in]]
    -- [[one frame: dropping the mark up front keeps a tab that is opened,]]
    -- [[hidden and opened again from queuing a second reveal behind this one.]]
    tab._elementsPending = false

    local page = tab.tabPage
    local viewTop = page.AbsolutePosition.Y
    local viewBottom = viewTop + page.AbsoluteWindowSize.Y

    -- [[The cascade's pace belongs to the motion service as well: a relaxed
    -- profile spreads the reveal further, a snappy one tightens it, and with
    -- animation off (step 0) every element appears on the same frame instead
    -- of the coroutine yielding a frame per element.]]
    local step = motion.step(stepDelay)
    local maxReveals = if step > 0 then math.floor(totalDuration / step) else math.huge
    local revealed = 0
    for _, element in ipairs(tab.elements) do
        if not isStillValid() then
            return
        end
        local elemTop = element.main.AbsolutePosition.Y
        local isVisible = (elemTop + element.main.AbsoluteSize.Y) > viewTop and elemTop < viewBottom
        if isVisible then
            element:_setShown(true, true)
            revealed += 1
            if step > 0 and revealed <= maxReveals then
                task.wait(step)
                if not isStillValid() then
                    return
                end
            end
        else
            element:_setShown(true, false)         end
    end
end

-- [[The content half of an entrance, and the beat that keeps it out of the
-- [[shell's way. The frame, its surface, the corner and the topbar copy animate
-- [[first; the page's controls come in one cooldown later, as a cascade rather
-- [[than on a single frame, and the overlay queue is opened once that has been
-- [[committed. `entranceId` is the entrance generation: a window that is hidden
-- [[or shown again in the meantime drops the half-finished sequence and lets
-- [[the newer entrance own the page.]]
function Window:_stageContentReveal(beat, stepDelay, totalDuration, entranceId)
    task.spawn(function()
        if beat > 0 then
            task.wait(beat)
        end
        if self.unloaded or self._contentEntranceId ~= entranceId then
            return
        end

        self:_revealElements(stepDelay, totalDuration)

        -- [[Runs even when the cascade above bailed out (a hide, a minimise):
        -- [[overlays belong to the screen layer, not to the page, so nothing
        -- [[here may leave the queue parked on an entrance that never finished.
        -- [[`_firstShow`'s settle opens it as well; this is the later of the
        -- [[two, which is what makes the cards arrive behind the content.]]
        overlayQueue.resume(self)
    end)
end

function Window:Show()
        -- [[An unload retires every instance the window owns, including the ones
        -- [[the entrance tweens next, and a window closed before its deferred
        -- [[startup reveal ran still has that reveal queued (`library_entrypoint`).]]
        if self.animating or self.unloaded or not self.hidden then
        return
    end
    self.animating = true
    self._revealing = true
    -- [[The handle waits while the shell is in the air: it is handed back by
    -- [[whichever settle path this entrance takes (_firstShow or
    -- [[_quickRestore), and only an entrance that actually lands gets one.
    self.drag:disable()

    -- General's heavier content is never built here: settings panels build
    -- lazily on first open through _buildSettingsContent / buildContent.

    self.hidden = false
    self.minimised = false
    self._collapsedShown = false
    chrome.setCollapsedShown(self, false)

        if self._themeRefreshPending then
        self._themeRefreshPending = false
        self:_refreshElementThemes()
    end

    self.collapsedInteract.Visible = false
    if self._freeMouse then
        self._freeMouse()
    end

    if self.hasShownOnce then
        self:_quickRestore()
    else
        self.hasShownOnce = true
        self.main.Visible = true
        if not self.unloaded then
            self:_firstShow()
        end
    end
end

function Window:_quickRestore()
    local restore = self._restorePosition
    if not restore or (restore.X.Scale == 0.5 and restore.Y.Scale == 0.5) then
        -- [[Never dragged — or parked by a recenter that ran while the]]
        -- [[window was hidden: come back to the pair's resting centre for]]
        -- [[the panel state as it is *now* (the player, the viewport or the]]
        -- [[setting may have changed since that position was recorded), so]]
        -- [[the window and its card land centred together.]]
        restore = self:_profileCenterPosition()
    end
    local targetPos = self:_clampedPosition(restore)
    self._restorePosition = targetPos
    -- [[The restore unfolds one surface between two shapes: the shell glides
    -- (symmetric ease) while the corner springs open with a small overshoot,
    -- so the capsule visibly "pops" back into the window.]]
    local restoreTween = "glide"
    local restoreCornerTween = "settle"

    self.main.ClipsDescendants = true
    self.main.BackgroundTransparency = windowSurfaceTransparency
    self.topbar.Visible = true
    self:_setContentVisible(true)
    self:_setTabSectionsVisible(true)
    self:_setTabSectionsShown(true)
    -- [[Same two-beat structure as the first show, on the shorter restore
    -- [[timing: the card comes back first and the page follows it, instead of
    -- [[every control on the page being rewritten on the frame the restore
    -- [[starts on.]]
    self._contentEntranceId = (self._contentEntranceId or 0) + 1
    local entranceId = self._contentEntranceId
    for _, tab in pairs(self.tabs) do
        if not tab.neglectSelector and tab.topbarItem then
            tab.topbarItem.Visible = true
            tab:_applyVisual(if self.selectedTab == tab then "selected" else "unselected")
        end
        if tab == self.selectedTab then
            self:_markTabElementsPending(tab)
        end
    end
    -- [[Restore the settings/normal rail split. The loop above leaves the
    -- [[selected tab marked: the staged reveal at the end of this function
    -- [[walks it one card per beat, the same way opening a tab does.]]
    self:_applySettingsLayout(self._settingsMode)
    self:_fadeSurfaces(true)

    -- [[The card is back on screen with this restore, and the pair — not]]
    -- [[the window alone — has to fit: re-clamp now that pairHalfSize can]]
    -- [[see it (above, the panel was still hidden from the Hide).]]
    targetPos = self:_clampedPosition(targetPos)
    self._restorePosition = targetPos

    if self.topbarIcon then
        self.topbarIcon.ImageTransparency = 0
    end
    if self.title then
        self.title.TextTransparency = 0
    end
    if self.subtitle then
        self.subtitle.TextTransparency = 0.7
    end
    for _, actionFrame in ipairs(self.actionContainer:GetChildren()) do
        local iconLabel = self:_actionIconLabel(actionFrame)
        if iconLabel then
            iconLabel.ImageTransparency = 0.6
        end
        if actionFrame:IsA("Frame") then
            self:_syncActionChrome(actionFrame, true)
        end
    end

    -- [[Same contract as the first show: run the settle path from the tween's
    -- Completed, or immediately when there is no tween (motion switched off),
    -- so `animating` is never left set.]]
    local restored = false
    local function settle()
        if restored or self.hidden or self.unloaded then
            return
        end
        restored = true
        self.main.ClipsDescendants = false
        self._collapsedShown = false
        self:_syncDragBar()
        -- [[The window is on screen and settled, so the handle is reachable
        -- [[again. Note it is *enabled*, not revealed: the pill was parked on
        -- [[the way out, so a restored window offers its handle to the pointer
        -- [[instead of announcing it.
        self.drag:enable()
        self.animating = false
        self._revealing = false
        -- [[The overlay gate opens with the shell, exactly as on the first
        -- [[show, and the staged reveal below re-opens it after the page has
        -- [[committed: whichever lands last is the one that lets a waiting
        -- [[card through, so the queue can never be left parked on a restore.]]
        overlayQueue.resume(self)
    end

    if not motion.tween(self.main, { Size = self.size, Position = targetPos }, restoreTween, settle) then
        settle()
    end
    motion.tween(self.windowCorner, { CornerRadius = self.theme.CornerRoundness }, restoreCornerTween)
    self:_stageContentReveal(motion.step(contentRevealBeat * 0.6), 0.02, 0.4, entranceId)
end
function Window:_firstShow()
    self.drag:disable()
    self.main.Visible = true
    self.main.ClipsDescendants = true
    self.main.BackgroundTransparency = 1

    if self.main.Position.X.Scale == 0.5 and self.main.Position.Y.Scale == 0.5 then
        -- [[Still at the anchored resting spot (the user has not dragged]]
        -- [[it), so centre the window + profile card pair for the panel]]
        -- [[state as it is *now*. Between building the frame and this first]]
        -- [[show the local player can turn up, the viewport can settle or]]
        -- [["Show profile" can arrive from disk — any of which moves the]]
        -- [[pair's centre — and a recenter that ran while the window was]]
        -- [[still hidden only parked the remembered position.]]
        self:_recenterForProfile(false)
    end

    self.main.Size = self.size
    self.windowScale.Scale = 0.94
    -- [[Entrance starts as a capsule (theme pill) then opens to CornerRoundness.]]
    self.windowCorner.CornerRadius = self.theme.PillCornerRadius or UDim.new(1, 0)
    -- [[Which entrance owns the staged content reveal below. A hide, a repeat
    -- [[show or a layout switch between now and then replaces it, and the
    -- [[older sequence stops instead of fighting the newer one for the page.]]
    self._contentEntranceId = (self._contentEntranceId or 0) + 1
    local entranceId = self._contentEntranceId
    self:_setContentVisible(true)
    self:_setTabSectionsVisible(true)
    self:_setTabSectionsShown(true)
    for _, tab in pairs(self.tabs) do
        if not tab.neglectSelector and tab.topbarItem then
            tab.topbarItem.Visible = true
            tab:_applyVisual(if self.selectedTab == tab then "selected" else "unselected")
        end
        if tab == self.selectedTab then
            -- [[The page's controls are deliberately not shown here: writing
            -- [[every card on the frame the entrance opens on is what made the
            -- [[first show look instant and cost a hitch on pages with a lot on
            -- [[them. The mark stays on, and the staged reveal below clears it
            -- [[as it walks the page.]]
            self:_markTabElementsPending(tab)
        end
    end
    -- [[Restore the settings/normal rail split. The loop above leaves the
    -- [[selected tab marked and every other tab either shown or still marked
    -- [[for when it is opened; the staged reveal below walks the page that is
    -- [[actually on screen, one control per beat.]]
    self:_applySettingsLayout(self._settingsMode)
    local tweenInfo = "smooth"
    if self.topbarIcon then
        self.topbarIcon.ImageTransparency = 1
        motion.tween(self.topbarIcon, { ImageTransparency = 0 }, tweenInfo)
    end
    if self.title then
        self.title.TextTransparency = 1
        motion.tween(self.title, { TextTransparency = 0 }, tweenInfo)
    end
    if self.subtitle then
        self.subtitle.TextTransparency = 1
        motion.tween(self.subtitle, { TextTransparency = 0.7 }, tweenInfo)
    end
    for _, actionFrame in ipairs(self.actionContainer:GetChildren()) do
        local iconLabel = self:_actionIconLabel(actionFrame)
        if iconLabel then
            iconLabel.ImageTransparency = 1
            motion.tween(iconLabel, { ImageTransparency = 0.6 }, tweenInfo)
        end
        if actionFrame:IsA("Frame") then
            self:_syncActionChrome(actionFrame, true, tweenInfo)
        end
    end

    self:_fadeSurfaces(true, tweenInfo)

    -- [[The entrance is the library's hero transition. Only UIScale moves:
    -- the main frame stays at its final size, so hundreds of descendants are
    -- not laid out again on every tween step. The Back/Out pop and surface
    -- fade make the load visible without front-loading the remaining content.
    --
    -- The settle path has to run either way: motion.tween returns no tween
    -- when animation is switched off (the "Instant" speed profile), and a
    -- window that never animates must not be left with `animating` set.]]
    local settled = false
    local function settle()
        if settled or self.unloaded then
            return
        end
        settled = true
        self.main.ClipsDescendants = false
        self:_syncDragBar()
        -- [[The shell has landed: the handle becomes reachable (unseen, at the
        -- [[parked look the entrance started from) and the hover reveal takes
        -- [[it from there. This is the write that used to keep the handle off
        -- [[for the whole life of an expanded window.
        self.drag:enable()
        self.animating = false
        self._revealing = false
        -- [[The window is stable now, so park the selected tab in view and
        -- [[let saved values/callbacks begin only after the opening frame.]]
        self:_scrollSelectedTabIntoView(false)
        self:_refreshTabStripChrome()
        -- [[The shell has landed, so the entrance is over as far as the
        -- [[overlay queue is concerned. The staged reveal below opens the same
        -- [[gate again when it has committed the page; whichever of the two
        -- [[happens last is what lets a waiting notification through.]]
        overlayQueue.resume(self)
    end

    motion.tween(self.main, { BackgroundTransparency = windowSurfaceTransparency }, "emphasized")
    if not motion.tween(self.windowScale, { Scale = 1 }, "pop", settle) then
        settle()
    end
    motion.tween(self.windowCorner, { CornerRadius = self.theme.CornerRoundness }, "pop")

    -- [[Second beat of the entrance: the single cascade walks the page's
    -- [[controls in once the frame is close to its resting size, and only
    -- [[then do the queued notifications start arriving one at a
    -- [[time. Everything after `CreateWindow` — a host's
    -- [["loaded N modules" notifications, the optional-icon warning — rides
    -- [[that queue instead of sharing the frame the window itself opens on.]]
    self:_stageContentReveal(motion.step(contentRevealBeat), 0.02, 0.35, entranceId)

    local player = state.localPlayer
end

function Window:GetPath()
    return persistence.getPath(self)
end

function Window:Save(name)
    if name ~= nil and (type(name) ~= "string" or name == "") then
        return false
    end
    return persistence.save(self, name)
end

function Window:Load(name)
    if name ~= nil and (type(name) ~= "string" or name == "") then
        return false
    end
    return persistence.load(self, name)
end

function Window:_applyNamedConfig(name)
    if not self:Load(name) then
        return false
    end
            local _, path = persistence.getPath(self)
    self._loadedConfigPath = path
    self:Save()
    return true
end

function Window:ListConfigs()
    return persistence.list(self)
end

function Window:DeleteConfig(name)
    return persistence.delete(self, name)
end

function Window:Get(flag)
    local control = self.controls[flag]
    return control and control.value
end

function Window:Set(flag, value)
    local control = self.controls[flag]
    if not control then
        return false
    end
    control:Set(value)
    return true
end

function Window:_jumpTo(page)
    if not page then
        return
    end
    if page.Parent == self.elements then
        self.elementsLayout:JumpTo(page)
    end
end

function Window:Navigate(tab)
    if tab == nil then
        return
    end

    local found
    for _, tab in self.tabs do
                if tab == tab or tab.name == tab or tab.tabPage == tab then
            found = tab
            break
        end
    end

    if not found then
        return
    end
    found:Select()
end

function Window:Create(className, props, themeBindings)
    assert(typeof(className) == "string", "Invalid argument #1 (string expected)")
    local instance = Instance.new(className)

            if themeBindings and self.theme then
        for prop, binding in themeBindings do
            instance[prop] = (
                if typeof(binding) == "table" then binding[2](self.theme[binding[1]]) else self.theme[binding]
            )
        end

        self.themeProperties[instance] = themeBindings
    end

    if props then
        for prop, value in props do
            if locale.isToken(value) then
                self:_bindLocale(instance, prop, locale.sourceOf(value))
            else
                image.assign(instance, prop, value)
            end
        end
    end

    table.insert(self.instances, instance)
    return instance
end

function Window:_bindLocale(instance, prop, token)
    instance[prop] = locale.resolve(token)

    local bindings = self.localeProperties[instance]
    if not bindings then
        bindings = {}
        self.localeProperties[instance] = bindings
    end
    bindings[prop] = token
end

function Window:SetLocale(localeId)
    locale.setActive(localeId)
    for instance, bindings in self.localeProperties do
        for prop, token in bindings do
            instance[prop] = locale.resolve(token)
        end
    end
    -- [[Translated row titles may measure differently.]]
    self:_applyContentRailWidth()
end

function Window:SetTranslator(translator)
    locale.translator = translator
end

function Window:RegisterTranslations(translations)
    locale.register(translations)
    self:SetLocale(locale.current)
end

-- [[The window's own background, applied to any frame: the base colour the
-- [[WindowColor gradient is laid over, and the gradient itself (same theme
-- [[token, rotation and offset as the frame the window is built from). The
-- [[profile card beside the window calls this too, so "the card is the window
-- [[in miniature" is enforced by construction instead of by two copies of the
-- [[same numbers that can drift apart. Returns the gradient so callers can
-- [[keep it in step with the window's own (see _syncLiveAnimation).]]
function Window:StyleWindowSurface(frame): UIGradient
    frame.BackgroundColor3 = windowSurfaceColor
    return self:Create("UIGradient", {
        Rotation = windowSurfaceGradientRotation,
        Offset = windowSurfaceGradientOffset,

        Parent = frame,
    }, { Color = { "WindowColor", functions.toColorSequence } })
end

-- [[The window's own opacity once it is on screen. Kept beside the surface
-- [[style so the card can state the same value without guessing.]]
function Window:WindowSurfaceTransparency(): number
    return windowSurfaceTransparency
end

function Window:CreateGlow(parent, color, blurRadius, transparency)
    local props = {
        BlurRadius = UDim.new(0, blurRadius),
        Transparency = transparency,
        ZIndex = -1,

        Parent = parent,
    }

    if typeof(color) == "string" then
        return self:Create("UIShadow", props, {
            Color = {
                color,
                function(color)
                    return if typeof(color) == "ColorSequence" then color.Keypoints[1].Value else color
                end,
            },
        })
    end

    props.Color = color
    return self:Create("UIShadow", props)
end

function Window:_flashResult(element, ok)
    local box = element.box
    if not box then
        return
    end
    local glow = if element._fieldGlow then element:_fieldGlow() else element.glow
    local boxStroke = element.boxStroke
    local flashColor = ok and constants.accent.on or self.theme.ErrorColor
    local flashStroke = ok and constants.accent.onStroke or self.theme.ErrorStrokeColor
    -- [[Hoisted out of the function: these two are fixed, and the flash runs
    -- on every element callback that reports a result.]]
    local flashTween = "fast"
    local restoreTween = "normal"

    motion.tween(box, { BackgroundColor3 = flashColor }, flashTween)
    if boxStroke then
        motion.tween(boxStroke, { Color = flashStroke, Transparency = 0.4 }, flashTween)
    end
    if glow then
        motion.tween(glow, { Color = flashStroke, Transparency = 0.6 }, flashTween)
    end

        element._flashToken = (element._flashToken or 0) + 1
    local token = element._flashToken
    task.delay(0.22, function()
        if element._flashToken ~= token then
            return
        end
        motion.tween(box, { BackgroundColor3 = self.theme.FieldBackground }, restoreTween)
        if boxStroke then
            motion.tween(boxStroke, { Color = self.theme.SurfaceStroke, Transparency = 0.85 }, restoreTween)
        end
        if glow then
            motion.tween(glow, { Color = self.theme.FieldGlow, Transparency = element._glowIdle or 1 }, restoreTween)
        end
    end)
end

function Window:CreateHoverOverlay(parent)
    local overlay = self:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        ZIndex = 1,

        Parent = parent,
    })

    self:Create("UICorner", {
        Parent = overlay,
    }, { CornerRadius = "ElementCornerRadius" })

    return overlay
end

function Window:_elementBackgroundTransparency(element)
    if element and element._collapsibleChild then
        return self.theme.CollapsibleChildElementTransparency or 0.75
    end
    return self.theme.ElementTransparency or 0
end

function Window:_elementStrokeTransparency(element, hover)
    if element and element._collapsibleChild then
        if hover then
            return self.theme.CollapsibleChildElementStrokeHoverTransparency
                or self.theme.CollapsibleChildElementStrokeTransparency
                or 1
        end
        return self.theme.CollapsibleChildElementStrokeTransparency or 1
    end
    return if hover then self.theme.ElementStrokeHoverTransparency else self.theme.ElementStrokeTransparency
end

function Window:_wireElementHover(element, options)
    local theme = self.theme
    -- [[Containers may scope the hover to a sub-frame (the collapsible
    -- header) instead of the whole element, and rest their title on a
    -- different theme token. Keys, not values, so a runtime theme change
    -- still flows through: the closures below read the theme live.]]
    options = if typeof(options) == "table" then options else {}
    local hoverTarget = options.target or element.main
    local titleRestKey = options.titleRestKey or "ContentColor"
    local titleHoverKey = options.titleHoverKey or "ElementTextHoverColor"

    -- [[Hover feedback is the single most-repeated animation in the library
    -- (every element, every pointer move across a grid), so it goes through
    -- the motion service: the spec is shared instead of allocated per event,
    -- and an enter/leave pair on the same element can no longer leave two
    -- tweens interpolating the same stroke — the second cancels the first.]]
    self:ConnectFor(element, hoverTarget.MouseEnter, function()
        if not self:_interactive() then
            return         end
        motion.tween(
            element.stroke,
            { Transparency = self:_elementStrokeTransparency(element, true), Color = theme.ElementStrokeHover },
            "snappy"
        )
        motion.tween(element.title, { TextColor3 = theme[titleHoverKey] }, "snappy")
        if element.hoverOverlay then
            motion.tween(element.hoverOverlay, { BackgroundTransparency = 0.97 }, "snappy")
        end
    end)

    self:ConnectFor(element, hoverTarget.MouseLeave, function()
        motion.tween(
            element.stroke,
            { Transparency = self:_elementStrokeTransparency(element, false), Color = theme.ElementStroke },
            "snappy"
        )
        motion.tween(element.title, { TextColor3 = theme[titleRestKey] }, "snappy")
        if element.hoverOverlay then
            motion.tween(element.hoverOverlay, { BackgroundTransparency = 1 }, "snappy")
        end
    end)
end

function Window:_runGuarded(element, fn, ...)
            if element.locked then
        return
    end

    local args = table.pack(...)
    task.spawn(function()
        local ok, err = pcall(function()
            return fn(table.unpack(args, 1, args.n))
        end)
        if ok or element._errored then
            return
        end
        element._errored = true

        local flashTarget = element.flashTarget or element.main
        local errorTween = "snappy"
        motion.tween(flashTarget, { BackgroundColor3 = self.theme.ErrorColor }, errorTween)
        motion.tween(element.stroke, { Color = self.theme.ErrorStrokeColor }, errorTween)
                if element.title then
            element.title.Text = locale.resolve("Error, log recorded in console.")
        end

        log.warn(
            `Astra encountered an error, with the callback for a {element.__type} component named '{element.name}':`
        )
        log.print(err)

        task.wait(1)

        if element.title then
            element.title.Text = locale.resolve(element.name)
        end
        motion.tween(
            flashTarget,
            { BackgroundColor3 = Color3.fromRGB(255, 255, 255) },
            "normal"
        )
        motion.tween(
            element.stroke,
            { Color = self.theme.ElementStroke },
            "emphasized"
        )
        element._errored = false
    end)
end

function Window:StyleElementBody(frame)
    self:Create("UIGradient", {
        Rotation = 270,

        Parent = frame,
    }, { Color = "ElementGradient" })

    self:Create("UICorner", {
        Parent = frame,
    }, { CornerRadius = "ElementCornerRadius" })

    return self:Create("UIStroke", {
        Transparency = 1,

        Parent = frame,
    }, { Color = "ElementStroke", Transparency = "ElementStrokeTransparency" })
end

function Window:_buildCompactRow(tab, name, zIndex)
    local row = self:Create("Frame", {
        Name = name,
        Size = UDim2.fromOffset(0, compactRowHeight),
        AutomaticSize = Enum.AutomaticSize.X,
        ClipsDescendants = true,         BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,

        BackgroundTransparency = 1,
        Parent = tab.tabPage,
    }, { BackgroundTransparency = "ElementTransparency" })

    local stroke = self:StyleElementBody(row)

        self:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Fill,
        Parent = row,
    })

        self:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalFlex = Enum.UIFlexAlignment.Fill,

        Parent = row,
    })

        local button = self:Create("TextButton", {
        Text = "",
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1,         AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, compactRowHeight),
        BorderSizePixel = 0,
        TextTransparency = 1,
        ZIndex = zIndex or 1,

        Parent = row,
    })

    self:Create("UICorner", {
        Parent = button,
    }, { CornerRadius = "ElementCornerRadius" })

    return row, stroke, button
end

function Window:StyleElementPanel(frame)
    self:Create("UIGradient", {
        Rotation = 270,

        Parent = frame,
    }, { Color = "ElementGradient" })

    self:Create("UICorner", {
        Parent = frame,
    }, { CornerRadius = "ElementCornerRadius" })

    local stroke = self:Create("UIStroke", {
        Color = Color3.fromRGB(255, 255, 255),
        Transparency = 1,
        Parent = frame,
    })

    self:Create("UIGradient", {
        Rotation = 270,

        Parent = stroke,
    }, { Color = "ElementStrokeGradient" })

    return stroke
end

function Window:_reveal(instance, targets, animate, tweenInfo)
    if not instance then
        return
    end
    -- [[Skip work when the target state is already applied: the cascade (and
    -- a window that is shown, hidden and shown again) asks the same elements
    -- to reveal repeatedly.]]
    local needsUpdate = false
    for prop, value in targets do
        if instance[prop] ~= value then
            needsUpdate = true
            break
        end
    end
    if not needsUpdate then
        return
    end

    if animate then
        -- [[The tween goes through the motion service, which shares the spec
        -- and cancels an in-flight tween on the same property — the cascade
        -- asks every element on the page to show on one frame, and a
        -- hidden/reshown window used to stack a second reveal on the first.]]
        motion.tween(instance, targets, tweenInfo or "normal")
    else
        for prop, value in targets do
            instance[prop] = value
        end
    end
end

function Window:_revealCommon(element, animate)
    self:_reveal(element.stroke, { Transparency = self:_elementStrokeTransparency(element, false) }, animate)
    self:_reveal(element.title, { TextTransparency = 0 }, animate)
    self:_reveal(element.main, { BackgroundTransparency = self:_elementBackgroundTransparency(element) }, animate)
    if element.iconLabel then
        self:_reveal(element.iconLabel, { ImageTransparency = 0 }, animate)
    end
    if element.tapIcon then
        self:_reveal(element.tapIcon, { ImageTransparency = 0.5 }, animate)
    end
    if element.descriptionLabel then
        self:_reveal(element.descriptionLabel, { TextTransparency = 0.45 }, animate)
    end
end

function Window:_hideCommon(element, animate)
    self:_reveal(element.stroke, { Transparency = 1 }, animate)
    self:_reveal(element.title, { TextTransparency = 1 }, animate)
    self:_reveal(element.main, { BackgroundTransparency = 1 }, animate)
    if element.iconLabel then
        self:_reveal(element.iconLabel, { ImageTransparency = 1 }, animate)
    end
    if element.tapIcon then
        self:_reveal(element.tapIcon, { ImageTransparency = 1 }, animate)
    end
    if element.descriptionLabel then
        self:_reveal(element.descriptionLabel, { TextTransparency = 1 }, animate)
    end
end

function Window:_resetCapsulePosition()
    if self.unloaded then
        return
    end
    -- Forget the drag override. The next collapse uses the default rectangle
    -- for the current capsule style; the expanded window's position is untouched.
    self._collapsedPosition = nil
    if self._collapsedShown and not self.animating then
        local position = self:_collapsedRect()
        motion.tween(self.main, { Position = position }, "glide")
    end
end

function Window:_collapsedRect()
    local size = if self.showIconOnly then collapsedIconOnlySize else collapsedWideSize
        if self._collapsedPosition then
        return self._collapsedPosition, size
    end
    local position = UDim2.new(
        collapsedPosition.X.Scale,
        collapsedPosition.X.Offset,
        collapsedPosition.Y.Scale,
        collapsedPosition.Y.Offset + size.Y.Offset / 2
    )
    return position, size
end

function Window:_interactive()
    return not self.animating and not self.hidden
end

function Window:_canDragCapsule()
    if self.hidden or self.animating then
        return false
    end
    return not self.minimised or (self.settings and self.settings.dragMinimisedBar == true)
end

function Window:_settled()
    return not self.hidden and not self._revealing
end

function Window:Connect(signal, callback)
    local connection = signal:Connect(callback)
    table.insert(self.connections, connection)
    return connection
end

function Window:ConnectFor(element, signal, callback)
    local connection = self:Connect(signal, callback)
    element.connections = element.connections or {}
    table.insert(element.connections, connection)
    return connection
end

function Window:Disconnect(connection)
    if not connection then
        return
    end
    local index = table.find(self.connections, connection)
    if index then
        table.remove(self.connections, index)
    end
    connection:Disconnect()
end

function Window:DestroySubtree(root)
    if not root then
        return
    end

    local descendants = { [root] = true }
    for _, desc in root:GetDescendants() do
        descendants[desc] = true
    end
    for i = #self.instances, 1, -1 do
        local inst = self.instances[i]
        if descendants[inst] then
            table.remove(self.instances, i)
            self.themeProperties[inst] = nil
            self.localeProperties[inst] = nil
        end
    end

    root:Destroy()
end

function Window:DisconnectMany(element, connections)
    if not connections or #connections == 0 then
        return
    end

    local removed = {}
    for _, conn in connections do
        removed[conn] = true
        conn:Disconnect()
    end

    local function compact(list)
        if not list then
            return
        end
        local write = 0
        for i = 1, #list do
            local conn = list[i]
            if not removed[conn] then
                write += 1
                list[write] = conn
            end
        end
        for i = #list, write + 1, -1 do
            list[i] = nil
        end
    end

    compact(self.connections)
    if element and element ~= self then
        compact(element.connections)
    end
end

function Window:DestroySubtrees(roots)
    if not roots or #roots == 0 then
        return
    end

    local descendants = {}
    for _, root in roots do
        descendants[root] = true
        for _, desc in root:GetDescendants() do
            descendants[desc] = true
        end
    end

    local write = 0
    for i = 1, #self.instances do
        local inst = self.instances[i]
        if descendants[inst] then
            self.themeProperties[inst] = nil
            self.localeProperties[inst] = nil
        else
            write += 1
            self.instances[write] = inst
        end
    end
    for i = #self.instances, write + 1, -1 do
        self.instances[i] = nil
    end

    for _, root in roots do
        root:Destroy()
    end
end

function Window:Unload()
    -- Do not lose the final edit if the window closes inside the save debounce.
    if not self.unloaded and self._saveScheduled and self.configuration.autoSave and not self._loading then
        self._saveScheduled = false
        pcall(self.Save, self)
    end
    self.unloaded = true
    -- [[Stop the entrance queue with the window: an overlay that had not taken
    -- [[its turn yet has no layer to be built into any more, and the pump must
    -- [[not wake up on a window whose instances are about to be destroyed.]]
    overlayQueue.close(self)
            haptic.teardown()
    haptic.releaseContainer(self.screenGui)
    for _, liveTween in self._liveTweens or {} do
        liveTween:Cancel()
    end
    self._liveTweens = nil
    for i = #self.connections, 1, -1 do
        self.connections[i]:Disconnect()
    end
    for i = #self.instances, 1, -1 do
        self.instances[i]:Destroy()
    end

                table.clear(self.connections)
    table.clear(self.instances)
    table.clear(self.themeProperties)
    table.clear(self.localeProperties)
    table.clear(self.controls)
    table.clear(self.tabs)
    -- [[overlayQueue.close already drained the queue into its safe resting
    -- [[shape (empty pending list, closed flag); wiping the table itself used
    -- [[to pull `pending` out from under the pump coroutine on its next resume
    -- [[(a `#nil` error racing an unload that landed mid-gate).]]
    self._overlayQueueRunning = false
end

return Window

end)() end,
    [22] = function()local wax,script,require=ImportGlobals(22)local ImportGlobals return (function(...)
-- [[Shared base card layout and styling for Astra UI elements.]]
-- [[Handles standard background frame, theme strokes, hover overlay,]]
-- [[content container, icon, and title label.]]

local baseCard = {}

local description = require(script.Parent.description)

export type BaseCardOpts = {
    height: number?,
    rightPadding: number?,
    titleText: string?,
}

function baseCard.buildFull(element: any, opts: BaseCardOpts?): any
    local window = element.window
    local tab = element.tab
    local height = (opts and opts.height) or 41
    local rightPadding = (opts and opts.rightPadding) or 40

    element.main = window:Create("Frame", {
        Size = UDim2.new(1, -20, 0, height),
        BorderSizePixel = 0,
        Name = element.name,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1,
        Parent = tab.tabPage,
    }, { BackgroundTransparency = "ElementTransparency" })

    element.stroke = window:StyleElementBody(element.main)
    element.hoverOverlay = window:CreateHoverOverlay(element.main)

    -- [[A description grows the card here, before the title row is built, so
    -- [[the row centres in the base region instead of drifting down into the
    -- [[line (see elements/description.luau).]]
    description.attach(element)
    local centerScale, centerOffset = description.center(element, height)

    element.container = window:Create("Frame", {
        BorderSizePixel = 0,
        Parent = element.main,
        Size = UDim2.new(1, -rightPadding, 0, 16),
        Position = UDim2.new(0, 20, centerScale, centerOffset),
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,
    })

    element.containerLayout = window:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        Parent = element.container,
    })

    if element.icon then
        element.iconLabel = window:Create("ImageLabel", {
            Image = element.icon,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            ImageTransparency = 1,
            Parent = element.container,
        }, { ImageColor3 = "ContentColor" })
    end

    element.title = window:Create("TextLabel", {
        Text = (opts and opts.titleText) or element.name,
        Size = UDim2.new(1, if element.icon then -21 else 0, 0, 16),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,
        TextTransparency = 1,
        Parent = element.container,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    return element.main
end

return baseCard

end)() end,
    [23] = function()local wax,script,require=ImportGlobals(23)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Button"

local constants = script.Parent.Parent.utilities

local Button = require(constants.Parent.core.state)
local tab = require(script.Parent.Parent.functions)
local props = require(constants.moveable)
local name = require(constants.lockable)
local icon = require(constants.locale)
local callback = require(constants.HapticEngine)
local baseCard = require(script.Parent.baseCard)
local description = require(script.Parent.description)
-- [[The click choreography rides the motion service's specs: the tap glyph
-- dips on `fast` and springs back on `settle` (a small overshoot that reads
-- as a physical press), the card nudges in on `snappy` and lands back on
-- `settle`, and the stroke follows hover on `snappy`.]]
local motion = require(constants.motion)
local element = "snappy"
local tapPress = "fast"
local tapRelease = "settle"

-- [[Built-in "tap" glyph for the card's right edge. Uses `mouse-pointer-click`
-- [[(lucide) as the trailing affordance, revealed at 50% transparency.]]
local tapCandidates = { "mouse-pointer-click" }

local function resolveTapIcon(window, override)
    if override ~= nil and override ~= true then
        return window:ResolveIcon(override)
    end
    for _, candidate in ipairs(tapCandidates) do
        local resolved = window:ResolveIcon(candidate)
        if resolved ~= candidate then
            return resolved
        end
    end
    return nil
end

function state.new(tab, frame)
    frame = if typeof(frame) == "table" then frame else {}

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        name = frame.name or frame.Name or "Button",
        icon = tab.window:ResolveIcon(frame.icon or frame.Icon),
        description = frame.description or frame.Description,
        compact = tab.compact or false,
        callback = frame.callback or frame.Callback or function() end,
    }, state)

    -- [[The trailing tap affordance is built-in: present unless the caller
    -- [[opts out with `tapIcon = false`, and any icon request (name, pack
    -- [[name, or asset id) replaces the glyph.]]
    local tapValue = frame.tapIcon
    if tapValue == nil then
        tapValue = frame.TapIcon
    end
    if tapValue ~= false then
        self.tapImage = resolveTapIcon(self.window, tapValue)
    end

    if self.compact then
        self:_buildCompact()
    else
        self:_buildFull()
    end

    return self
end

function state:_runCallback()
    self.window:_runGuarded(self, self.callback)
end

function state:_buildFull()
    baseCard.buildFull(self, {
        height = 43,
        rightPadding = 40,
        titleText = icon.t(self.name),
    })

    -- [[The tap glyph sits in the card's right gutter, centred in the base
    -- [[region exactly like the title row, and is created after `interact`
    -- [[so it paints above the (invisible) click surface.]]

    self.interact = self.window:Create("TextButton", {
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        BorderSizePixel = 0,
        Position = UDim2.fromScale(0.5, 0.5),
        AnchorPoint = Vector2.new(0.5, 0.5),
        TextTransparency = 1,

        Parent = self.main,
    })

    if self.tapImage then
        local centerScale, centerOffset = description.center(self, 43)
        self.tapIcon = self.window:Create("ImageLabel", {
            Image = self.tapImage,
            Size = UDim2.fromOffset(16, 16),
            Position = UDim2.new(1, -15, centerScale, centerOffset),
            AnchorPoint = Vector2.new(1, 0.5),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,

            ImageTransparency = 1,
            Parent = self.main,
        }, { ImageColor3 = "ContentColor" })

        self.tapScale = self.window:Create("UIScale", {
            Scale = 1,

            Parent = self.tapIcon,
        })
    end

    self.window:_wireElementHover(self)

    self.window:ConnectFor(self, self.interact.MouseButton1Click, function()
        callback.click()
        if self.tapScale then
            Button.tweenService:Create(self.tapScale, motion.spec(tapPress), { Scale = 0.78 }):Play()
        end
        Button.tweenService
            :Create(
                self.stroke,
                motion.spec(element),
                { Transparency = 1 }
            )
            :Play()
        Button.tweenService
            :Create(
                self.main,
                motion.spec("snappy"),
                { Size = UDim2.new(1, -26, 0, description.height(self, 43)) }
            )
            :Play()

        self:_runCallback()

        task.wait(0.11)

        if self.window.unloaded then
            return
        end

        if self.tapScale then
            Button.tweenService:Create(self.tapScale, motion.spec(tapRelease), { Scale = 1 }):Play()
        end
        Button.tweenService
            :Create(
                self.main,
                motion.spec(tapRelease),
                { Size = UDim2.new(1, -20, 0, description.height(self, 43)) }
            )
            :Play()
        Button.tweenService
            :Create(
                self.stroke,
                motion.spec(element),
                { Transparency = self.window:_elementStrokeTransparency(self, false) }
            )
            :Play()
    end)
end

function state:_buildCompact()
    local window = self.window

    self.main, self.stroke, self.interact = window:_buildCompactRow(self.tab, self.name)
    self.hoverOverlay = self.interact

    window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, 16),
        PaddingRight = UDim.new(0, 16),

        Parent = self.interact,
    })

    window:Create("UIListLayout", {
        SortOrder = Enum.SortOrder.LayoutOrder,
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        Padding = UDim.new(0, 6),

        Parent = self.interact,
    })

    if self.icon then
        self.iconLabel = window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            LayoutOrder = 0,

            ImageTransparency = 1,
            Parent = self.interact,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = window:Create("TextLabel", {
        Text = icon.t(self.name),
        Size = UDim2.fromOffset(0, 16),
        AutomaticSize = Enum.AutomaticSize.X,
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,

        TextTransparency = 1,
        Parent = self.interact,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Shrink,
        Parent = self.title,
    })

    -- [[Trailing item of the row: the same tap glyph as the full card, at the
    -- [[row's right edge. `interact` owns the row, so the icon is inside the
    -- [[click surface and tapping it is tapping the button.]]
    if self.tapImage then
        self.tapIcon = window:Create("ImageLabel", {
            Image = self.tapImage,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            LayoutOrder = 2,

            ImageTransparency = 1,
            Parent = self.interact,
        }, { ImageColor3 = "ContentColor" })

        self.tapScale = window:Create("UIScale", {
            Scale = 1,

            Parent = self.tapIcon,
        })
    end

    self.window:_wireElementHover(self)

    self.window:ConnectFor(self, self.interact.MouseButton1Click, function()
        callback.click()
        if self.tapScale then
            Button.tweenService:Create(self.tapScale, motion.spec(tapPress), { Scale = 0.78 }):Play()
        end
        Button.tweenService
            :Create(
                self.stroke,
                motion.spec(element),
                { Transparency = 1 }
            )
            :Play()

        self:_runCallback()

        task.wait(0.11)

        if self.window.unloaded then
            return
        end

        if self.tapScale then
            Button.tweenService:Create(self.tapScale, motion.spec(tapRelease), { Scale = 1 }):Play()
        end
        Button.tweenService
            :Create(
                self.stroke,
                motion.spec(element),
                { Transparency = self.window:_elementStrokeTransparency(self, false) }
            )
            :Play()
    end)
end

function state:_setShown(stroke, hoverOverlay)
    if stroke then
        self.window:_revealCommon(self, hoverOverlay)
    else
        self.window:_hideCommon(self, hoverOverlay)
    end
end

function state:_minWidth()
    local w = 32
    if self.icon then
        w += 22
    end
    if self.tapIcon then
        w += 22
    end
    w += tab.textWidth(self.window.theme.Font, 16, icon.resolve(self.name))
    return w
end

props(state)
name(state)

return state

end)() end,
    [24] = function()local wax,script,require=ImportGlobals(24)local ImportGlobals return (function(...)
local Changelog = {}
Changelog.__index = Changelog
Changelog.__type = "Changelog"
local moveable = require(script.Parent.Parent.utilities.moveable)
export type ChangelogChange = {
    symbol: string,
    text: string,
    category: string?,
}
export type ChangelogEntry = {
    version: string,
    date: string,
    game: string?,
    gameId: string?,
    title: string?,
    changes: { ChangelogChange },
}
type FadeItem = {
    instance: Instance,
    property: string,
    shownValue: number,
}
local addedColor = Color3.fromRGB(104, 210, 126)
local removedColor = Color3.fromRGB(232, 100, 100)
local changedColor = Color3.fromRGB(242, 172, 76)
local unknownColor = Color3.fromRGB(170, 170, 170)
local function read(properties, lower, upper)
    local value = properties[lower]
    if value == nil then
        value = properties[upper]
    end
    return value
end
local function normalizeSymbol(value): string
    local symbol = tostring(value or "~")
    local normalized = string.lower(symbol)
    if normalized == "added" or normalized == "add" then
        return "+"
    elseif normalized == "removed" or normalized == "remove" or normalized == "deleted" then
        return "-"
    elseif normalized == "changed" or normalized == "change" or normalized == "updated" then
        return "~"
    elseif symbol == "+" or symbol == "-" or symbol == "~" then
        return symbol
    end
    return "~"
end
local function symbolColor(symbol: string): Color3
    if symbol == "+" then
        return addedColor
    elseif symbol == "-" then
        return removedColor
    elseif symbol == "~" then
        return changedColor
    end
    return unknownColor
end
local function normalizeChange(raw): ChangelogChange?
    if typeof(raw) ~= "table" then
        return nil
    end
    local text = read(raw, "text", "Text")
    if text == nil then
        text = read(raw, "message", "Message")
    end
    if text == nil or tostring(text) == "" then
        return nil
    end
    local symbol = read(raw, "symbol", "Symbol")
    if symbol == nil then
        symbol = read(raw, "type", "Type")
    end
    local category = read(raw, "category", "Category")
    return {
        symbol = normalizeSymbol(symbol),
        text = tostring(text),
        category = if category ~= nil and tostring(category) ~= "" then tostring(category) else nil,
    }
end
local function normalizeEntry(raw): ChangelogEntry
    local properties = if typeof(raw) == "table" then raw else {}
    local rawChanges = read(properties, "changes", "Changes")
    local changes = {}
    if typeof(rawChanges) == "table" then
        for _, rawChange in ipairs(rawChanges) do
            local change = normalizeChange(rawChange)
            if change then
                table.insert(changes, change)
            end
        end
    end
    local version = read(properties, "version", "Version")
    local date = read(properties, "date", "Date")
    if date == nil then
        date = read(properties, "updatedAt", "UpdatedAt")
    end
    local game = read(properties, "game", "Game")
    if game == nil then
        game = read(properties, "gameName", "GameName")
    end
    local gameId = read(properties, "gameId", "GameId")
    if gameId == nil then
        gameId = read(properties, "placeId", "PlaceId")
    end
    local title = read(properties, "title", "Title")
    return {
        version = tostring(version or ""),
        date = tostring(date or ""),
        game = if game ~= nil and tostring(game) ~= "" then tostring(game) else nil,
        gameId = if gameId ~= nil and tostring(gameId) ~= "" then tostring(gameId) else nil,
        title = if title ~= nil and tostring(title) ~= "" then tostring(title) else nil,
        changes = changes,
    }
end
local function formatMetadata(entry: ChangelogEntry): string
    local values = {}
    if entry.version ~= "" then
        table.insert(values, `Version {entry.version}`)
    end
    if entry.game then
        local gameText = entry.game
        if entry.gameId then
            gameText ..= `  •  ID {entry.gameId}`
        end
        table.insert(values, gameText)
    elseif entry.gameId then
        table.insert(values, `Game ID {entry.gameId}`)
    end
    return table.concat(values, "  •  ")
end
function Changelog.new(tab, properties)
    properties = if typeof(properties) == "table" then properties else {}
    local entries = {}
    local rawEntries = read(properties, "entries", "Entries")
    if typeof(rawEntries) == "table" then
        for _, rawEntry in ipairs(rawEntries) do
            table.insert(entries, normalizeEntry(rawEntry))
        end
    end
    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        name = tostring(read(properties, "name", "Name") or "Changelog"),
        emptyText = tostring(read(properties, "emptyText", "EmptyText") or "No changelog entries yet."),
        entries = entries,
        _entryFrames = {},
        _dynamicFrames = {},
        _fadeItems = {},
        _shown = false,
    }, Changelog)
    self.main = self.window:Create("Frame", {
        Name = self.name,
        Size = UDim2.new(1, -20, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BorderSizePixel = 0,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1,
        ClipsDescendants = true,
        Parent = self.tab.tabPage,
    }, { BackgroundTransparency = "ElementTransparency" })
    self.stroke = self.window:StyleElementBody(self.main)
    self.window:Create("UIPadding", {
        PaddingTop = UDim.new(0, 14),
        PaddingBottom = UDim.new(0, 14),
        PaddingLeft = UDim.new(0, 14),
        PaddingRight = UDim.new(0, 14),
        Parent = self.main,
    })
    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 8),
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = self.main,
    })
    self.title = self.window:Create("TextLabel", {
        Name = "Title",
        Size = UDim2.new(1, 0, 0, 24),
        AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        RichText = true,
        TextSize = 17,
        TextWrapped = true,
        TextXAlignment = Enum.TextXAlignment.Left,
        LayoutOrder = 1,
        TextTransparency = 1,
        Parent = self.main,
    }, { TextColor3 = "TitlingColor", FontFace = "TitleFont" })
    self.window:_bindLocale(self.title, "Text", self.name)
    self.history = self.window:Create("Frame", {
        Name = "History",
        Size = UDim2.new(1, 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        LayoutOrder = 3,
        Parent = self.main,
    })
    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 8),
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = self.history,
    })
    table.insert(self._fadeItems, { instance = self.title, property = "TextTransparency", shownValue = 0 })
    self:_render()
    return self
end
function Changelog:_addFade(instance: Instance, property: string, shownValue: number)
    table.insert(self._fadeItems, { instance = instance, property = property, shownValue = shownValue })
end
function Changelog:_makeLabel(parent, name, text, textSize, shownValue, layoutOrder, theme)
    local label = self.window:Create("TextLabel", {
        Name = name,
        Size = UDim2.new(1, 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        RichText = true,
        TextSize = textSize,
        TextWrapped = true,
        TextXAlignment = Enum.TextXAlignment.Left,
        LayoutOrder = layoutOrder,
        TextTransparency = 1,
        Parent = parent,
    }, theme or { TextColor3 = "ContentColor", FontFace = "Font" })
    self.window:_bindLocale(label, "Text", text)
    self:_addFade(label, "TextTransparency", shownValue)
    return label
end
function Changelog:_renderEntry(entry: ChangelogEntry, index: number)
    local card = self.window:Create("Frame", {
        Name = `Entry_{index}`,
        Size = UDim2.new(1, 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ClipsDescendants = true,
        LayoutOrder = index,
        Parent = self.history,
    })
    local stroke = self.window:StyleElementPanel(card)
    self.window:Create("UIPadding", {
        PaddingTop = UDim.new(0, 11),
        PaddingBottom = UDim.new(0, 11),
        PaddingLeft = UDim.new(0, 12),
        PaddingRight = UDim.new(0, 12),
        Parent = card,
    })
    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = card,
    })
    self:_addFade(card, "BackgroundTransparency", self.window:_elementBackgroundTransparency(self))
    self:_addFade(stroke, "Transparency", self.window:_elementStrokeTransparency(self, false))
    local header = self.window:Create("Frame", {
        Name = "Header",
        Size = UDim2.new(1, 0, 0, 22),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        LayoutOrder = 1,
        Parent = card,
    })
    local heading = entry.title or if entry.version ~= "" then entry.version else "Update"
    local title =
        self:_makeLabel(header, "Title", heading, 15, 0, 1, { TextColor3 = "TitlingColor", FontFace = "TitleFont" })
    title.Size = UDim2.new(1, -100, 1, 0)
    title.AutomaticSize = Enum.AutomaticSize.None
    title.TextTruncate = Enum.TextTruncate.AtEnd
    title.TextYAlignment = Enum.TextYAlignment.Center
    local date = self.window:Create("TextLabel", {
        Name = "Date",
        AnchorPoint = Vector2.new(1, 0),
        Position = UDim2.new(1, 0, 0, 0),
        Size = UDim2.fromOffset(96, 22),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Text = entry.date,
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Right,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextTransparency = 1,
        Parent = header,
    }, { TextColor3 = "PlaceholderColor", FontFace = "Font" })
    self:_addFade(date, "TextTransparency", 0.55)
    local metadata = formatMetadata(entry)
    if metadata ~= "" then
        self:_makeLabel(card, "Metadata", metadata, 11, 0.55, 2)
    end
    local changesFrame = self.window:Create("Frame", {
        Name = "Changes",
        Size = UDim2.new(1, 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        LayoutOrder = 4,
        Parent = card,
    })
    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 4),
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = changesFrame,
    })
    local lastCategory = nil
    for changeIndex, change in ipairs(entry.changes) do
        if change.category and change.category ~= lastCategory then
            self:_makeLabel(
                changesFrame,
                `Category_{changeIndex}`,
                string.upper(change.category),
                10,
                0.45,
                changeIndex * 2 - 1,
                { TextColor3 = "PlaceholderColor", FontFace = "Font" }
            )
            lastCategory = change.category
        end
        local row = self.window:Create("Frame", {
            Name = `Change_{changeIndex}`,
            Size = UDim2.new(1, 0, 0, 0),
            AutomaticSize = Enum.AutomaticSize.Y,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            LayoutOrder = changeIndex * 2,
            Parent = changesFrame,
        })
        local symbol = self.window:Create("TextLabel", {
            Name = "Symbol",
            AnchorPoint = Vector2.new(0, 0),
            Position = UDim2.new(0, 0, 0, 0),
            Size = UDim2.fromOffset(18, 20),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Text = change.symbol,
            TextSize = 16,
            TextXAlignment = Enum.TextXAlignment.Center,
            TextYAlignment = Enum.TextYAlignment.Top,
            TextTransparency = 1,
            Parent = row,
        }, { FontFace = "TitleFont" })
        symbol.TextColor3 = symbolColor(change.symbol)
        self:_addFade(symbol, "TextTransparency", 0)
        local body = self:_makeLabel(
            row,
            `ChangeText_{changeIndex}`,
            change.text,
            12,
            0.15,
            2,
            { TextColor3 = "ContentColor", FontFace = "Font" }
        )
        body.Position = UDim2.fromOffset(25, 0)
        body.Size = UDim2.new(1, -25, 0, 0)
        body.AutomaticSize = Enum.AutomaticSize.Y
    end
    table.insert(self._entryFrames, card)
    table.insert(self._dynamicFrames, card)
end
function Changelog:_render()
    for _, frame in ipairs(self._dynamicFrames) do
        self.window:DestroySubtree(frame)
    end
    table.clear(self._entryFrames)
    table.clear(self._dynamicFrames)
    local rootFadeItems = {}
    for _, item in ipairs(self._fadeItems) do
        if item.instance == self.title then
            table.insert(rootFadeItems, item)
        end
    end
    self._fadeItems = rootFadeItems
    if #self.entries == 0 then
        local empty = self:_makeLabel(self.history, "Empty", self.emptyText, 12, 0.7, 1)
        empty.TextXAlignment = Enum.TextXAlignment.Center
        table.insert(self._dynamicFrames, empty)
    else
        for index, entry in ipairs(self.entries) do
            self:_renderEntry(entry, index)
        end
    end
    if self._shown then
        self:_setShown(true, false)
    end
end
function Changelog:_setShown(shown, animate)
    self._shown = shown
    self.window:_revealCommon(self, animate)
    for _, item in ipairs(self._fadeItems) do
        self.window:_reveal(item.instance, { [item.property] = if shown then item.shownValue else 1 }, animate)
    end
end
function Changelog:Set(entries)
    self.entries = {}
    if typeof(entries) == "table" then
        for _, rawEntry in ipairs(entries) do
            table.insert(self.entries, normalizeEntry(rawEntry))
        end
    end
    self:_render()
end
function Changelog:Refresh(entries)
    self:Set(entries)
end
function Changelog:Add(entry, prepend)
    local normalized = normalizeEntry(entry)
    if prepend == false then
        table.insert(self.entries, normalized)
    else
        table.insert(self.entries, 1, normalized)
    end
    self:_render()
end
function Changelog:Clear()
    self.entries = {}
    self:_render()
end
moveable(Changelog)
return Changelog

end)() end,
    [25] = function()local wax,script,require=ImportGlobals(25)local ImportGlobals return (function(...)
local utilities = script.Parent.Parent.utilities
local motion = require(utilities.motion)
local locale = require(utilities.locale)
local haptic = require(utilities.HapticEngine)
local moveable = require(utilities.moveable)
local lockable = require(utilities.lockable)
local ordering = require(utilities.ordering)

local Collapsible = {}
Collapsible.__index = Collapsible
Collapsible.__type = "CollapsibleGroup"

-- This is a declarative adapter over the real controls, not a second set of
-- implementations. Keep the list in sync with Tab's supported element types.
local constructors = {
    Button = function(parent, props) return require(script.Parent.button).new(parent, props) end,
    Toggle = function(parent, props) return require(script.Parent.toggle).new(parent, props) end,
    Switch = function(parent, props) return require(script.Parent.toggle).new(parent, props) end,
    Slider = function(parent, props) return require(script.Parent.slider).new(parent, props) end,
    Dropdown = function(parent, props) return require(script.Parent.dropdown).new(parent, props) end,
    Input = function(parent, props) return require(script.Parent.input).new(parent, props) end,
    Stat = function(parent, props) return require(script.Parent.stat).new(parent, props) end,
    Section = function(parent, props) return require(script.Parent.section).new(parent, props) end,
    Text = function(parent, props) return require(script.Parent.text).new(parent, props) end,
    Changelog = function(parent, props) return require(script.Parent.changelog).new(parent, props) end,
    Divider = function(parent, props) return require(script.Parent.divider).new(parent, props) end,
    Group = function(parent, props) return require(script.Parent.group).new(parent, props) end,
}
local compactTypes = { Button = true, Toggle = true, Switch = true, Slider = true, Stat = true, Group = true }

-- [[Which corners of the header band carry the container's arcs. Open, the band
-- [[is only the top of the card: its bottom edge is the straight divider, so the
-- [[bottom corners must stay square or they would notch into the revealed body.
-- [[Closed, the band *is* the whole card, so its bottom corners have to carry the
-- [[container's bottom arcs exactly like its top ones carry the top.]]
local bandCorners = {
    expanded = { "TopLeftRadius", "TopRightRadius" },
    collapsed = { "TopLeftRadius", "TopRightRadius", "BottomLeftRadius", "BottomRightRadius" },
}

local function validate(definitions, path, ancestors, depth)
    assert(type(definitions) == "table", path .. " must be an array")
    assert(not ancestors[definitions] and depth <= 32, path .. ": cyclic or excessively deep group definitions")
    ancestors[definitions] = true
    local count = 0
    for key in pairs(definitions) do
        assert(type(key) == "number" and key >= 1 and key % 1 == 0, path .. " must be an array")
        count += 1
    end
    for index = 1, count do
        local spec = definitions[index]
        local location = path .. "[" .. index .. "]"
        assert(type(spec) == "table", location .. " must be an element definition")
        local kind = spec.type or spec.Type
        assert(kind ~= "CollapsibleGroup", location .. ": CollapsibleGroups cannot be nested, including inside Groups")
        assert(constructors[kind], location .. ": unsupported element type " .. tostring(kind))
        if kind == "Group" then
            validate(spec.elements or {}, location .. ".elements", ancestors, depth + 1)
        else
            assert(spec.elements == nil, location .. ": only ordinary Groups can contain elements")
        end
    end
    ancestors[definitions] = nil
end

local function groupProps(spec)
    local props = table.clone(spec)
    -- Ordinary row Groups support compact controls only. A declarative Group
    -- containing other control types becomes a column so no element is dropped.
    for _, child in ipairs(spec.elements or {}) do
        if not compactTypes[child.type or child.Type] then
            props.direction = "column"
            break
        end
    end
    return props
end

local function buildChildren(parent, definitions)
    for _, spec in ipairs(definitions) do
        local kind = spec.type or spec.Type
        local child
        if parent.__type == "Group" then
            if kind == "Group" then
                child = parent:CreateGroup(groupProps(spec))
            else
                child = parent:_add(string.lower(kind), spec)
            end
        else
            child = constructors[kind](parent, if kind == "Group" then groupProps(spec) else spec)
            child._collapsibleChild = true
            table.insert(parent.elements, child)
            ordering(child, #parent.elements * 10)
            parent.window:_restoreLate(child)
        end
        if kind == "Group" then
            buildChildren(child, spec.elements or {})
        end
        parent.window:_constructionCheckpoint(false)
    end
end

local function visit(elements, callback)
    for _, child in ipairs(elements) do
        callback(child)
        if child.elements then visit(child.elements, callback) end
    end
end

function Collapsible.new(tab, props)
    assert(tab and tab.__type == "Tab", "CollapsibleGroups must be direct children of a Tab")
    props = if type(props) == "table" then props else {}
    validate(props.elements or {}, "CollapsibleGroup.elements", {}, 0)
    local self = setmetatable({
        tab = tab, window = tab.window, elements = {}, connections = {},
        name = props.name or props.Name or "Collapsible Group",
        icon = tab.window:ResolveIcon(props.icon or props.Icon),
        description = props.description or props.Description,
        deriveFlagFromName = tab.deriveFlagFromName,
        forgetState = props.forgetState or tab.forgetState,
        _expanded = false, _shown = false,
    }, Collapsible)
    local w = self.window
    -- [[One muted 14px line under the title band, the element-description
    -- [[recipe: the header grows by the line's 20px band (41 -> 61) instead of
    -- [[letting the line run into the title row or the first child.]]
    self.headerHeight = if self.description then 61 else 41
    -- [[One connected card, two surfaces: the header band rides the standard
    -- [[element gradient, the revealed body sits on the darker window surface
    -- [[and a 1px stroke-colored divider separates them — the unified
    -- [[container the design reference shows, instead of a header card and a
    -- [[detached panel floating a gap apart.
    self.bodyPad = 10
    self.main = w:Create("Frame", {
        Name = self.name, Size = UDim2.new(1, -20, 0, self.headerHeight),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1, BorderSizePixel = 0,
        ClipsDescendants = true, Parent = tab.tabPage,
    }, { BackgroundTransparency = "ElementTransparency" })
    -- [[The container's own surface is the element surface (the white base
    -- [[under the ElementGradient recipe every element uses), so the card's
    -- [[silhouette — its rounded top corners included — reads as the header
    -- [[band. Roblox rounds a GuiObject's own surface but never clips its
    -- [[descendants to the arcs, so the band below carries the same radius on
    -- [[its top corners and the body clipper paints the darker window surface
    -- [[the revealed body sits on.
    w:Create("UIGradient", { Rotation = 270, Parent = self.main }, { Color = "ElementGradient" })
    w:Create("UICorner", { Parent = self.main }, { CornerRadius = "ElementCornerRadius" })
    -- [[The container masks its own descendants to its bounds; the rounded
    -- [[silhouette comes from the container's corner plus the band's matching
    -- [[top-corner radius.]]
    -- [[UIStroke supports Border/Contextual modes, not Interior; Border keeps
    -- [[the outline on the container without reading an invalid enum member.]]
    self.stroke = w:Create("UIStroke", {
        ApplyStrokeMode = Enum.ApplyStrokeMode.Border, Transparency = 1, Parent = self.main,
    }, { Color = "ElementStroke", Transparency = "ElementStrokeTransparency" })
    self.headerSurface = w:Create("Frame", {
        Name = "HeaderSurface", Size = UDim2.new(1, 0, 0, self.headerHeight),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1, BorderSizePixel = 0, Parent = self.main,
    }, { BackgroundTransparency = "ElementTransparency" })
    -- [[The plain white base under the ElementGradient is the standard element
    -- [[surface recipe; a themed base would multiply the gradient near-black.
    w:Create("UIGradient", { Rotation = 270, Parent = self.headerSurface }, { Color = "ElementGradient" })
    -- [[The band is a child of the container, and Roblox only rounds a
    -- [[GuiObject's own surface — descendants paint over the arcs. Left
    -- [[square, the band filled the card's top-left and top-right corners and
    -- [[squared off the silhouette the stroke draws. Rounding the band's top
    -- [[corners with the container's own radius makes the two arcs one, and
    -- [[_fitHeaderCorners adds the bottom pair while the group is closed.
    self.headerCorner = w:_roundCorners(self.headerSurface, { "TopLeftRadius", "TopRightRadius" }, "ElementCornerRadius")
    self:_fitHeaderCorners()
    self.divider = w:Create("Frame", {
        Name = "HeaderDivider", Position = UDim2.fromOffset(0, self.headerHeight),
        Size = UDim2.new(1, 0, 0, 1), BorderSizePixel = 0,
        BackgroundTransparency = 1, Visible = false, Parent = self.main,
    }, { BackgroundColor3 = "ElementStroke", BackgroundTransparency = "ElementStrokeTransparency" })
    self.header = w:Create("Frame", {
        Name = "Header", Size = UDim2.new(1, 0, 0, self.headerHeight),
        BackgroundTransparency = 1, BorderSizePixel = 0, Parent = self.main,
    })
    self.hoverOverlay = w:CreateHoverOverlay(self.header)
    -- Header metrics mirror the Text element: 20px gutters, a 6px icon gap,
    -- a 16px TitlingColor title and a 14px body line at 0.45 transparency.
    local textX = if self.icon then 42 else 20
    if self.icon then
        self.iconLabel = w:Create("ImageLabel", {
            Image = self.icon, Position = UDim2.fromOffset(20, 12), Size = UDim2.fromOffset(16, 16),
            BackgroundTransparency = 1, ImageTransparency = 1, Parent = self.header,
        }, { ImageColor3 = "ContentColor" })
    end
    self.title = w:Create("TextLabel", {
        Text = locale.t(self.name), Position = UDim2.fromOffset(textX, 0),
        Size = UDim2.new(1, -(textX + 42), 0, 41), BackgroundTransparency = 1,
        TextSize = 16, TextXAlignment = Enum.TextXAlignment.Left, TextTruncate = Enum.TextTruncate.AtEnd,
        TextTransparency = 1, Parent = self.header,
    }, { TextColor3 = "TitlingColor", FontFace = "Font" })
    if self.description then
        self.descriptionLabel = w:Create("TextLabel", {
            Name = "Description",
            Text = locale.t(self.description), Position = UDim2.fromOffset(textX, 37),
            Size = UDim2.new(1, -(textX + 20), 0, 18), BackgroundTransparency = 1,
            TextSize = 14, TextXAlignment = Enum.TextXAlignment.Left, TextTruncate = Enum.TextTruncate.AtEnd,
            TextTransparency = 1, Parent = self.header,
        }, { TextColor3 = "ContentColor", FontFace = "Font" })
    end
    self.chevron = w:Create("ImageLabel", {
        Image = w:ResolveIcon("chevron-right"), Position = UDim2.new(1, -36, 0, 12),
        Size = UDim2.fromOffset(16, 16), BackgroundTransparency = 1,
        ImageTransparency = 1, Rotation = 0, Parent = self.header,
    }, { ImageColor3 = "ContentColor" })
    self.interact = w:Create("TextButton", {
        Text = "", BackgroundTransparency = 1, Size = UDim2.fromScale(1, 1),
        AutoButtonColor = false, ZIndex = 6, Parent = self.header,
    })
    -- [[The reveal mask spans the container edge to edge and rides its bottom
    -- [[edge, so the mask's floor always matches the container's own corner
    -- [[arcs. Children keep their normal width recipes — a card at
    -- [[UDim2.new(1, -20) of this full-width body lands exactly one gutter
    -- [[inside the container on each side, the inset the reference shows,
    -- [[instead of running edge to edge against the container stroke.
    self.bodyClip = w:Create("Frame", {
        Name = "ContentsClip",
        Position = UDim2.fromOffset(0, self.headerHeight + 1),
        Size = UDim2.new(1, 0, 1, -(self.headerHeight + 1)),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1, BorderSizePixel = 0,
        ClipsDescendants = true, Visible = false, Parent = self.main,
    }, { BackgroundTransparency = "ElementTransparency" })
    -- [[The revealed body sits on the darker window surface, and the clipper
    -- [[is flush with the container's bottom edge, so it paints that surface
    -- [[for the whole body region and keeps the container's bottom arcs (its
    -- [[top corners stay square — the divider above is a straight line).
    w:Create("UIGradient", { Rotation = 270, Parent = self.bodyClip }, { Color = "WindowColor" })
    w:_roundCorners(self.bodyClip, { "BottomLeftRadius", "BottomRightRadius" }, "ElementCornerRadius")
    -- [[body lives inside the clipper, which already starts under the divider,
    -- [[so only the top pad offsets it here.
    self.body = w:Create("Frame", {
        Name = "Contents", Position = UDim2.fromOffset(0, self.bodyPad),
        Size = UDim2.new(1, 0, 0, 0), AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundTransparency = 1, BorderSizePixel = 0, Visible = false, Parent = self.bodyClip,
    })
    self.tabPage = self.body
    self.contentLayout = w:Create("UIListLayout", {
        Padding = UDim.new(0, self.bodyPad), FillDirection = Enum.FillDirection.Vertical,
        HorizontalAlignment = Enum.HorizontalAlignment.Center, SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = self.body,
    })
    w:ConnectFor(self, self.contentLayout:GetPropertyChangedSignal("AbsoluteContentSize"), function()
        self:_resize(true)
    end)
    w:ConnectFor(self, self.interact.Activated, function()
        if self.locked or not w:_interactive() then return end
        haptic.click()
        self:_setExpanded(not self._expanded, true)
    end)
    w:_wireElementHover(self, { target = self.header, titleRestKey = "TitlingColor" })
    buildChildren(self, props.elements or {})
    return self
end

-- [[Collapsed, the band is the whole card — there is no body under it — so it
-- [[has to carry the container's bottom arcs too, or its square bottom corners
-- [[paint over the silhouette the stroke draws and the card reads as rounded at
-- [[the top and uneven at the bottom. Expanded, the clipper below takes the
-- [[bottom edge (it is flush with the container's floor and keeps the same
-- [[radius token), so the band squares off against the straight divider. One
-- [[token for both states means the corner never changes radius, only which
-- [[surface owns it, and the clipper keeps the arcs for every frame in between:
-- [[the collapse rounds the band as it settles, and the opening squares it
-- [[before the body is revealed underneath.]]
function Collapsible:_fitHeaderCorners()
    self.window:_setRoundedCorners(
        self.headerCorner,
        if self._expanded then bandCorners.expanded else bandCorners.collapsed,
        "ElementCornerRadius"
    )
end

function Collapsible:_resize(animate)
    -- (The engine always reports an AbsoluteContentSize; the test harness
    -- only stubs it once content is driven, so an unmeasured layout reads 0.)
    local measured = self.contentLayout.AbsoluteContentSize
    local contentHeight = if measured then measured.Y else 0
    local height = self.headerHeight
    if self._expanded then
        -- divider + top pad + content + bottom pad: the container ends one
        -- gutter under the last child card, the same inset its sides keep,
        -- with no dead slack.
        height += 1 + self.bodyPad + contentHeight + self.bodyPad
    end
    local size = UDim2.new(1, -20, 0, height)
    self._resizeRevision = (self._resizeRevision or 0) + 1
    local revision = self._resizeRevision
    local function finish()
        if self.window.unloaded or not self.main.Parent or self._resizeRevision ~= revision then
            return
        end
        if not self._expanded then
            self.body.Visible = false
            self.bodyClip.Visible = false
            self.divider.Visible = false
        end
        -- [[Runs on every settle, in both states: the corner set follows the
        -- [[height the container has actually reached, so a reversal mid-tween
        -- [[(a stale revision returns above) can never leave the band's bottom
        -- [[arcs on a card that is about to show its body, or off a card that
        -- [[has just released it.]]
        self:_fitHeaderCorners()
    end
    if animate and self._shown then
        -- The reveal mask derives its bounds from the container, so a single
        -- tween grows both in lockstep; the mask's edge never leads or trails
        -- the frame. A nil tween means the size is already applied (instant
        -- profile, or already at target), so the settle path runs directly.
        local mainTween = motion.tween(self.main, { Size = size }, "snappy", finish)
        if not mainTween then finish() end
    else
        motion.cancel(self.main)
        self.main.Size = size
        finish()
    end
end

function Collapsible:_setExpanded(expanded, animate)
    if self._expanded == expanded then return end
    self._expanded = expanded
    if expanded then
        -- [[Square the band off against the divider before the body surfaces
        -- [[under it, so the first revealed frame already shows a straight
        -- [[header/body seam instead of two notches at the band's floor.
        self:_fitHeaderCorners()
        self.body.Visible = true
        self.bodyClip.Visible = true
        self.divider.Visible = true
        for _, child in ipairs(self.elements) do child:_setShown(self._shown, false) end
    else
        -- End transient editing, but never reset values or trigger callbacks.
        visit(self.elements, function(child)
            if child.__type == "Dropdown" and child._isOpen then child:_close() end
            if child.recording then child:_stopRecording() end
            if child.__type == "Input" then
                local ok, focused = pcall(function() return child.input:IsFocused() end)
                if ok and focused then
                    -- Cancel the uncommitted edit, so hiding a container cannot
                    -- silently commit input or trigger its value callback.
                    child.input.Text = child.value
                    child.input:ReleaseFocus()
                end
            end
        end)
    end
    local chevronProps = { Rotation = if expanded then 90 else 0 }
    if animate then
        motion.tween(self.chevron, chevronProps, "snappy")
    else
        motion.set(self.chevron, chevronProps)
    end
    self:_resize(animate)
end

function Collapsible:_setShown(shown, animate)
    self._shown = shown
    local w = self.window
    w:_reveal(self.main, { BackgroundTransparency = if shown then w.theme.ElementTransparency or 0 else 1 }, animate)
    w:_reveal(self.headerSurface, { BackgroundTransparency = if shown then w.theme.ElementTransparency or 0 else 1 }, animate)
    w:_reveal(self.bodyClip, { BackgroundTransparency = if shown then w.theme.ElementTransparency or 0 else 1 }, animate)
    w:_reveal(self.stroke, { Transparency = if shown then w.theme.ElementStrokeTransparency else 1 }, animate)
    w:_reveal(self.divider, { BackgroundTransparency = if shown then w.theme.ElementStrokeTransparency else 1 }, animate)
    w:_reveal(self.title, { TextTransparency = if shown then 0 else 1 }, animate)
    w:_reveal(self.chevron, { ImageTransparency = if shown then 0.4 else 1 }, animate)
    if self.iconLabel then w:_reveal(self.iconLabel, { ImageTransparency = if shown then 0 else 1 }, animate) end
    if self.descriptionLabel then
        w:_reveal(self.descriptionLabel, { TextTransparency = if shown then 0.45 else 1 }, animate)
    end
    if self._expanded then
        for _, child in ipairs(self.elements) do child:_setShown(shown, animate) end
    end
    self:_resize(false)
end

function Collapsible:_refreshTheme()
    self:_setShown(self._shown, false)
    for _, child in ipairs(self.elements) do
        if child._refreshTheme then child:_refreshTheme() end
    end
end

function Collapsible:_moveElement(element, index)
    local old = table.find(self.elements, element)
    if not old then return end
    table.remove(self.elements, old)
    table.insert(self.elements, math.clamp(index, 1, #self.elements + 1), element)
    for i, child in ipairs(self.elements) do ordering(child, i * 10) end
end

moveable(Collapsible)
lockable(Collapsible)
return Collapsible

end)() end,
    [26] = function()local wax,script,require=ImportGlobals(26)local ImportGlobals return (function(...)
-- [[The in-card description line. An element created with a `description` prop
-- [[grows its own card by the measured height of this line instead of spawning
-- [[a standalone row beneath the card, so the description can never sit on top
-- [[of the element's own UI and nothing renders below the card:
--
-- [[  * the line is a muted 14px `ContentColor` label, the Collapsible Group
-- [[    header recipe (one line: a 41px card grows to 61px),
-- [[  * the card grows by the measured, wrapped line count, so a host copy
-- [[    that wraps reserves exactly the room it renders in,
-- [[  * controls keep centring in the *base* region the card had before it
-- [[    grew (`center`), and bottom-anchored tracks/labels ride up by the
-- [[    extra (`height`), so nothing drifts down into the line,
-- [[  * the line stays locale-bound (`Window:SetLocale` retypes it and the card
-- [[    re-measures) and doubles as the lock-message surface
-- [[    (`Window:_setElementLocked` swaps its text, restoring the description
-- [[    on unlock) exactly as the old below-card descriptor row did.
--
-- [[Call `attach` straight after the element's main frame is stroked and
-- [[before its centred children are built, so `center` already answers for the
-- [[grown card. Every writer of the card's height goes through `height`.]]
local description = {}

local insetX = 20
local lineSize = 14
local lineHeight = 17
local bottomPad = 3
local topGap = 4
local fallbackWidth = 260
-- [[The engine's wrapped height lands a hair under a whole number of lines
-- [[(17.0 per line at this size), so the count rounds down over the slack and
-- [[up over everything else.]]
local wrapSlack = 0.05

local functions = require(script.Parent.Parent.functions)
local locale = require(script.Parent.Parent.utilities.locale)

-- [[RichText hosts: measure the words, not the markup.]]
local function plain(text: string): string
    return (text:gsub("<[^>]+>", ""))
end

-- [[Fallback wrap count, for hosts whose text metrics cannot answer
-- [[(`functions.textHeight` returns the raw point size there): greedy
-- [[word-by-word, using the shared width metrics.]]
local function greedyLines(text: string, width: number, font: Font): number
    local count = 1
    local current = 0
    for word in text:gmatch("%S+") do
        local wordWidth = functions.textWidth(font, lineSize, word)
        local grown = if current == 0 then wordWidth else current + functions.textWidth(font, lineSize, " " .. word)
        if current > 0 and grown > width then
            count += 1
            current = wordWidth
        else
            current = grown
        end
    end
    return count
end

local function availableWidth(self): number
    local width = self.main.AbsoluteSize.X - insetX * 2
    -- [[Pre-layout guess; the width watcher below re-measures once the card
    -- [[has a real width.]]
    return if width > 0 then width else fallbackWidth
end

local function lineCount(self): number
    local text = plain(tostring(self.descriptionLabel.Text))
    if text == "" then
        return 1
    end
    local width = availableWidth(self)
    local font = self.window.theme.Font
    local measured = functions.textHeight(font, lineSize, text, width)
    -- [[The engine's own wrap when it answers (the popup, notification and
    -- [[toast reserve their room the same way); the hand count covers hosts
    -- [[with no text metrics, where the measurer reports the raw point size.]]
    local counted = if measured > lineSize
        then math.ceil(measured / lineHeight - wrapSlack)
        else greedyLines(text, width, font)
    return math.max(1, counted)
end

-- [[Card heights everywhere go through here: writers state their base and the
-- [[description's measured extra rides along, so hover tweens, layout switches
-- [[and the dropdown's open height all keep the line's room.]]
function description.height(self, base: number): number
    return base + (self._descriptionExtra or 0)
end

-- [[Y placement for the rows and controls that centre in the card: with a
-- [[description they centre in the base region, not the grown card.]]
function description.center(self, base: number): (number, number)
    if self.descriptionLabel then
        return 0, base / 2
    end
    return 0.5, 0
end

-- [[Elements whose card changes shape (the slider's wide/narrow/minimal
-- [[layouts) re-seat the line against the base region they just wrote, so the
-- [[line stays in the band under the controls in every mode.]]
function description.rebase(self, base: number)
    if not self.descriptionLabel then
        return
    end
    self._baseHeight = base
    self.descriptionLabel.Position = UDim2.fromOffset(insetX, base - topGap)
end

local function apply(self, count: number)
    self._descriptionLines = count
    local extra = count * lineHeight + bottomPad
    local delta = extra - (self._descriptionExtra or 0)
    self._descriptionExtra = extra
    self.descriptionLabel.Size = UDim2.new(1, -insetX * 2, 0, count * lineHeight)
    local size = self.main.Size
    self.main.Size = UDim2.new(size.X.Scale, size.X.Offset, size.Y.Scale, size.Y.Offset + delta)
end

-- [[Re-measure after a rewrap: the card's width changed (layout mode, window
-- [[resize, the search page) or the text did (a locale switch, the lock
-- [[message).]]
local function rewrap(self)
    local count = lineCount(self)
    if count ~= self._descriptionLines then
        apply(self, count)
    end
end

function description.attach(self)
    if self.descriptionLabel or not self.description or self.description == "" then
        return
    end

    self._baseHeight = self.main.Size.Y.Offset
    self._descriptionExtra = 0

    self.descriptionLabel = self.window:Create("TextLabel", {
        Name = "Description",
        Text = locale.t(self.description),
        Position = UDim2.fromOffset(insetX, self._baseHeight - topGap),
        Size = UDim2.new(1, -insetX * 2, 0, lineHeight),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ZIndex = 6,
        TextSize = lineSize,
        TextWrapped = true,
        RichText = true,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        TextTransparency = 1,
        Parent = self.main,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    apply(self, lineCount(self))

    self.window:ConnectFor(self, self.main:GetPropertyChangedSignal("AbsoluteSize"), function()
        rewrap(self)
    end)
    self.window:ConnectFor(self, self.descriptionLabel:GetPropertyChangedSignal("Text"), function()
        rewrap(self)
    end)
end

return description

end)() end,
    [27] = function()local wax,script,require=ImportGlobals(27)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Divider"

local constants = require(script.Parent.Parent.utilities.moveable)
local Divider = require(script.Parent.Parent.utilities.locale)

local tab = 1
local props = 12
local text = 10
local line = 14
local element = 12

local frame = 0.88
local a10 = 0.55

function state.new(tab, a11)
    a11 = if typeof(a11) == "table" then a11 else {}

    local a12 = a11.spacing or a11.Spacing
    local a13 = a11.text or a11.Text

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        text = if a13 ~= nil then tostring(a13) else "",
        spacing = if type(a12) == "number" then math.max(a12, 0) else props,
                        line = a11.line ~= false and a11.Line ~= false,
    }, state)

    self.main = self.window:Create("Frame", {
        Size = UDim2.new(1, -40, 0, 0),
        BorderSizePixel = 0,
        Name = "Divider",
        BackgroundTransparency = 1,

        Parent = self.tab.tabPage,
    })

    self.window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        Padding = UDim.new(0, text),
        SortOrder = Enum.SortOrder.LayoutOrder,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,

        Parent = self.main,
    })

    if self.line then
        self.left = self:_buildHalf(1)
    end

    if self.text ~= "" then
        self:_buildLabel()
    end

    self:_applyHeight()

    return self
end

function state:_buildHalf(a14)
    local a15 = self.window:Create("Frame", {
        Size = UDim2.new(0, 0, 0, tab),
        BorderSizePixel = 0,
        LayoutOrder = a14,

                BackgroundTransparency = 1,
        Parent = self.main,
    }, { BackgroundColor3 = "ContentColor" })

    self.window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Fill,

        Parent = a15,
    })

    return a15
end

function state:_buildLabel()
    self.title = self.window:Create("TextLabel", {
        Text = Divider.t(self.text),
        Size = UDim2.fromOffset(0, line),
        AutomaticSize = Enum.AutomaticSize.X,
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = element,
        TextXAlignment = Enum.TextXAlignment.Center,
        LayoutOrder = 2,

                TextTransparency = 1,
        Parent = self.main,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    if self.line then
        self.right = self:_buildHalf(3)
    end
end

function state:_applyHeight()
    local a16 = if self.text ~= "" then line elseif self.line then tab else 0
    self.main.Size = UDim2.new(1, -40, 0, self.spacing * 2 + a16)
end

function state:Set(a17)
    self.text = if a17 ~= nil then tostring(a17) else ""

    if self.text ~= "" and not self.title then
        self:_buildLabel()

                        if not self.window.hidden then
            self:_setShown(true, true)
        end
    end

        if self.title then
        self.window:_bindLocale(self.title, "Text", self.text)

                                        local a18 = self.text ~= ""
        self.title.Visible = a18
        if self.right then
            self.right.Visible = a18
        end
    end

    self:_applyHeight()
end

function state:_setShown(a19, a20)
    local w = self.window
    local a21 = if a19 then frame else 1

    w:_reveal(self.left, { BackgroundTransparency = a21 }, a20)
    w:_reveal(self.right, { BackgroundTransparency = a21 }, a20)
    w:_reveal(self.title, { TextTransparency = if a19 then a10 else 1 }, a20)
end

constants(state)

return state

end)() end,
    [28] = function()local wax,script,require=ImportGlobals(28)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Dropdown"

local constants = script.Parent.Parent.utilities

local Dropdown = require(constants.Parent.core.state)
local motion = require(constants.motion)
local tab = require(script.Parent.Parent.functions)
local props = require(script.Parent.Parent.images.image)
local name = require(constants.constants)
local flag = require(constants.locale)
local options = require(constants.HapticEngine)
local value = require(constants.windowSizing)
local multiSelect = require(constants.lockable)
local description = require(script.Parent.description)

local placeholder = name.icons.chevron
local callback = name.icons.check
local element = name.icons.dot
local frame = name.icons.search

local button = UDim.new(0, 12)
local list = UDim.new(0, 7)

local isOpen = 30
local selected = 38

local a19 = 38
local a20 = 5
local a21 = 2
local a22 = 41
local a23 = 6
local a24 = 7
local a25 = 6
local a26 = a24 + a25

local a27 = 4

-- [[The bulk-action row is one line of controls, taller than the bare text
-- [[buttons it replaced so the checkbox it now carries sits in the same 16px
-- [[slot an option row gives its glyph. The drawn outline is the 12px square
-- [[inside that slot - the exact width of the row glyphs - so the two states
-- [[of the box are never a size apart.]]
local a28 = 32
local actionBox = 12
local actionBoxRadius = UDim.new(0, 3)
local actionStrokeIdle = 0.45
local actionStrokeHover = 0.2
local actionStrokeChecked = 1
local actionTextIdle = 0.45
local actionTextHover = 0.15

-- [[The dropdown's motion vocabulary, resolved through the shared service:
-- rows snap, panels fade on the normal reveal curve, the list opens on the
-- smooth surface curve and the chevron spins.]]
local a29, a30, a31 = "fast", "snappy", "normal"
local a32, a33, a35 = "smooth", "spin", "normal"

local a34 = 0.4

local function a36(a37)
    local a38 = {}
    local a39 = {}
    for _, v in a37 do
        if typeof(v) == "string" and not a38[v] then
            a38[v] = true
            table.insert(a39, v)
        end
    end
    return a39
end

local function a40(a41, a42)
    if a41 == nil then
        return {}
    end
    if typeof(a41) == "string" then
        return { a41 }
    end
    if typeof(a41) == "table" then
        local a43 = a36(a41)
        if not a42 and #a43 > 1 then
            return { a43[1] }
        end
        return a43
    end
    return {}
end

local function a44(a45, a46)
    local a47 = {}
    for _, v in a45 do
        if table.find(a46, v) then
            table.insert(a47, v)
        end
    end
    return a47
end

local function a48(a, b)
    if #a ~= #b then
        return false
    end
    for _, v in a do
        if not table.find(b, v) then
            return false
        end
    end
    return true
end

function state.new(tab, a49)
    a49 = if typeof(a49) == "table" then a49 else {}

    local a50 = a49.options or a49.Options or {}
    local a51 = a49.multiSelect or a49.MultiSelect or a49.MultipleOptions or false

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        name = a49.name or a49.Name or "Dropdown",
        icon = tab.window:ResolveIcon(a49.icon or a49.Icon),
        description = a49.description or a49.Description,
        forgetState = a49.forgetState or a49.ForgetState or tab.forgetState,
        flag = a49.flag
            or a49.Flag
            or (
                not (a49.forgetState or a49.ForgetState or tab.forgetState)
                    and tab.deriveFlagFromName(a49.name or a49.Name or "Dropdown")
                or nil
            ),

        callback = a49.callback or a49.Callback or function() end,

        options = a36(a50),
        multiSelect = a51,
        placeholderText = flag.resolve(a49.placeholder or a49.Placeholder or "None"),
        value = a40(
            a49.value or a49.Value or a49.currentOption or a49.CurrentOption,
            a51
        ),

        _isOpen = false,
        _searchOpen = false,
        _optionFrames = {},
        _optionsBuilt = false,
    }, state)

            self._desiredValue = self.value
    self.value = a44(self.value, self.options)

        self.window:_registerControl(self)

                    self.main = self.window:Create("Frame", {
        Size = UDim2.new(1, -20, 0, 41),
        BorderSizePixel = 0,
        Name = self.name,
        BackgroundTransparency = 1,

        Parent = self.tab.tabPage,
    })

    -- [[The card grows for a description before anything is built inside it:
    -- [[the visible top card, the title row and the value column all seat
    -- [[themselves against the base region of the taller card, and the option
    -- [[panel keeps its 41px card plus the gap above it.]]
    description.attach(self)

        self.top = self.window:Create("Frame", {
        Size = UDim2.new(1, 0, 0, description.height(self, 41)),
        Position = UDim2.fromScale(0, 0),
        BorderSizePixel = 0,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1,
        ZIndex = 1,

        Parent = self.main,
    }, { BackgroundTransparency = "ElementTransparency" })

            self.stroke = self.window:StyleElementBody(self.top)
    self.hoverOverlay = self.window:CreateHoverOverlay(self.top)
    self.flashTarget = self.top
    self.container = self.window:Create("Frame", {
        BorderSizePixel = 0,

        Parent = self.top,
        Size = UDim2.new(1, -195, 0, 16),
        Position = UDim2.new(0, 20, description.center(self, 41)),
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,
        ZIndex = 5,
    })

    self.containerLayout = self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,

        Parent = self.container,
    })

    if self.icon then
        self.iconLabel = self.window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,

                        ImageTransparency = 1,
            ZIndex = 5,
            Parent = self.container,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = self.window:Create("TextLabel", {
        Text = flag.t(self.name),

        Size = UDim2.new(1, if self.icon then -21 else 0, 0, 16),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,

        TextTransparency = 1,
        ZIndex = 5,
        Parent = self.container,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    self.selectedLabel = self.window:Create("TextLabel", {
        AnchorPoint = Vector2.new(1, 0.5),
        Position = UDim2.new(1, -41, description.center(self, 41)),
        Size = UDim2.fromOffset(130, 15),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Right,
        TextTruncate = Enum.TextTruncate.AtEnd,

        TextTransparency = 1,
        ZIndex = 5,
        Parent = self.top,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    self.chevron = self.window:Create("ImageLabel", {
        AnchorPoint = Vector2.new(1, 0.5),
        Position = UDim2.new(1, -18, description.center(self, 41)),
        Size = UDim2.fromOffset(16, 16),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        Image = "rbxassetid://" .. tostring(placeholder),
        Rotation = 180,

                ImageTransparency = 1,
        ZIndex = 5,
        Parent = self.top,
    }, { ImageColor3 = "ContentColor" })

        self.interact = self.window:Create("TextButton", {
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 0, description.height(self, 41)),
        Position = UDim2.fromScale(0, 0),
        BorderSizePixel = 0,
        Text = "",
        TextTransparency = 1,
        ZIndex = 10,
        AutoButtonColor = false,

        Parent = self.main,
    })

                    self.panel = self.window:Create("Frame", {
        AnchorPoint = Vector2.new(1, 1),
        Position = UDim2.new(1, 0, 1, 0),
        Size = UDim2.new(1, 0, 1, -(a22 + a23 + (self._descriptionExtra or 0))),
        BorderSizePixel = 0,
        ClipsDescendants = true,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        ZIndex = 1,

        BackgroundTransparency = 1,
        Parent = self.main,
    })

    self.panelStroke = self.window:StyleElementPanel(self.panel)

                self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        FillDirection = Enum.FillDirection.Vertical,
        VerticalAlignment = Enum.VerticalAlignment.Top,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.panel,
    })

    self.window:Create("UIPadding", {
        PaddingTop = UDim.new(0, a24),
        PaddingBottom = UDim.new(0, a25),

        Parent = self.panel,
    })

    -- [[The search bar is built with the rows, on the first open: until then
    -- [[it is behind a closed panel and cannot be reached.]]
    self:_buildActions()

    self.list = self.window:Create("ScrollingFrame", {
        Active = true,
        Size = UDim2.new(1, 0, 0, 0),         BorderSizePixel = 0,
        BackgroundTransparency = 1,
        ClipsDescendants = true,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2.new(),
        ScrollBarImageColor3 = Color3.fromRGB(240, 240, 240),
        ScrollBarThickness = 3,
        ScrollBarImageTransparency = 1,         ScrollingDirection = Enum.ScrollingDirection.Y,
        LayoutOrder = 3,
        ZIndex = 1,

        Parent = self.panel,
    })

    self.window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Fill,

        Parent = self.list,
    })

    self.window:ConnectFor(self, self.list:GetPropertyChangedSignal("CanvasPosition"), function()
        self:_syncScrollHint()
    end)
    self.window:ConnectFor(self, self.list:GetPropertyChangedSignal("AbsoluteCanvasSize"), function()
        self:_syncScrollHint()
    end)

                self.window:ConnectFor(self, self.list:GetPropertyChangedSignal("AbsoluteWindowSize"), function()
        self:_syncScrollHint()
    end)

    self.listLayout = self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        SortOrder = Enum.SortOrder.LayoutOrder,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,

        Parent = self.list,
    })

            self.window:Create("UIPadding", {
        PaddingTop = UDim.new(0, a21),
        PaddingBottom = UDim.new(0, a21),

        Parent = self.list,
    })

            self.emptyLabel = self.window:Create("TextLabel", {
        Name = "Empty",
        Size = UDim2.new(1, -12, 0, a19),
        BackgroundTransparency = 1,
        Text = flag.t("No matches"),
        TextSize = 14,
        TextTransparency = 0.55,
        Visible = false,
        LayoutOrder = 1,

        Parent = self.list,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    local function a52(a53)
        return table.find(self.value, a53) ~= nil
    end

    local function a54(a55, a56)
        local a57 = a52(a55.name)
        local a58 = if self._isOpen then (a57 and 0.9 or 0.95) else 1
        local a59 = if self._isOpen then (a57 and 0 or 0.3) else 1
                local a60 = if self._isOpen then (a57 and 0 or 0.7) else 1
        local a61 = if self._isOpen then (a57 and 0.85 or 0.93) else 1

        props.assign(a55.checkIcon, "Image", if a57 then callback else element)

        if a56 then
            motion.tween(a55.frame, { BackgroundTransparency = a58 }, "snappy")
            motion.tween(a55.title, { TextTransparency = a59 }, "snappy")
            motion.tween(a55.checkIcon, { ImageTransparency = a60 }, "snappy")
            motion.tween(a55.stroke, { Transparency = a61 }, "snappy")
        else
            a55.frame.BackgroundTransparency = a58
            a55.title.TextTransparency = a59
            a55.checkIcon.ImageTransparency = a60
            a55.stroke.Transparency = a61
        end
    end

    local function a63()
        if self.multiSelect then
            local n = #self.value
            if n == 0 then
                self.selectedLabel.Text = self.placeholderText
            elseif n == 1 then
                self.selectedLabel.Text = self.value[1]
            else
                self.selectedLabel.Text = flag.resolve("Various")
            end
        else
            self.selectedLabel.Text = self.value[1] or self.placeholderText
        end
    end

    self._renderOptionState = a54
    self._updateSelectedLabel = a63

    local function a64(a65)
        local a66 = self.window:Create("Frame", {
            Size = UDim2.new(1, -12, 0, a19),
            BorderSizePixel = 0,
                                    LayoutOrder = #self._optionFrames + 1,

            BackgroundTransparency = 1,

            Parent = self.list,
        }, { BackgroundColor3 = "DropdownHighlight" })

                local a67 = self.window:Create("UICorner", {
            CornerRadius = list,
            Parent = a66,
        })

                local a68 = self.window:Create("UIStroke", {
            Color = Color3.fromRGB(255, 255, 255),
            Transparency = 1,             Parent = a66,
        })

        local a69 = self.window:Create("TextButton", {
            BackgroundTransparency = 1,
            Size = UDim2.fromScale(1, 1),
            Text = "",
            TextTransparency = 1,
            ZIndex = 50,
            Parent = a66,
        })

        local a70 = self.window:Create("Frame", {
            AnchorPoint = Vector2.new(0, 0.5),
            Position = UDim2.new(0, 14, 0.5, 0),
            Size = UDim2.fromOffset(170, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            ZIndex = 5,
            Parent = a66,
        })

        self.window:Create("UIListLayout", {
            Padding = UDim.new(0, 5),
            FillDirection = Enum.FillDirection.Horizontal,
            VerticalAlignment = Enum.VerticalAlignment.Center,
            SortOrder = Enum.SortOrder.LayoutOrder,
            Parent = a70,
        })

        local a71 = self.window:Create("ImageLabel", {
            Image = "rbxassetid://" .. tostring(callback),
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            ImageTransparency = 1,
            ZIndex = 5,
            Parent = a70,
        }, { ImageColor3 = "ContentColor" })

        local a72 = self.window:Create("TextLabel", {
            Text = a65,
            Size = UDim2.fromOffset(170, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            TextSize = 16,
            TextXAlignment = Enum.TextXAlignment.Left,
            TextWrapped = true,
            LayoutOrder = 1,
            TextTransparency = 1,
            ZIndex = 5,
            Parent = a70,
        }, { TextColor3 = "ContentColor", FontFace = "Font" })

        local a73 = {
            name = a65,
            frame = a66,
            interact = a69,
            title = a72,
            checkIcon = a71,
            container = a70,
            stroke = a68,
            corner = a67,
                        connections = {},
        }

        table.insert(
            a73.connections,
            self.window:ConnectFor(self, a66.MouseEnter, function()
                if not self._isOpen or not self.window:_interactive() then
                    return
                end
                if a52(a73.name) then
                    return
                end
                motion.tween(a66, { BackgroundTransparency = 0.9 }, a29)
                motion.tween(a72, { TextTransparency = 0.15 }, a29)
            end)
        )

        table.insert(
            a73.connections,
            self.window:ConnectFor(self, a66.MouseLeave, function()
                if not self._isOpen then
                    return
                end
                if a52(a73.name) then
                    return
                end
                motion.tween(a66, { BackgroundTransparency = 0.95 }, a29)
                motion.tween(a72, { TextTransparency = 0.3 }, a29)
            end)
        )

        table.insert(
            a73.connections,
            self.window:ConnectFor(self, a69.MouseButton1Click, function()
                                if not self._isOpen then
                    return
                end
                options.click()
                local a74 = a52(a73.name)

                if not self.multiSelect then
                    if a74 then
                        self:_close()
                        return
                    end
                    table.clear(self.value)
                    table.insert(self.value, a73.name)
                else
                    if a74 then
                        local a75 = table.find(self.value, a73.name)
                        if a75 then
                            table.remove(self.value, a75)
                        end
                    else
                        table.insert(self.value, a73.name)
                    end
                end

                                                self._desiredValue = table.clone(self.value)

                for _, d in self._optionFrames do
                    a54(d, true)
                end

                a63()
                self:_syncActions()

                self.window:_runGuarded(self, self.callback, self:_callbackValue())
                self.window:_persist(self)

                if not self.multiSelect then
                    task.wait(0.1)
                    if self.window.unloaded or not self.main.Parent then
                        return
                    end
                    self:_close()
                end
            end)
        )

        return a73
    end

    self._buildOption = a64

    -- [[Rows are built on the first open, not here: a closed dropdown shows
    -- [[only its header, so its options cost nothing until the user looks at
    -- [[them. self.options stays the source of truth, so anything that changes
    -- [[the list while closed is simply reflected the next time it opens.]]

    a63()
    self:_updateCorners()

        self.window:ConnectFor(self, self.interact.MouseButton1Click, function()
        options.click()
        if self._isOpen then
            self:_close()
        else
            self:_open()
        end
    end)

            self.window:ConnectFor(self, self.main.MouseEnter, function()
        if self._isOpen or not self.window:_interactive() then
            return
        end
        motion.tween(self.title, { TextColor3 = self.window.theme.ElementTextHoverColor }, a30)
        motion.tween(self.hoverOverlay, { BackgroundTransparency = 0.97 }, a30)
        motion.tween(
            self.stroke,
            {
                Transparency = self.window:_elementStrokeTransparency(self, true),
                Color = self.window.theme.ElementStrokeHover,
            },
            a30
        )
    end)

    self.window:ConnectFor(self, self.main.MouseLeave, function()
        motion.tween(self.title, { TextColor3 = self.window.theme.ContentColor }, a30)
        motion.tween(self.hoverOverlay, { BackgroundTransparency = 1 }, a30)
        motion.tween(
            self.stroke,
            {
                Transparency = self.window:_elementStrokeTransparency(self, false),
                Color = self.window.theme.ElementStroke,
            },
            a30
        )
    end)

    return self
end

function state:_callbackValue()
    if self.multiSelect then
        return table.clone(self.value)
    end
    return self.value[1]
end

function state:_buildSearch()
    self._searchOpen = false

    self.searchbar = self.window:Create("Frame", {
        Name = "Search",
        Size = UDim2.new(1, -12, 0, isOpen),
        BorderSizePixel = 0,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1,         LayoutOrder = 1,
        ClipsDescendants = false,
        ZIndex = 1,

        Parent = self.panel,
    })

    self.window:Create("UICorner", {
        CornerRadius = UDim.new(0, 12),
        Parent = self.searchbar,
    })

    self.searchStroke = self.window:Create("UIStroke", {
        Color = Color3.fromRGB(255, 255, 255),
        Transparency = 1,         Parent = self.searchbar,
    })

    self.searchShadow = self.window:CreateGlow(self.searchbar, Color3.fromRGB(255, 255, 255), 20, 1)
            self.searchToggle = self.window:Create("TextButton", {
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        Text = "",
        TextTransparency = 1,
        ZIndex = 51,
        Parent = self.searchbar,
    })

    self.searchInput = self.window:Create("TextBox", {
        Text = "",
        PlaceholderText = flag.t("Search..."),
        Size = UDim2.new(1, -58, 0, 16),
        Position = UDim2.new(0, 44, 0.5, 0),
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        ClearTextOnFocus = false,
        TextEditable = false,         Interactable = false,         ZIndex = 52,

        TextTransparency = 1,
        Parent = self.searchbar,
    }, { TextColor3 = "ContentColor", FontFace = "Font", PlaceholderColor3 = "PlaceholderColor" })

    self.searchIcon = self.window:Create("ImageButton", {
        Image = "rbxassetid://" .. tostring(frame),
        Size = UDim2.fromOffset(20, 20),
        Position = UDim2.new(0, 24, 0.5, 0),         AnchorPoint = Vector2.new(0.5, 0.5),
        BackgroundTransparency = 1,
        -- [[A GuiObject border draws the square outline of the frame's
        -- bounds (it never follows a UICorner), so every surface in the
        -- window opts out of it.]]
        BorderSizePixel = 0,
        ScaleType = Enum.ScaleType.Fit,
        AutoButtonColor = false,
        ZIndex = 53,

        ImageTransparency = 1,
        Parent = self.searchbar,
    }, { ImageColor3 = "ContentColor" })

    self.window:ConnectFor(self, self.searchToggle.MouseButton1Click, function()
        if not self._searchOpen then
            self:_expandSearch()
        end
    end)
    self.window:ConnectFor(self, self.searchIcon.MouseButton1Click, function()
        if self._searchOpen then
            self:_collapseSearch()
        else
            self:_expandSearch()
        end
    end)
    self.window:ConnectFor(self, self.searchInput:GetPropertyChangedSignal("Text"), function()
        self:_applyFilter(self.searchInput.Text)
    end)
    self.window:ConnectFor(self, self.searchInput.FocusLost, function()
        if self.searchInput.Text == "" then
            self:_collapseSearch()
        end
    end)
end

function state:_expandSearch()
    if self._searchOpen then
        return
    end
    self._searchOpen = true
    self.searchInput.TextEditable = true
    self.searchInput.Interactable = true

    motion.tween(
        self.searchbar,
        { Size = UDim2.new(1, -12, 0, selected), BackgroundTransparency = 0.92 },
        a35
    )
    motion.tween(self.searchStroke, { Transparency = 0.86 }, a35)
    motion.tween(self.searchShadow, { Transparency = 0.92 }, a35)
    motion.tween(self.searchInput, { TextTransparency = 0.3 }, a35)

    self:_resizeToOptions()     self.searchInput:CaptureFocus()
end

function state:_collapseSearch()
    if not self._searchOpen then
        return
    end
    self._searchOpen = false
    self.searchInput.TextEditable = false
    self.searchInput.Interactable = false
    self.searchInput:ReleaseFocus()
    self.searchInput.Text = ""
    motion.tween(
        self.searchbar,
        { Size = UDim2.new(1, -12, 0, isOpen), BackgroundTransparency = 1 },
        a35
    )
    motion.tween(self.searchStroke, { Transparency = 1 }, a35)
    motion.tween(self.searchShadow, { Transparency = 1 }, a35)
    motion.tween(self.searchInput, { TextTransparency = 1 }, a35)

    self:_resizeToOptions()
end

function state:_applyFilter(a78)
    a78 = string.lower(a78 or "")
    local a79 = 0
    for _, a80 in self._optionFrames do
        local a81 = a78 == "" or string.find(string.lower(a80.name), a78, 1, true) ~= nil
        a80.frame.Visible = a81
        if a81 then
            a79 += 1
        end
    end

            self.emptyLabel.Visible = a79 == 0 and a78 ~= ""
    self:_updateCorners()
    self:_syncActions()
    self:_resizeToOptions()
    self:_syncScrollHint()
end

function state:_syncScrollHint()
        local a82, a83, at =
        self.list.AbsoluteCanvasSize, self.list.AbsoluteWindowSize, self.list.CanvasPosition
    if not a82 or not a83 or not at then
        return
    end

    local a84 = self._isOpen and a83.Y > 0 and a82.Y - (at.Y + a83.Y) > 1

    motion.tween(self.list, { ScrollBarImageTransparency = if a84 then a34 else 1 }, a29)
end

function state:_visibleOptions(): { string }
    local a85 = {}
    for _, a86 in self._optionFrames do
        if a86.frame.Visible then
            table.insert(a85, a86.name)
        end
    end
    return a85
end

-- [[The Select all / Clear row. It is the one two-ended line in the panel: the
-- [[checkbox and its label sit at the left edge, Clear and its bin at the
-- [[right. Both controls act on the options the list is showing, so a search
-- [[that narrows the list narrows what they touch - and the checkbox answers
-- [[for exactly that set (on when every visible option is selected, off when
-- [[any of them is not, and off when the filter matches nothing).]]
function state:_buildActions()
    if not self.multiSelect then
        return
    end

    self.actions = self.window:Create("Frame", {
        Name = "Actions",
        Size = UDim2.new(1, -12, 0, a28),
        BackgroundTransparency = 1,
        LayoutOrder = 2,

        Parent = self.panel,
    })

    -- [[Select all: the glyph slot a row uses, then the label. Clicking it
    -- [[toggles the visible options - everything visible already selected
    -- [[clears them, anything else selects what is missing.]]
    self.selectAll = self.window:Create("TextButton", {
        Name = "SelectAll",
        AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, a28),
        Position = UDim2.new(0, 8, 0.5, 0),
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,
        Text = "",
        TextTransparency = 1,
        AutoButtonColor = false,

        Parent = self.actions,
    })

    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 8),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.selectAll,
    })

    local selectAllSlot = self.window:Create("Frame", {
        Size = UDim2.fromOffset(16, 16),
        BackgroundTransparency = 1,
        LayoutOrder = 1,

        Parent = self.selectAll,
    })

    -- [[Unchecked is the drawn outline and nothing else; checked hides it and
    -- [[shows the same pack glyph a selected row shows.]]
    self.selectAllBox = self.window:Create("Frame", {
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.fromScale(0.5, 0.5),
        Size = UDim2.fromOffset(actionBox, actionBox),
        BackgroundTransparency = 1,

        Parent = selectAllSlot,
    })

    self.window:Create("UICorner", {
        CornerRadius = actionBoxRadius,

        Parent = self.selectAllBox,
    })

    self.selectAllStroke = self.window:Create("UIStroke", {
        Transparency = actionStrokeIdle,
        Thickness = 1.5,

        Parent = self.selectAllBox,
    }, { Color = "ContentColor" })

    self.selectAllCheck = self.window:Create("ImageLabel", {
        Size = UDim2.fromOffset(16, 16),
        BackgroundTransparency = 1,
        Image = "rbxassetid://" .. tostring(callback),
        ImageTransparency = 1,

        Parent = selectAllSlot,
    }, { ImageColor3 = "ContentColor" })

    self.window:Create("TextLabel", {
        Text = flag.t("Select all"),
        AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, 16),
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        LayoutOrder = 2,

        Parent = self.selectAll,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    -- [[Clear: the label, then the bin. The bin is an ordinary pack icon, so it
    -- [[resolves (and caches) exactly like the icons everywhere else.]]
    self.clear = self.window:Create("TextButton", {
        Name = "Clear",
        AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, a28),
        Position = UDim2.new(1, -10, 0.5, 0),
        AnchorPoint = Vector2.new(1, 0.5),
        BackgroundTransparency = 1,
        Text = "",
        TextTransparency = 1,
        AutoButtonColor = false,

        Parent = self.actions,
    })

    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 6),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.clear,
    })

    self.clearLabel = self.window:Create("TextLabel", {
        Text = flag.t("Clear"),
        AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, 15),
        BackgroundTransparency = 1,
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Right,
        TextTransparency = actionTextIdle,
        LayoutOrder = 1,

        Parent = self.clear,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    self.clearIcon = self.window:Create("ImageLabel", {
        Size = UDim2.fromOffset(16, 16),
        BackgroundTransparency = 1,
        Image = self.window:ResolveIcon("trash"),
        ImageTransparency = actionTextIdle,
        LayoutOrder = 2,

        Parent = self.clear,
    }, { ImageColor3 = "ContentColor" })

    self.window:ConnectFor(self, self.selectAll.MouseEnter, function()
        self._selectAllHovered = true
        if self._selectAllChecked then
            return
        end
        motion.tween(self.selectAllStroke, { Transparency = actionStrokeHover }, a29)
    end)
    self.window:ConnectFor(self, self.selectAll.MouseLeave, function()
        self._selectAllHovered = false
        if self._selectAllChecked then
            return
        end
        motion.tween(self.selectAllStroke, { Transparency = actionStrokeIdle }, a29)
    end)

    local function fadeClear(transparency: number)
        motion.tween(self.clearLabel, { TextTransparency = transparency }, a29)
        motion.tween(self.clearIcon, { ImageTransparency = transparency }, a29)
    end
    self.window:ConnectFor(self, self.clear.MouseEnter, function()
        fadeClear(actionTextHover)
    end)
    self.window:ConnectFor(self, self.clear.MouseLeave, function()
        fadeClear(actionTextIdle)
    end)

    self.window:ConnectFor(self, self.selectAll.MouseButton1Click, function()
        if not self._isOpen then
            return
        end
        self:_toggleVisibleSelection()
        self:_afterBulkChange()
    end)
    self.window:ConnectFor(self, self.clear.MouseButton1Click, function()
        if not self._isOpen then
            return
        end
        self:_clearVisibleSelection()
        self:_afterBulkChange()
    end)

    self:_syncActions()
end

-- [[The options a bulk action applies to: what the list shows right now. Rows
-- [[are materialised on the first open, and until then no filter can be on
-- [[(the bar does not exist yet), so the whole option list is what is shown.]]
function state:_actionOptions(): { string }
    if #self._optionFrames == 0 then
        return self.options
    end
    return self:_visibleOptions()
end

-- [[On when every visible option is selected. A filter that matches nothing
-- [[leaves the box off: there is nothing there to select.]]
function state:_visibleSelection(): boolean
    local names = self:_actionOptions()
    if #names == 0 then
        return false
    end
    for _, name in names do
        if not table.find(self.value, name) then
            return false
        end
    end
    return true
end

function state:_toggleVisibleSelection()
    if self:_visibleSelection() then
        self:_clearVisibleSelection()
        return
    end
    for _, name in self:_actionOptions() do
        if not table.find(self.value, name) then
            table.insert(self.value, name)
        end
    end
end

function state:_clearVisibleSelection()
    local names = self:_actionOptions()
    for index = #self.value, 1, -1 do
        if table.find(names, self.value[index]) then
            table.remove(self.value, index)
        end
    end
end

-- [[Put the checkbox in step with the list. Everything that can move the
-- [[selection or the visible set calls this, so the box never lags the rows -
-- [[and one of those callers is the search filter, which runs on every
-- [[keystroke: a sync that would not change either half is left alone rather
-- [[than replaying the same two tweens per character.
-- [[]]
-- [[An unchecked box keeps its hover brightness: a click that empties it while
-- [[the pointer is still on it must not drop it to the idle dim.]]
function state:_syncActions()
    if not self.selectAllCheck then
        return
    end

    local checked = self:_visibleSelection()
    local outline = if checked
        then actionStrokeChecked
        elseif self._selectAllHovered then actionStrokeHover
        else actionStrokeIdle

    if checked == self._selectAllChecked and outline == self._selectAllOutline then
        return
    end
    self._selectAllChecked = checked
    self._selectAllOutline = outline

    motion.tween(self.selectAllCheck, { ImageTransparency = if checked then 0 else 1 }, a29)
    motion.tween(self.selectAllStroke, { Transparency = outline }, a29)
end

function state:_afterBulkChange()
            self._desiredValue = table.clone(self.value)

    for _, a96 in self._optionFrames do
        self._renderOptionState(a96, true)
    end
    self:_updateSelectedLabel()
    self:_updateCorners()
    self:_syncActions()
    self.window:_runGuarded(self, self.callback, self:_callbackValue())
    self.window:_persist(self)
    options.click()
end

function state:_resizeToOptions()
    if not self._isOpen then
        return
    end
    motion.tween(self.main, { Size = UDim2.new(1, -20, 0, self:_openHeight()) }, a35)
end

function state:_updateCorners()
    local a97 = {}
    for _, a98 in self._optionFrames do
        if a98.frame.Visible then
            table.insert(a97, a98)
        end
    end

    for i, a99 in a97 do
        local a100 = if i == 1 then button else list
        local a101 = if i == #a97 then button else list
        a99.corner.TopLeftRadius = a100
        a99.corner.TopRightRadius = a100
        a99.corner.BottomLeftRadius = a101
        a99.corner.BottomRightRadius = a101
    end
end

local function a102(a103: number): number
    return a103 * a19 + math.max(0, a103 - 1) * a20 + a21 * 2
end

local function a104(a105: number): number
    return math.max(math.floor((a105 - a21 * 2 + a20) / (a19 + a20)), 1)
end

function state:_pageHeight(): number
    local a106 = self.window.size
    return value.pageHeight(a106 and a106.Y.Offset, self.window.layout.mode)
end

function state:_openHeight()
    local n = 0
    for _, a107 in self._optionFrames do
        if a107.frame.Visible then
            n += 1
        end
    end

            local a108 = if self._searchOpen then selected else isOpen
    local a109 = a22
        + a23
        + a26
        + a108
        + a20
        + (if self.actions then a28 + a20 else 0)

    local a110 = self:_pageHeight()
        local a111 = math.min(math.max(n, 1), a27, a104(a110 - a109))

    return math.min(a109 + a102(a111), a110) + (self._descriptionExtra or 0)
end

-- [[Materialise one row, by option index. Idempotent: the row keeps whatever
-- [[state it already carries, so this can be called from any path that needs
-- [[the row to exist.]]
function state:_buildOptionAt(a: number)
    local built = self._optionFrames[a]
    if built then
        return built
    end

    local optionFrame = self._buildOption(self.options[a])
    optionFrame.frame.LayoutOrder = a
    self._optionFrames[a] = optionFrame
    return optionFrame
end

-- [[Build every row the current option list implies. Called when the list is
-- [[first looked at (opening), which is also the first moment the rows can be
-- [[seen, measured or hovered.]]
function state:_materialiseOptions()
    if self._optionsBuilt then
        return
    end
    self._optionsBuilt = true

    -- [[The bar goes in first, exactly where construction used to put it, so
    -- [[the panel's child order is what it always was. It is a sibling of the
    -- [[list (LayoutOrder 1 against the list's 3), not of the rows.]]
    if not self.searchbar then
        self:_buildSearch()
    end

    for a = 1, #self.options do
        self:_buildOptionAt(a)
    end
end

function state:_open()
    if self._isOpen then
        return
    end
    self:_materialiseOptions()
    self:_updateCorners()
    self._isOpen = true

    if self._outsideClickConn then
        self.window:Disconnect(self._outsideClickConn)
    end
    self._outsideClickConn = self.window:Connect(Dropdown.userInputService.InputBegan, function(a112)
        if
            a112.UserInputType ~= Enum.UserInputType.MouseButton1
            and a112.UserInputType ~= Enum.UserInputType.Touch
        then
            return
        end

        local a113 = a112.Position
        local a114 = self.main.AbsolutePosition
        local a115 = self.main.AbsoluteSize
        if
            a113.X < a114.X
            or a113.X > a114.X + a115.X
            or a113.Y < a114.Y
            or a113.Y > a114.Y + a115.Y
        then
            self:_close()
        end
    end)

    motion.tween(self.main, { Size = UDim2.new(1, -20, 0, self:_openHeight()) }, a32)
    motion.tween(self.chevron, { Rotation = 0 }, a33)
    motion.tween(
        self.panel,
        { BackgroundTransparency = self.window:_elementBackgroundTransparency(self) },
        a31
    )
    motion.tween(
        self.panelStroke,
        { Transparency = self.window:_elementStrokeTransparency(self, false) },
        a31
    )
    motion.tween(self.searchIcon, { ImageTransparency = 0.5 }, a31)

    for _, a116 in self._optionFrames do
        self._renderOptionState(a116, true)
    end

    self:_syncActions()
    self:_syncScrollHint()
    self:_bringIntoView()
end

function state:_bringIntoView()
    -- [[Controls built inside a Collapsible Group receive the group as their]]
    -- [[`tab` (that is what parents them into the group body), and an ordinary]]
    -- [[Group nests one link deeper still. Only the real Tab's tabPage is a]]
    -- [[ScrollingFrame: the group's body is a plain Frame, so reading]]
    -- [[AbsoluteWindowSize/CanvasPosition off it throws on the engine. Walk]]
    -- [[the owner chain up to the Tab before touching scroll members.]]
    local host = self.tab
    while host and host.__type ~= "Tab" do
        host = host.tab
    end
    local a117 = host and host.tabPage
    if not a117 then
        return
    end

        local a118, at, a119, a120 =
        a117.AbsoluteWindowSize, a117.CanvasPosition, a117.AbsolutePosition, self.main.AbsolutePosition
    if not a118 or not at or not a119 or not a120 or a118.Y <= 0 then
        return
    end

        local a121 = a120.Y - a119.Y + at.Y
    local a122 = a121 + self:_openHeight()
    local a123 = a122 - (at.Y + a118.Y)
    if a123 <= 0 then
        return     end

            local a124 = math.min(at.Y + a123 + 8, a121)
    motion.tween(
        a117,
        {
            CanvasPosition = Vector2.new(at.X, a124),
        },
        a31
    )
end

function state:_close()
    if not self._isOpen then
        return
    end
    self._isOpen = false

    if self._outsideClickConn then
        self.window:Disconnect(self._outsideClickConn)
        self._outsideClickConn = nil
    end

    self:_collapseSearch()
    self:_syncScrollHint()

    motion.tween(self.chevron, { Rotation = 180 }, a33)
    motion.tween(self.panel, { BackgroundTransparency = 1 }, a31)
    motion.tween(self.panelStroke, { Transparency = 1 }, a31)
    motion.tween(self.searchIcon, { ImageTransparency = 1 }, a31)

    for _, a125 in self._optionFrames do
        self._renderOptionState(a125, true)
    end

    motion.tween(self.main, { Size = UDim2.new(1, -20, 0, description.height(self, 41)) }, a32)
end

function state:_destroyOption(a126)
    if a126.connections then
        for _, a127 in a126.connections do
            local a128 = table.find(self.connections, a127)
            if a128 then
                table.remove(self.connections, a128)
            end
            self.window:Disconnect(a127)
        end
        a126.connections = nil
    end
    self.window:DestroySubtree(a126.frame)
end

function state:_deriveSelection()
    local a129 = self.value
    self.value = a44(self._desiredValue or self.value, self.options)
    return not a48(self.value, a129)
end

function state:_reindexOptions()
    for a130, a131 in self._optionFrames do
        a131.frame.LayoutOrder = a130
    end
end

function state:_rebindOption(a132, a133, a134)
    a132.name = a133
    a132.title.Text = a133
    a132.frame.LayoutOrder = a134
end

function state:_destroyOptionsFrom(a135)
    local a136, a137 = {}, {}

    for a138 = #self._optionFrames, a135, -1 do
        local a139 = self._optionFrames[a138]
        table.insert(a136, a139.frame)
        if a139.connections then
            table.move(a139.connections, 1, #a139.connections, #a137 + 1, a137)
            a139.connections = nil
        end
        self._optionFrames[a138] = nil
    end

    self.window:DisconnectMany(self, a137)
    self.window:DestroySubtrees(a136)
end

function state:Refresh(a140)
    self.options = a36(a140 or {})

    local a141 = self:_deriveSelection()

    -- [[A dropdown that was never opened has no rows to rebind: the new list
    -- [[is what the rows are built from the first time it is opened.]]
    if self._optionsBuilt then
                        local a142 = #self._optionFrames
        local a143 = #self.options

        for a144 = 1, math.min(a142, a143) do
            self:_rebindOption(self._optionFrames[a144], self.options[a144], a144)
        end

        for a145 = a142 + 1, a143 do
            local a146 = self._buildOption(self.options[a145])
            a146.frame.LayoutOrder = a145
            table.insert(self._optionFrames, a146)
        end

        if a143 < a142 then
            self:_destroyOptionsFrom(a143 + 1)
        end

                    for _, a147 in self._optionFrames do
            self._renderOptionState(a147, false)
        end
    end

    self._updateSelectedLabel()
        self:_applyFilter(if self._searchOpen then self.searchInput.Text else "")

        if a141 then
        self.window:_runGuarded(self, self.callback, self:_callbackValue())
        self.window:_persist(self)
    end
end

function state:Add(a148)
    if typeof(a148) ~= "string" or a148 == "" then
        return
    end
    if table.find(self.options, a148) then
        return
    end

    table.insert(self.options, a148)
    -- [[While the rows are still unbuilt the option list is the only record;
    -- [[the new row appears with the rest the first time this dropdown opens.]]
    local a149 = if self._optionsBuilt then self:_buildOptionAt(#self.options) else nil

        local a150 = self:_deriveSelection()

    if self._isOpen and a149 then
        self:_renderOptionState(a149, true)
    end

            self:_applyFilter(if self._searchOpen then self.searchInput.Text else "")

        if a150 then
        self._updateSelectedLabel()
        self.window:_runGuarded(self, self.callback, self:_callbackValue())
        self.window:_persist(self)
    end
end

function state:Remove(a151)
    local a152 = table.find(self.options, a151)
    if not a152 then
        return
    end

    table.remove(self.options, a152)

    for i, a153 in self._optionFrames do
        if a153.name == a151 then
            self:_destroyOption(a153)
            table.remove(self._optionFrames, i)
            self:_reindexOptions()
            break
        end
    end

            if self._desiredValue then
        local a154 = table.find(self._desiredValue, a151)
        if a154 then
            table.remove(self._desiredValue, a154)
        end
    end

    local a155 = table.find(self.value, a151)
    if a155 then
        table.remove(self.value, a155)
        self._updateSelectedLabel()
                self.window:_runGuarded(self, self.callback, self:_callbackValue())
        self.window:_persist(self)
    end

        self:_applyFilter(if self._searchOpen then self.searchInput.Text else "")
end

function state:Set(a156, a157)
    local a158 = a40(a156, self.multiSelect)
        self._desiredValue = a158
    self.value = a44(a158, self.options)

    for _, a159 in self._optionFrames do
        self._renderOptionState(a159, true)
    end
    self._updateSelectedLabel()
    self:_syncActions()

    if not a157 then
        self.window:_runGuarded(self, self.callback, self:_callbackValue())
        self.window:_persist(self)     end
end

function state:_setShown(a160, a161)
            local w = self.window
    w:_reveal(self.stroke, { Transparency = if a160 then w:_elementStrokeTransparency(self, false) else 1 }, a161)
    w:_reveal(self.title, { TextTransparency = if a160 then 0 else 1 }, a161)
    w:_reveal(self.top, { BackgroundTransparency = if a160 then (w:_elementBackgroundTransparency(self)) else 1 }, a161)
    if self.iconLabel then
        w:_reveal(self.iconLabel, { ImageTransparency = if a160 then 0 else 1 }, a161)
    end
    w:_reveal(self.selectedLabel, { TextTransparency = if a160 then 0.5 else 1 }, a161)
    w:_reveal(self.chevron, { ImageTransparency = if a160 then 0.5 else 1 }, a161)

        if not a160 and self._isOpen then
        self:_close()
    end
end

function state:MoveTo(a162)
    self.tab:_moveElement(self, a162)
end

function state:MoveToTop()
    self.tab:_moveElement(self, 1)
end

function state:MoveToBottom()
    self.tab:_moveElement(self, #self.tab.elements)
end

function state:MoveUp()
    local a163 = table.find(self.tab.elements, self)
    if a163 then
        self.tab:_moveElement(self, a163 - 1)
    end
end

function state:MoveDown()
    local a164 = table.find(self.tab.elements, self)
    if a164 then
        self.tab:_moveElement(self, a164 + 1)
    end
end

multiSelect(state)

return state

end)() end,
    [29] = function()local wax,script,require=ImportGlobals(29)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Group"

local constants = require(script.Parent.Parent.utilities.moveable)
local Group = require(script.Parent.Parent.utilities.log)
local tab = require(script.Parent.Parent.utilities.ordering)

local props = 8

local direction = {
    button = true,
    toggle = true,
    switch = true,
    stat = true,
    slider = true,
}

function state.new(tab, element)
    element = if typeof(element) == "table" then element else {}

    local frame = string.lower(element.direction or element.Direction or "row")
    local a9 = frame == "column" or frame == "vertical"
    local a10 = not a9
    local a11 = if a10 then Enum.FillDirection.Horizontal else Enum.FillDirection.Vertical

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),         window = tab.window,
        direction = a11,
        compact = a10,         forgetState = tab.forgetState,         elements = {},
        deriveFlagFromName = tab.deriveFlagFromName,
    }, state)

        local a12 = tab.direction == Enum.FillDirection.Horizontal

    self.main = self.window:Create("Frame", {
        Name = "Group",
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        AutomaticSize = Enum.AutomaticSize.Y,
                        Size = if a10 then UDim2.new(1, -20, 0, 0) else UDim2.new(1, 0, 0, 0),

        Parent = self.tab.tabPage,
    })

    if a12 then
                self.main.Size = UDim2.new(0, 0, 0, 0)
        self.window:Create("UIFlexItem", {
            FlexMode = Enum.UIFlexMode.Fill,
            Parent = self.main,
        })
    end

        self.tabPage = self.main

    self.layout = self.window:Create("UIListLayout", {
        FillDirection = a11,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, props),
        VerticalAlignment = if a10 then Enum.VerticalAlignment.Center else Enum.VerticalAlignment.Top,
        HorizontalAlignment = if a10 then Enum.HorizontalAlignment.Left else Enum.HorizontalAlignment.Center,

        Parent = self.main,
    })

    return self
end

function state:_add(a13, a14)
    -- "Switch" is the declarative alias of the toggle control.
    if a13 == "switch" then
        a13 = "toggle"
    end
    if self.compact and not direction[a13] then
        Group.warn(
            `Astra: a row only holds compact elements (button/toggle/stat/slider), ignoring '{a13}'. Use a column for it.`
        )
        return nil
    end

    local element = require(script.Parent[a13]).new(self, a14)
    if self._collapsibleChild then
        element._collapsibleChild = true
    end
    table.insert(self.elements, element)
    tab(element, #self.elements * 10)
    self.window:_restoreLate(element)

    if self.compact then
        self:_wrapChild(element)
    end
    self:_reflowRow()
    local ownerTab = self.tab
    local contentsVisible = true
    while ownerTab.tab do
        if ownerTab.__type == "CollapsibleGroup" and not ownerTab._expanded then contentsVisible = false end
        ownerTab = ownerTab.tab
    end
    if contentsVisible and not self.window.hidden and not self.window.minimised and self.window.selectedTab == ownerTab then
        element:_setShown(true, true)
    else
        ownerTab._elementsPending = true
    end
    self.window:_constructionCheckpoint(false)

    return element
end

function state:_reflowRow()
    if self.direction ~= Enum.FillDirection.Horizontal then
        return
    end

    local a15 = #self.elements > 0
    for _, element in self.elements do
        if element.__type ~= "Group" then
            a15 = false
            break
        end
    end

    self.layout.Padding = if a15 then UDim.new(0, -10) else UDim.new(0, props)
end

function state:_wrapChild(element)
    self.layout.Wraps = true
    self.layout.HorizontalFlex = Enum.UIFlexAlignment.Fill

            element._widthManaged = true

    local a16 = if element._minWidth then element:_minWidth() else 0
    if a16 > 0 then
        element.main.AutomaticSize = Enum.AutomaticSize.None
        element.main.Size = UDim2.new(0, a16, element.main.Size.Y.Scale, element.main.Size.Y.Offset)
    end
end

function state:CreateButton(a17)
    return self:_add("button", a17)
end
function state:CreateToggle(a18)
    return self:_add("toggle", a18)
end
function state:CreateStat(a20)
    return self:_add("stat", a20)
end
function state:CreateSlider(a21)
    return self:_add("slider", a21)
end
function state:CreateDropdown(a22)
    return self:_add("dropdown", a22)
end
function state:CreateSection(a23)
    return self:_add("section", a23)
end
function state:CreateText(a24)
    return self:_add("text", a24)
end
function state:CreateDivider(a25)
    return self:_add("divider", a25)
end

function state:_addGroup(a26)
    a26 = if typeof(a26) == "table" then table.clone(a26) else {}

        if self.direction == Enum.FillDirection.Horizontal then
                        self.layout.VerticalAlignment = Enum.VerticalAlignment.Top
                        if self.tab.direction ~= Enum.FillDirection.Horizontal then
            self.main.Size = UDim2.new(1, 0, 0, 0)
        end
    end

    local a27 = state.new(self, a26)
    if self._collapsibleChild then
        a27._collapsibleChild = true
    end
    table.insert(self.elements, a27)
    a27.main.LayoutOrder = #self.elements * 10
    self:_reflowRow()
    return a27
end

function state:CreateGroup(a28)
    return self:_addGroup(a28)
end

function state:_moveElement(element, a29)
    local a30 = table.find(self.elements, element)
    if not a30 then
        return
    end
    table.remove(self.elements, a30)
    a29 = math.clamp(a29, 1, #self.elements + 1)
    table.insert(self.elements, a29, element)
    for i, el in self.elements do
        tab(el, i * 10)
    end
end

function state:_setShown(a31, a32)
    for _, el in self.elements do
        el:_setShown(a31, a32)
    end
end

function state:_refreshTheme()
    for _, el in self.elements do
        if el._refreshTheme then
            el:_refreshTheme()
        end
    end
end

constants(state)

return state

end)() end,
    [30] = function()local wax,script,require=ImportGlobals(30)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Input"

local constants = script.Parent.Parent.utilities

local Input = require(constants.Parent.core.state)
local tab = require(script.Parent.Parent.functions)
local props = require(constants.moveable)
local name = require(constants.lockable)
local placeholder = require(constants.locale)
local description = require(script.Parent.description)
local numeric = require(constants.constants)
local clearOnFocus = require(constants.HapticEngine)

-- [[Field feedback rides the shared motion service: the box follows typing
-- on the hover spec (quick, but never jumpy), focus fades the caret text on
-- the press spec, and theme refreshes cross-fade on the surface spec.]]
local motion = require(constants.motion)
local callback = "snappy"
local element = "fast"

local function frame(textBox)
    local a, b = textBox:match("^([%d%.%-]+)%^([%d%.%-]+)$")
    if a and b then
        local na, nb = tonumber(a), tonumber(b)
        if na and nb then
            return na ^ nb
        end
    end
    return tonumber(textBox)
end

function state.new(tab, placeholderLabel)
    placeholderLabel = if typeof(placeholderLabel) == "table" then placeholderLabel else {}

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        name = placeholderLabel.name or placeholderLabel.Name or "Input",
        icon = tab.window:ResolveIcon(placeholderLabel.icon or placeholderLabel.Icon),
        description = placeholderLabel.description or placeholderLabel.Description,
        forgetState = placeholderLabel.forgetState or placeholderLabel.ForgetState or tab.forgetState,
        placeholder = placeholderLabel.placeholder or placeholderLabel.Placeholder or "",
        numeric = placeholderLabel.numeric or placeholderLabel.Numeric or false,
        clearOnFocus = placeholderLabel.clearOnFocus or placeholderLabel.ClearOnFocus or false,

        callback = placeholderLabel.callback or placeholderLabel.Callback or function() end,
    }, state)

    self.value =
        tostring(placeholderLabel.value or placeholderLabel.Value or placeholderLabel.currentValue or placeholderLabel.CurrentValue or "")

    self.flag = placeholderLabel.flag
        or placeholderLabel.Flag
        or (not self.forgetState and tab.deriveFlagFromName(self.name) or nil)
    self.window:_registerControl(self)

    self.main = self.window:Create("Frame", {
        Size = UDim2.new(1, -20, 0, 41),
        BorderSizePixel = 0,
        Name = self.name,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),

        BackgroundTransparency = 1,
        Parent = self.tab.tabPage,
    }, { BackgroundTransparency = "ElementTransparency" })

    self.stroke = self.window:StyleElementBody(self.main)
    self.hoverOverlay = self.window:CreateHoverOverlay(self.main)

    -- [[The card grows for a description before the title row and the field
    -- [[box are built, so both centre in the base region instead of riding
    -- [[down onto the line.]]
    description.attach(self)

    self.container = self.window:Create("Frame", {
        Size = UDim2.new(1, -115, 0, 16),
        Position = UDim2.new(0, 20, description.center(self, 41)),
        AnchorPoint = Vector2.new(0, 0.5),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ZIndex = 5,

        Parent = self.main,
    })

    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,

        Parent = self.container,
    })

    if self.icon then
        self.iconLabel = self.window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            ZIndex = 5,

            ImageTransparency = 1,
            Parent = self.container,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = self.window:Create("TextLabel", {
        Text = placeholder.t(self.name),
        Size = UDim2.new(1, if self.icon then -21 else 0, 0, 16),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,
        ZIndex = 5,

        TextTransparency = 1,
        Parent = self.container,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    self.box = self.window:Create("Frame", {
        AnchorPoint = Vector2.new(1, 0.5),
        Position = UDim2.new(1, -7, description.center(self, 41)),
        Size = UDim2.fromOffset(85, 30),
        BorderSizePixel = 0,

        BackgroundTransparency = 1,
        Parent = self.main,
    }, { BackgroundColor3 = "FieldBackground" })

    self.window:Create("UICorner", {
        Parent = self.box,
    }, { CornerRadius = "ElementCornerRadius" })

    self.boxStroke = self.window:Create("UIStroke", {
        Transparency = 1,
        Parent = self.box,
    }, { Color = "SurfaceStroke" })

    self._glowIdle = 1
    self.input = self.window:Create("TextBox", {
        Text = self.value,
        PlaceholderText = placeholder.t(self.placeholder),
        Size = UDim2.new(1, -15, 0, 15),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        AnchorPoint = Vector2.new(0.5, 0.5),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Center,
        TextTruncate = Enum.TextTruncate.AtEnd,
        ClearTextOnFocus = self.clearOnFocus,

        TextTransparency = 1,
        Parent = self.box,
    }, { TextColor3 = "ContentColor", FontFace = "Font", PlaceholderColor3 = "PlaceholderColor" })

    self.window:ConnectFor(self, self.input:GetPropertyChangedSignal("Text"), function()
        if self.numeric then
            local a15 = (self.input.Text:gsub("[^%d%.%-eE%^]", ""))
            if a15 ~= self.input.Text then
                self.input.Text = a15
                return
            end
        end
        self:_sizeBox(true)
    end)

    self.window:ConnectFor(self, self.input.Focused, function()
        motion.tween(self.input, { TextTransparency = 0 }, element)
    end)

    self.window:ConnectFor(self, self.input.FocusLost, function()
        motion.tween(self.input, { TextTransparency = 0.6 }, element)
        if self.clearOnFocus and self.input.Text == "" and self.value ~= "" then
            self.input.Text = self.value
            return
        end
        if self.input.Text == self.value then
            return
        end
        self:_commit(self.input.Text)
    end)

    self.window:_wireElementHover(self)

    self:_sizeBox(false)
    return self
end

function state:_sizeBox(a16)
    local a17 = self.input.Text ~= "" and self.input.Text or self.placeholder
    local a18 = math.clamp(tab.textWidth(self.window.theme.Font, 15, a17) + 30, 70, 220)
    self.container.Size = UDim2.new(1, -(a18 + 35), 0, 16)
    if a16 then
        motion.tween(self.box, { Size = UDim2.fromOffset(a18, 30) }, callback)
    else
        self.box.Size = UDim2.fromOffset(a18, 30)
    end
end

function state:_commit(a19, a20)
    a19 = tostring(a19)
    if self.numeric then
        local n = frame(a19)
        if not n or n ~= n or n == math.huge or n == -math.huge then
            if self.input.Text ~= self.value then
                self.input.Text = self.value
            end
            return
        end
        a19 = tostring(n)
    end

    local a21 = a19 ~= self.value
    self.value = a19
    if self.input.Text ~= a19 then
        self.input.Text = a19
    end

    if not a20 then
        self.window:_runGuarded(self, self.callback, a19)
        self.window:_persist(self)
        if a21 and not self.window._loading then
            clearOnFocus.click()
            self.window:_flashResult(self, true)
        end
    end
end

function state:Set(a22, a23)
    self:_commit(a22, a23)
end

function state:_fieldGlow()
    if not self.glow then
        self.glow = self.window:CreateGlow(self.box, "FieldGlow", 20, 1)
    end
    return self.glow
end

function state:_setShown(a24, a25)
    local w = self.window
    if a24 then
        w:_revealCommon(self, a25)
        w:_reveal(self.box, { BackgroundTransparency = w.theme.FieldTransparency }, a25)
        w:_reveal(self.boxStroke, { Transparency = 0.85 }, a25)
        w:_reveal(self.input, { TextTransparency = 0.6 }, a25)
    else
        w:_hideCommon(self, a25)
        w:_reveal(self.box, { BackgroundTransparency = 1 }, a25)
        w:_reveal(self.boxStroke, { Transparency = 1 }, a25)
        w:_reveal(self.input, { TextTransparency = 1 }, a25)
    end
end

function state:_refreshTheme()
    motion.tween(
        self.box,
        { BackgroundTransparency = self.window.theme.FieldTransparency },
        "smooth"
    )
end

props(state)
name(state)

return state

end)() end,
    [31] = function()local wax,script,require=ImportGlobals(31)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Section"

local constants = require(script.Parent.Parent.utilities.moveable)
local Section = require(script.Parent.Parent.utilities.locale)

function state.new(tab, element)
    element = if typeof(element) == "table" then element else {}

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        name = element.name or element.Name or "Section",
        icon = tab.window:ResolveIcon(element.icon or element.Icon),
    }, state)

    local props = if #self.tab.elements == 0 then 0 else 13

    self.main = self.window:Create("Frame", {
        Size = UDim2.new(1, -40, 0, 20 + props),
        BorderSizePixel = 0,
        Name = self.name,
        BackgroundTransparency = 1,
        Parent = self.tab.tabPage,
    })

    if props > 0 then
        self.window:Create("UIPadding", {
            PaddingTop = UDim.new(0, props),
            Parent = self.main,
        })
    end

    self.window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        Padding = UDim.new(0, 6),
        SortOrder = Enum.SortOrder.LayoutOrder,
        VerticalAlignment = Enum.VerticalAlignment.Top,
        Parent = self.main,
    })

    if self.icon then
        self.iconLabel = self.window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,

            ImageTransparency = 1,
            Parent = self.main,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = self.window:Create("TextLabel", {
        Text = Section.t(self.name),

        Size = UDim2.new(1, if self.icon then -22 else 0, 0, 16),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,

        TextTransparency = 1,
        Parent = self.main,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    return self
end

function state:_setShown(name, icon)
    local w = self.window
    w:_reveal(self.title, { TextTransparency = if name then 0.6 else 1 }, icon)
    if self.iconLabel then
        w:_reveal(self.iconLabel, { ImageTransparency = if name then 0.65 else 1 }, icon)
    end
end

constants(state)

return state

end)() end,
    [32] = function()local wax,script,require=ImportGlobals(32)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Slider"

local constants = script.Parent.Parent.utilities

local Slider = require(constants.Parent.core.state)
local tab = require(script.Parent.Parent.functions)
local props = require(constants.odometer)
local name = require(constants.moveable)
local flag = require(constants.lockable)
local range = require(constants.locale)
local value = require(constants.HapticEngine)
local description = require(script.Parent.description)

local function increment(suffix)
    local minimal = 0
    while minimal < 6 do
        local callback = suffix * 10 ^ minimal
        if math.abs(callback - math.round(callback)) < 1e-9 then
            break
        end
        minimal += 1
    end
    return minimal
end

local function element(frame, fill, handle)
    local min = frame[1] + math.round((handle - frame[1]) / fill) * fill
    return math.clamp(min, frame[1], frame[2])
end

-- [[The slider's fill and handle always move through the shared service:
-- `normal` carries value changes, `fast` follows a drag (the handle chases
-- the pointer, so it must be quick and never fight the next frame's target),
-- `snappy` carries hover. Glow reveals trail on the delayed accent beat.]]
local motion = require(constants.motion)
local max = "normal"
local isDragging = "fast"
local connection = "snappy"

local a22 = 300

local a23 = Vector2.new(35, 20)
local a24 = Vector2.new(41, 22)

local function a28(x: number, a29: number, a30: number): number
    return math.clamp((x - a29) / a30, 0, 1)
end

function state.new(ownerTab, a31)
    a31 = if typeof(a31) == "table" then a31 else {}

    local self = setmetatable({
        tab = assert(ownerTab, "Missing argument #1 (Tab expected)"),
        window = ownerTab.window,
        name = a31.name or a31.Name or "Slider",
        icon = ownerTab.window:ResolveIcon(a31.icon or a31.Icon),
        description = a31.description or a31.Description,
        forgetState = a31.forgetState or a31.ForgetState or ownerTab.forgetState,

        range = a31.range or a31.Range or { 0, 100 },
        increment = a31.increment or a31.Increment or 1,
        suffix = a31.suffix or a31.Suffix or "",

        callback = a31.callback or a31.Callback or function() end,

        dragging = false,
        minimal = a31.minimal or a31.Minimal or false,
        _handleWidth = a23.X,
    }, state)

    assert(
        typeof(self.range) == "table" and typeof(self.range[1]) == "number" and typeof(self.range[2]) == "number",
        "A slider range needs two numbers, like { 0, 100 }."
    )

    if self.range[1] > self.range[2] then
        self.range = { self.range[2], self.range[1] }
    end
    if self.increment <= 0 then
        self.increment = 1
    end

    self.value = if (a31.value or a31.Value) ~= nil
        then (a31.value or a31.Value)
        elseif (a31.currentValue or a31.CurrentValue) ~= nil then (
            a31.currentValue or a31.CurrentValue
        )
        else self.range[1]

    self.value = element(self.range, self.increment, self.value)

    self._decimals = increment(self.increment)

    self.flag = a31.flag
        or a31.Flag
        or (not self.forgetState and ownerTab.deriveFlagFromName(self.name) or nil)
    self.window:_registerControl(self)

    self.main = self.window:Create("Frame", {
        Size = UDim2.new(1, -20, 0, 65),
        BorderSizePixel = 0,
        Name = self.name,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),

        BackgroundTransparency = 1,

        Parent = self.tab.tabPage,
    }, { BackgroundTransparency = "ElementTransparency" })

    self.stroke = self.window:StyleElementBody(self.main)
    -- [[The card grows for a description before the label row and the track
    -- [[are built, so both seat themselves against the grown region below.]]
    description.attach(self)
    self._lastValue = self.value

    if not self.minimal then
        self:_buildLabel()
    end

    self.track = self.window:Create("Frame", {
        AnchorPoint = Vector2.new(1, 0.5),
        Position = UDim2.new(1, -15, 0.5, 0),
        Size = UDim2.fromOffset(222, 14),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        Parent = self.main,
    }, { BackgroundColor3 = "SliderBackground" })

    self.window:Create("UICorner", {
        CornerRadius = UDim.new(0, 13),

        Parent = self.track,
    })

    self.progress = self.window:Create("Frame", {
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.fromScale(0, 0.5),
        Size = UDim2.fromScale(0, 1),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        ZIndex = 2,
        BackgroundTransparency = 1,
        Parent = self.track,
    })

    self.window:Create("UICorner", {
        CornerRadius = UDim.new(0, 13),

        Parent = self.progress,
    })

    self.window:Create("UIGradient", {
        Offset = Vector2.new(0, 0.5),
        Rotation = 2,
        Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0, 0.85),
            NumberSequenceKeypoint.new(1, 0),
        }),

        Parent = self.progress,
    }, { Color = { "SliderProgress", tab.toColorSequence } })

    self.progressGlow = self.window:CreateGlow(self.progress, "AccentColor", 20, 1)

    self.handle = self.window:Create("Frame", {
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0, 0, 0.5, 0),
        Size = UDim2.fromOffset(a23.X, a23.Y),
        BorderSizePixel = 0,
        ZIndex = 50,
        BackgroundTransparency = 1,
        Parent = self.track,
    }, { BackgroundColor3 = "SliderHandle" })

    self.window:Create("UICorner", {
        CornerRadius = UDim.new(1, 0),

        Parent = self.handle,
    })

    self.handleGlow = self.window:CreateGlow(self.handle, Color3.fromRGB(255, 255, 255), 10, 1)

    self.handleStroke = self.window:Create("UIStroke", {
        Transparency = 1,
        Parent = self.handle,
    }, { Color = "SliderStroke" })

    self.window:ConnectFor(self, self.track:GetPropertyChangedSignal("AbsoluteSize"), function()
        self:_renderProgress()
    end)

    self.interact = self.window:Create("TextButton", {
        BackgroundTransparency = 1,
        Size = UDim2.fromScale(1, 1),
        Text = "",
        TextTransparency = 1,
        ZIndex = 10,

        Parent = self.track,
    })

    self.window:ConnectFor(self, self.main.MouseEnter, function()
        if not self.window:_interactive() then
            return
        end
        motion.tween(self.track, { BackgroundColor3 = self.window.theme.SliderBackgroundHover }, max)
    end)

    self.window:ConnectFor(self, self.main.MouseLeave, function()
        motion.tween(self.track, { BackgroundColor3 = self.window.theme.SliderBackground }, max)
    end)

    self.window:ConnectFor(self, self.interact.InputBegan, function(a32)
        if
            a32.UserInputType == Enum.UserInputType.MouseButton1
            or a32.UserInputType == Enum.UserInputType.Touch
        then
            value.click()
            self.dragging = true
            self:_setHeld(true)
            self:_updateFromMouse()

            if self._dragConnection then
                self._dragConnection:Disconnect()
                self._dragConnection = nil
            end

            self._dragConnection = Slider.runService.RenderStepped:Connect(function()
                if self.window.unloaded or not self.dragging then
                    if self._dragConnection then
                        self._dragConnection:Disconnect()
                        self._dragConnection = nil
                    end
                    return
                end
                self:_updateFromMouse()
            end)
        end
    end)

    self.window:ConnectFor(self, Slider.userInputService.InputEnded, function(a33)
        if
            a33.UserInputType == Enum.UserInputType.MouseButton1
            or a33.UserInputType == Enum.UserInputType.Touch
        then
            self:_endDrag()
        end
    end)

    self.window:ConnectFor(self, Slider.userInputService.WindowFocusReleased, function()
        self:_endDrag()
    end)

    self.window:ConnectFor(self, self.main:GetPropertyChangedSignal("AbsoluteSize"), function()
        if self.window.animating or (self.window.hidden and self.window.hasShownOnce) then
            return
        end
        self:_applyLayout()
    end)
    self:_applyLayout()

    if self.minimal then
        self.main.Size = UDim2.new(1, -20, 0, description.height(self, 41))
        description.rebase(self, 41)
        self.track.AnchorPoint = Vector2.new(0.5, 0.5)
        self.track.Position = UDim2.new(0.5, 0, description.center(self, 41))
        self.track.Size = UDim2.new(1, -30, 0, 14)
    end

    self:_renderProgress()

    return self
end

function state:_buildLabel()
    self.container = self.window:Create("Frame", {
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0, 20, description.center(self, 65)),
        Size = UDim2.new(1, -255, 0, 33),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ZIndex = 5,

        Parent = self.main,
    })

    self.containerLayout = self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 2),
        VerticalAlignment = Enum.VerticalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.container,
    })

    self.titleContainer = self.window:Create("Frame", {
        Size = UDim2.new(1, 0, 0, 16),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ZIndex = 5,

        Parent = self.container,
    })

    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.titleContainer,
    })

    self.titleFlex = self.window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.None,
        Parent = self.titleContainer,
    })

    if self.icon then
        self.iconLabel = self.window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            ZIndex = 5,

            ImageTransparency = 1,
            Parent = self.titleContainer,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = self.window:Create("TextLabel", {
        Text = range.t(self.name),
        Size = UDim2.new(1, 0, 0, 16),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        RichText = true,
        LayoutOrder = 1,
        ZIndex = 5,

        TextTransparency = 1,
        Parent = self.titleContainer,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    self.window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Shrink,
        Parent = self.title,
    })

    self.valueHost = self.window:Create("Frame", {
        Size = UDim2.new(1, 0, 0, 16),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ZIndex = 6,
        LayoutOrder = 1,
        Parent = self.container,
    })

    self.valueOdo = props.new(self.window, self.valueHost, {
        textSize = 15,
        alignment = Enum.HorizontalAlignment.Left,
        transparency = 1,
        duration = 0.28,
    })
    self.valueOdo:snap(self:_format(self.value))
end

function state:_setMainHeight(a34)
    -- [[`a34` is the base region every layout writes (65 wide, 70 narrow); the
    -- [[description's measured extra rides along and the line is re-seated
    -- [[against the new base.]]
    local height = description.height(self, a34)
    description.rebase(self, a34)
    if self._widthManaged then
        self.main.Size = UDim2.new(self.main.Size.X.Scale, self.main.Size.X.Offset, 0, height)
    else
        self.main.Size = UDim2.new(1, -20, 0, height)
    end
end

function state:_applyLayout()
    if self.minimal then
        return
    end
    local a35 = self.main.AbsoluteSize.X
    local a36 = if a35 > 0 and a35 < a22 then "narrow" else "wide"
    if self._layoutMode == a36 then
        return
    end
    self._layoutMode = a36

    if a36 == "narrow" then
        self:_setMainHeight(70)

        self.container.AnchorPoint = Vector2.new(0, 0)
        self.container.Position = UDim2.new(0, 20, 0, 14)
        self.container.Size = UDim2.new(1, -40, 0, 16)
        self.containerLayout.FillDirection = Enum.FillDirection.Horizontal
        self.titleFlex.FlexMode = Enum.UIFlexMode.Fill
        self.valueHost.AutomaticSize = Enum.AutomaticSize.X
        self.valueHost.Size = UDim2.new(0, 0, 0, 16)

        self.track.AnchorPoint = Vector2.new(0.5, 1)
        self.track.Position = UDim2.new(0.5, 0, 1, -14 - (self._descriptionExtra or 0))
        self.track.Size = UDim2.new(1, -30, 0, 14)
    else
        self:_setMainHeight(65)

        self.container.AnchorPoint = Vector2.new(0, 0.5)
        self.container.Position = UDim2.new(0, 20, description.center(self, 65))
        self.container.Size = UDim2.new(1, -255, 0, 33)
        self.containerLayout.FillDirection = Enum.FillDirection.Vertical
        self.titleFlex.FlexMode = Enum.UIFlexMode.None
        self.valueHost.AutomaticSize = Enum.AutomaticSize.None
        self.valueHost.Size = UDim2.new(1, 0, 0, 16)

        self.track.AnchorPoint = Vector2.new(1, 0.5)
        self.track.Position = UDim2.new(1, -15, description.center(self, 65))
        self.track.Size = UDim2.new(0, 222, 0, 14)
    end

    self:_renderProgress()
end

function state:_format(a37)
    local a38 = string.format("%." .. self._decimals .. "f", a37)
    if self.suffix ~= "" then
        return a38 .. " " .. self.suffix
    end
    return a38
end

function state:_pillTravel()
    return math.max(self.track.AbsoluteSize.X, 0)
end

function state:_renderProgress(a39)
    local a40 = self.range[2] - self.range[1]
    local a41 = if a40 ~= 0 then math.clamp((self.value - self.range[1]) / a40, 0, 1) else 0

    local half = (self._handleWidth or a23.X) / 2
    local center = half + a41 * math.max(self:_pillTravel() - half * 2, 0)
    local fill = UDim2.new(0, center, 1, 0)
    local at = UDim2.new(0, center, 0.5, 0)
    if a39 then
        motion.tween(self.progress, { Size = fill }, a39)
        motion.tween(self.handle, { Position = at }, a39)
    else
        self.progress.Size = fill
        self.handle.Position = at
    end
end

function state:_updateFromMouse()
    local a43 = self:_pillTravel()
    if a43 <= 0 then
        return
    end

    local a44 =
        a28(Slider.userInputService:GetMouseLocation().X, self.track.AbsolutePosition.X, a43)
    local a45 = element(self.range, self.increment, self.range[1] + a44 * (self.range[2] - self.range[1]))

    if a45 ~= self.value then
        self.value = a45
        if self.valueOdo then
            self.valueOdo:snap(self:_format(a45))
        end
        self._lastValue = a45
        self:_renderProgress(connection)
        self:_fireCallback(a45)
    end
end

function state:_endDrag()
    if not self.dragging then
        return
    end
    self.dragging = false
    self:_setHeld(false)

    if self._dragConnection then
        self._dragConnection:Disconnect()
        self._dragConnection = nil
    end

    self.window:_persist(self)
end

function state:_setHeld(a46)
    if not a46 then
        self:_fireCallback(self.value)
    end

    local a47 = if a46 then a24 else a23
    self._handleWidth = a47.X

    motion.tween(self.handle, {
        Size = UDim2.fromOffset(a47.X, a47.Y),
        BackgroundTransparency = if a46 then 0.7 else 0,
    }, isDragging)
    motion.tween(self.handleStroke, { Transparency = if a46 then 0.6 else 1 }, isDragging)
    self:_renderProgress(isDragging)
end

function state:_fireCallback(a48)
    self.window:_runGuarded(self, self.callback, a48, self.dragging == true)
end

function state:Set(a49, a50)
    a49 = element(self.range, self.increment, a49)
    self.value = a49
    if self.valueOdo then
        self.valueOdo:to(self:_format(a49), a49 >= (self._lastValue or a49))
    end
    self._lastValue = a49
    self:_renderProgress(max)

    if not a50 then
        self:_fireCallback(a49)
        self.window:_persist(self)
    end
end

local glowBase = TweenInfo.new(0.4, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out, 0, false, 0.35)
local function a51()
    return motion.spec(glowBase)
end

function state:_setShown(a52, a53)
    local w = self.window
    if a52 then
        w:_revealCommon(self, a53)
        w:_reveal(self.track, { BackgroundTransparency = 0 }, a53)
        w:_reveal(self.progress, { BackgroundTransparency = 0 }, a53)
        w:_reveal(self.handle, { BackgroundTransparency = 0 }, a53)
        if self.valueOdo then
            self.valueOdo:reveal(0.3, a53)
        end
        w:_reveal(self.progressGlow, { Transparency = math.max(0.55, w.theme.AccentGlow) }, a53, a51())
        w:_reveal(self.handleGlow, { Transparency = 0.8 }, a53, a51())
    else
        w:_hideCommon(self, a53)
        w:_reveal(self.track, { BackgroundTransparency = 1 }, a53)
        w:_reveal(self.progress, { BackgroundTransparency = 1 }, a53)
        w:_reveal(self.handle, { BackgroundTransparency = 1 }, a53)
        if self.valueOdo then
            self.valueOdo:reveal(1, a53)
        end
        w:_reveal(self.progressGlow, { Transparency = 1 }, a53, a51())
        w:_reveal(self.handleGlow, { Transparency = 1 }, a53, a51())
    end
end

function state:_refreshTheme()
    motion.tween(
        self.progressGlow,
        { Transparency = math.max(0.55, self.window.theme.AccentGlow) },
        "smooth"
    )
end

function state:_minWidth()
    if self.minimal then
        return 100
    end
    local w = 40
    if self.icon then
        w += 22
    end
    w += tab.textWidth(self.window.theme.Font, 16, range.resolve(self.name))
    w += 12
    w += tab.textWidth(self.window.theme.Font, 15, self:_format(self.value))
    return math.max(w, 160)
end

name(state)
flag(state)

return state

end)() end,
    [33] = function()local wax,script,require=ImportGlobals(33)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Statistic"

local constants = script.Parent.Parent.utilities

local Stat = require(constants.Parent.core.state)
local motion = require(constants.motion)
local tab = require(script.Parent.Parent.functions)
local props = require(constants.odometer)
local name = require(constants.moveable)
local value = require(constants.constants)
local description = require(script.Parent.description)
local prefix = require(constants.locale)
local suffix = require(constants.log)

local element = value.statAccents

local frame = NumberSequence.new({
    NumberSequenceKeypoint.new(0, 0.85),
    NumberSequenceKeypoint.new(1, 0),
})

local valueLabel = 38

-- [[The stat's accent gradient drifts to its new angle on the shared hero
-- spec — the same surface language as the window's own gradient.]]
local baseline = "emphasized"

-- [[Letter presentation: a stat whose value is not a number drops its numeric
-- readouts and carries the value's first letter as a glyph badge instead. The
-- glyph answer order is the value itself, then the stat's name, then "A", so
-- a badge always has exactly one letter — never an icon, digit or symbol.
-- Byte iteration keeps this free of any utf8 dependency.]]
local function firstGlyphChar(candidate)
    if candidate == nil then
        return nil
    end
    local plain = (tostring(candidate):gsub("%s+", ""))
    for index = 1, #plain do
        local char = plain:sub(index, index)
        if char:match("%a") then
            return string.upper(char)
        end
    end
    return nil
end

local function glyphText(value, fallbackName)
    return firstGlyphChar(value) or firstGlyphChar(fallbackName) or "A"
end

local function a14(a15)
    return a15.fill.Keypoints[1].Value
end

local function a16(a17)
    if a17 > 0 then
        return string.format("+%.1f%%", a17)
    elseif a17 < 0 then
        return string.format("%.1f%%", a17)
    else
        return "0%"
    end
end

local function a18(a19)
    if typeof(a19) ~= "number" then
        return tostring(a19)
    end
            local a20 = math.round(math.abs(a19))
    local s = string.format("%.0f", a20)
    local a21 = s:reverse():gsub("%d%d%d", "%0,"):reverse()
    if a21:sub(1, 1) == "," then
        a21 = a21:sub(2)
    end
        return if a19 < 0 and a20 ~= 0 then "-" .. a21 else a21
end

local function a22(a23)
        local a24 = math.round(a23)
    if a24 > 0 then
        return "+" .. a18(a24)
    elseif a24 < 0 then
        return a18(a24)
    else
        return "0"
    end
end

function state.new(tab, a25)
    a25 = if typeof(a25) == "table" then a25 else {}

    -- [[Non-numeric values switch the card to letter mode: the numeric
    -- readouts are skipped and the first letter of the value (or of the stat's
    -- own name) renders as a glyph badge instead. String values adopted the
    -- same passthrough the description already has, so "Default" reads as the
    -- letter D rather than a number roll. `letter = false` opts a text value
    -- out of the badge: the whole value then renders as a text readout.]]
    local desiredValue = if (a25.value or a25.Value) ~= nil then (a25.value or a25.Value) else 0
    local numeric = type(desiredValue) == "number"
    local letterMode = if (a25.letter ~= nil or a25.Letter ~= nil)
        then (a25.letter or a25.Letter) == true
        else not numeric

    local resolvedIcon = tab.window:ResolveIcon(a25.icon or a25.Icon)

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        name = a25.name or a25.Name or "Statistic",
        icon = resolvedIcon,
        description = a25.description or a25.Description,

        value = desiredValue,
                        _hasValue = (a25.value or a25.Value) ~= nil,

        numberEasing = if (a25.numberEasing ~= nil)
            then a25.numberEasing
            elseif a25.NumberEasing ~= nil then a25.NumberEasing
            else true,

        changeMode = a25.changeMode or a25.ChangeMode or "percentage",         changeBaseline = a25.changeBaseline or a25.ChangeBaseline or "previous",

        prefix = a25.prefix or a25.Prefix or "",
        suffix = a25.suffix or a25.Suffix or "",

                        compact = a25.compact or a25.Compact or tab.compact or false,
        display = (a25.display or a25.Display or "value"),
        -- [[The glyph badge replaces the numeric readouts for text values and
        -- whenever the caller asks for a letter explicitly. A provided icon
        -- keeps the normal leading icon.]]
        letter = letterMode,
        -- [[A text value that opts out of the badge (`letter = false`) reads
        -- as a plain text readout: one label carrying the whole value. The
        -- odometer stays out of it — it is a digit machine, so a word would
        -- cost it a label per character.]]
        textValue = not numeric and not letterMode,
        _initialValue = if (a25.value or a25.Value) ~= nil
            then (a25.value or a25.Value)
            else nil,
        _lastChange = 0,
    }, state)

    if self.compact then
        self:_buildCompact()
    else
        self:_buildFull()
    end


    return self
end

function state:_buildFull()
    local window = self.window

    self.main = window:Create("Frame", {
        Size = UDim2.new(1, -20, 0, 70),
        BorderSizePixel = 0,
        Name = self.name,
        ZIndex = 5,

        BackgroundTransparency = 1,

        Parent = self.tab.tabPage,
    }, { BackgroundColor3 = "StatBackground", BackgroundTransparency = "ElementTransparency" })

    window:Create("UICorner", {
        Parent = self.main,
    }, { CornerRadius = "ElementCornerRadius" })

    self.stroke = window:Create("UIStroke", {
        Transparency = 1,
        Color = Color3.fromRGB(255, 255, 255),

        Parent = self.main,
    }, { Transparency = "ElementStrokeTransparency" })

    self.strokeGradient = window:Create("UIGradient", {
        Rotation = 2,
        Offset = Vector2.new(0, 0.5),
        Color = element.neutral.stroke,
        Transparency = frame,

        Parent = self.stroke,
    })

    -- [[A description grows the card before the readouts are built, so the
    -- [[bottom-anchored value and change hosts ride up above the line while
    -- [[the title keeps its own top-left corner.]]
    description.attach(self)

    self.titleContainer = window:Create("Frame", {
        Size = UDim2.new(1, -30, 0, 22),
        Position = UDim2.fromOffset(15, 12),
        BorderSizePixel = 0,
        LayoutOrder = -1,
        BackgroundTransparency = 1,
        ZIndex = 5,

        Parent = self.main,
    })

    self.gradientContainer = window:Create("Frame", {
        Size = UDim2.fromScale(1, 1),
        Position = UDim2.fromOffset(0, 0),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        ZIndex = 5,

        Parent = self.main,
    })

    self.glow = window:CreateGlow(self.main, a14(element.neutral), 10, 1)

    self.title = window:Create("TextLabel", {
        Text = prefix.t(self.name),

        Size = UDim2.new(1, if self.icon then -30 else 0, 0, 16),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,
        ZIndex = 10,

        TextTransparency = 1,
        Parent = self.titleContainer,
    }, { TextColor3 = "ContentColor", FontFace = "TitleFont" })

    self.mainGradient = window:Create("UIGradient", {
        Rotation = 2,
        Offset = Vector2.new(0, 0.5),
        Color = element.neutral.fill,
        Transparency = frame,

        Parent = self.gradientContainer,
    })

    window:Create("UICorner", {
        Parent = self.gradientContainer,
    }, { CornerRadius = "ElementCornerRadius" })

    if self.icon then
        self.iconLabel = window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(22, 22),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            ZIndex = 10,

            ImageTransparency = 1,
            Parent = self.titleContainer,
        }, { ImageColor3 = "ContentColor" })
    end

    self.containerLayout = window:Create("UIListLayout", {
        Padding = UDim.new(0, 8),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.titleContainer,
    })

    -- [[Letter mode: a single glyph badge replaces the bottom-anchored value
    -- and change readouts, so a text-named value still reads as one character
    -- (never an icon, digit or symbol — just the letter the value starts
    -- with). Everything above the line keeps the normal stat recipe.]]
    if self.letter then
        self.letterHost = window:Create("Frame", {
            AnchorPoint = Vector2.new(0, 1),
            Position = UDim2.new(0, 15, 1, -12 - (self._descriptionExtra or 0)),
            Size = UDim2.new(1, -30, 0, 22),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            ZIndex = 10,

            Parent = self.main,
        })

        self.letterGlyph = window:Create("TextLabel", {
            Text = glyphText(self.value, self.name),
            Size = UDim2.new(1, 0, 0, 22),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            TextSize = 20,
            TextXAlignment = Enum.TextXAlignment.Center,
            TextYAlignment = Enum.TextYAlignment.Center,
            TextTransparency = 1,
            ZIndex = 11,

            Parent = self.letterHost,
        }, { TextColor3 = "TitlingColor", FontFace = "TitleFont" })

        return
    end

    -- [[Text readout: the same slot as the badge, but carrying the whole
    -- value. No change readout rides along — a percentage delta has no
    -- meaning for a word — and no odometer is built, so a text stat is
    -- cheaper than either of the numeric paths.]]
    if self.textValue then
        self.textLabel = window:Create("TextLabel", {
            Text = self:_formatValue(self.value),
            AnchorPoint = Vector2.new(0, 1),
            Position = UDim2.new(0, 15, 1, -12 - (self._descriptionExtra or 0)),
            Size = UDim2.new(1, -30, 0, 18),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            TextSize = 17,
            TextXAlignment = Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Center,
            TextTruncate = Enum.TextTruncate.AtEnd,
            TextTransparency = 1,
            ZIndex = 10,

            Parent = self.main,
        }, { TextColor3 = "ContentColor", FontFace = "Font" })

        return
    end

    self.valueHost = window:Create("Frame", {
        AnchorPoint = Vector2.new(0, 1),
        Position = UDim2.new(0, 15, 1, -12 - (self._descriptionExtra or 0)),
        Size = UDim2.new(0.5, -20, 0, 18),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ZIndex = 10,

        Parent = self.main,
    })

    self.changeHost = window:Create("Frame", {
        AnchorPoint = Vector2.new(1, 1),
        Position = UDim2.new(1, -15, 1, -12 - (self._descriptionExtra or 0)),
        Size = UDim2.new(0.5, -20, 0, 14),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ZIndex = 10,

        Parent = self.main,
    })

    self.valueOdo = props.new(window, self.valueHost, {
        textSize = 17,
        alignment = Enum.HorizontalAlignment.Left,
        transparency = 1,
    })
    self.valueOdo:snap(self:_formatValue(self.value))

    self.changeOdo = props.new(window, self.changeHost, {
        textSize = 13,
        alignment = Enum.HorizontalAlignment.Right,
        transparency = 1,
    })
    self.changeOdo:snap(self:_formatChange(0))

    self._accentGradients = { self.strokeGradient, self.mainGradient }
end

function state:_buildCompact()
    local window = self.window

    local a26 = self.tab.compact or false

    self.main = window:Create("Frame", {
        Name = self.name,
        Size = if a26 then UDim2.fromOffset(0, valueLabel) else UDim2.new(1, -20, 0, valueLabel),
        AutomaticSize = if a26 then Enum.AutomaticSize.X else Enum.AutomaticSize.None,
        ClipsDescendants = true,
        BorderSizePixel = 0,
        ZIndex = 5,

        BackgroundTransparency = 1,
        Parent = self.tab.tabPage,
    }, { BackgroundColor3 = "StatBackground", BackgroundTransparency = "ElementTransparency" })

    window:Create("UICorner", {
        Parent = self.main,
    }, { CornerRadius = "ElementCornerRadius" })

    self.stroke = window:Create("UIStroke", {
        Transparency = 1,
        Color = Color3.fromRGB(255, 255, 255),

        Parent = self.main,
    }, { Transparency = "ElementStrokeTransparency" })

    self.strokeGradient = window:Create("UIGradient", {
        Rotation = 2,
        Offset = Vector2.new(0, 0.5),
        Color = element.neutral.stroke,
        Transparency = frame,

        Parent = self.stroke,
    })

    if a26 then
        window:Create("UIFlexItem", {
            FlexMode = Enum.UIFlexMode.Fill,
            Parent = self.main,
        })
    end

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalFlex = Enum.UIFlexAlignment.Fill,

        Parent = self.main,
    })

    self.card = window:Create("Frame", {
        AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, valueLabel),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        ZIndex = 5,

        BackgroundTransparency = 1,
        Parent = self.main,
    })

    window:Create("UICorner", {
        Parent = self.card,
    }, { CornerRadius = "ElementCornerRadius" })

    self.mainGradient = window:Create("UIGradient", {
        Rotation = 2,
        Offset = Vector2.new(0, 0.5),
        Color = element.neutral.fill,
        Transparency = frame,

        Parent = self.card,
    })

    window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, 12),
        PaddingRight = UDim.new(0, 12),

        Parent = self.card,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalFlex = Enum.UIFlexAlignment.SpaceBetween,
        Padding = UDim.new(0, 8),

        Parent = self.card,
    })

    self.titleContainer = window:Create("Frame", {
        AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, 15),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        LayoutOrder = 0,
        ZIndex = 6,

        Parent = self.card,
    })

    window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Shrink,
        Parent = self.titleContainer,
    })

    window:Create("UIListLayout", {
        Padding = UDim.new(0, 6),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.titleContainer,
    })

    if self.icon then
        self.iconLabel = window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(18, 18),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            LayoutOrder = 0,
            ZIndex = 6,

            ImageTransparency = 1,
            Parent = self.titleContainer,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = window:Create("TextLabel", {
        Text = prefix.t(self.name),
        Size = UDim2.fromOffset(0, 15),
        AutomaticSize = Enum.AutomaticSize.X,
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,
        ZIndex = 6,

        TextTransparency = 1,
        Parent = self.titleContainer,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })
    window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Shrink,
        Parent = self.title,
    })

    if self.textValue then
        -- [[Text readout in the compact card: the whole value in one label.
        -- It joins the card's own flex row directly rather than sitting in
        -- the odometer's host frame — an AutomaticSize host with no layout
        -- inside it measures zero and would clip the word away.]]
        self.textLabel = window:Create("TextLabel", {
            Text = self:_formatValue(self.value),
            Size = UDim2.fromOffset(0, 15),
            AutomaticSize = Enum.AutomaticSize.X,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            LayoutOrder = 1,
            TextSize = 15,
            TextXAlignment = Enum.TextXAlignment.Right,
            TextYAlignment = Enum.TextYAlignment.Center,
            TextTruncate = Enum.TextTruncate.AtEnd,
            TextTransparency = 1,
            ZIndex = 6,

            Parent = self.card,
        }, { TextColor3 = "ContentColor", FontFace = "Font" })
    else
        self.readoutHost = window:Create("Frame", {
            AutomaticSize = Enum.AutomaticSize.X,
            Size = UDim2.fromOffset(0, 16),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            LayoutOrder = 1,
            ZIndex = 6,

            Parent = self.card,
        })

        self.readoutOdo = props.new(window, self.readoutHost, {
            textSize = 15,
            alignment = Enum.HorizontalAlignment.Right,
            transparency = 1,
        })
        self.readoutOdo:snap(self.display == "change" and self:_formatChange(0) or self:_formatValue(self.value))
    end

    self._accentGradients = { self.strokeGradient, self.mainGradient }
end

function state:_setAccent(a27, a28)
    local a29 = element[a27]
    self.strokeGradient.Color = a29.stroke
    self.mainGradient.Color = a29.fill
    if self.glow then
        self.glow.Color = a14(a29)
    end

        local a30 = if a28 == 1 then 14 elseif a28 == -1 then -10 else 2
    local a31 = if a28 == 1
        then Vector2.new(0.04, 0.5)
        elseif a28 == -1 then Vector2.new(-0.04, 0.5)
        else Vector2.new(0, 0.5)
    for _, a32 in self._accentGradients do
        motion.tween(a32, { Rotation = a30, Offset = a31 }, baseline)
    end
end

function state:_formatValue(a33)
    return self.prefix .. a18(a33) .. self.suffix
end

function state:_formatChange(a34)
    if self.changeMode == "absolute" then
        return a22(a34)
    else
        return a16(a34)
    end
end

function state:_showValue(a35, a36, a37, a38)
    if self.numberEasing and not a38 then
        a35:to(self:_formatValue(a36), a36 >= a37)
    else
        a35:snap(self:_formatValue(a36))
    end
end

function state:_updateFullReadouts(a39, a40, a41, a42, a43, a44)
    self:_showValue(self.valueOdo, a39, a40, a44)

    if a43 then
        self.changeOdo:snap(a43)
        self._lastChange = 0
    elseif self.numberEasing and not a42 then
        self.changeOdo:to(self:_formatChange(a41), a41 >= self._lastChange)
        self._lastChange = a41
    else
        self.changeOdo:snap(self:_formatChange(a41 or 0))
        self._lastChange = a41 or 0
    end
end

function state:_updateCompactReadout(a45, a46, a47, a48, a49, a50)
    if self.display == "change" then
        if a49 then
            self.readoutOdo:snap(a49)
            self._lastChange = 0
        elseif self.numberEasing and not a48 then
            self.readoutOdo:to(self:_formatChange(a47), a47 >= self._lastChange)
            self._lastChange = a47
        else
            self.readoutOdo:snap(self:_formatChange(a47 or 0))
            self._lastChange = a47 or 0
        end
    else         self:_showValue(self.readoutOdo, a45, a46, a50)
    end
end

function state:Set(a51)
    -- [[Letter mode never builds the numeric readouts, so a Set() there keeps
    -- the glyph badge in step instead of driving odometers that do not exist.]]
    if self.letter then
        self.value = a51
        self._hasValue = a51 ~= nil
        if self.letterGlyph then
            self.letterGlyph.Text = glyphText(a51, self.name)
        end
        return
    end

    -- [[A text stat has no odometers or baselines either: Set() writes the
    -- value straight into the whole-value label.]]
    if self.textValue then
        self.value = a51
        self._hasValue = a51 ~= nil
        self:_writeTextReadout()
        return
    end

    assert(typeof(a51) == "number", "Statistic:Set() - value must be a number, got " .. typeof(a51))

    local a52 = self.value
    local a53 = not self._hasValue
    self.value = a51
    self._hasValue = true

    if a53 then
        self._initialValue = a51
    end

    local a54 = if self.changeBaseline == "initial" then self._initialValue else a52
    local a55 = a53 or a54 == a51

        local a56, a57, a58, a59
    if a55 then
        a56 = 0
        a58 = "neutral"
        a59 = 0
    elseif a54 == 0 then
        local a60 = if a51 > 0 then "+" else "-"
        local a61 = if self.changeMode == "percentage" then "∞%" else "∞"
        a57 = a60 .. a61
        a58 = if a51 > 0 then "positive" else "negative"
        a59 = if a51 > 0 then 1 else -1
    elseif self.changeMode == "percentage" then
        a56 = ((a51 - a54) / math.abs(a54)) * 100
        a59 = if a56 > 0 then 1 elseif a56 < 0 then -1 else 0
        a58 = if a59 == 1 then "positive" elseif a59 == -1 then "negative" else "neutral"
    else
        a56 = a51 - a54
        a59 = if a56 > 0 then 1 elseif a56 < 0 then -1 else 0
        a58 = if a59 == 1 then "positive" elseif a59 == -1 then "negative" else "neutral"
    end

    self:_setAccent(a58, a59)

    if self.compact then
        self:_updateCompactReadout(a51, a52, a56, a55, a57, a53)
    else
        self:_updateFullReadouts(a51, a52, a56, a55, a57, a53)
    end
end

function state:ResetBaseline(a62)
    -- [[Letter mode has no numeric readouts or baselines; clearing the badge
    -- is the closest equivalent.]]
    if self.letter then
        self._initialValue = a62
        self.value = a62
        self._hasValue = a62 ~= nil
        if self.letterGlyph then
            self.letterGlyph.Text = glyphText(a62, self.name)
        end
        return
    end

    -- [[Same for the text readout: the value is rewritten, there is no
    -- baseline to clear.]]
    if self.textValue then
        self._initialValue = a62
        self.value = a62
        self._hasValue = a62 ~= nil
        self:_writeTextReadout()
        return
    end

    local a63 = self.value
    local a64 = not self._hasValue
    local a65 = if typeof(a62) == "number" then a62 else self.value
    self._initialValue = a65
    self.value = a65
    self._hasValue = true
    self._lastChange = 0

        if self.compact then
        if self.display == "change" then
            self.readoutOdo:snap(self:_formatChange(0))
        else
            self:_showValue(self.readoutOdo, a65, a63, a64)
        end
    else
        self.changeOdo:snap(self:_formatChange(0))
        self:_showValue(self.valueOdo, a65, a63, a64)
    end

    self:_setAccent("neutral", 0)
end

-- [[Text readout update: rewrites the whole-value label. Text stats only —
-- the numeric paths drive their odometers and the badge path its glyph.]]
function state:_writeTextReadout()
    if self.textLabel then
        self.textLabel.Text = self:_formatValue(self.value)
    end
end

-- [[Text readout update: swaps the value's own text. Only meaningful on a
-- stat built in text mode (a text value with letter = false); letter stats
-- keep their badge and numeric stats keep their own Set() path.]]
function state:SetText(text)
    if type(text) == "number" then
        self:Set(text)
        return
    end
    self.value = text
    self._hasValue = text ~= nil
    if self.letterGlyph then
        self.letterGlyph.Text = glyphText(text, self.name)
    else
        self:_writeTextReadout()
    end
end

function state:_setShown(a66, a67)
    local w = self.window

    if self.letter then
        if a66 then
            w:_revealCommon(self, a67)
            w:_reveal(self.gradientContainer, { BackgroundTransparency = 0 }, a67)
            w:_reveal(self.glow, { Transparency = 0.82 }, a67)
            w:_reveal(self.letterGlyph, { TextTransparency = 0 }, a67)
        else
            w:_hideCommon(self, a67)
            w:_reveal(self.gradientContainer, { BackgroundTransparency = 1 }, a67)
            w:_reveal(self.glow, { Transparency = 1 }, a67)
            w:_reveal(self.letterGlyph, { TextTransparency = 1 }, a67)
        end
        return
    end

    if self.compact then
        w:_reveal(
            self.main,
            { BackgroundTransparency = if a66 then (self.window:_elementBackgroundTransparency(self)) else 1 },
            a67
        )
        w:_reveal(
            self.stroke,
            { Transparency = if a66 then self.window:_elementStrokeTransparency(self, false) else 1 },
            a67
        )
        w:_reveal(self.card, { BackgroundTransparency = if a66 then 0 else 1 }, a67)
        w:_reveal(self.title, { TextTransparency = if a66 then 0 else 1 }, a67)
        if self.iconLabel then
            w:_reveal(self.iconLabel, { ImageTransparency = if a66 then 0 else 1 }, a67)
        end
        if self.readoutOdo then
            self.readoutOdo:reveal(if a66 then 0 else 1, a67)
        else
            w:_reveal(self.textLabel, { TextTransparency = if a66 then 0 else 1 }, a67)
        end
        return
    end

    if a66 then
        w:_revealCommon(self, a67)
        w:_reveal(self.gradientContainer, { BackgroundTransparency = 0 }, a67)
        w:_reveal(self.glow, { Transparency = 0.82 }, a67)
        if self.valueOdo then
            self.valueOdo:reveal(0, a67)
            self.changeOdo:reveal(0, a67)
        else
            -- Text readout: one label where the value/change pair would be.
            w:_reveal(self.textLabel, { TextTransparency = 0 }, a67)
        end
    else
        w:_hideCommon(self, a67)
        w:_reveal(self.gradientContainer, { BackgroundTransparency = 1 }, a67)
        w:_reveal(self.glow, { Transparency = 1 }, a67)
        if self.valueOdo then
            self.valueOdo:reveal(1, a67)
            self.changeOdo:reveal(1, a67)
        else
            w:_reveal(self.textLabel, { TextTransparency = 1 }, a67)
        end
    end
end

function state:_minWidth()
    local w = 24 + 8
    if self.icon then
        w += 24
    end
    w += tab.textWidth(self.window.theme.Font, 15, prefix.resolve(self.name))
    local a68 = if self.display == "change" then self:_formatChange(0) else self:_formatValue(self.value)
    w += tab.textWidth(self.window.theme.Font, 15, a68)
    return w
end

name(state)

return state

end)() end,
    [34] = function()local wax,script,require=ImportGlobals(34)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Tab"

local constants = script.Parent.Parent.utilities

local Tab = require(constants.Parent.core.state)
local motion = require(constants.motion)
local window = require(constants.ordering)
local props = require(constants.HapticEngine)
local name = require(script.Parent.Parent.components.search)
local icon = require(script.Parent.Parent.components.tabSelector)
local flagNames = require(script.Parent.Parent.functions.flagNames)

local function customOrder(window, forgetState)
    for _, element in forgetState do
        if element.__type == "Group" or element.__type == "CollapsibleGroup" then
            customOrder(window, element.elements)
        else
            window:_unregisterControl(element)
        end

                if element.connections then
            for _, neglectSelector in element.connections do
                window:Disconnect(neglectSelector)
            end
            element.connections = nil
        end

                if element._dragConnection then
            element._dragConnection:Disconnect()
            element._dragConnection = nil
        end
        if element._outsideClickConn then
            window:Disconnect(element._outsideClickConn)
            element._outsideClickConn = nil
        end
    end
end

-- [[Tab state changes ride the motion service's shared specs: `normal` for
-- the pill's selected/rest states, `snappy` for the page hand-off.]]
local element, page = "normal", "snappy"

local function selector(elements)
    if elements.mode == "sidebar" then
        return UDim2.new(1, -35, 1, 0)
    end
    return UDim2.new(1, -20, 1, 0)
end

local function a15(a16)
    if a16.mode == "sidebar" then
        return UDim2.new(0, 35, 0, 0)
    end
    return UDim2.new(0, 0, 0, 0)
end

function state.new(window, a17)
    a17 = if typeof(a17) == "table" then a17 else {}
    -- [[Validate the dependency before resolving the tab icon so malformed tab]]
    -- [[construction reports the actual missing Window argument.]]
    window = assert(window, "Missing argument #1 (Window expected)")

    local self = setmetatable({
        window = window,
        name = a17.name or a17.Name,
        icon = window:ResolveIcon(a17.icon or a17.Icon),
        neglectSelector = a17.neglectSelector or a17.NeglectSelector or false,
        customOrder = a17.customOrder or a17.CustomOrder or 0,
                forgetState = a17.forgetState or a17.ForgetState or false,
        _isSettingsTab = a17.isSettingsTab == true or a17.IsSettingsTab == true,
        deriveFlagFromName = flagNames.deriveFlagFromName,

        elements = {},
        connections = {},     }, state)

        assert(self.name or self.icon, "A tab needs a name or an icon.")

        if not self.neglectSelector then
        icon.build(self, self.window.layout)
    end

        self.tabPage = self.window:Create("ScrollingFrame", {
        Name = self.name,
        Size = selector(self.window.layout),
        Position = a15(self.window.layout),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 0,
        ScrollingDirection = Enum.ScrollingDirection.Y,

        LayoutOrder = self.customOrder or 0,

        Parent = self.window.elements,
    })

    self.tabPageLayout = self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 7),
        FillDirection = Enum.FillDirection.Vertical,
        VerticalAlignment = Enum.VerticalAlignment.Top,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.tabPage,
    })

    self.window:Create("UIPadding", {
        PaddingTop = UDim.new(0, 10),
        PaddingBottom = UDim.new(0, 33),

        Parent = self.tabPage,
    })

    self:_bindSelector()

    return self
end

function state:_bindSelector()
    if self.neglectSelector or not self.topbarItemInteract then
        return
    end
    table.insert(self.connections, self.window:Connect(self.topbarItemInteract.MouseButton1Click, function()
        props.click()
        self:Select()
    end))
    table.insert(self.connections, self.window:Connect(self.topbarItemInteract.MouseEnter, function()
        if not self.window:_interactive() then
            return
        end
        if self.window.selectedTab ~= self then
            self:_applyVisual("hover", page)
            self:_spinGradients()
        end
    end))
    table.insert(self.connections, self.window:Connect(self.topbarItemInteract.MouseLeave, function()
        if self.window.selectedTab ~= self then
            self:_applyVisual("unselected", page)
        end
    end))
end

function state:_rebuildSelector()
    if self.neglectSelector then
        return
    end
    for _, a18 in self.connections do
        self.window:Disconnect(a18)
    end
    table.clear(self.connections)
    if self.topbarItem then
        self.window:DestroySubtree(self.topbarItem)
    end
    icon.build(self, self.window.layout)
    self:_bindSelector()
end

function state:_applyVisual(a19, a20)
    if self.neglectSelector or not self.topbarItem then
        return
    end

    local a21 = icon.states[self.window.layout.mode][a19]
    if not a21 then
        return
    end

    icon.applyVisual(self, a21, a20)
end

function state:_spinGradients()
    if self.neglectSelector then
        return
    end

    for _, a23 in { self.topbarItemGradient, self.topbarItemStrokeGradient } do
        a23.Rotation = 90 - 360         Tab.tweenService:Create(a23, motion.spec("spin"), { Rotation = 90 }):Play()
    end
end

function state:Select(a24)
            if self.window._searching then
        name.close(self.window, { showTabs = true, jumpTo = false })
    end

    self.window.selectedTab = self
    if self.window._setSettingsMode then
        self.window:_setSettingsMode(self._isSettingsTab == true)
    elseif self.window._applySettingsLayout then
        self.window:_applySettingsLayout(self == self.window.rfSettings)
    end
    -- [[Settings tab content is created lazily (after construction):]]
    -- [[make sure it exists before the page is shown.]]
    if self._isSettingsTab and self.window._constructed and self.window._buildSettingsContent ~= nil then
        self.window:_buildSettingsContent(self)
    end
    self.window:_jumpTo(self.tabPage)

    -- [[A tab nobody has opened yet still has its elements hidden: its page is
    -- [[not the current page of window.elements until the line above, so this
    -- [[is the first frame any of them can be seen. Showing them here is the
    -- [[same instant show the window's reveal used to run for every tab.]]
    if self._elementsPending and not self.window.hidden and not self.window.minimised then
        self.window:_showTabElements(self)
    end

                    local a25 = a24 or not self.window:_interactive()

    if not self.neglectSelector and not a25 then
        self:_applyVisual("selected", element)
    end

    -- [[Topbar mode only (a no-op on the sidebar): the selected pill]]
    -- [[must stay in view as the user moves through a long strip.]]
    self.window:_scrollSelectedTabIntoView(not a25)

    for _, tab in self.window.tabs do
        if tab ~= self.window.selectedTab then
            tab:Deselect(a25)
        end
    end

    if self.window.settingsAction then
        local actionTransparency = if self._isSettingsTab then 0 else 0.6
        if a25 then
            self.window.settingsAction.iconLabel.ImageTransparency = actionTransparency
        else
            motion.tween(self.window.settingsAction.iconLabel, { ImageTransparency = actionTransparency }, "snappy")
        end
    end
end

function state:Deselect(a26)
    if not self.neglectSelector and not a26 then
        self:_applyVisual("unselected", element)
    end
end

function state:_register(element)
    table.insert(self.elements, element)
    window(element, #self.elements * 10)
    self.window:_restoreLate(element)

    if not self.window.hidden and not self.window.minimised and self.window.selectedTab == self then
        -- Controls added after the startup shell is visible should join it
        -- with their normal reveal animation instead of popping in instantly.
        -- Window:_constructionCheckpoint paces these registrations in small
        -- budget-limited batches while the initial host build is still active.
        element:_setShown(true, true)
    else
        -- [[The element stayed hidden with the window: remember that this tab
        -- [[has something to show when it is next opened.]]
        self._elementsPending = true
    end

    self.window:_constructionCheckpoint(false)
    return element
end

function state:CreateButton(a27)
    return self:_register(require(script.Parent.button).new(self, a27))
end

function state:CreateToggle(a28)
    return self:_register(require(script.Parent.toggle).new(self, a28))
end


function state:CreateSection(a30)
    return self:_register(require(script.Parent.section).new(self, a30))
end

function state:CreateText(a31)
    return self:_register(require(script.Parent.text).new(self, a31))
end

function state:CreateChangelog(a32)
    return self:_register(require(script.Parent.changelog).new(self, a32))
end

function state:CreateDivider(a32)
    return self:_register(require(script.Parent.divider).new(self, a32))
end

function state:CreateStat(a35)
    return self:_register(require(script.Parent.stat).new(self, a35))
end

function state:CreateSlider(a36)
    return self:_register(require(script.Parent.slider).new(self, a36))
end

function state:CreateDropdown(a37)
    return self:_register(require(script.Parent.dropdown).new(self, a37))
end

function state:CreateInput(a38)
    return self:_register(require(script.Parent.input).new(self, a38))
end

function state:CreateCollapsibleGroup(props)
    return self:_register(require(script.Parent.collapsibleGroup).new(self, props))
end

function state:CreateGroup(a41)
    return self:_register(require(script.Parent.group).new(self, a41))
end

function state:_moveElement(element, a42)
    local a43 = table.find(self.elements, element)
    if not a43 then
        return
    end
    table.remove(self.elements, a43)
    a42 = math.clamp(a42, 1, #self.elements + 1)
    table.insert(self.elements, a42, element)
    for i, el in self.elements do
        window(el, i * 10)
    end
end

function state:Remove()
    local window = self.window

            if window._searching then
        name.close(window, { showTabs = true, jumpTo = window.selectedTab and window.selectedTab.tabPage })
    end

    local a44 = table.find(window.tabs, self)
    if a44 then
        table.remove(window.tabs, a44)
    end

        if window.selectedTab == self then
        window.selectedTab = nil
        -- [[Prefer a fallback from the same rail so removing the selected tab]]
        -- [[cannot yank the user out of settings mode (or into it).]]
        local fallback
        for _, tab in ipairs(window.tabs) do
            if not tab.neglectSelector then
                fallback = fallback or tab
                if window:_railGroup(tab) == window:_railGroup(self) then
                    fallback = tab
                    break
                end
            end
        end
        if fallback then
            fallback:Select()
        end
    end

            customOrder(window, self.elements)

            for _, a45 in self.connections do
        window:Disconnect(a45)
    end
    self.connections = {}

    if self.topbarItem then
        window:DestroySubtree(self.topbarItem)
    end
    if self.tabPage then
        window:DestroySubtree(self.tabPage)
    end
    -- [[The removed row may have been the widest: re-derive the]]
    -- [[responsive rail's width from the rows that remain (no-op elsewhere).]]
    window:_applyContentRailWidth()
    self.topbarItem = nil
    self.tabPage = nil
    self.elements = {}
end

return state

end)() end,
    [35] = function()local wax,script,require=ImportGlobals(35)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "TabSection"

local constants = require(script.Parent.Parent.utilities.locale)
local TabSection = require(script.Parent.Parent.utilities.log)

local tab = 20

local props = 6

local name = 3

local function icon(window)
    for _, tab in window.tabs do
        if not tab.neglectSelector then
            return true
        end
    end
    return #window.tabSections > 0
end

function state.new(window, element)
    element = if typeof(element) == "table" then element else {}

    local self = setmetatable({
        window = assert(window, "Missing argument #1 (Window expected)"),
        name = element.name or element.Name or "Section",
        icon = window:ResolveIcon(element.icon or element.Icon),
    }, state)

    if window.layout.mode ~= "sidebar" then
        if not window._warnedTabSection then
            window._warnedTabSection = true
            TabSection.warn("Astra: Window:CreateSection needs the sidebar layout; it does nothing on the top strip.")
        end
        self.inert = true
        return self
    end

    self.main = window:Create("Frame", {
        Name = self.name,
        Size = UDim2.new(1, -tab * 2, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,

        Visible = false,
        Parent = window.tabList,
    })

    self.padding = window:Create("UIPadding", {
        PaddingTop = UDim.new(0, if icon(window) then props else 0),
        PaddingBottom = UDim.new(0, name),

        Parent = self.main,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        Padding = UDim.new(0, 6),
        SortOrder = Enum.SortOrder.LayoutOrder,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        VerticalAlignment = Enum.VerticalAlignment.Top,

        Parent = self.main,
    })

    if self.icon then
        self.iconLabel = window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(16, 16),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            LayoutOrder = 0,

            ImageTransparency = 1,
            Parent = self.main,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = window:Create("TextLabel", {
        Text = constants.t(self.name),

        Size = UDim2.new(1, if self.icon then -22 else 0, 0, 15),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Bottom,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,

        TextTransparency = 1,
        Parent = self.main,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    return self
end

function state:_setShown(frame, a10)
    if not self.main then
        return
    end
    local w = self.window
    w:_reveal(self.title, { TextTransparency = if frame then 0.6 else 1 }, a10)
    if self.iconLabel then
        w:_reveal(self.iconLabel, { ImageTransparency = if frame then 0.65 else 1 }, a10)
    end
end

function state:_setVisible(a11)
    if self.main then
        self.main.Visible = a11
    end
end

function state:Remove()
    local a12 = table.find(self.window.tabSections, self)
    if a12 then
        table.remove(self.window.tabSections, a12)
    end
    self.window:DestroySubtree(self.main)
    self.main = nil
end

return state

end)() end,
    [36] = function()local wax,script,require=ImportGlobals(36)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Text"

local constants = require(script.Parent.Parent.utilities.moveable)
local Text = require(script.Parent.Parent.utilities.locale)

-- [[Resting transparency of body text shown by this control.]]
local restingTextTransparency = 0.45

function state.new(tab, element)
    element = if typeof(element) == "table" then element else {}

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        name = tostring(element.name or element.Name or ""),
        text = tostring(element.text or element.Text or ""),
        icon = tab.window:ResolveIcon(element.icon or element.Icon),
    }, state)

            self.main = self.window:Create("Frame", {
        Size = UDim2.new(1, -20, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BorderSizePixel = 0,
        Name = if self.name ~= "" then self.name else "Text",
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),

                BackgroundTransparency = 1,
        Parent = self.tab.tabPage,
    }, { BackgroundTransparency = "ElementTransparency" })

    self.stroke = self.window:StyleElementBody(self.main)

    self.window:Create("UIPadding", {
        PaddingTop = UDim.new(0, 14),
        PaddingBottom = UDim.new(0, 14),
        PaddingLeft = UDim.new(0, 20),
        PaddingRight = UDim.new(0, 20),

        Parent = self.main,
    })

    self.window:Create("UIListLayout", {
        Padding = UDim.new(0, 6),
        SortOrder = Enum.SortOrder.LayoutOrder,

        Parent = self.main,
    })

        self.titleRow = self.window:Create("Frame", {
        Name = "Title",
        Size = UDim2.new(1, 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        LayoutOrder = 1,

        Parent = self.main,
    })

    self.window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        Padding = UDim.new(0, 6),
        SortOrder = Enum.SortOrder.LayoutOrder,
        VerticalAlignment = Enum.VerticalAlignment.Top,

        Parent = self.titleRow,
    })

    if self.icon then
        self.iconLabel = self.window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,

                        ImageTransparency = 1,
            Parent = self.titleRow,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = self.window:Create("TextLabel", {
        Text = Text.t(self.name),
        Size = UDim2.new(1, if self.icon then -22 else 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        RichText = true,
        TextSize = 16,
        TextWrapped = true,
        TextXAlignment = Enum.TextXAlignment.Left,
        LayoutOrder = 1,

                TextTransparency = 1,
        Parent = self.titleRow,
    }, { TextColor3 = "TitlingColor", FontFace = "Font" })

    self.body = self.window:Create("TextLabel", {
        Text = Text.t(self.text),
        Size = UDim2.new(1, 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y,
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        RichText = true,
        TextSize = 14,
        TextWrapped = true,
        TextXAlignment = Enum.TextXAlignment.Left,
        LayoutOrder = 2,

                TextTransparency = 1,
        Parent = self.main,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    self:_applyPresence()

    return self
end

function state:_applyPresence()
    self.titleRow.Visible = self.name ~= "" or self.icon ~= nil
    self.body.Visible = self.text ~= ""
end

function state:Set(icon)
    self.text = tostring(icon)
    self.window:_bindLocale(self.body, "Text", self.text)
    self:_applyPresence()
end

function state:SetTitle(element)
    self.name = tostring(element)
    self.window:_bindLocale(self.title, "Text", self.name)
    self:_applyPresence()
end

function state:_setShown(frame, a11)
        if frame then
        self.window:_revealCommon(self, a11)
    else
        self.window:_hideCommon(self, a11)
    end

    self.window:_reveal(self.body, { TextTransparency = if frame then restingTextTransparency else 1 }, a11)
end

constants(state)

return state

end)() end,
    [37] = function()local wax,script,require=ImportGlobals(37)local ImportGlobals return (function(...)
local state = {}
state.__index = state
state.__type = "Toggle"

local constants = script.Parent.Parent.utilities

local Toggle = require(constants.Parent.core.state)
local tab = require(script.Parent.Parent.functions)
local props = require(constants.moveable)
local name = require(constants.lockable)
local flag = require(constants.locale)
local value = require(constants.HapticEngine)
local baseCard = require(script.Parent.baseCard)
local description = require(script.Parent.description)
-- [[The switch rides the shared motion vocabulary. The knob is the one
-- genuinely mechanical surface in the library, so it gets the short Back
-- overshoot (`settle`) — it reads as a physical slide with a landing rather
-- than a glide. The accent glow trails the knob by a beat (accentTween's
-- delay, rescaled with the speed profile through the service).]]
local motion = require(constants.motion)
local callback = "snappy"
local knobSlide = "settle"

-- [[Switch metrics, in one place. The track and the knob are both pills and
-- [[the knob keeps the same clearance on every side it can see (`switchInset`
-- [[at the parked end, the same above and below), so the two resting states
-- [[are exact mirrors of each other.
--
-- [[The track is also deliberately only as wide as the knob plus its travel.
-- [[With the old 50px track around a 25px knob, "on" parked the knob 3px from
-- [[the right edge and left 22px of empty track on the left: a ratio of
-- [[roughly 7:1 at the same moment the knob looked like it floated. Dropping
-- [[the track to 44px keeps the knob's travel readable while the empty side
-- [[drops to 14px, so neither state reads as lopsided.
--
-- [[Positions are expressed from the track's right edge (scale 1), which is
-- [[how the knob's AnchorPoint (0, 0.5) resolves against its parent.]]
local switchTrackWidth = 44
local switchTrackHeight = 22
local switchKnobWidth = 26
local switchKnobHeight = 18
local switchInset = 2

local switchKnobActive = UDim2.new(1, -(switchKnobWidth + switchInset), 0.5, 0)
local switchKnobRest = UDim2.new(1, -(switchTrackWidth - switchInset), 0.5, 0)

function state.new(tab, element)
    element = if typeof(element) == "table" then element else {}

    local self = setmetatable({
        tab = assert(tab, "Missing argument #1 (Tab expected)"),
        window = tab.window,
        name = element.name or element.Name or "Toggle",
        icon = tab.window:ResolveIcon(element.icon or element.Icon),
        description = element.description or element.Description,
        forgetState = element.forgetState or element.ForgetState or tab.forgetState,
        compact = tab.compact or false,
        flag = element.flag
            or element.Flag
            or (
                not (element.forgetState or element.ForgetState or tab.forgetState)
                    and tab.deriveFlagFromName(element.name or element.Name or "Toggle")
                or nil
            ),

        callback = element.callback or element.Callback or function() end,

        value = if (element.value or element.Value) ~= nil then (element.value or element.Value) else false,
    }, state)

    self.window:_registerControl(self)

    if self.compact then
        self:_buildCompact()
    else
        self:_buildFull()
    end

    return self
end

function state:_buildSwitch(frame)
    local window = self.window

    self.functionContainer = window:Create("Frame", {
        BorderSizePixel = 0,
        Size = UDim2.fromOffset(switchTrackWidth, switchTrackHeight),

        BackgroundTransparency = 1,
        Parent = frame,
    }, { BackgroundColor3 = "ToggleTrack" })

    window:Create("UICorner", {
        CornerRadius = UDim.new(1, 0),
        Parent = self.functionContainer,
    })

    self.containerStroke = window:Create("UIStroke", {
        Transparency = 1,
        Parent = self.functionContainer,
    }, { Color = "SurfaceStroke" })

    self.indicator = window:Create("Frame", {
        BorderSizePixel = 0,
        Size = UDim2.fromOffset(switchKnobWidth, switchKnobHeight),
        Position = if self.value then switchKnobActive else switchKnobRest,
        AnchorPoint = Vector2.new(0, 0.5),
        ZIndex = 2,
        BackgroundColor3 = self.value and self.window.theme.AccentColor or self.window.theme.ToggleKnobOff,

        BackgroundTransparency = 1,
        Parent = self.functionContainer,
    })

    window:Create("UICorner", {
        CornerRadius = UDim.new(1, 0),
        Parent = self.indicator,
    })

    self.indicatorStroke = window:Create("UIStroke", {
        Color = self.value and self.window.theme.AccentStroke or Color3.fromRGB(255, 255, 255),
        Transparency = 1,
        Parent = self.indicator,
    })

    self.overlay = window:Create("Frame", {
        BorderSizePixel = 0,
        Size = UDim2.fromScale(1, 1),
        Position = UDim2.fromScale(0, 0),
        AnchorPoint = Vector2.new(0, 0),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        ZIndex = 1,

        BackgroundTransparency = 1,
        Parent = self.functionContainer,
    }, { Visible = "DarkToggleOverlay" })

    window:Create("UICorner", {
        CornerRadius = UDim.new(1, 0),
        Parent = self.overlay,
    })

    self.overlayGradient = window:Create("UIGradient", {
        Rotation = 90,
        Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 30, 30)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(30, 30, 30)),
        }),
        Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0, 1),
            NumberSequenceKeypoint.new(1, 0.35),
        }),

        Parent = self.overlay,
    })

    return self.functionContainer
end

function state:_performToggle()
    value.click()
    self.value = not self.value
    self:_animateIndicator()

    self.window:_runGuarded(self, self.callback, self.value)
    self.window:_persist(self)
end

function state:_buildFull()
    local window = self.window

    baseCard.buildFull(self, {
        height = 41,
        rightPadding = switchTrackWidth + 45,
        titleText = flag.t(self.name),
    })

    self.interact = window:Create("TextButton", {
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 1, 0),
        BorderSizePixel = 0,
        Position = UDim2.fromScale(1, 0.5),
        AnchorPoint = Vector2.new(1, 0.5),
        TextTransparency = 1,
        ZIndex = 10,

        Parent = self.main,
    })

    self:_buildSwitch(self.main)
    -- [[The switch centres in the base region too, so a described card grows
    -- [[under the toggle instead of pushing it down onto the line.]]
    self.functionContainer.Position = UDim2.new(1, -15, description.center(self, 41))
    self.functionContainer.AnchorPoint = Vector2.new(1, 0.5)

    self.window:_wireElementHover(self)

    self.window:ConnectFor(self, self.interact.MouseButton1Click, function()
        motion.tween(self.stroke, { Transparency = 1 }, callback)
        Toggle.tweenService
            :Create(
                self.main,
                motion.spec("snappy"),
                { Size = UDim2.new(1, -26, 0, description.height(self, 41)) }
            )
            :Play()

        self:_performToggle()

        task.wait(0.11)

        if self.window.unloaded then
            return
        end

        Toggle.tweenService
            :Create(
                self.main,
                motion.spec("settle"),
                { Size = UDim2.new(1, -20, 0, description.height(self, 41)) }
            )
            :Play()
        motion.tween(
            self.stroke,
            { Transparency = self.window:_elementStrokeTransparency(self, false) },
            callback
        )
    end)
end

function state:_buildCompact()
    local window = self.window

    self.main, self.stroke, self.interact = window:_buildCompactRow(self.tab, self.name, 10)
    self.hoverOverlay = self.interact

    window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, 15),
        PaddingRight = UDim.new(0, 15),

        Parent = self.interact,
    })

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalFlex = Enum.UIFlexAlignment.SpaceBetween,
        Padding = UDim.new(0, 10),

        Parent = self.interact,
    })

    self.container = window:Create("Frame", {
        AutomaticSize = Enum.AutomaticSize.X,
        Size = UDim2.fromOffset(0, 16),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        LayoutOrder = 0,

        Parent = self.interact,
    })

    window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Shrink,
        Parent = self.container,
    })

    window:Create("UIListLayout", {
        Padding = UDim.new(0, 5),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,

        Parent = self.container,
    })

    if self.icon then
        self.iconLabel = window:Create("ImageLabel", {
            Image = self.icon,
            Size = UDim2.fromOffset(16, 16),
            BorderSizePixel = 0,
            BackgroundTransparency = 1,
            LayoutOrder = 0,

            ImageTransparency = 1,
            Parent = self.container,
        }, { ImageColor3 = "ContentColor" })
    end

    self.title = window:Create("TextLabel", {
        Text = flag.t(self.name),
        Size = UDim2.fromOffset(0, 16),
        AutomaticSize = Enum.AutomaticSize.X,
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd,
        LayoutOrder = 1,

        TextTransparency = 1,
        Parent = self.container,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    window:Create("UIFlexItem", {
        FlexMode = Enum.UIFlexMode.Shrink,
        Parent = self.title,
    })

    self:_buildSwitch(self.interact)
    self.functionContainer.LayoutOrder = 1

    self.window:_wireElementHover(self)

    self.window:ConnectFor(self, self.interact.MouseButton1Click, function()
        motion.tween(self.stroke, { Transparency = 1 }, callback)

        self:_performToggle()

        task.wait(0.11)

        if self.window.unloaded then
            return
        end

        motion.tween(
            self.stroke,
            { Transparency = self.window:_elementStrokeTransparency(self, false) },
            callback
        )
    end)
end

function state:_indicatorGlow()
    if not self.indicatorGlow then
        self.indicatorGlow = self.window:CreateGlow(self.indicator, "AccentColor", 20, 1)
    end
    return self.indicatorGlow
end

function state:_animateIndicator()
    local track = knobSlide
    local glow = if self.value then self:_indicatorGlow() else self.indicatorGlow
    if glow then
        Toggle.tweenService
            :Create(glow, motion.spec("fast"), { Transparency = self.value and self.window.theme.AccentGlow or 1 })
            :Play()
    end
    if self.value then
        Toggle.tweenService
            :Create(self.indicator, motion.spec(track), {
                Position = switchKnobActive,
                BackgroundColor3 = self.window.theme.AccentColor,
                BackgroundTransparency = 0,
            })
            :Play()
        Toggle.tweenService
            :Create(self.indicatorStroke, motion.spec("fast"), { Color = self.window.theme.AccentStroke, Transparency = 0 })
            :Play()
    else
        Toggle.tweenService
            :Create(self.indicator, motion.spec(track), {
                Position = switchKnobRest,
                BackgroundColor3 = self.window.theme.ToggleKnobOff,
                BackgroundTransparency = self.window.theme.ToggleKnobOffTransparency,
            })
            :Play()
        Toggle.tweenService
            :Create(self.indicatorStroke, motion.spec("fast"), { Color = Color3.fromRGB(255, 255, 255), Transparency = 0.7 })
            :Play()
    end
end

local knob = "normal"
local accentBase = TweenInfo.new(0.6, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out, 0, false, 0.35)
local function accentTween()
    return motion.spec(accentBase)
end

function state:_setShown(flag, a16)
    local w = self.window
    if flag then
        w:_revealCommon(self, a16)
        w:_reveal(self.indicator, {
            BackgroundColor3 = self.value and w.theme.AccentColor or w.theme.ToggleKnobOff,
            BackgroundTransparency = self.value and 0 or w.theme.ToggleKnobOffTransparency,
        }, a16, knob)
        w:_reveal(self.indicatorStroke, {
            Color = self.value and w.theme.AccentStroke or Color3.fromRGB(255, 255, 255),
            Transparency = self.value and 0 or 0.7,
        }, a16, knob)
        local shownGlow = if self.value then self:_indicatorGlow() else self.indicatorGlow
        if shownGlow then
            w:_reveal(
                shownGlow,
                { Transparency = self.value and w.theme.AccentGlow or 1 },
                a16,
                accentTween
            )
        end
        w:_reveal(self.overlay, { BackgroundTransparency = 0 }, a16, knob)
        w:_reveal(self.containerStroke, { Transparency = 0.85 }, a16, knob)
        w:_reveal(
            self.functionContainer,
            { BackgroundTransparency = w.theme.ToggleTrackTransparency },
            a16,
            knob
        )
    else
        w:_hideCommon(self, a16)
        w:_reveal(self.indicator, { BackgroundTransparency = 1 }, a16, knob)
        w:_reveal(self.indicatorStroke, { Transparency = 1 }, a16, knob)
        if self.indicatorGlow then
            w:_reveal(self.indicatorGlow, { Transparency = 1 }, a16, accentTween)
        end
        w:_reveal(self.overlay, { BackgroundTransparency = 1 }, a16, knob)
        w:_reveal(self.containerStroke, { Transparency = 1 }, a16, knob)
        w:_reveal(self.functionContainer, { BackgroundTransparency = 1 }, a16, knob)
    end
end

function state:_refreshTheme()
    local t = self.window.theme
    local a17 = "smooth"
    local ts = Toggle.tweenService
    ts:Create(self.functionContainer, motion.spec(a17), { BackgroundTransparency = t.ToggleTrackTransparency }):Play()
    ts:Create(self.indicator, motion.spec(a17), {
        BackgroundColor3 = self.value and t.AccentColor or t.ToggleKnobOff,
        BackgroundTransparency = self.value and 0 or t.ToggleKnobOffTransparency,
    }):Play()
    ts:Create(self.indicatorStroke, motion.spec(a17), {
        Color = self.value and t.AccentStroke or Color3.fromRGB(255, 255, 255),
    }):Play()
    local glow = if self.value then self:_indicatorGlow() else self.indicatorGlow
    if glow then
        ts:Create(glow, motion.spec(a17), { Transparency = self.value and t.AccentGlow or 1 }):Play()
    end
end

function state:_minWidth()
    local w = 30 + 10 + switchTrackWidth
    if self.icon then
        w += 21
    end
    w += tab.textWidth(self.window.theme.Font, 16, flag.resolve(self.name))
    return w
end

props(state)
name(state)

function state:Set(a18, a19)
    local a20 = self.value ~= a18
    self.value = a18

    if a20 then
        self:_animateIndicator()
    end

    if not a19 then
        self.window:_runGuarded(self, self.callback, self.value)
        self.window:_persist(self)
    end
end

return state

end)() end,
    [46] = function()local wax,script,require=ImportGlobals(46)local ImportGlobals return (function(...)

local managerModule = require(script.manager)
local registryModule = require(script.registry)
local defaultsModule = require(script.defaults)
local persistenceModule = require(script.persistence)

local settings = {
    manager = managerModule,
    registry = registryModule,
    defaults = defaultsModule,
    persistence = persistenceModule,
}

function settings.newManager(overrides: { [string]: any }?)
    return managerModule.new(overrides)
end

function settings.readPersisted(key: string, default: any): any
    return persistenceModule.read(key, default)
end

return settings

end)() end,
    [39] = function()local wax,script,require=ImportGlobals(39)local ImportGlobals return (function(...)

local appearance = {}

function appearance.validate(key: string, value: any): (boolean, any)
    if key == "keepOnScreen" or key == "showProfile" or key == "showFullUsername" then
        return type(value) == "boolean", value == true
    end
    if key == "layoutMode" then
        if value == "top" or value == "sidebar" or value == "collapsedSidebar" then
            return true, value
        end
        return false, nil
    end
    if key == "activeSubTab" then
        return type(value) == "number" and value >= 1, math.floor(value)
    end
    return false, nil
end

return appearance

end)() end,
    [40] = function()local wax,script,require=ImportGlobals(40)local ImportGlobals return (function(...)

local behavior = {}

function behavior.validate(key: string, value: any): (boolean, any)
    if key == "toggleKeybind" then
        return typeof(value) == "EnumItem", value
    end
    if key == "mouseOverride" or key == "antiWindowDuplicate" or key == "autoSaveConfig" or key == "autoLoadConfig" then
        return type(value) == "boolean", value == true
    end
    return false, nil
end

return behavior

end)() end,
    [41] = function()local wax,script,require=ImportGlobals(41)local ImportGlobals return (function(...)

local defaults = {}

defaults.values = {
    toggleKeybind = Enum.KeyCode.K,
    mouseOverride = false,
    keepOnScreen = true,
    haptics = false,
    showProfile = false,
    showFullUsername = false,
    antiWindowDuplicate = true,
    layoutMode = "top",
    activeSubTab = 1,
    motionSpeed = "normal",
    autoSaveConfig = true,
    autoLoadConfig = true,
}

function defaults.clone(overrides: { [string]: any }?): { [string]: any }
    local result = table.clone(defaults.values)
    if overrides then
        for key, value in overrides do
            result[key] = value
        end
    end
    return result
end

return defaults

end)() end,
    [42] = function()local wax,script,require=ImportGlobals(42)local ImportGlobals return (function(...)

local registry = require(script.Parent.registry)
local defaults = require(script.Parent.defaults)
local persistence = require(script.Parent.persistence)
local appearance = require(script.Parent.appearance)
local behavior = require(script.Parent.behavior)
local performance = require(script.Parent.performance)

local SettingsManager = {}

SettingsManager.__index = SettingsManager

export type SettingsManager = {
    defaults: { [string]: any },
    persistence: { [any]: (string, any, any) -> () },
    get: (self: SettingsManager, key: string) -> any,
    set: (self: SettingsManager, key: string, value: any) -> boolean,
    reset: (self: SettingsManager, key: string?) -> boolean,
    onChange: (self: SettingsManager, listener: (string, any, any) -> ()) -> (),
    save: (self: SettingsManager) -> boolean,
    load: (self: SettingsManager) -> boolean,
}

local domains = {
    appearance = appearance,
    behavior = behavior,
    performance = performance,
}

function SettingsManager.new(overrides: { [string]: any }?): SettingsManager
    -- [[`values` and `listeners` were never created here, so every method that
    -- touches them (:get, :set, :save, :load, :onChange) raised "attempt to
    -- index nil" the first time a host used a manager directly. The window
    -- keeps its own plain settings table, which is why this went unnoticed.]]
    local self = setmetatable({
        defaults = defaults.clone(overrides),
        values = defaults.clone(overrides),
        listeners = {},
        persistence = {},
    }, SettingsManager)
    return self
end

function SettingsManager:get(key: string): any
    return self.values[key]
end

function SettingsManager:set(key: string, value: any): boolean
    local definition = registry.definition(key)
    if not definition then
        return false
    end

    local domainValidator = domains[definition.domain]
    local ok, validated
    if domainValidator then
        ok, validated = domainValidator.validate(key, value)
    else
        ok, validated = true, value
    end
    if not ok then
        return false
    end

    local oldValue = self.values[key]
    if oldValue == validated then
        return true
    end

    self.values[key] = validated
    for _, listener in self.listeners do
        task.spawn(listener, key, validated, oldValue)
    end
    return true
end

function SettingsManager:reset(key: string?): boolean
    if key then
        if not registry.definition(key) then
            return false
        end
        return self:set(key, defaults.values[key])
    end
    for _, key in registry.keys() do
        self:reset(key)
    end
    return true
end

function SettingsManager:onChange(listener: (string, any, any) -> ())
    table.insert(self.listeners, listener)
end

function SettingsManager:save(): boolean
    return persistence.save(self.values)
end

function SettingsManager:load(): boolean
    local snapshot = table.clone(self.values)
    local ok = persistence.load(self.values)
    if not ok then
        return ok
    end

    -- [[Notify listeners only for values that actually changed, matching the]]
    -- [[(key, newValue, oldValue) contract of :set(). Persisted values are]]
    -- [[written into self.values first; callbacks fire afterwards.]]
    for _, key in registry.keys() do
        local newValue = self.values[key]
        if snapshot[key] ~= newValue then
            for _, listener in self.listeners do
                task.spawn(listener, key, newValue, snapshot[key])
            end
        end
    end
    return ok
end

return SettingsManager

end)() end,
    [43] = function()local wax,script,require=ImportGlobals(43)local ImportGlobals return (function(...)

local motion = require(script.Parent.Parent.utilities.motion)

local performance = {}

function performance.validate(key: string, value: any): (boolean, any)
    if key == "haptics" then
        return type(value) == "boolean", value == true
    end
    if key == "motionSpeed" then
        -- [[Validated against the motion service's own profile list, so the
        -- setting can never name a profile the library does not implement.]]
        if type(value) == "string" and motion.profiles[value] then
            return true, value
        end
        return false, nil
    end
    return false, nil
end

return performance

end)() end,
    [44] = function()local wax,script,require=ImportGlobals(44)local ImportGlobals return (function(...)

local settingsPersistence = require(script.Parent.Parent.utilities.persistenceSettings)
local settingsCache = require(script.Parent.Parent.cache.persistenceCache)

local persistenceWrite = {}

function persistenceWrite.path(): (string, string)
    return settingsPersistence.getSettingsPath()
end

function persistenceWrite.save(persistence: { [string]: any }): boolean
    local ok = settingsPersistence.saveSettings({ settings = persistence })
    settingsCache.invalidateSettings()
    return ok
end

function persistenceWrite.load(values: { [string]: any }): boolean
    return settingsPersistence.loadSettings({ settings = values })
end

function persistenceWrite.read(key: string, default: any): any
    local _, path = settingsPersistence.getSettingsPath()
    local cached = settingsCache.getSettings(path)
    if cached == nil then
        local data = nil :: any?
        local fs = require(script.Parent.Parent.utilities.filesystem)
        local httpService = (require(script.Parent.Parent.core.state)).httpService
        pcall(function()
            if fs.isfile(path) then
                local raw = fs.readfile(path)
                local ok, decoded = pcall(httpService.JSONDecode, httpService, raw)
                if ok and type(decoded) == "table" then
                    data = decoded :: any
                end
            end
        end)
        settingsCache.putSettings(path, data or false)
        cached = settingsCache.getSettings(path)
    end
    if type(cached) == "table" and (cached :: any)[key] ~= nil then
        return (cached :: any)[key]
    end
    return default
end

return persistenceWrite

end)() end,
    [45] = function()local wax,script,require=ImportGlobals(45)local ImportGlobals return (function(...)

local registry = {}

export type SettingDefinition = {
    key: string,
    kind: "boolean" | "keybind" | "enum",
    domain: "appearance" | "behavior" | "performance",
    description: string?,
}

registry.definitions = {
    autoSaveConfig = {
        key = "autoSaveConfig", kind = "boolean", domain = "behavior",
        description = "Automatically saves control values when they change.",
    },
    autoLoadConfig = {
        key = "autoLoadConfig", kind = "boolean", domain = "behavior",
        description = "Restores saved control values on the next startup.",
    },
    toggleKeybind = {
        key = "toggleKeybind",
        kind = "keybind",
        domain = "behavior",
        description = "Keybind used to show/hide the window.",
    },
    mouseOverride = {
        key = "mouseOverride",
        kind = "boolean",
        domain = "behavior",
        description = "Unlocks the cursor while the window is open.",
    },
    keepOnScreen = {
        key = "keepOnScreen",
        kind = "boolean",
        domain = "appearance",
        description = "Keeps the window clamped inside the viewport.",
    },
    haptics = {
        key = "haptics",
        kind = "boolean",
        domain = "performance",
        description = "Toggles haptic feedback on supported devices.",
    },
    showProfile = {
        key = "showProfile",
        kind = "boolean",
        domain = "appearance",
        description = "Shows the account profile in a panel beside the window.",
    },
    showFullUsername = {
        key = "showFullUsername",
        kind = "boolean",
        domain = "appearance",
        -- [[Depends on showProfile: it unmasks values that live on the]]
        -- [[profile card, so it can only be on while the card is. The rule]]
        -- [[is enforced by profilePanel.revealAllowed/setReveal.]]
        description = "Reveals the display name, username, user ID, place ID, job ID and license key on the profile card instead of the masked versions. Requires showProfile to be on.",
    },
    profileSide = {
        key = "profileSide",
        kind = "enum",
        domain = "appearance",
        description = "Which side of the window the profile panel sits on (right or left).",
    },
    antiWindowDuplicate = {
        key = "antiWindowDuplicate",
        kind = "boolean",
        domain = "behavior",
        description = "Prevents creating another window while one is active.",
    },
    layoutMode = {
        key = "layoutMode",
        kind = "enum",
        domain = "appearance",
        description = "Bar layout mode: top, sidebar, or collapsedSidebar.",
    },
    activeSubTab = {
        key = "activeSubTab",
        kind = "enum",
        domain = "appearance",
        description = "Index of the currently selected settings sub-tab.",
    },
    motionSpeed = {
        key = "motionSpeed",
        kind = "enum",
        domain = "performance",
        -- [[Backed by utilities/motion.luau: relaxed/normal/snappy rescale
        -- every transition routed through the motion service (the window's
        -- entrance, the element reveals and hovers, the result flashes), and
        -- instant switches that animation off - targets are applied on the
        -- spot - for accessibility and for machines where the UI is competing
        -- with the game for frame time.]]
        description = "Animation speed: relaxed, normal, snappy, or instant (no animation).",
    },
}

function registry.definition(definitions: string): SettingDefinition?
    return registry.definitions[definitions]
end

function registry.keys(): { string }
    local list = {}
    for key in registry.definitions do
        table.insert(list, key)
    end
    table.sort(list)
    return list
end

return registry

end)() end,
    [50] = function()local wax,script,require=ImportGlobals(50)local ImportGlobals return (function(...)

local imageCache = require(script.moduleCache)

local moduleCache = {
    modules = imageCache,
}

return moduleCache

end)() end,
    [47] = function()local wax,script,require=ImportGlobals(47)local ImportGlobals return (function(...)
local filesystem = require(script.Parent.Parent.utilities.filesystem)
local path = require(script.Parent.Parent.utilities.path)
local variables = require(script.Parent.Parent.core.state)
local constants = require(script.Parent.Parent.utilities.constants)
local windowIcons = require(script.Parent.Parent.images.windowIcons)

export type RewriteMap = { [number]: string }
export type CacheSettledCallback = (failed: number, failedRoles: { string }) -> ()
export type PreloadCallback = CacheSettledCallback
export type AvatarCallback = (uri: string) -> ()
export type OnCachedCallback = (id: number) -> ()
export type ThumbnailEntry = {
    state: string?,
    imageUrl: string?,
}
export type ThumbnailResponse = {
    data: { ThumbnailEntry }?,
}
local imageCache = {}

type RemoteCallback = (uri: string?) -> ()

local cacheRoot = variables.fileSystemManager:getRootFolder()
local cacheFolder = variables.fileSystemManager:getAssetsFolder("images-icon")
local assetResolver = variables.assetResolver

local assetBase = "https://raw.githubusercontent.com/Kira762/astra-version-1/main/assets/"
local headshotPx = 48
local thumbEndpoint =
    "https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds=%d&size=%dx%d&format=Png&isCircular=false"

local manifest: { [number]: string } = {}
local iconManifest = windowIcons.manifest()
for iconId in iconManifest do
    manifest[iconId] = assetBase .. tostring(iconId) .. ".png"
end
local manifestSize = 0
for _ in manifest do
    manifestSize += 1
end

imageCache.rewrites = {} :: RewriteMap
	imageCache.onCached = nil :: OnCachedCallback?
local pngMagic = "\137PNG\r\n\26\n"
local remoteRewrites: { [string]: string } = {}
local remotePending: { [string]: { RemoteCallback } } = {}

local function cacheFile(filePath: string, url: string): string?
        if type(getfenv().getcustomasset) ~= "function" or typeof(filesystem.isfile) ~= "function" then
        return nil
    end
    if not filesystem.isfile(filePath) then
        pcall(filesystem.ensureFolder, cacheRoot)
        pcall(filesystem.ensureFolder, cacheFolder)
        local body = assetResolver:getAssetContentFromUrl(url, filePath, false)
                        if not body or string.sub(body, 1, 8) ~= pngMagic then
            return nil
        end
        if not pcall(filesystem.writefile, filePath, body) then
            return nil
        end
    end
    local ok, uri = pcall(getfenv().getcustomasset, filePath)
    return if ok and type(uri) == "string" then uri else nil
end

local function remoteFilePath(url: string): string
    local hash = 0
    for index = 1, #url do
        hash = (hash * 33 + string.byte(url, index)) % 2147483647
    end
    return path.join(cacheFolder, "icon_" .. tostring(hash) .. ".png")
end

function imageCache.remote(url: string, onReady: RemoteCallback?): string
    local env = getfenv()
    if type(env.getcustomasset) ~= "function" or typeof(filesystem.isfile) ~= "function" then
        if onReady then
            task.defer(onReady, nil)
        end
        return ""
    end

    local rewritten = remoteRewrites[url]
    if rewritten then
        if onReady then
            task.defer(onReady, rewritten)
        end
        return rewritten
    end

    local filePath = remoteFilePath(url)
    if filesystem.isfile(filePath) then
        local ok, uri = pcall(env.getcustomasset, filePath)
        if ok and type(uri) == "string" then
            remoteRewrites[url] = uri
            if onReady then
                task.defer(onReady, uri)
            end
            return uri
        end
    end

    local pending = remotePending[url]
    if not pending then
        pending = {}
        remotePending[url] = pending
        task.spawn(function()
            local uri = cacheFile(filePath, url)
            if uri then
                remoteRewrites[url] = uri
            end
            local callbacks = remotePending[url]
            remotePending[url] = nil
            if callbacks then
                for _, callback in callbacks do
                    pcall(callback, uri)
                end
            end
        end)
    end
    if onReady then
        table.insert(pending, onReady)
    end
    return ""
end

local function avatarPath(userId: number): string
    return path.join(cacheFolder, "avatar_" .. tostring(userId) .. ".png")
end

local function decodeThumbnailUrl(body: string): string?
    local decodeOk, parsed = pcall(function()
        return variables.httpService:JSONDecode(body)
    end)
    if not decodeOk or type(parsed) ~= "table" then
        return nil
    end

    local data = (parsed :: ThumbnailResponse).data
    if type(data) ~= "table" then
        return nil
    end

    local entry = data[1]
    if type(entry) ~= "table" then
        return nil
    end

    local thumbnail = entry :: ThumbnailEntry
    if thumbnail.state == "Completed" and type(thumbnail.imageUrl) == "string" then
        return thumbnail.imageUrl
    end

    return nil
end

local function fetchAvatar(userId: number): string?
    local cdnUrl: string? = nil
    for attempt = 1, 4 do
                local body = assetResolver:getAssetContentFromUrl(
            string.format(thumbEndpoint, userId, headshotPx, headshotPx),
            "avatar:" .. tostring(userId),
            attempt > 1
        )
        if body then
            local imageUrl = decodeThumbnailUrl(body)
            if imageUrl then
                cdnUrl = imageUrl
                break
            end
        end
        if attempt < 4 then
            task.wait(0.3)
        end
    end
    if not cdnUrl then
        return nil
    end

    return cacheFile(avatarPath(userId), cdnUrl)
end

local pendingAvatars: { [number]: { AvatarCallback } } = {}
local failedAvatars: { [number]: boolean } = {}

function imageCache.preload(onSettled: PreloadCallback?): (boolean, number)
    local env = getfenv()
    if type(env.getcustomasset) ~= "function" or typeof(filesystem.isfile) ~= "function" then
        if onSettled then
            task.defer(onSettled, manifestSize, windowIcons.roleList())
        end
        return false, manifestSize
    end

    local rewrites = imageCache.rewrites
	    table.clear(rewrites)
    local function settle()
        if not onSettled then
            return
        end
        local failed = 0
        local failedRoles = {}
        for id in manifest do
            if not rewrites[id] then
                failed += 1
                table.insert(failedRoles, windowIcons.roleForId(id))
            end
        end
        table.sort(failedRoles)
        onSettled(failed, failedRoles)
    end

    local pending, missing = 0, 0
    local spawning = true     for id, url in manifest do
        local filePath = path.join(cacheFolder, tostring(id) .. ".png")
        local uri: string? = nil
        if filesystem.isfile(filePath) then
            local ok, res = pcall(env.getcustomasset, filePath)
            uri = if ok and type(res) == "string" then res else nil
        end
        if uri then
                        rewrites[id] = uri
        else
                        missing += 1
            pending += 1
            task.spawn(function()
                local cached = cacheFile(filePath, url)
                if cached then
                    rewrites[id] = cached
                    if imageCache.onCached then
                                                pcall(imageCache.onCached, id)
                    end
                end
                pending -= 1
                if pending == 0 and not spawning then
                    settle()
                end
            end)
        end
    end

    spawning = false
    if pending == 0 then
        settle()
    end

    return missing == 0, missing
end

function imageCache.avatar(userId: unknown, onReady: AvatarCallback?): string
    if type(userId) ~= "number" then
        return ""
    end

        if typeof(filesystem.isfile) ~= "function" then
        return ""
    end

    local filePath = avatarPath(userId)
    if filesystem.isfile(filePath) then
        local ok, uri = pcall(getfenv().getcustomasset, filePath)
        if ok and type(uri) == "string" then
            return uri
        end
    end

        if failedAvatars[userId] then
        return ""
    end

            local waiting = pendingAvatars[userId]
    if waiting then
        if onReady then
            table.insert(waiting, onReady)
        end
        return ""
    end

    if onReady then
        pendingAvatars[userId] = { onReady }
        task.spawn(function()
            local uri = fetchAvatar(userId)
            local callbacks = pendingAvatars[userId]
            pendingAvatars[userId] = nil
            if not uri then
                failedAvatars[userId] = true
            end
            if callbacks then
                for _, callback in callbacks do
                    pcall(callback, uri or "")
                end
            end
        end)
    end

    return ""
end

return imageCache

end)() end,
    [48] = function()local wax,script,require=ImportGlobals(48)local ImportGlobals return (function(...)

local moduleCache = {}

local cache: { [string]: any } = {}

function moduleCache.singleton(key: string, factory: () -> any): any
    local cached = cache[key]
    if cached ~= nil then
        return cached
    end
    local result = factory()
    cache[key] = result
    return result
end

function moduleCache.get(key: string): any?
    return cache[key]
end

function moduleCache.peek(key: string): boolean
    return cache[key] ~= nil
end

function moduleCache._forget(key: string)
    cache[key] = nil
end

return moduleCache

end)() end,
    [49] = function()local wax,script,require=ImportGlobals(49)local ImportGlobals return (function(...)

local persistenceCache = {}

local cache: { [string]: any } = {}

function persistenceCache.getSettings(key: string): any?
    return cache[key]
end

function persistenceCache.putSettings(key: string, value: any)
    cache[key] = value
end

function persistenceCache.invalidateSettings(key: string?)
    if key then
        cache[key] = nil
    else
        table.clear(cache)
    end
end

return persistenceCache

end)() end,
    [54] = function()local wax,script,require=ImportGlobals(54)local ImportGlobals return (function(...)

local textMetrics = require(script.textMetrics)
local colors = require(script.colors)
local flagNames = require(script.flagNames)

local functions = {}

functions.textWidth = textMetrics.textWidth
functions.textHeight = textMetrics.textHeight
functions.deriveFlagFromName = flagNames.deriveFlagFromName
functions.contrastColor = colors.contrastColor
functions.toColorSequence = colors.toColorSequence
functions.contrastText = colors.contrastText

return functions

end)() end,
    [51] = function()local wax,script,require=ImportGlobals(51)local ImportGlobals return (function(...)
local colors = {}

function colors.contrastColor(color: Color3): Color3
    local luminance = 0.299 * color.R + 0.587 * color.G + 0.114 * color.B
    return if luminance > 0.5 then Color3.fromRGB(0, 0, 0) else Color3.fromRGB(255, 255, 255)
end

function colors.toColorSequence(input: Color3 | ColorSequence): ColorSequence
    return if typeof(input) == "ColorSequence" then input else ColorSequence.new(input)
end

function colors.contrastText(color: Color3): Color3
    local luminance = 0.299 * color.R + 0.587 * color.G + 0.114 * color.B
    return if luminance > 0.6 then Color3.fromRGB(20, 20, 20) else Color3.fromRGB(255, 255, 255)
end

return colors

end)() end,
    [52] = function()local wax,script,require=ImportGlobals(52)local ImportGlobals return (function(...)
local flagNames = {}

local offsetBasis = 2166136261
local prime = 16777619

local function fnv1a(str: string): number
    local hash = offsetBasis
    for i = 1, #str do
        hash = bit32.bxor(hash, string.byte(str, i))
                local low = hash % 65536
        local high = (hash - low) / 65536
        hash = (((high * prime) % 65536) * 65536 + low * prime) % 4294967296
    end
    return hash
end

function flagNames.deriveFlagFromName(name: string): string
    local camelCase = name:gsub("(%S+)", function(w: string): string
        return w:sub(1, 1):upper() .. w:sub(2, -1)
    end):gsub("[^%w]", "")

            if camelCase == "" and name ~= "" then
        return string.format("Flag%08x", fnv1a(name))
    end

    return camelCase
end

return flagNames

end)() end,
    [53] = function()local wax,script,require=ImportGlobals(53)local ImportGlobals return (function(...)
local state = require(script.Parent.Parent.core.state)
local textService = state.textService

local textMetrics = {}

local widthCache: { [string]: number } = {}
local widthCount = 0
local widthMax = 1024
local heightCache: { [string]: number } = {}
local heightCount = 0
local heightMax = 1024

local function createParams(text: string, font: Font, size: number, width: number): GetTextBoundsParams
	local params = Instance.new("GetTextBoundsParams")
	params.Text = text
	params.Font = font
	params.Size = size
	params.Width = width
	return params
end

local function measureAsync(params: GetTextBoundsParams): unknown
    local ok, result = pcall(function()
        return textService:GetTextBoundsAsync(params)
    end)
    return if ok then result else nil
end

local function extractAxis(result: unknown, axis: "X" | "Y"): number?
    if typeof(result) == "Vector2" then
        return if axis == "X" then result.X else result.Y
    end
    if type(result) == "table" and type((result :: any)[axis]) == "number" then
        return (result :: any)[axis]
    end
    return nil
end

function textMetrics.textWidth(font: Font, size: number, text: any): number
    text = tostring(text)
    local cacheKey = tostring(font.Family)
        .. "|"
        .. tostring(font.Weight)
        .. "|"
        .. tostring(font.Style)
        .. "|"
        .. tostring(size)
        .. "|"
        .. text
    local cached = widthCache[cacheKey]
    if cached then
        return cached
    end

    local width = extractAxis(measureAsync(createParams(text, font, size, math.huge)), "X")
    if not width then
        local charCount = utf8.len(text) or #text
        return math.ceil(size * 0.55 * charCount)
    end

    local ceiled = math.ceil(width)
    if widthCount >= widthMax then
        widthCache = {}
        widthCount = 0
    end
    widthCache[cacheKey] = ceiled
    widthCount += 1
    return ceiled
end

function textMetrics.textHeight(font: Font, size: number, text: any, maxWidth: number): number
    text = tostring(text)
    local cacheKey = tostring(font.Family)
        .. "|"
        .. tostring(font.Weight)
        .. "|"
        .. tostring(font.Style)
        .. "|"
        .. tostring(size)
        .. "|"
        .. tostring(maxWidth)
        .. "|"
        .. text
    local cached = heightCache[cacheKey]
    if cached then
        return cached
    end

    local height = extractAxis(measureAsync(createParams(text, font, size, maxWidth)), "Y")
    if not height then
        return size
    end

    local ceiled = math.ceil(height)
    if heightCount >= heightMax then
        heightCache = {}
        heightCount = 0
    end
    heightCache[cacheKey] = ceiled
    heightCount += 1
    return ceiled
end

return textMetrics

end)() end,
    [55] = function()local wax,script,require=ImportGlobals(55)local ImportGlobals return (function(...)
local sidebar = require(script.Parent.Parent.components.sidebar)

local Sidebar = {}

local function buildRail(window, layout)
    window.tabList = window:Create("ScrollingFrame", {
        Name = "Tabs",
        Active = true,
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 0,
        ScrollBarImageTransparency = 1,
        ScrollingDirection = Enum.ScrollingDirection.Y,
        ClipsDescendants = true,
        Parent = window.sidebar,
    })
    window:Create("UIPadding", {
        PaddingTop = UDim.new(0, layout.railPadding),
        PaddingBottom = UDim.new(0, layout.railPadding),
        Parent = window.tabList,
    })
    window.tabListLayout = window:Create("UIListLayout", {
        Padding = UDim.new(0, layout.rowSpacing),
        FillDirection = Enum.FillDirection.Vertical,
        VerticalAlignment = Enum.VerticalAlignment.Top,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = window.tabList,
    })
end

function Sidebar.Build(window, layout)
    window.sidebar = window:Create("Frame", {
        Name = "Sidebar",
        AnchorPoint = Vector2.new(0, 1),
        Position = UDim2.fromScale(0, 1),
        Size = UDim2.new(0, layout.railWidth, 1, -layout.chromeHeight),
        BackgroundTransparency = 1,
        Visible = false,
        Parent = window.main,
    })
    buildRail(window, layout)
end

function Sidebar.ApplyWidth(window, width)
    local layout = window.layout
    window.sidebar.Size = UDim2.new(0, width, 1, -layout.chromeHeight)
    window.elements.Size = UDim2.new(1, -width, 1, -layout.chromeHeight)
    window.bottomFade.Size = UDim2.new(1, -width, layout.fadeSize.Y.Scale, layout.fadeSize.Y.Offset)
    sidebar.applyRailRows(window, width, layout)
end

return Sidebar

end)() end,
    [56] = function()local wax,script,require=ImportGlobals(56)local ImportGlobals return (function(...)
local sidebar = require(script.Parent.Parent.components.sidebar)

local SidebarCollapsed = {}

local function buildRail(window, layout)
    window.tabList = window:Create("ScrollingFrame", {
        Name = "CollapsedTabs",
        Active = true,
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        AutomaticCanvasSize = Enum.AutomaticSize.Y,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 0,
        ScrollBarImageTransparency = 1,
        ScrollingDirection = Enum.ScrollingDirection.Y,
        ClipsDescendants = true,
        Parent = window.sidebar,
    })
    window:Create("UIPadding", {
        PaddingTop = UDim.new(0, layout.railPadding),
        PaddingBottom = UDim.new(0, layout.railPadding),
        Parent = window.tabList,
    })
    window.tabListLayout = window:Create("UIListLayout", {
        Padding = UDim.new(0, layout.rowSpacing),
        FillDirection = Enum.FillDirection.Vertical,
        VerticalAlignment = Enum.VerticalAlignment.Top,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = window.tabList,
    })
end

function SidebarCollapsed.Build(window, layout)
    window.sidebar = window:Create("Frame", {
        Name = "CollapsedSidebar",
        AnchorPoint = Vector2.new(0, 1),
        Position = UDim2.fromScale(0, 1),
        Size = UDim2.new(0, layout.railCollapsedWidth, 1, -layout.chromeHeight),
        BackgroundTransparency = 1,
        Visible = false,
        Parent = window.main,
    })
    buildRail(window, layout)
end

function SidebarCollapsed.ApplyWidth(window)
    local layout = window.layout
    local width = layout.railCollapsedWidth
    window.sidebar.Size = UDim2.new(0, width, 1, -layout.chromeHeight)
    window.elements.Size = UDim2.new(1, -width, 1, -layout.chromeHeight)
    window.bottomFade.Size = UDim2.new(1, -width, layout.fadeSize.Y.Scale, layout.fadeSize.Y.Offset)
    sidebar.applyRailRows(window, width, layout)
end

return SidebarCollapsed

end)() end,
    [57] = function()local wax,script,require=ImportGlobals(57)local ImportGlobals return (function(...)
local search = require(script.Parent.Parent.components.search)
local functions = require(script.Parent.Parent.functions)
local utilities = script.Parent.Parent.utilities
local constants = require(utilities.constants)

local Topbar = {}

local DEFAULT_FADE_WIDTH = 30
local DEFAULT_EDGE_PADDING = 14

-- [[The window surface's WindowColor gradient runs bottom-to-top (rotation]]
-- [[270), so the colour behind the strip — at the top of the window — is]]
-- [[the sequence's last keypoint. Flattening it is the same trick the]]
-- [[bottom fade uses, so a theme can never drift the two apart.]]
local function topSurfaceColor(color)
    local keypoints = functions.toColorSequence(color).Keypoints
    return ColorSequence.new(keypoints[#keypoints].Value)
end

local function buildFade(window, layout, inset, name, anchoredRight, transparentEdge)
    -- [[transparentEdge is the frame edge that must be fully clear:]]
    -- [[left for the right fade (it dissolves towards the visible tabs),]]
    -- [[right for the left fade.]]
    local fadeWidth = layout.tabStripFadeWidth or DEFAULT_FADE_WIDTH
    local fade = window:Create("Frame", {
        Name = name,
        Size = UDim2.fromOffset(fadeWidth, layout.tabStripHeight),
        Position = if anchoredRight
            then UDim2.new(1, -(inset + fadeWidth), 0, layout.tabStripTop)
            else UDim2.new(0, inset, 0, layout.tabStripTop),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 0,
        BackgroundTransparency = 1,
        Visible = false,
        ZIndex = constants.zIndex.tabFade,

        Parent = window.main,
    })

    window:Create("UIGradient", {
        Rotation = 0,
        Transparency = if transparentEdge == "left"
            then NumberSequence.new({
                NumberSequenceKeypoint.new(0, 1),
                NumberSequenceKeypoint.new(1, 0),
            })
            else NumberSequence.new({
                NumberSequenceKeypoint.new(0, 0),
                NumberSequenceKeypoint.new(1, 1),
            }),
        Parent = fade,
    }, { Color = { "WindowColor", topSurfaceColor } })

    return fade
end

function Topbar.Build(window, layout)
    -- [[The strip frame itself is inset from the window edge on both]]
    -- [[sides, and the canvas carries its own UIPadding at each end. The]]
    -- [[two margins stack: an overflowing tab clips well inside the]]
    -- [[window border, and even the first/last pill keeps a visible gap]]
    -- [[from the clip edge (UIPadding scrolls with the content, unlike a]]
    -- [[margin on the frame). The edge fades below mark where tabs go off.]]
    local inset = layout.tabStripInset or 30
    local edgePadding = layout.tabStripEdgePadding or DEFAULT_EDGE_PADDING

    window.tabList = window:Create("ScrollingFrame", {
        Name = "Tabs",
        Active = true,
        Size = UDim2.new(1, -inset * 2, 0, layout.tabStripHeight),
        Position = UDim2.new(0, inset, 0, layout.tabStripTop),
        BackgroundTransparency = 1,
        AutomaticCanvasSize = Enum.AutomaticSize.X,
        CanvasSize = UDim2.new(0, 0, 0, 0),
        ScrollBarThickness = 0,
        ScrollBarImageTransparency = 1,
        ScrollingDirection = Enum.ScrollingDirection.X,
        Parent = window.main,
    })

    -- [[End gutters: empty space the pills can never occupy, so the]]
    -- [[outermost pill never touches the strip edge at either scroll]]
    -- [[extreme. AutomaticCanvasSize folds the padding into the canvas,]]
    -- [[which is also what keeps the last pill scrollable clear of the]]
    -- [[right fade.]]
    window:Create("UIPadding", {
        PaddingLeft = UDim.new(0, edgePadding),
        PaddingRight = UDim.new(0, edgePadding),

        Parent = window.tabList,
    })

    window.tabListLayout = window:Create("UIListLayout", {
        Padding = UDim.new(0, 8),
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Parent = window.tabList,
    })

    window.tabFadeLeft = buildFade(window, layout, inset, "TabFadeLeft", false, "right")
    window.tabFadeRight = buildFade(window, layout, inset, "TabFadeRight", true, "left")

    -- [[Canvas content changes (tabs added/removed) and scrolling move the]]
    -- [[clipping edges; the window's Size covers resizes and the entrance]]
    -- [[tween. _refreshTabStripChrome skips writes when nothing changed.]]
    window:Connect(window.tabList:GetPropertyChangedSignal("CanvasSize"), function()
        window:_refreshTabStripChrome()
    end)
    window:Connect(window.tabList:GetPropertyChangedSignal("CanvasPosition"), function()
        window:_refreshTabStripChrome()
    end)
    window:Connect(window.main:GetPropertyChangedSignal("Size"), function()
        window:_refreshTabStripChrome()
    end)
    window:_refreshTabStripChrome()
end

function Topbar.ApplyWidth(window)
    search.railWidth(window, 0)
    window.elements.Size = UDim2.new(1, 0, 1, -window.layout.chromeHeight)
    window.bottomFade.Size = UDim2.new(1, 0, window.layout.fadeSize.Y.Scale, window.layout.fadeSize.Y.Offset)
end

return Topbar

end)() end,
    [61] = function()local wax,script,require=ImportGlobals(61)local ImportGlobals return (function(...)

local image = require(script.image)

local windowIcons = {
    resolve = image.resolve,
    assign = image.assign,
    preload = image.preload,
    avatar = image.avatar,
    rewrites = image.rewrites,
}

return windowIcons

end)() end,
    [59] = function()local wax,script,require=ImportGlobals(59)local ImportGlobals return (function(...)
local imageCache = require(script.Parent.Parent.cache.imageCache)
local variables = require(script.Parent.Parent.core.state)

type AvatarCallback = imageCache.AvatarCallback
type PreloadCallback = imageCache.PreloadCallback

local image = {}

image.rewrites = imageCache.rewrites
image.onBlock = nil :: ((unknown) -> ())?

type PendingProperties = { [string]: boolean }

image.pending = {} :: { [number]: { [Instance]: PendingProperties } }

local settled = false

local imageProperties: { [string]: boolean } = {
    Image = true,
    HoverImage = true,
    PressedImage = true,
}

local function idOf(value: unknown): number?
    if type(value) == "number" then
        return value
    elseif type(value) == "string" then
        return tonumber(string.match(value, "^rbxassetid://(%d+)$"))
    end
    return nil
end

local function blocked(value: unknown): string
    if image.onBlock then
        image.onBlock(value)
    end
    return ""
end

function image.preload(onSettled: PreloadCallback?): (boolean, number)
    settled = false
    return imageCache.preload(function(failed, failedRoles)
        settled = true
        table.clear(image.pending)
        if onSettled then
            onSettled(failed, failedRoles)
        end
    end)
end

function image.avatar(userId: number, onReady: AvatarCallback?): string
    local uri = `rbxthumb://type=AvatarHeadShot&id={userId}&w=48&h=48`
    if not variables.secureMode then
        -- [[The URI is immediately usable; still deliver the callback so]]
        -- [[callers that only render on it do not stay blank.]]
        if onReady then
            task.defer(onReady, uri)
        end
        return uri
    end

    return imageCache.avatar(userId, onReady)
end

function image.assign(instance: Instance, property: string, value: unknown)
    local target = instance :: any
    if not imageProperties[property] then
        target[property] = value
        return
    end

    local isRemoteUrl = type(value) == "string" and string.match(value, "^https?://") ~= nil
    if isRemoteUrl then
        target[property] = imageCache.remote(value :: string, function(uri)
            if instance.Parent then
                target[property] = uri or ""
            end
        end)
    else
        target[property] = image.resolve(value)
    end
    if variables.secureMode and not settled then
        local id = idOf(value)
        if id and not image.rewrites[id] then
            local waiting = image.pending[id]
            if not waiting then
                waiting = setmetatable({}, { __mode = "k" }) :: any
                image.pending[id] = waiting
            end
            local properties = waiting[instance]
            if not properties then
                properties = {}
                waiting[instance] = properties
            end
            properties[property] = true
        end
    end
end

imageCache.onCached = function(id: number)
    local waiting = image.pending[id]
    if not waiting then
        return
    end
    local uri = image.rewrites[id]
    if uri then
        for instance, properties in waiting do
            if instance.Parent then
                local target = instance :: any
                for property in properties do
                    target[property] = uri
                end
            end
        end
    end
    image.pending[id] = nil
end

function image.resolve(value: unknown): string
    if value == nil or value == 0 or value == "" then
        return ""
    end

    if type(value) == "string" then
        if string.match(value, "^https?://") then
            return imageCache.remote(value)
        end
        if string.sub(value, 1, 11) == "rbxasset://" then
            return value
        end
        if string.sub(value, 1, 11) == "rbxthumb://" then
            return if variables.secureMode then blocked(value) else value
        end
    end

    local id: number? = nil
    if type(value) == "number" then
        id = value
    elseif type(value) == "string" then
        id = tonumber(string.match(value, "^rbxassetid://(%d+)$"))
    end

    local rewrite = if id then image.rewrites[id] else nil
    if rewrite then
        return rewrite
    end

    if variables.secureMode then
        return blocked(value)
    end

    if type(value) == "number" then
        return "rbxassetid://" .. value
    end
    if type(value) == "string" then
        return value
    end

    return blocked(value)
end

return image

end)() end,
    [60] = function()local wax,script,require=ImportGlobals(60)local ImportGlobals return (function(...)

local windowIcons = {}

windowIcons.ids = {
    window = 80387863064905,
    close = 83277910885129,
    minimise = 108115485663409,
    maximise = 88738500661569,
    settings = 129180860773723,
    search = 100604009889706,
    chevron = 88479147175134,
    check = 125626312718314,
    dot = 91452555903853,
    config = 125823673784681,
}

windowIcons.roles = {
    close = "close",
    minimise = "minimise",
    maximise = "maximise",
    settings = "settings",
    search = "search",
    chevron = "chevron",
    check = "check",
    dot = "dot",
    config = "config",
}

-- [[The id/role tables are module constants, so the reverse manifest and the
-- [[sorted role list are built once on first use instead of on every call -
-- [[roleForId used to rebuild the whole manifest per failed icon.]]
local cachedManifest: { [number]: { roles: { string } } }? = nil
local cachedRoleList: { string }? = nil

function windowIcons.manifest()
    if cachedManifest then
        return cachedManifest
    end
    local manifest: { [number]: { roles: { string } } } = {}
    for name, id in windowIcons.ids do
        local entry = manifest[id]
        if not entry then
            entry = { roles = {} }
            manifest[id] = entry
        end
        table.insert(entry.roles, windowIcons.roles[name] or name)
    end
    cachedManifest = manifest
    return manifest
end

function windowIcons.roleForId(id: number): string
    local entry = windowIcons.manifest()[id]
    if not entry or #entry.roles == 0 then
        return "window icon"
    end
    return table.concat(entry.roles, "/")
end

function windowIcons.roleList(): { string }
    if cachedRoleList then
        return cachedRoleList
    end
    local list = {}
    for name in windowIcons.ids do
        table.insert(list, windowIcons.roles[name] or name)
    end
    table.sort(list)
    cachedRoleList = list
    return list
end

return windowIcons

end)() end,
    [70] = function()local wax,script,require=ImportGlobals(70)local ImportGlobals return (function(...)
-- [[Icon resolver service.]]
-- [[]]
-- [[Public API is compatible with the previous single-module catalog:]]
-- [[local Icons = require(path.to.icons)]]
-- [[Icons.get("arrow-down")            -- searches every pack in priority order]]
-- [[Icons.get("arrow-down", "lucide")  -- one pack only]]
-- [[Icons.get("material:home")         -- pack-qualified: that pack, exactly]]
-- [[Icons.getByPack("material", "add")]]
-- [[Icons.resolve(value, pack)]]
-- [[Icons.list(pack) / Icons.packs() / Icons.count(pack) / Icons.isPack(pack)]]
-- [[Icons.priority() / Icons.loaded() / Icons.refreshCustom()]]
-- [[Icons.Lucide == "lucide" (and the other pack name constants)]]
-- [[]]
-- [[Packs are lazy: each pack's data table lives in its own module and is]]
-- [[required on first lookup for that pack only, then kept cached. No HTTP]]
-- [[requests happen between internal modules; pack data is static.]]
-- [[]]
-- [[Lookups are exact. Icon names and pack names are case-sensitive: nothing]]
-- [[is lowercased, corrected or fuzzy-matched, so a miss is a miss. A name]]
-- [[that exists in several packs resolves to the first pack listed by]]
-- [[`Icons.priority()` (lucide, material, tabler, phosphor, heroicons, feather,]]
-- [[remix); an explicit pack - `get(name, pack)` or the `"pack:name"` form -]]
-- [[is never overridden by that order, and an unknown or mis-cased pack name]]
-- [[resolves to nothing rather than to some other pack.]]

local icons = {}

-- [[Raw GitHub base URL for all icons (kept for API compatibility).]]
icons.BASE = "https://raw.githubusercontent.com/Haijo12/sisys_ididh/main"

-- [[Pack priority for a bare name. Deterministic, and also the order packs]]
-- [[load in: the search stops at the first pack that has the name, so a lucide]]
-- [[icon never pulls in the other six tables.]]
local PACK_ORDER = { "lucide", "material", "tabler", "phosphor", "heroicons", "feather", "remix" }

-- [[Pack registry: pack name -> lazy loader. Each loader requires the pack]]
-- [[module on demand; require() itself caches the module table afterwards.]]
local packLoaders = {
    lucide = function()
        return require(script.lucide)
    end,
    material = function()
        return require(script.material)
    end,
    tabler = function()
        return require(script.tabler)
    end,
    phosphor = function()
        return require(script.phosphor)
    end,
    heroicons = function()
        return require(script.heroicons)
    end,
    feather = function()
        return require(script.feather)
    end,
    remix = function()
        return require(script.remix)
    end,
}

local loadedPacks: { [string]: { [string]: string } } = {}

-- [[Pack name constants (legacy surface: Icons.Lucide, Icons.Material, ...)]]
icons.Lucide = "lucide"
icons.Material = "material"
icons.Tabler = "tabler"
icons.Phosphor = "phosphor"
icons.Heroicons = "heroicons"
icons.Feather = "feather"
icons.Remix = "remix"

-- [[Lazily exposed pack tables (Icons.lucide, Icons.material, ...).]]
setmetatable(icons, {
    __index = function(self, pack: string)
        local loader = packLoaders[pack]
        if not loader then
            return nil
        end
        local data = loader()
        loadedPacks[pack] = data
        -- [[Cache the table so future reads skip the metatable entirely.]]
        rawset(self, pack, data)
        return data
    end,
})

local function packTable(pack: string?): { [string]: string }?
    pack = pack or "lucide"
    if type(pack) ~= "string" then
        return nil
    end
    local cached = loadedPacks[pack]
    if cached then
        return cached
    end
    local loader = packLoaders[pack]
    if not loader then
        return nil
    end
    local data = loader()
    loadedPacks[pack] = data
    rawset(icons, pack, data)
    return data
end

function icons.isPack(pack: string?): boolean
    return pack ~= nil and type(pack) == "string" and packLoaders[pack] ~= nil
end

-- [[The order a bare name is searched in (a copy: callers cannot reorder it).]]
function icons.priority(): { string }
    local order = table.create(#PACK_ORDER)
    for index, pack in ipairs(PACK_ORDER) do
        order[index] = pack
    end
    return order
end

-- [[Which packs have been loaded so far. Laziness is part of the contract]]
-- [[(startup must not read 13.7k lines of tables to show one icon), so it is]]
-- [[observable: a suite asserts that one lucide name loads one pack.]]
function icons.loaded(): { string }
    local names = {}
    for pack in loadedPacks do
        table.insert(names, pack)
    end
    table.sort(names)
    return names
end

local namedAssets = {
    Astra = "assets/Astra.png",
    Sirius = "assets/Sirius.png",
}

local function environment(): any
    return (getfenv and getfenv()) or _G
end

-- [[Warnings are once per unknown pack name: a typo in a hot path must not]]
-- [[spam the console every frame, but the first occurrence has to be findable.]]
local warnedPacks: { [string]: boolean } = {}

local function report(message: string)
    local env = environment()
    if type(env.warn) == "function" then
        pcall(env.warn, message)
    end
end

local function warnUnknownPack(pack: string, request: string)
    if warnedPacks[pack] then
        return
    end
    warnedPacks[pack] = true
    report(`[Icons] Unknown icon pack "{pack}" in "{request}": pack names are case-sensitive ({table.concat(PACK_ORDER, ", ")})`)
end

-- [[`"pack:name"` -> pack, name. Split on the first colon only, so an icon]]
-- [[name that itself contains a colon is still addressable. No pack prefix]]
-- [[means "search every pack".]]
local function splitRequest(value: string): (string?, string)
    local colon = string.find(value, ":", 1, true)
    if not colon then
        return nil, value
    end
    return string.sub(value, 1, colon - 1), string.sub(value, colon + 1)
end

local function findInPack(pack: string, name: string): string?
    local data = packTable(pack)
    if not data then
        return nil
    end
    return data[name]
end

local assetBase = "https://raw.githubusercontent.com/Kira762/astra-version-1/main/"

local function prefixed(resolved: string?): string?
    if type(resolved) == "string" and string.sub(resolved, 1, 7) == "assets/" then
        return assetBase .. resolved
    end
    return resolved
end

-- =========================================================================
-- Custom assets
-- =========================================================================
-- [[User files under `custom_asset/` win over every pack. The folder is]]
-- [[indexed once with the executor's `listfiles` instead of being probed per]]
-- [[lookup: a name that has no file there costs nothing after the first]]
-- [[search (a 16-element page used to spend 195 getcustomasset calls on]]
-- [[misses alone), and a file that is used is imported once per path.]]
-- [[]]
-- [[Executors without `listfiles` keep the historic per-name probe, now]]
-- [[memoised - including the misses, which is what repeats do not re-run.]]
-- [[]]
-- [[Files may live in subfolders (`custom_asset/brand/house.png` is looked up]]
-- [[as `"brand/house"`). Extensions keep the historic set and preference]]
-- [[order: png, jpg, jpeg, webp, then a bare filename with no extension.]]

local customAssetFolder = "custom_asset"
local customExtensions = { ".png", ".jpg", ".jpeg", ".webp" }

local customIndex: { [string]: string }? = nil
local customIndexBuilt = false
local customProbed: { [string]: string? } = {}
local customUris: { [string]: string? } = {}
local resolved: { [string]: string? } = {}

-- [[A custom file's lookup key (its path inside the folder, without the]]
-- [[extension) and the rank of that extension. Files with an unknown]]
-- [[extension are not indexed; a bare filename ranks last, as it always did.]]
local function customKey(path: string): (string?, number?)
    local lower = string.lower(path)
    for rank, ext in ipairs(customExtensions) do
        if string.sub(lower, -#ext) == ext then
            return string.sub(path, 1, #path - #ext), rank
        end
    end
    local base = string.match(lower, "[^/]*$") or lower
    if not string.find(base, ".", 1, true) then
        return path, #customExtensions + 1
    end
    return nil, nil
end

local function buildCustomIndex(): { [string]: string }?
    local env = environment()
    if type(env.listfiles) ~= "function" then
        return nil
    end
    local ok, entries = pcall(env.listfiles, customAssetFolder)
    if not ok or type(entries) ~= "table" then
        -- [[No folder (or no listing support right now): an empty index, so]]
        -- [[every later lookup is a table miss instead of a filesystem probe.]]
        return {}
    end
    local index: { [string]: string } = {}
    local ranks: { [string]: number } = {}
    for _, entry in ipairs(entries) do
        if type(entry) == "string" and entry ~= "" then
            local key, rank = customKey(entry)
            if key then
                local prefix = customAssetFolder .. "/"
                if string.sub(key, 1, #prefix) == prefix then
                    key = string.sub(key, #prefix + 1)
                end
                local previous = ranks[key]
                if previous == nil or rank < previous then
                    index[key] = entry
                    ranks[key] = rank
                end
            end
        end
    end
    return index
end

local function customFile(value: string): string?
    if not customIndexBuilt then
        customIndex = buildCustomIndex()
        customIndexBuilt = true
    end
    local index = customIndex
    if index then
        return index[value]
    end
    local probed = customProbed[value]
    if probed ~= nil then
        return probed or nil
    end
    local env = environment()
    if type(env.getcustomasset) ~= "function" then
        customProbed[value] = false
        return nil
    end
    for _, ext in ipairs(customExtensions) do
        local path = customAssetFolder .. "/" .. value .. ext
        local ok, uri = pcall(env.getcustomasset, path)
        if ok and type(uri) == "string" and uri ~= "" then
            customProbed[value] = path
            customUris[path] = uri
            return path
        end
    end
    -- [[A file with no extension is the last accepted form (historic).]]
    local bare = customAssetFolder .. "/" .. value
    local ok, uri = pcall(env.getcustomasset, bare)
    if ok and type(uri) == "string" and uri ~= "" then
        customProbed[value] = bare
        customUris[bare] = uri
        return bare
    end
    customProbed[value] = false
    return nil
end

local function customUri(value: string): string?
    local path = customFile(value)
    if not path then
        return nil
    end
    local cached = customUris[path]
    if cached ~= nil then
        return cached or nil
    end
    local env = environment()
    local ok, uri = pcall(env.getcustomasset, path)
    if ok and type(uri) == "string" and uri ~= "" then
        customUris[path] = uri
        return uri
    end
    customUris[path] = false
    return nil
end

-- [[Re-read the custom asset folder and drop every memo that depended on it]]
-- [[(including resolved icons). Call it after adding or replacing files at]]
-- [[runtime. Returns how many files the listing found; 0 means the executor]]
-- [[has no listfiles and lookups fall back to the per-name probe.]]
function icons.refreshCustom(): number
    customIndex = buildCustomIndex()
    customIndexBuilt = true
    table.clear(customProbed)
    table.clear(customUris)
    table.clear(resolved)
    local count = 0
    for _ in customIndex or {} do
        count += 1
    end
    return count
end

-- =========================================================================
-- Lookup
-- =========================================================================

-- [[Get icon path by name. Without a pack, every pack is searched in]]
-- [[`icons.priority()` order and the first match wins.]]
function icons.get(name: string, pack: string?): string?
    if type(name) ~= "string" or name == "" then
        return nil
    end
    if namedAssets[name] then
        return namedAssets[name]
    end
    local qualifiedPack, qualifiedName = splitRequest(name)
    if qualifiedPack ~= nil then
        if not packLoaders[qualifiedPack] then
            warnUnknownPack(qualifiedPack, name)
            return nil
        end
        if qualifiedName == "" then
            return nil
        end
        return findInPack(qualifiedPack, qualifiedName)
    end
    if pack ~= nil then
        if type(pack) ~= "string" or not packLoaders[pack] then
            warnUnknownPack(tostring(pack), name)
            return nil
        end
        return findInPack(pack, name)
    end
    for _, candidate in ipairs(PACK_ORDER) do
        local hit = findInPack(candidate, name)
        if hit then
            return hit
        end
    end
    return nil
end

-- [[Get icon URL by pack and name (a pack-qualified `get`).]]
function icons.getByPack(pack: string, name: string): string?
    return icons.get(name, pack)
end

-- [[The loadable form of one request, or nil when nothing matched.]]
local function lookup(value: string, pack: string?): string?
    if namedAssets[value] then
        return prefixed(namedAssets[value])
    end
    local qualifiedPack, qualifiedName = splitRequest(value)
    if qualifiedPack ~= nil then
        if not packLoaders[qualifiedPack] then
            warnUnknownPack(qualifiedPack, value)
            return nil
        end
        if qualifiedName == "" then
            return nil
        end
        return prefixed(findInPack(qualifiedPack, qualifiedName))
    end
    if pack ~= nil then
        -- [[An explicit pack is exactly that: `resolve(name, "material")` and
        -- [[`window:ResolveIcon` (which passes the window's pack) answer from
        -- [[that pack or not at all. Falling back here would quietly overrule
        -- [[the pack a host picked, and it is what the profile card's alias
        -- [[chain relies on: a name the active pack does not have has to come
        -- [[back unresolved so the next candidate is tried.]]
        if type(pack) ~= "string" or not packLoaders[pack] then
            warnUnknownPack(tostring(pack), value)
            return nil
        end
        return prefixed(findInPack(pack, value))
    end
    for _, candidate in ipairs(PACK_ORDER) do
        local hit = prefixed(findInPack(candidate, value))
        if hit then
            return hit
        end
    end
    return nil
end

local function isLoadable(value: string): boolean
    return string.match(value, "^rbxassetid://") ~= nil
        or string.match(value, "^rbxasset://") ~= nil
        or string.match(value, "^rbxthumb://") ~= nil
        or string.match(value, "^https?://") ~= nil
end

-- [[Resolve a component icon.]]
-- [[]]
-- [[  1. values that are already loadable pass through untouched,]]
-- [[  2. a file in the custom asset folder wins over every pack,]]
-- [[  3. `"pack:name"` is exact - that pack or nothing,]]
-- [[  4. with no pack, a bare name is looked up in `icons.priority()` order]]
-- [[     and the first pack that has it wins; with a pack, that pack answers]]
-- [[     or the request stays unresolved (explicit packs are never overruled),]]
-- [[  5. nothing matches: the value comes back unchanged, which is the]]
-- [[     fallback the UI has always used.]]
-- [[]]
-- [[Results are memoised per (request, pack): repeated resolves of the same]]
-- [[icon - and every element that asks for it - reuse one answer, and a]]
-- [[custom file's imported URI is taken from the executor once.]]
function icons.resolve(value: (string | number)?, pack: string?): (string | number)?
    if value == nil or value == 0 or value == "" then
        return value
    end
    if type(value) ~= "string" then
        return value
    end
    if isLoadable(value) then
        return value
    end
    local key = (if type(pack) == "string" then pack else "") .. "\0" .. value
    local cached = resolved[key]
    if cached ~= nil then
        return if cached == false then value else cached
    end
    local hit = customUri(value) or lookup(value, pack)
    resolved[key] = hit or false
    return hit or value
end

-- [[List all available icon names in a pack]]
function icons.list(pack: string?): { string }
    pack = pack or "lucide"
    local data = packTable(pack)
    if not data then
        return {}
    end
    local result = {}
    for name in data do
        table.insert(result, name)
    end
    table.sort(result)
    return result
end

-- [[List all available packs]]
function icons.packs(): { string }
    local names = table.create(#PACK_ORDER)
    for index, pack in ipairs(PACK_ORDER) do
        names[index] = pack
    end
    return names
end

-- [[Count icons in a pack (loads that pack on demand)]]
function icons.count(pack: string?): number
    pack = pack or "lucide"
    local data = packTable(pack)
    if not data then
        return 0
    end
    local n = 0
    for _ in data do
        n += 1
    end
    return n
end

return icons

end)() end,
    [62] = function()local wax,script,require=ImportGlobals(62)local ImportGlobals return (function(...)
-- [[feather icon pack.]]
-- [[Compact catalog: names only. PNG URLs are derived on demand by]]
-- [[icons/packBuilder (kebab/snake name to PascalCase), so this module]]
-- [[parses as one string constant instead of thousands of table]]
-- [[constructors. Regenerate with scripts/generate_icon_packs.js.]]

return require(script.Parent.packBuilder).create(
	"assets/icons/feather-pack/",
	"feather",
[==[
activity airplay alert-circle alert-octagon alert-triangle align-center align-justify align-left align-right anchor
aperture archive arrow-down-circle arrow-down-left arrow-down-right arrow-down arrow-left-circle arrow-left arrow-right-circle arrow-right
arrow-up-circle arrow-up-left arrow-up-right arrow-up at-sign award bar-chart-2 bar-chart battery-charging battery
bell-off bell bluetooth bold book-open book bookmark box briefcase calendar
camera-off camera cast check-circle check-square check chevron-down chevron-left chevron-right chevron-up
chevrons-down chevrons-left chevrons-right chevrons-up chrome circle clipboard clock cloud-drizzle cloud-lightning
cloud-off cloud-rain cloud-snow cloud code codepen codesandbox coffee columns command
compass copy corner-down-left corner-down-right corner-left-down corner-left-up corner-right-down corner-right-up corner-up-left corner-up-right
cpu credit-card crop crosshair database delete disc divide-circle divide-square divide
dollar-sign download-cloud download dribbble droplet edit-2 edit-3 edit external-link eye-off
eye facebook fast-forward feather figma file-minus file-plus file-text file film
filter flag folder-minus folder-plus folder framer frown gift git-branch git-commit
git-merge git-pull-request github gitlab globe grid hard-drive hash headphones heart
help-circle hexagon home image inbox info instagram italic key layers
layout life-buoy link-2 link linkedin list loader lock log-in log-out
mail map-pin map maximize-2 maximize meh menu message-circle message-square mic-off
mic minimize-2 minimize minus-circle minus-square minus monitor moon more-horizontal more-vertical
mouse-pointer move music navigation-2 navigation octagon package paperclip pause-circle pause
pen-tool percent phone-call phone-forwarded phone-incoming phone-missed phone-off phone-outgoing phone pie-chart
play-circle play plus-circle plus-square plus pocket power printer radio refresh-ccw
refresh-cw repeat rewind rotate-ccw rotate-cw rss save scissors search send
server settings share-2 share shield-off shield shopping-bag shopping-cart shuffle sidebar
skip-back skip-forward slack slash sliders smartphone smile speaker square star
stop-circle sun sunrise sunset table tablet tag target terminal thermometer
thumbs-down thumbs-up toggle-left toggle-right tool trash-2 trash trello trending-down trending-up
triangle truck tv twitch twitter type umbrella underline unlock upload-cloud
upload user-check user-minus user-plus user-x user users video-off video voicemail
volume-1 volume-2 volume-x volume watch wifi-off wifi wind x-circle x-octagon
x-square x youtube zap-off zap zoom-in zoom-out
]==]
)

end)() end,
    [63] = function()local wax,script,require=ImportGlobals(63)local ImportGlobals return (function(...)
-- [[heroicons icon pack.]]
-- [[Compact catalog: names only. PNG URLs are derived on demand by]]
-- [[icons/packBuilder (kebab/snake name to PascalCase), so this module]]
-- [[parses as one string constant instead of thousands of table]]
-- [[constructors. Regenerate with scripts/generate_icon_packs.js.]]

return require(script.Parent.packBuilder).create(
	"assets/icons/heroicons-pack/",
	"heroicons",
[==[
academic-cap academic-cap_solid adjustments-horizontal adjustments-horizontal_solid adjustments-vertical adjustments-vertical_solid archive-box-arrow-down archive-box-arrow-down_solid archive-box-x-mark archive-box-x-mark_solid
archive-box archive-box_solid arrow-down-circle arrow-down-circle_solid arrow-down-left arrow-down-left_solid arrow-down-on-square-stack arrow-down-on-square-stack_solid arrow-down-on-square arrow-down-on-square_solid
arrow-down-right arrow-down-right_solid arrow-down-tray arrow-down-tray_solid arrow-down arrow-down_solid arrow-left-circle arrow-left-circle_solid arrow-left-end-on-rectangle arrow-left-end-on-rectangle_solid
arrow-left-on-rectangle arrow-left-on-rectangle_solid arrow-left-start-on-rectangle arrow-left-start-on-rectangle_solid arrow-left arrow-left_solid arrow-long-down arrow-long-down_solid arrow-long-left arrow-long-left_solid
arrow-long-right arrow-long-right_solid arrow-long-up arrow-long-up_solid arrow-path-rounded-square arrow-path-rounded-square_solid arrow-path arrow-path_solid arrow-right-circle arrow-right-circle_solid
arrow-right-end-on-rectangle arrow-right-end-on-rectangle_solid arrow-right-on-rectangle arrow-right-on-rectangle_solid arrow-right-start-on-rectangle arrow-right-start-on-rectangle_solid arrow-right arrow-right_solid arrow-small-down arrow-small-down_solid
arrow-small-left arrow-small-left_solid arrow-small-right arrow-small-right_solid arrow-small-up arrow-small-up_solid arrow-top-right-on-square arrow-top-right-on-square_solid arrow-trending-down arrow-trending-down_solid
arrow-trending-up arrow-trending-up_solid arrow-turn-down-left arrow-turn-down-left_solid arrow-turn-down-right arrow-turn-down-right_solid arrow-turn-left-down arrow-turn-left-down_solid arrow-turn-left-up arrow-turn-left-up_solid
arrow-turn-right-down arrow-turn-right-down_solid arrow-turn-right-up arrow-turn-right-up_solid arrow-turn-up-left arrow-turn-up-left_solid arrow-turn-up-right arrow-turn-up-right_solid arrow-up-circle arrow-up-circle_solid
arrow-up-left arrow-up-left_solid arrow-up-on-square-stack arrow-up-on-square-stack_solid arrow-up-on-square arrow-up-on-square_solid arrow-up-right arrow-up-right_solid arrow-up-tray arrow-up-tray_solid
arrow-up arrow-up_solid arrow-uturn-down arrow-uturn-down_solid arrow-uturn-left arrow-uturn-left_solid arrow-uturn-right arrow-uturn-right_solid arrow-uturn-up arrow-uturn-up_solid
arrows-pointing-in arrows-pointing-in_solid arrows-pointing-out arrows-pointing-out_solid arrows-right-left arrows-right-left_solid arrows-up-down arrows-up-down_solid at-symbol at-symbol_solid
backspace backspace_solid backward backward_solid banknotes banknotes_solid bars-2 bars-2_solid bars-3-bottom-left bars-3-bottom-left_solid
bars-3-bottom-right bars-3-bottom-right_solid bars-3-center-left bars-3-center-left_solid bars-3 bars-3_solid bars-4 bars-4_solid bars-arrow-down bars-arrow-down_solid
bars-arrow-up bars-arrow-up_solid battery-0 battery-0_solid battery-100 battery-100_solid battery-50 battery-50_solid beaker beaker_solid
bell-alert bell-alert_solid bell-slash bell-slash_solid bell-snooze bell-snooze_solid bell bell_solid bold bold_solid
bolt-slash bolt-slash_solid bolt bolt_solid book-open book-open_solid bookmark-slash bookmark-slash_solid bookmark-square bookmark-square_solid
bookmark bookmark_solid briefcase briefcase_solid bug-ant bug-ant_solid building-library building-library_solid building-office-2 building-office-2_solid
building-office building-office_solid building-storefront building-storefront_solid cake cake_solid calculator calculator_solid calendar-date-range calendar-date-range_solid
calendar-days calendar-days_solid calendar calendar_solid camera camera_solid chart-bar-square chart-bar-square_solid chart-bar chart-bar_solid
chart-pie chart-pie_solid chat-bubble-bottom-center-text chat-bubble-bottom-center-text_solid chat-bubble-bottom-center chat-bubble-bottom-center_solid chat-bubble-left-ellipsis chat-bubble-left-ellipsis_solid chat-bubble-left-right chat-bubble-left-right_solid
chat-bubble-left chat-bubble-left_solid chat-bubble-oval-left-ellipsis chat-bubble-oval-left-ellipsis_solid chat-bubble-oval-left chat-bubble-oval-left_solid check-badge check-badge_solid check-circle check-circle_solid
check check_solid chevron-double-down chevron-double-down_solid chevron-double-left chevron-double-left_solid chevron-double-right chevron-double-right_solid chevron-double-up chevron-double-up_solid
chevron-down chevron-down_solid chevron-left chevron-left_solid chevron-right chevron-right_solid chevron-up-down chevron-up-down_solid chevron-up chevron-up_solid
circle-stack circle-stack_solid clipboard-document-check clipboard-document-check_solid clipboard-document-list clipboard-document-list_solid clipboard-document clipboard-document_solid clipboard clipboard_solid
clock clock_solid cloud-arrow-down cloud-arrow-down_solid cloud-arrow-up cloud-arrow-up_solid cloud cloud_solid code-bracket-square code-bracket-square_solid
code-bracket code-bracket_solid cog-6-tooth cog-6-tooth_solid cog-8-tooth cog-8-tooth_solid cog cog_solid command-line command-line_solid
computer-desktop computer-desktop_solid cpu-chip cpu-chip_solid credit-card credit-card_solid cube-transparent cube-transparent_solid cube cube_solid
currency-bangladeshi currency-bangladeshi_solid currency-dollar currency-dollar_solid currency-euro currency-euro_solid currency-pound currency-pound_solid currency-rupee currency-rupee_solid
currency-yen currency-yen_solid cursor-arrow-rays cursor-arrow-rays_solid cursor-arrow-ripple cursor-arrow-ripple_solid device-phone-mobile device-phone-mobile_solid device-tablet device-tablet_solid
divide divide_solid document-arrow-down document-arrow-down_solid document-arrow-up document-arrow-up_solid document-chart-bar document-chart-bar_solid document-check document-check_solid
document-currency-bangladeshi document-currency-bangladeshi_solid document-currency-dollar document-currency-dollar_solid document-currency-euro document-currency-euro_solid document-currency-pound document-currency-pound_solid document-currency-rupee document-currency-rupee_solid
document-currency-yen document-currency-yen_solid document-duplicate document-duplicate_solid document-magnifying-glass document-magnifying-glass_solid document-minus document-minus_solid document-plus document-plus_solid
document-text document-text_solid document document_solid ellipsis-horizontal-circle ellipsis-horizontal-circle_solid ellipsis-horizontal ellipsis-horizontal_solid ellipsis-vertical ellipsis-vertical_solid
envelope-open envelope-open_solid envelope envelope_solid equals equals_solid exclamation-circle exclamation-circle_solid exclamation-triangle exclamation-triangle_solid
eye-dropper eye-dropper_solid eye-slash eye-slash_solid eye eye_solid face-frown face-frown_solid face-smile face-smile_solid
film film_solid finger-print finger-print_solid fire fire_solid flag flag_solid folder-arrow-down folder-arrow-down_solid
folder-minus folder-minus_solid folder-open folder-open_solid folder-plus folder-plus_solid folder folder_solid forward forward_solid
funnel funnel_solid gif gif_solid gift-top gift-top_solid gift gift_solid globe-alt globe-alt_solid
globe-americas globe-americas_solid globe-asia-australia globe-asia-australia_solid globe-europe-africa globe-europe-africa_solid h1 h1_solid h2 h2_solid
h3 h3_solid hand-raised hand-raised_solid hand-thumb-down hand-thumb-down_solid hand-thumb-up hand-thumb-up_solid hashtag hashtag_solid
heart heart_solid home-modern home-modern_solid home home_solid identification identification_solid inbox-arrow-down inbox-arrow-down_solid
inbox-stack inbox-stack_solid inbox inbox_solid information-circle information-circle_solid italic italic_solid key key_solid
language language_solid lifebuoy lifebuoy_solid light-bulb light-bulb_solid link-slash link-slash_solid link link_solid
list-bullet list-bullet_solid lock-closed lock-closed_solid lock-open lock-open_solid magnifying-glass-circle magnifying-glass-circle_solid magnifying-glass-minus magnifying-glass-minus_solid
magnifying-glass-plus magnifying-glass-plus_solid magnifying-glass magnifying-glass_solid map-pin map-pin_solid map map_solid megaphone megaphone_solid
microphone microphone_solid minus-circle minus-circle_solid minus-small minus-small_solid minus minus_solid moon moon_solid
musical-note musical-note_solid newspaper newspaper_solid no-symbol no-symbol_solid numbered-list numbered-list_solid paint-brush paint-brush_solid
paper-airplane paper-airplane_solid paper-clip paper-clip_solid pause-circle pause-circle_solid pause pause_solid pencil-square pencil-square_solid
pencil pencil_solid percent-badge percent-badge_solid phone-arrow-down-left phone-arrow-down-left_solid phone-arrow-up-right phone-arrow-up-right_solid phone-x-mark phone-x-mark_solid
phone phone_solid photo photo_solid play-circle play-circle_solid play-pause play-pause_solid play play_solid
plus-circle plus-circle_solid plus-small plus-small_solid plus plus_solid power power_solid presentation-chart-bar presentation-chart-bar_solid
presentation-chart-line presentation-chart-line_solid printer printer_solid puzzle-piece puzzle-piece_solid qr-code qr-code_solid question-mark-circle question-mark-circle_solid
queue-list queue-list_solid radio radio_solid receipt-percent receipt-percent_solid receipt-refund receipt-refund_solid rectangle-group rectangle-group_solid
rectangle-stack rectangle-stack_solid rocket-launch rocket-launch_solid rss rss_solid scale scale_solid scissors scissors_solid
server-stack server-stack_solid server server_solid share share_solid shield-check shield-check_solid shield-exclamation shield-exclamation_solid
shopping-bag shopping-bag_solid shopping-cart shopping-cart_solid signal-slash signal-slash_solid signal signal_solid slash slash_solid
sparkles sparkles_solid speaker-wave speaker-wave_solid speaker-x-mark speaker-x-mark_solid square-2-stack square-2-stack_solid square-3-stack-3d square-3-stack-3d_solid
squares-2x2 squares-2x2_solid squares-plus squares-plus_solid star star_solid stop-circle stop-circle_solid stop stop_solid
strikethrough strikethrough_solid sun sun_solid swatch swatch_solid table-cells table-cells_solid tag tag_solid
ticket ticket_solid trash trash_solid trophy trophy_solid truck truck_solid tv tv_solid
underline underline_solid user-circle user-circle_solid user-group user-group_solid user-minus user-minus_solid user-plus user-plus_solid
user user_solid users users_solid variable variable_solid video-camera-slash video-camera-slash_solid video-camera video-camera_solid
view-columns view-columns_solid viewfinder-circle viewfinder-circle_solid wallet wallet_solid wifi wifi_solid window window_solid
wrench-screwdriver wrench-screwdriver_solid wrench wrench_solid x-circle x-circle_solid x-mark x-mark_solid
]==]
)

end)() end,
    [64] = function()local wax,script,require=ImportGlobals(64)local ImportGlobals return (function(...)
-- [[lucide icon pack.]]
-- [[Compact catalog: names only. PNG URLs are derived on demand by]]
-- [[icons/packBuilder (kebab/snake name to PascalCase), so this module]]
-- [[parses as one string constant instead of thousands of table]]
-- [[constructors. Regenerate with scripts/generate_icon_packs.js.]]

return require(script.Parent.packBuilder).create(
	"assets/icons/lucide-pack/",
	"lucide",
[==[
a-arrow-down a-arrow-up a-large-small accessibility activity ad air-vent airplay alarm-clock-check alarm-clock-minus
alarm-clock-off alarm-clock-plus alarm-clock alarm-smoke album align-center-horizontal align-center-vertical align-end-horizontal align-end-vertical align-horizontal-distribute-center
align-horizontal-distribute-end align-horizontal-distribute-start align-horizontal-justify-center align-horizontal-justify-end align-horizontal-justify-start align-horizontal-space-around align-horizontal-space-between align-start-horizontal align-start-vertical align-vertical-distribute-center
align-vertical-distribute-end align-vertical-distribute-start align-vertical-justify-center align-vertical-justify-end align-vertical-justify-start align-vertical-space-around align-vertical-space-between ambulance ampersand ampersands
amphora anchor angle antenna anvil aperture app-window-mac app-window apple archive-restore
archive-x archive armchair arrow-big-down-dash arrow-big-down arrow-big-left-dash arrow-big-left arrow-big-right-dash arrow-big-right arrow-big-up-dash
arrow-big-up arrow-down-0-1 arrow-down-1-0 arrow-down-a-z arrow-down-from-line arrow-down-left arrow-down-narrow-wide arrow-down-right arrow-down-to-dot arrow-down-to-line
arrow-down-up arrow-down-wide-narrow arrow-down-z-a arrow-down arrow-left-from-line arrow-left-right arrow-left-to-line arrow-left arrow-right-from-line arrow-right-left
arrow-right-to-line arrow-right arrow-up-0-1 arrow-up-1-0 arrow-up-a-z arrow-up-down arrow-up-from-dot arrow-up-from-line arrow-up-left arrow-up-narrow-wide
arrow-up-right arrow-up-to-line arrow-up-wide-narrow arrow-up-z-a arrow-up arrows-up-from-line asterisk astroid at-sign atom
audio-lines-off audio-lines-x audio-lines audio-waveform award axe axis-3d baby backpack badge-alert
badge-cent badge-check badge-dollar-sign badge-euro badge-indian-rupee badge-info badge-japanese-yen badge-minus badge-percent badge-plus
badge-pound-sterling badge-question-mark badge-russian-ruble badge-swiss-franc badge-turkish-lira badge-x badge baggage-claim balloon ban
banana bandage banknote-arrow-down banknote-arrow-up banknote-check banknote-x banknote barcode barrel baseline
bath battery-charging battery-full battery-low battery-medium battery-plus battery-warning battery beaker bean-off
bean bed-double bed-single bed beef-off beef beer-off beer bell-check bell-dot
bell-electric bell-minus bell-off bell-plus bell-ring bell between-horizontal-end between-horizontal-start between-vertical-end between-vertical-start
biceps-flexed bike binary binoculars biohazard bird birdhouse bitcoin blend blender
blinds blocks bluetooth-connected bluetooth-off bluetooth-searching bluetooth bold bolt bomb bone-fracture
bone book-a book-alert book-audio book-check book-copy book-dashed book-down book-headphones book-heart
book-image book-key book-lock book-marked book-minus book-open-check book-open-text book-open book-plus book-search
book-text book-type book-up-2 book-up book-user book-x book bookmark-check bookmark-minus bookmark-off
bookmark-plus bookmark-x bookmark boom-box bot-message-square bot-off bot bottle-wine bow-arrow box
boxes braces brackets brain-circuit brain-cog brain brick-wall-fire brick-wall-shield brick-wall briefcase-business
briefcase-conveyor-belt briefcase-medical briefcase bring-to-front broccoli broom-sparkles broom brush-cleaning brush bubbles
bug-off bug-play bug building-2 building bus-front bus cable-car cable cake-slice
cake calculator calendar-1 calendar-arrow-down calendar-arrow-up calendar-check-2 calendar-check calendar-clock calendar-cog calendar-days
calendar-fold calendar-heart calendar-minus-2 calendar-minus calendar-off calendar-plus-2 calendar-plus calendar-range calendar-search calendar-sync
calendar-x-2 calendar-x calendar calendars camera-off camera candy-cane candy-off candy cannabis-off
cannabis captions-off captions car-battery car-front car-taxi-front car caravan card-sim carrot
case-lower case-sensitive case-upper cassette-tape cast castle cat cctv-off cctv chart-area
chart-bar-big chart-bar-decreasing chart-bar-increasing chart-bar-stacked chart-bar chart-candlestick chart-column-big chart-column-decreasing chart-column-increasing chart-column-stacked
chart-column chart-gantt chart-line chart-network chart-no-axes-column-decreasing chart-no-axes-column-increasing chart-no-axes-column chart-no-axes-combined chart-no-axes-gantt chart-pie
chart-scatter chart-spline check-check check-line check chef-hat cherry chess-bishop chess-king chess-knight
chess-pawn chess-queen chess-rook chevron-down chevron-first chevron-last chevron-left chevron-right chevron-up chevrons-down-up
chevrons-down chevrons-left-right-ellipsis chevrons-left-right chevrons-left chevrons-right-left chevrons-right chevrons-up-down chevrons-up church cigarette-off
cigarette circle-alert circle-arrow-down circle-arrow-left circle-arrow-out-down-left circle-arrow-out-down-right circle-arrow-out-up-left circle-arrow-out-up-right circle-arrow-right circle-arrow-up
circle-check-big circle-check circle-chevron-down circle-chevron-left circle-chevron-right circle-chevron-up circle-dashed circle-divide circle-dollar-sign circle-dot-dashed
circle-dot circle-ellipsis circle-equal circle-euro circle-fading-arrow-up circle-fading-plus circle-gauge circle-minus circle-off circle-parking-off
circle-parking circle-pause circle-percent circle-pile circle-play circle-plus circle-pound-sterling circle-power circle-question-mark circle-slash-2
circle-slash circle-small circle-star circle-stop circle-user-round circle-user circle-x circle circuit-board citrus
clapperboard clipboard-check clipboard-clock clipboard-copy clipboard-list clipboard-minus clipboard-paste clipboard-pen-line clipboard-pen clipboard-plus
clipboard-type clipboard-x clipboard clock-1 clock-10 clock-11 clock-12 clock-2 clock-3 clock-4
clock-5 clock-6 clock-7 clock-8 clock-9 clock-alert clock-arrow-down clock-arrow-left clock-arrow-right clock-arrow-up
clock-check clock-fading clock-plus clock closed-caption cloud-alert cloud-backup cloud-check cloud-cog cloud-download
cloud-drizzle cloud-fog cloud-hail cloud-lightning cloud-moon-rain cloud-moon cloud-off cloud-rain-wind cloud-rain cloud-snow
cloud-sun-rain cloud-sun cloud-sync cloud-upload cloud cloudy clover club code-xml code
coffee cog coins columns-2 columns-3-cog columns-3 columns-4 combine command compass
component computer concierge-bell cone construction contact-round contact container contrast cookie
cooking-pot copy-check copy-minus copy-plus copy-slash copy-x copy copyleft copyright corner-down-left
corner-down-right corner-left-down corner-left-up corner-right-down corner-right-up corner-up-left corner-up-right cpu creative-commons credit-card
croissant crop cross crosshair crown cuboid cup-soda currency cylinder dam
database-arrow-down database-arrow-up database-backup database-check database-minus database-plus database-search database-x database-zap database
decimals-arrow-left decimals-arrow-right delete dessert diameter diamond-minus diamond-percent diamond-plus diamond dice-1
dice-2 dice-3 dice-4 dice-5 dice-6 dices diff disc-2 disc-3 disc-album
disc divide dna-off dna dock dog dollar-sign donut door-closed-locked door-closed
door-open dot download drafting-compass drama drill drone droplet-off droplet droplets
drum drumstick dumbbell ear-off ear earth-lock earth eclipse egg-fried egg-off
egg eject ellipse ellipsis-vertical ellipsis equal-approximately equal-not equal eraser ethernet-port
euro ev-charger expand external-link eye-closed eye-dashed eye-off eye face-angry face-expressionless
face-grinning face-neutral face-slightly-frowning face-slightly-smiling-plus face-slightly-smiling factory fan fast-forward feather fence
ferris-wheel file-archive file-axis-3d file-badge file-box file-braces-corner file-braces file-chart-column-increasing file-chart-column file-chart-line
file-chart-pie file-check-corner file-check file-clock file-code-corner file-code file-cog file-diff file-digit file-down
file-exclamation-point file-headphone file-heart file-image file-input file-key file-lock file-minus-corner file-minus file-music
file-output file-pen-line file-pen file-play file-plus-corner file-plus file-question-mark file-scan file-search-corner file-search
file-signal file-sliders file-spreadsheet file-stack file-symlink file-terminal file-text file-type-corner file-type file-up
file-user file-video-camera file-volume file-x-corner file-x file files film fingerprint-pattern fire-extinguisher
fish-off fish-symbol fish fishing-hook fishing-rod flag-off flag-triangle-left flag-triangle-right flag flame-kindling
flame flashlight-off flashlight flask-conical-off flask-conical flask-round flip-horizontal-2 flip-vertical-2 flower-2 flower
focus fold-horizontal fold-vertical folder-archive folder-bookmark folder-check folder-clock folder-closed folder-code folder-cog
folder-dot folder-down folder-git-2 folder-git folder-heart folder-input folder-kanban folder-key folder-lock folder-minus
folder-open-dot folder-open folder-output folder-pen folder-plus folder-root folder-search-2 folder-search folder-symlink folder-sync
folder-tree folder-up folder-x folder folders footprints forklift form forward frame
fuel fullscreen funnel-plus funnel-x funnel gallery-horizontal-end gallery-horizontal gallery-thumbnails gallery-vertical-end gallery-vertical
gamepad-2 gamepad-directional gamepad gauge gavel gem georgian-lari ghost gift git-branch-minus
git-branch-plus git-branch git-commit-horizontal git-commit-vertical git-compare-arrows git-compare git-fork git-graph git-merge-conflict git-merge
git-pull-request-arrow git-pull-request-closed git-pull-request-create-arrow git-pull-request-create git-pull-request-draft git-pull-request glass-water glasses globe-check globe-lock
globe-off globe-x globe goal gpu graduation-cap grape grid-2x2-check grid-2x2-plus grid-2x2-x
grid-2x2 grid-3x2 grid-3x3 grip-horizontal grip-vertical grip group guitar ham hamburger
hammer hand-coins hand-fist hand-grab hand-heart hand-helping hand-metal hand-platter hand handbag
handshake hard-drive-download hard-drive-upload hard-drive hard-hat hash hat-glasses haze hd hdmi-port
heading-1 heading-2 heading-3 heading-4 heading-5 heading-6 heading headphone-off headphones headset
heart-crack heart-handshake heart-minus heart-off heart-plus heart-pulse heart-x heart heater helicopter
hexagon highlighter hop-off hop hospital hotel hourglass house-heart house-plug house-plus
house-wifi house ice-cream-bowl ice-cream-cone id-card-lanyard id-card image-down image-minus image-off image-play
image-plus image-up image-upscale image images import inbox indian-rupee infinity info
inspection-panel italic iteration-ccw iteration-cw japanese-yen joystick kanban kayak key-round key-square
key keyboard-music keyboard-off keyboard lamp-ceiling lamp-desk lamp-floor lamp-wall-down lamp-wall-up lamp
land-plot landmark languages laptop-minimal-check laptop-minimal laptop lasso-select lasso layer-arrow-down layer-arrow-up
layers-2 layers-arrow-down layers-arrow-up layers-minus layers-plus layers layout-dashboard layout-freeform layout-grid layout-list
layout-panel-left layout-panel-top layout-template leaf leafy-green lectern lens-concave lens-convex library-big library
life-buoy ligature lightbulb-off lightbulb line-dot-right-horizontal line-squiggle line-style link-2-off link-2 link
list-check list-checks list-chevrons-down-up list-chevrons-up-down list-clock list-collapse list-end list-filter-plus list-filter list-indent-decrease
list-indent-increase list-minus list-music list-ordered list-plus list-restart list-sort-ascending list-sort-descending list-start list-todo
list-tree list-video list-x list loader-circle loader-pinwheel loader locate-fixed locate-off locate
lock-keyhole-open lock-keyhole lock-open lock log-in log-out logs lollipop luggage magnet
mail-badge mail-check mail-minus mail-open mail-plus mail-question-mark mail-search mail-warning mail-x mail
mailbox mails map-minus map-pin-check-inside map-pin-check map-pin-house map-pin-minus-inside map-pin-minus map-pin-off map-pin-pen
map-pin-plus-inside map-pin-plus map-pin-search map-pin-x-inside map-pin-x map-pin map-pinned map-plus map mars-stroke
mars martini maximize-2 maximize medal megaphone-off megaphone memory-stick menu merge
message-circle-check message-circle-code message-circle-dashed message-circle-heart message-circle-more message-circle-off message-circle-plus message-circle-question-mark message-circle-reply message-circle-warning
message-circle-x message-circle message-square-check message-square-code message-square-dashed message-square-diff message-square-dot message-square-heart message-square-lock message-square-more
message-square-off message-square-plus message-square-quote message-square-reply message-square-share message-square-text message-square-warning message-square-x message-square messages-square
metronome mic-audio-lines mic-off mic-signal mic-vocal mic microchip microscope microwave midi-port
milestone milk-off milk minimize-2 minimize minus mirror-rectangular mirror-round monitor-check monitor-cloud
monitor-cog monitor-dot monitor-down monitor-off monitor-pause monitor-play monitor-smartphone monitor-speaker monitor-stop monitor-up
monitor-x monitor moon-star moon mop-sparkles mop mosque motorbike mountain-snow mountain
mouse-left mouse-off mouse-pointer-2-off mouse-pointer-2 mouse-pointer-ban mouse-pointer-click mouse-pointer mouse-right mouse move-3d
move-diagonal-2 move-diagonal move-down-left move-down-right move-down move-horizontal move-left move-right move-up-left move-up-right
move-up move-vertical move music-2 music-3 music-4 music navigation-2-off navigation-2 navigation-off
navigation network newspaper nfc non-binary notebook-pen notebook-tabs notebook-text notebook notepad-text-dashed
notepad-text nut-off nut octagon-alert octagon-minus octagon-pause octagon-x octagon omega option
orbit origami package-2 package-check package-minus package-open package-plus package-search package-x package
paint-bucket paint-roller paintbrush-vertical paintbrush palette panda panel-bottom-close panel-bottom-dashed panel-bottom-open panel-bottom
panel-left-close panel-left-dashed panel-left-open panel-left-right-dashed panel-left panel-right-close panel-right-dashed panel-right-open panel-right panel-top-bottom-dashed
panel-top-close panel-top-dashed panel-top-open panel-top panels-left-bottom panels-right-bottom panels-top-left paper-bag paperclip parasol
parentheses parking-meter party-popper pause paw-print pc-case pen-line pen-off pen-tool pen
pencil-line pencil-off pencil-ruler pencil-sparkles pencil pentagon percent person-standing phi philippine-peso
phone-call phone-forwarded phone-incoming phone-missed phone-off phone-outgoing phone pi piano pickaxe
picture-in-picture-2 picture-in-picture piggy-bank pilcrow-left pilcrow-right pilcrow pill-bottle pill pin-off pin
pipette pizza plane-landing plane-takeoff plane play-off play plug-2 plug-zap plug
plus pocket-knife podium pointer-off pointer popcorn popsicle pound-sterling power-off power
presentation printer-check printer-x printer projector proportions puzzle pyramid qr-code quote
rabbit radar radiation radical radio-off radio-receiver radio-tower radio radius rainbow
rat ratio receipt-cent receipt-euro receipt-indian-rupee receipt-japanese-yen receipt-pound-sterling receipt-russian-ruble receipt-swiss-franc receipt-text
receipt-turkish-lira receipt rectangle-circle rectangle-ellipsis rectangle-goggles rectangle-horizontal rectangle-vertical recycle redo-2 redo-dot
redo refresh-ccw-dot refresh-ccw refresh-cw-off refresh-cw refrigerator regex remove-formatting repeat-1 repeat-2
repeat-off repeat replace-all replace reply-all reply rewind ribbon road rocket
rocking-chair roller-coaster rose rotate-3d rotate-ccw-clock rotate-ccw-key rotate-ccw-square rotate-ccw rotate-cw-fading-clock rotate-cw-square
rotate-cw route-off route router rows-2 rows-3 rows-4 rss ruler-dimension-line ruler
russian-ruble sailboat salad sandwich satellite-dish satellite saudi-riyal save-all save-check save-off
save-pen save-plus save scale-3d scale scaling scan-barcode scan-box scan-eye scan-face
scan-heart scan-line scan-qr-code scan-search scan-square scan-text scan school scissors-line-dashed scissors
scooter screen-share-off screen-share scroll-text scroll search-alert search-check search-code search-slash search-x
search section send-horizontal send-to-back send separator-horizontal separator-vertical server-cog server-crash server-off
server-plus server settings-2 settings shapes share-2 share sheet shell shelving-unit
shield-alert shield-ban shield-check shield-cog-corner shield-cog shield-ellipsis shield-half shield-keyhole shield-lock shield-minus
shield-off shield-plus shield-question-mark shield-user shield-x shield ship-wheel ship shirt shopping-bag
shopping-basket shopping-cart shovel shower-head shredder shrimp shrink shrub shuffle sigma
signal-high signal-low signal-medium signal-zero signal signature signpost-big signpost siren skip-back
skip-forward skull slash slice sliders-horizontal sliders-vertical smartphone-charging smartphone-nfc smartphone snail
snowflake soap-dispenser-droplet sofa solar-panel soup space spade sparkle sparkles speaker
speech spell-check-2 spell-check spline-pointer spline split spool sport-shoe spotlight spray-can
sprout square-activity square-arrow-down-left square-arrow-down-right square-arrow-down square-arrow-left square-arrow-out-down-left square-arrow-out-down-right square-arrow-out-up-left square-arrow-out-up-right
square-arrow-right-enter square-arrow-right-exit square-arrow-right square-arrow-up-left square-arrow-up-right square-arrow-up square-asterisk square-bottom-dashed-scissors square-centerline-dashed-horizontal square-centerline-dashed-vertical
square-chart-gantt square-check-big square-check square-chevron-down square-chevron-left square-chevron-right square-chevron-up square-code square-dashed-bottom-code square-dashed-bottom
square-dashed-kanban square-dashed-mouse-pointer square-dashed-text square-dashed-top-solid square-dashed square-dimensions square-divide square-dot square-equal square-function
square-kanban square-library square-m square-menu square-minus square-mouse-pointer square-off square-parking-off square-parking square-pause
square-pen square-percent square-pi square-pilcrow square-play square-plus square-power square-radical square-round-corner square-scissors
square-sigma square-slash square-split-horizontal square-split-vertical square-square square-stack square-star square-stop square-terminal square-text
square-user-round square-user square-x square squares-exclude squares-intersect squares-subtract squares-unite squircle-dashed squircle
squirrel stamp star-check star-half star-minus star-off star-plus star-x star step-back
step-forward stethoscope sticker sticky-note-check sticky-note-minus sticky-note-off sticky-note-plus sticky-note-x sticky-note sticky-notes
stone store stretch-horizontal stretch-vertical strikethrough subscript summary sun-dim sun-medium sun-moon
sun-snow sun sunrise sunset superscript swatch-book swiss-franc switch-camera sword swords
syringe table-2 table-cells-merge table-cells-split table-columns-split table-of-contents table-properties table-rows-split table tablet-smartphone
tablet tablets tag-plus tag-x tag tags tally-1 tally-2 tally-3 tally-4
tally-5 tangent target telescope tent-tree tent terminal test-tube-diagonal test-tube test-tubes
text-align-center text-align-end text-align-justify text-align-start text-cursor-input text-cursor text-initial text-quote text-search text-wrap
theater thermometer-snowflake thermometer-sun thermometer thumbs-down thumbs-up ticket-check ticket-minus ticket-percent ticket-plus
ticket-slash ticket-x ticket tickets-plane tickets timeline timer-off timer-reset timer toggle-left
toggle-right toilet tool-case toolbox tornado torus touchpad-off touchpad towel-rack tower-control
toy-brick tractor traffic-cone train-front-tunnel train-front train-track tram-front transgender trash-2 trash
tree-deciduous tree-palm tree-pine trees trending-down trending-up-down trending-up triangle-alert triangle-dashed triangle-right
triangle trophy truck-electric truck turkish-lira turntable turtle tv-minimal-play tv-minimal tv
type-outline type umbrella-off umbrella underline undo-2 undo-dot undo unfold-horizontal unfold-vertical
ungroup university unlink-2 unlink unplug upload usb-c-port usb user-check user-cog
user-key user-lock user-minus user-pen user-plus user-round-arrow-left user-round-check user-round-cog user-round-key user-round-minus
user-round-pen user-round-plus user-round-search user-round-x user-round user-search user-shield user-star user-x user
users-round users utensils-crossed utensils utility-pole van variable vault vector-square vegan
venetian-mask venus-and-mars venus vibrate-off vibrate video-off video videotape view voicemail
volleyball volume-1 volume-2 volume-off volume-x volume vote wallet-cards wallet-minimal wallet
wallpaper wand-sparkles wand warehouse washing-machine watch waves-arrow-down waves-arrow-up waves-horizontal waves-ladder
waves-vertical waypoints webcam-off webcam webhook-off webhook weight-tilde weight wheat-off wheat
whole-word wifi-cog wifi-high wifi-low wifi-off wifi-pen wifi-sync wifi-zero wifi wind-arrow-down
wind wine-off wine workflow worm wrench-off wrench x-line-top x zap-off
zap zodiac-aquarius zodiac-aries zodiac-cancer zodiac-capricorn zodiac-gemini zodiac-leo zodiac-libra zodiac-ophiuchus zodiac-pisces
zodiac-sagittarius zodiac-scorpio zodiac-taurus zodiac-virgo zoom-in zoom-out
]==]
)

end)() end,
    [65] = function()local wax,script,require=ImportGlobals(65)local ImportGlobals return (function(...)
-- [[material icon pack.]]
-- [[Compact catalog: names only. PNG URLs are derived on demand by]]
-- [[icons/packBuilder (kebab/snake name to PascalCase), so this module]]
-- [[parses as one string constant instead of thousands of table]]
-- [[constructors. Regenerate with scripts/generate_icon_packs.js.]]

return require(script.Parent.packBuilder).create(
	"assets/icons/material-pack/white/",
	"material",
[==[
ac_unit access_alarm access_alarms access_time accessibility accessibility_new accessible accessible_forward account_balance account_balance_wallet
account_box account_circle account_tree adb add add_a_photo add_alarm add_alert add_box add_circle
add_circle_outline add_comment add_location add_photo_alternate add_shopping_cart add_to_home_screen add_to_photos add_to_queue adjust airline_seat_flat
airline_seat_flat_angled airline_seat_individual_suite airline_seat_legroom_extra airline_seat_legroom_normal airline_seat_legroom_reduced airline_seat_recline_extra airline_seat_recline_normal airplanemode_active airplanemode_inactive airplay
airport_shuttle alarm alarm_add alarm_off alarm_on album all_inbox all_inclusive all_out alternate_email
amp_stories android announcement apartment apps archive arrow_back arrow_back_ios arrow_downward arrow_drop_down
arrow_drop_down_circle arrow_drop_up arrow_forward arrow_forward_ios arrow_left arrow_right arrow_right_alt arrow_upward art_track aspect_ratio
assessment assignment assignment_ind assignment_late assignment_return assignment_returned assignment_turned_in assistant assistant_photo atm
attach_file attach_money attachment audiotrack autorenew av_timer backspace backup ballot bar_chart
barcode bathtub battery_20 battery_30 battery_50 battery_60 battery_80 battery_90 battery_alert battery_charging_20
battery_charging_30 battery_charging_50 battery_charging_60 battery_charging_80 battery_charging_90 battery_charging_full battery_full battery_std battery_unknown beach_access
beenhere block bluetooth bluetooth_audio bluetooth_connected bluetooth_disabled bluetooth_searching blur_circular blur_linear blur_off
blur_on book bookmark bookmark_border bookmarks border_all border_bottom border_clear border_color border_horizontal
border_inner border_left border_outer border_right border_style border_top border_vertical branding_watermark brightness_1 brightness_2
brightness_3 brightness_4 brightness_5 brightness_6 brightness_7 brightness_auto brightness_high brightness_low brightness_medium broken_image
brush bubble_chart bug_report build burst_mode business business_center cached cake calendar_today
calendar_view_day call call_end call_made call_merge call_missed call_missed_outgoing call_received call_split call_to_action
camera camera_alt camera_enhance camera_front camera_rear camera_roll cancel cancel_presentation cancel_schedule_send card_giftcard
card_membership card_travel casino cast cast_connected cast_for_education category cell_wifi center_focus_strong center_focus_weak
change_history chat chat_bubble chat_bubble_outline check check_box check_box_outline_blank check_circle check_circle_outline chevron_left
chevron_right child_care child_friendly chrome_reader_mode class clear clear_all close closed_caption cloud
cloud_circle cloud_done cloud_download cloud_off cloud_queue cloud_upload code collections collections_bookmark color_lens
colorize comment commute compare compare_arrows compass_calibration computer confirmation_number contact_mail contact_phone
contact_support contactless contacts content_copy content_cut content_paste control_camera control_point control_point_duplicate copyright
create create_new_folder credit_card crop crop_16_9 crop_3_2 crop_5_4 crop_7_5 crop_din crop_free
crop_landscape crop_original crop_portrait crop_rotate crop_square dashboard data_usage date_range deck dehaze
delete delete_forever delete_outline delete_sweep departure_board description desktop_access_disabled desktop_mac desktop_windows details
developer_board developer_mode device_hub device_unknown devices devices_other dialer_sip dialpad directions directions_bike
directions_boat directions_bus directions_car directions_railway directions_run directions_subway directions_transit directions_walk disc_full divide
dns do_not_disturb do_not_disturb_alt do_not_disturb_off dock domain domain_disabled done done_all done_outline
donut_large donut_small double_arrow drafts drag_handle drag_indicator drive_eta duo dvr dynamic_feed
eco edit edit_attributes edit_location eject email emoji_emotions emoji_events emoji_flags emoji_food_beverage
emoji_nature emoji_objects emoji_people emoji_symbols emoji_transportation enhanced_encryption equalizer equals error error_outline
euro euro_symbol ev_station event event_available event_busy event_note event_seat exit_to_app expand_less
expand_more explicit explore explore_off exposure exposure_neg_1 exposure_neg_2 exposure_plus_1 exposure_plus_2 exposure_zero
extension face fast_forward fast_rewind fastfood favorite favorite_border featured_play_list featured_video feedback
fiber_dvr fiber_manual_record fiber_new fiber_pin fiber_smart_record file_copy file_upload filter filter_1 filter_2
filter_3 filter_4 filter_5 filter_6 filter_7 filter_8 filter_9 filter_9_plus filter_b_and_w filter_center_focus
filter_drama filter_frames filter_hdr filter_list filter_none filter_tilt_shift filter_vintage find_in_page find_replace fingerprint
fireplace first_page fitness_center flag flare flash_auto flash_off flash_on flight flight_land
flight_takeoff flip flip_camera_android flip_camera_ios flip_to_back flip_to_front folder folder_open folder_shared folder_special
font_download format_align_center format_align_justify format_align_left format_align_right format_bold format_clear format_color_fill format_color_reset format_color_text
format_indent_decrease format_indent_increase format_italic format_line_spacing format_list_bulleted format_list_numbered format_list_numbered_rtl format_paint format_quote format_shapes
format_size format_strikethrough format_textdirection_l_to_r format_textdirection_r_to_l format_underlined forum forward forward_10 forward_30 forward_5
free_breakfast fullscreen fullscreen_exit functions g_translate gamepad games gavel gesture get_app
gif golf_course gps_fixed gps_not_fixed gps_off grade gradient grain graphic_eq greater_than
greater_than_equal grid_off grid_on group group_add group_work hd hdr_off hdr_on hdr_strong
hdr_weak headset headset_mic healing hearing height help help_outline high_quality highlight
highlight_off history home home_work horizontal_split hot_tub hotel hourglass_empty hourglass_full house
how_to_reg how_to_vote http https image image_aspect_ratio image_search import_contacts import_export important_devices
inbox indeterminate_check_box info input insert_chart insert_chart_outlined insert_comment insert_drive_file insert_emoticon insert_invitation
insert_link insert_photo invert_colors invert_colors_off iso keyboard keyboard_arrow_down keyboard_arrow_left keyboard_arrow_right keyboard_arrow_up
keyboard_backspace keyboard_capslock keyboard_hide keyboard_return keyboard_tab keyboard_voice king_bed kitchen label label_important
label_off landscape language laptop laptop_chromebook laptop_mac laptop_windows last_page launch layers
layers_clear leak_add leak_remove lens less_than less_than_equal library_add library_books library_music lightbulb
line_style line_weight linear_scale link link_off linked_camera list list_alt live_help live_tv
local_activity local_airport local_atm local_bar local_cafe local_car_wash local_convenience_store local_dining local_drink local_florist
local_gas_station local_grocery_store local_hospital local_hotel local_laundry_service local_library local_mall local_movies local_offer local_parking
local_pharmacy local_phone local_pizza local_play local_post_office local_printshop local_see local_shipping local_taxi location_city
location_disabled location_off location_on location_searching lock lock_open log_in log_out looks looks_3
looks_4 looks_5 looks_6 looks_one looks_two loop loupe low_priority loyalty mail
mail_outline map markunread markunread_mailbox maximize meeting_room memory menu menu_book menu_open
merge_type message mic mic_none mic_off minimize minus missed_video_call mms mobile_friendly
mobile_off mobile_screen_share mode_comment monetization_on money money_off monochrome_photos mood mood_bad more
more_horiz more_vert motorcycle mouse move_to_inbox movie movie_creation movie_filter multiline_chart museum
music_note music_off music_video my_location nature nature_people navigate_before navigate_next navigation near_me
network_cell network_check network_locked network_wifi new_releases next_week nfc nights_stay no_encryption no_meeting_room
no_sim not_equal not_interested not_listed_location note note_add notes notification_important notifications notifications_active
notifications_none notifications_off notifications_paused offline_bolt offline_pin ondemand_video opacity open_in_browser open_in_new open_with
outdoor_grill outlined_flag pages pageview palette pan_tool panorama panorama_fish_eye panorama_horizontal panorama_vertical
panorama_wide_angle party_mode pause pause_circle_filled pause_circle_outline pause_presentation payment people people_alt people_outline
percentage perm_camera_mic perm_contact_calendar perm_data_setting perm_device_information perm_identity perm_media perm_phone_msg perm_scan_wifi person
person_add person_add_disabled person_outline person_pin person_pin_circle personal_video pets phone phone_android phone_bluetooth_speaker
phone_callback phone_disabled phone_enabled phone_forwarded phone_in_talk phone_iphone phone_locked phone_missed phone_paused phonelink
phonelink_erase phonelink_lock phonelink_off phonelink_ring phonelink_setup photo photo_album photo_camera photo_filter photo_library
photo_size_select_actual photo_size_select_large photo_size_select_small picture_as_pdf picture_in_picture picture_in_picture_alt pie_chart pin pin_drop pin_off
place play_arrow play_circle_filled play_circle_filled_white play_circle_outline play_for_work playlist_add playlist_add_check playlist_play plus
plus_minus plus_minus_alt plus_one policy poll polymer pool portable_wifi_off portrait post_add
power power_input power_off power_settings_new pregnant_woman present_to_all print print_disabled priority_high public
publish qrcode query_builder question_answer queue queue_music queue_play_next radio radio_button_checked radio_button_unchecked
rate_review receipt recent_actors record_voice_over redeem redo refresh remove remove_circle remove_circle_outline
remove_from_queue remove_red_eye remove_shopping_cart reorder repeat repeat_one replay replay_10 replay_30 replay_5
reply reply_all report report_off report_problem restaurant restaurant_menu restore restore_from_trash restore_page
ring_volume rocket room room_service rotate_90_degrees_ccw rotate_left rotate_right rounded_corner router rowing
rss_feed rv_hookup satellite save save_alt scanner scatter_plot schedule school score
screen_lock_landscape screen_lock_portrait screen_lock_rotation screen_rotation screen_share sd_card sd_storage search security select_all
send sentiment_dissatisfied sentiment_neutral sentiment_satisfied sentiment_satisfied_alt sentiment_slightly_dissatisfied sentiment_very_dissatisfied sentiment_very_satisfied settings settings_applications
settings_backup_restore settings_bluetooth settings_brightness settings_cell settings_ethernet settings_input_antenna settings_input_component settings_input_composite settings_input_hdmi settings_input_svideo
settings_overscan settings_phone settings_power settings_remote settings_system_daydream settings_voice share shop shop_two shopping_basket
shopping_cart short_text show_chart shuffle shutter_speed signal_cellular_0_bar signal_cellular_1_bar signal_cellular_2_bar signal_cellular_3_bar signal_cellular_4_bar
signal_cellular_alt signal_cellular_connected_no_internet_0_bar signal_cellular_connected_no_internet_1_bar signal_cellular_connected_no_internet_2_bar signal_cellular_connected_no_internet_3_bar signal_cellular_connected_no_internet_4_bar signal_cellular_no_sim signal_cellular_null signal_cellular_off signal_wifi_0_bar
signal_wifi_1_bar signal_wifi_1_bar_lock signal_wifi_2_bar signal_wifi_2_bar_lock signal_wifi_3_bar signal_wifi_3_bar_lock signal_wifi_4_bar signal_wifi_4_bar_lock signal_wifi_off sim_card
sim_card_alert single_bed skip_next skip_previous slideshow slow_motion_video smartphone smoke_free smoking_rooms sms
sms_failed snooze sort sort_by_alpha spa space_bar speaker speaker_group speaker_notes speaker_notes_off
speaker_phone speed spellcheck sports sports_baseball sports_basketball sports_cricket sports_esports sports_football sports_golf
sports_handball sports_hockey sports_kabaddi sports_mma sports_motorsports sports_rugby sports_soccer sports_tennis sports_volleyball square_foot
star star_border star_half star_rate stars stay_current_landscape stay_current_portrait stay_primary_landscape stay_primary_portrait stop
stop_circle stop_screen_share storage store store_mall_directory storefront straighten streetview strikethrough_s style
subdirectory_arrow_left subdirectory_arrow_right subject subscriptions subtitles subway supervised_user_circle supervisor_account surround_sound swap_calls
swap_horiz swap_horizontal_circle swap_vert swap_vertical_circle switch_camera switch_video sync sync_alt sync_disabled sync_problem
system_update system_update_alt tab tab_unselected table_chart tablet tablet_android tablet_mac tag_faces tap_and_play
terrain text_fields text_format text_rotate_up text_rotate_vertical text_rotation_angledown text_rotation_angleup text_rotation_down text_rotation_none textsms
texture theaters thumb_down thumb_down_alt thumb_up thumb_up_alt thumbs_up_down time_to_leave timelapse timeline
timer timer_10 timer_3 timer_off title toc today toggle_off toggle_on toll
tonality touch_app toys track_changes traffic train tram transfer_within_a_station transform transit_enterexit
translate trending_down trending_flat trending_up trip_origin tune turned_in turned_in_not tv tv_off
unarchive undo unfold_less unfold_more unsubscribe update usb verified_user vertical_align_bottom vertical_align_center
vertical_align_top vertical_split vibration video_call video_label video_library videocam videocam_off videogame_asset view_agenda
view_array view_carousel view_column view_comfy view_compact view_day view_headline view_list view_module view_quilt
view_stream view_week vignette visibility visibility_off voice_chat voice_over_off voicemail volume_down volume_mute
volume_off volume_up vpn_key vpn_lock wallpaper warning watch watch_later waves wb_auto
wb_cloudy wb_incandescent wb_iridescent wb_sunny wc web web_asset weekend whatshot where_to_vote
widgets wifi wifi_lock wifi_off wifi_tethering work work_off work_outline wrap_text youtube_searched_for
zoom_in zoom_out zoom_out_map
]==]
)

end)() end,
    [66] = function()local wax,script,require=ImportGlobals(66)local ImportGlobals return (function(...)
-- [[Lazy icon-pack builder.
-- [[]]
-- [[A pack is a compact newline-separated list of kebab names plus the shared
-- [[URL prefix. Nothing is expanded up front:
-- [[]]
-- [[]  * `data[name]` computes the PNG URL on the first lookup and caches it,
-- [[    so a startup that shows twenty icons builds twenty strings instead of
-- [[    1,700-5,100 strings for a pack's full catalog;
-- [[]  * names that are not in the pack resolve to nil without being cached,
-- [[    so the resolver's "try the next pack" search keeps working;
-- [[]  * `__iter` enumerates the catalog (Icons.list / Icons.count), building
-- [[    each URL as it is asked for;
-- [[]  * the pack's table is a plain table with a metatable, so it behaves
-- [[    exactly like the old eagerly expanded table to every caller.
-- [[]]
-- [[The PNG file base is the kebab name converted to PascalCase, splitting on
-- [["-" and "_" (material uses underscores, heroicons mixes both). Every one
-- [[of the catalog's entries is machine-verified to follow this rule, so the
-- [[value column the old tables carried is derived instead of stored, which
-- [[removes thousands of table constructors from the bundle's compile path.
-- ]]

local PackBuilder = {}

local sub = string.sub
local lower = string.lower
local gmatch = string.gmatch
local gsub = string.gsub
local upper = string.upper

-- [[kebab/snake name -> PascalCase file base: "a-b-2" -> "AB2",
-- [["x-mark_solid" -> "XMarkSolid". Only ASCII icon names are catalogued.]]
local function pascalCase(name: string): string
	-- [[Each "-x"/"_x" segment loses its separator and capitalises the letter.]]
	local base = gsub(name, "[-_](%w)", function(letter)
		return upper(letter)
	end)
	-- [[A leading lowercase letter is the first Pascal segment.]]
	local first = sub(base, 1, 1)
	local lowered = lower(first)
	if first ~= lowered then
		return base
	end
	return upper(first) .. sub(base, 2)
end

-- [[Build a lazy pack table. `prefix` ends in "/", `packName` is the segment
-- [[used in the legacy URL layout (`.../<lowerFirst>/<pack><Name>.png`).]]
function PackBuilder.create(prefix: string, packName: string, keyList: string): { [string]: string }
	-- [[Membership + stable enumeration order, built once when the pack module
	-- [[is first required. These are short string inserts only - the long URL
	-- [[concatenations the old expand() did per entry happen lazily below.]]
	local keys = {}
	local known = {}
	for key in gmatch(keyList, "%S+") do
		if not known[key] then
			known[key] = true
			keys[#keys + 1] = key
		end
	end

	local data = {}

	return setmetatable(data, {
		__index = function(_, key: string)
			if not known[key] then
				return nil
			end
			local fileBase = pascalCase(key)
			local url = prefix .. lower(sub(fileBase, 1, 1)) .. "/" .. packName .. fileBase .. ".png"
			-- [[Cache the answer so repeated lookups skip this closure.]]
			rawset(data, key, url)
			return url
		end,
		__iter = function()
			local index = 0
			return function()
				index += 1
				local key = keys[index]
				if key == nil then
					return nil
				end
				-- [[Second value is the resolved URL, matching a normal
				-- [[key/value traversal of the old expanded table.]]
				return key, data[key]
			end
		end,
	}) :: { [string]: string }
end

return PackBuilder

end)() end,
    [67] = function()local wax,script,require=ImportGlobals(67)local ImportGlobals return (function(...)
-- [[phosphor icon pack.]]
-- [[Compact catalog: names only. PNG URLs are derived on demand by]]
-- [[icons/packBuilder (kebab/snake name to PascalCase), so this module]]
-- [[parses as one string constant instead of thousands of table]]
-- [[constructors. Regenerate with scripts/generate_icon_packs.js.]]

return require(script.Parent.packBuilder).create(
	"assets/icons/phosphor-pack/",
	"phosphor",
[==[
acorn address-book-tabs address-book air-traffic-control airplane-in-flight airplane-landing airplane-takeoff airplane-taxiing airplane-tilt airplane
airplay alarm alien align-bottom-simple align-bottom align-center-horizontal-simple align-center-horizontal align-center-vertical-simple align-center-vertical align-left-simple
align-left align-right-simple align-right align-top-simple align-top amazon-logo ambulance anchor-simple anchor android-logo
angle angular-logo aperture app-store-logo app-window apple-logo apple-podcasts-logo approximate-equals archive armchair
arrow-arc-left arrow-arc-right arrow-bend-double-up-left arrow-bend-double-up-right arrow-bend-down-left arrow-bend-down-right arrow-bend-left-down arrow-bend-left-up arrow-bend-right-down arrow-bend-right-up
arrow-bend-up-left arrow-bend-up-right arrow-circle-down-left arrow-circle-down-right arrow-circle-down arrow-circle-left arrow-circle-right arrow-circle-up-left arrow-circle-up-right arrow-circle-up
arrow-clockwise arrow-counter-clockwise arrow-down-left arrow-down-right arrow-down arrow-elbow-down-left arrow-elbow-down-right arrow-elbow-left-down arrow-elbow-left-up arrow-elbow-left
arrow-elbow-right-down arrow-elbow-right-up arrow-elbow-right arrow-elbow-up-left arrow-elbow-up-right arrow-fat-down arrow-fat-left arrow-fat-line-down arrow-fat-line-left arrow-fat-line-right
arrow-fat-line-up arrow-fat-lines-down arrow-fat-lines-left arrow-fat-lines-right arrow-fat-lines-up arrow-fat-right arrow-fat-up arrow-left arrow-line-down-left arrow-line-down-right
arrow-line-down arrow-line-left arrow-line-right arrow-line-up-left arrow-line-up-right arrow-line-up arrow-right arrow-square-down-left arrow-square-down-right arrow-square-down
arrow-square-in arrow-square-left arrow-square-out arrow-square-right arrow-square-up-left arrow-square-up-right arrow-square-up arrow-u-down-left arrow-u-down-right arrow-u-left-down
arrow-u-left-up arrow-u-right-down arrow-u-right-up arrow-u-up-left arrow-u-up-right arrow-up-left arrow-up-right arrow-up arrows-clockwise arrows-counter-clockwise
arrows-down-up arrows-horizontal arrows-in-cardinal arrows-in-line-horizontal arrows-in-line-vertical arrows-in-simple arrows-in arrows-left-right arrows-merge arrows-out-cardinal
arrows-out-line-horizontal arrows-out-line-vertical arrows-out-simple arrows-out arrows-split arrows-vertical article-medium article-ny-times article asclepius
asterisk-simple asterisk at atom avocado axe baby-carriage baby backpack backspace
bag-simple bag balloon bandaids bank barbell barcode barn barricade baseball-cap
baseball-helmet baseball basket basketball bathtub battery-charging-vertical battery-charging battery-empty battery-full battery-high
battery-low battery-medium battery-plus-vertical battery-plus battery-vertical-empty battery-vertical-full battery-vertical-high battery-vertical-low battery-vertical-medium battery-warning-vertical
battery-warning beach-ball beanie bed beer-bottle beer-stein behance-logo bell-ringing bell-simple-ringing bell-simple-slash
bell-simple-z bell-simple bell-slash bell-z bell belt bezier-curve bicycle binary binoculars
biohazard bird blueprint bluetooth-connected bluetooth-slash bluetooth-x bluetooth boat bomb bone
book-bookmark book-open-text book-open-user book-open book bookmark-simple bookmark bookmarks-simple bookmarks books
boot boules bounding-box bowl-food bowl-steam bowling-ball box-arrow-down box-arrow-up boxing-glove brackets-angle
brackets-curly brackets-round brackets-square brain brandy bread bridge briefcase-metal briefcase broadcast
broom browser browsers bug-beetle bug-droid bug building-apartment building-office building buildings
bulldozer bus butterfly cable-car cactus cake calculator calendar-blank calendar-check calendar-dot
calendar-dots calendar-heart calendar-minus calendar-plus calendar-slash calendar-star calendar-x calendar call-bell camera-plus
camera-rotate camera-slash camera campfire car-battery car-profile car-simple car cardholder cards-three
cards caret-circle-double-down caret-circle-double-left caret-circle-double-right caret-circle-double-up caret-circle-down caret-circle-left caret-circle-right caret-circle-up-down caret-circle-up
caret-double-down caret-double-left caret-double-right caret-double-up caret-down caret-left caret-line-down caret-line-left caret-line-right caret-line-up
caret-right caret-up-down caret-up carrot cash-register cassette-tape castle-turret cat cell-signal-full cell-signal-high
cell-signal-low cell-signal-medium cell-signal-none cell-signal-slash cell-signal-x cell-tower certificate chair chalkboard-simple chalkboard-teacher
chalkboard champagne charging-station chart-bar-horizontal chart-bar chart-donut chart-line-down chart-line-up chart-line chart-pie-slice
chart-pie chart-polar chart-scatter chat-centered-dots chat-centered-slash chat-centered-text chat-centered chat-circle-dots chat-circle-slash chat-circle-text
chat-circle chat-dots chat-slash chat-teardrop-dots chat-teardrop-slash chat-teardrop-text chat-teardrop chat-text chat chats-circle
chats-teardrop chats check-circle check-fat check-square-offset check-square check checkerboard checks cheers
cheese chef-hat cherries church cigarette-slash cigarette circle-dashed circle-half-tilt circle-half circle-notch
circle circles-four circles-three-plus circles-three circuitry city clipboard-text clipboard clock-afternoon clock-clockwise
clock-countdown clock-counter-clockwise clock-user clock closed-captioning cloud-arrow-down cloud-arrow-up cloud-check cloud-fog cloud-lightning
cloud-moon cloud-rain cloud-slash cloud-snow cloud-sun cloud-warning cloud-x cloud clover club
coat-hanger coda-logo code-block code-simple code codepen-logo codesandbox-logo coffee-bean coffee coin-vertical
coin coins columns-plus-left columns-plus-right columns command compass-rose compass-tool compass computer-tower
confetti contactless-payment control cookie cooking-pot copy-simple copy copyleft copyright corners-in
corners-out couch court-basketball cow cowboy-hat cpu crane-tower crane credit-card cricket
crop cross crosshair-simple crosshair crown-cross crown-simple crown cube-focus cube-transparent cube
currency-btc currency-circle-dollar currency-cny currency-dollar-simple currency-dollar currency-eth currency-eur currency-gbp currency-inr currency-jpy
currency-krw currency-kzt currency-ngn currency-rub cursor-click cursor-text cursor cylinder database desk
desktop-tower desktop detective dev-to-logo device-mobile-camera device-mobile-slash device-mobile-speaker device-mobile device-rotate device-tablet-camera
device-tablet-speaker device-tablet devices diamond diamonds-four dice-five dice-four dice-one dice-six dice-three
dice-two disc disco-ball discord-logo divide dna dog door-open door dot-outline
dot dots-nine dots-six-vertical dots-six dots-three-circle-vertical dots-three-circle dots-three-outline-vertical dots-three-outline dots-three-vertical dots-three
download-simple download dress dresser dribbble-logo drone drop-half-bottom drop-half drop-simple drop-slash
drop dropbox-logo ear-slash ear egg-crack egg eject-simple eject elevator empty
engine envelope-open envelope-simple-open envelope-simple envelope equalizer equals eraser escalator-down escalator-up
exam exclamation-mark exclude-square exclude export eye-closed eye-slash eye eyedropper-sample eyedropper
eyeglasses eyes face-mask facebook-logo factory faders-horizontal faders fallout-shelter fan farm
fast-forward-circle fast-forward feather fediverse-logo figma-logo file-archive file-arrow-down file-arrow-up file-audio file-c-sharp
file-c file-cloud file-code file-cpp file-css file-csv file-dashed file-doc file-html file-image
file-ini file-jpg file-js file-jsx file-lock file-magnifying-glass file-md file-minus file-pdf file-plus
file-png file-ppt file-py file-rs file-sql file-svg file-text file-ts file-tsx file-txt
file-video file-vue file-x file-xls file-zip file files film-reel film-script film-slate
film-strip fingerprint-simple fingerprint finn-the-human fire-extinguisher fire-simple fire-truck fire first-aid-kit first-aid
fish-simple fish flag-banner-fold flag-banner flag-checkered flag-pennant flag flame flashlight flask
flip-horizontal flip-vertical floppy-disk-back floppy-disk flow-arrow flower-lotus flower-tulip flower flying-saucer folder-dashed
folder-lock folder-minus folder-open folder-plus folder-simple-dashed folder-simple-lock folder-simple-minus folder-simple-plus folder-simple-star folder-simple-user
folder-simple folder-star folder-user folder folders football-helmet football footprints fork-knife four-k
frame-corners framer-logo function funnel-simple-x funnel-simple funnel-x funnel game-controller garage gas-can
gas-pump gauge gavel gear-fine gear-six gear gender-female gender-intersex gender-male gender-neuter
gender-nonbinary gender-transgender ghost gif gift git-branch git-commit git-diff git-fork git-merge
git-pull-request github-logo gitlab-logo-simple gitlab-logo globe-hemisphere-east globe-hemisphere-west globe-simple-x globe-simple globe-stand globe-x
globe goggles golf goodreads-logo google-cardboard-logo google-chrome-logo google-drive-logo google-logo google-photos-logo google-play-logo
google-podcasts-logo gps-fix gps-slash gps gradient graduation-cap grains-slash grains graph graphics-card
greater-than-or-equal greater-than grid-four grid-nine guitar hair-dryer hamburger hammer hand-arrow-down hand-arrow-up
hand-coins hand-deposit hand-eye hand-fist hand-grabbing hand-heart hand-palm hand-peace hand-pointing hand-soap
hand-swipe-left hand-swipe-right hand-tap hand-waving hand-withdraw hand handbag-simple handbag hands-clapping hands-praying
handshake hard-drive hard-drives hard-hat hash-straight hash head-circuit headlights headphones headset
heart-break heart-half heart-straight-break heart-straight heart heartbeat hexagon high-definition high-heel highlighter-circle
highlighter hockey hoodie horse hospital hourglass-high hourglass-low hourglass-medium hourglass-simple-high hourglass-simple-low
hourglass-simple-medium hourglass-simple hourglass house-line house-simple house hurricane ice-cream identification-badge identification-card
image-broken image-square image images-square images infinity info instagram-logo intersect-square intersect-three
intersect intersection invoice island jar-label jar jeep joystick kanban key-return
key keyboard keyhole knife ladder-simple ladder lamp-pendant lamp laptop lasso
lastfm-logo layout leaf lectern lego-smiley lego less-than-or-equal less-than letter-circle-h letter-circle-p
letter-circle-v lifebuoy lightbulb-filament lightbulb lighthouse lightning-a lightning-slash lightning line-segment line-segments
line-vertical link-break link-simple-break link-simple-horizontal-break link-simple-horizontal link-simple link linkedin-logo linktree-logo linux-logo
list-bullets list-checks list-dashes list-heart list-magnifying-glass list-numbers list-plus list-star list lock-key-open
lock-key lock-laminated-open lock-laminated lock-open lock-simple-open lock-simple lock lockers log magic-wand
magnet-straight magnet magnifying-glass-minus magnifying-glass-plus magnifying-glass mailbox map-pin-area map-pin-line map-pin-plus map-pin-simple-area
map-pin-simple-line map-pin-simple map-pin map-trifold markdown-logo marker-circle martini mask-happy mask-sad mastodon-logo
math-operations matrix-logo medal-military medal medium-logo megaphone-simple megaphone member-of memory messenger-logo
meta-logo meteor metronome microphone-slash microphone-stage microphone microscope microsoft-excel-logo microsoft-outlook-logo microsoft-powerpoint-logo
microsoft-teams-logo microsoft-word-logo minus-circle minus-square minus money-wavy money monitor-arrow-up monitor-play monitor
moon-stars moon moped-front moped mosque motorcycle mountains mouse-left-click mouse-middle-click mouse-right-click
mouse-scroll mouse-simple mouse music-note-simple music-note music-notes-minus music-notes-plus music-notes-simple music-notes navigation-arrow
needle network-slash network-x network newspaper-clipping newspaper not-equals not-member-of not-subset-of not-superset-of
notches note-blank note-pencil note notebook notepad notification notion-logo nuclear-plant number-circle-eight
number-circle-five number-circle-four number-circle-nine number-circle-one number-circle-seven number-circle-six number-circle-three number-circle-two number-circle-zero number-eight
number-five number-four number-nine number-one number-seven number-six number-square-eight number-square-five number-square-four number-square-nine
number-square-one number-square-seven number-square-six number-square-three number-square-two number-square-zero number-three number-two number-zero numpad
nut ny-times-logo octagon office-chair onigiri open-ai-logo option orange-slice orange oven
package paint-brush-broad paint-brush-household paint-brush paint-bucket paint-roller palette panorama pants paper-plane-right
paper-plane-tilt paper-plane paperclip-horizontal paperclip parachute paragraph parallelogram park password path
patreon-logo pause-circle pause paw-print paypal-logo peace pen-nib-straight pen-nib pen pencil-circle
pencil-line pencil-ruler pencil-simple-line pencil-simple-slash pencil-simple pencil-slash pencil pentagon pentagram pepper
percent person-arms-spread person-simple-bike person-simple-circle person-simple-hike person-simple-run person-simple-ski person-simple-snowboard person-simple-swim person-simple-tai-chi
person-simple-throw person-simple-walk person-simple person perspective phone-call phone-disconnect phone-incoming phone-list phone-outgoing
phone-pause phone-plus phone-slash phone-transfer phone-x phone phosphor-logo pi piano-keys picnic-table
picture-in-picture piggy-bank pill ping-pong pint-glass pinterest-logo pinwheel pipe-wrench pipe pix-logo
pizza placeholder planet plant play-circle play-pause play playlist plug-charging plug
plugs-connected plugs plus-circle plus-minus plus-square plus poker-chip police-car polygon popcorn
popsicle potted-plant power prescription presentation-chart presentation printer prohibit-inset prohibit projector-screen-chart
projector-screen pulse push-pin-simple-slash push-pin-simple push-pin-slash push-pin puzzle-piece qr-code question-mark question
queue quotes rabbit racquet radical radio-button radio radioactive rainbow-cloud rainbow
ranking read-cv-logo receipt-x receipt record rectangle-dashed rectangle recycle reddit-logo repeat-once
repeat replit-logo resize rewind-circle rewind road-horizon robot rocket-launch rocket rows-plus-bottom
rows-plus-top rows rss-simple rss rug ruler sailboat scales scan-smiley scan
scissors scooter screencast screwdriver scribble-loop scribble scroll seal-check seal-percent seal-question
seal-warning seal seat seatbelt security-camera selection-all selection-background selection-foreground selection-inverse selection-plus
selection-slash selection shapes share-fat share-network share shield-check shield-checkered shield-chevron shield-plus
shield-slash shield-star shield-warning shield shipping-container shirt-folded shooting-star shopping-bag-open shopping-bag shopping-cart-simple
shopping-cart shovel shower shrimp shuffle-angular shuffle-simple shuffle sidebar-simple sidebar sigma
sign-in sign-out signature signpost sim-card siren sketch-logo skip-back-circle skip-back skip-forward-circle
skip-forward skull skype-logo slack-logo sliders-horizontal sliders slideshow smiley-angry smiley-blank smiley-meh
smiley-melting smiley-nervous smiley-sad smiley-sticker smiley-wink smiley-x-eyes smiley snapchat-logo sneaker-move sneaker
snowflake soccer-ball sock solar-panel solar-roof sort-ascending sort-descending soundcloud-logo spade sparkle
speaker-hifi speaker-high speaker-low speaker-none speaker-simple-high speaker-simple-low speaker-simple-none speaker-simple-slash speaker-simple-x speaker-slash
speaker-x speedometer sphere spinner-ball spinner-gap spinner spiral split-horizontal split-vertical spotify-logo
spray-bottle square-half-bottom square-half square-logo square-split-horizontal square-split-vertical square squares-four stack-minus stack-overflow-logo
stack-plus stack-simple stack stairs stamp standard-definition star-and-crescent star-four star-half star-of-david
star steam-logo steering-wheel steps stethoscope sticker stool stop-circle stop storefront
strategy stripe-logo student subset-of subset-proper-of subtitles-slash subtitles subtract-square subtract subway
suitcase-rolling suitcase-simple suitcase sun-dim sun-horizon sun sunglasses superset-of superset-proper-of swap
swatches swimming-pool sword synagogue syringe t-shirt table tabs tag-chevron tag-simple
tag target taxi tea-bag telegram-logo television-simple television tennis-ball tent terminal-window
terminal test-tube text-a-underline text-aa text-align-center text-align-justify text-align-left text-align-right text-b text-columns
text-h-five text-h-four text-h-one text-h-six text-h-three text-h-two text-h text-indent text-italic text-outdent
text-strikethrough text-subscript text-superscript text-t-slash text-t text-underline textbox thermometer-cold thermometer-hot thermometer-simple
thermometer threads-logo three-d thumbs-down thumbs-up ticket tidal-logo tiktok-logo tilde timer
tip-jar tipi tire toggle-left toggle-right toilet-paper toilet toolbox tooth tornado
tote-simple tote towel tractor trademark-registered trademark traffic-cone traffic-sign traffic-signal train-regional
train-simple train tram translate trash-simple trash tray-arrow-down tray-arrow-up tray treasure-chest
tree-evergreen tree-palm tree-structure tree-view tree trend-down trend-up triangle-dashed triangle trolley-suitcase
trolley trophy truck-trailer truck tumblr-logo twitch-logo twitter-logo umbrella-simple umbrella union
unite-square unite upload-simple upload usb user-check user-circle-check user-circle-dashed user-circle-gear user-circle-minus
user-circle-plus user-circle user-focus user-gear user-list user-minus user-plus user-rectangle user-sound user-square
user-switch user users-four users-three users van vault vector-three vector-two vibrate
video-camera-slash video-camera video-conference video vignette vinyl-record virtual-reality virus visor voicemail
volleyball wall wallet warehouse warning-circle warning-diamond warning-octagon warning washing-machine watch
wave-sawtooth wave-sine wave-square wave-triangle waveform-slash waveform waves webcam-slash webcam webhooks-logo
wechat-logo whatsapp-logo wheelchair-motion wheelchair wifi-high wifi-low wifi-medium wifi-none wifi-slash wifi-x
wind windmill windows-logo wine wrench x-circle x-logo x-square x yarn
yin-yang youtube-logo
]==]
)

end)() end,
    [68] = function()local wax,script,require=ImportGlobals(68)local ImportGlobals return (function(...)
-- [[remix icon pack.]]
-- [[Compact catalog: names only. PNG URLs are derived on demand by]]
-- [[icons/packBuilder (kebab/snake name to PascalCase), so this module]]
-- [[parses as one string constant instead of thousands of table]]
-- [[constructors. Render with scripts/render_remix_pack.js.]]

return require(script.Parent.packBuilder).create(
	"assets/icons/remix-pack/",
	"remix",
[==[
24-hours-fill 24-hours-line 4k-fill 4k-line a-b accessibility-fill accessibility-line account-box-2-fill account-box-2-line account-box-fill
account-box-line account-circle-2-fill account-circle-2-line account-circle-fill account-circle-line account-pin-box-fill account-pin-box-line account-pin-circle-fill account-pin-circle-line add-box-fill
add-box-line add-circle-fill add-circle-line add-fill add-large-fill add-large-line add-line admin-fill admin-line advertisement-fill
advertisement-line aed-electrodes-fill aed-electrodes-line aed-fill aed-line ai ai-agent-fill ai-agent-line ai-generate ai-generate-2
ai-generate-2-fill ai-generate-2-line ai-generate-3d-fill ai-generate-3d-line ai-generate-text airplay-fill airplay-line alarm-add-fill alarm-add-line alarm-fill
alarm-line alarm-snooze-fill alarm-snooze-line alarm-warning-fill alarm-warning-line album-fill album-line alert-fill alert-line alibaba-cloud-fill
alibaba-cloud-line aliens-fill aliens-line align-bottom align-center align-item-bottom-fill align-item-bottom-line align-item-horizontal-center-fill align-item-horizontal-center-line align-item-left-fill
align-item-left-line align-item-right-fill align-item-right-line align-item-top-fill align-item-top-line align-item-vertical-center-fill align-item-vertical-center-line align-justify align-left align-right
align-top align-vertically alipay-fill alipay-line amazon-fill amazon-line anchor-fill anchor-line ancient-gate-fill ancient-gate-line
ancient-pavilion-fill ancient-pavilion-line android-fill android-line angularjs-fill angularjs-line anthropic-fill anthropic-line anticlockwise-2-fill anticlockwise-2-line
anticlockwise-fill anticlockwise-line app-store-fill app-store-line apple-fill apple-line apps-2-add-fill apps-2-add-line apps-2-ai-fill apps-2-ai-line
apps-2-fill apps-2-line apps-ai-fill apps-ai-line apps-fill apps-line archive-2-fill archive-2-line archive-drawer-fill archive-drawer-line
archive-fill archive-line archive-stack-fill archive-stack-line area-chart-fill area-chart-line armchair-fill armchair-line arrow-down-box-fill arrow-down-box-line
arrow-down-circle-fill arrow-down-circle-line arrow-down-double-fill arrow-down-double-line arrow-down-fill arrow-down-line arrow-down-long-fill arrow-down-long-line arrow-down-s-fill arrow-down-s-line
arrow-down-wide-fill arrow-down-wide-line arrow-drop-down-fill arrow-drop-down-line arrow-drop-left-fill arrow-drop-left-line arrow-drop-right-fill arrow-drop-right-line arrow-drop-up-fill arrow-drop-up-line
arrow-go-back-fill arrow-go-back-line arrow-go-forward-fill arrow-go-forward-line arrow-left-box-fill arrow-left-box-line arrow-left-circle-fill arrow-left-circle-line arrow-left-double-fill arrow-left-double-line
arrow-left-down-box-fill arrow-left-down-box-line arrow-left-down-fill arrow-left-down-line arrow-left-down-long-fill arrow-left-down-long-line arrow-left-fill arrow-left-line arrow-left-long-fill arrow-left-long-line
arrow-left-right-fill arrow-left-right-line arrow-left-s-fill arrow-left-s-line arrow-left-up-box-fill arrow-left-up-box-line arrow-left-up-fill arrow-left-up-line arrow-left-up-long-fill arrow-left-up-long-line
arrow-left-wide-fill arrow-left-wide-line arrow-right-box-fill arrow-right-box-line arrow-right-circle-fill arrow-right-circle-line arrow-right-double-fill arrow-right-double-line arrow-right-down-box-fill arrow-right-down-box-line
arrow-right-down-fill arrow-right-down-line arrow-right-down-long-fill arrow-right-down-long-line arrow-right-fill arrow-right-line arrow-right-long-fill arrow-right-long-line arrow-right-s-fill arrow-right-s-line
arrow-right-up-box-fill arrow-right-up-box-line arrow-right-up-fill arrow-right-up-line arrow-right-up-long-fill arrow-right-up-long-line arrow-right-wide-fill arrow-right-wide-line arrow-turn-back-fill arrow-turn-back-line
arrow-turn-forward-fill arrow-turn-forward-line arrow-up-box-fill arrow-up-box-line arrow-up-circle-fill arrow-up-circle-line arrow-up-double-fill arrow-up-double-line arrow-up-down-fill arrow-up-down-line
arrow-up-fill arrow-up-line arrow-up-long-fill arrow-up-long-line arrow-up-s-fill arrow-up-s-line arrow-up-wide-fill arrow-up-wide-line artboard-2-fill artboard-2-line
artboard-fill artboard-line article-fill article-line aspect-ratio-fill aspect-ratio-line asterisk at-fill at-line atom-fill
atom-line attachment-2 attachment-fill attachment-line auction-fill auction-line award-fill award-line baidu-fill baidu-line
ball-pen-fill ball-pen-line bank-card-2-fill bank-card-2-line bank-card-fill bank-card-line bank-fill bank-line bar-chart-2-fill bar-chart-2-line
bar-chart-box-ai-fill bar-chart-box-ai-line bar-chart-box-fill bar-chart-box-line bar-chart-fill bar-chart-grouped-fill bar-chart-grouped-line bar-chart-horizontal-fill bar-chart-horizontal-line bar-chart-line
barcode-box-fill barcode-box-line barcode-fill barcode-line bard-fill bard-line barricade-fill barricade-line base-station-fill base-station-line
baseball-fill baseball-line basketball-fill basketball-line battery-2-charge-fill battery-2-charge-line battery-2-fill battery-2-line battery-charge-fill battery-charge-line
battery-fill battery-line battery-low-fill battery-low-line battery-saver-fill battery-saver-line battery-share-fill battery-share-line bear-smile-fill bear-smile-line
beer-fill beer-line behance-fill behance-line bell-fill bell-line bike-fill bike-line bilibili-fill bilibili-line
bill-fill bill-line billiards-fill billiards-line binoculars-fill binoculars-line bit-coin-fill bit-coin-line blaze-fill blaze-line
blender-fill blender-line blogger-fill blogger-line bluesky-fill bluesky-line bluetooth-connect-fill bluetooth-connect-line bluetooth-fill bluetooth-line
blur-off-fill blur-off-line bnb-fill bnb-line body-scan-fill body-scan-line bold book-2-fill book-2-line book-3-fill
book-3-line book-ai-fill book-ai-line book-fill book-line book-marked-fill book-marked-line book-open-fill book-open-line book-read-fill
book-read-line book-shelf-fill book-shelf-line booklet-fill booklet-line bookmark-2-fill bookmark-2-line bookmark-3-fill bookmark-3-line bookmark-fill
bookmark-line bootstrap-fill bootstrap-line bowl-fill bowl-line box-1-fill box-1-line box-2-fill box-2-line box-3-fill
box-3-line boxing-fill boxing-line braces-fill braces-line brackets-fill brackets-line brain-2-fill brain-2-line brain-3-fill
brain-3-line brain-4-fill brain-4-line brain-ai-3-fill brain-ai-3-line brain-fill brain-line bread-fill bread-line briefcase-2-fill
briefcase-2-line briefcase-3-fill briefcase-3-line briefcase-4-fill briefcase-4-line briefcase-5-fill briefcase-5-line briefcase-fill briefcase-line bring-forward
bring-to-front broadcast-fill broadcast-line brush-2-fill brush-2-line brush-3-fill brush-3-line brush-4-fill brush-4-line brush-ai-3-fill
brush-ai-3-line brush-ai-fill brush-ai-line brush-fill brush-line btc-fill btc-line bubble-chart-fill bubble-chart-line bug-2-fill
bug-2-line bug-fill bug-line building-2-fill building-2-line building-3-fill building-3-line building-4-fill building-4-line building-fill
building-line bus-2-fill bus-2-line bus-fill bus-line bus-wifi-fill bus-wifi-line cactus-fill cactus-line cake-2-fill
cake-2-line cake-3-fill cake-3-line cake-fill cake-line calculator-fill calculator-line calendar-2-fill calendar-2-line calendar-check-fill
calendar-check-line calendar-close-fill calendar-close-line calendar-event-fill calendar-event-line calendar-fill calendar-line calendar-schedule-fill calendar-schedule-line calendar-todo-fill
calendar-todo-line calendar-view camera-2-fill camera-2-line camera-3-fill camera-3-line camera-4-fill camera-4-line camera-ai-2-fill camera-ai-2-line
camera-ai-fill camera-ai-line camera-fill camera-lens-ai-fill camera-lens-ai-line camera-lens-fill camera-lens-line camera-line camera-off-fill camera-off-line
camera-switch-fill camera-switch-line candle-fill candle-line capsule-fill capsule-line car-fill car-line car-washing-fill car-washing-line
caravan-fill caravan-line carousel-view cash-fill cash-line cast-fill cast-line cellphone-fill cellphone-line celsius-fill
celsius-line centos-fill centos-line certificate-2-fill certificate-2-line certificate-fill certificate-line character-recognition-fill character-recognition-line charging-pile-2-fill
charging-pile-2-line charging-pile-fill charging-pile-line chat-1-fill chat-1-line chat-2-fill chat-2-line chat-3-fill chat-3-line chat-4-fill
chat-4-line chat-ai-2-fill chat-ai-2-line chat-ai-3-fill chat-ai-3-line chat-ai-4-fill chat-ai-4-line chat-ai-fill chat-ai-line chat-check-fill
chat-check-line chat-delete-fill chat-delete-line chat-download-fill chat-download-line chat-follow-up-fill chat-follow-up-line chat-forward-fill chat-forward-line chat-heart-fill
chat-heart-line chat-history-fill chat-history-line chat-new-fill chat-new-line chat-off-fill chat-off-line chat-poll-fill chat-poll-line chat-private-fill
chat-private-line chat-quote-fill chat-quote-line chat-search-fill chat-search-line chat-settings-fill chat-settings-line chat-smile-2-fill chat-smile-2-line chat-smile-3-fill
chat-smile-3-line chat-smile-ai-3-fill chat-smile-ai-3-line chat-smile-ai-fill chat-smile-ai-line chat-smile-fill chat-smile-line chat-thread-fill chat-thread-line chat-unread-fill
chat-unread-line chat-upload-fill chat-upload-line chat-voice-ai-fill chat-voice-ai-line chat-voice-fill chat-voice-line check-double-fill check-double-line check-fill
check-line checkbox-blank-circle-fill checkbox-blank-circle-line checkbox-blank-fill checkbox-blank-line checkbox-circle-fill checkbox-circle-line checkbox-fill checkbox-indeterminate-fill checkbox-indeterminate-line
checkbox-line checkbox-multiple-blank-fill checkbox-multiple-blank-line checkbox-multiple-fill checkbox-multiple-line chess-fill chess-line china-railway-fill china-railway-line chrome-fill
chrome-line circle-fill circle-line clapperboard-ai-fill clapperboard-ai-line clapperboard-fill clapperboard-line claude-fill claude-line clipboard-fill
clipboard-line clockwise-2-fill clockwise-2-line clockwise-fill clockwise-line close-circle-fill close-circle-line close-fill close-large-fill close-large-line
close-line closed-captioning-ai-fill closed-captioning-ai-line closed-captioning-fill closed-captioning-line cloud-fill cloud-line cloud-off-fill cloud-off-line cloud-windy-fill
cloud-windy-line cloudy-2-fill cloudy-2-line cloudy-fill cloudy-line code-ai-fill code-ai-line code-block code-box-fill code-box-line
code-fill code-line code-s-fill code-s-line code-s-slash-fill code-s-slash-line code-view codepen-fill codepen-line coin-fill
coin-line coins-fill coins-line collage-fill collage-line collapse-diagonal-2-fill collapse-diagonal-2-line collapse-diagonal-fill collapse-diagonal-line collapse-horizontal-fill
collapse-horizontal-line collapse-vertical-fill collapse-vertical-line color-filter-ai-fill color-filter-ai-line color-filter-fill color-filter-line command-fill command-line community-fill
community-line compass-2-fill compass-2-line compass-3-fill compass-3-line compass-4-fill compass-4-line compass-discover-fill compass-discover-line compass-fill
compass-line compasses-2-fill compasses-2-line compasses-fill compasses-line computer-fill computer-line connector-fill connector-line contacts-book-2-fill
contacts-book-2-line contacts-book-3-fill contacts-book-3-line contacts-book-fill contacts-book-line contacts-book-upload-fill contacts-book-upload-line contacts-fill contacts-line contract-fill
contract-left-fill contract-left-line contract-left-right-fill contract-left-right-line contract-line contract-right-fill contract-right-line contract-up-down-fill contract-up-down-line contrast-2-fill
contrast-2-line contrast-drop-2-fill contrast-drop-2-line contrast-drop-fill contrast-drop-line contrast-fill contrast-line cookie-fill cookie-line copilot-fill
copilot-line copper-coin-fill copper-coin-line copper-diamond-fill copper-diamond-line copyleft-fill copyleft-line copyright-fill copyright-line coreos-fill
coreos-line corner-down-left-fill corner-down-left-line corner-down-right-fill corner-down-right-line corner-left-down-fill corner-left-down-line corner-left-up-fill corner-left-up-line corner-right-down-fill
corner-right-down-line corner-right-up-fill corner-right-up-line corner-up-left-double-fill corner-up-left-double-line corner-up-left-fill corner-up-left-line corner-up-right-double-fill corner-up-right-double-line corner-up-right-fill
corner-up-right-line coupon-2-fill coupon-2-line coupon-3-fill coupon-3-line coupon-4-fill coupon-4-line coupon-5-fill coupon-5-line coupon-fill
coupon-line cpu-fill cpu-line creative-commons-by-fill creative-commons-by-line creative-commons-fill creative-commons-line creative-commons-nc-fill creative-commons-nc-line creative-commons-nd-fill
creative-commons-nd-line creative-commons-sa-fill creative-commons-sa-line creative-commons-zero-fill creative-commons-zero-line criminal-fill criminal-line crop-2-fill crop-2-line crop-fill
crop-line cross-fill cross-line crosshair-2-fill crosshair-2-line crosshair-fill crosshair-line css3-fill css3-line cup-fill
cup-line currency-fill currency-line cursor-ai-fill cursor-ai-line cursor-fill cursor-hand cursor-line custom-size customer-service-2-fill
customer-service-2-line customer-service-fill customer-service-line dashboard-2-fill dashboard-2-line dashboard-3-fill dashboard-3-line dashboard-fill dashboard-horizontal-fill dashboard-horizontal-line
dashboard-line database-2-fill database-2-line database-fill database-line deepseek-fill deepseek-line delete-back-2-fill delete-back-2-line delete-back-fill
delete-back-line delete-bin-2-fill delete-bin-2-line delete-bin-3-fill delete-bin-3-line delete-bin-4-fill delete-bin-4-line delete-bin-5-fill delete-bin-5-line delete-bin-6-fill
delete-bin-6-line delete-bin-7-fill delete-bin-7-line delete-bin-fill delete-bin-line delete-column delete-row device-fill device-line device-recover-fill
device-recover-line diamond-fill diamond-line diamond-ring-fill diamond-ring-line dice-1-fill dice-1-line dice-2-fill dice-2-line dice-3-fill
dice-3-line dice-4-fill dice-4-line dice-5-fill dice-5-line dice-6-fill dice-6-line dice-fill dice-line dingding-fill
dingding-line direction-fill direction-line disc-fill disc-line discord-fill discord-line discount-percent-fill discount-percent-line discuss-fill
discuss-line dislike-fill dislike-line disqus-fill disqus-line divide-fill divide-line dna-fill dna-line donut-chart-fill
donut-chart-line door-closed-fill door-closed-line door-fill door-line door-lock-box-fill door-lock-box-line door-lock-fill door-lock-line door-open-fill
door-open-line dossier-fill dossier-line douban-fill douban-line double-quotes-l double-quotes-r download-2-fill download-2-line download-cloud-2-fill
download-cloud-2-line download-cloud-fill download-cloud-line download-fill download-line draft-fill draft-line drag-drop-fill drag-drop-line drag-move-2-fill
drag-move-2-line drag-move-fill drag-move-line draggable draw-fill draw-line dribbble-fill dribbble-line drinks-2-fill drinks-2-line
drinks-fill drinks-line drive-fill drive-line drizzle-fill drizzle-line drop-fill drop-line dropbox-fill dropbox-line
dropdown-list dropper-fill dropper-line dual-sim-1-fill dual-sim-1-line dual-sim-2-fill dual-sim-2-line dv-fill dv-line dvd-ai-fill
dvd-ai-line dvd-fill dvd-line e-bike-2-fill e-bike-2-line e-bike-fill e-bike-line earth-fill earth-line earthquake-fill
earthquake-line edge-fill edge-line edge-new-fill edge-new-line edit-2-fill edit-2-line edit-box-fill edit-box-line edit-circle-fill
edit-circle-line edit-fill edit-line eject-fill eject-line emoji-sticker-fill emoji-sticker-line emotion-2-fill emotion-2-line emotion-add-fill
emotion-add-line emotion-fill emotion-happy-fill emotion-happy-line emotion-laugh-fill emotion-laugh-line emotion-line emotion-normal-fill emotion-normal-line emotion-sad-fill
emotion-sad-line emotion-unhappy-fill emotion-unhappy-line empathize-fill empathize-line emphasis emphasis-cn english-input equal-fill equal-line
equalizer-2-fill equalizer-2-line equalizer-3-fill equalizer-3-line equalizer-fill equalizer-line eraser-fill eraser-line error-warning-fill error-warning-line
eth-fill eth-line evernote-fill evernote-line exchange-2-fill exchange-2-line exchange-box-fill exchange-box-line exchange-cny-fill exchange-cny-line
exchange-dollar-fill exchange-dollar-line exchange-fill exchange-funds-fill exchange-funds-line exchange-line expand-diagonal-2-fill expand-diagonal-2-line expand-diagonal-fill expand-diagonal-line
expand-diagonal-s-2-fill expand-diagonal-s-2-line expand-diagonal-s-fill expand-diagonal-s-line expand-height-fill expand-height-line expand-horizontal-fill expand-horizontal-line expand-horizontal-s-fill expand-horizontal-s-line
expand-left-fill expand-left-line expand-left-right-fill expand-left-right-line expand-right-fill expand-right-line expand-up-down-fill expand-up-down-line expand-vertical-fill expand-vertical-line
expand-vertical-s-fill expand-vertical-s-line expand-width-fill expand-width-line export-fill export-line external-link-fill external-link-line eye-2-fill eye-2-line
eye-close-fill eye-close-line eye-fill eye-line eye-off-fill eye-off-line facebook-box-fill facebook-box-line facebook-circle-fill facebook-circle-line
facebook-fill facebook-line fahrenheit-fill fahrenheit-line fediverse-fill fediverse-line feedback-fill feedback-line figma-fill figma-line
file-2-fill file-2-line file-3-fill file-3-line file-4-fill file-4-line file-add-fill file-add-line file-ai-2-fill file-ai-2-line
file-ai-fill file-ai-line file-chart-2-fill file-chart-2-line file-chart-fill file-chart-line file-check-fill file-check-line file-close-fill file-close-line
file-cloud-fill file-cloud-line file-code-fill file-code-line file-copy-2-fill file-copy-2-line file-copy-fill file-copy-line file-damage-fill file-damage-line
file-download-fill file-download-line file-edit-fill file-edit-line file-excel-2-fill file-excel-2-line file-excel-fill file-excel-line file-fill file-forbid-fill
file-forbid-line file-gif-fill file-gif-line file-history-fill file-history-line file-hwp-fill file-hwp-line file-image-fill file-image-line file-info-fill
file-info-line file-line file-list-2-fill file-list-2-line file-list-3-fill file-list-3-line file-list-fill file-list-line file-lock-fill file-lock-line
file-marked-fill file-marked-line file-music-fill file-music-line file-paper-2-fill file-paper-2-line file-paper-fill file-paper-line file-pdf-2-fill file-pdf-2-line
file-pdf-fill file-pdf-line file-ppt-2-fill file-ppt-2-line file-ppt-fill file-ppt-line file-reduce-fill file-reduce-line file-scan-fill file-scan-line
file-search-fill file-search-line file-settings-fill file-settings-line file-shield-2-fill file-shield-2-line file-shield-fill file-shield-line file-shred-fill file-shred-line
file-text-fill file-text-line file-transfer-fill file-transfer-line file-unknow-fill file-unknow-line file-upload-fill file-upload-line file-user-fill file-user-line
file-video-fill file-video-line file-warning-fill file-warning-line file-word-2-fill file-word-2-line file-word-fill file-word-line file-zip-fill file-zip-line
film-ai-fill film-ai-line film-fill film-line filter-2-fill filter-2-line filter-3-fill filter-3-line filter-fill filter-line
filter-off-fill filter-off-line find-replace-fill find-replace-line finder-fill finder-line fingerprint-2-fill fingerprint-2-line fingerprint-fill fingerprint-line
fire-fill fire-line firebase-fill firebase-line firefox-browser-fill firefox-browser-line firefox-fill firefox-line first-aid-kit-fill first-aid-kit-line
fiverr-fill fiverr-line flag-2-fill flag-2-line flag-fill flag-line flag-off-fill flag-off-line flashlight-fill flashlight-line
flask-fill flask-line flickr-fill flickr-line flight-land-fill flight-land-line flight-takeoff-fill flight-takeoff-line flip-horizontal-2-fill flip-horizontal-2-line
flip-horizontal-fill flip-horizontal-line flip-vertical-2-fill flip-vertical-2-line flip-vertical-fill flip-vertical-line flood-fill flood-line flow-chart flower-fill
flower-line flutter-fill flutter-line focus-2-fill focus-2-line focus-3-fill focus-3-line focus-fill focus-line focus-mode
foggy-fill foggy-line folder-2-fill folder-2-line folder-3-fill folder-3-line folder-4-fill folder-4-line folder-5-fill folder-5-line
folder-6-fill folder-6-line folder-add-fill folder-add-line folder-chart-2-fill folder-chart-2-line folder-chart-fill folder-chart-line folder-check-fill folder-check-line
folder-close-fill folder-close-line folder-cloud-fill folder-cloud-line folder-download-fill folder-download-line folder-fill folder-forbid-fill folder-forbid-line folder-history-fill
folder-history-line folder-image-fill folder-image-line folder-info-fill folder-info-line folder-keyhole-fill folder-keyhole-line folder-line folder-lock-fill folder-lock-line
folder-music-fill folder-music-line folder-open-fill folder-open-line folder-received-fill folder-received-line folder-reduce-fill folder-reduce-line folder-settings-fill folder-settings-line
folder-shared-fill folder-shared-line folder-shield-2-fill folder-shield-2-line folder-shield-fill folder-shield-line folder-transfer-fill folder-transfer-line folder-unknow-fill folder-unknow-line
folder-upload-fill folder-upload-line folder-user-fill folder-user-line folder-video-fill folder-video-line folder-warning-fill folder-warning-line folder-zip-fill folder-zip-line
folders-fill folders-line font-color font-family font-mono font-sans-serif font-serif font-size font-size-2 font-size-ai
football-fill football-line footprint-fill footprint-line forbid-2-fill forbid-2-line forbid-fill forbid-line format-clear formula
forward-10-fill forward-10-line forward-15-fill forward-15-line forward-30-fill forward-30-line forward-5-fill forward-5-line forward-end-fill forward-end-line
forward-end-mini-fill forward-end-mini-line fridge-fill fridge-line friendica-fill friendica-line fullscreen-exit-fill fullscreen-exit-line fullscreen-fill fullscreen-line
function-add-fill function-add-line function-ai-fill function-ai-line function-fill function-line functions funds-box-fill funds-box-line funds-fill
funds-line gallery-fill gallery-line gallery-upload-fill gallery-upload-line gallery-view gallery-view-2 game-2-fill game-2-line game-fill
game-line gamepad-fill gamepad-line gas-station-fill gas-station-line gatsby-fill gatsby-line gemini-fill gemini-line genderless-fill
genderless-line ghost-2-fill ghost-2-line ghost-3-fill ghost-3-line ghost-4-fill ghost-4-line ghost-fill ghost-line ghost-smile-fill
ghost-smile-line gift-2-fill gift-2-line gift-fill gift-line git-branch-fill git-branch-line git-close-pull-request-fill git-close-pull-request-line git-commit-fill
git-commit-line git-fork-fill git-fork-line git-merge-fill git-merge-line git-pr-draft-fill git-pr-draft-line git-pull-request-fill git-pull-request-line git-repository-commits-fill
git-repository-commits-line git-repository-fill git-repository-line git-repository-private-fill git-repository-private-line gitbook-fill gitbook-line gitee-fill gitee-line github-fill
github-line gitlab-fill gitlab-line glasses-2-fill glasses-2-line glasses-fill glasses-line global-fill global-line global-off-fill
global-off-line globe-fill globe-line goblet-2-fill goblet-2-line goblet-broken-fill goblet-broken-line goblet-fill goblet-line goggles-fill
goggles-line golf-ball-fill golf-ball-line google-fill google-line google-play-fill google-play-line government-fill government-line gps-fill
gps-line gradienter-fill gradienter-line graduation-cap-fill graduation-cap-line grid-fill grid-line grok-ai-fill grok-ai-line group-2-fill
group-2-line group-3-fill group-3-line group-fill group-line guide-fill guide-line h-1 h-2 h-3
h-4 h-5 h-6 hail-fill hail-line hammer-fill hammer-line hand hand-2 hand-coin-fill
hand-coin-line hand-heart-fill hand-heart-line hand-sanitizer-fill hand-sanitizer-line handbag-fill handbag-line hard-drive-2-fill hard-drive-2-line hard-drive-3-fill
hard-drive-3-line hard-drive-fill hard-drive-line hashtag haze-2-fill haze-2-line haze-fill haze-line hd-fill hd-line
heading headphone-fill headphone-line health-book-fill health-book-line heart-2-fill heart-2-line heart-3-fill heart-3-line heart-add-2-fill
heart-add-2-line heart-add-fill heart-add-line heart-fill heart-line heart-pulse-fill heart-pulse-line hearts-fill hearts-line heavy-showers-fill
heavy-showers-line hexagon-fill hexagon-line history-fill history-line home-2-fill home-2-line home-3-fill home-3-line home-4-fill
home-4-line home-5-fill home-5-line home-6-fill home-6-line home-7-fill home-7-line home-8-fill home-8-line home-9-fill
home-9-line home-fill home-gear-fill home-gear-line home-heart-fill home-heart-line home-line home-office-fill home-office-line home-smile-2-fill
home-smile-2-line home-smile-fill home-smile-line home-wifi-fill home-wifi-line honor-of-kings-fill honor-of-kings-line honour-fill honour-line hospital-fill
hospital-line hotel-bed-fill hotel-bed-line hotel-fill hotel-line hotspot-fill hotspot-line hourglass-2-fill hourglass-2-line hourglass-fill
hourglass-line hq-fill hq-line html5-fill html5-line id-card-fill id-card-line ie-fill ie-line image-2-fill
image-2-line image-add-fill image-add-line image-ai-fill image-ai-line image-circle-ai-fill image-circle-ai-line image-circle-fill image-circle-line image-download-fill
image-download-line image-edit-fill image-edit-line image-fill image-line image-upload-fill image-upload-line import-fill import-line inbox-2-fill
inbox-2-line inbox-archive-fill inbox-archive-line inbox-fill inbox-line inbox-unarchive-fill inbox-unarchive-line increase-decrease-fill increase-decrease-line indent-decrease
indent-increase indeterminate-circle-fill indeterminate-circle-line infinity-fill infinity-line info-card-fill info-card-line info-i information-2-fill information-2-line
information-fill information-line information-off-fill information-off-line infrared-thermometer-fill infrared-thermometer-line ink-bottle-fill ink-bottle-line input-cursor-move input-field
input-method-fill input-method-line insert-column-left insert-column-right insert-row-bottom insert-row-top instagram-fill instagram-line install-fill install-line
instance-fill instance-line invision-fill invision-line issues-fill issues-line issues-reopen-fill issues-reopen-line italic java-fill
java-line javascript-fill javascript-line jewelry-fill jewelry-line kakao-talk-fill kakao-talk-line kanban-view kanban-view-2 key-2-fill
key-2-line key-fill key-line keyboard-box-fill keyboard-box-line keyboard-fill keyboard-line keynote-fill keynote-line kick-fill
kick-line knife-blood-fill knife-blood-line knife-fill knife-line landscape-ai-fill landscape-ai-line landscape-fill landscape-line layout-2-fill
layout-2-line layout-3-fill layout-3-line layout-4-fill layout-4-line layout-5-fill layout-5-line layout-6-fill layout-6-line layout-bottom-2-fill
layout-bottom-2-line layout-bottom-fill layout-bottom-line layout-column-fill layout-column-line layout-fill layout-grid-2-fill layout-grid-2-line layout-grid-fill layout-grid-line
layout-horizontal-fill layout-horizontal-line layout-left-2-fill layout-left-2-line layout-left-fill layout-left-line layout-line layout-masonry-fill layout-masonry-line layout-right-2-fill
layout-right-2-line layout-right-fill layout-right-line layout-row-fill layout-row-line layout-top-2-fill layout-top-2-line layout-top-fill layout-top-line layout-vertical-fill
layout-vertical-line leaf-fill leaf-line letter-spacing-2 lifebuoy-fill lifebuoy-line lightbulb-ai-fill lightbulb-ai-line lightbulb-fill lightbulb-flash-fill
lightbulb-flash-line lightbulb-line line-chart-fill line-chart-line line-fill line-height line-height-2 line-line link link-m
link-unlink link-unlink-m linkedin-box-fill linkedin-box-line linkedin-fill linkedin-line links-fill links-line list-check list-check-2
list-check-3 list-indefinite list-ordered list-ordered-2 list-radio list-settings-fill list-settings-line list-unordered list-view live-fill
live-line loader-2-fill loader-2-line loader-3-fill loader-3-line loader-4-fill loader-4-line loader-5-fill loader-5-line loader-fill
loader-line lock-2-fill lock-2-line lock-fill lock-line lock-password-fill lock-password-line lock-star-fill lock-star-line lock-unlock-fill
lock-unlock-line login-box-fill login-box-line login-circle-fill login-circle-line logout-box-fill logout-box-line logout-box-r-fill logout-box-r-line logout-circle-fill
logout-circle-line logout-circle-r-fill logout-circle-r-line loop-left-ai-fill loop-left-ai-line loop-left-fill loop-left-line loop-right-ai-fill loop-right-ai-line loop-right-fill
loop-right-line luggage-cart-fill luggage-cart-line luggage-deposit-fill luggage-deposit-line lungs-fill lungs-line mac-fill mac-line macbook-fill
macbook-line magic-fill magic-line mail-add-fill mail-add-line mail-ai-fill mail-ai-line mail-check-fill mail-check-line mail-close-fill
mail-close-line mail-download-fill mail-download-line mail-fill mail-forbid-fill mail-forbid-line mail-line mail-lock-fill mail-lock-line mail-open-fill
mail-open-line mail-send-fill mail-send-line mail-settings-fill mail-settings-line mail-star-fill mail-star-line mail-unread-fill mail-unread-line mail-volume-fill
mail-volume-line map-2-fill map-2-line map-fill map-line map-pin-2-fill map-pin-2-line map-pin-3-fill map-pin-3-line map-pin-4-fill
map-pin-4-line map-pin-5-fill map-pin-5-line map-pin-add-fill map-pin-add-line map-pin-fill map-pin-line map-pin-range-fill map-pin-range-line map-pin-time-fill
map-pin-time-line map-pin-user-fill map-pin-user-line mark-pen-fill mark-pen-line markdown-fill markdown-line markup-fill markup-line mastercard-fill
mastercard-line mastodon-fill mastodon-line medal-2-fill medal-2-line medal-fill medal-line medicine-bottle-fill medicine-bottle-line medium-fill
medium-line megaphone-2-fill megaphone-2-line megaphone-fill megaphone-line memories-fill memories-line men-fill men-line mental-health-fill
mental-health-line menu-2-fill menu-2-line menu-3-fill menu-3-line menu-4-fill menu-4-line menu-5-fill menu-5-line menu-add-fill
menu-add-line menu-fill menu-fold-2-fill menu-fold-2-line menu-fold-3-fill menu-fold-3-line menu-fold-4-fill menu-fold-4-line menu-fold-fill menu-fold-line
menu-line menu-search-fill menu-search-line menu-unfold-2-fill menu-unfold-2-line menu-unfold-3-fill menu-unfold-3-line menu-unfold-4-fill menu-unfold-4-line menu-unfold-fill
menu-unfold-line merge-cells-horizontal merge-cells-vertical message-2-fill message-2-line message-3-fill message-3-line message-ai-3-fill message-ai-3-line message-fill
message-line messenger-fill messenger-line meta-fill meta-line meteor-fill meteor-line mic-2-ai-fill mic-2-ai-line mic-2-fill
mic-2-line mic-ai-fill mic-ai-line mic-fill mic-line mic-off-fill mic-off-line mickey-fill mickey-line microscope-fill
microscope-line microsoft-copilot-fill microsoft-copilot-line microsoft-fill microsoft-line microsoft-loop-fill microsoft-loop-line mind-map mini-program-fill mini-program-line
mist-fill mist-line mixtral-fill mixtral-line mobile-download-fill mobile-download-line money-cny-box-fill money-cny-box-line money-cny-circle-fill money-cny-circle-line
money-dollar-box-fill money-dollar-box-line money-dollar-circle-fill money-dollar-circle-line money-euro-box-fill money-euro-box-line money-euro-circle-fill money-euro-circle-line money-pound-box-fill money-pound-box-line
money-pound-circle-fill money-pound-circle-line money-rupee-circle-fill money-rupee-circle-line moon-clear-fill moon-clear-line moon-cloudy-fill moon-cloudy-line moon-fill moon-foggy-fill
moon-foggy-line moon-line more-2-fill more-2-line more-fill more-line mosaic-fill mosaic-line motorbike-fill motorbike-line
mouse-fill mouse-line movie-2-ai-fill movie-2-ai-line movie-2-fill movie-2-line movie-ai-fill movie-ai-line movie-fill movie-line
multi-image-fill multi-image-line music-2-fill music-2-line music-ai-fill music-ai-line music-fill music-line mv-ai-fill mv-ai-line
mv-fill mv-line navigation-fill navigation-line netease-cloud-music-fill netease-cloud-music-line netflix-fill netflix-line network-error-fill network-error-line
network-fill network-line network-off-fill network-off-line news-fill news-line newspaper-fill newspaper-line nextjs-fill nextjs-line
nft-fill nft-line no-credit-card-fill no-credit-card-line node-tree nodejs-fill nodejs-line notification-2-fill notification-2-line notification-3-fill
notification-3-line notification-4-fill notification-4-line notification-badge-fill notification-badge-line notification-fill notification-line notification-off-fill notification-off-line notification-snooze-fill
notification-snooze-line notion-fill notion-line npmjs-fill npmjs-line number-0 number-1 number-2 number-3 number-4
number-5 number-6 number-7 number-8 number-9 numbers-fill numbers-line nurse-fill nurse-line octagon-fill
octagon-line oil-fill oil-line omega open-arm-fill open-arm-line open-source-fill open-source-line openai-fill openai-line
openbase-fill openbase-line opera-fill opera-line order-play-fill order-play-line organization-chart outlet-2-fill outlet-2-line outlet-fill
outlet-line overline p2p-fill p2p-line page-separator pages-fill pages-line paint-brush-fill paint-brush-line paint-fill
paint-line painting-ai-fill painting-ai-line painting-fill painting-line palette-fill palette-line pantone-fill pantone-line paragraph
parent-fill parent-line parentheses-fill parentheses-line parking-box-fill parking-box-line parking-fill parking-line pass-expired-fill pass-expired-line
pass-pending-fill pass-pending-line pass-valid-fill pass-valid-line passport-fill passport-line patreon-fill patreon-line pause-circle-fill pause-circle-line
pause-fill pause-large-fill pause-large-line pause-line pause-mini-fill pause-mini-line paypal-fill paypal-line pen-nib-fill pen-nib-line
pencil-ai-2-fill pencil-ai-2-line pencil-ai-fill pencil-ai-line pencil-fill pencil-line pencil-ruler-2-fill pencil-ruler-2-line pencil-ruler-fill pencil-ruler-line
pentagon-fill pentagon-line percent-fill percent-line perplexity-fill perplexity-line phone-camera-fill phone-camera-line phone-fill phone-find-fill
phone-find-line phone-line phone-lock-fill phone-lock-line php-fill php-line piano-fill piano-grand-fill piano-grand-line piano-line
picture-in-picture-2-fill picture-in-picture-2-line picture-in-picture-exit-fill picture-in-picture-exit-line picture-in-picture-fill picture-in-picture-line pie-chart-2-fill pie-chart-2-line pie-chart-box-fill pie-chart-box-line
pie-chart-fill pie-chart-line pin-distance-fill pin-distance-line ping-pong-fill ping-pong-line pinterest-fill pinterest-line pinyin-input pix-fill
pix-line pixelfed-fill pixelfed-line plane-fill plane-line planet-fill planet-line plant-fill plant-line play-circle-fill
play-circle-line play-fill play-large-fill play-large-line play-line play-list-2-fill play-list-2-line play-list-add-fill play-list-add-line play-list-fill
play-list-line play-mini-fill play-mini-line play-reverse-fill play-reverse-large-fill play-reverse-large-line play-reverse-line play-reverse-mini-fill play-reverse-mini-line playstation-fill
playstation-line plug-2-fill plug-2-line plug-3-fill plug-3-line plug-fill plug-line poker-clubs-fill poker-clubs-line poker-diamonds-fill
poker-diamonds-line poker-hearts-fill poker-hearts-line poker-spades-fill poker-spades-line polaroid-2-fill polaroid-2-line polaroid-fill polaroid-line police-badge-fill
police-badge-line police-car-fill police-car-line presentation-fill presentation-line price-tag-2-fill price-tag-2-line price-tag-3-fill price-tag-3-line price-tag-fill
price-tag-line printer-cloud-fill printer-cloud-line printer-fill printer-line product-hunt-fill product-hunt-line profile-fill profile-line progress-1-fill
progress-1-line progress-2-fill progress-2-line progress-3-fill progress-3-line progress-4-fill progress-4-line progress-5-fill progress-5-line progress-6-fill
progress-6-line progress-7-fill progress-7-line progress-8-fill progress-8-line prohibited-2-fill prohibited-2-line prohibited-fill prohibited-line projector-2-fill
projector-2-line projector-fill projector-line psychotherapy-fill psychotherapy-line pulse-ai-fill pulse-ai-line pulse-fill pulse-line pushpin-2-fill
pushpin-2-line pushpin-fill pushpin-line puzzle-2-fill puzzle-2-line puzzle-fill puzzle-line qq-fill qq-line qr-code-fill
qr-code-line qr-scan-2-fill qr-scan-2-line qr-scan-ai-fill qr-scan-ai-line qr-scan-fill qr-scan-line question-answer-fill question-answer-line question-fill
question-line question-mark questionnaire-fill questionnaire-line quill-pen-ai-fill quill-pen-ai-line quill-pen-fill quill-pen-line quote-text qwen-ai-fill
qwen-ai-line radar-fill radar-line radio-2-fill radio-2-line radio-button-fill radio-button-line radio-fill radio-line rainbow-fill
rainbow-line rainy-fill rainy-line ram-2-fill ram-2-line ram-fill ram-line reactjs-fill reactjs-line receipt-fill
receipt-line record-circle-fill record-circle-line record-mail-fill record-mail-line rectangle-fill rectangle-line recycle-fill recycle-line red-packet-fill
red-packet-line reddit-2-fill reddit-2-line reddit-fill reddit-line refresh-fill refresh-line refund-2-fill refund-2-line refund-fill
refund-line registered-fill registered-line remix-fill remix-line remix-run-fill remix-run-line remixicon-fill remixicon-line remote-control-2-fill
remote-control-2-line remote-control-fill remote-control-line repeat-2-fill repeat-2-line repeat-fill repeat-line repeat-one-fill repeat-one-line replay-10-fill
replay-10-line replay-15-fill replay-15-line replay-30-fill replay-30-line replay-5-fill replay-5-line reply-all-fill reply-all-line reply-fill
reply-line reserved-fill reserved-line reset-left-fill reset-left-line reset-right-fill reset-right-line rest-time-fill rest-time-line restart-fill
restart-line restaurant-2-fill restaurant-2-line restaurant-fill restaurant-line rewind-fill rewind-line rewind-mini-fill rewind-mini-line rewind-start-fill
rewind-start-line rewind-start-mini-fill rewind-start-mini-line rfid-fill rfid-line rhythm-fill rhythm-line riding-fill riding-line road-map-fill
road-map-line roadster-fill roadster-line robot-2-fill robot-2-line robot-3-fill robot-3-line robot-fill robot-line rocket-2-fill
rocket-2-line rocket-fill rocket-line rotate-lock-fill rotate-lock-line rounded-corner route-fill route-line router-fill router-line
rss-fill rss-line ruler-2-fill ruler-2-line ruler-fill ruler-line run-fill run-line safari-fill safari-line
safe-2-fill safe-2-line safe-3-fill safe-3-line safe-fill safe-line sailboat-fill sailboat-line save-2-fill save-2-line
save-3-fill save-3-line save-fill save-line scales-2-fill scales-2-line scales-3-fill scales-3-line scales-fill scales-line
scan-2-fill scan-2-line scan-fill scan-line school-fill school-line scissors-2-fill scissors-2-line scissors-cut-fill scissors-cut-line
scissors-fill scissors-line screenshot-2-fill screenshot-2-line screenshot-fill screenshot-line scroll-to-bottom-fill scroll-to-bottom-line sd-card-fill sd-card-line
sd-card-mini-fill sd-card-mini-line search-2-fill search-2-line search-ai-2-fill search-ai-2-line search-ai-3-fill search-ai-3-line search-ai-4-fill search-ai-4-line
search-ai-fill search-ai-line search-eye-fill search-eye-line search-fill search-line secure-payment-fill secure-payment-line seedling-fill seedling-line
send-backward send-ins-fill send-ins-line send-plane-2-fill send-plane-2-line send-plane-fill send-plane-line send-to-back sensor-fill sensor-line
seo-fill seo-line separator server-fill server-line service-bell-fill service-bell-line service-fill service-line settings-2-fill
settings-2-line settings-3-fill settings-3-line settings-4-fill settings-4-line settings-5-fill settings-5-line settings-6-fill settings-6-line settings-fill
settings-line shadow-fill shadow-line shake-hands-fill shake-hands-line shape-2-fill shape-2-line shape-fill shape-line shapes-fill
shapes-line share-2-fill share-2-line share-box-fill share-box-line share-circle-fill share-circle-line share-fill share-forward-2-fill share-forward-2-line
share-forward-box-fill share-forward-box-line share-forward-fill share-forward-line share-line shield-check-fill shield-check-line shield-cross-fill shield-cross-line shield-fill
shield-flash-fill shield-flash-line shield-keyhole-fill shield-keyhole-line shield-line shield-star-fill shield-star-line shield-user-fill shield-user-line shining-2-fill
shining-2-line shining-fill shining-line ship-2-fill ship-2-line ship-fill ship-line shirt-fill shirt-line shopping-bag-2-fill
shopping-bag-2-line shopping-bag-3-fill shopping-bag-3-line shopping-bag-4-fill shopping-bag-4-line shopping-bag-fill shopping-bag-line shopping-basket-2-fill shopping-basket-2-line shopping-basket-fill
shopping-basket-line shopping-cart-2-fill shopping-cart-2-line shopping-cart-fill shopping-cart-line showers-fill showers-line shuffle-fill shuffle-line shut-down-fill
shut-down-line side-bar-fill side-bar-line sidebar-fold-fill sidebar-fold-line sidebar-unfold-fill sidebar-unfold-line signal-cellular-1-fill signal-cellular-1-line signal-cellular-2-fill
signal-cellular-2-line signal-cellular-3-fill signal-cellular-3-line signal-cellular-off-fill signal-cellular-off-line signal-tower-fill signal-tower-line signal-wifi-1-fill signal-wifi-1-line signal-wifi-2-fill
signal-wifi-2-line signal-wifi-3-fill signal-wifi-3-line signal-wifi-error-fill signal-wifi-error-line signal-wifi-fill signal-wifi-line signal-wifi-off-fill signal-wifi-off-line signpost-fill
signpost-line sim-card-2-fill sim-card-2-line sim-card-fill sim-card-line sim-card-warning-fill sim-card-warning-line single-quotes-l single-quotes-r sip-fill
sip-line sketching skip-back-fill skip-back-line skip-back-mini-fill skip-back-mini-line skip-down-fill skip-down-line skip-forward-fill skip-forward-line
skip-forward-mini-fill skip-forward-mini-line skip-left-fill skip-left-line skip-right-fill skip-right-line skip-up-fill skip-up-line skull-2-fill skull-2-line
skull-fill skull-line skype-fill skype-line slack-fill slack-line slash-commands slash-commands-2 slice-fill slice-line
slideshow-2-fill slideshow-2-line slideshow-3-fill slideshow-3-line slideshow-4-fill slideshow-4-line slideshow-fill slideshow-line slideshow-view slow-down-fill
slow-down-line smartphone-fill smartphone-line snapchat-fill snapchat-line snowflake-fill snowflake-line snowy-fill snowy-line sofa-fill
sofa-line sort-alphabet-asc sort-alphabet-desc sort-asc sort-desc sort-number-asc sort-number-desc sound-module-fill sound-module-line soundcloud-fill
soundcloud-line space space-ship-2-fill space-ship-2-line space-ship-fill space-ship-line spam-2-fill spam-2-line spam-3-fill spam-3-line
spam-fill spam-line sparkling-2-fill sparkling-2-line sparkling-fill sparkling-line speak-ai-fill speak-ai-line speak-fill speak-line
speaker-2-fill speaker-2-line speaker-3-fill speaker-3-line speaker-fill speaker-line spectrum-fill spectrum-line speech-to-text-fill speech-to-text-line
speed-fill speed-line speed-mini-fill speed-mini-line speed-up-fill speed-up-line split-cells-horizontal split-cells-vertical spotify-fill spotify-line
spy-fill spy-line square-fill square-line square-root stack-fill stack-line stack-overflow-fill stack-overflow-line stacked-chart-fill
stacked-chart-line stacked-view stackshare-fill stackshare-line stairs-fill stairs-line star-fill star-half-fill star-half-line star-half-s-fill
star-half-s-line star-line star-off-fill star-off-line star-s-fill star-s-line star-smile-fill star-smile-line steam-fill steam-line
steering-2-fill steering-2-line steering-fill steering-line stethoscope-fill stethoscope-line sticky-note-2-fill sticky-note-2-line sticky-note-add-fill sticky-note-add-line
sticky-note-fill sticky-note-line stock-fill stock-line stop-circle-fill stop-circle-line stop-fill stop-large-fill stop-large-line stop-line
stop-mini-fill stop-mini-line store-2-fill store-2-line store-3-fill store-3-line store-fill store-line strikethrough strikethrough-2
subreddit-fill subreddit-line subscript subscript-2 subtract-fill subtract-line subway-fill subway-line subway-wifi-fill subway-wifi-line
suitcase-2-fill suitcase-2-line suitcase-3-fill suitcase-3-line suitcase-fill suitcase-line sun-cloudy-fill sun-cloudy-line sun-fill sun-foggy-fill
sun-foggy-line sun-line supabase-fill supabase-line superscript superscript-2 surgical-mask-fill surgical-mask-line surround-sound-fill surround-sound-line
survey-fill survey-line svelte-fill svelte-line swap-2-fill swap-2-line swap-3-fill swap-3-line swap-box-fill swap-box-line
swap-fill swap-line switch-fill switch-line sword-fill sword-line syringe-fill syringe-line t-box-fill t-box-line
t-shirt-2-fill t-shirt-2-line t-shirt-air-fill t-shirt-air-line t-shirt-fill t-shirt-line table-2 table-3 table-alt-fill table-alt-line
table-fill table-line table-view tablet-fill tablet-line tailwind-css-fill tailwind-css-line takeaway-fill takeaway-line taobao-fill
taobao-line tape-fill tape-line target-fill target-line task-fill task-line taxi-fill taxi-line taxi-wifi-fill
taxi-wifi-line team-fill team-line telegram-2-fill telegram-2-line telegram-fill telegram-line temp-cold-fill temp-cold-line temp-hot-fill
temp-hot-line tent-fill tent-line terminal-box-fill terminal-box-line terminal-fill terminal-line terminal-window-fill terminal-window-line test-tube-fill
test-tube-line text text-block text-direction-l text-direction-r text-snippet text-spacing text-to-speech-fill text-to-speech-line text-wrap
thermometer-fill thermometer-line threads-fill threads-line thumb-down-fill thumb-down-line thumb-up-fill thumb-up-line thunderstorms-fill thunderstorms-line
ticket-2-fill ticket-2-line ticket-fill ticket-line tiktok-fill tiktok-line time-fill time-line time-zone-fill time-zone-line
timeline-view timer-2-fill timer-2-line timer-fill timer-flash-fill timer-flash-line timer-line todo-fill todo-line toggle-fill
toggle-line token-swap-fill token-swap-line tools-fill tools-line tooth-fill tooth-line tornado-fill tornado-line trademark-fill
trademark-line traffic-light-fill traffic-light-line train-fill train-line train-wifi-fill train-wifi-line translate translate-2 translate-ai
translate-ai-2 travesti-fill travesti-line treasure-map-fill treasure-map-line tree-fill tree-line trello-fill trello-line triangle-fill
triangle-line triangular-flag-fill triangular-flag-line trophy-fill trophy-line truck-fill truck-line tumblr-fill tumblr-line tv-2-fill
tv-2-line tv-fill tv-line twitch-fill twitch-line twitter-fill twitter-line twitter-x-fill twitter-x-line typhoon-fill
typhoon-line u-disk-fill u-disk-line ubuntu-fill ubuntu-line umbrella-fill umbrella-line underline uninstall-fill uninstall-line
unpin-fill unpin-line unsplash-fill unsplash-line upload-2-fill upload-2-line upload-cloud-2-fill upload-cloud-2-line upload-cloud-fill upload-cloud-line
upload-fill upload-line upwork-fill upwork-line usb-fill usb-line user-2-fill user-2-line user-3-fill user-3-line
user-4-fill user-4-line user-5-fill user-5-line user-6-fill user-6-line user-add-fill user-add-line user-community-fill user-community-line
user-fill user-follow-fill user-follow-line user-forbid-fill user-forbid-line user-heart-fill user-heart-line user-line user-location-fill user-location-line
user-minus-fill user-minus-line user-received-2-fill user-received-2-line user-received-fill user-received-line user-search-fill user-search-line user-settings-fill user-settings-line
user-shared-2-fill user-shared-2-line user-shared-fill user-shared-line user-smile-fill user-smile-line user-star-fill user-star-line user-unfollow-fill user-unfollow-line
user-voice-fill user-voice-line vercel-fill vercel-line verified-badge-fill verified-badge-line video-add-fill video-add-line video-ai-fill video-ai-line
video-chat-fill video-chat-line video-download-fill video-download-line video-fill video-line video-off-fill video-off-line video-on-ai-fill video-on-ai-line
video-on-fill video-on-line video-upload-fill video-upload-line vidicon-2-fill vidicon-2-line vidicon-fill vidicon-line vimeo-fill vimeo-line
vip-crown-2-fill vip-crown-2-line vip-crown-fill vip-crown-line vip-diamond-fill vip-diamond-line vip-fill vip-line virus-fill virus-line
visa-fill visa-line vk-fill vk-line voice-ai-fill voice-ai-line voice-recognition-fill voice-recognition-line voiceprint-fill voiceprint-line
volume-down-fill volume-down-line volume-mute-fill volume-mute-line volume-off-vibrate-fill volume-off-vibrate-line volume-up-fill volume-up-line volume-vibrate-fill volume-vibrate-line
vuejs-fill vuejs-line walk-fill walk-line wallet-2-fill wallet-2-line wallet-3-fill wallet-3-line wallet-fill wallet-line
water-flash-fill water-flash-line water-percent-fill water-percent-line webcam-fill webcam-line webhook-fill webhook-line wechat-2-fill wechat-2-line
wechat-channels-fill wechat-channels-line wechat-fill wechat-line wechat-pay-fill wechat-pay-line weibo-fill weibo-line weight-fill weight-line
whatsapp-fill whatsapp-line wheelchair-fill wheelchair-line wifi-fill wifi-line wifi-off-fill wifi-off-line window-2-fill window-2-line
window-fill window-line windows-fill windows-line windy-fill windy-line wireless-charging-fill wireless-charging-line women-fill women-line
wordpress-fill wordpress-line wrench-fill wrench-line wubi-input xbox-fill xbox-line xing-fill xing-line xrp-fill
xrp-line xtz-fill xtz-line youtube-fill youtube-line yuque-fill yuque-line zcool-fill zcool-line zhihu-fill
zhihu-line zhipu-ai-fill zhipu-ai-line zoom-in-fill zoom-in-line zoom-out-fill zoom-out-line zzz-fill zzz-line
]==]
)

end)() end,
    [69] = function()local wax,script,require=ImportGlobals(69)local ImportGlobals return (function(...)
-- [[tabler icon pack.]]
-- [[Compact catalog: names only. PNG URLs are derived on demand by]]
-- [[icons/packBuilder (kebab/snake name to PascalCase), so this module]]
-- [[parses as one string constant instead of thousands of table]]
-- [[constructors. Regenerate with scripts/generate_icon_packs.js.]]

return require(script.Parent.packBuilder).create(
	"assets/icons/tabler-pack/",
	"tabler",
[==[
a-b-2 a-b-off a-b abacus-off abacus abc access-point-off access-point accessible-off accessible
acorn acrobatic activity-heartbeat activity ad-2 ad-circle-off ad-circle ad-off ad address-book-off
address-book adjustments-alt adjustments-bolt adjustments-cancel adjustments-check adjustments-code adjustments-cog adjustments-dollar adjustments-down adjustments-exclamation
adjustments-heart adjustments-horizontal adjustments-minus adjustments-off adjustments-pause adjustments-pin adjustments-plus adjustments-question adjustments-search adjustments-share
adjustments-spark adjustments-star adjustments-up adjustments-x adjustments aerial-lift affiliate ai-agent ai-agents ai-gateway
ai air-balloon air-conditioning-disabled air-conditioning air-traffic-control alarm-average alarm-minus alarm-off alarm-plus alarm-smoke
alarm-snooze alarm album-off album alert-circle-off alert-circle alert-hexagon-off alert-hexagon alert-octagon alert-small-off
alert-small alert-square-rounded-off alert-square-rounded alert-square alert-triangle-off alert-triangle alien align-box-bottom-center align-box-bottom-left align-box-bottom-right
align-box-center-bottom align-box-center-middle align-box-center-stretch align-box-center-top align-box-left-bottom align-box-left-middle align-box-left-stretch align-box-left-top align-box-right-bottom align-box-right-middle
align-box-right-stretch align-box-right-top align-box-top-center align-box-top-left align-box-top-right align-center align-justified align-left-2 align-left align-right-2
align-right alpha alphabet-arabic alphabet-bangla alphabet-cyrillic alphabet-greek alphabet-hebrew alphabet-korean alphabet-latin alphabet-polish
alphabet-runes alphabet-thai alt ambulance ampersand analyze-off analyze anchor-off anchor angle
ankh antenna-bars-1 antenna-bars-2 antenna-bars-3 antenna-bars-4 antenna-bars-5 antenna-bars-off antenna-off antenna aperture-off
aperture api-app-off api-app api-book api-off api app-window-bottom-left app-window-bottom-right app-window-bottom app-window-center
app-window apple apps-off apps archery-arrow archive-off archive armchair-2-off armchair-2 armchair-off
armchair arrow-autofit-content arrow-autofit-down arrow-autofit-height arrow-autofit-left arrow-autofit-right arrow-autofit-up arrow-autofit-width arrow-back-up-double arrow-back-up
arrow-back arrow-badge-down arrow-badge-left arrow-badge-right arrow-badge-up arrow-bar-both arrow-bar-down arrow-bar-left arrow-bar-right arrow-bar-to-down-dashed
arrow-bar-to-down arrow-bar-to-left-dashed arrow-bar-to-left arrow-bar-to-right-dashed arrow-bar-to-right arrow-bar-to-up-dashed arrow-bar-to-up arrow-bar-up arrow-bear-left-2 arrow-bear-left
arrow-bear-right-2 arrow-bear-right arrow-big-down-line arrow-big-down-lines arrow-big-down arrow-big-left-line arrow-big-left-lines arrow-big-left arrow-big-right-line arrow-big-right-lines
arrow-big-right arrow-big-up-line arrow-big-up-lines arrow-big-up arrow-bounce arrow-capsule arrow-curve-left arrow-curve-right arrow-down-bar arrow-down-circle
arrow-down-dashed arrow-down-from-arc arrow-down-left-circle arrow-down-left arrow-down-rhombus arrow-down-right-circle arrow-down-right arrow-down-square arrow-down-tail arrow-down-to-arc
arrow-down arrow-elbow-left arrow-elbow-right arrow-fork-triple arrow-fork arrow-forward-up-double arrow-forward-up arrow-forward arrow-guide arrow-iteration
arrow-left-bar arrow-left-circle arrow-left-dashed arrow-left-from-arc arrow-left-rhombus arrow-left-right arrow-left-square arrow-left-tail arrow-left-to-arc arrow-left
arrow-loop-left-2 arrow-loop-left arrow-loop-right-2 arrow-loop-right arrow-merge-alt-left arrow-merge-alt-right arrow-merge-both arrow-merge-left arrow-merge-right arrow-merge
arrow-move-down arrow-move-left arrow-move-right arrow-move-up arrow-narrow-down-dashed arrow-narrow-down arrow-narrow-left-dashed arrow-narrow-left arrow-narrow-right-dashed arrow-narrow-right
arrow-narrow-up-dashed arrow-narrow-up arrow-ramp-left-2 arrow-ramp-left-3 arrow-ramp-left arrow-ramp-right-2 arrow-ramp-right-3 arrow-ramp-right arrow-right-bar arrow-right-circle
arrow-right-dashed arrow-right-from-arc arrow-right-rhombus arrow-right-square arrow-right-tail arrow-right-to-arc arrow-right arrow-rotary-first-left arrow-rotary-first-right arrow-rotary-last-left
arrow-rotary-last-right arrow-rotary-left arrow-rotary-right arrow-rotary-straight arrow-roundabout-left arrow-roundabout-right arrow-sharp-turn-left arrow-sharp-turn-right arrow-up-bar arrow-up-circle
arrow-up-dashed arrow-up-from-arc arrow-up-left-circle arrow-up-left arrow-up-rhombus arrow-up-right-circle arrow-up-right arrow-up-square arrow-up-tail arrow-up-to-arc
arrow-up arrow-wave-left-down arrow-wave-left-up arrow-wave-right-down arrow-wave-right-up arrow-zig-zag arrows-cross arrows-diagonal-2 arrows-diagonal-minimize-2 arrows-diagonal-minimize
arrows-diagonal arrows-diff arrows-double-ne-sw arrows-double-nw-se arrows-double-se-nw arrows-double-sw-ne arrows-down-up arrows-down arrows-exchange-2 arrows-exchange
arrows-horizontal arrows-join-2 arrows-join arrows-left-down arrows-left-right arrows-left arrows-maximize arrows-minimize arrows-move-horizontal arrows-move-vertical
arrows-move arrows-random arrows-right-down arrows-right-left arrows-right arrows-shuffle-2 arrows-shuffle arrows-sort arrows-split-2 arrows-split
arrows-transfer-down arrows-transfer-up-down arrows-transfer-up arrows-up-down arrows-up-left arrows-up-right arrows-up arrows-vertical artboard-off artboard
article-off article aspect-ratio-off aspect-ratio assembly-off assembly asset asterisk-simple asterisk at-off
at atom-2 atom-off atom augmented-reality-2 augmented-reality-off augmented-reality auth-2fa automatic-gearbox automation
avocado award-off award axe axis-x axis-y baby-bottle baby-carriage background backhoe
backpack-off backpack backslash backspace badge-2k badge-3d badge-3k badge-4k badge-5k badge-8k
badge-ad-off badge-ad badge-ar badge-cc badge-hd badge-off badge-sd badge-tm badge-vo badge-vr
badge-wc badge badges-off badges baguette ball-american-football-off ball-american-football ball-baseball ball-basketball ball-bowling
ball-football-off ball-football ball-tennis ball-volleyball balloon-off balloon ballpen-off ballpen ban banana
bandage-off bandage barbell-off barbell barcode-off barcode barrel-off barrel barrier-block-off barrier-block
baseline-density-large baseline-density-medium baseline-density-small baseline basket-bolt basket-cancel basket-check basket-code basket-cog basket-discount
basket-dollar basket-down basket-exclamation basket-heart basket-minus basket-off basket-pause basket-pin basket-plus basket-question
basket-search basket-share basket-star basket-up basket-x basket bat bath-off bath battery-1
battery-2 battery-3 battery-4 battery-automotive battery-charging-2 battery-charging battery-eco battery-exclamation battery-off battery-spark
battery-vertical-1 battery-vertical-2 battery-vertical-3 battery-vertical-4 battery-vertical-charging-2 battery-vertical-charging battery-vertical-eco battery-vertical-exclamation battery-vertical-off battery-vertical
battery beach-off beach bed-flat bed-off bed beer-off beer bell-bolt bell-cancel
bell-check bell-code bell-cog bell-dollar bell-down bell-exclamation bell-heart bell-minus bell-off bell-pause
bell-pin bell-plus bell-question bell-ringing-2 bell-ringing bell-school bell-search bell-share bell-star bell-up
bell-x bell-z bell beta bible bike-off bike binary-off binary-tree-2 binary-tree
binary binoculars biohazard-off biohazard blade bleach-chlorine bleach-no-chlorine bleach-off bleach blend-mode
blender blind blob blockquote blocks bluetooth-connected bluetooth-off bluetooth-x bluetooth blur-off
blur bmp body-scan bold-off bold bolt-off bolt bomb bone-off bone
bong-off bong book-2 book-download book-off book-upload book bookmark-ai bookmark-edit bookmark-minus
bookmark-off bookmark-plus bookmark-question bookmark bookmarks-off bookmarks books-off books boom border-all
border-bottom-plus border-bottom border-corner-ios border-corner-pill border-corner-rounded border-corner-square border-corners border-horizontal border-inner border-left-plus
border-left border-none border-outer border-radius border-right-plus border-right border-sides border-style-2 border-style border-top-plus
border-top border-vertical bot-id bottle-off bottle bounce-left bounce-right bow bowl-chopsticks bowl-spoon
bowl bowling box-align-bottom-left box-align-bottom-right box-align-bottom box-align-left box-align-right box-align-top-left box-align-top-right box-align-top
box-margin box-model-2-off box-model-2 box-model-off box-model box-multiple-0 box-multiple-1 box-multiple-2 box-multiple-3 box-multiple-4
box-multiple-5 box-multiple-6 box-multiple-7 box-multiple-8 box-multiple-9 box-multiple box-off box-padding box braces-off
braces brackets-angle-off brackets-angle brackets-contain-end brackets-contain-start brackets-contain brackets-off brackets braille brain
brand-4chan brand-abstract brand-adobe-after-effects brand-adobe-illustrator brand-adobe-indesign brand-adobe-photoshop brand-adobe-premiere brand-adobe-xd brand-adobe brand-adonis-js
brand-airbnb brand-airtable brand-algolia brand-alipay brand-alpine-js brand-amazon brand-amd brand-amie brand-amigo brand-among-us
brand-android brand-angular brand-ansible brand-ao3 brand-appgallery brand-apple-arcade brand-apple-news brand-apple-podcast brand-apple brand-appstore
brand-arc brand-asana brand-astro brand-audible brand-auth0 brand-aws brand-azure brand-backbone brand-badoo brand-baidu
brand-bandcamp brand-bandlab brand-beats brand-bebo brand-behance brand-bilibili brand-binance brand-bing brand-bitbucket brand-blackberry
brand-blender brand-blogger brand-bluesky brand-booking brand-bootstrap brand-bulma brand-bumble brand-bunpo brand-c-sharp brand-cake
brand-cakephp brand-campaignmonitor brand-carbon brand-cashapp brand-chrome brand-cinema-4d brand-citymapper brand-cloudflare brand-codecov brand-codepen
brand-codesandbox brand-cohost brand-coinbase brand-comedy-central brand-coreos brand-couchdb brand-couchsurfing brand-cpp brand-craft brand-crunchbase
brand-css3 brand-ctemplar brand-cucumber brand-cupra brand-cypress brand-d3 brand-databricks brand-days-counter brand-dcos brand-debian
brand-deezer brand-deliveroo brand-deno brand-denodo brand-deviantart brand-digg brand-dingtalk brand-discord brand-disney brand-disqus
brand-django brand-docker brand-doctrine brand-dolby-digital brand-douban brand-dribbble brand-dropbox brand-drops brand-drupal brand-edge
brand-elastic brand-electronic-arts brand-ember brand-envato brand-etsy brand-evernote brand-facebook brand-feedly brand-figma brand-filezilla
brand-finder brand-firebase brand-firefox brand-fiverr brand-flickr brand-flightradar24 brand-flipboard brand-flutter brand-fortnite brand-foursquare
brand-framer-motion brand-framer brand-funimation brand-gatsby brand-git brand-github-copilot brand-github brand-gitlab brand-gmail brand-golang
brand-google-analytics brand-google-big-query brand-google-drive brand-google-fit brand-google-home brand-google-maps brand-google-one brand-google-photos brand-google-play brand-google-podcasts
brand-google brand-grammarly brand-graphql brand-gravatar brand-grindr brand-guardian brand-gumroad brand-hackerrank brand-hbo brand-headlessui
brand-hexo brand-hipchat brand-html5 brand-inertia brand-infakt brand-instagram brand-intercom brand-itch brand-javascript brand-jira
brand-juejin brand-kakao-talk brand-kbin brand-kick brand-kickstarter brand-kotlin brand-laravel brand-lastfm brand-leetcode brand-letterboxd
brand-line brand-linkedin brand-linktree brand-linqpad brand-livewire brand-loom brand-mailgun brand-mantine brand-mastercard brand-mastodon
brand-matrix brand-mcdonalds brand-medium brand-meetup brand-mercedes brand-messenger brand-meta brand-metabrainz brand-minecraft brand-miniprogram
brand-mixpanel brand-monday brand-mongodb brand-my-oppo brand-mysql brand-national-geographic brand-nem brand-netbeans brand-netease-music brand-netflix
brand-nexo brand-nextcloud brand-nextjs brand-nodejs brand-nord-vpn brand-notion brand-npm brand-nuxt brand-nytimes brand-oauth
brand-office brand-ok-ru brand-onedrive brand-onlyfans brand-open-source brand-openai brand-openvpn brand-opera brand-pagekit brand-parsinta
brand-patreon brand-paypal brand-paypay brand-peanut brand-pepsi brand-php brand-picsart brand-pinterest brand-planetscale brand-pnpm
brand-pocket brand-polymer brand-powershell brand-printables brand-prisma brand-producthunt brand-pushbullet brand-pushover brand-python brand-qq
brand-radix-ui brand-react-native brand-react brand-reason brand-reddit brand-redhat brand-redux brand-revolut brand-rumble brand-rust
brand-safari brand-samsungpass brand-sass brand-sentry brand-sharik brand-shazam brand-shopee brand-signal brand-sketch brand-skype
brand-slack brand-snapchat brand-snapseed brand-snowflake brand-socket-io brand-solidjs brand-soundcloud brand-spacehey brand-speedtest brand-spotify
brand-stackoverflow brand-stackshare brand-steam brand-stellar brand-stocktwits brand-storj brand-storybook brand-storytel brand-strava brand-stripe
brand-sublime-text brand-sugarizer brand-supabase brand-superhuman brand-supernova brand-surfshark brand-svelte brand-swift brand-symfony brand-tabler
brand-tabnine brand-tailwind brand-taobao brand-teams brand-ted brand-telegram brand-terraform brand-tesla brand-tether brand-thingiverse
brand-threads brand-threejs brand-tidal brand-tiktok brand-tinder brand-topbuzz brand-torchain brand-toyota brand-trello brand-tripadvisor
brand-tumblr brand-twilio brand-twitch brand-twitter brand-typescript brand-uber brand-ubuntu brand-unity brand-unsplash brand-upwork
brand-valorant brand-vechain brand-vercel brand-vimeo brand-vinted brand-visa brand-visual-studio brand-vite brand-vivaldi brand-vk
brand-vlc brand-volkswagen brand-vsco brand-vscode brand-vue brand-walmart brand-waze brand-webflow brand-wechat brand-weibo
brand-whatsapp brand-wikipedia brand-windows brand-windy brand-wish brand-wix brand-wordpress brand-x brand-xamarin brand-xbox
brand-xdeep brand-xing brand-yahoo brand-yandex brand-yarn brand-yatse brand-ycombinator brand-youtube-kids brand-youtube brand-zalando
brand-zapier brand-zeit brand-zhihu brand-zoom brand-zulip brand-zwift bread-off bread briefcase-2 briefcase-off
briefcase brightness-2 brightness-auto brightness-down brightness-half brightness-off brightness-up brightness broadcast-off broadcast
browser-check browser-maximize browser-minus browser-off browser-plus browser-share browser-x browser brush-off brush
bubble-minus bubble-plus bubble-tea-2 bubble-tea bubble-text bubble-x bubble bucket-droplet bucket-off bucket
bug-off bug building-airport building-arch building-bank building-bridge-2 building-bridge building-broadcast-tower building-burj-al-arab building-carousel
building-castle building-church building-circus building-cog building-community building-cottage building-eiffel-tower building-estate building-factory-2 building-factory
building-fortress building-hospital building-lighthouse building-minus building-monument building-mosque building-off building-pavilion building-plus building-skyscraper
building-stadium building-store building-tunnel building-warehouse building-wind-turbine building buildings bulb-off bulb bulldozer
burger bus-off bus-stop bus businessplan butterfly cactus-off cactus cake-off cake-roll
cake calculator-off calculator calendar-bolt calendar-cancel calendar-check calendar-clock calendar-code calendar-cog calendar-dollar
calendar-dot calendar-down calendar-due calendar-event calendar-exclamation calendar-heart calendar-minus calendar-month calendar-off calendar-pause
calendar-pin calendar-plus calendar-question calendar-repeat calendar-sad calendar-search calendar-share calendar-smile calendar-star calendar-stats
calendar-time calendar-up calendar-user calendar-week calendar-x calendar camera-ai camera-bitcoin camera-bolt camera-cancel
camera-check camera-code camera-cog camera-dollar camera-down camera-exclamation camera-heart camera-minus camera-moon camera-off
camera-pause camera-pin camera-plus camera-question camera-rotate camera-search camera-selfie camera-share camera-spark camera-star
camera-up camera-x camera camper campfire canary cancel candle candy-off candy
cane cannabis cap-projecting cap-rounded cap-straight capsule-horizontal capsule capture-off capture car-4wd
car-crane car-crash car-door car-fan-1 car-fan-2 car-fan-3 car-fan-auto car-fan car-garage car-lifter
car-off-road car-off car-suspension car-suv car-turbine car carambola caravan cardboards-off cardboards
cards caret-down caret-left-right caret-left caret-right caret-up-down caret-up carousel-horizontal carousel-vertical carrot-off
carrot cash-banknote-edit cash-banknote-heart cash-banknote-minus cash-banknote-move-back cash-banknote-move cash-banknote-off cash-banknote-plus cash-banknote cash-edit
cash-heart cash-minus cash-move-back cash-move cash-off cash-plus cash-register cash cast-off cast
cat category-2 category-minus category-plus category ce-off ce cell-signal-1 cell-signal-2 cell-signal-3
cell-signal-4 cell-signal-5 cell-signal-off cell certificate-2-off certificate-2 certificate-off certificate chair-director chalkboard-off
chalkboard-teacher chalkboard charging-pile chart-arcs-3 chart-arcs chart-area-line chart-area chart-arrows-vertical chart-arrows chart-bar-off
chart-bar-popular chart-bar chart-bubble chart-candle chart-circles chart-cohort chart-column chart-covariate chart-donut-2 chart-donut-3
chart-donut-4 chart-donut chart-dots-2 chart-dots-3 chart-dots chart-funnel chart-grid-dots chart-histogram chart-infographic chart-line
chart-pie-2 chart-pie-3 chart-pie-4 chart-pie-off chart-pie chart-ppf chart-radar chart-sankey chart-scatter-3d chart-scatter
chart-treemap check checkbox checklist checks checkup-list cheese chef-hat-off chef-hat cherry
chess-bishop chess-king chess-knight chess-queen chess-rook chess chevron-compact-down chevron-compact-left chevron-compact-right chevron-compact-up
chevron-down-left chevron-down-right chevron-down chevron-left-pipe chevron-left chevron-right-pipe chevron-right chevron-up-left chevron-up-right chevron-up
chevrons-down-left chevrons-down-right chevrons-down chevrons-left chevrons-right chevrons-up-left chevrons-up-right chevrons-up chisel chocolate
christmas-ball christmas-tree-off christmas-tree circle-arrow-down-left circle-arrow-down-right circle-arrow-down circle-arrow-left circle-arrow-right circle-arrow-up-left circle-arrow-up-right
circle-arrow-up circle-asterisk circle-caret-down circle-caret-left circle-caret-right circle-caret-up circle-check circle-chevron-down circle-chevron-left circle-chevron-right
circle-chevron-up circle-chevrons-down circle-chevrons-left circle-chevrons-right circle-chevrons-up circle-dashed-check circle-dashed-letter-a circle-dashed-letter-b circle-dashed-letter-c circle-dashed-letter-d
circle-dashed-letter-e circle-dashed-letter-f circle-dashed-letter-g circle-dashed-letter-h circle-dashed-letter-i circle-dashed-letter-j circle-dashed-letter-k circle-dashed-letter-l circle-dashed-letter-m circle-dashed-letter-n
circle-dashed-letter-o circle-dashed-letter-p circle-dashed-letter-q circle-dashed-letter-r circle-dashed-letter-s circle-dashed-letter-t circle-dashed-letter-u circle-dashed-letter-v circle-dashed-letter-w circle-dashed-letter-x
circle-dashed-letter-y circle-dashed-letter-z circle-dashed-minus circle-dashed-number-0 circle-dashed-number-1 circle-dashed-number-2 circle-dashed-number-3 circle-dashed-number-4 circle-dashed-number-5 circle-dashed-number-6
circle-dashed-number-7 circle-dashed-number-8 circle-dashed-number-9 circle-dashed-percentage circle-dashed-plus circle-dashed-x circle-dashed circle-dot circle-dotted-letter-a circle-dotted-letter-b
circle-dotted-letter-c circle-dotted-letter-d circle-dotted-letter-e circle-dotted-letter-f circle-dotted-letter-g circle-dotted-letter-h circle-dotted-letter-i circle-dotted-letter-j circle-dotted-letter-k circle-dotted-letter-l
circle-dotted-letter-m circle-dotted-letter-n circle-dotted-letter-o circle-dotted-letter-p circle-dotted-letter-q circle-dotted-letter-r circle-dotted-letter-s circle-dotted-letter-t circle-dotted-letter-u circle-dotted-letter-v
circle-dotted-letter-w circle-dotted-letter-x circle-dotted-letter-y circle-dotted-letter-z circle-dotted circle-half-2 circle-half-vertical circle-half circle-key circle-letter-a
circle-letter-b circle-letter-c circle-letter-d circle-letter-e circle-letter-f circle-letter-g circle-letter-h circle-letter-i circle-letter-j circle-letter-k
circle-letter-l circle-letter-m circle-letter-n circle-letter-o circle-letter-p circle-letter-q circle-letter-r circle-letter-s circle-letter-t circle-letter-u
circle-letter-v circle-letter-w circle-letter-x circle-letter-y circle-letter-z circle-minus-2 circle-minus circle-number-0 circle-number-1 circle-number-2
circle-number-3 circle-number-4 circle-number-5 circle-number-6 circle-number-7 circle-number-8 circle-number-9 circle-off circle-open-arrow-down circle-open-arrow-left
circle-open-arrow-right circle-open-arrow-up circle-percentage circle-plus-2 circle-plus-minus circle-plus circle-rectangle-off circle-rectangle circle-square circle-triangle
circle-x circle circles-relation circles circuit-ammeter circuit-battery circuit-bulb circuit-capacitor-polarized circuit-capacitor circuit-cell-plus
circuit-cell circuit-changeover circuit-diode-zener circuit-diode circuit-ground-digital circuit-ground circuit-inductor circuit-motor circuit-pushbutton circuit-resistor
circuit-switch-closed circuit-switch-open circuit-voltmeter clear-all clear-formatting clef-staff clef click cliff-jumping clipboard-check
clipboard-copy clipboard-data clipboard-heart clipboard-list clipboard-off clipboard-plus clipboard-search clipboard-smile clipboard-text clipboard-typography
clipboard-x clipboard clock-12 clock-2 clock-24 clock-bitcoin clock-bolt clock-cancel clock-check clock-code
clock-cog clock-dollar clock-down clock-edit clock-exclamation clock-heart clock-hour-1 clock-hour-10 clock-hour-11 clock-hour-12
clock-hour-2 clock-hour-3 clock-hour-4 clock-hour-5 clock-hour-6 clock-hour-7 clock-hour-8 clock-hour-9 clock-minus clock-off
clock-pause clock-pin clock-play clock-plus clock-question clock-record clock-search clock-share clock-shield clock-star
clock-stop clock-up clock-x clock clothes-rack-off clothes-rack cloud-bitcoin cloud-bolt cloud-cancel cloud-check
cloud-code cloud-cog cloud-computing cloud-data-connection cloud-dollar cloud-down cloud-download cloud-exclamation cloud-fog cloud-heart
cloud-lock-open cloud-lock cloud-minus cloud-network cloud-off cloud-pause cloud-pin cloud-plus cloud-question cloud-rain
cloud-search cloud-share cloud-snow cloud-star cloud-storm cloud-up cloud-upload cloud-x cloud clover-2
clover clubs code-ai code-asterisk code-circle-2 code-circle code-dots code-minus code-off code-plus
code-variable-minus code-variable-plus code-variable code codeblock coffee-off coffee coffin coin-bitcoin coin-euro
coin-monero coin-off coin-pound coin-rupee coin-taka coin-yen coin-yuan coin coins color-filter
color-picker-off color-picker color-swatch-off color-swatch column-insert-left column-insert-right column-remove columns-1 columns-2 columns-3
columns-off columns comet command-off command compass-off compass components-off components cone-2
cone-off cone-plus cone confetti-off confetti confucius congruent-to connection container-off container
contract contrast-2-off contrast-2 contrast-off contrast cooker cookie-man cookie-off cookie copy-check
copy-minus copy-off copy-plus copy-x copy copyleft-off copyleft copyright-off copyright corner-down-left-double
corner-down-left corner-down-right-double corner-down-right corner-left-down-double corner-left-down corner-left-up-double corner-left-up corner-right-down-double corner-right-down corner-right-up-double
corner-right-up corner-up-left-double corner-up-left corner-up-right-double corner-up-right cpu-2 cpu-off cpu crane-off crane
creative-commons-by creative-commons-nc creative-commons-nd creative-commons-off creative-commons-sa creative-commons-zero creative-commons credit-card-hand credit-card-off credit-card-pay
credit-card-refund credit-card credits cricket crop-1-1 crop-16-9 crop-3-2 crop-5-4 crop-7-5 crop-landscape
crop-portrait crop cross-off cross crosshair crown-off crown crutches-off crutches crystal-ball
csv cube-3d-sphere-off cube-3d-sphere cube-off cube-plus cube-send cube-spark cube-unfolded cube cup-off
cup curling curly-loop currency-afghani currency-bahraini currency-baht currency-bitcoin currency-cent currency-dinar currency-dirham
currency-dogecoin currency-dollar-australian currency-dollar-brunei currency-dollar-canadian currency-dollar-guyanese currency-dollar-off currency-dollar-singapore currency-dollar-zimbabwean currency-dollar currency-dong
currency-dram currency-ethereum currency-euro-off currency-euro currency-florin currency-forint currency-frank currency-guarani currency-hryvnia currency-husd
currency-iranian-rial currency-kip currency-krone-czech currency-krone-danish currency-krone-swedish currency-lari currency-leu currency-lira currency-litecoin currency-lyd
currency-manat currency-monero currency-naira currency-nano currency-off currency-paanga currency-peso currency-pound-off currency-pound currency-quetzal
currency-real currency-renminbi currency-ripple currency-riyal currency-ruble currency-rufiyaa currency-rupee-nepalese currency-rupee currency-shekel currency-solana
currency-som currency-taka currency-tenge currency-tether currency-tugrik currency-won currency-xrp currency-yen-off currency-yen currency-yuan
currency-zcash currency-zloty currency current-location-off current-location cursor-off cursor-text cut cylinder-off cylinder-plus
cylinder dashboard-off dashboard database-cog database-dollar database-edit database-exclamation database-export database-heart database-import
database-leak database-minus database-off database-plus database-search database-share database-smile database-star database-x database
deaf decimal deer delta dental-broken dental-off dental deselect desk details-off
details device-3d-camera device-3d-lens device-airpods-case device-airpods device-airtag device-analytics device-audio-tape device-camera-phone device-cctv-off
device-cctv device-computer-camera-2 device-computer-camera-off device-computer-camera device-desktop-analytics device-desktop-bolt device-desktop-cancel device-desktop-check device-desktop-code device-desktop-cog
device-desktop-dollar device-desktop-down device-desktop-exclamation device-desktop-heart device-desktop-minus device-desktop-off device-desktop-pause device-desktop-pin device-desktop-plus device-desktop-question
device-desktop-search device-desktop-share device-desktop-star device-desktop-up device-desktop-x device-desktop device-floppy device-gamepad-2 device-gamepad-3 device-gamepad
device-heart-monitor device-imac-bolt device-imac-cancel device-imac-check device-imac-code device-imac-cog device-imac-dollar device-imac-down device-imac-exclamation device-imac-heart
device-imac-minus device-imac-off device-imac-pause device-imac-pin device-imac-plus device-imac-question device-imac-search device-imac-share device-imac-star device-imac-up
device-imac-x device-imac device-ipad-bolt device-ipad-cancel device-ipad-check device-ipad-code device-ipad-cog device-ipad-dollar device-ipad-down device-ipad-exclamation
device-ipad-heart device-ipad-horizontal-bolt device-ipad-horizontal-cancel device-ipad-horizontal-check device-ipad-horizontal-code device-ipad-horizontal-cog device-ipad-horizontal-dollar device-ipad-horizontal-down device-ipad-horizontal-exclamation device-ipad-horizontal-heart
device-ipad-horizontal-minus device-ipad-horizontal-off device-ipad-horizontal-pause device-ipad-horizontal-pin device-ipad-horizontal-plus device-ipad-horizontal-question device-ipad-horizontal-search device-ipad-horizontal-share device-ipad-horizontal-star device-ipad-horizontal-up
device-ipad-horizontal-x device-ipad-horizontal device-ipad-minus device-ipad-off device-ipad-pause device-ipad-pin device-ipad-plus device-ipad-question device-ipad-search device-ipad-share
device-ipad-star device-ipad-up device-ipad-x device-ipad device-landline-phone device-laptop-off device-laptop device-mobile-bolt device-mobile-cancel device-mobile-charging
device-mobile-check device-mobile-code device-mobile-cog device-mobile-dollar device-mobile-down device-mobile-exclamation device-mobile-heart device-mobile-message device-mobile-minus device-mobile-off
device-mobile-pause device-mobile-pin device-mobile-plus device-mobile-question device-mobile-rotated device-mobile-search device-mobile-share device-mobile-star device-mobile-up device-mobile-vibration
device-mobile-x device-mobile device-nintendo-off device-nintendo device-projector device-remote device-screen device-sd-card device-sim-1 device-sim-2
device-sim-3 device-sim device-speaker-off device-speaker device-tablet-bolt device-tablet-cancel device-tablet-check device-tablet-code device-tablet-cog device-tablet-dollar
device-tablet-down device-tablet-exclamation device-tablet-heart device-tablet-minus device-tablet-off device-tablet-pause device-tablet-pin device-tablet-plus device-tablet-question device-tablet-search
device-tablet-share device-tablet-star device-tablet-up device-tablet-x device-tablet device-tv-off device-tv-old device-tv device-unknown device-usb
device-vision-pro-wifi device-vision-pro device-watch-bolt device-watch-cancel device-watch-check device-watch-code device-watch-cog device-watch-dollar device-watch-down device-watch-exclamation
device-watch-heart device-watch-minus device-watch-off device-watch-pause device-watch-pin device-watch-plus device-watch-question device-watch-search device-watch-share device-watch-star
device-watch-stats-2 device-watch-stats device-watch-up device-watch-x device-watch device-workstation devices-2 devices-bolt devices-cancel devices-check
devices-code devices-cog devices-dollar devices-down devices-exclamation devices-heart devices-minus devices-off devices-pause devices-pc-off
devices-pc devices-pin devices-plus devices-question devices-search devices-share devices-star devices-up devices-x devices
diabolo-off diabolo-plus diabolo dialpad-off dialpad diamond-off diamond diamonds diaper dice-1
dice-2 dice-3 dice-4 dice-5 dice-6 dice dimensions direction-arrows direction-horizontal direction-sign-off
direction-sign direction directions-off directions disabled-2 disabled-off disabled disc-golf disc-off disc
discount-off discount divide dna-2-off dna-2 dna-off dna dog-bowl dog door-enter
door-exit door-hanger door-off door dots-circle-horizontal dots-diagonal-2 dots-diagonal dots-vertical dots download-off
download drag-drop-2 drag-drop dragon drone-off drone drop-circle droplet-bolt droplet-cancel droplet-check
droplet-code droplet-cog droplet-dollar droplet-down droplet-exclamation droplet-half-2 droplet-half droplet-heart droplet-minus droplet-off
droplet-pause droplet-pin droplet-plus droplet-question droplet-search droplet-share droplet-star droplet-up droplet-x droplet
droplets dual-screen dumbbell dumpling e-passport ear-off ear-scan ear earphone-bluetooth ease-in-control-point
ease-in-out-control-points ease-in-out ease-in ease-out-control-point ease-out edit-circle-off edit-circle edit-off edit egg-cracked
egg-fried egg-off egg eggs elevator-off elevator email-stamp emergency-bed empathize-off empathize
emphasis engine-off engine equal-double equal-not equal eraser-off eraser error-404-off error-404
escalator-down escalator-up escalator exchange-off exchange exclamation-circle exclamation-mark-off exclamation-mark exercise-ball explicit-off
explicit exposure-0 exposure-minus-1 exposure-minus-2 exposure-off exposure-plus-1 exposure-plus-2 exposure external-link-off external-link
eye-bitcoin eye-bolt eye-cancel eye-check eye-closed eye-code eye-cog eye-discount eye-dollar eye-dotted
eye-down eye-edit eye-exclamation eye-heart eye-minus eye-off eye-pause eye-pin eye-plus eye-question
eye-search eye-share eye-spark eye-star eye-table eye-up eye-x eye eyeglass-2 eyeglass-off
eyeglass face-id-error face-id face-mask-off face-mask fall favicon feather-off feather fence-off
fence ferry fidget-spinner file-3d file-ai file-alert file-analytics file-arrow-left file-arrow-right file-barcode
file-bitcoin file-broken file-certificate file-chart file-check file-code-2 file-code file-cv file-database file-delta
file-description file-diff file-digit file-dislike file-dollar file-dots file-download file-euro file-excel file-export
file-function file-horizontal file-import file-infinity file-info file-invoice file-isr file-lambda file-like file-minus
file-music file-neutral file-off file-orientation file-pencil file-percent file-phone file-plus file-power file-report
file-rss file-sad file-scissors file-search file-settings file-shredder file-signal file-smile file-spark file-spreadsheet
file-stack file-star file-symlink file-text-ai file-text-shield file-text-spark file-text file-time file-type-bmp file-type-css
file-type-csv file-type-doc file-type-docx file-type-html file-type-jpg file-type-js file-type-jsx file-type-pdf file-type-php file-type-png
file-type-ppt file-type-rs file-type-sql file-type-svg file-type-ts file-type-tsx file-type-txt file-type-vue file-type-xls file-type-xml
file-type-zip file-typography file-unknown file-upload file-vector file-word file-x file-zip file files-off
files filter-2-bolt filter-2-cancel filter-2-check filter-2-code filter-2-cog filter-2-discount filter-2-dollar filter-2-down filter-2-edit
filter-2-exclamation filter-2-minus filter-2-pause filter-2-pin filter-2-plus filter-2-question filter-2-search filter-2-share filter-2-spark filter-2-up
filter-2-x filter-2 filter-bolt filter-cancel filter-check filter-code filter-cog filter-discount filter-dollar filter-down
filter-edit filter-exclamation filter-heart filter-minus filter-off filter-pause filter-pin filter-plus filter-question filter-search
filter-share filter-spark filter-star filter-up filter-x filter filters fingerprint-off fingerprint-scan fingerprint
fire-extinguisher fire-hydrant-off fire-hydrant firetruck firewall-check firewall-flame first-aid-kit-off first-aid-kit fish-bone fish-christianity
fish-hook-off fish-hook fish-off fish flag-2-off flag-2 flag-3 flag-bitcoin flag-bolt flag-cancel
flag-check flag-code flag-cog flag-discount flag-dollar flag-down flag-exclamation flag-heart flag-minus flag-off
flag-pause flag-pin flag-plus flag-question flag-search flag-share flag-spark flag-star flag-up flag-x
flag flame-off flame flare flask-2-off flask-2 flask-off flask flip-flops flip-horizontal
flip-vertical float-center float-left float-none float-right flood flower-off flower focus-2 focus-auto
focus-centered focus fold-down fold-up fold folder-bolt folder-cancel folder-check folder-code folder-cog
folder-dollar folder-down folder-exclamation folder-heart folder-minus folder-off folder-open folder-pause folder-pin folder-plus
folder-question folder-root folder-search folder-share folder-star folder-symlink folder-up folder-x folder folders-off
folders footsteps forbid-2 forbid forklift forms fountain-off fountain frame-off frame
free-rights freeze-column freeze-row-column freeze-row fridge-off fridge friends-off friends frustum-off frustum-plus
frustum function-off function galaxy garden-cart-off garden-cart gas-station-off gas-station gauge-off gauge
gavel gender-agender gender-androgyne gender-bigender gender-demiboy gender-demigirl gender-epicene gender-female gender-femme gender-genderfluid
gender-genderless gender-genderqueer gender-hermaphrodite gender-intergender gender-male gender-neutrois gender-third gender-transgender gender-travesti geometry
ghost-2 ghost-3 ghost-off ghost gif gift-card gift-off gift git-branch-deleted git-branch
git-cherry-pick git-commit git-compare git-fork git-merge git-pull-request-closed git-pull-request-conflict git-pull-request-draft git-pull-request gizmo
glass-champagne glass-cocktail glass-full glass-gin glass-off glass globe-off globe go-game golf-off
golf gps gradienter grain grape graph-off graph grave-2 grave grid-3x3
grid-4x4 grid-dots grid-goldenratio grid-pattern grid-scan grill-fork grill-off grill-spatula grill grip-horizontal
grip-vertical growth guitar-pick gymnastics h-1 h-2 h-3 h-4 h-5 h-6
hammer-drill hammer-off hammer hand-click-off hand-click hand-finger-down hand-finger-left hand-finger-off hand-finger-right hand-finger
hand-grab hand-little-finger hand-love-you hand-middle-finger hand-move hand-off hand-ring-finger hand-sanitizer hand-stop hand-three-fingers
hand-two-fingers hanger-2 hanger-off hanger hash haze-moon haze hdr heading-off heading
headphones-off headphones headset-off headset health-recognition heart-bitcoin heart-bolt heart-broken heart-cancel heart-check
heart-code heart-cog heart-discount heart-dollar heart-down heart-exclamation heart-handshake heart-minus heart-off heart-pause
heart-pin heart-plus heart-question heart-rate-monitor heart-search heart-share heart-spark heart-star heart-up heart-x
heart heartbeat hearts-off hearts helicopter-landing helicopter helmet-off helmet help-circle help-hexagon
help-octagon help-off help-small help-square-rounded help-square help-triangle help hemisphere-off hemisphere-plus hemisphere
hexagon-3d hexagon-asterisk hexagon-letter-a hexagon-letter-b hexagon-letter-c hexagon-letter-d hexagon-letter-e hexagon-letter-f hexagon-letter-g hexagon-letter-h
hexagon-letter-i hexagon-letter-j hexagon-letter-k hexagon-letter-l hexagon-letter-m hexagon-letter-n hexagon-letter-o hexagon-letter-p hexagon-letter-q hexagon-letter-r
hexagon-letter-s hexagon-letter-t hexagon-letter-u hexagon-letter-v hexagon-letter-w hexagon-letter-x hexagon-letter-y hexagon-letter-z hexagon-minus-2 hexagon-minus
hexagon-number-0 hexagon-number-1 hexagon-number-2 hexagon-number-3 hexagon-number-4 hexagon-number-5 hexagon-number-6 hexagon-number-7 hexagon-number-8 hexagon-number-9
hexagon-off hexagon-plus-2 hexagon-plus hexagon hexagonal-prism-off hexagonal-prism-plus hexagonal-prism hexagonal-pyramid-off hexagonal-pyramid-plus hexagonal-pyramid
hexagons-off hexagons hierarchy-2 hierarchy-3 hierarchy-off hierarchy highlight-off highlight history-off history-toggle
history home-2 home-bitcoin home-bolt home-cancel home-check home-cog home-dollar home-dot home-down
home-eco home-edit home-exclamation home-hand home-heart home-infinity home-link home-lock home-minus home-move
home-off home-plus home-question home-ribbon home-search home-share home-shield home-signal home-spark home-star
home-stats home-up home-x home horse-toy horse horseshoe hospital-circle hospital hotel-service
hourglass-empty hourglass-high hourglass-low hourglass-off hourglass hours-12 hours-24 html http-connect-off http-connect
http-delete-off http-delete http-get-off http-get http-head-off http-head http-options-off http-options http-patch-off http-patch
http-post-off http-post http-put-off http-put http-que-off http-que http-trace-off http-trace hula-hoop ice-cream-2
ice-cream-off ice-cream ice-skating iceberg icons-off icons icosahedron id-badge-2 id-badge-off id-badge
id-off id image-generation image-in-picture inbox-off inbox indent-decrease indent-increase infinity-2 infinity-off
infinity info-circle info-hexagon info-octagon info-small info-square-rounded info-square info-triangle inner-shadow-bottom-left inner-shadow-bottom-right
inner-shadow-bottom inner-shadow-left inner-shadow-right inner-shadow-top-left inner-shadow-top-right inner-shadow-top input-ai input-check input-search input-spark
input-x invoice ironing-1 ironing-2 ironing-3 ironing-off ironing-steam-off ironing-steam ironing irregular-polyhedron-off
irregular-polyhedron-plus irregular-polyhedron italic-off italic jacket jetpack jetski jewish-star join-bevel join-round
join-straight joker jpg json jump-rope karate kayak kerning key-off key
keyboard-hide keyboard-off keyboard-show keyboard keyframe-align-center keyframe-align-horizontal keyframe-align-vertical keyframe keyframes label-important
label-off label ladder-off ladder ladle lambda lamp-2 lamp-off lamp lane
language-hiragana language-katakana language-off language lasso-off lasso-polygon lasso laurel-wreath-1 laurel-wreath-2 laurel-wreath-3
laurel-wreath lawn-mower layers-difference layers-intersect-2 layers-intersect layers-linked layers-off layers-selected-bottom layers-selected layers-subtract
layers-union layout-2 layout-align-bottom layout-align-center layout-align-left layout-align-middle layout-align-right layout-align-top layout-board-split layout-board
layout-bottombar-collapse layout-bottombar-expand layout-bottombar-inactive layout-bottombar layout-cards layout-collage layout-columns layout-dashboard layout-distribute-horizontal layout-distribute-vertical
layout-grid-add layout-grid-remove layout-grid layout-kanban layout-list layout-navbar-collapse layout-navbar-expand layout-navbar-inactive layout-navbar layout-off
layout-rows layout-sidebar-inactive layout-sidebar-left-collapse layout-sidebar-left-expand layout-sidebar-right-collapse layout-sidebar-right-expand layout-sidebar-right-inactive layout-sidebar-right layout-sidebar layout
leaf-2 leaf-maple leaf-off leaf lego-off lego lemon-2 lemon letter-a-small letter-a
letter-b-small letter-b letter-c-small letter-c letter-case-lower letter-case-toggle letter-case-upper letter-case letter-d-small letter-d
letter-e-small letter-e letter-f-small letter-f letter-g-small letter-g letter-h-small letter-h letter-i-small letter-i
letter-j-small letter-j letter-k-small letter-k letter-l-small letter-l letter-m-small letter-m letter-n-small letter-n
letter-o-small letter-o letter-p-small letter-p letter-q-small letter-q letter-r-small letter-r letter-s-small letter-s
letter-spacing letter-t-small letter-t letter-u-small letter-u letter-v-small letter-v letter-w-small letter-w letter-x-small
letter-x letter-y-small letter-y letter-z-small letter-z library-minus library-photo library-plus library license-off
license lifebuoy-off lifebuoy lighter line-dashed line-dotted line-height line-scan line link-minus
link-off link-plus link list-check list-details list-letters list-numbers list-search list-tree list
live-photo-off live-photo live-view load-balancer loader-2 loader-3 loader-4 loader-quarter loader location-bolt
location-broken location-cancel location-check location-code location-cog location-discount location-dollar location-down location-exclamation location-heart
location-minus location-off location-pause location-pin location-plus location-question location-search location-share location-star location-up
location-x location lock-access-off lock-access lock-bitcoin lock-bolt lock-cancel lock-check lock-code lock-cog
lock-dollar lock-down lock-exclamation lock-heart lock-minus lock-off lock-open-2 lock-open-off lock-open lock-password
lock-pause lock-pin lock-plus lock-question lock-search lock-share lock-square-rounded lock-square lock-star lock-up
lock-x lock logic-and logic-buffer logic-nand logic-nor logic-not logic-or logic-xnor logic-xor
login-2 login logout-2 logout logs lollipop-off lollipop luggage-off luggage lungs-off
lungs macro-off macro magnet-off magnet magnetic mail-ai mail-bitcoin mail-bolt mail-cancel
mail-check mail-code mail-cog mail-dollar mail-down mail-exclamation mail-fast mail-forward mail-heart mail-minus
mail-off mail-opened mail-pause mail-pin mail-plus mail-question mail-search mail-share mail-spark mail-star
mail-up mail-x mail mailbox-off mailbox man manual-gearbox map-2 map-bolt map-cancel
map-check map-code map-cog map-discount map-dollar map-down map-east map-exclamation map-heart map-lock
map-minus map-north map-off map-pause map-pin-2 map-pin-bolt map-pin-cancel map-pin-check map-pin-code map-pin-cog
map-pin-dollar map-pin-down map-pin-exclamation map-pin-heart map-pin-minus map-pin-off map-pin-pause map-pin-pin map-pin-plus map-pin-question
map-pin-search map-pin-share map-pin-star map-pin-up map-pin-x map-pin map-pins map-plus map-question map-route
map-search map-share map-shield map-south map-star map-up map-west map-x map markdown-off
markdown marquee-2 marquee-off marquee mars mask-off mask masks-theater-off masks-theater massage
matchstick math-1-divide-2 math-1-divide-3 math-avg math-cos math-ctg math-equal-greater math-equal-lower math-function-off math-function-y
math-function math-greater math-integral-x math-integral math-integrals math-lower math-max-min math-max math-min math-not
math-off math-pi-divide-2 math-pi math-sec math-sin math-symbols math-tg math-x-divide-2 math-x-divide-y-2 math-x-divide-y
math-x-floor-divide-y math-x-minus-x math-x-minus-y math-x-plus-x math-x-plus-y math-xy math-y-minus-y math-y-plus-y math matrix
maximize-off maximize meat-off meat medal-2 medal medical-cross-circle medical-cross-off medical-cross medicine-syrup
meeple melon menorah menu-2 menu-3 menu-4 menu-deep menu-order menu mesh
message-2-bolt message-2-cancel message-2-check message-2-code message-2-cog message-2-dollar message-2-down message-2-exclamation message-2-heart message-2-minus
message-2-off message-2-pause message-2-pin message-2-plus message-2-question message-2-search message-2-share message-2-star message-2-up message-2-x
message-2 message-bolt message-cancel message-chatbot message-check message-circle-bolt message-circle-cancel message-circle-check message-circle-code message-circle-cog
message-circle-dollar message-circle-down message-circle-exclamation message-circle-heart message-circle-minus message-circle-off message-circle-pause message-circle-pin message-circle-plus message-circle-question
message-circle-search message-circle-share message-circle-star message-circle-up message-circle-user message-circle-x message-circle message-code message-cog message-dollar
message-dots message-down message-exclamation message-forward message-heart message-language message-minus message-off message-pause message-pin
message-plus message-question message-reply message-report message-search message-share message-star message-up message-user message-x
message messages-off messages meteor-off meteor meter-cube meter-square metronome michelin-bib-gourmand michelin-star-green
michelin-star mickey microfrontends microphone-2-off microphone-2 microphone-off microphone microscope-off microscope microwave-off
microwave middleware military-award military-rank milk-off milk milkshake minimize minus-vertical minus
mist-off mist mobiledata-off mobiledata moneybag-edit moneybag-heart moneybag-minus moneybag-move-back moneybag-move moneybag-plus
moneybag monkeybar mood-angry mood-annoyed-2 mood-annoyed mood-bitcoin mood-boy mood-check mood-cog mood-confused
mood-crazy-happy mood-cry mood-dollar mood-edit mood-empty mood-happy mood-heart mood-kid mood-look-down mood-look-left
mood-look-right mood-look-up mood-minus mood-nerd mood-nervous mood-neutral mood-off mood-pin mood-plus mood-puzzled
mood-sad-2 mood-sad-dizzy mood-sad-squint mood-sad mood-search mood-share mood-sick mood-silence mood-sing mood-smile-beam
mood-smile-dizzy mood-smile mood-spark mood-surprised mood-tongue-wink-2 mood-tongue-wink mood-tongue mood-unamused mood-up mood-wink-2
mood-wink mood-wrrr mood-x mood-xd moon-2 moon-off moon-stars moon moped mosque
motorbike mountain-off mountain mouse-2 mouse-off mouse moustache movie-off movie mug-off
mug multiplier-0-5x multiplier-1-5x multiplier-1x multiplier-2x mushroom-off mushroom music-bolt music-cancel music-check
music-code music-cog music-discount music-dollar music-down music-exclamation music-heart music-minus music-off music-pause
music-pin music-plus music-question music-search music-share music-star music-up music-x music navigation-bolt
navigation-cancel navigation-check navigation-code navigation-cog navigation-discount navigation-dollar navigation-down navigation-east navigation-exclamation navigation-heart
navigation-minus navigation-north navigation-off navigation-pause navigation-pin navigation-plus navigation-question navigation-search navigation-share navigation-south
navigation-star navigation-top navigation-up navigation-west navigation-x navigation needle-thread needle network-off network
new-section news-off news nfc-off nfc no-copyright no-creative-commons no-derivatives noise-reduction north-star
notdef note-off note notebook-off notebook notes-off notes notification-off notification number-0-small
number-0 number-1-small number-1 number-10-small number-10 number-100-small number-11-small number-11 number-12-small number-123
number-13-small number-14-small number-15-small number-16-small number-17-small number-18-small number-19-small number-2-small number-2 number-20-small
number-21-small number-22-small number-23-small number-24-small number-25-small number-26-small number-27-small number-28-small number-29-small number-3-small
number-3 number-30-small number-31-small number-32-small number-33-small number-34-small number-35-small number-36-small number-37-small number-38-small
number-39-small number-4-small number-4 number-40-small number-41-small number-42-small number-43-small number-44-small number-45-small number-46-small
number-47-small number-48-small number-49-small number-5-small number-5 number-50-small number-51-small number-52-small number-53-small number-54-small
number-55-small number-56-small number-57-small number-58-small number-59-small number-6-small number-6 number-60-small number-61-small number-62-small
number-63-small number-64-small number-65-small number-66-small number-67-small number-68-small number-69-small number-7-small number-7 number-70-small
number-71-small number-72-small number-73-small number-74-small number-75-small number-76-small number-77-small number-78-small number-79-small number-8-small
number-8 number-80-small number-81-small number-82-small number-83-small number-84-small number-85-small number-86-small number-87-small number-88-small
number-89-small number-9-small number-9 number-90-small number-91-small number-92-small number-93-small number-94-small number-95-small number-96-small
number-97-small number-98-small number-99-small number numbers nurse nut object-scan octagon-minus-2 octagon-minus
octagon-off octagon-plus-2 octagon-plus octagon octahedron-off octahedron-plus octahedron old olympic-torch olympics-off
olympics om omega option outbound outlet oval-vertical oval overline package-export
package-import package-off package packages pacman page-break paint-off paint palette-off palette
panorama-horizontal-off panorama-horizontal panorama-vertical-off panorama-vertical paper-bag-off paper-bag paperclip parachute-off parachute parentheses-off
parentheses parking-circle parking-meter parking-off parking password-fingerprint password-mobile-phone password-user password paw-off
paw paywall pdf peace pencil-bolt pencil-cancel pencil-check pencil-code pencil-cog pencil-discount
pencil-dollar pencil-down pencil-exclamation pencil-heart pencil-minus pencil-off pencil-pause pencil-pin pencil-plus pencil-question
pencil-search pencil-share pencil-star pencil-up pencil-x pencil pendulum pennant-2 pennant-off pennant
pentagon-minus pentagon-number-0 pentagon-number-1 pentagon-number-2 pentagon-number-3 pentagon-number-4 pentagon-number-5 pentagon-number-6 pentagon-number-7 pentagon-number-8
pentagon-number-9 pentagon-off pentagon-plus pentagon-x pentagon pentagram pepper-off pepper percentage-0 percentage-10
percentage-100 percentage-20 percentage-25 percentage-30 percentage-33 percentage-40 percentage-50 percentage-60 percentage-66 percentage-70
percentage-75 percentage-80 percentage-90 percentage perfume perspective-off perspective phone-call phone-calling phone-check
phone-done phone-end phone-incoming phone-off phone-outgoing phone-pause phone-plus phone-ringing phone-spark phone-x
phone photo-ai photo-alt photo-bitcoin photo-bolt photo-cancel photo-check photo-circle-minus photo-circle-plus photo-circle
photo-code photo-cog photo-dollar photo-down photo-edit photo-exclamation photo-heart photo-hexagon photo-minus photo-off
photo-pause photo-pentagon photo-pin photo-plus photo-question photo-scan photo-search photo-sensor-2 photo-sensor-3 photo-sensor
photo-share photo-shield photo-spark photo-square-rounded photo-star photo-up photo-video photo-x photo physiotherapist
piano pick picnic-table picture-in-picture-off picture-in-picture-on picture-in-picture-top picture-in-picture pig-money pig-off pig
pilcrow-left pilcrow-right pilcrow pill-off pill pillow pills pin-end pin-invoke pin
ping-pong pinned-off pinned pipeline pizza-off pizza placeholder plane-arrival plane-departure plane-inflight
plane-off plane-tilt plane planet-off planet plant-2-off plant-2 plant-off plant play-basketball
play-bug play-bugs play-card-1 play-card-10 play-card-2 play-card-3 play-card-4 play-card-5 play-card-6 play-card-7
play-card-8 play-card-9 play-card-a play-card-j play-card-k play-card-off play-card-q play-card-star play-card play-football
play-handball play-volleyball player-eject player-pause player-play player-record player-skip-back player-skip-forward player-stop player-track-next
player-track-prev playlist-add playlist-off playlist-x playlist playstation-circle playstation-square playstation-triangle playstation-x plug-connected-x
plug-connected plug-off plug-x plug plunger plus-equal plus-minus plus png podium-off
podium point-off point pointer-2 pointer-bolt pointer-cancel pointer-check pointer-code pointer-cog pointer-collaboration-2
pointer-collaboration pointer-dollar pointer-down pointer-exclamation pointer-heart pointer-minus pointer-off pointer-pause pointer-pin pointer-plus
pointer-question pointer-search pointer-share pointer-star pointer-up pointer-x pointer pokeball-off pokeball poker-chip
polaroid polygon-off polygon poo pool-off pool power pray premium-rights prescription
presentation-analytics presentation-off presentation printer-off printer prism-light prism-off prism-plus prism prison
progress-alert progress-bolt progress-check progress-down progress-help progress-x progress prompt prong propeller-off
propeller protocol pumpkin-scary puzzle-2 puzzle-off puzzle pyramid-off pyramid-plus pyramid qrcode-off
qrcode question-mark queue-pop-in queue-pop-out quote-off quote-open quote quotes radar-2 radar-off
radar radio-off radio radioactive-off radioactive radius-bottom-left radius-bottom-right radius-top-left radius-top-right rainbow-off
rainbow rating-12-plus rating-14-plus rating-16-plus rating-18-plus rating-21-plus razor-electric razor receipt-2 receipt-bitcoin
receipt-dollar receipt-euro receipt-off receipt-pound receipt-refund receipt-rupee receipt-tax receipt-yen receipt-yuan receipt
recharging record-mail-off record-mail rectangle-rounded-bottom rectangle-rounded-top rectangle-vertical rectangle rectangular-prism-off rectangular-prism-plus rectangular-prism
recycle-off recycle refresh-alert refresh-dot refresh-off refresh regex-off regex registered relation-many-to-many
relation-one-to-many relation-one-to-one reload remote-control reorder repeat-off repeat-once repeat replace-off replace-user
replace report-analytics report-medical report-money report-off report-search report reserved-line resize restore
rewind-backward-10 rewind-backward-15 rewind-backward-20 rewind-backward-30 rewind-backward-40 rewind-backward-5 rewind-backward-50 rewind-backward-60 rewind-forward-10 rewind-forward-15
rewind-forward-20 rewind-forward-30 rewind-forward-40 rewind-forward-5 rewind-forward-50 rewind-forward-60 ribbon-health rings ripple-down ripple-off
ripple-up ripple road-off road-sign road robot-face robot-off robot rocket-off rocket
rocking-chair roller-skating rollercoaster-off rollercoaster rosette-asterisk rosette-discount-check-off rosette-discount-check rosette-discount-off rosette-discount rosette-number-0
rosette-number-1 rosette-number-2 rosette-number-3 rosette-number-4 rosette-number-5 rosette-number-6 rosette-number-7 rosette-number-8 rosette-number-9 rosette
rotate-2 rotate-360 rotate-3d rotate-clockwise-2 rotate-clockwise rotate-dot rotate-rectangle rotate roulette route-2
route-alt-left route-alt-right route-off route-scan route-square-2 route-square route-x-2 route-x route router-off
router row-insert-bottom row-insert-top row-remove rss rubber-stamp-off rubber-stamp rugby ruler-2-off ruler-2
ruler-3 ruler-measure-2 ruler-measure ruler-off ruler run-sprint run rv-truck s-turn-down s-turn-left
s-turn-right s-turn-up sailboat-2 sailboat-off sailboat salad salt sandbox satellite-off satellite
sausage scale-off scale-outline-off scale-outline scale scan-cube scan-eye scan-letter-a scan-letter-t scan-position
scan-traces scan schema-off schema school-bell school-off school scissors-off scissors scooter-electric
scooter scoreboard screen-share-off screen-share screenshot scribble-off scribble script-minus script-plus script-x
script scuba-diving-tank scuba-diving scuba-mask-off scuba-mask sdk search-off search section-sign section
seedling-off seedling segway select-all select selector send-2 send-off send seo
separator-horizontal separator-vertical separator server-2 server-bolt server-cog server-off server-spark server serverless
servicemark settings-2 settings-ai settings-automation settings-bolt settings-cancel settings-check settings-code settings-cog settings-dollar
settings-down settings-exclamation settings-heart settings-minus settings-off settings-pause settings-pin settings-plus settings-question settings-search
settings-share settings-spark settings-star settings-up settings-x settings shadow-off shadow shape-2 shape-3
shape-off shape share-2 share-3 share-off share shareplay shield-bolt shield-cancel shield-check
shield-checkered shield-chevron shield-code shield-cog shield-dollar shield-down shield-exclamation shield-half shield-heart shield-lock
shield-minus shield-off shield-pause shield-pin shield-plus shield-question shield-search shield-share shield-star shield-up
shield-x shield ship-off ship shirt-off shirt-sport shirt shoe-off shoe shopping-bag-check
shopping-bag-discount shopping-bag-edit shopping-bag-exclamation shopping-bag-heart shopping-bag-minus shopping-bag-plus shopping-bag-search shopping-bag-x shopping-bag shopping-cart-bolt
shopping-cart-cancel shopping-cart-check shopping-cart-code shopping-cart-cog shopping-cart-copy shopping-cart-discount shopping-cart-dollar shopping-cart-down shopping-cart-exclamation shopping-cart-heart
shopping-cart-minus shopping-cart-off shopping-cart-pause shopping-cart-pin shopping-cart-plus shopping-cart-question shopping-cart-search shopping-cart-share shopping-cart-star shopping-cart-up
shopping-cart-x shopping-cart shovel-pitchforks shovel shredder sign-left sign-right signal-2g signal-3g signal-4g-plus
signal-4g signal-5g signal-6g signal-e signal-g signal-h-plus signal-h signal-lte signature-off signature
sitemap-off sitemap skateboard-off skateboard skateboarding sketching skew-x skew-y ski-jumping skull
slash slashes sleigh slice slideshow smart-home-off smart-home smoking-no smoking snowboarding
snowflake-off snowflake snowman soccer-field social-off social sock sofa-off sofa solar-electricity
solar-panel-2 solar-panel sort-0-9 sort-9-0 sort-a-z sort-ascending-2 sort-ascending-letters sort-ascending-numbers sort-ascending-shapes sort-ascending-small-big
sort-ascending sort-descending-2 sort-descending-letters sort-descending-numbers sort-descending-shapes sort-descending-small-big sort-descending sort-z-a sos soup-off
soup source-code space-off space spaces spacing-horizontal spacing-vertical spade sparkle-2 sparkle-highlight
sparkle sparkles-2-off sparkles-2 sparkles speakerphone speedboat sphere-2 sphere-off sphere-plus sphere
spider spiral-off spiral sport-billiard spray spy-off spy sql square-arrow-down square-arrow-left
square-arrow-right square-arrow-up square-asterisk square-check square-chevron-down square-chevron-left square-chevron-right square-chevron-up square-chevrons-down square-chevrons-left
square-chevrons-right square-chevrons-up square-dashed square-dot square-f0 square-f1 square-f2 square-f3 square-f4 square-f5
square-f6 square-f7 square-f8 square-f9 square-forbid-2 square-forbid square-half square-key square-letter-a square-letter-b
square-letter-c square-letter-d square-letter-e square-letter-f square-letter-g square-letter-h square-letter-i square-letter-j square-letter-k square-letter-l
square-letter-m square-letter-n square-letter-o square-letter-p square-letter-q square-letter-r square-letter-s square-letter-t square-letter-u square-letter-v
square-letter-w square-letter-x square-letter-y square-letter-z square-minus-2 square-minus square-number-0 square-number-1 square-number-2 square-number-3
square-number-4 square-number-5 square-number-6 square-number-7 square-number-8 square-number-9 square-off square-percentage square-plus-2 square-plus
square-root-2 square-root square-rotated-asterisk square-rotated-forbid-2 square-rotated-forbid square-rotated-off square-rotated square-rounded-arrow-down square-rounded-arrow-left square-rounded-arrow-right
square-rounded-arrow-up square-rounded-check square-rounded-chevron-down square-rounded-chevron-left square-rounded-chevron-right square-rounded-chevron-up square-rounded-chevrons-down square-rounded-chevrons-left square-rounded-chevrons-right square-rounded-chevrons-up
square-rounded-letter-a square-rounded-letter-b square-rounded-letter-c square-rounded-letter-d square-rounded-letter-e square-rounded-letter-f square-rounded-letter-g square-rounded-letter-h square-rounded-letter-i square-rounded-letter-j
square-rounded-letter-k square-rounded-letter-l square-rounded-letter-m square-rounded-letter-n square-rounded-letter-o square-rounded-letter-p square-rounded-letter-q square-rounded-letter-r square-rounded-letter-s square-rounded-letter-t
square-rounded-letter-u square-rounded-letter-v square-rounded-letter-w square-rounded-letter-x square-rounded-letter-y square-rounded-letter-z square-rounded-minus-2 square-rounded-minus square-rounded-number-0 square-rounded-number-1
square-rounded-number-2 square-rounded-number-3 square-rounded-number-4 square-rounded-number-5 square-rounded-number-6 square-rounded-number-7 square-rounded-number-8 square-rounded-number-9 square-rounded-percentage square-rounded-plus-2
square-rounded-plus square-rounded-x square-rounded square-toggle-horizontal square-toggle square-x square squares-diagonal squares-selected squares
stack-2 stack-3 stack-back stack-backward stack-forward stack-front stack-middle stack-pop stack-push stack
stairs-down stairs-up stairs star-half star-off star stars-off stars status-change steam
steering-wheel-off steering-wheel step-into step-out stereo-glasses stethoscope-off stethoscope sticker-2 sticker stopwatch
storm-off storm stretching-2 stretching strikethrough stroke-curved stroke-dynamic stroke-straight submarine subscript
subtask subtitles-ai subtitles-edit subtitles-off subtitles sum-off sum sun-electricity sun-high sun-low
sun-moon sun-off sun-wind sun sunglasses sunrise sunset-2 sunset superscript svg
swimming swipe-down swipe-left swipe-right swipe-up swipe switch-2 switch-3 switch-horizontal switch-vertical
switch sword-off sword swords tab-close table-alias table-column table-dashed table-down table-export
table-heart table-import table-minus table-off table-options table-plus table-row table-share table-shortcut table-spark
table tabs tag-minus tag-off tag-plus tag-starred tag tags-chevron-down tags-chevron-left tags-chevron-right
tags-chevron-up tags-off tags taiwan-dollar tallymark-1 tallymark-2 tallymark-3 tallymark-4 tallymarks tank
target-2 target-arrow target-off target tax-euro tax-pound tax teapot telescope-off telescope
temperature-celsius temperature-fahrenheit temperature-minus temperature-off temperature-plus temperature-snow temperature-sun temperature template-off template
tent-off tent terminal-2 terminal test-pipe-2 test-pipe-off test-pipe tex text-caption text-color
text-decrease text-direction-ltr text-direction-rtl text-grammar text-increase text-orientation text-outline text-plus text-recognition text-regex-asterisk
text-regex-end text-regex-plus text-regex-question text-regex-start text-resize text-scan-2 text-scan-ai text-size text-spellcheck text-wrap-column
text-wrap-disabled text-wrap texture theater thermometer thinking-high thinking-low thinking-medium thumb-down-off thumb-down
thumb-up-off thumb-up tic-tac ticket-off ticket tie tilde tilt-shift-off tilt-shift time-duration-0
time-duration-10 time-duration-15 time-duration-30 time-duration-45 time-duration-5 time-duration-60 time-duration-90 time-duration-off timeline-event-exclamation timeline-event-minus
timeline-event-plus timeline-event-text timeline-event-x timeline-event timeline timezone tip-jar-euro tip-jar-pound tip-jar tir
toggle-left toggle-right toilet-paper-off toilet-paper toml tool tools-kitchen-2-off tools-kitchen-2 tools-kitchen-3 tools-kitchen-off
tools-kitchen tools-off tools tooltip topology-bus topology-complex topology-full-hierarchy topology-full topology-ring-2 topology-ring-3
topology-ring topology-star-2 topology-star-3 topology-star-ring-2 topology-star-ring-3 topology-star-ring topology-star torii tornado tournament
tower-off tower track tractor trademark traffic-cone-off traffic-cone traffic-lights-off traffic-lights train
transaction-bitcoin transaction-dollar transaction-euro transaction-pound transaction-rupee transaction-yen transaction-yuan transfer-in transfer-out transfer-vertical
transfer transform-point-bottom-left transform-point-bottom-right transform-point-top-left transform-point-top-right transform-point transform transition-bottom transition-left transition-right
transition-top trash-off trash-x trash treadmill treasure-chest tree trees trekking trending-down-2
trending-down-3 trending-down trending-up-2 trending-up-3 trending-up-down trending-up triangle-inverted triangle-minus-2 triangle-minus triangle-off
triangle-plus-2 triangle-plus triangle-square-circle triangle triangles trident trolley trophy-off trophy trowel
truck-delivery truck-loading truck-off truck-return truck twig txt typeface typography-off typography
u-turn-left u-turn-right ufo-off ufo uhd umbrella-2 umbrella-closed-2 umbrella-closed umbrella-off umbrella
underline-off underline unicycle universe unlink upload urgent usb user-bitcoin user-bolt
user-cancel user-check user-circle user-code user-cog user-dollar user-down user-edit user-exclamation user-heart
user-hexagon user-key user-minus user-off user-pause user-pentagon user-pin user-plus user-question user-scan
user-screen user-search user-share user-shield user-square-rounded user-square user-star user-up user-x user
users-group users-minus users-plus users uv-index ux-circle vaccine-bottle-off vaccine-bottle vaccine-off vaccine
vacuum-cleaner variable-minus variable-off variable-plus variable vault vector-bezier-2 vector-bezier-arc vector-bezier-circle vector-bezier
vector-off vector-spline vector-triangle-off vector-triangle vector venus versions-off versions video-minus video-off
video-plus video view-360-arrow view-360-number view-360-off view-360 viewfinder-off viewfinder viewport-narrow viewport-short
viewport-tall viewport-wide vignette vinyl vip-2 vip-off vip virtual-space virus-off virus-search
virus vocabulary-off vocabulary volcano volume-2 volume-3 volume-4 volume-off volume vs
walk wall-off wall wallet-off wallet wallpaper-off wallpaper wand-off wand wash-dry-1
wash-dry-2 wash-dry-3 wash-dry-a wash-dry-dip wash-dry-f wash-dry-flat wash-dry-hang wash-dry-off wash-dry-p wash-dry-shade
wash-dry-w wash-dry wash-dryclean-off wash-dryclean wash-eco wash-gentle wash-hand wash-machine wash-off wash-press
wash-temperature-1 wash-temperature-2 wash-temperature-3 wash-temperature-4 wash-temperature-5 wash-temperature-6 wash-tumble-dry wash-tumble-off wash waterpolo
wave-saw-tool wave-sine wave-square waves-electricity webhook-off webhook weight wheat-off wheat wheel
wheelchair-off wheelchair whirl whisk wifi-0 wifi-1 wifi-2 wifi-off wifi wind-electricity
wind-off wind windmill-off windmill window-maximize window-minimize window-off window windsock wiper-wash
wiper woman wood world-bolt world-cancel world-check world-code world-cog world-dollar world-down
world-download world-exclamation world-heart world-latitude world-longitude world-map world-minus world-off world-pause world-pin
world-plus world-question world-search world-share world-star world-up world-upload world-www world-x world
wrecking-ball writing-off writing-sign-off writing-sign writing x-mark x-power-y x xbox-a xbox-b
xbox-x xbox-y xd xxx yarn yin-yang yoga zeppelin-off zeppelin zero-config
zip zodiac-aquarius zodiac-aries zodiac-cancer zodiac-capricorn zodiac-gemini zodiac-leo zodiac-libra zodiac-pisces zodiac-sagittarius
zodiac-scorpio zodiac-taurus zodiac-virgo zoom-cancel zoom-check zoom-code zoom-exclamation zoom-in-area zoom-in zoom-money
zoom-out-area zoom-out zoom-pan zoom-question zoom-replace zoom-reset zoom-scan zoom zzz-off zzz
]==]
)

end)() end,
    [81] = function()local wax,script,require=ImportGlobals(81)local ImportGlobals return (function(...)
-- [[Theme resolution engine (themes/init.luau).]]
-- [[]]
-- [[Extracted verbatim from components/window.luau so the window no longer owns]]
-- [[theme logic. Responsibilities: named-theme lookup against the themes folder,]]
-- [[value coercion (Color3 -> ColorSequence for gradient keys), stroke]]
-- [[derivation for user-supplied theme tables, and brand-font fallbacks.]]
-- [[Only the default theme module is required eagerly; named themes load on]]
-- [[demand when selected.]]

-- [[This file IS the `themes` ModuleScript (init.luau semantics): the theme]]
-- [[modules (default, frost, ...) are children of `script`, and other folders]]
-- [[(core, utilities) are siblings reachable through script.Parent.]]
local themesFolder = script
local variables = require(script.Parent.core.state)
local log = require(script.Parent.utilities.log)

local themes = {}

local gradientKeys = {
    WindowColor = true,
    ElementGradient = true,
    ElementStrokeGradient = true,
    TabBackground = true,
    TabStroke = true,
    SliderProgress = true,
}

function themes.coerceValue(key, value)
    if gradientKeys[key] and typeof(value) == "Color3" then
        return ColorSequence.new(value)
    end
    return value
end

function themes.firstColor(value)
    return if typeof(value) == "ColorSequence" then value.Keypoints[1].Value else value
end

local function edgeShade(color, amount)
    local luminance = 0.299 * color.R + 0.587 * color.G + 0.114 * color.B
    local target = if luminance > 0.5 then Color3.new(0, 0, 0) else Color3.new(1, 1, 1)
    return color:Lerp(target, amount)
end

-- [[Stroke derivation for user-supplied theme tables. Exposed so ChangeTheme]]
-- [[can re-derive derived stroke colors after merging user overrides.]]
function themes.deriveStrokes(resolved, overrides)
    if overrides.ElementGradient then
        local fill = themes.firstColor(resolved.ElementGradient)
        if overrides.ElementStroke == nil then
            resolved.ElementStroke = edgeShade(fill, 0.28)
        end
        if overrides.ElementStrokeGradient == nil then
            resolved.ElementStrokeGradient = ColorSequence.new(edgeShade(fill, 0.4))
        end
        if overrides.ElementStrokeHover == nil then
            resolved.ElementStrokeHover = edgeShade(fill, 0.52)
        end
    end
    if overrides.TabBackground and overrides.TabStroke == nil then
        resolved.TabStroke = ColorSequence.new(edgeShade(themes.firstColor(resolved.TabBackground), 0.4))
    end
end

local function themeOverrides(value)
    if typeof(value) == "table" then
        return value
    elseif typeof(value) == "string" then
        local named = themesFolder:FindFirstChild(string.lower(value))
        if named then
            return require(named)
        end
        log.warn("Astra: unknown theme '" .. value .. "', using default")
    elseif value ~= nil then
        log.warn("Astra: invalid theme (expected a built-in name or a theme table), using default")
    end
    return require(themesFolder["default"])
end

-- [[Resolve a theme value (nil, built-in name, or theme table) to a full theme]]
-- [[table. Public API: window code calls themes.resolve(value).]]
function themes.resolve(value)
    local resolved = table.clone(require(themesFolder["default"]))
    local overrides = themeOverrides(value)
	for key, override in overrides do
		resolved[key] = themes.coerceValue(key, override)
	end
	if typeof(value) == "table" then
		themes.deriveStrokes(resolved, overrides)
	end

	local userTable = if typeof(value) == "table" then value else nil
    if not (userTable and (userTable.Font or userTable.font)) then
        resolved.Font = variables.brandFont(Enum.FontWeight.Medium)
    end
    if not (userTable and (userTable.TitleFont or userTable.titleFont)) then
        resolved.TitleFont = variables.brandFont(Enum.FontWeight.SemiBold)
    end

    return resolved
end

return themes

end)() end,
    [71] = function()local wax,script,require=ImportGlobals(71)local ImportGlobals return (function(...)
return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(16, 13, 24),
    WindowSurface = Color3.fromRGB(26, 21, 36),
    TopbarSurface = Color3.fromRGB(31, 25, 43),
    SidebarSurface = Color3.fromRGB(22, 18, 31),
    TabSurface = Color3.fromRGB(39, 31, 53),
    TabSelected = Color3.fromRGB(75, 54, 91),
    ElementSurface = Color3.fromRGB(34, 27, 46),
    ElementHover = Color3.fromRGB(44, 35, 58),
    ElementPressed = Color3.fromRGB(28, 22, 38),
    SecondaryElement = Color3.fromRGB(29, 23, 40),
    Stroke = Color3.fromRGB(70, 56, 87),
    SecondaryStroke = Color3.fromRGB(54, 43, 69),
    PrimaryText = Color3.fromRGB(237, 231, 242),
    SecondaryText = Color3.fromRGB(190, 181, 198),
    MutedText = Color3.fromRGB(139, 128, 151),
    DisabledText = Color3.fromRGB(100, 91, 111),
    Accent = Color3.fromRGB(177, 126, 230),
    AccentHover = Color3.fromRGB(197, 151, 244),
    AccentPressed = Color3.fromRGB(137, 89, 189),
    AccentStrokeSemantic = Color3.fromRGB(215, 177, 250),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(23, 18, 33), Color3.fromRGB(34, 27, 47)),
    ShadowColor = Color3.fromRGB(8, 5, 14),
    ElementStroke = Color3.fromRGB(63, 50, 79),
    CardSurface = Color3.fromRGB(34, 27, 46),
    ElementGradient = ColorSequence.new(Color3.fromRGB(34, 27, 46), Color3.fromRGB(39, 31, 52)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(58, 46, 74), Color3.fromRGB(76, 60, 95)),
    ElementStrokeHover = Color3.fromRGB(93, 70, 112),
    TabBackground = ColorSequence.new(Color3.fromRGB(47, 37, 62), Color3.fromRGB(35, 28, 47)),
    TabStroke = ColorSequence.new(Color3.fromRGB(82, 64, 101), Color3.fromRGB(58, 45, 74)),
    TabColor = Color3.fromRGB(224, 215, 231),
    SliderBackground = Color3.fromRGB(44, 35, 58),
    SliderBackgroundHover = Color3.fromRGB(57, 45, 74),
    SliderProgress = ColorSequence.new(Color3.fromRGB(184, 132, 235), Color3.fromRGB(143, 91, 197)),
    SliderStroke = Color3.fromRGB(118, 98, 135),
    AccentColor = Color3.fromRGB(177, 126, 230),
    AccentStroke = Color3.fromRGB(215, 177, 250),
    AccentGlow = 0.3,
    DropdownHighlight = Color3.fromRGB(197, 151, 244),
    ToggleKnobOff = Color3.fromRGB(216, 206, 225),
    -- [[Solid off-knob: do not inherit Gen2 default's translucent white knob.]]
    ToggleKnobOffTransparency = 0.08,
    ToggleTrack = Color3.fromRGB(10, 8, 14),
    ToggleTrackTransparency = 0.82,
    StatBackground = Color3.fromRGB(27, 21, 36),
    NeutralButton = Color3.fromRGB(43, 34, 57),
    NeutralButtonHover = Color3.fromRGB(57, 45, 74),
    NeutralButtonStroke = Color3.fromRGB(105, 84, 124),
    FieldBackground = Color3.fromRGB(20, 16, 28),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(177, 126, 230),
    PlaceholderColor = Color3.fromRGB(139, 128, 151),
    SurfaceStroke = Color3.fromRGB(58, 46, 72),
    ContentColor = Color3.fromRGB(221, 214, 226),
    TitlingColor = Color3.fromRGB(242, 236, 246),
    ElementTextHoverColor = Color3.fromRGB(248, 242, 250),
    ActionColor = Color3.fromRGB(235, 228, 239),
    ErrorColor = Color3.fromRGB(205, 80, 100),
    ErrorStrokeColor = Color3.fromRGB(237, 125, 145),
}

end)() end,
    [72] = function()local wax,script,require=ImportGlobals(72)local ImportGlobals return (function(...)
return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(11, 16, 25),
    WindowSurface = Color3.fromRGB(18, 25, 37),
    TopbarSurface = Color3.fromRGB(22, 30, 44),
    SidebarSurface = Color3.fromRGB(15, 21, 32),
    TabSurface = Color3.fromRGB(29, 40, 58),
    TabSelected = Color3.fromRGB(37, 66, 91),
    ElementSurface = Color3.fromRGB(25, 34, 49),
    ElementHover = Color3.fromRGB(33, 45, 63),
    ElementPressed = Color3.fromRGB(19, 27, 40),
    SecondaryElement = Color3.fromRGB(21, 29, 43),
    Stroke = Color3.fromRGB(52, 68, 90),
    SecondaryStroke = Color3.fromRGB(39, 53, 72),
    PrimaryText = Color3.fromRGB(230, 237, 245),
    SecondaryText = Color3.fromRGB(176, 190, 207),
    MutedText = Color3.fromRGB(121, 139, 160),
    DisabledText = Color3.fromRGB(84, 100, 119),
    Accent = Color3.fromRGB(78, 157, 235),
    AccentHover = Color3.fromRGB(105, 181, 248),
    AccentPressed = Color3.fromRGB(48, 119, 195),
    AccentStrokeSemantic = Color3.fromRGB(137, 203, 255),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(15, 21, 32), Color3.fromRGB(24, 33, 48)),
    ShadowColor = Color3.fromRGB(4, 8, 15),
    ElementStroke = Color3.fromRGB(44, 59, 79),
    CardSurface = Color3.fromRGB(25, 34, 49),
    ElementGradient = ColorSequence.new(Color3.fromRGB(25, 34, 49), Color3.fromRGB(30, 41, 57)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(43, 57, 77), Color3.fromRGB(60, 77, 101)),
    ElementStrokeHover = Color3.fromRGB(70, 91, 117),
    TabBackground = ColorSequence.new(Color3.fromRGB(37, 51, 72), Color3.fromRGB(26, 36, 52)),
    TabStroke = ColorSequence.new(Color3.fromRGB(64, 84, 111), Color3.fromRGB(45, 60, 82)),
    TabColor = Color3.fromRGB(211, 224, 238),
    SliderBackground = Color3.fromRGB(35, 48, 68),
    SliderBackgroundHover = Color3.fromRGB(47, 64, 88),
    SliderProgress = ColorSequence.new(Color3.fromRGB(82, 164, 239), Color3.fromRGB(49, 124, 202)),
    SliderStroke = Color3.fromRGB(91, 112, 139),
    AccentColor = Color3.fromRGB(78, 157, 235),
    AccentStroke = Color3.fromRGB(137, 203, 255),
    AccentGlow = 0.3,
    DropdownHighlight = Color3.fromRGB(105, 181, 248),
    ToggleKnobOff = Color3.fromRGB(196, 210, 226),
    -- [[Solid off-knob: do not inherit Gen2 default's translucent white knob.]]
    ToggleKnobOffTransparency = 0.08,
    ToggleTrack = Color3.fromRGB(8, 12, 18),
    ToggleTrackTransparency = 0.82,
    StatBackground = Color3.fromRGB(19, 26, 39),
    NeutralButton = Color3.fromRGB(34, 46, 65),
    NeutralButtonHover = Color3.fromRGB(47, 63, 86),
    NeutralButtonStroke = Color3.fromRGB(91, 113, 143),
    FieldBackground = Color3.fromRGB(14, 20, 30),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(78, 157, 235),
    PlaceholderColor = Color3.fromRGB(121, 139, 160),
    SurfaceStroke = Color3.fromRGB(44, 59, 79),
    ContentColor = Color3.fromRGB(214, 225, 237),
    TitlingColor = Color3.fromRGB(237, 244, 250),
    ElementTextHoverColor = Color3.fromRGB(245, 250, 255),
    ActionColor = Color3.fromRGB(226, 237, 246),
    ErrorColor = Color3.fromRGB(201, 76, 82),
    ErrorStrokeColor = Color3.fromRGB(232, 116, 120),
}

end)() end,
    [73] = function()local wax,script,require=ImportGlobals(73)local ImportGlobals return (function(...)
local crimson = require(script.Parent.Parent.core.state)

return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(19, 12, 13),
    WindowSurface = Color3.fromRGB(25, 17, 18),
    TopbarSurface = Color3.fromRGB(30, 21, 22),
    SidebarSurface = Color3.fromRGB(22, 15, 16),
    TabSurface = Color3.fromRGB(40, 27, 29),
    TabSelected = Color3.fromRGB(84, 38, 42),
    ElementSurface = Color3.fromRGB(31, 22, 23),
    ElementHover = Color3.fromRGB(39, 28, 29),
    ElementPressed = Color3.fromRGB(26, 19, 20),
    SecondaryElement = Color3.fromRGB(28, 20, 21),
    Stroke = Color3.fromRGB(64, 47, 50),
    SecondaryStroke = Color3.fromRGB(49, 37, 39),
    PrimaryText = Color3.fromRGB(240, 233, 233),
    SecondaryText = Color3.fromRGB(198, 183, 184),
    MutedText = Color3.fromRGB(150, 132, 134),
    DisabledText = Color3.fromRGB(108, 94, 96),
    Accent = Color3.fromRGB(224, 78, 84),
    AccentHover = Color3.fromRGB(236, 100, 105),
    AccentPressed = Color3.fromRGB(181, 50, 56),
    AccentStrokeSemantic = Color3.fromRGB(238, 112, 117),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(23, 15, 16), Color3.fromRGB(33, 22, 24)),
    ShadowColor = Color3.fromRGB(7, 4, 4),
    LiveAnimation = false,
    ElementTransparency = 0,
    ElementStroke = Color3.fromRGB(59, 44, 47),
    CardSurface = Color3.fromRGB(31, 22, 23),
    ElementGradient = ColorSequence.new(Color3.fromRGB(31, 22, 23), Color3.fromRGB(35, 26, 27)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(52, 39, 42), Color3.fromRGB(68, 51, 55)),
    ElementStrokeTransparency = 0,
    ElementStrokeHoverTransparency = 0,
    ElementStrokeHover = Color3.fromRGB(224, 78, 84),
    DarkToggleOverlay = true,
    TabColor = Color3.fromRGB(226, 214, 215),
    TabBackground = ColorSequence.new(Color3.fromRGB(38, 25, 27), Color3.fromRGB(29, 19, 21)),
    TabStroke = ColorSequence.new(Color3.fromRGB(96, 44, 49), Color3.fromRGB(68, 30, 35)),
    SliderBackground = Color3.fromRGB(49, 34, 36),
    SliderBackgroundHover = Color3.fromRGB(62, 44, 46),
    SliderProgress = ColorSequence.new(Color3.fromRGB(228, 86, 92), Color3.fromRGB(186, 52, 58)),
    SliderStroke = Color3.fromRGB(114, 88, 91),
    AccentColor = Color3.fromRGB(224, 78, 84),
    AccentStroke = Color3.fromRGB(238, 112, 117),
    AccentGlow = 0.3,
    StatBackground = Color3.fromRGB(26, 19, 20),
    SliderHandle = Color3.fromRGB(238, 222, 223),
    ToggleTrack = Color3.fromRGB(10, 6, 7),
    ToggleTrackTransparency = 0.82,
    ToggleKnobOff = Color3.fromRGB(196, 184, 185),
    ToggleKnobOffTransparency = 0.08,
    FieldBackground = Color3.fromRGB(19, 13, 14),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(201, 62, 68),
    PlaceholderColor = Color3.fromRGB(150, 132, 134),
    SurfaceStroke = Color3.fromRGB(55, 42, 45),
    NeutralButton = Color3.fromRGB(35, 25, 27),
    NeutralButtonHover = Color3.fromRGB(46, 33, 35),
    NeutralButtonStroke = Color3.fromRGB(84, 62, 66),
    ActionColor = Color3.fromRGB(234, 227, 228),
    TitlingColor = Color3.fromRGB(243, 238, 238),
    TitleFont = crimson.brandFont(Enum.FontWeight.SemiBold),
    Font = crimson.brandFont(Enum.FontWeight.Medium),
    ContentColor = Color3.fromRGB(222, 212, 213),
    ElementTextHoverColor = Color3.fromRGB(247, 242, 243),
    DropdownHighlight = Color3.fromRGB(236, 100, 105),
    ErrorColor = Color3.fromRGB(211, 86, 88),
    ErrorStrokeColor = Color3.fromRGB(239, 118, 119),
}

end)() end,
    [74] = function()local wax,script,require=ImportGlobals(74)local ImportGlobals return (function(...)
local default = require(script.Parent.Parent.core.state)

-- [[Rayfield Gen2 default palette, ported 1:1 for shared keys. Astra-only
-- [[semantic keys (Background, WindowSurface, text tiers, Success/Warning,
-- [[etc.) are derived from the same greyscale + teal accent so the capsule,
-- [[profile card and settings surfaces stay coherent with Gen2.]]
return {
    -- [[Semantic palette (derived from Gen2 greys + teal accent)]]
    Background = Color3.fromRGB(10, 10, 10),
    WindowSurface = Color3.fromRGB(25, 25, 25),
    TopbarSurface = Color3.fromRGB(30, 30, 30),
    SidebarSurface = Color3.fromRGB(18, 18, 18),
    TabSurface = Color3.fromRGB(35, 35, 35),
    TabSelected = Color3.fromRGB(23, 153, 110),
    ElementSurface = Color3.fromRGB(30, 30, 30),
    ElementHover = Color3.fromRGB(40, 40, 40),
    ElementPressed = Color3.fromRGB(22, 22, 22),
    SecondaryElement = Color3.fromRGB(25, 25, 25),
    Stroke = Color3.fromRGB(64, 64, 64),
    SecondaryStroke = Color3.fromRGB(48, 48, 48),
    PrimaryText = Color3.fromRGB(255, 255, 255),
    SecondaryText = Color3.fromRGB(200, 200, 200),
    MutedText = Color3.fromRGB(178, 178, 178),
    DisabledText = Color3.fromRGB(120, 120, 120),
    Accent = Color3.fromRGB(23, 153, 110),
    AccentHover = Color3.fromRGB(32, 201, 144),
    AccentPressed = Color3.fromRGB(0, 134, 98),
    AccentStrokeSemantic = Color3.fromRGB(32, 201, 144),
    Success = Color3.fromRGB(0, 170, 127),
    Warning = Color3.fromRGB(224, 166, 74),
    Error = Color3.fromRGB(185, 50, 50),
    NotificationSurface = Color3.fromRGB(30, 30, 30),
    PopupSurface = Color3.fromRGB(25, 25, 25),
    InputSurface = Color3.fromRGB(18, 18, 18),

    -- [[Gen2 shared keys — values match src/themes/default.luau]]
    CornerRoundness = UDim.new(0, 20),
    WindowColor = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(10, 10, 10)),
        ColorSequenceKeypoint.new(0.9999, Color3.fromRGB(25, 25, 25)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(35, 35, 35)),
    }),
    ShadowColor = Color3.fromRGB(20, 20, 20),
    ElementStroke = Color3.fromRGB(58, 58, 58),
    CardSurface = Color3.fromRGB(30, 30, 30),
    ElementGradient = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 30, 30)),
        ColorSequenceKeypoint.new(0.9999, Color3.fromRGB(35, 35, 35)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(35, 35, 35)),
    }),
    ElementStrokeGradient = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(56, 56, 56)),
        ColorSequenceKeypoint.new(0.9999, Color3.fromRGB(72, 72, 72)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(56, 56, 56)),
    }),
    TabColor = Color3.fromRGB(255, 255, 255),
    TabBackground = ColorSequence.new(Color3.fromRGB(50, 50, 50), Color3.fromRGB(35, 35, 35)),
    TabStroke = ColorSequence.new(Color3.fromRGB(95, 95, 95), Color3.fromRGB(50, 50, 50)),
    SliderBackground = Color3.fromRGB(47, 47, 47),
    SliderBackgroundHover = Color3.fromRGB(60, 60, 60),
    SliderProgress = ColorSequence.new(Color3.fromRGB(0, 170, 127), Color3.fromRGB(0, 134, 98)),
    SliderStroke = Color3.fromRGB(255, 255, 255),
    ActionColor = Color3.fromRGB(255, 255, 255),
    TitleFont = default.brandFont(Enum.FontWeight.SemiBold),
    Font = default.brandFont(Enum.FontWeight.Medium),
    ContentColor = Color3.fromRGB(255, 255, 255),
    LiveAnimation = false,
    DarkToggleOverlay = true,
    ElementTransparency = 0,
    -- [[Grouped children render as defined cards — a surface a shade under the
    -- [[header band and a visible stroke — the crisp rows the design
    -- [[reference shows, not 75%-faded ghosts without strokes.
    CollapsibleChildElementTransparency = 0.35,
    ElementStrokeTransparency = 0,
    CollapsibleChildElementStrokeTransparency = 0,
    ElementStrokeHoverTransparency = 0,
    CollapsibleChildElementStrokeHoverTransparency = 0,
    ElementStrokeHover = Color3.fromRGB(82, 82, 82),
    ElementCornerRadius = UDim.new(0, 12),
    ElementTextHoverColor = Color3.fromRGB(255, 255, 255),
    TitlingColor = Color3.fromRGB(255, 255, 255),
    DropdownHighlight = Color3.fromRGB(255, 255, 255),
    AccentColor = Color3.fromRGB(23, 153, 110),
    AccentStroke = Color3.fromRGB(32, 201, 144),
    AccentGlow = 0.4,
    StatBackground = Color3.fromRGB(25, 25, 25),
    SliderHandle = Color3.fromRGB(255, 255, 255),
    PillCornerRadius = UDim.new(1, 0),
    ToggleTrack = Color3.fromRGB(0, 0, 0),
    ToggleTrackTransparency = 0.9,
    ToggleKnobOff = Color3.fromRGB(255, 255, 255),
    ToggleKnobOffTransparency = 0.8,
    FieldBackground = Color3.fromRGB(255, 255, 255),
    FieldTransparency = 0.9,
    FieldGlow = Color3.fromRGB(255, 255, 255),
    PlaceholderColor = Color3.fromRGB(178, 178, 178),
    SurfaceStroke = Color3.fromRGB(72, 72, 72),
    NeutralButton = Color3.fromRGB(38, 38, 38),
    NeutralButtonHover = Color3.fromRGB(54, 54, 54),
    NeutralButtonStroke = Color3.fromRGB(255, 255, 255),
    ErrorColor = Color3.fromRGB(185, 50, 50),
    ErrorStrokeColor = Color3.fromRGB(240, 75, 75),
}

end)() end,
    [75] = function()local wax,script,require=ImportGlobals(75)local ImportGlobals return (function(...)
return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(22, 18, 16),
    WindowSurface = Color3.fromRGB(31, 26, 23),
    TopbarSurface = Color3.fromRGB(37, 31, 27),
    SidebarSurface = Color3.fromRGB(26, 22, 20),
    TabSurface = Color3.fromRGB(47, 39, 33),
    TabSelected = Color3.fromRGB(83, 59, 38),
    ElementSurface = Color3.fromRGB(38, 31, 27),
    ElementHover = Color3.fromRGB(49, 40, 34),
    ElementPressed = Color3.fromRGB(29, 24, 21),
    SecondaryElement = Color3.fromRGB(32, 27, 24),
    Stroke = Color3.fromRGB(74, 61, 51),
    SecondaryStroke = Color3.fromRGB(57, 47, 40),
    PrimaryText = Color3.fromRGB(243, 236, 229),
    SecondaryText = Color3.fromRGB(198, 186, 173),
    MutedText = Color3.fromRGB(145, 131, 117),
    DisabledText = Color3.fromRGB(104, 93, 82),
    Accent = Color3.fromRGB(226, 145, 58),
    AccentHover = Color3.fromRGB(243, 169, 78),
    AccentPressed = Color3.fromRGB(181, 105, 31),
    AccentStrokeSemantic = Color3.fromRGB(255, 193, 112),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(27, 22, 19), Color3.fromRGB(40, 33, 28)),
    ShadowColor = Color3.fromRGB(11, 7, 5),
    ElementStroke = Color3.fromRGB(65, 53, 44),
    CardSurface = Color3.fromRGB(38, 31, 27),
    ElementGradient = ColorSequence.new(Color3.fromRGB(38, 31, 27), Color3.fromRGB(44, 36, 31)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(61, 50, 42), Color3.fromRGB(80, 66, 54)),
    ElementStrokeHover = Color3.fromRGB(94, 76, 61),
    TabBackground = ColorSequence.new(Color3.fromRGB(52, 43, 36), Color3.fromRGB(39, 32, 27)),
    TabStroke = ColorSequence.new(Color3.fromRGB(86, 70, 57), Color3.fromRGB(60, 49, 41)),
    TabColor = Color3.fromRGB(233, 222, 209),
    SliderBackground = Color3.fromRGB(51, 42, 35),
    SliderBackgroundHover = Color3.fromRGB(65, 53, 44),
    SliderProgress = ColorSequence.new(Color3.fromRGB(232, 151, 60), Color3.fromRGB(190, 108, 29)),
    SliderStroke = Color3.fromRGB(122, 102, 83),
    AccentColor = Color3.fromRGB(226, 145, 58),
    AccentStroke = Color3.fromRGB(255, 193, 112),
    AccentGlow = 0.28,
    DropdownHighlight = Color3.fromRGB(243, 169, 78),
    ToggleKnobOff = Color3.fromRGB(226, 215, 202),
    -- [[Solid off-knob: do not inherit Gen2 default's translucent white knob.]]
    ToggleKnobOffTransparency = 0.08,
    ToggleTrack = Color3.fromRGB(12, 9, 7),
    ToggleTrackTransparency = 0.82,
    StatBackground = Color3.fromRGB(30, 24, 21),
    NeutralButton = Color3.fromRGB(47, 38, 32),
    NeutralButtonHover = Color3.fromRGB(63, 51, 42),
    NeutralButtonStroke = Color3.fromRGB(123, 101, 80),
    FieldBackground = Color3.fromRGB(22, 18, 16),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(226, 145, 58),
    PlaceholderColor = Color3.fromRGB(145, 131, 117),
    SurfaceStroke = Color3.fromRGB(65, 53, 44),
    ContentColor = Color3.fromRGB(225, 215, 204),
    TitlingColor = Color3.fromRGB(245, 238, 230),
    ElementTextHoverColor = Color3.fromRGB(252, 245, 236),
    ActionColor = Color3.fromRGB(237, 227, 216),
    ErrorColor = Color3.fromRGB(202, 77, 69),
    ErrorStrokeColor = Color3.fromRGB(235, 117, 103),
}

end)() end,
    [76] = function()local wax,script,require=ImportGlobals(76)local ImportGlobals return (function(...)
local emerald = require(script.Parent.Parent.core.state)

return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(12, 17, 13),
    WindowSurface = Color3.fromRGB(17, 23, 19),
    TopbarSurface = Color3.fromRGB(21, 28, 23),
    SidebarSurface = Color3.fromRGB(15, 20, 16),
    TabSurface = Color3.fromRGB(28, 38, 31),
    TabSelected = Color3.fromRGB(34, 71, 48),
    ElementSurface = Color3.fromRGB(23, 31, 26),
    ElementHover = Color3.fromRGB(30, 40, 33),
    ElementPressed = Color3.fromRGB(19, 25, 21),
    SecondaryElement = Color3.fromRGB(20, 27, 22),
    Stroke = Color3.fromRGB(51, 66, 56),
    SecondaryStroke = Color3.fromRGB(39, 52, 43),
    PrimaryText = Color3.fromRGB(230, 240, 233),
    SecondaryText = Color3.fromRGB(172, 190, 178),
    MutedText = Color3.fromRGB(118, 136, 124),
    DisabledText = Color3.fromRGB(85, 99, 89),
    Accent = Color3.fromRGB(72, 199, 116),
    AccentHover = Color3.fromRGB(96, 218, 138),
    AccentPressed = Color3.fromRGB(45, 155, 88),
    AccentStrokeSemantic = Color3.fromRGB(112, 220, 146),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(15, 20, 17), Color3.fromRGB(22, 30, 25)),
    ShadowColor = Color3.fromRGB(4, 7, 5),
    LiveAnimation = false,
    ElementTransparency = 0,
    ElementStroke = Color3.fromRGB(48, 62, 53),
    CardSurface = Color3.fromRGB(23, 31, 26),
    ElementGradient = ColorSequence.new(Color3.fromRGB(23, 31, 26), Color3.fromRGB(27, 36, 30)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(42, 56, 47), Color3.fromRGB(56, 74, 62)),
    ElementStrokeTransparency = 0,
    ElementStrokeHoverTransparency = 0,
    ElementStrokeHover = Color3.fromRGB(72, 199, 116),
    DarkToggleOverlay = true,
    TabColor = Color3.fromRGB(206, 222, 212),
    TabBackground = ColorSequence.new(Color3.fromRGB(26, 36, 29), Color3.fromRGB(20, 27, 22)),
    TabStroke = ColorSequence.new(Color3.fromRGB(50, 102, 72), Color3.fromRGB(34, 74, 52)),
    SliderBackground = Color3.fromRGB(33, 45, 38),
    SliderBackgroundHover = Color3.fromRGB(44, 58, 49),
    SliderProgress = ColorSequence.new(Color3.fromRGB(82, 210, 128), Color3.fromRGB(49, 163, 96)),
    SliderStroke = Color3.fromRGB(90, 110, 97),
    AccentColor = Color3.fromRGB(72, 199, 116),
    AccentStroke = Color3.fromRGB(112, 220, 146),
    AccentGlow = 0.3,
    StatBackground = Color3.fromRGB(19, 25, 21),
    SliderHandle = Color3.fromRGB(216, 236, 222),
    ToggleTrack = Color3.fromRGB(7, 11, 8),
    ToggleTrackTransparency = 0.82,
    ToggleKnobOff = Color3.fromRGB(183, 196, 186),
    ToggleKnobOffTransparency = 0.08,
    FieldBackground = Color3.fromRGB(13, 18, 15),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(64, 185, 105),
    PlaceholderColor = Color3.fromRGB(118, 136, 124),
    SurfaceStroke = Color3.fromRGB(45, 58, 49),
    NeutralButton = Color3.fromRGB(27, 36, 30),
    NeutralButtonHover = Color3.fromRGB(37, 49, 41),
    NeutralButtonStroke = Color3.fromRGB(62, 80, 67),
    ActionColor = Color3.fromRGB(222, 233, 226),
    TitlingColor = Color3.fromRGB(234, 242, 237),
    TitleFont = emerald.brandFont(Enum.FontWeight.SemiBold),
    Font = emerald.brandFont(Enum.FontWeight.Medium),
    ContentColor = Color3.fromRGB(209, 222, 213),
    ElementTextHoverColor = Color3.fromRGB(238, 248, 241),
    DropdownHighlight = Color3.fromRGB(96, 218, 138),
    ErrorColor = Color3.fromRGB(211, 86, 88),
    ErrorStrokeColor = Color3.fromRGB(239, 118, 119),
}

end)() end,
    [77] = function()local wax,script,require=ImportGlobals(77)local ImportGlobals return (function(...)
local frost = require(script.Parent.Parent.core.state)

return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(10, 14, 19),
    WindowSurface = Color3.fromRGB(16, 21, 28),
    TopbarSurface = Color3.fromRGB(20, 27, 35),
    SidebarSurface = Color3.fromRGB(14, 19, 25),
    TabSurface = Color3.fromRGB(27, 35, 45),
    TabSelected = Color3.fromRGB(26, 56, 68),
    ElementSurface = Color3.fromRGB(22, 29, 37),
    ElementHover = Color3.fromRGB(29, 38, 48),
    ElementPressed = Color3.fromRGB(18, 24, 31),
    SecondaryElement = Color3.fromRGB(19, 26, 34),
    Stroke = Color3.fromRGB(52, 66, 79),
    SecondaryStroke = Color3.fromRGB(40, 52, 63),
    PrimaryText = Color3.fromRGB(232, 240, 245),
    SecondaryText = Color3.fromRGB(172, 188, 199),
    MutedText = Color3.fromRGB(118, 134, 146),
    DisabledText = Color3.fromRGB(86, 98, 108),
    Accent = Color3.fromRGB(86, 197, 235),
    AccentHover = Color3.fromRGB(116, 214, 245),
    AccentPressed = Color3.fromRGB(52, 158, 196),
    AccentStrokeSemantic = Color3.fromRGB(133, 216, 242),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(14, 19, 26), Color3.fromRGB(22, 30, 39)),
    ShadowColor = Color3.fromRGB(4, 7, 10),
    LiveAnimation = false,
    ElementTransparency = 0,
    ElementStroke = Color3.fromRGB(49, 63, 76),
    CardSurface = Color3.fromRGB(22, 29, 37),
    ElementGradient = ColorSequence.new(Color3.fromRGB(22, 29, 37), Color3.fromRGB(26, 34, 43)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(44, 57, 69), Color3.fromRGB(58, 74, 87)),
    ElementStrokeTransparency = 0,
    ElementStrokeHoverTransparency = 0,
    ElementStrokeHover = Color3.fromRGB(86, 197, 235),
    DarkToggleOverlay = true,
    TabColor = Color3.fromRGB(208, 224, 232),
    TabBackground = ColorSequence.new(Color3.fromRGB(25, 34, 44), Color3.fromRGB(19, 26, 34)),
    TabStroke = ColorSequence.new(Color3.fromRGB(56, 118, 138), Color3.fromRGB(38, 84, 100)),
    SliderBackground = Color3.fromRGB(34, 44, 55),
    SliderBackgroundHover = Color3.fromRGB(45, 58, 71),
    SliderProgress = ColorSequence.new(Color3.fromRGB(96, 204, 238), Color3.fromRGB(56, 160, 200)),
    SliderStroke = Color3.fromRGB(94, 112, 126),
    AccentColor = Color3.fromRGB(86, 197, 235),
    AccentStroke = Color3.fromRGB(133, 216, 242),
    AccentGlow = 0.35,
    StatBackground = Color3.fromRGB(18, 24, 31),
    SliderHandle = Color3.fromRGB(224, 240, 247),
    ToggleTrack = Color3.fromRGB(8, 12, 16),
    ToggleTrackTransparency = 0.82,
    ToggleKnobOff = Color3.fromRGB(184, 196, 204),
    ToggleKnobOffTransparency = 0.08,
    FieldBackground = Color3.fromRGB(13, 18, 24),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(74, 180, 216),
    PlaceholderColor = Color3.fromRGB(118, 134, 146),
    SurfaceStroke = Color3.fromRGB(46, 59, 71),
    NeutralButton = Color3.fromRGB(28, 36, 45),
    NeutralButtonHover = Color3.fromRGB(38, 49, 60),
    NeutralButtonStroke = Color3.fromRGB(66, 82, 96),
    ActionColor = Color3.fromRGB(224, 233, 239),
    TitlingColor = Color3.fromRGB(236, 243, 247),
    TitleFont = frost.brandFont(Enum.FontWeight.SemiBold),
    Font = frost.brandFont(Enum.FontWeight.Medium),
    ContentColor = Color3.fromRGB(212, 223, 231),
    ElementTextHoverColor = Color3.fromRGB(240, 248, 251),
    DropdownHighlight = Color3.fromRGB(116, 214, 245),
    ErrorColor = Color3.fromRGB(211, 86, 88),
    ErrorStrokeColor = Color3.fromRGB(239, 118, 119),
}

end)() end,
    [78] = function()local wax,script,require=ImportGlobals(78)local ImportGlobals return (function(...)
local gold = require(script.Parent.Parent.core.state)

return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(19, 16, 10),
    WindowSurface = Color3.fromRGB(25, 21, 14),
    TopbarSurface = Color3.fromRGB(30, 25, 17),
    SidebarSurface = Color3.fromRGB(22, 19, 12),
    TabSurface = Color3.fromRGB(38, 32, 22),
    TabSelected = Color3.fromRGB(74, 61, 30),
    ElementSurface = Color3.fromRGB(31, 26, 18),
    ElementHover = Color3.fromRGB(39, 33, 23),
    ElementPressed = Color3.fromRGB(26, 22, 15),
    SecondaryElement = Color3.fromRGB(28, 24, 16),
    Stroke = Color3.fromRGB(66, 57, 38),
    SecondaryStroke = Color3.fromRGB(50, 44, 30),
    PrimaryText = Color3.fromRGB(240, 236, 226),
    SecondaryText = Color3.fromRGB(196, 188, 170),
    MutedText = Color3.fromRGB(148, 139, 118),
    DisabledText = Color3.fromRGB(106, 99, 84),
    Accent = Color3.fromRGB(233, 196, 74),
    AccentHover = Color3.fromRGB(244, 210, 96),
    AccentPressed = Color3.fromRGB(193, 158, 45),
    AccentStrokeSemantic = Color3.fromRGB(240, 214, 110),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(23, 19, 13), Color3.fromRGB(32, 27, 18)),
    ShadowColor = Color3.fromRGB(8, 7, 4),
    LiveAnimation = false,
    ElementTransparency = 0,
    ElementStroke = Color3.fromRGB(61, 53, 35),
    CardSurface = Color3.fromRGB(31, 26, 18),
    ElementGradient = ColorSequence.new(Color3.fromRGB(31, 26, 18), Color3.fromRGB(35, 30, 21)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(53, 46, 31), Color3.fromRGB(70, 60, 40)),
    ElementStrokeTransparency = 0,
    ElementStrokeHoverTransparency = 0,
    ElementStrokeHover = Color3.fromRGB(233, 196, 74),
    DarkToggleOverlay = true,
    TabColor = Color3.fromRGB(226, 218, 198),
    TabBackground = ColorSequence.new(Color3.fromRGB(36, 30, 20), Color3.fromRGB(28, 23, 15)),
    TabStroke = ColorSequence.new(Color3.fromRGB(94, 79, 42), Color3.fromRGB(66, 56, 30)),
    SliderBackground = Color3.fromRGB(48, 41, 28),
    SliderBackgroundHover = Color3.fromRGB(61, 52, 35),
    SliderProgress = ColorSequence.new(Color3.fromRGB(238, 202, 84), Color3.fromRGB(198, 158, 47)),
    SliderStroke = Color3.fromRGB(116, 103, 72),
    AccentColor = Color3.fromRGB(233, 196, 74),
    AccentStroke = Color3.fromRGB(240, 214, 110),
    AccentGlow = 0.32,
    StatBackground = Color3.fromRGB(26, 22, 15),
    SliderHandle = Color3.fromRGB(238, 230, 206),
    ToggleTrack = Color3.fromRGB(10, 9, 5),
    ToggleTrackTransparency = 0.82,
    ToggleKnobOff = Color3.fromRGB(194, 188, 170),
    ToggleKnobOffTransparency = 0.08,
    FieldBackground = Color3.fromRGB(18, 15, 10),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(201, 168, 60),
    PlaceholderColor = Color3.fromRGB(148, 139, 118),
    SurfaceStroke = Color3.fromRGB(58, 51, 34),
    NeutralButton = Color3.fromRGB(35, 30, 21),
    NeutralButtonHover = Color3.fromRGB(46, 39, 27),
    NeutralButtonStroke = Color3.fromRGB(82, 71, 47),
    ActionColor = Color3.fromRGB(234, 229, 214),
    TitlingColor = Color3.fromRGB(243, 239, 229),
    TitleFont = gold.brandFont(Enum.FontWeight.SemiBold),
    Font = gold.brandFont(Enum.FontWeight.Medium),
    ContentColor = Color3.fromRGB(222, 215, 197),
    ElementTextHoverColor = Color3.fromRGB(247, 244, 235),
    DropdownHighlight = Color3.fromRGB(244, 210, 96),
    ErrorColor = Color3.fromRGB(211, 86, 88),
    ErrorStrokeColor = Color3.fromRGB(239, 118, 119),
}

end)() end,
    [79] = function()local wax,script,require=ImportGlobals(79)local ImportGlobals return (function(...)
local onyx = require(script.Parent.Parent.core.state)

return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(8, 8, 9),
    WindowSurface = Color3.fromRGB(13, 13, 15),
    TopbarSurface = Color3.fromRGB(17, 17, 20),
    SidebarSurface = Color3.fromRGB(11, 11, 13),
    TabSurface = Color3.fromRGB(24, 24, 27),
    TabSelected = Color3.fromRGB(48, 48, 53),
    ElementSurface = Color3.fromRGB(19, 19, 22),
    ElementHover = Color3.fromRGB(25, 25, 29),
    ElementPressed = Color3.fromRGB(15, 15, 18),
    SecondaryElement = Color3.fromRGB(17, 17, 20),
    Stroke = Color3.fromRGB(48, 48, 54),
    SecondaryStroke = Color3.fromRGB(37, 37, 42),
    PrimaryText = Color3.fromRGB(238, 238, 240),
    SecondaryText = Color3.fromRGB(176, 178, 184),
    MutedText = Color3.fromRGB(122, 124, 131),
    DisabledText = Color3.fromRGB(87, 89, 96),
    Accent = Color3.fromRGB(205, 210, 220),
    AccentHover = Color3.fromRGB(222, 226, 233),
    AccentPressed = Color3.fromRGB(165, 170, 181),
    AccentStrokeSemantic = Color3.fromRGB(216, 220, 229),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(12, 12, 14), Color3.fromRGB(18, 18, 21)),
    ShadowColor = Color3.fromRGB(0, 0, 0),
    LiveAnimation = false,
    ElementTransparency = 0,
    ElementStroke = Color3.fromRGB(45, 45, 51),
    CardSurface = Color3.fromRGB(19, 19, 22),
    ElementGradient = ColorSequence.new(Color3.fromRGB(19, 19, 22), Color3.fromRGB(23, 23, 27)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(40, 40, 46), Color3.fromRGB(54, 54, 61)),
    ElementStrokeTransparency = 0,
    ElementStrokeHoverTransparency = 0,
    ElementStrokeHover = Color3.fromRGB(205, 210, 220),
    DarkToggleOverlay = true,
    TabColor = Color3.fromRGB(214, 216, 221),
    TabBackground = ColorSequence.new(Color3.fromRGB(22, 22, 25), Color3.fromRGB(16, 16, 19)),
    TabStroke = ColorSequence.new(Color3.fromRGB(64, 64, 72), Color3.fromRGB(46, 46, 53)),
    SliderBackground = Color3.fromRGB(32, 32, 37),
    SliderBackgroundHover = Color3.fromRGB(42, 42, 48),
    SliderProgress = ColorSequence.new(Color3.fromRGB(212, 216, 225), Color3.fromRGB(168, 173, 184)),
    SliderStroke = Color3.fromRGB(92, 94, 102),
    AccentColor = Color3.fromRGB(205, 210, 220),
    AccentStroke = Color3.fromRGB(216, 220, 229),
    AccentGlow = 0.22,
    StatBackground = Color3.fromRGB(15, 15, 18),
    SliderHandle = Color3.fromRGB(230, 232, 237),
    ToggleTrack = Color3.fromRGB(5, 5, 6),
    ToggleTrackTransparency = 0.82,
    ToggleKnobOff = Color3.fromRGB(178, 181, 189),
    ToggleKnobOffTransparency = 0.08,
    FieldBackground = Color3.fromRGB(10, 10, 12),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(188, 193, 203),
    PlaceholderColor = Color3.fromRGB(122, 124, 131),
    SurfaceStroke = Color3.fromRGB(42, 42, 48),
    NeutralButton = Color3.fromRGB(23, 23, 27),
    NeutralButtonHover = Color3.fromRGB(31, 31, 36),
    NeutralButtonStroke = Color3.fromRGB(66, 66, 74),
    ActionColor = Color3.fromRGB(228, 230, 234),
    TitlingColor = Color3.fromRGB(240, 240, 243),
    TitleFont = onyx.brandFont(Enum.FontWeight.SemiBold),
    Font = onyx.brandFont(Enum.FontWeight.Medium),
    ContentColor = Color3.fromRGB(212, 214, 219),
    ElementTextHoverColor = Color3.fromRGB(246, 246, 248),
    DropdownHighlight = Color3.fromRGB(222, 226, 233),
    ErrorColor = Color3.fromRGB(211, 86, 88),
    ErrorStrokeColor = Color3.fromRGB(239, 118, 119),
}

end)() end,
    [80] = function()local wax,script,require=ImportGlobals(80)local ImportGlobals return (function(...)
return {
    -- [[Semantic palette]]
    Background = Color3.fromRGB(23, 15, 19),
    WindowSurface = Color3.fromRGB(32, 22, 27),
    TopbarSurface = Color3.fromRGB(38, 26, 32),
    SidebarSurface = Color3.fromRGB(27, 19, 23),
    TabSurface = Color3.fromRGB(47, 32, 39),
    TabSelected = Color3.fromRGB(88, 51, 65),
    ElementSurface = Color3.fromRGB(39, 27, 32),
    ElementHover = Color3.fromRGB(51, 35, 42),
    ElementPressed = Color3.fromRGB(30, 21, 26),
    SecondaryElement = Color3.fromRGB(33, 23, 28),
    Stroke = Color3.fromRGB(78, 54, 63),
    SecondaryStroke = Color3.fromRGB(60, 42, 49),
    PrimaryText = Color3.fromRGB(244, 234, 238),
    SecondaryText = Color3.fromRGB(201, 183, 190),
    MutedText = Color3.fromRGB(149, 128, 137),
    DisabledText = Color3.fromRGB(108, 91, 99),
    Accent = Color3.fromRGB(218, 105, 145),
    AccentHover = Color3.fromRGB(237, 132, 168),
    AccentPressed = Color3.fromRGB(174, 70, 109),
    AccentStrokeSemantic = Color3.fromRGB(250, 169, 197),

    -- [[Legacy consumer keys]]
    WindowColor = ColorSequence.new(Color3.fromRGB(29, 19, 24), Color3.fromRGB(43, 29, 36)),
    ShadowColor = Color3.fromRGB(12, 6, 9),
    ElementStroke = Color3.fromRGB(69, 47, 56),
    CardSurface = Color3.fromRGB(39, 27, 32),
    ElementGradient = ColorSequence.new(Color3.fromRGB(39, 27, 32), Color3.fromRGB(46, 32, 38)),
    ElementStrokeGradient = ColorSequence.new(Color3.fromRGB(65, 45, 53), Color3.fromRGB(84, 58, 68)),
    ElementStrokeHover = Color3.fromRGB(99, 68, 81),
    TabBackground = ColorSequence.new(Color3.fromRGB(55, 38, 46), Color3.fromRGB(41, 28, 34)),
    TabStroke = ColorSequence.new(Color3.fromRGB(91, 63, 74), Color3.fromRGB(65, 45, 54)),
    TabColor = Color3.fromRGB(237, 221, 227),
    SliderBackground = Color3.fromRGB(53, 36, 44),
    SliderBackgroundHover = Color3.fromRGB(68, 46, 56),
    SliderProgress = ColorSequence.new(Color3.fromRGB(224, 112, 151), Color3.fromRGB(185, 70, 112)),
    SliderStroke = Color3.fromRGB(125, 92, 105),
    AccentColor = Color3.fromRGB(218, 105, 145),
    AccentStroke = Color3.fromRGB(250, 169, 197),
    AccentGlow = 0.3,
    DropdownHighlight = Color3.fromRGB(237, 132, 168),
    ToggleKnobOff = Color3.fromRGB(228, 209, 217),
    -- [[Solid off-knob: do not inherit Gen2 default's translucent white knob.]]
    ToggleKnobOffTransparency = 0.08,
    ToggleTrack = Color3.fromRGB(12, 8, 10),
    ToggleTrackTransparency = 0.82,
    StatBackground = Color3.fromRGB(31, 21, 26),
    NeutralButton = Color3.fromRGB(51, 35, 42),
    NeutralButtonHover = Color3.fromRGB(68, 46, 56),
    NeutralButtonStroke = Color3.fromRGB(131, 94, 108),
    FieldBackground = Color3.fromRGB(24, 16, 20),
    FieldTransparency = 0,
    FieldGlow = Color3.fromRGB(218, 105, 145),
    PlaceholderColor = Color3.fromRGB(149, 128, 137),
    SurfaceStroke = Color3.fromRGB(69, 47, 56),
    ContentColor = Color3.fromRGB(228, 213, 219),
    TitlingColor = Color3.fromRGB(247, 238, 242),
    ElementTextHoverColor = Color3.fromRGB(253, 245, 248),
    ActionColor = Color3.fromRGB(239, 226, 232),
    ErrorColor = Color3.fromRGB(205, 76, 91),
    ErrorStrokeColor = Color3.fromRGB(237, 121, 139),
}

end)() end,
    [82] = function()local wax,script,require=ImportGlobals(82)local ImportGlobals return (function(...)
local coreState = require(script.Parent.Parent.core.state)

local hapticService = {}

hapticService.enabled = false
type HapticTypes = {
    click: Enum.HapticEffectType?,
    notify: Enum.HapticEffectType?,
}

local effectTypes: HapticTypes = {}
local probeOk, probeResult = pcall(function()
    Instance.new("HapticEffect"):Destroy()     return {
        click = Enum.HapticEffectType.UIHover,         notify = Enum.HapticEffectType.UIClick,     }
end)
if probeOk then
    effectTypes = probeResult
end

local effectInstances: { [Enum.HapticEffectType]: Instance } = {}
local containerInstance: Instance? = nil

function hapticService.setContainer(newContainer: Instance?)
    containerInstance = newContainer
end

function hapticService.releaseContainer(releasedContainer: Instance?)
    if releasedContainer == nil or containerInstance == releasedContainer then
        containerInstance = nil
    end
end

local function resolveContainer(): Instance
    if containerInstance and containerInstance.Parent then
        return containerInstance
    end
    return coreState.guiContainer
end

local function getEffect(effectType: Enum.HapticEffectType): Instance?
    local cached = effectInstances[effectType]
        if cached and cached.Parent then
        return cached
    end
    local ok, instance = pcall(Instance.new, "HapticEffect")
    if not ok then
        return nil
    end
    local typed = instance :: any
    typed.Type = effectType
    local parentOk = pcall(function()
        instance.Parent = resolveContainer()
    end)
    if not parentOk then
        instance:Destroy()
        return nil
    end
    effectInstances[effectType] = instance
    return instance
end

local function play(requestedType: Enum.HapticEffectType?)
    if not requestedType or not hapticService.enabled then
        return
    end
    local effect = getEffect(requestedType)
    if effect then
        local playable = effect :: any
        pcall(playable.Play, effect)
    end
end

function hapticService.click()
    play(effectTypes.click)
end

function hapticService.notify()
    play(effectTypes.notify)
end

function hapticService.setEnabled(isEnabled: boolean?)
    hapticService.enabled = isEnabled and true or false
    if not hapticService.enabled then
        hapticService.teardown()
    end
end

function hapticService.teardown()
    for cachedType, cachedInstance in effectInstances do
        pcall(cachedInstance.Destroy, cachedInstance)
        effectInstances[cachedType] = nil
    end
end

return hapticService

end)() end,
    [83] = function()local wax,script,require=ImportGlobals(83)local ImportGlobals return (function(...)
local networkUtil = require(script.Parent.network)
local logger = require(script.Parent.log)

export type AssetId = number | string
export type AssetDownloadUrl = string
export type CacheKey = AssetId
export type ResolvedAsset = AssetId
export type AssetRequestPayload = {
    Url: string,
    Method: string,
}
local assetResolver = {
    Enum = {
        AssetDownloadUrl = {
            RobloxDownloadUrl = "https://assetdelivery.roblox.com/v1/asset/?id=%d" :: AssetDownloadUrl,
            RoProxyDownloadUrl = "https://assetdelivery.roproxy.com/v1/asset?id=%d" :: AssetDownloadUrl,
        },
    },
}
assetResolver.__index = assetResolver
assetResolver.__type = "assetResolver"
type AssetResolverState = {
    contentCache: { [CacheKey]: string }?,
    contentCacheOrder: { CacheKey }?,
    contentDownloadUrl: AssetDownloadUrl,
    pendingRequests: { [CacheKey]: { thread } },
}
export type AssetResolver = AssetResolverState & {
    resolve: (self: AssetResolver, value: unknown) -> ResolvedAsset?,
    getAssetContentFromUrl: (self: AssetResolver, url: string, cacheKey: CacheKey?, forced: boolean?) -> string?,
    getAssetContentFromId: (self: AssetResolver, id: AssetId, forced: boolean?) -> string?,
}

local maxCacheEntries = 8

function assetResolver.new(cacheEnabled: boolean?, downloadUrl: AssetDownloadUrl?): AssetResolver
    local cacheTable: { [CacheKey]: string }? = if cacheEnabled then {} else nil
    local self = setmetatable(
        {
            contentCache = cacheTable,
            contentCacheOrder = if cacheEnabled then {} :: { CacheKey } else nil,
            contentDownloadUrl = downloadUrl or assetResolver.Enum.AssetDownloadUrl.RobloxDownloadUrl,
            pendingRequests = {},
        } :: AssetResolverState,
        assetResolver
    ) :: any
    return self
end

function assetResolver.resolve(resolver: AssetResolver, value: unknown): ResolvedAsset?
    if type(value) == "number" then
        return value
    end

    if type(value) ~= "string" then
        return nil
    end

    if string.sub(value, 1, 11) == "rbxasset://" or string.sub(value, 1, 11) == "rbxthumb://" then
        return value
    end

    local id = tonumber(string.match(value, "^rbxassetid://(%d+)$"))
    if id then
        return id
    end

    return nil
end

local function isSuccessResponse(response: unknown): boolean
    if type(response) ~= "table" then
        return false
    end
    local responsePayload = response :: { Body: unknown, StatusCode: unknown, Success: unknown }
    local body = responsePayload.Body
    if type(body) ~= "string" or #body == 0 then
        return false
    end
    if type(responsePayload.StatusCode) == "number" then
        local statusCode = responsePayload.StatusCode :: number
        return statusCode >= 200 and statusCode < 300
    end
    if type(responsePayload.Success) == "boolean" then
        return responsePayload.Success :: boolean
    end
        return false
end

function assetResolver.getAssetContentFromUrl(
    self: AssetResolver,
    url: string,
    cacheKey: CacheKey?,
    forced: boolean?
): string?
        local cache = self.contentCache
    if cacheKey ~= nil and cache and not forced then
        local cachedContent = cache[cacheKey]
        if cachedContent then
            return cachedContent
        end
    end

            local pending = self.pendingRequests
    if cacheKey ~= nil then
        local waiters = pending[cacheKey]
        if waiters then
            if not coroutine.isyieldable() then
                return nil
            end
            local caller = coroutine.running()
            if not caller then
                return nil
            end
            table.insert(waiters, caller)
            return coroutine.yield()
        end
        pending[cacheKey] = {}
    end

    local content: string? = nil
    local requestFn = networkUtil.getRequestFn()
    if not requestFn then
        logger.warn("No request function available to download asset content.")
    else
        local requestOk, requestResult = pcall(
            requestFn,
            {
                Url = url,
                Method = "GET",
            } :: AssetRequestPayload
        )
        if requestOk and isSuccessResponse(requestResult) then
            local responseBody = (requestResult :: { Body: string }).Body
            content = responseBody
            if cacheKey ~= nil and cache then
                local order = self.contentCacheOrder
                if order and cache[cacheKey] == nil then
                    table.insert(order, cacheKey)
                    local evictedKey = if #order > maxCacheEntries then table.remove(order, 1) else nil
                    if evictedKey ~= nil then
                        cache[evictedKey] = nil
                    end
                end
                cache[cacheKey] = responseBody
            end
        elseif not forced then
            logger.warn("Failed to download asset content for url: " .. tostring(url))
        end
    end

            if cacheKey ~= nil then
        local queued = pending[cacheKey]
        pending[cacheKey] = nil
        if queued then
            for _, waiter in queued do
                pcall(coroutine.resume, waiter, content)
            end
        end
    end
    return content
end

function assetResolver.getAssetContentFromId(self: AssetResolver, id: AssetId, forcedRefresh: boolean?): string?
    local resolvedId = self:resolve(id)
        if not resolvedId or type(resolvedId) ~= "number" then
        logger.warn("Invalid asset id: " .. tostring(resolvedId))
        return nil
    end

    local assetUrl = string.format(self.contentDownloadUrl, resolvedId)
    return self:getAssetContentFromUrl(assetUrl, resolvedId, forcedRefresh)
end

return assetResolver

end)() end,
    [84] = function()local wax,script,require=ImportGlobals(84)local ImportGlobals return (function(...)
local iconRegistry = require(script.Parent.Parent.images.windowIcons)
local constants = {}

constants.fontAsset = "rbxassetid://12187375716"

constants.icons = iconRegistry.ids

constants.accent = {
    on = Color3.fromRGB(23, 153, 110),
    onStroke = Color3.fromRGB(32, 201, 144),
}

constants.statAccents = {
    positive = {
        fill = ColorSequence.new(Color3.fromRGB(0, 170, 127), Color3.fromRGB(0, 134, 98)),
        stroke = ColorSequence.new(Color3.fromRGB(0, 213, 156), Color3.fromRGB(0, 189, 135)),
    },
    negative = {
        fill = ColorSequence.new(Color3.fromRGB(172, 47, 47), Color3.fromRGB(135, 37, 37)),
        stroke = ColorSequence.new(Color3.fromRGB(255, 75, 75), Color3.fromRGB(244, 67, 67)),
    },
    neutral = {
        fill = ColorSequence.new(Color3.fromRGB(0, 170, 127), Color3.fromRGB(0, 134, 98)),
        stroke = ColorSequence.new(Color3.fromRGB(0, 213, 156), Color3.fromRGB(0, 189, 135)),
    },
}

constants.zIndex = {
    elementLock = 60,
    elementLockContent = 65,
    bottomFade = 100,
    tabFade = 2,
    notification = 1500,
    drag = 100003,
    restoreContent = 100001,
    restoreInteract = 100002,
}

constants.displayOrder = {
    window = 99999,

    popup = 100001,
}

return constants

end)() end,
    [85] = function()local wax,script,require=ImportGlobals(85)local ImportGlobals return (function(...)
local enums = {}

function enums.itemFromValue(enumType, value)
    local ok, directItem = pcall(function()
        return enumType:FromValue(value)
    end)
    if ok and directItem then
        return directItem
    end

    local listedOk, items = pcall(function()
        return enumType:GetEnumItems()
    end)
    if not listedOk then
        return nil
    end

    for _, candidate in items do
        if candidate.Value == value then
            return candidate
        end
    end

    return nil
end

return enums

end)() end,
    [86] = function()local wax,script,require=ImportGlobals(86)local ImportGlobals return (function(...)
local serviceProvider = require(script.Parent.services)
local filesystem = {}

local isStudio = serviceProvider.getService("RunService"):IsStudio()
local hasExecutorFilesystem = not isStudio
    and typeof(writefile) == "function"
    and typeof(readfile) == "function"
    and typeof(isfile) == "function"
    and typeof(isfolder) == "function"
    and typeof(makefolder) == "function"
    and typeof(listfiles) == "function"
    and typeof(delfile) == "function"
    and typeof(delfolder) == "function"

if hasExecutorFilesystem then
        function filesystem.writefile(writePath: string, writeContents: string)
        writefile(writePath, writeContents)
    end

    function filesystem.readfile(readPath: string): string
        return readfile(readPath)
    end

            if typeof(appendfile) == "function" then
        function filesystem.appendfile(appendPath: string, appendContents: string)
            appendfile(appendPath, appendContents)
        end
    end

    function filesystem.isfile(isFilePath: string): boolean
        return isfile(isFilePath)
    end

    function filesystem.delfile(deleteFilePath: string)
        delfile(deleteFilePath)
    end

    function filesystem.listfiles(listDir: string): { string }
        return listfiles(listDir)
    end

    function filesystem.makefolder(makeFolderPath: string)
        makefolder(makeFolderPath)
    end

    function filesystem.isfolder(isFolderPath: string): boolean
        return isfolder(isFolderPath)
    end

    function filesystem.delfolder(deleteFolderPath: string)
        delfolder(deleteFolderPath)
    end
elseif isStudio then
            local rootFolder = Instance.new("Folder")
    rootFolder.Name = "Filesystem"
    rootFolder.Parent = serviceProvider.getService("ReplicatedStorage")

    local function splitPath(splitInput: string): { string }
        local segments = {}
        for segment in string.gmatch(splitInput, "[^/]+") do
            table.insert(segments, segment)
        end
        return segments
    end

    local function resolveParent(parentSegments: { string }, createMissing: boolean): Instance?
        local current: Instance = rootFolder
        for i = 1, #parentSegments - 1 do
            local child = current:FindFirstChild(parentSegments[i])
            if not child then
                if not createMissing then
                    return nil
                end
                local newFolder = Instance.new("Folder")
                newFolder.Name = parentSegments[i]
                newFolder.Parent = current
                child = newFolder
            end
            current = child :: Instance
        end
        return current
    end

    local function asStringValue(candidate: Instance?): StringValue?
        if candidate and candidate:IsA("StringValue") then
            return candidate
        end
        return nil
    end

    function filesystem.writefile(studioWritePath: string, studioWriteContents: string)
        local writeSegments = splitPath(studioWritePath)
        assert(#writeSegments > 0, "Invalid path")

        local writeParent = resolveParent(writeSegments, true)
        assert(writeParent, "Invalid path")
        local writeName = writeSegments[#writeSegments]

        local existingWrite = writeParent:FindFirstChild(writeName)
        local existingValue = asStringValue(existingWrite)
        if existingValue then
            existingValue.Value = studioWriteContents
        else
            if existingWrite then
                existingWrite:Destroy()
            end
            local newValue = Instance.new("StringValue")
            newValue.Name = writeName
            newValue.Value = studioWriteContents
            newValue.Parent = writeParent
        end
    end

    function filesystem.readfile(studioReadPath: string): string
        local readSegments = splitPath(studioReadPath)
        assert(#readSegments > 0, "Invalid path")

        local readParent = resolveParent(readSegments, false)
        assert(readParent, "File not found: " .. studioReadPath)

        local readChild = readParent:FindFirstChild(readSegments[#readSegments])
        local readValue = asStringValue(readChild)
        assert(readValue, "File not found: " .. studioReadPath)

        return readValue.Value
    end

    function filesystem.isfile(studioIsFilePath: string): boolean
        local isFileSegments = splitPath(studioIsFilePath)
        if #isFileSegments == 0 then
            return false
        end

        local isFileParent = resolveParent(isFileSegments, false)
        if not isFileParent then
            return false
        end

        return asStringValue(isFileParent:FindFirstChild(isFileSegments[#isFileSegments])) ~= nil
    end

    function filesystem.listfiles(studioListDir: string): { string }
        local listSegments = splitPath(studioListDir)
        local listFolder: Instance = rootFolder

        for _, listSegment in listSegments do
            local listChild = listFolder:FindFirstChild(listSegment)
            if not listChild or not listChild:IsA("Folder") then
                error("Folder not found: " .. studioListDir)
            end
            listFolder = listChild
        end

        local listed: { string } = {}
        for _, listEntry in listFolder:GetChildren() do
            table.insert(listed, studioListDir .. "/" .. listEntry.Name)
        end
        return listed
    end

    function filesystem.delfile(studioDeleteFilePath: string)
        local deleteFileSegments = splitPath(studioDeleteFilePath)
        assert(#deleteFileSegments > 0, "Invalid path")

        local deleteFileParent = resolveParent(deleteFileSegments, false)
        assert(deleteFileParent, "File not found: " .. studioDeleteFilePath)

        local deleteFileChild = deleteFileParent:FindFirstChild(deleteFileSegments[#deleteFileSegments])
        local deleteFileValue = asStringValue(deleteFileChild)
        assert(deleteFileValue, "File not found: " .. studioDeleteFilePath)

        deleteFileValue:Destroy()
    end

    function filesystem.makefolder(studioMakeFolderPath: string)
        local makeFolderSegments = splitPath(studioMakeFolderPath)
        if #makeFolderSegments == 0 then
            return
        end

        local makeFolderCurrent: Instance = rootFolder
        for _, makeFolderSegment in makeFolderSegments do
            local makeFolderChild = makeFolderCurrent:FindFirstChild(makeFolderSegment)
            if not makeFolderChild then
                local makeFolderNew = Instance.new("Folder")
                makeFolderNew.Name = makeFolderSegment
                makeFolderNew.Parent = makeFolderCurrent
                makeFolderChild = makeFolderNew
            end
            makeFolderCurrent = makeFolderChild :: Instance
        end
    end

    function filesystem.isfolder(studioIsFolderPath: string): boolean
        local isFolderSegments = splitPath(studioIsFolderPath)
        if #isFolderSegments == 0 then
            return false
        end

        local isFolderCurrent: Instance = rootFolder
        for _, isFolderSegment in isFolderSegments do
            local isFolderChild = isFolderCurrent:FindFirstChild(isFolderSegment)
            if not isFolderChild or not isFolderChild:IsA("Folder") then
                return false
            end
            isFolderCurrent = isFolderChild
        end
        return true
    end

    function filesystem.delfolder(studioDeleteFolderPath: string)
        local deleteFolderSegments = splitPath(studioDeleteFolderPath)
        assert(#deleteFolderSegments > 0, "Invalid path")

        local deleteFolderParent = resolveParent(deleteFolderSegments, false)
        assert(deleteFolderParent, "Folder not found: " .. studioDeleteFolderPath)

        local deleteFolderChild = deleteFolderParent:FindFirstChild(deleteFolderSegments[#deleteFolderSegments])
        assert(deleteFolderChild and deleteFolderChild:IsA("Folder"), "Folder not found: " .. studioDeleteFolderPath)

        deleteFolderChild:Destroy()
    end
end

if filesystem.writefile and not filesystem.appendfile then
    function filesystem.appendfile(fallbackAppendPath: string, fallbackAppendContents: string)
        local fallbackExisting = if filesystem.isfile(fallbackAppendPath) then filesystem.readfile(fallbackAppendPath) else nil
        filesystem.writefile(fallbackAppendPath, if fallbackExisting then fallbackExisting .. fallbackAppendContents else fallbackAppendContents)
    end
end

function filesystem.ensureFolder(ensureFolderPath: string)
    if not filesystem.isfolder(ensureFolderPath) then
        filesystem.makefolder(ensureFolderPath)
    end
end

function filesystem.ensureDir(ensureDirPath: string)
    local partial = ""
    for ensureSegment in string.gmatch(ensureDirPath, "[^/]+") do
        partial = if partial == "" then ensureSegment else partial .. "/" .. ensureSegment
        if not filesystem.isfolder(partial) then
            filesystem.makefolder(partial)
        end
    end
end

return filesystem

end)() end,
    [87] = function()local wax,script,require=ImportGlobals(87)local ImportGlobals return (function(...)
local fs = require(script.Parent.filesystem)
local pathUtil = require(script.Parent.path)

local defaultRoot = "Astra"

local filesystemManager = {}
filesystemManager.__index = filesystemManager

export type FileSystemManager = {
    root: string,
    assets: string,
    getPath: (self: FileSystemManager, subpath: string?) -> string,
    getAssetsFolder: (self: FileSystemManager, subfolder: string?) -> string,
    getRootFolder: (self: FileSystemManager) -> string,
}

function filesystemManager.new(rootOverride: string?): FileSystemManager
    local root = rootOverride or defaultRoot
    local self = setmetatable({
        root = root,
        assets = root .. "/assets",
    }, filesystemManager) :: any
    pcall(fs.ensureFolder, self.root)
    pcall(fs.ensureFolder, self.assets)

    return self
end

function filesystemManager:getPath(subpath: string?): string
    return pathUtil.join(self.root, subpath)
end

function filesystemManager:getAssetsFolder(subfolder: string?): string
    if subfolder then
        local folderPath = pathUtil.join(self.assets, subfolder)
        pcall(fs.ensureFolder, folderPath)
        return folderPath
    end
    return self.assets
end

function filesystemManager:getRootFolder(): string
    return self.root
end

return filesystemManager

end)() end,
    [88] = function()local wax,script,require=ImportGlobals(88)local ImportGlobals return (function(...)
local fontManager = require(script.Parent.services)
local a2 = fontManager.getService("HttpService")
local a3 = fontManager.getService("RunService")
local a4 = require(script.Parent.filesystem)
local a5 = require(script.Parent.assetResolver)
local a6 = require(script.Parent.log)
local a7 = require(script.Parent.path)

local function a8(a9: unknown): string?
    local a10, a11 = pcall(function()
        return a2:JSONEncode(a9)
    end)
    if a10 and type(a11) == "string" then
        return a11
    else
        a6.warn("Failed to encode JSON:", a11)
        return nil
    end
end

local function a12(a13: string): unknown?
    local a14, a15 = pcall(function()
        return a2:JSONDecode(a13)
    end)
    if a14 then
        return a15
    else
        a6.warn("Failed to decode JSON:", a15)
        return nil
    end
end

local a16 = {}
a16.__type = "fontManager"

export type FontFace = {
    name: string,
    family: string,
    weight: number,
    style: string,
    assetId: string,
}

export type FontManifest = {
    name: string,
    faces: { FontFace },
}

export type CachedFont = {
    customId: number | string,
    manifest: FontManifest,
    loadedFromDisk: boolean,
    variants: { [string]: Font },
}

export type FontCache = { [number]: CachedFont }

export type FontResolverOptions = {
    fallbackFont: Font?,
    saveToDisk: boolean?,
    skipCache: boolean?,
}

type FontManagerState = {
    _debug: boolean,
    _pendingLoads: { [string]: { thread } },
    rootFolder: string,
    assetResolver: assetResolver.AssetResolver,
    defaultOptions: FontResolverOptions,
    fontCache: FontCache?,
}

export type FontManager = FontManagerState & {
    resolve: (self: FontManager, id: number | string) -> Font?,
    loadFont: (
        self: FontManager,
        id: (number | string)?,
        fontWeight: Enum.FontWeight?,
        fontStyle: Enum.FontStyle?,
        saveToDisk: boolean?,
        skipCache: boolean?
    ) -> Font?,
    getFontFromId: (self: FontManager, id: number | string) -> Font?,
}

export type fontManager = FontManager

local function a17(a18: unknown): FontManifest?
    local a19 = a18 :: any
    if type(a19) ~= "table" or type(a19.name) ~= "string" or type(a19.faces) ~= "table" then
        return nil
    end
    a19.name = a7.sanitizeFile(a19.name)
    if #a19.name == 0 or #a19.faces == 0 then
        return nil
    end
    for _, a20 in a19.faces do
        if type(a20) ~= "table" or type(a20.name) ~= "string" or type(a20.assetId) ~= "string" then
            return nil
        end
        a20.name = a7.sanitizeFile(a20.name)
        if #a20.name == 0 then
            return nil
        end
    end
    return a19 :: FontManifest
end

local a21 = { "\0\1\0\0", "OTTO", "true", "ttcf", "wOFF", "wOF2" }

local function a22(a23: string): boolean
    for _, a24 in a21 do
        if string.sub(a23, 1, #a24) == a24 then
            return true
        end
    end
    return false
end

local a25 = getfenv()

local function a26(a27: Enum.FontWeight, a28: Enum.FontStyle): string
    return tostring(a27) .. "|" .. tostring(a28)
end

local function a29(id: unknown): number?
    if type(id) == "number" then
        return id
    end

    if type(id) == "string" then
        return tonumber(id)
    end

    return nil
end

function a16.__index(self: FontManagerState, a30: unknown): unknown
    local a31 = (a16 :: any)[a30]
    if a31 ~= nil then
        return a31
    end

    local a32 = a29(a30)
    if not a32 then
        return nil
    end

    local a33 = self.fontCache
    if a33 then
        local a34 = a33[a32]
        if a34 then
            local a35 = a26(Enum.FontWeight.Regular, Enum.FontStyle.Normal)
            local a36 = a34.variants and a34.variants[a35]
            if a36 then
                return a36
            end

            for _, a37 in pairs(a34.variants or {}) do
                return a37
            end
        end
    end

    return nil
end

function a16.new(
    a38: string,
    a39: boolean?,
    a40: boolean?,
    a41: assetResolver.AssetDownloadUrl?,
    a42: FontResolverOptions?
): FontManager
    local a43 = a42
        or {
            saveToDisk = true,
            skipCache = not a40 or false,
            fallbackFont = Font.fromEnum(Enum.Font.SourceSans),
        }
    local self = setmetatable(
        {
            rootFolder = a38,
            fontCache = if a40 then {} :: FontCache else nil,
            assetResolver = a5.new(a40, a41),
            defaultOptions = a43,
            _debug = a39 or a3:IsStudio() or false,
            _pendingLoads = {},
        } :: FontManagerState,
        a16
    ) :: any
        pcall(a4.ensureFolder, a38)
    return self
end

local function a44(
    self: FontManager,
    a45: number,
    a46: Enum.FontWeight,
    a47: Enum.FontStyle,
    a48: string
): Font?
    local a49 = self.rootFolder .. "/" .. a45 .. ".json"
        if typeof(a4.isfile) ~= "function" then
        return self.defaultOptions.fallbackFont
    end
    local a50: string? = nil
    local a51 = false
    if a3:IsStudio() and self._debug then
        a6.warn("Font manifest loading is not supported in Studio. Using default font manifest for testing.")
        a50 =
            '{"name":"Inter","faces":[{"name":"Thin","weight":100,"style":"normal","assetId":"rbxassetid://12187277209"},{"name":"Extra Light","weight":200,"style":"normal","assetId":"rbxassetid://12187293441"},{"name":"Light","weight":300,"style":"normal","assetId":"rbxassetid://12187268450"},{"name":"Regular","weight":400,"style":"normal","assetId":"rbxassetid://12187266066"},{"name":"Medium","weight":500,"style":"normal","assetId":"rbxassetid://12187336822"},{"name":"Semi Bold","weight":600,"style":"normal","assetId":"rbxassetid://12187254443"},{"name":"Bold","weight":700,"style":"normal","assetId":"rbxassetid://12187275575"},{"name":"Extra Bold","weight":800,"style":"normal","assetId":"rbxassetid://12187267750"},{"name":"Black","weight":900,"style":"normal","assetId":"rbxassetid://12187359223"}]}'
    elseif a4.isfile(a49) then
        local ok, a52 = pcall(a4.readfile, a49)
        a50 = ok and a52 or nil
    else
                a50 = self.assetResolver:getAssetContentFromId(a45, false)
        a51 = a50 ~= nil
    end
    local a53: FontManifest? = nil
    if a50 then
        local ok, a54 = pcall(a12, a50)
        if ok and a54 then
            a53 = a17(a54)
        else
            if self._debug then
                a6.warn(
                    "Failed to parse font manifest for font id: "
                        .. tostring(a45)
                        .. ". Error: "
                        .. tostring(a54)
                )
            end
        end
    else
        return self.defaultOptions.fallbackFont
    end
    if not a53 then
        if self._debug then
            a6.warn("Font manifest is nil for font id: " .. tostring(a45))
        end
                pcall(a4.delfile, a49)
        return self.defaultOptions.fallbackFont
    end
    if a51 and a50 then
        pcall(a4.writefile, a49, a50)
    end
    local a55 = self.rootFolder .. "/" .. a53.name
    local a56 = true
        if not pcall(a4.ensureFolder, a55) then
        return self.defaultOptions.fallbackFont
    end

            for i, a57 in ipairs(a53.faces) do
        local a58 = a55 .. "/" .. a57.name:gsub(" ", "-") .. ".ttf"
                        if a4.isfile(a58) then
            local a59, a60 = pcall(a4.readfile, a58)
            if not a59 or type(a60) ~= "string" or not a22(a60) then
                pcall(a4.delfile, a58)
            end
        end
        if not a4.isfile(a58) then
            local a61 = self.assetResolver:resolve(a57.assetId)
            local a62 = if a61 ~= nil
                then self.assetResolver:getAssetContentFromId(a61, false)
                else nil
            if not a62 or not a22(a62) then
                a56 = false
                if self._debug then
                    a6.warn(
                        "Font face content is missing or not a font for font id: "
                            .. tostring(a45)
                            .. ", face: "
                            .. a57.name
                    )
                end
                continue
            end
            pcall(a4.writefile, a58, a62)
        end
        local ok, a63 = pcall(a25.getcustomasset, a58)
        if ok and type(a63) == "string" then
            a53.faces[i].assetId = a63
            if self._debug then
                a6.print("Loaded font face for id: " .. tostring(a45) .. ", face: " .. a57.name)
            end
        else
            a56 = false
            if self._debug then
                a6.warn("Failed to load font face for id: " .. tostring(a45) .. ", face: " .. a57.name)
            end
        end
    end
            if not a56 then
        return self.defaultOptions.fallbackFont
    end

    local a64 = a8(a53)
    if not a64 or not pcall(a4.writefile, a55 .. "/manifest.json", a64) then
        return self.defaultOptions.fallbackFont
    end
    local a65, a66 = pcall(a25.getcustomasset, a55 .. "/manifest.json")
    if not a65 or not a66 then
        if self._debug then
            a6.warn("Failed to load font manifest for id: " .. tostring(a45))
        end
        return self.defaultOptions.fallbackFont
    end
        local ok, a67 = pcall(Font.new, a66 :: any, a46, a47)
    if not ok or not a67 then
        if self._debug then
            a6.warn("Failed to load font for id: " .. tostring(a45))
        end
        return self.defaultOptions.fallbackFont
    end

        local a68 = self.fontCache
    if a68 then
        a68[a45] = {
            customId = a66,
            manifest = a53,
            loadedFromDisk = true,
            variants = {
                [a48] = a67,
            },
        }
        if self._debug then
            a6.print("Cached font for id: " .. tostring(a45))
        end
    end
    return a67
end

function a16.loadFont(
    self: FontManager,
    id: (number | string)?,
    a69: Enum.FontWeight?,
    a70: Enum.FontStyle?,
    a71: boolean?,
    a72: boolean?
): Font?
    if a71 == nil then
        a71 = self.defaultOptions.saveToDisk
    end
    if a72 == nil then
        a72 = self.defaultOptions.skipCache
    end
    local a73 = a69 or Enum.FontWeight.Regular
    local a74 = a70 or Enum.FontStyle.Normal
    local a75 = a26(a73, a74)
    local a76 = self.assetResolver:resolve(id)
    local a77 = a29(a76)
    if not a77 then
        a6.warn("Invalid font id: " .. tostring(id))
        return self.defaultOptions.fallbackFont
    end
        local a78 = self.fontCache
    if a78 and not a72 then
        local a79 = a78[a77]
        if a79 then
            local a80 = a79.variants and a79.variants[a75]
            if a80 then
                if self._debug then
                    a6.print("Loaded font from cache for id: " .. tostring(a77))
                end
                return a80
            end

            local ok, a81 = pcall(Font.new, a79.customId :: any, a73, a74)
            if ok and a81 then
                a79.variants = a79.variants or {}
                a79.variants[a75] = a81
                if self._debug then
                    a6.print("Loaded font variant from cache for id: " .. tostring(a77))
                end
                return a81
            else
                if self._debug then
                    a6.warn("Failed to load font from cache for id: " .. tostring(a77))
                end
                a78[a77] = nil
                return self.defaultOptions.fallbackFont
            end
        end
    end

        if not a71 then
        if self._debug then
            a6.warn("Font loading without saving to disk can be detected by Anti-cheats.")
        end

        return self.defaultOptions.fallbackFont
    end

        local a82 = self._pendingLoads
    local a83 = tostring(a77) .. "|" .. a75
    local a84 = a82[a83]
    if a84 then
        if not coroutine.isyieldable() then
            return self.defaultOptions.fallbackFont
        end
        local a85 = coroutine.running()
        if not a85 then
            return self.defaultOptions.fallbackFont
        end
        table.insert(a84, a85)
        return coroutine.yield()
    end
    a82[a83] = {}

    local ok, a86 = pcall(a44, self, a77, a73, a74, a75)
    local a87 = if ok then a86 else self.defaultOptions.fallbackFont

                a84 = a82[a83]
    a82[a83] = nil
    if a84 then
        for _, a88 in a84 do
            pcall(coroutine.resume, a88, a87)
        end
    end
    return a87
end

function a16.resolve(self: FontManager, id: number | string): Font?
    local a89 = a29(id)
    if not a89 then
        return nil
    end

    local a90 = self.fontCache
    if a90 then
        local a91 = a90[a89]
        if a91 then
            if self._debug then
                a6.print("Resolved font for id: " .. tostring(a89))
            end
            for _, a92 in pairs(a91.variants or {}) do
                return a92
            end
        end
    end

    return nil
end

function a16.getFontFromId(self: FontManager, id: number | string): Font?
    return self:resolve(id)
end

return a16

end)() end,
    [89] = function()local wax,script,require=ImportGlobals(89)local ImportGlobals return (function(...)

local functions = {}

local colors = require(script.Parent.Parent.functions.textMetrics)
local textMetrics = require(script.Parent.Parent.functions.colors)
local flagNames = require(script.Parent.Parent.functions.flagNames)

functions.textWidth = colors.textWidth
functions.textHeight = colors.textHeight
functions.deriveFlagFromName = flagNames.deriveFlagFromName
functions.contrastColor = textMetrics.contrastColor
functions.toColorSequence = textMetrics.toColorSequence
functions.contrastText = textMetrics.contrastText

return functions

end)() end,
    [90] = function()local wax,script,require=ImportGlobals(90)local ImportGlobals return (function(...)
local layouts = {}

local topbarLayout = require(script.Parent.Parent.layouts.Topbar)
local sidebarLayout = require(script.Parent.Parent.layouts.Sidebar)
local sidebarCollapsedLayout = require(script.Parent.Parent.layouts.SidebarCollapsed)

local topbarHeightConstant = 64

-- [[Clearance above and below the tab row. The strip is centred in the]]
-- [[chrome band with matching gaps on both sides, so the pills read as]]
-- [[neither pressed against the topbar nor pressed against the content.]]
local tabStripTopClearance = 6
local tabStripBottomClearance = 6
local tabStripTopOffset = topbarHeightConstant + tabStripTopClearance
local tabStripRowHeight = 38
local chromeGap = tabStripBottomClearance

export type Mode = "top" | "sidebar"

export type Layout = {
    mode: Mode,

        topbarHeight: number,
    chromeHeight: number,

            pageDirection: Enum.FillDirection,

            fadeSize: UDim2,
    fadeTransparency: NumberSequence,
    fadeCorners: { string }?,

        tabStripTop: number?,
    tabStripHeight: number?,
    tabStripInset: number?,
    tabStripEdgePadding: number?,
    tabStripFadeWidth: number?,

        railWidth: number?,
    railCollapsedWidth: number?,
    railCollapseBelow: number?,
    rowHeight: number?,
    rowCornerRadius: number?,
    rowSpacing: number?,
    railPadding: number?,
    rowInset: number?,
    rowPadding: number?,
    rowContentSpacing: number?,
    rowIconSize: number?,
    cardTransparency: number?,
    cardStrokeRotation: number?,
    cardStrokeTransparency: NumberSequence?,
    cardCorners: { string }?,
    forceCollapsed: boolean?,
}

layouts.top = {
    mode = "top" :: Mode,

    topbarHeight = topbarHeightConstant,
    chromeHeight = tabStripTopOffset + tabStripRowHeight + chromeGap,

    tabStripTop = tabStripTopOffset,
    tabStripHeight = tabStripRowHeight,

    -- [[Margin between the window edge and the tab strip. The strip's]]
    -- [[ScrollingFrame is inset by this much on each side, so overflowing]]
    -- [[tabs clip inside the window instead of running into its border.]]
    tabStripInset = 30,

    -- [[Empty gutter *inside* the strip at each end (canvas UIPadding):]]
    -- [[the first and last pills never touch the strip's clip edge, at]]
    -- [[rest or scrolled, so the edge fades dissolve over empty space]]
    -- [[instead of over a pill pressed up against the edge.]]
    tabStripEdgePadding = 14,

    -- [[Width of the gradient fade that marks a clipping edge; wide]]
    -- [[enough to cover the end gutter plus a little strip beyond it.]]
    tabStripFadeWidth = 30,

    pageDirection = Enum.FillDirection.Horizontal,

            fadeSize = UDim2.new(1, 0, -0.093, 100),
    fadeTransparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0),
        NumberSequenceKeypoint.new(0.4414, 0),
        NumberSequenceKeypoint.new(0.7007, 0.631),
        NumberSequenceKeypoint.new(1, 1),
    }),
} :: Layout

layouts.sidebar = {
    mode = "sidebar" :: Mode,

    topbarHeight = topbarHeightConstant,
    chromeHeight = topbarHeightConstant,

            railWidth = 219,
            railCollapsedWidth = 64,
    railCollapseBelow = 520,

    rowHeight = 38,
    rowCornerRadius = 14,
    rowSpacing = 4,
        railPadding = 17,
    rowInset = 15,
    rowPadding = 10,
    rowContentSpacing = 6,
    rowIconSize = 20,

    pageDirection = Enum.FillDirection.Vertical,

    cardTransparency = 0.98,
    cardStrokeRotation = 55,
            cardCorners = { "TopLeftRadius", "BottomRightRadius" },
    cardStrokeTransparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.9),
        NumberSequenceKeypoint.new(0.128, 0.95),
        NumberSequenceKeypoint.new(0.414, 0.985),
        NumberSequenceKeypoint.new(1, 1),
    }),

    fadeSize = UDim2.new(1, 0, 0, 55),
    fadeTransparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0),
        NumberSequenceKeypoint.new(0.377, 0),
        NumberSequenceKeypoint.new(1, 1),
    }),
        fadeCorners = { "BottomLeftRadius", "BottomRightRadius" },
} :: Layout

local collapsedSidebarLayout = table.clone(layouts.sidebar)
collapsedSidebarLayout.forceCollapsed = true

local implementments = {
    top = topbarLayout,
    sidebar = sidebarLayout,
    collapsedSidebar = sidebarCollapsedLayout,
}

function layouts.get(requestedMode: unknown): Layout?
    if requestedMode == "sidebar" then
        return layouts.sidebar
    elseif requestedMode == "collapsedSidebar" then
        return collapsedSidebarLayout
    elseif requestedMode == "top" then
        return layouts.top
    end
    return nil
end

function layouts.implementation(requestedMode: unknown)
    return implementments[requestedMode]
end

function layouts.railWidthFor(layout: Layout, viewportWidth: number): number
    if layout.mode ~= "sidebar" then
        return 0
    end
    local expandedWidth = layout.railWidth :: number
    if layout.forceCollapsed or viewportWidth < (layout.railCollapseBelow :: number) then
        return layout.railCollapsedWidth :: number
    end
    return expandedWidth
end

return layouts

end)() end,
    [91] = function()local wax,script,require=ImportGlobals(91)local ImportGlobals return (function(...)
local coreState = require(script.Parent.Parent.core.state)
local logger = require(script.Parent.log)

local locale = {}

export type LocaleToken = { [any]: string }
export type Translator = (source: string, localeId: string) -> string?
export type TranslationTables = { [string]: { [string]: string } }

locale.strings = {} :: TranslationTables
locale.current = "en"
locale.translator = nil :: Translator?

local tokenKey = {}

local function languagePrefix(id: string): string
    return string.match(id, "^(%a+)") or id
end

function locale.t(value: unknown): unknown
    if type(value) ~= "string" or value == "" then
        return value
    end
    return { [tokenKey] = value }
end

function locale.isToken(candidate: unknown): boolean
    return type(candidate) == "table" and type((candidate :: { [any]: unknown })[tokenKey]) == "string"
end

function locale.sourceOf(token: LocaleToken): string
    return token[tokenKey]
end

function locale.resolve(text: unknown): any
    if type(text) ~= "string" then
        return text
    end

    if locale.translator then
        local ok, translated = pcall(locale.translator, text, locale.current)
        if ok and type(translated) == "string" and translated ~= "" then
            return translated
        end
    end

    local exactTable = locale.strings[locale.current]
    if exactTable and exactTable[text] then
        return exactTable[text]
    end

    local prefixTable = locale.strings[languagePrefix(locale.current)]
    if prefixTable and prefixTable[text] then
        return prefixTable[text]
    end

    return text
end

function locale.register(tables: TranslationTables?)
    if type(tables) ~= "table" then
        return
    end
    for id, entryTable in tables do
        if type(id) == "string" and type(entryTable) == "table" then
            id = string.lower(id)
            local target = locale.strings[id]
            if not target then
                target = {}
                locale.strings[id] = target
            end
            for sourceKey, sourceText in entryTable do
                if type(sourceKey) == "string" and type(sourceText) == "string" then
                    target[sourceKey] = sourceText
                else
                    logger.warn(`Astra: skipping a '{id}' translation, entries must be string to string.`)
                end
            end
        end
    end
end

function locale.setActive(requestedLocale: string?): string
    locale.current = if type(requestedLocale) == "string" and requestedLocale ~= "" then string.lower(requestedLocale) else "en"
    return locale.current
end

function locale.detect(): string
    local id = coreState.localizationService.RobloxLocaleId
    if type(id) == "string" and id ~= "" then
        return string.lower(id)
    end
    return "en"
end

return locale

end)() end,
    [92] = function()local wax,script,require=ImportGlobals(92)local ImportGlobals return (function(...)
local function lockable<T>(element: T): T
    local target = element :: any

    function target:Lock(reason: string?)
        self.window:_setElementLocked(self, true, reason)
    end

    function target:Unlock()
        self.window:_setElementLocked(self, false)
    end

    function target:IsLocked(): boolean
        return self.locked == true
    end

    return element
end

return lockable

end)() end,
    [93] = function()local wax,script,require=ImportGlobals(93)local ImportGlobals return (function(...)
local log = {}

type SuppressPredicate = () -> boolean

local function defaultSecureModePredicate(): boolean
    if typeof(getgenv) ~= "function" then
        return false
    end
    local ok, secureModeSource = pcall(function()
        return getgenv().ASTRA_SECURE
    end)
    return ok and secureModeSource == true
end

local secureModePredicate: SuppressPredicate = defaultSecureModePredicate
local suppressPredicate: SuppressPredicate? = nil

function log.setSecureModeSource(source: SuppressPredicate?)
    secureModePredicate = if type(source) == "function" then source else defaultSecureModePredicate
end

function log.setSuppressPredicate(predicate: SuppressPredicate?)
    suppressPredicate = if type(predicate) == "function" then predicate else nil
end

local function shouldSuppress(): boolean
    if suppressPredicate and suppressPredicate() then
        return true
    end
    return secureModePredicate()
end

function log.warn(...)
    if shouldSuppress() then
        return
    end
    -- [[`warn` is a Roblox global; plain Luau runtimes (the CLI suites) do]]
    -- [[not provide it. Logging must never crash the library.]]
    if typeof(warn) == "function" then
        warn(...)
    end
end

function log.print(...)
    if shouldSuppress() then
        return
    end
    print(...)
end

return log

end)() end,
    [94] = function()local wax,script,require=ImportGlobals(94)local ImportGlobals return (function(...)
-- [[Astra motion service.
--
-- Every animation in the library used to build its own TweenInfo and call
-- TweenService:Create directly. That has three costs:
--
--   1. TweenInfo values are re-created per call, including in hot paths such
--      as element hover, where the same 0.25s Quint/Out spec is allocated on
--      every pointer enter and leave.
--   2. Nothing tracks the tween a component already started on an instance,
--      so a fast hover in/out leaves two tweens interpolating the same
--      property. The newer one wins per frame, which is wasted work and the
--      usual source of "the stroke flickers" jitter.
--   3. Hosts had no way to slow the interface down or switch animation off
--      for accessibility or low-end devices.
--
-- This module is the one place that answers all three: named specs are
-- created once, `motion.tween` cancels an in-flight tween when it would fight
-- over the same property, and everything animated through it answers to a
-- single time scale (`motion.setProfile`, the "Animation speed" setting).
-- Sites that still call `TweenService:Create` directly (component-local
-- flourishes such as the notification queue) keep their own timing until they are
-- moved over.
--
-- Behaviour contract, deliberately conservative:
--   * `motion.tween` is a drop-in for `TweenService:Create(instance, info,
--     props):Play()` — same target values, same easing, same duration at the
--     default profile.
--   * Properties already equal to their target are dropped from the tween; a
--     call whose properties all match does not create a tween at all (this is
--     the same "skip unchanged" rule the window's reveal path already used).
--   * With motion disabled, or the time scale at zero, the target properties
--     are assigned directly and no tween is created. Cancelling the previous
--     tween first keeps a disabled interface from being overwritten by an
--     animation that was already running.
--]]
local coreState = require(script.Parent.Parent.core.state)

local motion = {}

export type MotionSpec = string | TweenInfo

-- [[Named specs: the library's motion vocabulary. Each one is a fixed,
-- shared TweenInfo, so a site that animates at 0.25s Quint/Out no longer
-- allocates a new TweenInfo every time it runs. Names are stable API: a host
-- can use them through `Astra.Motion.tween` to match the window's timing.
--
-- The vocabulary follows one system, so unrelated components still read as
-- the same interface:
--   * entrances decelerate (Out) — things arrive fast and settle gently;
--   * exits accelerate (In) — things leaving should not linger;
--   * lateral state moves ease InOut — a bar that slides between states
--     reads as deliberate, not reactive;
--   * playful surfaces (the window shell, the capsule, oversized feedback)
--     get a small Back overshoot; everything else stays honest.
-- Durations step up with the size of what moves: 0.16 for a press, 0.25 for
-- hover, 0.4 for an element, 0.5–0.6 for surfaces, 0.55 for the shell.]]
local SPECS: { [string]: TweenInfo } = {
	-- [[One frame's worth of movement: the "no animation" target for hosts
	-- that want the tween-completed path without a visible transition.]]
	instant = TweenInfo.new(0.01, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),

	-- [[Press/lift feedback: the shortest readable transition.]]
	fast = TweenInfo.new(0.16, Enum.EasingStyle.Quint, Enum.EasingDirection.Out),

	-- [[Hover feedback and dropdown rows: the most-used spec in the library.]]
	snappy = TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out),

	-- [[Element reveal: what `Window:_reveal` plays when an element appears.]]
	normal = TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out),

	-- [[Surface moves: window open/restore, panel reveal.]]
	smooth = TweenInfo.new(0.5, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out),

	-- [[The window's own entrance and other hero transitions.]]
	emphasized = TweenInfo.new(0.6, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out),

	-- [[Window entrance scale and corner. Long enough to read as a deliberate
	-- load transition, with a small overshoot so the shell settles softly.]]
	pop = TweenInfo.new(0.55, Enum.EasingStyle.Back, Enum.EasingDirection.Out),

	-- [[Lateral state moves: the window folding into its capsule and back,
	-- a bar sliding between resting places. InOut because the surface is
	-- neither arriving nor leaving — it is moving house.]]
	glide = TweenInfo.new(0.35, Enum.EasingStyle.Quart, Enum.EasingDirection.InOut),

	-- [[Dismissals: quicker than the entrances that created them, and
	-- accelerating (In) so a card that is leaving gains pace instead of
	-- lingering at the end of its curve.]]
	exit = TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.In),

	-- [[Overshoot for drag/pop feedback (the capsule settling back).]]
	spring = TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out),

	-- [[Short overshoot for small surfaces: the collapsed capsule appearing,
	-- a pill landing back after a press. Same family as `spring`, scaled
	-- down so small elements never read bouncy.]]
	settle = TweenInfo.new(0.34, Enum.EasingStyle.Back, Enum.EasingDirection.Out),

	-- [[Tab selection gradient spin.]]
	spin = TweenInfo.new(0.7, Enum.EasingStyle.Quint, Enum.EasingDirection.Out),

	-- [[The slow ambient drift of a theme's LiveAnimation gradient.]]
	drift = TweenInfo.new(10, Enum.EasingStyle.Quint, Enum.EasingDirection.Out),
}

local DEFAULT_SPEC = "normal"

-- [[Profiles map the "Animation speed" setting onto the time scale. `instant`
-- is the reduced-motion / lowest-cost option: nothing tweens, every target is
-- applied on the spot.]]
motion.profiles = {
	relaxed = { timeScale = 1.35 },
	normal = { timeScale = 1 },
	snappy = { timeScale = 0.7 },
	instant = { timeScale = 0, enabled = false },
}

motion.specNames = {
	"instant",
	"fast",
	"snappy",
	"normal",
	"smooth",
	"emphasized",
	"pop",
	"glide",
	"exit",
	"spring",
	"settle",
	"spin",
	"drift",
}

motion.enabled = true
motion.timeScale = 1
motion.profile = "normal"

-- [[Scaled specs are cached per input (a name or a TweenInfo) and dropped
-- whenever the time scale changes, so the hot paths never allocate.]]
local scaled: { [any]: TweenInfo } = setmetatable({}, { __mode = "k" })

-- [[The tween this service last started on an instance, with the property set
-- it targets. Weak keys: a destroyed instance must not be kept alive.]]
local active: { [any]: { tween: Tween, props: { [string]: any } } } = setmetatable({}, { __mode = "k" })

local function scaleInfo(info: TweenInfo, timeScale: number): TweenInfo
	return TweenInfo.new(
		info.Time * timeScale,
		info.EasingStyle,
		info.EasingDirection,
		info.RepeatCount,
		info.Reverses,
		info.DelayTime
	)
end

-- [[A TweenInfo, recognised structurally rather than through
-- `typeof(x) == "TweenInfo"`: the library's own test stubs build TweenInfo
-- values as plain tables, and a spec passed in from a caller must survive
-- both environments instead of silently collapsing to the default.]]
local function isInfo(value): boolean
	return type(value) == "table" and type(value.Time) == "number" and value.EasingStyle ~= nil
end

-- [[Resolves a spec (name or TweenInfo) to the TweenInfo that should be played
-- right now, applying the active time scale. At the default scale a named spec
-- returns the shared table itself, so the common path allocates nothing.]]
function motion.spec(spec: MotionSpec?): TweenInfo
	local resolved = spec or DEFAULT_SPEC
	if type(resolved) == "string" then
		resolved = SPECS[resolved] or SPECS[DEFAULT_SPEC]
	elseif not isInfo(resolved) then
		resolved = SPECS[DEFAULT_SPEC]
	end

	if motion.timeScale == 1 then
		return resolved :: TweenInfo
	end

	local cached = scaled[resolved]
	if not cached then
		cached = scaleInfo(resolved :: TweenInfo, motion.timeScale)
		scaled[resolved] = cached
	end
	return cached
end

-- [[Drops the cached scaled specs. Called whenever the time scale changes.]]
local function invalidate()
	scaled = setmetatable({}, { __mode = "k" })
end

-- [[Seconds between cascade steps, scaled like everything else: a relaxed
-- profile reveals a page more slowly, `instant` reveals it in one go.]]
function motion.step(base: number): number
	if not motion.enabled then
		return 0
	end
	return base * motion.timeScale
end

function motion.setTimeScale(scale: number): number
	local previous = motion.timeScale
	scale = math.max(0, math.min(4, scale or 1))
	if scale ~= previous then
		motion.timeScale = scale
		invalidate()
	end
	return previous
end

function motion.getTimeScale(): number
	return motion.timeScale
end

function motion.setEnabled(enabled: boolean): boolean
	local previous = motion.enabled
	motion.enabled = enabled ~= false
	return previous
end

function motion.isEnabled(): boolean
	return motion.enabled
end

-- [[Applies a named profile. Unknown names are ignored (returns false) so a
-- stale persisted value can never leave the library in a half-configured
-- state.]]
function motion.setProfile(name: string): boolean
	local profile = motion.profiles[name] :: { timeScale: number?, enabled: boolean? }?
	if not profile then
		return false
	end
	motion.setTimeScale(profile.timeScale or 1)
	motion.setEnabled(profile.enabled ~= false)
	motion.profile = name
	return true
end

function motion.getProfile(): string
	return motion.profile
end

-- [[Stops the tween this service started on the instance, if any. The slot is
-- cleared so a later call does not try to cancel it twice.]]
function motion.cancel(instance: Instance?): boolean
	if not instance then
		return false
	end
	local entry = active[instance]
	if not entry then
		return false
	end
	active[instance] = nil
	local ok = pcall(function()
		entry.tween:Cancel()
	end)
	return ok
end

local function overlaps(entry, props): boolean
	for key in props do
		if entry.props[key] ~= nil then
			return true
		end
	end
	return false
end

local function assign(instance, props)
	for key, value in props do
		instance[key] = value
	end
end

-- [[Animate `props` on `instance` with `spec` (a spec name or TweenInfo).
--
-- Drops properties that already hold their target value, and skips the tween
-- entirely when nothing would change. When the new tween would animate a
-- property the instance is already animating through this service, the older
-- tween is cancelled first — one writer per property, which is what removes
-- the double-tween churn on rapid hover in/out.
--
-- `onCompleted`, when given, is connected *before* the tween is played, so a
-- caller that has to run a settle path cannot miss a completion that happens
-- on the same tick (which is exactly what a test stub's synchronous Play
-- does). Callers that need that path should treat a nil return as "the work
-- is already done" and run it themselves.
--
-- Returns the tween, or nil when nothing was animated (already at target, or
-- motion is disabled).]]
function motion.tween(
	instance: Instance?,
	props: { [string]: any },
	spec: MotionSpec?,
	onCompleted: (() -> ())?
): Tween?
	if not instance or not props then
		return nil
	end

	local entry = active[instance]
	if entry and overlaps(entry, props) then
		motion.cancel(instance)
		entry = nil
	end

	if not motion.enabled or motion.timeScale == 0 then
		assign(instance, props)
		return nil
	end

	-- [[Skip the tween when every property is already where it should be.
	-- Comparisons are per property, so a partially-satisfied call still
	-- animates the properties that need it.]]
	local pending: { [string]: any }? = nil
	for key, value in props do
		if instance[key] ~= value then
			pending = pending or {}
			pending[key] = value
		end
	end
	if not pending then
		return nil
	end

	local info = motion.spec(spec)
	local tween = coreState.tweenService:Create(instance :: Instance, info, pending)
	active[instance] = { tween = tween, props = pending } :: any
	if onCompleted then
		tween.Completed:Connect(onCompleted)
	end
	tween:Play()
	return tween
end

-- [[Applies `props` on the spot (cancelling anything this service is running
-- on the instance) - the state a tween would have settled on, with no
-- animation. The same target the caller would pass to `motion.tween`.]]
function motion.set(instance: Instance?, props: { [string]: any }): ()
	if instance and props then
		motion.cancel(instance)
		assign(instance, props)
	end
end

return motion

end)() end,
    [95] = function()local wax,script,require=ImportGlobals(95)local ImportGlobals return (function(...)
local function moveable<T>(dragState: T): T
    local target = dragState :: any

    function target:MoveTo(index: number)
        self.tab:_moveElement(self, index)
    end

    function target:MoveToTop()
        self.tab:_moveElement(self, 1)
    end

    function target:MoveToBottom()
        self.tab:_moveElement(self, #self.tab.elements)
    end

    function target:MoveUp()
        local upPosition = table.find(self.tab.elements, self)
        if upPosition then
            self.tab:_moveElement(self, upPosition - 1)
        end
    end

    function target:MoveDown()
        local downPosition = table.find(self.tab.elements, self)
        if downPosition then
            self.tab:_moveElement(self, downPosition + 1)
        end
    end

    return dragState
end

return moveable

end)() end,
    [96] = function()local wax,script,require=ImportGlobals(96)local ImportGlobals return (function(...)
local network = {}
network.__index = network

export type RequestFn = (...any) -> any

function network.getRequestFn(env: any?): RequestFn?
    env = env or getfenv()
    return env.request
        or env.http_request
        or (env.http and env.http.request)
        or (env.syn and env.syn.request)
        or (env.fluxus and env.fluxus.request)
end

return network

end)() end,
    [97] = function()local wax,script,require=ImportGlobals(97)local ImportGlobals return (function(...)
local coreState = require(script.Parent.Parent.core.state)
local motion = require(script.Parent.motion)
local textService = coreState.textService

local odometer = {}
odometer.__index = odometer

local reelCells = 20

local function measureText(font, size, text)
    local params = Instance.new("GetTextBoundsParams")
    params.Text = text
    params.Font = font
    params.Size = size
    params.Width = math.huge
    local ok, bounds = pcall(textService.GetTextBoundsAsync, textService, params)
    return if ok then bounds else Vector2.new(size * 0.6, size)
end

local metricsCache = {}
local cacheCount = 0
local cacheLimit = 64
local function fontMetrics(metricFont, metricSize)
        local cacheKey = tostring(metricFont.Family)
        .. "|"
        .. tostring(metricFont.Weight)
        .. "|"
        .. tostring(metricFont.Style)
        .. "|"
        .. tostring(metricSize)
    local entry = metricsCache[cacheKey]
    if entry then
        return entry
    end

    local advances, widest = {}, 0
    for measuredDigit = 0, 9 do
        local glyphWidth = math.ceil(measureText(metricFont, metricSize, tostring(measuredDigit)).X)
        advances[measuredDigit] = glyphWidth
        if glyphWidth > widest then
            widest = glyphWidth
        end
    end
    entry = { advance = advances, maxWidth = widest }
    if cacheCount >= cacheLimit then
        metricsCache = {}
        cacheCount = 0
    end
    metricsCache[cacheKey] = entry
    cacheCount += 1
    return entry
end

function odometer.new(window, container, options)
    options = options or {}
    local self = setmetatable({
        window = window,
        container = container,
        textSize = options.textSize or 16,
        transparency = options.transparency or 1,         duration = options.duration or 0.55,
        slots = {},         length = 0,     }, odometer)

    self.roll = TweenInfo.new(self.duration, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

            self.zIndex = math.max(container.ZIndex + 1, 6)

            self.height = math.ceil(self.textSize)

                local metrics = fontMetrics(window.theme.Font, self.textSize)
    self.advance = metrics.advance
    self.maxWidth = metrics.maxWidth

    window:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        HorizontalAlignment = options.alignment or Enum.HorizontalAlignment.Left,
        SortOrder = Enum.SortOrder.LayoutOrder,
        Padding = UDim.new(0, 0),

        Parent = container,
    })

    return self
end

function odometer:_reel(reelIndex)
    local slot = self.slots[reelIndex]
    if slot.reel then
        return slot.reel
    end

    local cell = self.window:Create("Frame", {
        Name = "Reel",
        Size = UDim2.fromOffset(self.maxWidth, self.height),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ClipsDescendants = true,
        ZIndex = self.zIndex,

        Parent = self.container,
    })

    local strip = self.window:Create("Frame", {
        Size = UDim2.fromOffset(self.maxWidth, self.height),
        AnchorPoint = Vector2.new(0.5, 0),         Position = UDim2.new(0.5, 0, 0, 0),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        ZIndex = self.zIndex,

        Parent = cell,
    })

    -- [[Rows are created on demand (see _rows): a reel only ever shows the
    -- [[contiguous band of rows its current transition walks over, so building
    -- [[all `reelCells` of them upfront left up to 19 clipped, never-visible
    -- [[TextLabels behind every digit of every readout — the single largest
    -- [[instance cost in the library (a Stat built 42 text labels for one
    -- [[value). Positions and glyphs are unchanged, so nothing about the roll
    -- [[looks different.]]
    local reel = {
        cell = cell,
        strip = strip,
        digit = 0,
        target = 0,
        stripTween = nil,
        sizeTween = nil,
        rows = {},
    }
    slot.reel = reel
    return reel
end

-- [[Materialises the rows a transition needs, and only those. `rowIndex` is
-- [[the strip's own row number (0-9 is the digit, 10-19 the repeated digit
-- [[the downward roll travels through), matching _reelRoll's maths.]]
function odometer:_row(reel, rowIndex)
    local label = reel.rows[rowIndex]
    if label then
        return label
    end

    label = self.window:Create("TextLabel", {
        Text = tostring(rowIndex % 10),
        Position = UDim2.fromOffset(0, rowIndex * self.height),
        Size = UDim2.fromOffset(self.maxWidth, self.height),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = self.textSize,
        TextXAlignment = Enum.TextXAlignment.Center,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextTransparency = self.transparency,
        ZIndex = self.zIndex,

        Parent = reel.strip,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    reel.rows[rowIndex] = label
    return label
end

function odometer:_rows(reel, lowRow, highRow)
    lowRow = math.max(0, lowRow)
    highRow = math.min(reelCells - 1, highRow)
    for rowIndex = lowRow, highRow do
        self:_row(reel, rowIndex)
    end
end

function odometer:_static(staticIndex)
    local staticSlot = self.slots[staticIndex]
    if staticSlot.static then
        return staticSlot.static
    end

    local staticLabel = self.window:Create("TextLabel", {
        Name = "Static",
        Size = UDim2.fromOffset(0, self.height),
        AutomaticSize = Enum.AutomaticSize.X,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        TextSize = self.textSize,
        TextXAlignment = Enum.TextXAlignment.Center,
        TextYAlignment = Enum.TextYAlignment.Center,
        TextTransparency = self.transparency,
        ZIndex = self.zIndex,

        Parent = self.container,
    }, { TextColor3 = "ContentColor", FontFace = "Font" })

    staticSlot.static = staticLabel
    return staticLabel
end

local function cancelTweens(reelState)
    if reelState.stripTween then
        reelState.stripTween:Cancel()
        reelState.stripTween = nil
    end
    if reelState.sizeTween then
        reelState.sizeTween:Cancel()
        reelState.sizeTween = nil
    end
end

function odometer:_reelSnap(snapReel, snapDigit)
    cancelTweens(snapReel)
    self:_rows(snapReel, snapDigit, snapDigit)
    snapReel.cell.Size = UDim2.fromOffset(self.advance[snapDigit], self.height)
    snapReel.strip.Position = UDim2.new(0.5, 0, 0, -snapDigit * self.height)
    snapReel.digit = snapDigit
    snapReel.target = snapDigit
end

function odometer:_reelRoll(rollReel, rollDigit, up)
        if rollReel.stripTween or rollReel.sizeTween then
        self:_reelSnap(rollReel, rollReel.target)
    end

    local currentDigit = rollReel.digit
    if currentDigit == rollDigit then
        return
    end

    local fromRow, toRow
    if up then
        fromRow = currentDigit
        toRow = currentDigit + (rollDigit - currentDigit) % 10     else
        fromRow = currentDigit + 10         toRow = fromRow - (currentDigit - rollDigit) % 10
    end

    self:_rows(rollReel, math.min(fromRow, toRow), math.max(fromRow, toRow))
    rollReel.strip.Position = UDim2.new(0.5, 0, 0, -fromRow * self.height)
    rollReel.target = rollDigit

        -- [[`motion.spec` rescales the reel's TweenInfo with the active speed
    -- profile (cached per info), so the digit roll answers the same
    -- "Animation speed" setting as every other surface.]]
    local roll = motion.spec(self.roll)
    local stripTween = coreState.tweenService:Create(rollReel.strip, roll, {
        Position = UDim2.new(0.5, 0, 0, -toRow * self.height),
    })
    local sizeTween = coreState.tweenService:Create(rollReel.cell, roll, {
        Size = UDim2.fromOffset(self.advance[rollDigit], self.height),
    })
    rollReel.stripTween = stripTween
    rollReel.sizeTween = sizeTween
    stripTween.Completed:Connect(function(playbackState)
        if playbackState == Enum.PlaybackState.Completed and rollReel.stripTween == stripTween then
            self:_reelSnap(rollReel, rollDigit)
        end
    end)
    stripTween:Play()
    sizeTween:Play()
    rollReel.digit = rollDigit
end

function odometer:_putDigit(digitIndex, digitValue, up, animate)
    local digitReel = self:_reel(digitIndex)
    local wasHidden = not digitReel.cell.Visible
    digitReel.cell.Visible = true
    digitReel.cell.LayoutOrder = -digitIndex
    if self.slots[digitIndex].static then
        self.slots[digitIndex].static.Visible = false
    end
    if animate and not wasHidden then
        self:_reelRoll(digitReel, digitValue, up)
    else
        self:_reelSnap(digitReel, digitValue)
    end
end

function odometer:_putStatic(staticPutIndex, staticChar)
    local staticTarget = self:_static(staticPutIndex)
    if not staticTarget.Visible then
                staticTarget.TextTransparency = self.transparency
    end
    staticTarget.Text = staticChar
    staticTarget.Visible = true
    staticTarget.LayoutOrder = -staticPutIndex
    if self.slots[staticPutIndex].reel then
        self.slots[staticPutIndex].reel.cell.Visible = false
    end
end

function odometer:_hide(hideIndex)
    local hideSlot = self.slots[hideIndex]
    if not hideSlot then
        return
    end
    if hideSlot.reel then
        hideSlot.reel.cell.Visible = false
    end
    if hideSlot.static then
        hideSlot.static.Visible = false
    end
end

function odometer:_render(renderValue, renderAnimate, up)
    renderValue = tostring(renderValue)

        if renderValue == self._lastText then
        return
    end
    self._lastText = renderValue

            local chars = {}
    for _, codepoint in utf8.codes(renderValue) do
        table.insert(chars, utf8.char(codepoint))
    end

    local charCount = #chars
    for i = 0, math.max(charCount, self.length) - 1 do
        self.slots[i] = self.slots[i] or {}
        if i < charCount then
            local glyphChar = chars[charCount - i]             if glyphChar:match("%d") then
                self:_putDigit(i, tonumber(glyphChar), up, renderAnimate)
            else
                self:_putStatic(i, glyphChar)
            end
        else
            self:_hide(i)
        end
    end
    self.length = charCount
end

function odometer:to(targetValue, up)
    self:_render(targetValue, true, up)
end

function odometer:snap(snapValue)
    self:_render(snapValue, false, true)
end

function odometer:reveal(targetTransparency, revealTweenInfo, revealOverride)
    self.transparency = targetTransparency
    for _, revealSlot in self.slots do
        if revealSlot.static and revealSlot.static.Visible then
            self.window:_reveal(revealSlot.static, { TextTransparency = targetTransparency }, revealTweenInfo, revealOverride)
        end
        if revealSlot.reel then
            for _, revealLabel in revealSlot.reel.strip:GetChildren() do
                if revealLabel:IsA("TextLabel") then
                    self.window:_reveal(revealLabel, { TextTransparency = targetTransparency }, revealTweenInfo, revealOverride)
                end
            end
        end
    end
end

return odometer

end)() end,
    [98] = function()local wax,script,require=ImportGlobals(98)local ImportGlobals return (function(...)
type OrderedElement = {
    main: GuiObject,
}

local function ordering(element: OrderedElement, order: number)
    element.main.LayoutOrder = order
end

return ordering

end)() end,
    [99] = function()local wax,script,require=ImportGlobals(99)local ImportGlobals return (function(...)
local path = {}

function path.join(base: string, suffix: string?): string
    if not suffix or suffix == "" then
        return base
    end

    return base .. "/" .. suffix
end

local function stripDotDot(input: string): string
    local previous
    repeat
        previous = input
        input = input:gsub("%.%.", "")
    until input == previous
    return input
end

function path.sanitizeFolder(folderValue: unknown): string
    local cleanedFolder = tostring(folderValue):gsub("\\", "/"):gsub('[:<>"|?*%c]', "")
    cleanedFolder = stripDotDot(cleanedFolder):gsub("/+", "/")
    return (cleanedFolder:gsub("^/+", ""))
end

function path.sanitizeFile(fileValue: unknown): string
    local cleanedFile = tostring(fileValue):gsub("[/\\]", ""):gsub('[:<>"|?*%c]', "")
    return stripDotDot(cleanedFile)
end

function path.basename(pathValue: unknown): string
    return tostring(pathValue):match("[^/\\]+$") or tostring(pathValue)
end

function path.stripExtension(nameValue: unknown, extension: string?): string
    local nameText = tostring(nameValue)
    if extension and extension ~= "" then
        if nameText:sub(-#extension):lower() == extension:lower() then
            return nameText:sub(1, -#extension - 1)
        end
        return nameText
    end

    local matchedName = nameText:match("^(.+)%.%w+$")
    return matchedName or nameText
end

return path

end)() end,
    [100] = function()local wax,script,require=ImportGlobals(100)local ImportGlobals return (function(...)
local persistence = {}

local configPersistence = require(script.Parent.persistenceConfig)
local settingsPersistence = require(script.Parent.persistenceSettings)

persistence.getPath = configPersistence.getPath
persistence.save = configPersistence.save
persistence.load = configPersistence.load
persistence.applyTo = configPersistence.applyTo
persistence.list = configPersistence.list
persistence.delete = configPersistence.delete
persistence.getSettingsPath = settingsPersistence.getSettingsPath
persistence.saveSettings = settingsPersistence.saveSettings
persistence.loadSettings = settingsPersistence.loadSettings

return persistence

end)() end,
    [101] = function()local wax,script,require=ImportGlobals(101)local ImportGlobals return (function(...)
local coreState = require(script.Parent.Parent.core.state)
local fs = require(script.Parent.filesystem)
local logger = require(script.Parent.log)
local pathUtil = require(script.Parent.path)
local paths = require(script.Parent.persistencePaths)
local writer = require(script.Parent.persistenceWrite)

local persistenceConfig = {}

type PersistedControl = {
    value: unknown,
    flag: string?,
    _canBeNil: boolean?,
    _serialize: ((PersistedControl) -> unknown)?,
    _deserialize: ((PersistedControl, unknown) -> ())?,
    Set: (PersistedControl, unknown) -> (),
}

type ConfigWindow = {
    controls: { [string]: PersistedControl },
    configuration: {
        fileName: string?,
        customFolder: string?,
    },
    name: string,
    _loading: boolean?,
    _loadedConfig: { [string]: unknown }?,
    _loadedConfigPath: string?,
    _loadCheckpoint: ((any, number) -> ())?,
}

function persistenceConfig.getPath(window: ConfigWindow, requestedName: unknown?): (string?, string?)
    return paths.getConfigPath(window, requestedName)
end

function persistenceConfig.save(window: ConfigWindow, saveName: unknown?): boolean
    local saveFolder, savePath = persistenceConfig.getPath(window, saveName)
    if not saveFolder or not savePath then
        logger.warn("Astra: configuration name '" .. tostring(saveName) .. "' has no usable characters")
        return false
    end

        if typeof(fs.writefile) ~= "function" then
        return false
    end

    local payload: { [string]: unknown } = {}
    for controlFlag, control in window.controls do
        local ok, serialized = pcall(function()
            local serializer = control._serialize
            if serializer then
                return serializer(control)
            end
            return control.value
        end)
        if ok then
            payload[controlFlag] = serialized
        else
            logger.warn("Astra: Failed to serialize flag '" .. tostring(controlFlag) .. "' - " .. tostring(serialized))
        end
    end

                local loadedConfig = window._loadedConfig
    if loadedConfig and window._loadedConfigPath == savePath then
        for preservedKey, preservedValue in loadedConfig do
            if window.controls[preservedKey] == nil then
                payload[preservedKey] = preservedValue
            end
        end
    end

    local encodeOk, encoded = pcall(coreState.httpService.JSONEncode, coreState.httpService, payload)
    if not encodeOk then
        logger.warn("Astra: Failed to encode configuration - " .. tostring(encoded))
        return false
    end

    local ok, writeErr = pcall(function()
        writer.write(saveFolder, savePath, encoded)
    end)

    if not ok then
        logger.warn("Astra: Failed to save configuration - " .. tostring(writeErr))
        return false
    end

                if window._loadedConfigPath == nil or window._loadedConfigPath == savePath then
        window._loadedConfig = payload
        window._loadedConfigPath = savePath
    end

    return true
end

local function readFile(sourcePath: string): ({ [string]: unknown }?, string?)
    if not fs.isfile(sourcePath) then
        return nil, nil
    end

    local readOk, raw = pcall(fs.readfile, sourcePath)
    if not readOk or type(raw) ~= "string" then
        logger.warn("Astra: Failed to read configuration file")
        return nil, nil
    end

    local decodeOk, decoded = pcall(coreState.httpService.JSONDecode, coreState.httpService, raw)

        if not decodeOk or type(decoded) ~= "table" then
        return nil, raw
    end

    return decoded :: { [string]: unknown }, raw
end

local function backupPathFor(targetPath: string): string
        local stem = pathUtil.stripExtension(targetPath, ".rfld")
    local candidatePath = stem .. " (Incorrect Format).rfld"
    local counter = 2
    while counter <= 100 and fs.isfile(candidatePath) do
        candidatePath = stem .. " (Incorrect Format " .. counter .. ").rfld"
        counter += 1
    end
    return candidatePath
end

function persistenceConfig.load(window: ConfigWindow, loadName: unknown?): boolean
    local loadFolder, loadPath = persistenceConfig.getPath(window, loadName)
    if not loadFolder or not loadPath then
        logger.warn("Astra: configuration name '" .. tostring(loadName) .. "' has no usable characters")
        return false
    end

        if typeof(fs.isfile) ~= "function" then
        return false
    end

    local restored, rawFallback = readFile(loadPath)

            if not restored and fs.isfile(loadPath) then
        local tempDecoded, tempRaw = readFile(writer.tempPathFor(loadPath))
        if tempDecoded and tempRaw then
            restored = tempDecoded
                        pcall(writer.write, loadFolder, loadPath, tempRaw)
        end
    end

    if not restored then
        if rawFallback then
            logger.warn("Astra: Configuration file has an invalid format, backing up and resetting")
            local backupPath = backupPathFor(loadPath)
            pcall(function()
                fs.ensureDir(loadFolder)
                fs.writefile(backupPath, rawFallback)
                fs.delfile(loadPath)
            end)
        end
        return false
    end

    local values = restored :: { [string]: unknown }
    local wasLoading = window._loading
    window._loading = true
    local applyOk, applyErr = pcall(function()
        local appliedCount = 0
        for loadedFlag, loadedControl in window.controls do
            local storedValue = values[loadedFlag]
            if storedValue ~= nil or loadedControl._canBeNil then
                persistenceConfig.applyTo(loadedControl, storedValue)
                appliedCount += 1
                if window._loadCheckpoint then
                    window:_loadCheckpoint(appliedCount)
                end
            end
        end
    end)
    window._loading = wasLoading
    if not applyOk then
        logger.warn("Astra: Failed to apply configuration - " .. tostring(applyErr))
    end

                window._loadedConfig = values
    window._loadedConfigPath = loadPath

    return true
end

function persistenceConfig.applyTo(target: PersistedControl, controlValue: unknown)
    if controlValue == nil and not target._canBeNil then
        return
    end
    local ok, setErr = pcall(function()
        local deserializer = target._deserialize
        if deserializer then
            deserializer(target, controlValue)
        else
            target:Set(controlValue)
        end
    end)
    if not ok then
        logger.warn("Astra: Failed to restore flag '" .. tostring(target.flag) .. "' - " .. tostring(setErr))
    end
end

function persistenceConfig.list(window: ConfigWindow): { string }
    local listFolder = persistenceConfig.getPath(window)

    local names: { string } = {}
        if not listFolder then
        return names
    end
    local ok, files = pcall(fs.listfiles, listFolder)
    if not ok or type(files) ~= "table" then
        return names
    end

    for _, file in files do
        local baseName = pathUtil.basename(file)
        if baseName:sub(-5):lower() == ".rfld" then
            local configName = pathUtil.stripExtension(baseName, ".rfld")
            if configName and configName ~= "" and not configName:find(" %(Incorrect Format[^%)]*%)$") then
                table.insert(names, configName)
            end
        end
    end

    table.sort(names)
    return names
end

function persistenceConfig.delete(window: ConfigWindow, deleteName: unknown): boolean
    if type(deleteName) ~= "string" or deleteName == "" then
        return false
    end

    local _, deletePath = persistenceConfig.getPath(window, deleteName)
    if not deletePath then
        return false
    end
    if typeof(fs.isfile) ~= "function" or not fs.isfile(deletePath) then
        return false
    end

        pcall(fs.delfile, writer.tempPathFor(deletePath))

    return (pcall(fs.delfile, deletePath))
end

return persistenceConfig

end)() end,
    [102] = function()local wax,script,require=ImportGlobals(102)local ImportGlobals return (function(...)
local coreState = require(script.Parent.Parent.core.state)
local pathUtil = require(script.Parent.path)

local persistencePaths = {}

type ConfigWindow = {
    configuration: {
        fileName: string?,
        customFolder: string?,
    },
    name: string,
}

function persistencePaths.getConfigPath(window: ConfigWindow, configName: unknown?): (string?, string?)
    local folder = coreState.fileSystemManager:getPath("config")
    if window.configuration.customFolder then
        folder = pathUtil.join(folder, pathUtil.sanitizeFolder(window.configuration.customFolder))
    end
    if configName ~= nil then
        local sanitizedName = pathUtil.sanitizeFile(configName)
        if sanitizedName == "" then
            return nil, nil
        end
        return folder, pathUtil.join(folder, sanitizedName .. ".rfld")
    end
    local baseName = pathUtil.sanitizeFile(window.configuration.fileName or window.name)
    if baseName == "" then
        baseName = pathUtil.sanitizeFile(window.name)
    end
    if baseName == "" then
        baseName = "Configuration"
    end
    return folder, pathUtil.join(folder, baseName .. ".rfld")
end

function persistencePaths.getSettingsPath(): (string, string)
    local settingsFolder = coreState.fileSystemManager:getPath("settings")
    return settingsFolder, pathUtil.join(settingsFolder, "astra.rfld")
end

return persistencePaths

end)() end,
    [103] = function()local wax,script,require=ImportGlobals(103)local ImportGlobals return (function(...)
local coreState = require(script.Parent.Parent.core.state)
local fs = require(script.Parent.filesystem)
local paths = require(script.Parent.persistencePaths)
local writer = require(script.Parent.persistenceWrite)
local enumHelper = require(script.Parent.enums)

local persistenceSettings = {}

-- [[Settings files written before profile visibility was explicitly kept off
-- [[did not carry a schema marker. Treat those files as legacy once, clear the
-- [[profile visibility pair, and write the marker back so a user can still turn
-- [[the profile on intentionally afterward.]]
local SETTINGS_VERSION = 1

type SettingsWindow = {
    settings: {
        toggleKeybind: EnumItem,
        theme: string,
        mouseOverride: boolean,
        keepOnScreen: boolean,
        dragMinimisedBar: boolean,
        haptics: boolean,
        showProfile: boolean,
        showFullUsername: boolean,
        profileSide: string,
        antiWindowDuplicate: boolean,
        layoutMode: string,
        activeSubTab: number,
        motionSpeed: string,
        autoSaveConfig: boolean,
        autoLoadConfig: boolean,
    },
}

type DecodedSettings = {
    settingsVersion: unknown?,
    toggleKeybind: { [number]: unknown }?,
    theme: unknown?,
    mouseOverride: unknown?,
    keepOnScreen: unknown?,
    dragMinimisedBar: unknown?,
        haptics: unknown?,
        showProfile: unknown?,
        showFullUsername: unknown?,    profileSide: unknown?,
    antiWindowDuplicate: unknown?,
    layoutMode: unknown?,
    activeSubTab: unknown?,
    motionSpeed: unknown?,
    autoSaveConfig: unknown?,
    autoLoadConfig: unknown?,
}

function persistenceSettings.getSettingsPath(): (string, string)
    return paths.getSettingsPath()
end

function persistenceSettings.saveSettings(window: SettingsWindow): boolean
    local folder, filePath = persistenceSettings.getSettingsPath()

    local payload: { [string]: unknown } = {
        settingsVersion = SETTINGS_VERSION,
        toggleKeybind = {
            tostring(window.settings.toggleKeybind.EnumType),
            window.settings.toggleKeybind.Value,
        } :: { unknown },
        theme = window.settings.theme,
        mouseOverride = window.settings.mouseOverride,
        keepOnScreen = window.settings.keepOnScreen,
        dragMinimisedBar = window.settings.dragMinimisedBar,
        haptics = window.settings.haptics,
        showProfile = window.settings.showProfile,
        showFullUsername = window.settings.showFullUsername,
        profileSide = window.settings.profileSide,
        antiWindowDuplicate = window.settings.antiWindowDuplicate,
        layoutMode = window.settings.layoutMode,
        activeSubTab = window.settings.activeSubTab,
        motionSpeed = window.settings.motionSpeed,
        autoSaveConfig = window.settings.autoSaveConfig,
        autoLoadConfig = window.settings.autoLoadConfig,
    }

    local ok, encoded = pcall(coreState.httpService.JSONEncode, coreState.httpService, payload)
    if not ok then
        return false
    end

    local writeOk = pcall(writer.write, folder, filePath, encoded)

    if not writeOk then
        return false
    end

    return true
end

local function readDecoded(path: string): (DecodedSettings?, string?)
    local exists = false
    pcall(function()
        exists = fs.isfile(path)
    end)
    if not exists then
        return nil, nil
    end

    local readOk, raw = pcall(fs.readfile, path)
    if not readOk or type(raw) ~= "string" then
        return nil, nil
    end

    local decodeOk, decoded = pcall(coreState.httpService.JSONDecode, coreState.httpService, raw)
        if not decodeOk or type(decoded) ~= "table" then
        return nil, raw
    end

        return decoded :: DecodedSettings, raw
end

local function readWithFallback(): DecodedSettings?
    local settingsFolder, settingsPath = persistenceSettings.getSettingsPath()
    local tempFolder = readDecoded(settingsPath)
    if tempFolder then
        return tempFolder
    end

    local tempPath, tempRaw = readDecoded(writer.tempPathFor(settingsPath))
    if tempPath and tempRaw then
        pcall(writer.write, settingsFolder, settingsPath, tempRaw)
        return tempPath
    end

    return nil
end

function persistenceSettings.loadSettings(window: SettingsWindow): (boolean, boolean)
    local decodedSettings = readWithFallback()

    if not decodedSettings then
        return false, false
    end

    local storedVersion = tonumber(decodedSettings.settingsVersion) or 0
    local migrated = storedVersion < SETTINGS_VERSION
    if migrated then
        -- [[A legacy profile preference may have been saved as true. Clear it
        -- [[only during the one-time migration; after this file is rewritten,
        -- [[users can enable the profile normally and keep that choice.]]
        decodedSettings.showProfile = false
        decodedSettings.showFullUsername = false
    end

    for _, key in { "autoSaveConfig", "autoLoadConfig" } do
        if type(decodedSettings[key]) == "boolean" then
            window.settings[key] = decodedSettings[key]
        end
    end

    if decodedSettings.toggleKeybind then
        pcall(function()
            local enumTypeName = tostring(decodedSettings.toggleKeybind[1]):gsub("^Enum%.", "")
            local enumType = (Enum :: any)[enumTypeName]
            if enumType then
                local keybindItem = enumHelper.itemFromValue(enumType, decodedSettings.toggleKeybind[2])
                if keybindItem then
                    window.settings.toggleKeybind = keybindItem
                end
            end
        end)
    end

    if type(decodedSettings.theme) == "string" then
        local theme = string.lower(decodedSettings.theme)
        if theme == "default" or theme == "amethyst" or theme == "cobalt"
            or theme == "crimson" or theme == "ember" or theme == "emerald"
            or theme == "frost" or theme == "gold" or theme == "onyx"
            or theme == "rose"
        then
            window.settings.theme = theme
        end
    end

    if type(decodedSettings.mouseOverride) == "boolean" then
        window.settings.mouseOverride = decodedSettings.mouseOverride
    end

    if type(decodedSettings.keepOnScreen) == "boolean" then
        window.settings.keepOnScreen = decodedSettings.keepOnScreen
    end

    if type(decodedSettings.dragMinimisedBar) == "boolean" then
        window.settings.dragMinimisedBar = decodedSettings.dragMinimisedBar
    end

    if type(decodedSettings.haptics) == "boolean" then
        window.settings.haptics = decodedSettings.haptics
    end

    if type(decodedSettings.showProfile) == "boolean" then
        window.settings.showProfile = decodedSettings.showProfile
    end
    if type(decodedSettings.showFullUsername) == "boolean" then
        window.settings.showFullUsername = decodedSettings.showFullUsername
    end
    if decodedSettings.profileSide == "left" or decodedSettings.profileSide == "right" then
        window.settings.profileSide = decodedSettings.profileSide
    end
    if type(decodedSettings.antiWindowDuplicate) == "boolean" then
        window.settings.antiWindowDuplicate = decodedSettings.antiWindowDuplicate
    end
    if decodedSettings.layoutMode == "top"
        or decodedSettings.layoutMode == "sidebar"
        or decodedSettings.layoutMode == "collapsedSidebar"
    then
        window.settings.layoutMode = decodedSettings.layoutMode
    end

    if type(decodedSettings.activeSubTab) == "number" and decodedSettings.activeSubTab >= 1 then
        window.settings.activeSubTab = math.floor(decodedSettings.activeSubTab)
    end

    -- [[Motion profiles are validated by the motion service, the same way the
    -- live setting is, so a hand-edited settings file cannot put the library
    -- on a profile that does not exist.]]
    if type(decodedSettings.motionSpeed) == "string" then
        local motion = require(script.Parent.motion)
        if motion.profiles[decodedSettings.motionSpeed] then
            window.settings.motionSpeed = decodedSettings.motionSpeed
        end
    end

    return true, migrated
end

return persistenceSettings

end)() end,
    [104] = function()local wax,script,require=ImportGlobals(104)local ImportGlobals return (function(...)
local fs = require(script.Parent.filesystem)

local persistenceWrite = {}

local tempSuffix = ".saving"

function persistenceWrite.tempPathFor(path: string): string
    return path .. tempSuffix
end

function persistenceWrite.write(folder: string, filePath: string, contents: string)
    local tempPath = persistenceWrite.tempPathFor(filePath)

    fs.ensureDir(folder)
    fs.writefile(tempPath, contents)

    if fs.readfile(tempPath) ~= contents then
        error("parked copy did not write cleanly")
    end

    fs.writefile(filePath, contents)
    pcall(fs.delfile, tempPath)
end

return persistenceWrite

end)() end,
    [105] = function()local wax,script,require=ImportGlobals(105)local ImportGlobals return (function(...)
local services = {}

function services.getService(serviceName)
    local service = game:GetService(serviceName)
    return if cloneref then cloneref(service) else service
end

return services

end)() end,
    [106] = function()local wax,script,require=ImportGlobals(106)local ImportGlobals return (function(...)
local layoutMetrics = require(script.Parent.layouts)
local coreState = require(script.Parent.Parent.core.state)

local breakpoints = {}

export type Profile = {
	defaultSize: Vector2,
	minSize: Vector2,
	maxSize: Vector2?,
	mobileSize: Vector2,
	maxOccupancyX: number,
	maxOccupancyY: number?,
	marginFloorX: number,
	marginFloorY: number,
	topbarClearance: number?,
	maxAspectRatio: number,
	minAspectRatio: number?,
	widthCompensation: number?,
	chromeHeight: number,
}

local minTrackedViewport = 200

-- [[Astra's phone tier: viewports below this width are phone-sized. The same]]
-- [[breakpoint gates the fixed mobile profile in `isMobileViewport`.]]
local mobileBreakpoint = 700

-- [[Fixed mobile footprint (per layout mode). Mobile no longer recomputes its]]
-- [[window from live viewport occupancy: it opens at one stable size, so]]
-- [[rotation / on-screen keyboard / inset changes never resize the window.]]
-- [[The values are the existing phone-tier result (maxOccupancyX width,]]
-- [[availableHeight / aspect-limited height) evaluated once at a 360x390]]
-- [[reference: 360 is the narrowest mainstream phone width, 390 the short side]]
-- [[of the reference phone scaleForViewport documents. Measuring against the]]
-- [[short side keeps the fixed window inside a phone held in portrait AND in]]
-- [[landscape (no clipping, no recalculation):]]
-- [[   top     -> 360 * 0.88 = 316 wide, 390 - 36 * 2 = 318 tall]]
-- [[   sidebar -> 360 * 0.92 = 331 wide, min(390 * 0.84, 331 / 1.18) = 280 tall]]
-- [[Desktop/PC keeps the responsive profiles below: 600x420 default target,]]
-- [[responsive shrink on smaller screens, maxSize caps so large screens stay]]
-- [[responsive without growing into an oversized window.]]
local profiles = {
	top = {
		defaultSize = Vector2.new(600, 420),
		minSize = Vector2.new(500, 330),
		maxSize = Vector2.new(720, 480),
		mobileSize = Vector2.new(560, 380),
		maxOccupancyX = 0.88,
		topbarClearance = 36,
		marginFloorX = 20,
		marginFloorY = 20,
		maxAspectRatio = 2.3,
		widthCompensation = 80,
		chromeHeight = layoutMetrics.top.chromeHeight,
	} :: Profile,
	sidebar = {
		defaultSize = Vector2.new(600, 420),
		minSize = Vector2.new(540, 360),
		maxSize = Vector2.new(760, 500),
		mobileSize = Vector2.new(560, 380),
		maxOccupancyX = 0.92,
		maxOccupancyY = 0.84,
		marginFloorX = 20,
		marginFloorY = 20,
		maxAspectRatio = 2.6,
		minAspectRatio = 1.18,
		chromeHeight = layoutMetrics.sidebar.chromeHeight,
	} :: Profile,
}

function breakpoints.profile(mode: layouts.Mode?): Profile
	if mode == "sidebar" then
		return profiles.sidebar
	end
	return profiles.top
end

function breakpoints.pageHeight(totalHeight: number?, modeForHeight: layouts.Mode?): number
	local sizingProfile = breakpoints.profile(modeForHeight)
	local height = if totalHeight and totalHeight > 0 then totalHeight else sizingProfile.minSize.Y
	return math.max(height - sizingProfile.chromeHeight, 0)
end

local function availableHeight(heightProfile: Profile, viewportHeight: number): number
	local available = viewportHeight - heightProfile.marginFloorY
	if heightProfile.topbarClearance then
		available = math.min(available, viewportHeight - heightProfile.topbarClearance * 2)
	end
	if heightProfile.maxOccupancyY then
		available = math.min(available, viewportHeight * heightProfile.maxOccupancyY)
	end
	return available
end

local function shrinkFactor(current: number, upper: number, lower: number): number
	return math.clamp((upper - current) / (upper - lower), 0, 1)
end

local function fitTop(topProfile: Profile, maxWidth: number, requestedHeight: number): UDim2
	local clampedHeight = math.floor(math.min(math.clamp(requestedHeight, topProfile.minSize.Y, topProfile.defaultSize.Y), requestedHeight))
	local blend = shrinkFactor(clampedHeight, topProfile.defaultSize.Y, topProfile.minSize.Y)
	local width = math.max(topProfile.defaultSize.X + (topProfile.widthCompensation :: number) * blend, topProfile.minSize.X)
	return UDim2.fromOffset(math.floor(math.min(width, maxWidth, clampedHeight * topProfile.maxAspectRatio)), clampedHeight)
end

local function fitSidebar(sidebarProfile: Profile, requestedWidth: number, requestedSidebarHeight: number): UDim2
    -- [[The Sidebar keeps its historical 540px minimum for user resizing, while]]
    -- [[its default width is the shared 600px window default. Keep clamp bounds]]
    -- [[ordered for the responsive fit calculation without changing the]]
    -- [[declared minimum.]]
    local minWidthBound = math.min(sidebarProfile.minSize.X, sidebarProfile.defaultSize.X)
    local sidebarWidth = math.min(math.clamp(requestedWidth, minWidthBound, sidebarProfile.defaultSize.X), requestedWidth)
	local sidebarHeight = math.min(math.clamp(requestedSidebarHeight, sidebarProfile.minSize.Y, sidebarProfile.defaultSize.Y), requestedSidebarHeight)
	sidebarHeight = math.floor(math.min(sidebarHeight, sidebarWidth / (sidebarProfile.minAspectRatio :: number)))
	sidebarWidth = math.floor(math.min(sidebarWidth, sidebarHeight * sidebarProfile.maxAspectRatio))
	return UDim2.fromOffset(sidebarWidth, sidebarHeight)
end

-- [[Tiered occupancy for desktop/PC: tablet and desktop share the same]]
-- [[perceived scale. Desktop uses lower occupancy so 1920px doesn't give a]]
-- [[huge window, while smaller screens still shrink responsively.]]
-- [[Phones (touch-only, sub-breakpoint) are diverted to the fixed mobile size]]
-- [[in fit(); this phone branch remains for narrow desktop/Studio viewports.]]
local function getTieredOccupancy(viewport: Vector2, modeOrProfile: Profile): (number, number)
	local w = viewport.X
	if w < mobileBreakpoint then
		-- [[narrow viewport: near full width]]
		return modeOrProfile.maxOccupancyX, availableHeight(modeOrProfile, viewport.Y)
	elseif w < 1200 then
		-- [[tablet / small laptop]]
		return math.min(modeOrProfile.maxOccupancyX, 0.72), math.min(availableHeight(modeOrProfile, viewport.Y), viewport.Y * 0.82)
	else
		-- [[desktop: default target, capped by maxSize, never full-screen]]
		return 0.52, math.min(availableHeight(modeOrProfile, viewport.Y), viewport.Y * 0.72)
	end
end

-- [[Mobile detection: touch-only devices (phones, no hardware keyboard) whose]]
-- [[short side is phone-sized. Uses the min of both axes so a phone stays on]]
-- [[the fixed mobile size in portrait and landscape. Tablets and desktops -]]
-- [[including narrow desktop/Studio viewports - keep the responsive fit.]]
function breakpoints.isMobileViewport(mobileViewport: Vector2?): boolean
	if not mobileViewport then
		return false
	end
	if mobileViewport.X < minTrackedViewport or mobileViewport.Y < minTrackedViewport then
		return false
	end
	if math.min(mobileViewport.X, mobileViewport.Y) >= mobileBreakpoint then
		return false
	end
	local inputService = coreState.userInputService
	if not inputService then
		return false
	end
	return inputService.TouchEnabled == true and inputService.KeyboardEnabled == false
end

local function fitViewport(fitProfile: Profile, fitTarget: Vector2): UDim2
	-- [[tiered target based on screen size]]
	local occX, availY = getTieredOccupancy(fitTarget, fitProfile)
	local targetWidth = math.min(fitTarget.X * occX, fitTarget.X - fitProfile.marginFloorX)
	-- [[clamp to maxSize so desktop doesn't explode on ultrawide]]
	if fitProfile.maxSize then
		targetWidth = math.min(targetWidth, fitProfile.maxSize.X)
		availY = math.min(availY, fitProfile.maxSize.Y)
	end
	local targetHeight = availY
	if fitProfile.minAspectRatio then
		return fitSidebar(fitProfile, targetWidth, targetHeight)
	end
	return fitTop(fitProfile, targetWidth, targetHeight)
end

function breakpoints.fit(viewport: Vector2?, modeOrProfile: layouts.Mode?): UDim2
	-- [[Fix: respect mode (old code always used sidebar)]]
	local fitProfile = if modeOrProfile == "top" then profiles.top else profiles.sidebar
	if not viewport or viewport.X < minTrackedViewport or viewport.Y < minTrackedViewport then
		return UDim2.fromOffset(fitProfile.defaultSize.X, fitProfile.defaultSize.Y)
	end
	-- [[Mobile: fixed size/profile - no occupancy math, so viewport changes]]
	-- [[never resize the window once it is on the mobile path.]]
	if breakpoints.isMobileViewport(viewport) then
		return UDim2.fromOffset(fitProfile.mobileSize.X, fitProfile.mobileSize.Y)
	end
	-- [[Desktop/PC: responsive viewport-based sizing (min/max protected).]]
	return fitViewport(fitProfile, viewport)
end

-- [[UIScale helper: desktop remains responsive, while the fixed mobile window]]
-- [[keeps its requested dimensions instead of being reduced by device scale.]]
function breakpoints.scaleForViewport(scaleViewport: Vector2?): number
	if not scaleViewport or scaleViewport.X < minTrackedViewport then return 1 end
	if math.min(scaleViewport.X, scaleViewport.Y) < mobileBreakpoint then return 1 end
	-- [[390 -> 0.92, 768 -> 0.98, 1280 -> 1.04, 1920 -> 1.12, 2560 -> 1.18 (clamped)]]
	local s = 0.88 + (scaleViewport.X / 2200) * 0.32
	-- [[y also matters on portrait phones: don't let tall narrow screens over-scale]]
	if scaleViewport.Y < 500 then s = math.min(s, 0.95) end
	return math.clamp(s, 0.88, 1.18)
end

return breakpoints

end)() end,
}


local LineOffsets = {
    [2] = 12,
    [3] = 243,
    [5] = 613,
    [6] = 648,
    [7] = 693,
    [8] = 599,
    [9] = 810,
    [10] = 943,
    [11] = 1182,
    [12] = 1528,
    [13] = 1880,
    [14] = 2168,
    [15] = 2889,
    [16] = 5316,
    [17] = 5663,
    [18] = 6541,
    [19] = 6602,
    [20] = 7017,
    [22] = 10283,
    [23] = 10369,
    [24] = 10680,
    [25] = 11101,
    [26] = 11503,
    [27] = 11673,
    [28] = 11818,
    [29] = 13336,
    [30] = 13555,
    [31] = 13829,
    [32] = 13915,
    [33] = 14502,
    [34] = 15351,
    [35] = 15731,
    [36] = 15861,
    [37] = 16013,
    [39] = 16507,
    [40] = 16530,
    [41] = 16547,
    [42] = 16579,
    [43] = 16701,
    [44] = 16725,
    [45] = 16774,
    [46] = 16482,
    [47] = 16898,
    [48] = 17201,
    [49] = 17232,
    [50] = 16887,
    [51] = 17275,
    [52] = 17295,
    [53] = 17327,
    [54] = 17257,
    [55] = 17434,
    [56] = 17493,
    [57] = 17553,
    [59] = 17697,
    [60] = 17862,
    [61] = 17682,
    [62] = 18440,
    [63] = 18484,
    [64] = 18564,
    [65] = 18757,
    [66] = 18886,
    [67] = 18980,
    [68] = 19147,
    [69] = 19485,
    [70] = 17938,
    [71] = 20119,
    [72] = 20185,
    [73] = 20251,
    [74] = 20326,
    [75] = 20432,
    [76] = 20498,
    [77] = 20573,
    [78] = 20648,
    [79] = 20723,
    [80] = 20798,
    [81] = 20013,
    [82] = 20864,
    [83] = 20962,
    [84] = 21140,
    [85] = 21188,
    [86] = 21218,
    [87] = 21469,
    [88] = 21518,
    [89] = 21971,
    [90] = 21989,
    [91] = 22158,
    [92] = 22257,
    [93] = 22279,
    [94] = 22333,
    [95] = 22683,
    [96] = 22719,
    [97] = 22737,
    [98] = 23091,
    [99] = 23103,
    [100] = 23154,
    [101] = 23173,
    [102] = 23424,
    [103] = 23468,
    [104] = 23704,
    [105] = 23732,
    [106] = 23743,
}

-- [[Misc AOT variable imports]]
local WaxVersion = "0.4.1"
local EnvName = "WaxRuntime"

local ObjectTree = {
    {
        1,
        1,
        {
            "Astra",
        },
        {
            {
                2,
                2,
                {
                    "MainModule",
                },
            },
            {
                3,
                2,
                {
                    "Types",
                },
            },
            {
                4,
                1,
                {
                    "assets",
                },
                {},
            },
            {
                8,
                2,
                {
                    "core",
                },
                {
                    {
                        5,
                        2,
                        {
                            "loader",
                        },
                    },
                    {
                        6,
                        2,
                        {
                            "registry",
                        },
                    },
                    {
                        7,
                        2,
                        {
                            "state",
                        },
                    },
                },
            },
            {
                21,
                1,
                {
                    "components",
                },
                {
                    {
                        9,
                        2,
                        {
                            "action",
                        },
                    },
                    {
                        10,
                        2,
                        {
                            "chrome",
                        },
                    },
                    {
                        11,
                        2,
                        {
                            "drag",
                        },
                    },
                    {
                        12,
                        2,
                        {
                            "notification",
                        },
                    },
                    {
                        13,
                        2,
                        {
                            "overlayQueue",
                        },
                    },
                    {
                        14,
                        2,
                        {
                            "popup",
                        },
                    },
                    {
                        15,
                        2,
                        {
                            "profilePanel",
                        },
                    },
                    {
                        16,
                        2,
                        {
                            "search",
                        },
                    },
                    {
                        17,
                        2,
                        {
                            "settings",
                        },
                    },
                    {
                        18,
                        2,
                        {
                            "sidebar",
                        },
                    },
                    {
                        19,
                        2,
                        {
                            "tabSelector",
                        },
                    },
                    {
                        20,
                        2,
                        {
                            "window",
                        },
                    },
                },
            },
            {
                38,
                1,
                {
                    "elements",
                },
                {
                    {
                        22,
                        2,
                        {
                            "baseCard",
                        },
                    },
                    {
                        23,
                        2,
                        {
                            "button",
                        },
                    },
                    {
                        24,
                        2,
                        {
                            "changelog",
                        },
                    },
                    {
                        25,
                        2,
                        {
                            "collapsibleGroup",
                        },
                    },
                    {
                        26,
                        2,
                        {
                            "description",
                        },
                    },
                    {
                        27,
                        2,
                        {
                            "divider",
                        },
                    },
                    {
                        28,
                        2,
                        {
                            "dropdown",
                        },
                    },
                    {
                        29,
                        2,
                        {
                            "group",
                        },
                    },
                    {
                        30,
                        2,
                        {
                            "input",
                        },
                    },
                    {
                        31,
                        2,
                        {
                            "section",
                        },
                    },
                    {
                        32,
                        2,
                        {
                            "slider",
                        },
                    },
                    {
                        33,
                        2,
                        {
                            "stat",
                        },
                    },
                    {
                        34,
                        2,
                        {
                            "tab",
                        },
                    },
                    {
                        35,
                        2,
                        {
                            "tabSection",
                        },
                    },
                    {
                        36,
                        2,
                        {
                            "text",
                        },
                    },
                    {
                        37,
                        2,
                        {
                            "toggle",
                        },
                    },
                },
            },
            {
                46,
                2,
                {
                    "settings",
                },
                {
                    {
                        39,
                        2,
                        {
                            "appearance",
                        },
                    },
                    {
                        40,
                        2,
                        {
                            "behavior",
                        },
                    },
                    {
                        41,
                        2,
                        {
                            "defaults",
                        },
                    },
                    {
                        42,
                        2,
                        {
                            "manager",
                        },
                    },
                    {
                        43,
                        2,
                        {
                            "performance",
                        },
                    },
                    {
                        44,
                        2,
                        {
                            "persistence",
                        },
                    },
                    {
                        45,
                        2,
                        {
                            "registry",
                        },
                    },
                },
            },
            {
                50,
                2,
                {
                    "cache",
                },
                {
                    {
                        47,
                        2,
                        {
                            "imageCache",
                        },
                    },
                    {
                        48,
                        2,
                        {
                            "moduleCache",
                        },
                    },
                    {
                        49,
                        2,
                        {
                            "persistenceCache",
                        },
                    },
                },
            },
            {
                54,
                2,
                {
                    "functions",
                },
                {
                    {
                        51,
                        2,
                        {
                            "colors",
                        },
                    },
                    {
                        52,
                        2,
                        {
                            "flagNames",
                        },
                    },
                    {
                        53,
                        2,
                        {
                            "textMetrics",
                        },
                    },
                },
            },
            {
                58,
                1,
                {
                    "layouts",
                },
                {
                    {
                        55,
                        2,
                        {
                            "Sidebar",
                        },
                    },
                    {
                        56,
                        2,
                        {
                            "SidebarCollapsed",
                        },
                    },
                    {
                        57,
                        2,
                        {
                            "Topbar",
                        },
                    },
                },
            },
            {
                61,
                2,
                {
                    "images",
                },
                {
                    {
                        59,
                        2,
                        {
                            "image",
                        },
                    },
                    {
                        60,
                        2,
                        {
                            "windowIcons",
                        },
                    },
                },
            },
            {
                70,
                2,
                {
                    "icons",
                },
                {
                    {
                        62,
                        2,
                        {
                            "feather",
                        },
                    },
                    {
                        63,
                        2,
                        {
                            "heroicons",
                        },
                    },
                    {
                        64,
                        2,
                        {
                            "lucide",
                        },
                    },
                    {
                        65,
                        2,
                        {
                            "material",
                        },
                    },
                    {
                        66,
                        2,
                        {
                            "packBuilder",
                        },
                    },
                    {
                        67,
                        2,
                        {
                            "phosphor",
                        },
                    },
                    {
                        68,
                        2,
                        {
                            "remix",
                        },
                    },
                    {
                        69,
                        2,
                        {
                            "tabler",
                        },
                    },
                },
            },
            {
                81,
                2,
                {
                    "themes",
                },
                {
                    {
                        71,
                        2,
                        {
                            "amethyst",
                        },
                    },
                    {
                        72,
                        2,
                        {
                            "cobalt",
                        },
                    },
                    {
                        73,
                        2,
                        {
                            "crimson",
                        },
                    },
                    {
                        74,
                        2,
                        {
                            "default",
                        },
                    },
                    {
                        75,
                        2,
                        {
                            "ember",
                        },
                    },
                    {
                        76,
                        2,
                        {
                            "emerald",
                        },
                    },
                    {
                        77,
                        2,
                        {
                            "frost",
                        },
                    },
                    {
                        78,
                        2,
                        {
                            "gold",
                        },
                    },
                    {
                        79,
                        2,
                        {
                            "onyx",
                        },
                    },
                    {
                        80,
                        2,
                        {
                            "rose",
                        },
                    },
                },
            },
            {
                107,
                1,
                {
                    "utilities",
                },
                {
                    {
                        82,
                        2,
                        {
                            "HapticEngine",
                        },
                    },
                    {
                        83,
                        2,
                        {
                            "assetResolver",
                        },
                    },
                    {
                        84,
                        2,
                        {
                            "constants",
                        },
                    },
                    {
                        85,
                        2,
                        {
                            "enums",
                        },
                    },
                    {
                        86,
                        2,
                        {
                            "filesystem",
                        },
                    },
                    {
                        87,
                        2,
                        {
                            "filesystemManager",
                        },
                    },
                    {
                        88,
                        2,
                        {
                            "fontManager",
                        },
                    },
                    {
                        89,
                        2,
                        {
                            "functions",
                        },
                    },
                    {
                        90,
                        2,
                        {
                            "layouts",
                        },
                    },
                    {
                        91,
                        2,
                        {
                            "locale",
                        },
                    },
                    {
                        92,
                        2,
                        {
                            "lockable",
                        },
                    },
                    {
                        93,
                        2,
                        {
                            "log",
                        },
                    },
                    {
                        94,
                        2,
                        {
                            "motion",
                        },
                    },
                    {
                        95,
                        2,
                        {
                            "moveable",
                        },
                    },
                    {
                        96,
                        2,
                        {
                            "network",
                        },
                    },
                    {
                        97,
                        2,
                        {
                            "odometer",
                        },
                    },
                    {
                        98,
                        2,
                        {
                            "ordering",
                        },
                    },
                    {
                        99,
                        2,
                        {
                            "path",
                        },
                    },
                    {
                        100,
                        2,
                        {
                            "persistence",
                        },
                    },
                    {
                        101,
                        2,
                        {
                            "persistenceConfig",
                        },
                    },
                    {
                        102,
                        2,
                        {
                            "persistencePaths",
                        },
                    },
                    {
                        103,
                        2,
                        {
                            "persistenceSettings",
                        },
                    },
                    {
                        104,
                        2,
                        {
                            "persistenceWrite",
                        },
                    },
                    {
                        105,
                        2,
                        {
                            "services",
                        },
                    },
                    {
                        106,
                        2,
                        {
                            "windowSizing",
                        },
                    },
                },
            },
        },
    },
}


local task_defer = task and task.defer

    -- [[If we're not running on the Roblox engine, we won't have a `task` global]]
local Defer = task_defer or function(f, ...)
    coroutine_wrap(f)(...)
end

    -- [[ClassName "IDs"]]
local ClassNameIdBindings = {
    [1] = "Folder",
    [2] = "ModuleScript",
    [3] = "Script",
    [4] = "LocalScript",
    [5] = "StringValue",
}

local RefBindings = {} -- [[ [RefId] = RealObject ]]

local ScriptClosures = {}
local ScriptClosureRefIds = {} -- [[ [ScriptClosure] = RefId ]]
local StoredModuleValues = {}
local ScriptsToRun = {}

    -- [[wax.shared __index/__newindex]]
local SharedEnvironment = {}

    -- [[We're creating 'fake' instance refs soley for traversal of the DOM for require() compatibility]]

local RefChildren = {}

local unpack = table and table.unpack or unpack
local table_insert = table.insert
local table_remove = table.remove
local string_match = string.match
local next = next
local type = type
local tostring = tostring
local tonumber = tonumber
local error = error
local pcall = pcall
local setmetatable = setmetatable

local InstanceMethods = {
    GetFullName = { {}, function(self)
        local Path = self.Name
        local ObjectPointer = self.Parent

        while ObjectPointer do
            Path = ObjectPointer.Name .. "." .. Path

            ObjectPointer = ObjectPointer.Parent
        end

        return Path
    end},

    GetChildren = { {}, function(self)
        local ReturnArray = {}

        for Child in next, RefChildren[self] do
            table_insert(ReturnArray, Child)
        end

        return ReturnArray
    end},

    GetDescendants = { {}, function(self)
        local ReturnArray = {}

        for Child in next, RefChildren[self] do
            table_insert(ReturnArray, Child)

            for _, Descendant in next, Child:GetDescendants() do
                table_insert(ReturnArray, Descendant)
            end
        end

        return ReturnArray
    end},

    FindFirstChild = { {"string", "boolean?"}, function(self, name, recursive)
        local Children = RefChildren[self]

        for Child in next, Children do
            if Child.Name == name then
                return Child
            end
        end

        if recursive then
            for Child in next, Children do
                return Child:FindFirstChild(name, true)
            end
        end
    end},

    FindFirstAncestor = { {"string"}, function(self, name)
        local RefPointer = self.Parent
        while RefPointer do
            if RefPointer.Name == name then
                return RefPointer
            end
            RefPointer = RefPointer.Parent
        end
    end},

    WaitForChild = { {"string", "number?"}, function(self, name)
        return self:FindFirstChild(name)
    end},
}

local InstanceMethodProxies = {}
for MethodName, MethodObject in next, InstanceMethods do
    local Types = MethodObject[1]
    local Method = MethodObject[2]

    local EvaluatedTypeInfo = {}
    for ArgIndex, TypeInfo in next, Types do
        local ExpectedType, IsOptional = string_match(TypeInfo, "^([^%?]+)(%??)")
        EvaluatedTypeInfo[ArgIndex] = {ExpectedType, IsOptional}
    end

    InstanceMethodProxies[MethodName] = function(self, ...)
        if not RefChildren[self] then
            error("Expected ':' not '.' calling member function " .. MethodName, 2)
        end

        local Args = {...}
        for ArgIndex, TypeInfo in next, EvaluatedTypeInfo do
            local RealArg = Args[ArgIndex]
            local RealArgType = type(RealArg)
            local ExpectedType, IsOptional = TypeInfo[1], TypeInfo[2]

            if RealArg == nil and not IsOptional then
                error("Argument " .. RealArg .. " missing or nil", 3)
            end

            if ExpectedType ~= "any" and RealArgType ~= ExpectedType and not (RealArgType == "nil" and IsOptional) then
                error("Argument " .. ArgIndex .. " expects type \"" .. ExpectedType .. "\", got \"" .. RealArgType .. "\"", 2)
            end
        end

        return Method(self, ...)
    end
end

local function CreateRef(className, name, parent)
    local StringValue_Value

    local Children = setmetatable({}, {__mode = "k"})

    local function InvalidMember(member)
        error(member .. " is not a valid (virtual) member of " .. className .. " \"" .. name .. "\"", 3)
    end
    local function ReadOnlyProperty(property)
        error("Unable to assign (virtual) property " .. property .. ". Property is read only", 3)
    end

    local Ref = {}
    local RefMetatable = {}

    RefMetatable.__metatable = false

    RefMetatable.__index = function(_, index)
        if index == "ClassName" then
            return className
        elseif index == "Name" then
            return name
        elseif index == "Parent" then
            return parent
        elseif className == "StringValue" and index == "Value" then
            return StringValue_Value
        else
            local InstanceMethod = InstanceMethodProxies[index]

            if InstanceMethod then
                return InstanceMethod
            end
        end

        for Child in next, Children do
            if Child.Name == index then
                return Child
            end
        end

        InvalidMember(index)
    end

    RefMetatable.__newindex = function(_, index, value)
        if index == "ClassName" then
            ReadOnlyProperty(index)
        elseif index == "Name" then
            name = value
        elseif index == "Parent" then
            if value == Ref then
                return
            end

            if parent ~= nil then
                RefChildren[parent][Ref] = nil
            end

            parent = value

            if value ~= nil then
                RefChildren[value][Ref] = true
            end
        elseif className == "StringValue" and index == "Value" then
            StringValue_Value = value
        else
            InvalidMember(index)
        end
    end

    RefMetatable.__tostring = function()
        return name
    end

    setmetatable(Ref, RefMetatable)

    RefChildren[Ref] = Children

    if parent ~= nil then
        RefChildren[parent][Ref] = true
    end

    return Ref
end

local function CreateRefFromObject(object, parent)
    local RefId = object[1]
    local ClassNameId = object[2]
    local Properties = object[3]
    local Children = object[4]

    local ClassName = ClassNameIdBindings[ClassNameId]

    local Name = Properties and table_remove(Properties, 1) or ClassName

    local Ref = CreateRef(ClassName, Name, parent)
    RefBindings[RefId] = Ref

    if Properties then
        for PropertyName, PropertyValue in next, Properties do
            Ref[PropertyName] = PropertyValue
        end
    end

    if Children then
        for _, ChildObject in next, Children do
            CreateRefFromObject(ChildObject, Ref)
        end
    end

    return Ref
end

local RealObjectRoot = CreateRef("Folder", "[" .. EnvName .. "]")
for _, Object in next, ObjectTree do
    CreateRefFromObject(Object, RealObjectRoot)
end

for RefId, Closure in next, ClosureBindings do
    local Ref = RefBindings[RefId]

    ScriptClosures[Ref] = Closure
    ScriptClosureRefIds[Ref] = RefId

    local ClassName = Ref.ClassName
    if ClassName == "LocalScript" or ClassName == "Script" then
        table_insert(ScriptsToRun, Ref)
    end
end

local function LoadScript(scriptRef)
    local ScriptClassName = scriptRef.ClassName

    local StoredModuleValue = StoredModuleValues[scriptRef]
    if StoredModuleValue and ScriptClassName == "ModuleScript" then
        return unpack(StoredModuleValue)
    end

    local Closure = ScriptClosures[scriptRef]

    local function FormatError(originalErrorMessage)
        originalErrorMessage = tostring(originalErrorMessage)

        local VirtualFullName = scriptRef:GetFullName()

        local OriginalErrorLine, BaseErrorMessage = string_match(originalErrorMessage, "[^:]+:(%d+): (.+)")

        if not OriginalErrorLine or not LineOffsets then
            return VirtualFullName .. ":*: " .. (BaseErrorMessage or originalErrorMessage)
        end

        OriginalErrorLine = tonumber(OriginalErrorLine)
        local RefId = ScriptClosureRefIds[scriptRef]
        local LineOffset = LineOffsets[RefId]

        local RealErrorLine = OriginalErrorLine - LineOffset + 1
        if RealErrorLine < 0 then
            RealErrorLine = "?"
        end

        return VirtualFullName .. ":" .. RealErrorLine .. ": " .. BaseErrorMessage
    end

    if ScriptClassName == "LocalScript" or ScriptClassName == "Script" then
        local RunSuccess, ErrorMessage = pcall(Closure)
        if not RunSuccess then
            error(FormatError(ErrorMessage), 0)
        end
    else
        local PCallReturn = {pcall(Closure)}

        local RunSuccess = table_remove(PCallReturn, 1)
        if not RunSuccess then
            local ErrorMessage = table_remove(PCallReturn, 1)
            error(FormatError(ErrorMessage), 0)
        end

        StoredModuleValues[scriptRef] = PCallReturn
        return unpack(PCallReturn)
    end
end

function ImportGlobals(refId)
    local ScriptRef = RefBindings[refId]

    local function RealCall(f, ...)
        local PCallReturn = {pcall(f, ...)}

        local CallSuccess = table_remove(PCallReturn, 1)
        if not CallSuccess then
            error(PCallReturn[1], 3)
        end

        return unpack(PCallReturn)
    end

    local WaxShared = table.freeze and table.freeze(setmetatable({}, {
        __index = SharedEnvironment,
        __newindex = function(_, index, value)
            SharedEnvironment[index] = value
        end,
        __len = function()
            return #SharedEnvironment
        end,
        __iter = function()
            return next, SharedEnvironment
        end,
    })) or setmetatable({}, {
        __index = SharedEnvironment,
        __newindex = function(_, index, value)
            SharedEnvironment[index] = value
        end,
    })

    local function VirtualRequire(module)
        if module == wax then
            return WaxShared
        end

        local moduleRef = module

        if typeof and typeof(module) == "Instance" then
            moduleRef = module
        end

        if type(moduleRef) ~= "table" then
            error("Attempt to require an invalid module", 2)
        end

        local PathMatch = nil
        for RefId = 1, #RefBindings do
            if RefBindings[RefId] == moduleRef then
                PathMatch = RefId
                break
            end
        end

        if not PathMatch then
            error("Could not resolve require", 2)
        end

        local CurrentRefPointer = moduleRef

        -- [[Error text for the two guard branches below. Declared here rather
        -- [[than injected at generation time: the generator used to
        -- [[string-replace a "local ErrorNonModuleScript" declaration that the
        -- [[template never contained, so both names resolved to nil and the
        -- [[branches raised a blank message.]]
        local ErrorNonModuleScript = "Expected ModuleScript got " .. CurrentRefPointer.ClassName
        local ErrorSelfRequire = "Cannot require self"

        if CurrentRefPointer.ClassName ~= "ModuleScript" then
            error(ErrorNonModuleScript, 2)
        elseif CurrentRefPointer == ScriptRef then
            error(ErrorSelfRequire, 2)
        end

        return LoadScript(CurrentRefPointer)
    end

    return WaxShared, ScriptRef, VirtualRequire
end

for _, ScriptRef in next, ScriptsToRun do
    Defer(LoadScript, ScriptRef)
end

-- [[Standalone loader contract: return the public MainModule API to loadstring callers.]]
local AstraRoot = RealObjectRoot:FindFirstChild("Astra")
local MainModule = if AstraRoot then AstraRoot:FindFirstChild("MainModule") else nil
if MainModule then
    return LoadScript(MainModule)
end
error("Astra bundle is missing its MainModule", 0)
